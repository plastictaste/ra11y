[2026-05-27] | Published skill install instructions used the wrong Codex skill directory and a repo-only copy source | I assumed host-specific skill paths instead of verifying the actual discovery directory and the packaged artifact location | For every new skill install doc, verify the host's real discovery path and provide commands that work both from the source repo and from the packaged `node_modules` artifact.
