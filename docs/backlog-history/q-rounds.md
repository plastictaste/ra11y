# Closed Q-round backlog items

Archived from `.claude/backlog.md` to keep planner subagent context lean.
Open items remain in the live backlog; closed items keep their full bodies
here so cross-references from open items still resolve.

Format: original `- [x] **ID** body` shape preserved, grouped by section header.

---

## Track Q — Agent-consumer feedback


Owner: main session + general-purpose. Source: 10 independent agent runs against an external React codebase (2026-04-17 eval brief). All accepted items are MCP shape/honesty fixes — none touch detection logic. Dispatch in parallel; each touches a different surface.

- [x] **P0-E** Top-level `warnings: string[]` for silent-failure modes on `scan_project` / `scan` responses (31e040f). Codes wired: `scanned_zero_files`, `root_source_defaulted` (scan_project only), `no_config_found`, `tailwind_detected_css_undercounted`, `template_files_parsed_as_literal`. Conditional-spread; never `warnings: []`.
- [x] **P0-C** Populate `snippet` on every `review_candidates` and `checklist` candidate (9ba1c2b). ±3 lines, de-indented, 300-char cap, omitted (not `""`/`null`) when file:line is absent. Per-response source cache so each file is read at most once.
- [x] **P0-D** Widen `suggest_fix.oldText` to a unique anchor window (50b4514). Three-step ladder (opening-tag cluster → ±1 line → bracket expansion), 200-char cap, falls back to original narrow edit + `caveat` when no unique window fits. `apply_fix`'s existing literal-match path consumes the widened pair without changes. Bonus: extracted `buildSuggestFixPayload` to its own file to keep `tools.ts` under the 500-line budget.
- [x] **P1-F** AST-verify auto-detected native wrappers (c725cd0). One-hop probe: matches PascalCase basename to a `*.{tsx,jsx,ts,js}` file and inspects the first JSX element's tag name; native interactive root → `confirmed`, anything else → `assumed`. Only `confirmed` enter `activeNativeWrappers` and silence findings; `assumed` are surfaced under `bySource.fromAutoDetect.assumed` but findings on them are NOT silenced. No name-pattern matching, no transitive following.
- [x] **P1-J** Stable `findingId: string` on every Violation (49f9227). Truncated sha256 of (ruleId, relativeFilePath, lineContextHash); 12 hex chars. Line numbers NOT in the hash by design (drift resilience); ±3-line source context IS. Path normalization to relative-form so IDs match across machines. Baseline cross-run matching now uses findingId; legacy SHA1 helper kept as `fingerprintOf` for any straggling callers.
- [x] **P1-M** `plan.actionableManualItems` + `plan.untargetedCriteria` both top-level; summary leads with the actionable count (ab35222). Old composite `manualReviewRequired` removed.
- [x] **P1-H** `plan.mechanicalEditsAvailable` + `plan.guidanceFixesAvailable` both top-level (ab35222). Old composite `fixSuggestionAvailable` removed.
- [x] **P1-K** `nextStepStructured: { tool, args }` alongside prose `nextStep` (ab35222). Both omitted together when the next step is generic; aligned pair by construction.
- [x] **P1-L** Concrete `editCandidate` for `label-in-name` non-contiguous fixes (53ab36b). Synthesizes `aria-label="<visible text>: <remaining aria-label words>"` only when diagnosis is `isInterleavedExpansion`; case-insensitive dedupe of overlapping tokens. Field is `editCandidate` (NOT `edit`) so kind stays `guidance` — agent decides whether to apply.
- [x] **P2-V** `criteriaTitles: string[]` aligned index-for-index with `criteria` (70a9cd7). Standard-filter looks up titles from the criterion registry; scanner stamps both at violation construction; formatters and MCP `formatFinding` forward.
- [x] **P2-N** `plan.limitations` emitted on every scan response, not only clean ones. Prevents agents from overclaiming conformance on mixed-result responses.
- [x] **P2-P** `opaqueCustomComponents.names` inlined on every response when count ≤ 50 (c1f7e6b). Above 50 keep the current top-5 + `verboseMeta` pattern. Named constant `OPAQUE_COMPONENT_INLINE_NAMES_MAX = 50` with size-budget rationale.
- [x] **P2-R** `file` is canonical across suggest_fix + apply_fix; `filePath` accepted as deprecated alias (c2ea3b5). Using the alias fires `warnings: ["deprecated_param_filepath"]`; passing both raises a structured error.
- [x] **P0-F** Hard-error envelope on silent-failure paths (6a50836). `code: "cwd-not-found"` on `scan_project` with nonexistent `cwd`; `code: "scan-paths-not-found"` on `scan` when every requested path is missing. Discriminator: did the user point at something that exists? Yes → soft signal (warnings: ["scanned_zero_files"]); no → hard error envelope. Categorically different cases now have categorically different shapes.
- [x] **P1-CHG** `changedOnly: true` silently falls back to a full scan with no staged files (7824f81). Picked the hybrid: hard-error envelope (`code: "no_staged_files"`) for the in-repo case, honest fallback signal (`scanMode: "full-fallback"`, `fallbackReason`) for the not-in-a-git-repo case. Same lying-field bug exists in `tool-scan-diff.ts` per agent report — follow-up below.
- [x] **P1-CFG** Superseded by **P0-F** (6a50836). A bogus `cwd` now returns a structured `cwd-not-found` error envelope rather than a successful-looking empty scan, so the schema-presence-check failure mode no longer applies — the envelope shape is categorically different from the success shape, and agents branch on `isError: true` + `code`.
- [x] **P1-OVF** scan_project pagination (ab23bd5). `limit` (default 200, clamped [1, 2000]) + `offset` (default 0) input params; response carries `truncated: true` + `nextOffset` + `totalFilesWithFindings` when the cap truncates, else omits them. Offset-based (stable across rescans). Scan still runs over everything — only the emitted `files` array is capped; `plan.totalFindings` reports the full pre-truncation tally so page 1 doesn't mislead.
- [x] **P1-DUP** Dedupe `reviewPrompt` text in `review_candidates` (496b2e1). Top-level `prompts: { [criterionId]: { text } }` keyed map; candidates omit the per-item `reviewPrompt`. Field omitted when zero prompts apply. Lossless.
- [x] **P1-FILE-ENV** `scan_file` envelope parity (70a9cd7). Now carries `meta.configSource`, `activeNativeWrappers`, `rulesEvaluated`, `nextStep`, `opaqueCustomComponents`. Reused scan_project helpers rather than duplicating.
- [x] **P1-FILE-DEDUP** `scan_file` cross-standard dedup (70a9cd7). Same finding under multiple loaded standards collapses to one with `criteria: [...]`, matching scan_project. Shared dedup pass in `review-candidate-dedup.ts`.
- [x] **P1-ACCT** `analysisCoverage.opaqueCustomComponentsExcludedByAutoDetect` surfaces the subtraction count when auto-detect contributed (457635d). Agents computing "with-flag count + excluded = without-flag count" now see the scan-scope of the flag in the same field it affects.
- [x] **P1-IGN** Subpath scans now honor root `.gitignore` (389ba58). Walks up to git root, collects ancestor `.gitignore` files in order, translates patterns to scan-root-relative form, then enumerates from `cwd`. Per-scan memoization. Edge cases handled: `**/`-leading patterns, anchored-at-scan-root, anchored-outside-scan-root (dropped), FS-root termination. `!`-negation re-includes still unsupported (pre-existing glob.ts limitation, called out in docs).
- [x] **P1-CONF** Typed `confidence` on every manual-review candidate (3d258a3). `"high" | "medium" | "low"`, same enum as automated findings. Each finder picks its default; `priority: "high"` on checklist items stays (different axis).
- [x] **P2-BASE** `meta.baselineStatus: { exists, path, lastModified }` on scan_project when `.ra11y-baseline.json` exists at the scan root (457635d). Omitted when absent. Prevents re-proposing fixes for grandfathered violations without the baseline-check round-trip.
- [x] **P2-BUILD** `meta.scannedBuildArtifacts: [paths]` + `warnings: ["scanned_build_artifacts_present"]` when scanned set includes compiled-CSS / bundler-output (c2ea3b5). Findings on those files STILL surface — this is labeling, not filtering. Detection is deterministic (escape-bracket utility selectors, size threshold, /dist/ /build/ /static/assets/ path markers).

## Track Q2 — Agent-consumer feedback (round 3, 10-agent eval)


Owner: main session + general-purpose. Source: a 10-agent parallel eval against an external React/Vite/TS/Tailwind codebase — each agent exercised a different MCP slice in a "you-find, I-fix" workflow. Round 3 ran after Q (rounds 1-2) closed, on the post-Q shape. All accepted items are shape-honesty, batch-primitive, or workflow-gap fixes — none are heuristic suppression.

- [x] **Q2-PROSE** Top-level `referenceGuide.suppressPlacement: { [ext]: prose }` on scan / scan_file / scan_project / scan_diff responses; findings no longer carry `suppressPlacement` inline. Keyed by file extension (`tsx`, `css`, `html`, `htm`, `default`), populated only with extensions that actually have findings, omitted entirely when no findings exist (honest conditional-spread). `suppressWith` stays inline per finding — short, rule-ID-specific, ready-to-paste, not worth hoisting. apply_fix/baseline still use the old per-finding shape (small response bodies, no hoist benefit); documented as intentional scope. New module `src/mcp/reference-guide.ts` owns the builder + language mapping. `analysisCoverage.rulesByExtension` is already top-level — not actually per-finding prose, so nothing to hoist there.
- [x] **Q2-FIXKIND** Per-finding `fixClass` discriminator inline on every violation (bbbecf1 + 0da8a45). `mechanical | guidance | runtime-only | verify-in-source`. Stamped by the engine from required Rule.fixClass metadata; forwarded through MCP formatFinding. Distribution: 15 mechanical, 8 guidance, 2 runtime-only, 24 verify-in-source. ADR 0007. Named `fixClass` not `fix.kind` to avoid collision with existing `Violation.fix` auto-fixer shape and `suggest_fix.kind` 2-value axis.
- [x] **Q2-REASON** Bare `ra11y-disable` pragmas (no `: reason`) emit a review candidate under `suppression/no-reason` (d69175f). New `ra11y:` process-rule namespace with one criterion (`ra11y:suppression-no-reason`); engine activates `RA11Y_PROCESS_CRITERION_IDS` unconditionally so the finder fires regardless of enabled standards. Suppression semantics unchanged — pragma still honored; additive accountability signal.
- [x] **Q2-RULEDETAILS** `includeRuleDetails: "none" | "unique" | "all"` accepted on `scan` + `scan_project`; when set, response carries `ruleCatalog: { [ruleId]: { description, rationale, normativeQuote, goodExample, badExample, references } }` at the top level. `unique` is filtered to ruleIds that actually fired in the scan; `all` includes every loaded rule for first-contact caching. Default `none` keeps the baseline shape (field omitted entirely, not `{}`). New module `src/mcp/rule-catalog.ts` owns the builder, `collectFiredRuleIds`, the shared JSON-Schema fragment, and a `ruleCatalogField` conditional-spread helper so both tools share one code path. Mirrors the Q2-PROSE inline-once/reference-from-findings architecture.
- [x] **Q2-HUNK** `scan_diff` now accepts `hunksOnly: boolean` + `comparisonRef` (ff21fa5). Shells out to `git diff --unified=0 <ref>` via `spawnSync` with array args (zero-dep, no shell injection); parses hunk headers per file; filters findings to `(filePath, line)` inside a hunk. Baseline path is the default and untouched. Error envelope: `not-a-git-repo` + `unknown-ref`; soft signal `warnings: ["no_hunks_in_comparison"]` when both succeed but diff is empty. macOS symlink realpath normalization applied so `/var/` vs `/private/var/` doesn't break path comparisons.
- [x] **Q2-RESOLVED** `scan_diff` baseline mode now carries `resolved: ResolvedFinding[]` + `resolvedCount: number` (9d85885). `ResolvedFinding = { filePath, ruleId, message }` — mirrors the baseline entry minus the opaque hash. Both fields emitted even at zero in baseline mode (meaningful: "this scan fixed nothing"). In `hunksOnly: true` mode the fields are **omitted entirely** (not empty-array) — concept doesn't apply without a baseline. `nextStep` surfaces the resolved count when non-zero and nudges toward `baseline` mode: "update" to prune.
- [x] **Q2-GROUPKEY** `groupKey: string` stamped on every Violation (217d97c + ce8d6ac + 9184f6d + b979244). 12-hex-char truncated SHA-256 of `ruleId + "\0" + describeNodeShape(node)`. Helper `describeNodeShape` lives in `src/engine/ast-helpers.ts`; attribute NAMES and children-structure coarsened to `[text]`/`[expr]`/`[element]`/`[empty]`/`[mixed]` — values stripped, positions excluded. Stamped by engine (scanner + rule-runner), forwarded through MCP + JSON + agent + SARIF (`partialFingerprints.groupKey`) formatters. ADR 0008. Constant `GROUP_KEY_HEX_LENGTH = 12` mirrors `FINDING_ID_LENGTH` for symmetric read.
- [x] **Q2-CHECKLIST-LIMIT** Checklist pagination (df8946c + 73a545b). Inputs: `limit` (default 200, clamp [1,2000]), `offset` (default 0, ≥0), `maxCandidatesPerCriterion` (default 10, clamp [1,100]). `totalCandidates` always emitted. `truncated: true` + `nextOffset` conditionally spread when global `limit` clipped the flat stream; `perCriterionClipped: true` conditionally spread when any criterion hit the per-criterion cap. Orthogonal signals — either, both, or neither. Named constants; no magic numbers. Scan_project pagination helper extraction deferred as follow-up.
- [x] **Q2-SKIPCRIT** `skipCriterion: string[]` accepted on `checklist` + `scan_project`. Findings / checklist entries whose entire `criteria` set is contained in the skip list are dropped; entries tied to un-skipped criteria stay. Caller's filter of its own result — not tool suppression — surfaced as `summary.skippedByCaller` (checklist) and `meta.skippedByCaller` (scan_project). Shared schema + `skippedByCallerField` helper live in `src/mcp/skip-criterion.ts`. Filter applied in `runScanAndFormat` via new `applyCriterionSkip` pass so plan counts stay honest. Also consolidated `referenceGuideField` + `baselineStatusField` helpers into their owning modules to free file-budget headroom.
- [x] **Q2-LISTSUPP** `list_suppressions` MCP tool (d0a017c + 60df697). Input `{ cwd?, additionalPaths? }`; output `{ suppressions: [{ file, line, ruleId|null, criterionId|null, reason?, wildcard }], meta, nextStep }`. Does NOT run rules — enumerates pragmas only via the existing parser. Wildcard pragmas emit one entry with `ruleId: null, criterionId: null, wildcard: true`; multi-token pragmas emit one entry per token. `reason` field is conditionally spread (omitted for bare pragmas). Registered as tool #17.
- [x] **Q2-WRAPMAP** Config accepts `nativeWrappers` as `{ Button: "button", Link: "a" }` alongside the legacy `string[]` form (4e40f10). Loader normalizes to two honest LoadedConfig fields: `nativeWrappers: string[]` (names — every existing consumer keeps working unchanged) and `nativeWrapperElements: Record<string, string>` (new, populated only when the object form was supplied, empty `{}` otherwise). Tests cover all three input shapes (array, object, absent). Rule opt-in shipped as Q2-WRAPMAP-RULES (3f7a07f + 7c7e43b + 713506a); compound-component extension shipped as Q2R2-COMPOUND (e1ca368). MCP tool wiring of `nativeWrapperElements` is the last remaining wedge — promoted as its own item below.
- [x] **Q2-WRAPMAP-MCP** Thread `LoadedConfig.nativeWrapperElements` through the MCP handlers so `configure` / `sessionConfigure` / `scan` / `scan_project` surface the element mapping alongside the flat names list (92dc714). `meta.activeNativeWrapperElements` emitted when map non-empty, omitted otherwise; `sessionConfigure` accepts both flat-array and object forms via `oneOf`. New `src/mcp/scan-assembly.ts` extracted plan/meta assemblers to stay under the line budget.
- [x] **Q2-WRAPGLOB** Glob patterns now accepted on `nativeWrappers` — `*Button`, `Icon*`, `*Card*`, `Icon*Button`. `*` expands to `[A-Za-z0-9]*` so globs stay inside identifier boundaries (no cross-dash or cross-whitespace match). Centralized in `src/mcp/wrapper-matcher.ts` (`matchesWrapperPattern`, `nameMatchesAnyWrapper`, `wrapperPatternToTagRegexSource`) and threaded through the three consumer sites: `dropWrapperNoise` (silence findings), `collectUsedWrappers` (unusedWrappers detection via AST walk), `findWrappersInExcludedSources` (text-search widening into stories/dev-tools). `unusedNativeWrappers` now tracks which PATTERNS matched — `*Button` counts as "used" when any variant appears; `Ghost*` surfaces as unused when nothing matches. No `?` / `[abc]` / `!` — just `*`, by design.
- [x] **Q2-WRAPMAP-RULES** `Rule.wrapperTreatsAsElement?: string` opt-in (3f7a07f + 7c7e43b). Context builder filters `LoadedConfig.nativeWrapperElements` against the rule's declared target tag and exposes `ctx.wrappersForElement: ReadonlySet<string>`. Rules that don't set the field see an empty set — identical to pre-Q2-WRAPMAP-RULES. Wired on 3 rules: `navigation/link-descriptive-text` (`"a"`), `media/alt-text-missing` (`"img"`), `forms/labels-required` (`"input"`). `forms/labels-required` also teaches implicit-label detection to accept mapped wrappers. CLI scan threads `fileConfig.nativeWrapperElements` into `runScan`. MCP tool wiring deferred — task guardrails excluded `src/mcp/` during concurrent work.
- [x] **Q2-WRAPPATH** `detect_native_wrappers` candidates now carry `definitionFile: string | null` (6930fce). One-hop basename probe (matches `ComponentName` against `{ComponentName}.{tsx,jsx,ts,js}`) via the existing `indexFilesByComponentName` helper P1-F already uses — no import-graph traversal, no transitive barrel-following. Barrels are invisible to the probe because the basename still matches the defining file. `null` is emitted (not omitted) when the probe fails — the attempt is meaningful.
- [x] **Q2-SNIPPET-KIND** Reason-driven snippet widening (bd94a21 + b0e26cc + 11a9472). `CROSS_LINE_REASON_PATTERNS` matches cues like "defined outside this line" / "referenced function"; matched candidates get a wide snippet instead of the ±3-line default. For TSX/JSX/TS/JS, `findEnclosingBlock` does a regex-free brace-balance walk (string/comment/template-expression aware via a per-state machine) to bound the enclosing function/block; over-cap blocks honestly fall back to the 10-line window rather than silently truncating. HTML/CSS keep the 10-line fallback. Snippet remains a plain string — no shape change.
- [x] **Q2-SESSIONCFG** Canonical tool name is now `sessionConfigure`; `configure` is a dispatch-only backward-compat alias not listed in `tools/list`. Calls via the legacy name still land on the same handler but get `warnings: ["deprecated_tool_name_configure"]` layered onto the response so agents can migrate on their own schedule. Description reframed to lead with the ephemeral session-only semantics and point at `ra11y.config.ts` for durable committed config. New `src/mcp/deprecation-warning.ts` owns the warning-layering helper (will be reusable when we rename other tools). Pattern mirrors P2-R's `file`/`filePath` alias.
- [x] **Q2-PRUNE** `ra11y baseline prune [--dry-run]` subcommand (ebbe753). Pure `pruneBaseline(entries, fileExists)` helper in `src/engine/baseline.ts`; CLI wrapper in `src/cli/commands/baseline.ts`. Reuses the existing `writeBaseline` serializer so metadata and retained-entry order round-trip. Missing/malformed baseline → exit 2 with stderr pointer to `ra11y baseline create`. No new exit codes.
- [x] **Q2-VERIFYCMD** `verifyCommand` + `verifyCommandStructured` on every `suggest_fix` response (100a0fa). Prose + structured pair, always populated (no conditional-spread — no suggest_fix response is meaningful without a re-verify). Structured form: `{ tool: "scan_file", args: { file, ruleId? } }`. Chose `scan_file` (narrowest deterministic verify) over `scan_project`; `ruleId` is advisory so the agent can post-filter the re-scan output.
- [x] **Q2-SARIF-DOCS** SARIF output + exit codes documented (d987876). `docs/cli.md` gets a `## SARIF output` section with field-mapping table sourced from `src/output/formatters/sarif.ts`; `docs/ci.md` is new — exit-code table, `--fail-on` reference, GitHub Actions (blocking + Security-tab upload via `github/codeql-action/upload-sarif@v3`), GitLab CI with artifact export, baseline adoption in CI.
- [x] **Q2R2-DIR-NEXT** `scan` (directory mode) now emits `nextStep` + `nextStepStructured` at parity with `scan_project` and `scan_file`. Uses the shared `buildNextStep` helper; `iterativeTip` left empty (scan has no scope-dimension to nudge about, unlike scan_project full vs changed mode). Empty-files branch still skips the pair — same as scan_project — because `warnings: ["scanned_zero_files"]` carries the "nothing scanned" signal and a `checklist` suggestion would be dishonest when no files were actually scanned. [round 2, agent 5]
- [x] **Q2R2-FIX-DEDUPE** When a finding's inline `fix` (now `fixClass`-typed, bbbecf1) already carries primary + alternatives + context, drop the `suggest_fix` suggestion from `nextStep`/`nextStepStructured`. Pairs with Q2-VERIFYCMD — the verify step still applies, but the "now call suggest_fix" handoff is redundant. Saves ~600 tokens per finding in tight fix loops. [round 2, agent 1] — 14e3d9a
- [x] **Q2R2-SCANNED-KEY** Canonical `scanned: { mode: "project" | "dir" | "file", root?, paths?, file? }` envelope now flows through scan_project / scan / scan_file / scan_diff / detect_native_wrappers / propose_config / propose_baseline / bootstrap / wrapper_introspect — nine MCP tools in total (the original 7 plus tool-bootstrap + tool-wrapper-introspect, which landed after the stash and shared the same `scannedRoot` smell). Stash@{14} (`unrelated-mcp-wip`) was applied, conflicts with the later `applyMetaCacheMode` refactor resolved by porting the stash intent onto HEAD rather than `--theirs`-ing the refactor away. Tests rewritten across tests/integration/mcp-{tools,roots,meta-cache}.test.ts + tests/unit/mcp/{tool-bootstrap,tool-propose-baseline,tool-propose-baseline-invariants,tool-propose-config,tool-wrapper-introspect,tools}.test.ts. CHANGELOG Breaking entry added. Full verify green. [round 2, agent 5]
- [x] **Q2R2-UNTARGETED-NAME** One canonical field name for the untargeted-criteria count (today: `plan.untargetedCriteria` on scan_project, `untargeted` on checklist, `manualUntargetedCount` on coverage, top-level `untargetedCriteria` on scan_file — four names, same concept). Same canonicalization pattern as P2-R (file/filePath). [round 2, agent 9]
- [x] **Q2R2-WRAPPER-SOURCES** Collapse `activeNativeWrappers` + `activeNativeWrappersBySource` + `sessionNativeWrappers` into one tagged list: `activeNativeWrappers: [{name, source: "config"|"autoDetect"|"session", confirmed?}]`. `confirmed` folds in P1-F. [round 2, agent 9]
- [x] **Q2R2-COVERAGE-CHECKLIST** ADR 0010 landed (fe962f6) + execution (3093b67, 27e12d0). `checklist.summary.automatedCoverage` trimmed to `{ standardId, automatedCriteriaPassRate }`; `criteriaAutomatable` + `criteriaAutomatablePassing` dropped. `coverage` cross-points to `checklist` when `manualWithCandidates` non-empty, to `scan_project` when only failing-automated remain, omitted when clean. `checklist` cross-points to `coverage` when `actionable === 0`, to `checklist` (paging) when truncated, omitted otherwise. Both tools conditional-spread `nextStep`+`nextStepStructured` as one unit. 4 new integration tests cover scalar parity + args round-trip + aligned-pair discipline. Merge with verbosity knob still deferred to v1.0.0 behind a deprecation alias path. [round 2, agent 7]
- [x] **Q2R2-META-CACHE** Meta-cache skeleton + wiring landed across scan / scan_file / scan_project / scan_diff (14c35a4 + 608cda7 + a2d7b57). Input flag is `metaMode: "full" | "delta"`, default `"full"` so legacy callers see zero shape change. Under `metaMode: "delta"`, first call returns full meta stamped with `sessionRef: "<tool>-<8hex>"`; repeat call with same signature returns `{ sessionRef, delta: {...}, removedFields? }` (removedFields omitted when empty, honest conditional-spread). Signature hash covers tool name + cwd + paths + standards/level + additionalPaths; excludes `metaMode` itself + pagination so a paged walk stays on one ref. Unit + integration tests committed (2ae7baa + c14e137). Extension shipped across checklist / coverage / list_suppressions — three out of the four follow-up tools wired; list_rules deferred because its response is pure enumeration data (no scan-confidence telemetry, no cwd/filesScanned signal to cache) and the meta block would be trivial. For checklist + coverage the `meta` block is opt-in per `metaMode: "delta"` so legacy callers see zero shape change (neither tool had `meta` historically); list_suppressions' existing meta gets wrapped by `applyMetaCacheMode` directly. [round 2, agent 1]
- [x] **Q2R2-WRAPPER-INTROSPECT** ADR 0012 (e47ec5d) + execution (81707b1, 5bf4c00, a16a3a9). `wrapper_introspect` MCP tool returns per-wrapper records with `observedRoot ∈ {button|a|input|div|opaque|unknown}` + `confidence ∈ {confirmed|assumed|unresolved}`; textarea/select/input collapse onto `"input"` to match the ADR's DOM bucket set. Per-file-hash cache keyed via `WeakMap<McpSession>`; natural invalidation on content change. One-hop basename probe only (no transitive import following). `nextStep` routes to `propose_config` when all records are confirmed, else `detect_native_wrappers`. 11 tests. Per ADR, introspection is an audit/discovery signal — never silently rewrites `LoadedConfig.nativeWrappers`. [round 2, agent 2]
- [x] **Q2R2-DRIFT** New rule `wrapper/drift` — component declared in `nativeWrappers` (or auto-detected) whose definition no longer renders the expected native element. Fires at the DEFINITION file. Closes the "fixing a wrapper is silently safe; breaking it is silently catastrophic" asymmetry — the breakage surfaces the moment the wrapper is edited. [round 2, agent 2]
- [x] **Q2R2-INHERITED** ADR 0014 `docs/adr/0014-inherited-findings.md` (a271077) + implementation `src/engine/inherited-findings.ts` + `tests/unit/engine/inherited-findings.test.ts` (landed earlier; ADR now documents the decisions the impl already encodes). Cap decision: **all call sites, no cap** — per CLAUDE.md §1 "Surface, don't suppress" + "Labeled buckets are suppression too"; agents dedup via groupKey, suppress via source pragma. Rollout: **always on, no flag** — the signal is not something an agent can opt into discovering. `confidence: "inherited"` extends the existing enum (single axis); fresh findingId/groupKey per call site so each is an independent baseline/attestation target. Interaction with polymorphic-dispatch + baseline + attestations (ADR 0013) documented in §Consequences. [round 2, agent 2]
- [x] **Q2R2-POLYMORPHIC** Resolve literal `as` / `asChild` prop values and re-dispatch rules against the resolved element. `<Button as="a" href=...>` re-runs `link-no-href`, `link-descriptive-text`. Only literals — dynamic `as={Something}` stays unresolved (honest — agent reads and decides). Engine surface shipped (0b130dd: `ctx.resolvePolymorphic`, link-descriptive-text opted in); follow-up opt-ins landed (b07455b: alt-text-missing, labels-required, semantics/button-name). [round 2, agent 2]
- [x] **Q2R2-COMPOUND** Typed compound-component mapping in config: `nativeWrappers: { Card: { Header: "div", Body: "div" }, Composer: { SendButton: "button" } }`. Extends Q2-WRAPMAP object form with nested dotted-path keys. [round 2, agent 2]
- [x] **Q2R2-RULE-COV** Per-rule coverage confidence on scan responses: `{ ruleId, filesEvaluated, filesEligible, coverageConfidence: "high"|"low", reason?, remediation? }`. Derivative split: `confidentlyClean: [ruleIds]` vs `lowConfidenceClean: [ruleIds]`. Tailwind-pre-build is the acute case (`contrast/minimum` runs against 2 CSS files, 0 findings ≠ clean). Distinct from Q2-RULEDETAILS (rule *catalog* inline) — this is per-rule *trust* per scan. [round 2, agent 6]
- [x] **Q2R2-CWBB** Shape + forwarders landed (2df787a + ADR 0009, 3e63715 for json/sarif). Optional `readonly couldBeWrongBecause?: readonly string[]` on `Violation`; wired through MCP `formatFinding`, `agent` formatter, `json` (passthrough), and SARIF (`Result.properties.couldBeWrongBecause`) with conditional-spread so empty/absent reads identically to consumers. Text-contrast opt-in shipped (02b8bf8): `contrast/minimum` + `contrast/enhanced` emit `tailwind_class_on_consumer` when a `text-*`/`bg-*` utility on the consumer would override the failing declaration; rules promoted to project scope; helpers in `src/rules/contrast/_shared.ts`. Non-text contrast opt-in shipped: `contrast/non-text` emits `tailwind_class_on_consumer` when a `border-*` / `outline-*` / `ring-*` utility on the consumer would override the failing declaration (boundary-vs-background axis — distinct family set from the text-contrast opt-in, same reason code); rule promoted to project scope; family sets exposed as `TEXT_CONTRAST_OVERRIDE_FAMILIES` / `NON_TEXT_CONTRAST_OVERRIDE_FAMILIES` in `_shared.ts`; `divide-*` intentionally excluded (applies between children, not the element itself). **Deferred** (rejected by the implementing agent as a fuzzy signal): `forms/required-indicator-missing` → `replacement_indicator_in_sibling_file` — "required"/"Required" tokens fire on nearly every form-heavy codebase (react-hook-form, zod, formik, test assertions); needs a tighter signal before it earns the opt-in. `estimatedFpRate` numeric remains rejected per §1 "Numeric-threshold heuristics are suppression." [round 2, agent 8]
- [x] **Q2R2-STORYBOOK-PRESET** MVP shipped (6dfa231 + aee3134). `Config.preset: "storybook"` accepted + validated; `isStorybookStoryFile` matches `*.stories.{tsx,jsx,ts,js}` + `*.story.*`; `DiscoverOptions.includeStoryFiles` drops only story-file patterns when the preset fires (tests/mocks still excluded); Storybook primitives (`Meta`, `StoryObj`, `StoryFn`, `Story`) exempt from opaque-component classification inside story files only (product `<Meta/>` stays opaque); `warnings: ["storybook_preset_active"]` emitted when preset engages. Args-binding follow-up landed (0bb8842 + 0fa523d + 55ba1f7 + 2a95a48 + 6c878dd): `parseTsx(source, { filePath })` runs a Storybook synthesis pass that appends a virtual `<ComponentName attr=... />` JsxElement for each `StoryObj` / `StoryFn` / `Story` / `Meta`-typed declarator with literal `args`, so rules that need to evaluate the rendered shape (alt-text-missing, button-name, labels-required, etc.) get a chance to fire on story files. Each synthesized element carries `synthesized: { source: "storybook-args", storyName }` for honest provenance. Spreads, callbacks, identifier-reference values, dynamic templates, and unresolvable components all skip synthesis (no half-known elements). [round 2, agent 4]
- [x] **Q2R2-FORM-REQ** New rule `forms/required-indicator-missing` — component forwards `required` to a native input but renders no visible marker and no `aria-required`. Cites wcag22:3.3.2. Fires at the wrapper DEFINITION. [round 2, agent 3]
- [x] **Q2R2-FORM-TIE** Review finder `forms/server-error-untied` under wcag22:3.3.1. `<p role="alert">` / `<div aria-live>` sibling of a field where the field has no `aria-invalid` / `aria-describedby` pointing at the error's id. Finder, not rule — cross-element proof requires inspection agents do better. [round 2, agent 3]
- [x] **Q2R2-FORM-TIMING** Review finder `forms/validation-timing` under wcag22:3.3.3 / 3.3.4. Surfaces `onChange` validation handlers that fire per-keystroke. Reason-text enrichment only — agent decides whether `onBlur` is appropriate; no auto-downgrade. [round 2, agent 3]
- [x] **Q2R2-BOOTSTRAP** `bootstrap` MCP tool — composes `detect_native_wrappers` + `propose_config` + `scan_project` + `baseline create` + returns a CI snippet in one call. Interrogation note (§1 "Interrogate the problem"): is this a docs gap or a genuine composition primitive? Round-2 signal from two independent agents suggests the latter; build after Q2R2-PROPOSE-CFG lands. [round 2, agents 7, 10]
- [x] **Q2R2-SUPPRESS-TOOL** `suppress` MCP tool — input `{ file, line, ruleId, reason }`. Inserts the appropriate source pragma (block-comment for HTML, JSX-comment for TSX). Required reason (rejects bare — mirrors Q2-REASON source-rule). [round 2, agent 7]
- [x] **Q2R2-CFG-SNIPPET** `detect_native_wrappers` response adds `suggestedConfigSnippet: string` as a structured field. Today it's buried in `nextStep`'s English; agents parse prose to extract config. [round 2, agent 9]
- [x] **Q2R2-PROPOSE-CFG** `propose_config` MCP tool — synthesizes `ra11y.config.ts` from scan state (wrappers + exclude + commented rules stub). Deterministic; no LLM. [round 2, agent 10]
- [x] **Q2R2-PROPOSE-BASE** `propose_baseline` MCP tool — categorizes each would-be entry with a machine-readable reason code (`wrapper-undetected` / `third-party-html` / `legacy-route` / `design-system-internal`). Reviewers triage by category. [round 2, agent 10]
- [x] **Q2R2-INTENTIONAL** `@ra11y-intentional` JSDoc tag on a component declaration — intentional-failure-demo marker (Storybook "bad example" pattern). Functionally a file-scoped `ra11y-disable *` pragma but syntactically part of docs markup. Required reason. [round 2, agent 4]
- [x] **Q2R2-SCAN-MIXED-DOC** Document that `scan` accepts `paths: string[]` of mixed files + dirs. The primitive exists; it's non-obvious. One-line in `tools/list` description + a `docs/mcp/tools.md` note. [round 2, agent 5]

## Track Q3 — Design-system field test


Owner: main session + general-purpose. Source: 10-agent parallel eval against a design-system docs repo. Slices: components MDX (×4 agents), forms MDX, layout/utilities/helpers/content MDX, getting-started/about/customize/extend MDX, Astro pages/layouts/components, raw HTML test fixtures under `js/tests/`, and an MCP surface critique. All findings triaged against CLAUDE.md §1 and the AI-first-consumer doctrine.

A design-system doc-site tree is a useful stress-test because (a) it's the shape ra11y agents see most often; (b) it has genuine a11y content (accessibility.mdx, prose-embedded ARIA patterns); (c) it ships MDX + Astro + raw HTML + SCSS in one tree — four file classes at once.

**Parser-lifecycle discovery (process issue, not code gap):** `dist/cli.js` was rebuilt at 22:30 but the MCP subprocesses were started at 22:13 and kept running the pre-rebuild bundle. All 10 agents saw `.mdx` / `.astro` return `file-unsupported` and `filesAdded: 0` — a behavior that evaporates once the host re-spawns the MCP subprocess against the fresh bundle. The parser code itself (`src/input/parsers/{mdx,astro,scss}.ts` + `src/mcp/session.ts:440-457` dispatch + `src/utils/path.ts` allow-list) is correct. Items below are the findings that remain valid AFTER the MCP is restarted — the stale-process symptom is folded into a single lifecycle item.

- [x] **Q3-MCP-RESTART-HINT** New `src/mcp/stale-subprocess.ts` records the bundle file's mtime at `startMcpServer` and re-stats on every `tools/call` dispatch. When on-disk mtime is strictly greater than the recorded baseline, `annotateStaleSubprocess` merges `stale_mcp_subprocess` into the response's `warnings[]` (deduped, prepended so it's first) and adds a sibling `staleSubprocessHint` prose field ("rebuild detected; reconnect the MCP to pick up parser and rule changes"). Leaves `isError` and non-JSON payloads untouched. 13 unit tests in `tests/unit/mcp/stale-subprocess.test.ts` cover record-idempotency, merge-with-existing-warnings, dedup, non-string filter, non-object payload bypass, error-envelope bypass, empty-content, and trailing-content preservation.
- [x] **Q3-SCAN-FILE-ERROR-EXT-DRIFT** (d44363f) `src/utils/path.ts` now exports `PARSEABLE_EXTENSIONS` + `parseableExtensions()`; `src/mcp/tools.ts` derives the `scan_file` `file-unsupported` remediation from the registry via a module-level `SCAN_FILE_UNSUPPORTED_REMEDIATION` constant, so `.mdx` / `.astro` / `.scss` are no longer invisibly omitted and the message stays in lockstep as the parser registry grows. Unit test asserts every current extension is present.
- [x] **Q3-ADDITIONAL-PATHS-SKIP-REASON** (aa23b13) `scan_project.additionalPathsScanned.skipped: [{ path, reason }]` now surfaces per-path skip reasons — `"not-found"` / `"unsupported-extension"` / `"excluded-by-glob"`. New `src/mcp/additional-paths-classifier.ts` owns `classifyAdditionalPathSkips` + `additionalPathsScannedField`; directory-exists-with-zero-parseable-files deliberately NOT a fourth reason (response-level `extensions_skipped_no_parser` warning already carries that signal). `skipped` conditional-spread (omitted when every additional path contributed files). Four tests cover all three reasons + omission.
- [x] **Q3-GUIDANCE-FIELD-NAME** Superseded by **V1-COMP-PLAN-GUIDANCE-FIXES** (82f41e5). The misnamed `plan.guidanceFixesAvailable` composite was replaced with structured `plan.fixesByClass: { mechanical, guidance, runtimeOnly, verifyInSource }` — each lane counts one kind of thing per the doctrine. `mechanicalEditsAvailable` retained as an orthogonal "actionable inline edit" counter. No rename path for the old misnomer — agents read the structured lane directly.
- [x] **Q3-PLAN-SUMMARY-PROSE** Superseded by **V1-SHAPE-FIXCLASS-HEADLINE** (33a1978) + **V1-SHAPE-CLI-AGENT-HEADLINE** (331a805). The violations parenthetical now breaks down by `fixClass` lane (`mechanical` / `guidance` / `runtime-only` / `verify-in-source`) rather than summing prose-only items under a single "guidance fixes" label; zero-count lanes omitted. Both the MCP `plan.summary` and CLI `--format agent` summary route through the shared `buildFixClassBreakdown` helper. No separate rename required — the composite is gone from both surfaces.
- [x] **Q3-FIX-PAYLOAD-EMPTY** (1699abe) Investigation: the `fix: { safety: "safe" }` alone shape the field report described CANNOT occur on current main. `buildFix` in `src/output/agent-response/build-finding.ts` always populates `description` in both emitting branches (mechanical: `v.suggestion ?? v.fixPaths.primary.label` — label is required on FixPath; guidance: requires non-empty suggestion). `rewriteFinding` in `src/mcp/reference-guide.ts` atomically strips `description` + adds `fixDescriptionRef: { hash }`. Previously closed by **075b64c** (atomic hoist) + **6b6349c** (null-sentinel sweep) + **2a26c25** (hoist invariants) + **ea6dd5c** (snippet collapse). Added regression tests: `AgentFix-shape-is-honest` helper in `tests/unit/output/agent-response/build-finding.test.ts` covers every buildFix branch + mechanical-without-suggestion + guidance-only + empty-suggestion + direct-construction corner case; parallel invariant in `tests/unit/mcp/reference-guide-hoist.test.ts` covers the post-hoist pipeline.
- [x] **Q3-DEFAULT-LIMIT-BUDGET** Superseded by **V1-SIZE-RESPONSE-BUDGET-DENSITY-2** (66a9147 + 9c73266 + 538e392). ADR 0021 amended with the token-density secondary budget; `src/mcp/token-budget.ts` exposes `applyTokenBudget` + `DEFAULT_TOKEN_BUDGET_CHARS = 88000` (~22000 tokens, ~3k headroom under the 25k ceiling). Wired through `scan` / `scan_project` / `scan_diff`. Post-merge measurement on a representative scan: 25k MCP ceiling held with ~3.5k-token envelope headroom; `truncated: true`, `nextOffset` advances on `scan_project`, `response_token_budget_truncated` warning fires. Secondary token-density cap kicks in before the file-count cap when needed.
- [x] **Q3-CHECKLIST-NEXTSTEP** (050cf88) `buildChecklistNextStep` now emits the pair in the previously-empty actionable-items-no-truncation branch. Structured hint routes to `scan_project { cwd }` (closed-form, directly callable); prose names `attest` as the verdict-recording step. `attest` is kept out of the structured form because its required `reason` + `evidenceSource` come from the agent's per-item investigation — pre-seeding would fabricate provenance. Pair-invariant (both fields together or neither) preserved across all three branches.
- [x] **Q3-BOOTSTRAP-NEXTSTEP-SELFLOOP** (573fcec) `buildNextStepStructured` in `tool-bootstrap.ts` no longer self-loops. When violations exist and baseline has not been written: route to `detect_native_wrappers({ cwd: root })` if the detect leg surfaced PascalCase+onClick candidates, else `scan_project({})`. baseline-written branch still emits `baseline check`; clean branch still emits `scan_project`. Prose untouched — still names `writeBaseline: true` as the grandfather alternative (legitimate English-tier guidance, not a structured self-loop).
- [x] **Q3-PROPOSE-CONFIG-FIELD-DRIFT** (275d4bb) `bootstrap` response now emits `suggestedConfig` as the canonical key alongside the legacy `proposedConfig` for one release (both populated together on a successful propose leg; both absent together when the leg degrades). Canonical name matches `propose_config.suggestedConfig` + `detect_native_wrappers.suggestedConfigSnippet` — two of three surfaces already used `suggested` prefix. Output-side alias (no `warnings` code — no input call site to warn about); transition plan documented in CHANGELOG `[Unreleased]` → `### Deprecated`.
- [x] **Q3-DETECT-WRAPPERS-EMPTY-REASON** (808940e) `detect_native_wrappers` now stamps `emptyReason: "no-parseable-files"` on the `files.length === 0` early return and `emptyReason: "no-pascalcase-onclick-components"` when `collectWrapperCandidates` returned empty on a non-empty file set; omitted via conditional-spread when candidates are populated. Chose one combined token for the zero-candidate case — `collectWrapperCandidates` checks PascalCase AND onClick together, so splitting would require reshaping the collector. Tool description advertises the field; three tests cover both variants + omission.
- [x] **Q3-RULE-NESTED-INTERACTIVE-ROLE-WRAPPER** `src/rules/semantics/nested-interactive.ts` over-fires on role-only wrappers. Fires on `<a href>` inside `<div role="tab">` even though the outer div is not materially interactive (no handler, no tabindex, not a native interactive tag). 7 false positives across `js/tests/visual/{collapse,modal}.html`. Tighten: require the outer element to be (a) a native interactive tag, (b) have `tabindex >= 0`, or (c) have an actual click/key handler — role alone should not qualify.
- [x] **Q3-RULE-LINK-HREF-HASH** (3d5dbc0) `src/rules/navigation/link-no-href.ts` now fires on `<a href="">` and `<a href="#">` identically to missing href (whitespace trimmed first). Fragment navigation `<a href="#non-empty-id">` stays silent. `satisfies` + WCAG citation unchanged — same 1.3.1 / 4.1.2 criteria. `docs.description` widened to "missing, empty, or placeholder (#) href." Three new fixtures (empty-href.html, hash-only-href.html, fragment-nav.html) + ≥3 positive + ≥2 negative unit tests across HTML + JSX.
- [x] **Q3-RULE-EXPANDED-DISCLOSURE-RANGE** `src/rules/aria/expanded-on-disclosure.ts` never fires on the canonical disclosure pattern — `<button aria-expanded="false" data-bs-toggle="dropdown">` without `aria-controls`. Verify rule coverage matches the spec; this pattern matches WAI-ARIA Authoring Practices §disclosure exactly.
- [x] **Q3-RULE-KEYBOARD-HANDLER-ATTR** (77c0378) `keyboard/handler-missing` widened to flag `data-bs-toggle`, `data-bs-dismiss`, `data-bs-ride`, `data-bs-target` on non-interactive elements (div, span, p, section, etc.) and inline `onclick="..."` attributes. 8 new HTML fixtures (bad + good), expanded unit tests. `satisfies` kept at wcag22:2.1.1 only (2.1.3 is AAA+manual per agent judgment). KB drift check passed without regen — rule-kb entry updated in-place.
- [x] **Q3-RULE-PAUSE-STOP-INLINE-STYLE** (288c039) `src/rules/motion/pause-stop-hide.ts` only scans `.css` files, not inline `<style>` blocks or `style=` attributes. Carousel fixtures use inline `transition-duration` + `data-bs-ride="carousel"` for auto-advance — zero findings today. Widen to inline-style scanning; `data-bs-ride="carousel"` itself is a static signal for WCAG 2.2.2.
- [x] **Q3-RULE-TABLE-CAPTION** New `semantics/table-caption-missing`. `<table>` without `<caption>` or `aria-label`/`aria-labelledby`. WCAG 1.3.1 (A). No table-family rule exists today; `semantics/table-headers` covers `<th scope>` but not the wrapper-level label.
- [x] **Q3-RULE-TABLE-TH-SCOPE** (77db3b5) New `semantics/table-th-scope-missing`. `<th>` in multi-row/multi-col tables without `scope="col"`/`"row"`. WCAG 1.3.1 (A).
- [x] **Q3-RULE-IFRAME-TITLE** New `media/iframe-title-missing`. `<iframe>` without `title` or with generic titles (`"YouTube video"`, `"iframe"`). WCAG 4.1.2 + 2.4.1. No `iframe/` folder today.
- [x] **Q3-RULE-PROGRESSBAR-VALUE-RANGE** New `aria/progressbar-value-range`. Validate `role="progressbar"` has coherent `aria-valuenow` / `aria-valuemin` / `aria-valuemax` and `valuemin ≤ valuenow ≤ valuemax`. WCAG 4.1.2 (A). (b0cefd5)
- [x] **Q3-RULE-LABELLEDBY-TARGET** New `aria/labelledby-target-exists` (mirror of `forms/label-for-id-mismatch` for ARIA). `aria-labelledby` / `aria-describedby` / `aria-controls` must reference existing element IDs in the same document. WCAG 4.1.2. (b39f502)
- [x] **Q3-RULE-SKIPLINK-TARGET** Widen `src/rules/navigation/skip-link.ts` or add `navigation/skip-link-target-valid`. `<a href="#content">Skip to main content</a>` must resolve to an element with `id="content"`. WCAG 2.4.1. Docs routinely document `href="#content"` as the canonical pattern while the rendered BaseLayout omits that id on most routes. (fb66578)
- [x] **Q3-RULE-STRETCHED-LINK-TOPOLOGY** New `pointer/stretched-link-multiple-in-container`. Two or more `.stretched-link` (or equivalent full-card-overlay links) in the same positioned ancestor. WCAG 2.4.4. (a4834e8)
- [x] **Q3-RULE-HEADING-CLASS-NONHEADING** (9da0ff1) New `semantics/heading-class-on-nonheading`. `<p class="h1">` / `<span class="h2">` / `<div class="display-1">` — visual heading, non-heading element. WCAG 1.3.1.
- [x] **Q3-RULE-COLOR-SOLE-INDICATOR** New `color/meaning-by-color-only`. `.text-danger` / `.text-success` / `.text-warning` as the only signal for status with no icon sibling, no SR-only label, no prose prefix. WCAG 1.4.1. (bc76f1d)
- [x] **Q3-RULE-ARIA-INVALID-MISSING** (8903e67) New `forms/aria-invalid-missing`. Field carries `.is-invalid` / `:invalid` styling to convey error but never sets `aria-invalid="true"` — color-only error feedback. WCAG 1.4.1 + 4.1.3.
- [x] **Q3-RULE-ERROR-MESSAGE-ASSOC** New `forms/error-message-not-associated`. `.invalid-feedback` / `[role="alert"]` sibling of an input where the input has no `aria-describedby` pointing at the error's `id`. WCAG 3.3.1 + 3.3.3. (3f2c160+04f0259)
- [x] **Q3-RULE-PLACEHOLDER-AS-LABEL** New `forms/placeholder-as-label`. Input has `placeholder` but no visible label, `aria-label`, `aria-labelledby`, or wrapping `<label>` — or the placeholder text duplicates what a CSS-styled float-label would show (the floating-label antipattern). WCAG 3.3.2 + 1.3.1. (e163c12)
- [x] **Q3-RULE-RADIO-GROUP-FIELDSET** New `forms/radio-group-without-fieldset` (stronger than existing `fieldset-legend`). ≥2 `<input type="radio">` sharing a `name=` without an ancestor `<fieldset><legend>` or `role="radiogroup"` + `aria-labelledby`. WCAG 1.3.1 + 3.3.2 + 4.1.2. (5777e89+b3dc3af+8fc3be4+27083fc)
- [x] **Q3-RULE-LIVE-REGION-PRE-EXISTENCE** New review finder `review/live-region-pre-existence` for wcag22:4.1.3 + wcag21:4.1.3. Live-region container (`role="alert|status|log"` or `aria-live="polite|assertive"`) hidden at initial render via `display:none`, `visibility:hidden`, the `hidden` attribute, or class tokens `d-none|is-hidden|invisible|hide` — on the element itself or any ancestor. `sr-only` / `visually-hidden` / `screen-reader-text` deliberately excluded (AT-visible). Reason text echoes the matched signal so the reviewer knows where to look; confidence `medium` per ai-first doctrine. HTML + JSX coverage; 23 unit tests + 7 fixtures (3 bad / 3 good / 1 edge).
- [x] **Q3-RULE-CHARSET-DECLARATION** New `document/charset-first-1024-bytes`. `<meta charset>` must be the first child of `<head>` within the first 1024 bytes (HTML spec; pairs with `document/viewport-zoom`). Low priority but mechanical.
- [x] **Q3-FINDER-FORM-TIMING-BOOTSTRAP** Validation docs teach `.needs-validation novalidate` + per-keystroke visual feedback. The existing `forms/validation-timing` finder (Q2R2-FORM-TIMING) should also recognize this pattern when parsing restores. Verify coverage after parser restart. (be3e49c4) Verification found the finder was JSX-only; widened appliesTo to `.html`/`.htm`, added Constraint Validation API tokens + was-validated class to the inline patterns, added form-level detection for `<form class="needs-validation" novalidate>`, reordered detectValidation so inline tokens beat reference-name matching for dotted calls like `this.checkValidity()`.
- [x] **Q3-FINDER-CAROUSEL-PATTERN** New review finder `motion/carousel-pattern`. Surface `data-bs-ride="carousel"` + role=region/aria-roledescription="carousel" + accessible name checks. WCAG 2.2.2. Companion to Q3-RULE-PAUSE-STOP-INLINE-STYLE.
- [x] **SCAFFOLDER-IMPORT-POSITION** `scripts/scaffold-rule.ts` appends the new rule's import at the end of `src/rules/index.ts`'s import block instead of inserting alphabetically within the correct path-group cluster (`forms/`, `navigation/`, `media/`, etc.). Observed independently by three rule-implementer agents (Q4-RULE-EXTERNAL-LINK, Q4-RULE-PLACEHOLDER-ALT, Q3-RULE-PLACEHOLDER-AS-LABEL) — each had to hand-reorder or ship a biome-cleanup `fix(rules): import position` follow-up commit, and cross-turn integration conflicts on `src/rules/index.ts` are almost entirely driven by the tail-append collision. Fix: teach the scaffolder to parse the existing import block, find the right path-group cluster for the new rule's path, and insert in alphabetical order inside that cluster. Fallback for unknown paths: append at end (current behavior). (12da16e — insertIntoRegistry uses module-path key for imports and identifier key for BUILTIN_RULES/exports; unit test exercises aria cluster insertion + identifier-block ordering)
- [x] **Q3-RULE-NESTED-INTERACTIVE-ROLE-REGRESSION** Regression audit on `src/rules/semantics/nested-interactive.ts`. Q3-RULE-NESTED-INTERACTIVE-ROLE-WRAPPER marked closed but the rule still fires on role-only wrappers: outer `<div role="tab">` with no click handler / tabindex / native interactive tag triggers findings at `js/tests/visual/modal.html:63,78,92` and `collapse.html:17,32,46,60` (seven false positives). Determine whether the closure regressed or the originally-landed closure never matched these fixtures; add a real-world fixture under `tests/fixtures/real-world/role-tab-wrapper/` before fixing per CLAUDE.md §7 bug-fix workflow. WCAG 2.1.1 + 4.1.2. (5aa703d — fixture GREEN on current main; regression guard against re-introduction)
- [x] **Q3-RULE-POPOVER-NOT-DISCLOSURE** `src/rules/aria/expanded-on-disclosure.ts` fires on `data-bs-toggle="popover"` as if popovers were disclosure widgets; evidence `popover.html:13,17,21,25,29` + `modal.html:53`. Popovers are tooltip-shaped (`role="tooltip"` + `aria-describedby`), not disclosure (`aria-expanded`). Remove `"popover"` from the disclosure-toggle allow-list; keep `"dropdown"`, `"collapse"`, and the JS-classList-toggle predicates. Add a good-fixture under `tests/fixtures/good/expanded-on-disclosure/popover-not-disclosure.html`. WCAG 4.1.2. (cfc089f)
- [x] **Q3-RULE-PAUSE-STOP-HTML-INLINE-STYLE-MISS** Q3-RULE-PAUSE-STOP-INLINE-STYLE (closed) missed the canonical carousel case: `carousel.html:10,20` has a `<style>.carousel-item { transition: transform 2s ease }</style>` block alongside `data-bs-ride="carousel"` and the rule emits zero findings. Verify that inline `<style>` scanning runs against HTML documents at the scanner's root (not only when `.css` files are parsed separately); add a fixture under `tests/fixtures/bad/motion-pause-stop-hide/html-inline-style.html` asserting the rule fires on the transition. Pairs with Q3-FINDER-CAROUSEL-PATTERN — the `data-bs-ride` signal itself is also a 2.2.2 candidate source. WCAG 2.2.2. (de4e32c — fixture-only; rule-fix follow-up in next invocation)
- [x] **Q3-OPAQUE-COMPONENTS-TOP-EMPTY-DISHONEST** (f793550) `assembleOpaqueComponentBlock` now omits `opaqueCustomComponentsTop` entirely when the interactive filter drops every candidate, instead of shipping `[]` alongside a populated `opaqueCustomComponentNames`. `buildHints` elides the `(top: ...)` parenthetical when examples is empty, so the prose no longer renders `"(top: )"` with nothing after the colon. Two new tests lock both codepaths.
- [x] **Q3-PLAN-MECHANICAL-EDITS-COUNTER-DRIFT** `plan.mechanicalEditsAvailable: 14` co-occurred with `plan.fixesByClass.mechanical: 0` — the 14 counted are all `verify-in-source`, not mechanical edits. Either rename `plan.mechanicalEditsAvailable` to `plan.safeEditsAvailable` (covers mechanical + verify-in-source — the two lanes an agent can act on with local edits) or align the counter to `fixesByClass.mechanical` only. Pick one definition; document the chosen semantic in CHANGELOG under `[Unreleased]`. Doctrine: "Composite headline counts are dishonest" — the counter currently sums two distinct lanes under a name that promises one.
- [x] **Q3-PARSE-ERROR-FILE-CONTRADICTION** `analysisCoverage.parseErrorFiles: [modal.mdx]` was emitted alongside 14 findings on that same `modal.mdx` with live line numbers. The current shape conflates "file wholly failed to parse" with "file partially parsed and some rules fired." Split: keep `parseErrorFiles` for total-parse-failure (zero findings possible), emit a separate `partialParseFiles: [{ path, reason }]` for files where the parser recovered and downstream rules ran on the recovered slice. Agents currently read `parseErrorFiles` as "these files are invisible" and miss the findings that did emerge.
- [x] **Q3-TEMPLATE-DIRECTIVE-STARLIKE-MISDETECT** An Astro/Starlight tree is not a Handlebars/Mustache project, but `analysisCoverage.templateDirectivesFound: ["handlebars-or-mustache"]` fired. The `{{...}}` hits came from `.github/workflows/*.yml` GitHub Actions expressions (`${{ github.event.pull_request.number }}`) and Astro JSX props `overrides={{ body: bodyProps }}`. Tighten the detector: require a closing `}}` followed by non-JSX context, or drop the family entirely when an SSG has already been detected at the scan root. Pair with adding `"starlight"` as a recognized `ssg` value in the Q4-SSG-BUILD-HINT path so Astro + Starlight repos are named honestly. Doctrine: the classifier is already per-file majority-vote (Q4-TEMPLATE-DIRECTIVE-CLASSIFIER-DRIFT); this tightens what counts as evidence of a template family in the first place.
- [x] **Q3-BOOTSTRAP-WRAPPER-DETECT-EMPTY-REASON** `detect_native_wrappers.wrappers.candidates: []` surfaced alongside a scan carrying 15 opaque PascalCase components in `opaqueCustomComponentNames`. The detector requires an `onClick` prop — MDX/Astro components rarely carry inline `onClick`. Two paths: (a) lower the bar in MDX/Astro to include components with any interactive-looking prop (`href`, `onClick`, `onPress`) or any rendered-as-element signal; (b) emit a structured `wrappers.emptyReason: "no-jsx-onclick-candidates-found-but-opaque-components-present"` so the agent sees the detector's lower-bound premise failed even though opaque components exist. Prefer (b) as an additive honest-signal shim; (a) risks widening false positives. Extends Q3-DETECT-WRAPPERS-EMPTY-REASON with the Astro/MDX-specific case. Doctrine: "Zero-output success is ambiguous failure."
- [x] **Q3-IMAGES-OF-TEXT-SVG-DATA-URI-SRC-CORPUS** `wcag22:1.4.5` candidate at `js/tests/integration/index.html:31` claims "alt text repeated in surrounding text" when the `<img alt="First slide">` is adjacent to a `<text>First slide</text>` node inside an SVG `data:` URI sitting in the same element's own `src=` attribute. Surrounding-text scan should exclude the flagged element's own `src` / `data-*` attribute values (they are not visible text on the rendered page). Extend the helper to skip attribute values that contain `data:image/svg+xml`; alternatively, name the repetition's match location in the reason text so the agent can tell the text was inside the same element's `src`. Distinct from V1-FP-IMAGES-OF-TEXT-SVG-DATA-URI (that item enriches the reason when the `<img>` itself has a data-SVG src; this item is about the SVG text payload polluting the surrounding-text for a different element or the same element).
- [x] **Q3-TEXT-SUCCESS-CANDIDATE-IGNORES-TEXTCONTENT** `wcag22:1.4.1` candidate says `text-success` "with no visible text" on `<strong class="... text-success-emphasis">Design</strong>` — the element's `textContent` is "Design," which is visible. Finder must pre-check `element.textContent.trim().length > 0` before emitting the "no visible text" variant; when text is present, emit the simpler variant that frames the question as "color-only indicator check — verify the state is not conveyed by `text-success` alone." Pairs with (but does not block) Q3-RULE-COLOR-SOLE-INDICATOR. WCAG 1.4.1. (committed def98eaf)
- [x] **Q3-MCP-RESTART-HINT-ERROR-ENVELOPE** `annotateStaleSubprocess` short-circuits on `result.isError === true`, so stale-subprocess + `file-unsupported` on `.md` is indistinguishable from "extension truly unsupported." Highest-risk path for silent miss: an agent that hits an error envelope on a stale subprocess never learns the tool needs a restart. Fix: merge `stale_mcp_subprocess` into `warnings[]` regardless of `isError` — the warning is about the subprocess, not the individual call's success. Three tests: `isError: true` + warnings merge, non-string filter on error envelope, error-envelope preserves `structuredContent` when present.
- [x] **Q3-MCP-RESTART-HINT-SUBPROCESS-RACE** Q3-MCP-RESTART-HINT was closed earlier but failed to fire across a full 90-min session even though dist mtime advanced mid-session. Root-cause three candidates: (a) `recordSubprocessStart` records baseline at module-init and never re-stats; (b) `import.meta.url` resolution returns a stale path when the subprocess is spawned before the rebuild and the path becomes a different inode; (c) the one-shot recording is cached and never retried. Reproduce with a deterministic test (spawn subprocess → rewrite dist → call tool → assert warning fires), not just added coverage. The fix self-heals ≥40% of the enumerated BUGs in the follow-up pass triage.
- [x] **Q3-META-BUILD-PROVENANCE** Add `meta.ra11yVersion: string`, `meta.commitHash?: string`, and `meta.bundleMtime: string` (ISO) to every MCP response so an agent can detect drift between expected and actual tool versions without relying on `stale_mcp_subprocess` heuristic alone. Conditional-spread `commitHash` per the AI-first consumer model (non-git deployments ship without it). Pairs with Q3-MCP-RESTART-HINT-SUBPROCESS-RACE — provenance is the positive signal; stale-warning is the negative signal; both needed. Additive meta field; no new tool parameters.
- [x] **Q3-RULES-BY-EXTENSION-UNDERCOUNT** `rulesByExtension` now routes through `extensionMatches` (the same helper rule-runner.ts `applies()` uses for per-file eligibility), so alias-heavy scans (`.scss`/`.mdx`/`.astro`/`.md`/`.markdown`/`.js`/`.ts`) list every rule that actually evaluated files with that extension — matching `perRuleCoverage`. Picked (a): literal-equality inside `rulesByExtension` was a bug against the field's own TSDoc claim of "mirrors the gate in rule-runner.ts `applies()`." Added unit coverage: direct `rulesByExtension` alias expansion across every EXTENSION_ALIASES row + a live `scan_project` invariant asserting every rule reporting `filesEvaluated > 0` appears under `rulesByExtension[".scss"]`. Doctrine: cross-surface consistency — two fields naming "rules run on this extension" now agree by construction.
- [x] **Q3-RULE-TOOLTIP-NOT-DISCLOSURE** `src/rules/aria/expanded-on-disclosure.ts` still over-applies to `data-bs-toggle="tooltip"`. Evidence: `js/tests/integration/index.html:18` — a button carrying `data-bs-toggle="tooltip"` + native `title="Tooltip on top"` is flagged as a disclosure trigger. A tooltip is descriptive content (per ARIA APG `role="tooltip"` + `aria-describedby`), not an ARIA disclosure pattern (which uses `aria-expanded`). The rule's own reason text names the misclassification verbatim (`data-bs-toggle="tooltip" is a disclosure-style toggle value`). Roughly 37 of 169 emissions of this rule are polluted by this false positive. Fix parallel to Q3-RULE-POPOVER-NOT-DISCLOSURE (closed): remove `"tooltip"` from the disclosure-toggle allow-list; keep `"dropdown"`, `"collapse"`, and the JS classList-toggle predicates. Add good-fixture `tests/fixtures/good/expanded-on-disclosure/tooltip-not-disclosure.html` (mirrors the popover companion). Satisfies wcag22:4.1.2.
- [x] **Q3-COLOR-MEANING-READS-TEXT-CONTENT** `color/meaning-by-color-only` reason-text generator does NOT inspect the element's own `textContent` before emitting "text content `'Danger'` carries no status word." Evidence: `js/tests/visual/alert.html:24` has `<button class="btn btn-danger">Danger</button>` — the text literally IS the status word, so the reason text is factually wrong. Worse: the fix ranker proposes "prefix with 'Danger:'" as alternative #1, which would produce `"Danger: Danger"` on this button. Two paired fixes: (a) pre-check the element's trimmed `textContent` against a status-word dictionary (`danger|error|fail|warning|success|info|caution|alert` + common localized forms) and when the text already carries a status word, change the reason to `"text already carries status word — verify color is not the *sole* meaning signal for users who cannot distinguish the color"` (reason-text enrichment) and demote the "prefix with status word" alternative in the fix ranker; (b) still emit the candidate — don't suppress — because `text-danger` on a button with text "Danger" is less confident than a `text-danger` on a numeric balance, but the agent decides after reading. Pairs with the closed Q3-RULE-COLOR-SOLE-INDICATOR. Satisfies wcag22:1.4.1.
- [x] **Q3-MDX-EXAMPLE-TEMPLATE-LITERAL-SUBSTRATE** Docs sites ship `.mdx` files using `<Example code={` ... `}/>` — the HTML body rendered on the docs site lives inside a JSX template-literal attribute value. Rules that parse the AST via the MDX pipeline get `template_files_parsed_as_literal` warning; ~110 `.mdx` files' worth of HTML substrate is skipped from rule coverage. Concrete miss: `forms/labels-required` does not fire on `site/src/content/docs/forms/overview.mdx:36`'s `<input type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp">` inside a template literal, despite `scan_file` reporting `filesEligible: 1, findingsEmitted: 0`. This is the rendered docs-site markup — a canonical, load-bearing substrate. Add a Starlight/docs-component-aware extractor: when the parent MDX AST node is a JSX element matching a configurable name list (default `["Example", "Demo", "Playground"]`) with a `code` prop that is a template literal, parse the template-literal body as HTML and feed findings back into the rule pipeline keyed to the original line range. Pairs with the `Restore <Example code={`…`}> parsing via a Starlight-specific extractor` deferral note in Q3's "Considered and rejected" — deferred, not rejected. Satisfies wcag22:1.3.1 + 3.3.2 + every HTML-parser rule that would now run on ~110 additional file equivalents.
- [x] **Q3-BUILD-ARTIFACT-SINGLE-LONG-LINE-SECOND-PROBE** `scannedBuildArtifacts` classifier at `src/mcp/build-artifacts.ts:176` gates `minified` on `MINIFIED_LINE_THRESHOLD = 500` — any file with one line > 500 chars is labeled minified. 54 authored files mis-labeled (`site/src/assets/examples/blog-rtl/index.astro` 255 lines, `site/src/content/docs/components/accordion.mdx` 240 lines, `scss/_functions.scss` 302 lines — none are minified). The single-long-line heuristic fires on (a) Astro `<Example code={`…`}/>` template literals that embed multi-line HTML as one JS string, (b) SCSS function bodies with long type signatures, (c) MDX component prop bundles. Fix: require a second predicate alongside the single-long-line probe — EITHER ≥25% of lines exceed the char threshold OR the file's median line length > threshold OR `.min.` infix OR build-path ancestry (`dist/` / `build/` / `_site/` / `public/`) OR a sibling `.map` file. Any ONE of the second-tier predicates AND the single-long-line probe is sufficient. Q-SHARED-BUILD-ARTIFACT-SASS-PARTIAL (closed) added the `_*.scss` carve-out but didn't touch this root predicate. Reproduces at scale (101 authored HTMLs mis-labeled, single Google-Maps embed per file is the signature). Pair test: `tests/fixtures/real-world/astro-template-single-long-line/` + `tests/fixtures/real-world/template-gmaps-embed-single-long-line/`. Doctrine: the `scannedBuildArtifacts` label must be provable from the file shape (ai-first-consumer.md §labeled-buckets).
- [x] **Q3-HEADING-HIERARCHY-MISSING-H1-VARIANT** `semantics/heading-hierarchy` catches level-skips (`<h1>` then `<h3>`) but does NOT catch full-page shapes with zero `<h1>` at all. Evidence: scanned trees include `js/tests/visual/button.html`, `alert.html`, `breadcrumb.html` (plus several vanilla-JS demos) with full-page DOCTYPE+html+body shape and no `<h1>` — rule fires 46 findings on one scan but none flag "missing h1." A full page with zero h1 is a 1.3.1 + 2.4.6 violation distinct from level-skip. Fix: add a `missing-h1-on-full-page` variant to the rule; the eligibility predicate is `looksLikeFullPage(node) && count(h1) === 0`, emitting at the `<body>` line. Pairs with Q5-LANDMARK-FULLPAGE-HEURISTIC-TIGHTEN (open) — both rules share the `looksLikeFullPage` predicate, so the fix lands once the fragment heuristic stabilizes. Satisfies wcag22:1.3.1 + 2.4.6.
- [x] **Q3-SENSORY-CHARACTERISTIC-LOCATIVE-WORD-SCOPE** `wcag22:1.3.3` finder matches the string `"fields below"` as a sensory characteristic on `js/tests/visual/alert.html:15` ("Please correct errors in the fields below"). "fields below" is a grammatical locative referring to form fields by document order — the user who can't see the visual page still parses the sentence as "fields that come next in the form." SC 1.3.3 targets *sensory* cues (color, shape, size, visual location, sound) that wouldn't be conveyed by assistive tech — "fields below" is conveyed correctly. Fix: tighten the finder's corpus: a location word (`above | below | next to | adjacent to | to the right of | to the left of | following | preceding`) alone is insufficient evidence for 1.3.3. Require co-occurrence of (a) a visual signifier token (color name, shape name, size adjective) OR (b) a pointing verb ("see", "click", "tap", "select", "press") within ±10 tokens of the locative. Bare locatives describing document-order relations should not fire. Reason-text enrichment is insufficient here — the candidate itself is noise in its current shape. Satisfies wcag22:1.3.3.
- [x] **Q3-REVIEW-CROSS-TREE-3.2.3-CONFIDENCE-CAP** `wcag22:3.2.3` consistent-navigation candidate pairs files from unrelated trees (e.g. `js/tests/visual/dropdown.html` vs `site/src/assets/examples/navbars/index.astro`) with `confidence: "high"` — while the reason text itself acknowledges "Heuristic match across the whole scanned tree." The confidence label contradicts the acknowledged heuristic weakness. No `processes: []` declaration binds these files together; the pairing is a tree-wide fuzzy match. Fix: when the 3.2.3 finder's pairing crosses directory-tree boundaries AND the repo has no `processes: []` declaration in ra11y config, cap emission confidence at `"medium"` and enrich reason with "note: these files are heuristically paired by token similarity; declare `processes` in ra11y.config.ts to bind the correct page sets." Reason-text enrichment ALONGSIDE confidence cap — both are needed. Pairs with the general "confidence claim must match evidence strength" honesty rule surfaced in Q5-COVERAGE-CONFIDENCE-HONESTY-ARCHITECTURAL-BLINDSPOT (open). Satisfies wcag22:3.2.3.
- [x] **Q-SHARED-PROPOSE-CONFIG-RELATIVE-PATHS** `propose_config` now pipes build-artifact paths through `normalizeExcludes(paths, root)` before emitting. Every emitted path is POSIX-style root-relative (`dist/compiled.css`, never an outside-root absolute path), paths outside the scan root are dropped, and groups with 3+ entries sharing a top-level directory collapse to a single `<dir>/**` glob (motivating case: at scale, 301 entries under one `dist/` tree). Two-entry groups stay itemized so a small `assets/` pair reads clearer than a wildcard. Collapse threshold pulled out as `EXCLUDE_GLOB_COLLAPSE_THRESHOLD = 3` with rationale. Three new tests (relative-only invariant, 3-file collapse, 2-file preserve) locked in alongside the existing exclude-branch test. Design-system scans inherit the fix transparently via their propose_config leg. Consolidates three cross-track field-test reports.
- [x] **Q-SHARED-BUILD-ARTIFACT-SASS-PARTIAL** `scannedBuildArtifacts` classifier fires on Sass partials (`_variables.scss`, `_style.scss`) because the filename starts with `_`. That prefix is the Sass partial convention (authored source) — not a generated-artifact signal. Key the classifier off deterministic markers instead: `.min.` infix, `dist/` / `build/` / `_site/` / `public/` path ancestry, hashed-filename patterns (`.[a-f0-9]{8,}.`), or a sibling sourcemap. Consolidates two cross-track field-test reports. Doctrine: the `scannedBuildArtifacts` label itself is honest (findings still surface per Q4-SCSS-DISCOVERY-WIRE), but mis-labeling authored Sass as artifact misleads the agent's triage — the label must be provable from the file shape.
- [x] **Q-SHARED-MD-DISPATCH-UNIFY** `scan_file` on `.md`/`.markdown` returns `{code: "file-unsupported", remediation: "...astro, .css, .htm, .html, .js, .jsx, .mdx, .scss, .ts, .tsx."}` — `.md` and `.markdown` elided from the advertised set — while `session.parseFile` and `tool-apply-fix-internals.ts` both dispatch correctly. Root cause: commit `38e86f5` claimed "route .md/.markdown through parseMarkdown end-to-end" but `src/cli/commands/scan.ts::parseFor` has no `.md`/`.markdown` branch, and the MCP `SCAN_FILE_UNSUPPORTED_REMEDIATION` constant is not rebuilt against the live registry at call time. Reproduced across design-system, SSG, and vanilla-JS trees (hundreds of .md/.markdown files invisible to CLI in some cases). Fix: land the CLI `parseFor` `.md`/`.markdown` branch; derive `SCAN_FILE_UNSUPPORTED_REMEDIATION` from `parseableExtensions()` at call time (not module-init); golden fixture (sanitized SSG-style `_docs/assets.md` with embedded HTML). Folds Q4-MD-CLI-PARSEFOR-MISSING + Q4-SCAN-FILE-UNSUPPORTED-REMEDIATION-STALE filed by a deeper-pass scan. Doctrine: "Ambiguous field shapes are dishonest" — remediation prose claims a single source of truth that doesn't match runtime.
- [x] **Q-SHARED-RULES-EVALUATED-SSOT** `rulesEvaluated` disagrees across MCP surfaces on the same `cwd`: `scan_project` reports 53, `propose_config` reports 54, `list_rules` reports 54 (reproduced on and at scale where `navigation/href-javascript-void` sits in source + is imported but absent from running registry). Likely cause: JSX/TSX-only rule counted-as-evaluated when JSX input is absent differs between tools; stale subprocess serving a 53-rule bundle against a 54-rule on-disk bundle compounds. Fix: centralize "rules evaluated on this input" in one helper consumed by every surface; add cross-surface invariant test asserting `list_rules` rule set == keys in any scan's `perRuleCoverage`. Pairs with the "Needs judgment" question on JSX/TSX-only rules counted as evaluated on non-JSX inputs. Doctrine: "Verbose meta is signal, not clutter" + cross-surface consistency.
- [x] **Q-SHARED-NEXTSTEP-SCHEMA-DISPATCH-TEST** `nextStepStructured.args.file` on a scan response names `file` as the parameter, but `scan_file`'s schema declares `path`; `suggest_fix.verifyCommandStructured` emits a non-existent `ruleId` parameter on `scan_file` args. An agent that pipes `args` through verbatim hits `InputValidationError` — the structured-next-step contract is broken by silent drift. Reshape: paired src+test fix (originally framed as test-only; agent dispatch returned classification_mismatch because the correctly-written test fails on real drift today). Step 1 — fix the two known drift sites: `src/mcp/next-step.ts:231` rename `args.file`→`args.path` for the `scan_file` emission; `src/mcp/tool-suggest-fix-internals.ts:98-101` rename `args.file`→`args.path` and drop the advisory `ruleId` (move it to a sibling metadata field if the advisory signal is worth keeping). Step 2 — land the typed dispatch test in `tests/unit/mcp/` asserting every `nextStepStructured` and `verifyCommandStructured` `args` object validates against the target tool's input schema at build time (any `args` key not in the target's JSONSchema fails the test); with the src fixes the test now passes green and locks the invariant. Doctrine: "One tool call should answer 'what next?'" — the structured hint must be directly callable.
- [x] **Q-SHARED-SCAN-PROJECT-INLINE-REVIEW-CANDIDATES** `scan_project` returns `actionableManualItems: N` with `files: []` when automated findings are zero on the scanned root. Agent has no inline file:line pointer and must make a separate `checklist` call. Reproduced on (`test/source` sub-scan returned `actionableManualItems: 1` + empty files array) and at scale (`plugins/` sub-scan returned `actionableManualItems: 1, files: []`). Fix: add `reviewCandidateFiles?: [{ path, count }]` (or inline `reviewCandidates?: [{file, line, criterion, reason}]` capped by the same `limit`) on `scan_project` per-file entries or as a top-level array, conditional-spread when automated-findings files exist. Doctrine: "One tool call should answer 'what next?'" — currently forces a wasted round trip.
- [x] **Q-SHARED-SCAN-FILE-ERROR-DISCRIMINATION** `scan_file` uses `code: "file-unsupported"` for both unsupported-extension and file-not-found. Reproduced: `scan_file` on a nonexistent `.mdx` path (extension IS supported, file doesn't exist) returns `file-unsupported`; `scan_project` has a distinct `cwd-not-found` code. Split: `file-not-found` (path doesn't exist) / `unsupported-extension` (extension not in `PARSEABLE_EXTENSIONS`) / `file-unreadable` (permission / IO). Pairs with Q-SHARED-MD-DISPATCH-UNIFY — an agent hitting `file-unsupported` on a `.md` input needs to distinguish "extension truly unsupported" from "not-supported-by-this-subprocess" from "not found on disk." Doctrine: "Ambiguous field shapes are dishonest."
- [x] **Q-SHARED-DISCLOSURE-REASON-ENRICHMENT** `aria/expanded-on-disclosure` fires on patterns where the reason is insufficiently annotated: design-system MDX popover examples (Q3-RULE-POPOVER-NOT-DISCLOSURE closed, but 103 lingering findings on attribute combos the closure didn't widen for), a at-scale collapse button with `<span class="sr-only">Toggle navigation</span>` accessible name. When the rule fires on an element carrying `.sr-only` / `.visually-hidden` / `aria-label` children, enrich `reason` with "note: element has a visually-hidden text child — if that is the disclosure label, verify the accessible name is complete." Reason-text enrichment only — candidate stays in the primary list. Doctrine: "Surface don't suppress" — enrich reason so agent dismisses faster without heuristic suppression.
- [x] **Q-SHARED-META-ARRAY-BUDGET-CAP** Meta-array bloat overruns the MCP host token cap even at `limit: 1` / `minSeverity: "error"`. Full-root scan at scale returns 315KB response of which 281KB (89%) is `meta`: `meta.scannedBuildArtifacts` 148KB + `meta.analysisCoverage.parseErrorFiles` + `partialParseFiles` 121KB — both are raw absolute-path arrays that grow linearly with input size. Reproduced on design-system site/docs (66KB at `limit:30 → effectiveLimit:3`), SSG docs (66KB at `limit:10`), 3/5 template subsets >66KB at scale, and coverage responses (~100% meta). `Q6-BUDGET-UNDER-VENDOR-NOISE` addresses per-file finding cap + per-criterion checklist clip; this item addresses the orthogonal meta-path-array cap. Fix: cap each `meta.*` path array to `≤ N` entries with `{ shown, total }` summary + `truncated: true`, analogous to how `plan` / `files` pagination already works; surface a `response_meta_truncated` warning when applied. Pairs with `response_token_budget_truncated` for end-to-end honesty. Doctrine: composite-budget truncation must be honest across every field that grows with input.
- [x] **Q-SHARED-COVERAGE-FAILING-CRITERIA-DERIVATION** `coverage.failingAutomatedCriteria` drifts from `scan_project.criteriaInFindings` on the same cwd. Design-system site/docs: scan surfaces 1.4.1 (14 `color/meaning` findings); coverage omits 1.4.1 from failingAutomatedCriteria. SSG docs: scan hits {1.3.3, 1.4.1, 2.4.9}, coverage classifies those as manual-only yet conformance_statement stamps 1.3.3 "fail" and VPAT claims 120 findings. Catalog-scale (coffee-shop subset): coverage asserts 6 criteria failing with zero emitted findings — token-truncated scan feeding stale derivation. Root cause: `failingAutomatedCriteria` derives from rule→criterion eligibility independently of what actually fired in this scan; truncated scans inflate the drift. Fix: derive `failingAutomatedCriteria` from the emitted violations set (intersect with `rulesEvaluated.fired`), honour `truncated: true` by carrying the per-criterion "evidence-complete" flag, and add a cross-surface invariant test asserting `coverage.failingAutomatedCriteria ⊆ scan.criteriaInFindings ∪ scan.truncatedCriteriaPossible`. Doctrine: "One tool call should answer 'what next?'" — cross-surface drift forces wasted round trips.
- [x] **Q-SHARED-TSX-PARSER-FALSE-JSX-CONTEXTS** (89d535aa + 6fd5c3e9) TSX parser enters JSX mode in two contexts where it shouldn't: (a) template-literal contents inside JSX attribute values — `<Example code={`<iframe src="demo" />`}>` and `<Callout>...</Callout>` on `.mdx`/`.astro` sources trigger `Unclosed JSX element <iframe>/<body>/<span>` partialParse on 12 files (`src/content/docs/components/dropdowns.mdx:384`, `src/content/docs/helpers/ratio.mdx:22`); (b) minified `.js` files containing `<` comparison operators — `lib/jekyll/commands/serve/livereload_assets/livereload.js` errors "Unclosed JSX element `<r.length>`" because `r.length<b.length` parses as a JSX open tag. Both cases silently drop downstream rule coverage for the affected file. Fix for (a): do NOT enter JSX mode inside a template-literal body when the surrounding attribute value is a template-literal JSX expression; treat the contents as opaque string. Fix for (b): gate JSX-mode entry on file extension (`.jsx`/`.tsx`/`.mdx`/`.astro` — not bare `.js`/`.ts`) unless the file has a leading JSX-import signal. **Status (2026-04-22): fixtures landed but parser fix did NOT — reopening.** Real-world fixtures `tests/fixtures/real-world/astro-template-literal-jsx-attr/` and `tests/fixtures/real-world/livereload-minified-js/` ship with `zero-parse-errors` expectations **commented out** per CLAUDE.md §7 bug-fix workflow (fixture-first, fix-later); this item was prematurely ticked off when the fixtures landed. A follow-up pass reproduced both symptoms: 12 `.mdx` files partial-parse, `livereload.js` routed to TSX with "Unclosed JSX element `<r.length>`." Pending work: land the two parser fixes in `src/input/parsers/tsx.ts` (attribute-expression template-literal opaque-string handling + JSX-mode entry gate on extension); once both land, reinstate the commented `zero-parse-errors` assertions in both real-world fixtures and mark this item `[x]` with the fix commit hash. Doctrine: silent file drops mask real violations; a fixture whose assertion is commented out does not close its parent item.
- [x] **Q-SHARED-INLINE-STYLE-CONTRAST** `contrast/minimum` and `contrast/non-text` never evaluate `style=` attributes on HTML elements. `rulesByExtension .html` lists contrast rules only under `.css`. Website-templates reproducers: `super-simple-site/index.html:17 style="float:right"` (harmless), `ideal-interior/index.html:211 style="background-color:#cccccc"` on a paragraph carrying dark text (silent miss). Zero contrast findings across 5 sampled templates despite heavy inline-style use. Fix: HTML parser should synthesize a per-element style block (e.g. `#__ra11y_inline_${n} { background-color:#ccc; color:#aaa }`) fed into the CSS-rule pipeline so contrast rules evaluate inline declarations identically to stylesheet rules. Alternative: add an inline-style-specific code path in `src/rules/contrast/*` that reads the element's parsed `style` attribute directly. Pairs with Q3-RULES-BY-EXTENSION-UNDERCOUNT (meta honesty about what ran on `.html`). Doctrine: silent-miss on a common real-world pattern is more costly than any false-positive risk from widening scope.
- [x] **Q-SHARED-VPAT-HONESTY-PACK** Two compounding VPAT honesty bugs surface on any cross-tool run: (1) `conformance_statement` and VPAT report `status: "pass"` on criteria with `reason: "no-evidence"` (15 of 18 "pass" criteria on an SSG docs-site scan had no emitted findings — absence interpreted as proof). (2) VPAT defaults to all 86 A/AA/AAA criteria even when `scan_project` ran at default AA level — 45 criteria appear as "Not Evaluated" including AAA items scan never attempted, and VPAT's `Partially Supports` bucket conflates 0-finding criteria ("manual review still required") with 120-finding criteria (both stamped PS). Fix (1): split `status: "pass"` into `"pass"` (evidence-supported) vs `"untested"` (no-evidence) — never sum them under "pass"; surface `untestedCriteriaCount` as a sibling counter. Fix (2): VPAT respects the scan's `level`; criteria outside scope render as `Not Applicable — out of scope` with the scope citation, never `Not Evaluated` without explanation. Pair with `evaluator.scanLevel: "AA"` as a top-level VPAT field; add a cross-tool invariant test asserting VPAT rows are a subset of the standards-at-that-level. Doctrine: composite headline counts are dishonest; "pass" is a load-bearing claim in procurement artifacts.
- [x] **Q-SHARED-DETECT-NATIVE-WRAPPERS-CWD-NOT-FOUND** `detect_native_wrappers` silently succeeds on a nonexistent cwd: `<root>/nonexistent` returns `{ candidates: [], emptyReason: "no-parseable-files" }` with no warning. `scan_project` correctly errors `cwd-not-found` on the same path. Canonical zero-output-success antipattern named in doctrine. Fix: reuse the `scan_project` cwd-existence guard in `src/mcp/handlers/detect-native-wrappers.ts` before globbing; emit structured error `{ code: "cwd-not-found", path }` with no `candidates` array. Tiny — mechanical. Pairs with Q3-DETECT-WRAPPERS-EMPTY-REASON (different empty-reason codes for real-but-empty cases).
- [x] **Q-SHARED-SUGGEST-FIX-GUIDANCE-PRIMARY** `suggest_fix` tool description promises `guidance` responses carry a ranked `primary` fix path plus `alternatives` array. Actual emission for `kind: "guidance"` is `{ kind, explanation, sourceContext, confidence, verifyCommand, verifyCommandStructured }` — no `primary`, no `alternatives`. Reproduced across 4 of 4 guidance-kind calls at scale. Two resolutions: (a) populate `primary` + `alternatives` as structured fix-path objects (the tool's advertised shape); (b) update the tool description to match the actual shape and drop the promise. Prefer (a): agents budgeting against the documented shape hit a schema-contract gap. Pairs with V1-FIX-AUDIT acceptance gate.
- [x] **Q-SHARED-EXPLAIN-RULE-EQUIVALENT-CRITERIA** `explain_rule` returns `satisfies: [wcag22:*, wcag21:*]` only — omits `section508:*` and `en301549:*` equivalents that the same rule surfaces via `equivalentTo` index on per-finding `criteria` arrays in `scan_file`. Agent auditing Section 508 cannot tell from `explain_rule` whether a rule covers §1194.22(n) or 9.3.3.2 — forced to cross-read rule metadata + standards registry. Fix: `explain_rule` expands `satisfies` through the same reciprocal index the scanner uses for cross-standard equivalence (CLAUDE.md §6 "At registry init, the engine walks every loaded standard's equivalentTo field and builds a reciprocal index"). Asymmetric surface is the bug. Tiny.
- [x] **Q-SHARED-VIDEO-CAPTIONS-IFRAME-EMBEDS** `media/video-captions-missing` only inspects `<video>` elements; `<iframe src="https://www.youtube.com/embed/…">` / Vimeo / Wistia embeds never flagged. Reproduced on a catalog subset and expected at scale on any content-site shape. Fix: extend the rule's element selector to include `<iframe>` whose `src` matches a curated host allowlist (youtube.com/youtu.be, vimeo.com, wistia.{com,net}, brightcove, loom), with reason "iframe-embedded media — captions must be provided by the host platform; verify the embedded source has captions enabled." Satisfies wcag22:1.2.2 + 1.2.4.
- [x] **Q-SHARED-PASS-RATE-COMPOSITE** `coverage.automatedCriteriaPassRate: 58%` on reveal vs `81%` on super-simple-site — headline composite bundles "criteria the scanner actually ran" with "criteria that emitted zero violations." Rate sinks when 2.2.1 surfaces 7 `setInterval` review candidates on a minified vendor bundle — "candidates present on unactionable file," not "criterion failure." Headline "58% clean" is false on a codebase whose author didn't write the 2.2.1-triggering code. Fix: split into `criteriaEvaluated` (ran), `criteriaClean` (ran, zero findings), `criteriaWithFindings` (ran, ≥1 finding), and `criteriaUntestable` (rule declared eligible but no applicable input). Headline becomes `criteriaClean / criteriaEvaluated`; never mix in untestable-by-definition criteria. Pairs with Q4-RULES-EVALUATED-COMPOSITE (closed) — same doctrine, different field.
- [x] **Q-SHARED-NO-CONFIG-WARNING-TINY-REPO** `warnings: ["no_config_found"]` fires on every tiny repo and every one-off demo — (50 standalone files each of ≤ 3 files), demo-size scans of external sub-trees, at-scale individual template dirs. `configSource: null` in `meta` already signals the same absence; the top-level warning is duplicative noise. Fix: drop the warning when `configSource: null` is already emitted AND either `filesScanned < 10` OR no `package.json` / `ra11y.config.*` exists anywhere in the searched directory walk. Alternative: demote to a `meta.configHint` prose field. Doctrine: warnings are codes for genuinely out-of-band signals; demo-size scan with no config is the normal case.
- [x] **Q-SHARED-ACTIVE-NATIVE-WRAPPERS-NULL-VS-EMPTY** (60332d9c) `activeNativeWrappers: null` on pure-HTML templates (catalog 5/5 subset) vs `activeNativeWrappers: []` on JSX projects with no detected wrappers vs `activeNativeWrappers: [{name,source,...}]` when populated — three shapes for "wrapper detection didn't find anything." The null-vs-[] distinction forces a two-read disambiguation ("is this feature unavailable, or did it run but return empty?"). Fix: omit the field entirely when native-wrapper detection is not applicable (non-JSX file set) — present-when-meaningful; ship `[]` when JSX files were scanned but zero wrappers detected; ship the tagged list when populated. Tiny. Pairs with Q3-OPAQUE-COMPONENTS-TOP-EMPTY-DISHONEST (closed).
- [x] **Q-SHARED-LIMIT-REQUEST-VS-EFFECTIVE** `limit: 100` (callers raise it deliberately to page fewer times) silently clipped to `effectiveLimit: 5` on a typical scan, `15` on an SSG docs-site scan, `1` at scale via the token-density cap. `effectiveLimit` is already on `meta` but buried; the structured signal is `warnings: ["response_token_budget_truncated"]` with no `(requested, effective, reasonCode)` payload. A caller asking for 100 and getting 5 has a 20× silent drop unless they know to inspect `meta.effectiveLimit`. Fix: (a) promote `effectiveLimit` to the same surface as `truncated`/`nextOffset` on every paginated response (scan, scan_project, review_candidates, checklist); (b) attach a `warningsDetails.response_token_budget_truncated: { requestedLimit, effectiveLimit, reason: "token_density" | "meta_array_size" | "per_file_cap" }` payload. Pairs with Q-SHARED-META-ARRAY-BUDGET-CAP (closed) and Q6-BUDGET-UNDER-VENDOR-NOISE (closed) — both close the *why*; this item closes *whether the caller notices*. Doctrine: "Zero-output success is ambiguous failure" — 20× silent reduction is its sibling.
- [x] **Q-SHARED-PLAN-SUMMARY-VERIFY-IN-SOURCE-INFLATION** `scan_project.plan.summary` prose still reads `"2273 violations (266 mechanical, 616 guidance, 57 runtime-only, 1334 verify-in-source)"` on a representative scan — 59% of the claimed "violations" total is verify-in-source (prose-only, not-yet-actionable). V1-SHAPE-FIXCLASS-HEADLINE (closed) broke out the structured `fixesByClass` lanes, but the legacy `summary` prose still sums all four lanes under the word "violations." Fix: `summary` prose reads `"N findings (mechanical: M, guidance: G, runtime-only: R, verify-in-source: V)"` — drop the word "violations" from the parenthetical headline when verify-in-source is non-zero, OR split into two summaries: `violationsSummary` (mechanical + guidance + runtime-only) and `reviewSummary` (verify-in-source). Pairs with Q6-PLAN-TOTALFINDINGS-VIOLATIONS-VS-NOTES (closed) and J-3 (open). Doctrine: composite headline counts are dishonest.
- [x] **Q-SHARED-SAFE-EDITS-VS-MECHANICAL-DISAGREEMENT** `plan.safeEditsAvailable: 14` vs `plan.fixesByClass.mechanical: 266` on the same scan response — two sibling counters both framed as "how many fixes an agent can apply" disagree 18×. Q3-PLAN-MECHANICAL-EDITS-COUNTER-DRIFT (closed) standardized `safeEditsAvailable` as "mechanical + verify-in-source" but `fixesByClass.mechanical` counts mechanical only — the names are confusing because "safe" sounds like "mechanical" to an agent reading the headline. Fix: rename `safeEditsAvailable` to `mechanicalPlusVerifyInSourceCount` (ugly but honest) OR drop it and let agents sum `fixesByClass.mechanical + fixesByClass.verifyInSource` themselves. Two sibling counters with overlapping semantics force the agent to pick one; the doctrine rule is that counters must count one kind. Doctrine: composite headline counts are dishonest.
- [x] **Q-SHARED-LIKELY-IRRELEVANT-IFRAME-NOT-MEDIA** `wcag22:1.2.1 / 1.2.3 / 1.2.5` candidates surface as ACTIONABLE (not `likelyIrrelevant`) on despite zero `<video>` / `<audio>` / `<track>` present. Root cause: the media finder classifies `<iframe>` as a media indicator, and `site/src/content/docs/helpers/ratio.mdx:14` has an `<iframe>`. Iframes are NOT prerecorded A/V per the SC definition. Fix: iframe presence should count as media ONLY when `src` matches the video-host allowlist (youtube/vimeo/wistia/loom/brightcove — same list as Q-SHARED-VIDEO-CAPTIONS-IFRAME-EMBEDS). Bare iframes don't promote 1.2.* out of `likelyIrrelevant`. Pairs with Q-SHARED-VIDEO-CAPTIONS-IFRAME-EMBEDS (closed) — same allowlist, inverse direction.
- [x] **Q-SHARED-FIXDESCREF-SAME-GROUP-INLINE-DEDUPE** Duplicate `fixDescriptionRef.hash` emitted inline on 6 adjacent same-rule findings that share a `groupKey` — a vanilla-JS `verify-account-ui/index.html` tree's `forms/labels-required` emits six `fixDescriptionRef: { hash: "2670e6e25ccd" }` rows on adjacent lines, all pointing at the identical dedupe-table entry. V1-REF-DEDUPE collapsed descriptions to the table, but the ref itself is still re-inlined per finding when the shared groupKey could carry it once. Fix: when ≥2 findings share the same `groupKey` AND `fixDescriptionRef.hash`, hoist the ref to the group level as `groupFixDescriptionRef` and omit the per-finding `fixDescriptionRef` on the siblings. Pairs with V1-REF-DEDUPE extension and with the per-file pattern rollup (V1-NOISE-RULE-PER-FILE-ROLLUP). Doctrine: verbose meta is signal, but repeated identical payload is bloat.

## Track Q4 — Static-site-generator field test


Owner: main session + general-purpose. Source: 10-agent parallel eval against a static-site-generator repo. Slices: `_layouts/`, `_includes/`, `pages/`, `_docs/` (alphabetical split), `_posts/` + `_tutorials/`, CSS/SCSS, raw `test/source/*.html`, `lib/` scaffold templates, Ruby source tree, and an MCP surface critique from a non-React-project angle.

A canonical static-site-generator tree: HTML templates with Liquid (`{% ... %}`, `{{ ... }}`), YAML frontmatter, `.md`/`.markdown` content, SCSS, ERB fixtures, Ruby source. Where Q3 probed the docs-site MDX/Astro axis, Q4 probes the Ruby/SSG/Liquid axis — the failure modes are different. Cross-referenced against Q3 to avoid duplication; items that overlap are tagged as such.

**MCP freshness:** one MCP subprocess spawned post-rebuild (pid 16243, started 23:07 vs dist rebuilt at 22:30), so findings reflect current bundle behavior — the Q3 stale-process confound does not apply here.

- [x] **Q4-LIQUID-ATTR-QUOTE-BUG** HTML parser's attribute-value tokenizer breaks on nested-quote Liquid. `lang="{{ site.lang | default: "en-US" }}"` tokenizes as `lang="{{ site.lang | default: "` — the inner `"` terminates the attribute value, leaving `en-US" }}"` as stray content. Triggers FALSE `parsing/html-has-lang` findings on every SSG scaffold template (`lib/blank_template/_layouts/default.html:2`, `lib/theme_template/...`) — every `jekyll new` site inherits the false positive. Fix: teach the attribute-value tokenizer to recognize `{{ ... }}` / `{% ... %}` spans and skip their contents when searching for the closing quote. Load-bearing: until this is fixed, every Liquid-templated HTML project gets confident wrong findings.
- [x] **Q4-LIQUID-TEXT-LITERAL** Rules that consume visible text (`semantics/label-in-name`, `semantics/list-structure`, `document/page-titled`, `navigation/link-descriptive-text`) treat Liquid template expressions as literal visible text, producing FALSE positives when the rendered output would be fine. Examples: `docs_contents_mobile.html:2` (Liquid `{% for %}` loop inside `<select>` tagged as the accessible-name string); `docs_contents.html:9` (`{% capture %}` block reinserts `<a>` inside `<li>` at a later line — `list-structure` sees naked `<a>`-in-`<ul>`). Fix: strip or placeholder-ize Liquid/ERB/Jinja directives in the parser's text-node output so rules operate on the rendered-text shape, not the source shape. Pair with a `template_directive_stripped` signal in `reason` text so an agent knows the check ran against the stripped text. Doctrine: not heuristic suppression — the rule's premise (visible text) is what's being computed, and Liquid directives are not visible text.
- [x] **Q4-SCSS-DISCOVERY-WIRE** Audited the full path — the wiring was already complete at the time the item was filed: `feat(input): route .scss through the scss parser end-to-end` (96620ec) landed all four plumbing pieces (`PARSEABLE_EXTENSIONS`, `parseForExtension` dispatch, `extensionMatches` `.scss → .css` alias, apply-fix `Ext` discriminator). Reproduced against current `main` via a direct `scan_file` handler call and an MCP-subprocess JSON-RPC round-trip: both parse `.scss` cleanly and fire `contrast/minimum` on authored Sass (ratio 4.48:1 flagged). `discoverFilesWithDiagnostics` accepts `.scss` and emits no `skippedByExtension` entry. The field report was observing a stale bundle despite the freshness check. Added two regression tests in `tests/unit/mcp/tools.test.ts` — one asserting `scan_file` on a `.scss` file parses end-to-end and fires `contrast/minimum` with `wcag22:1.4.3`, one asserting `scan_project` on a mixed `.scss`+`.css` directory counts every `.scss` under `filesByExtension` and does NOT report them as `skippedByExtension`. Future refactors that drop any link in the chain (discovery allow-list → parser dispatcher → rule eligibility) will trip the tripwires.
- [x] **Q4-MARKDOWN-SUPPORT** ADR-only phase done in ADR 0025 (4e0bc95); leaving item open for Q4-MARKDOWN-IMPL. `.md` (157) and `.markdown` (150) return `file-unsupported` on SSG trees — every `_docs/*.md`, `_posts/*.markdown`, `_tutorials/*.md`, `pages/*.md`, and `README.markdown` is invisible. Raw HTML embedded in markdown (tables, iframes, admonition divs, inline `<a>`/`<img>`) never reaches any rule. Options: (a) full CommonMark parser (large); (b) lightweight HTML-in-markdown extractor that strips `#`-headings + fenced-code blocks, feeds HTML-block residue to `parseHtml`, scans `![alt](url)` for alt-text, and maps kramdown IAL (`{: .note .warning}`) to `class=` on the preceding block. (b) is ~300 LOC, unlocks static-site-generator ecosystem (Jekyll, Hugo, MkDocs, Docusaurus, Astro content collections). Decision: explore (b); ADR before implementation.
- [x] **Q4-MARKDOWN-IMPL** Implement Option B per ADR 0025 — ~300 LOC HTML-residue extractor at src/input/parsers/markdown.ts, discovery wiring, analysisCoverage hint. Blocks on nothing. Medium. **Cross-ref (2026-04-21 design-system second-pass):** a design-system docs tree skipped 13 `.md` files as "extensions_skipped_no_parser" despite having no MDX/Liquid syntax — those are plain-HTML-in-markdown just like SSG `.md`. When implementing, consider a fast-path: route `.md` through the `.mdx` parser when no MDX syntax is detected (no `import`/`export`, no JSX element opens), emit `md_files_skipped_no_parser` warning if the parser declines. Same implementation; two caller sites validate it. (a1a0e3b+a7b169e)
- [x] **Q4-FRAGMENT-DETECTION** SSG `_includes/` partials (top.html, footer.html, header.html, error.html) are fragments — they have no `<html>`, no `<head>`, start mid-document with `{%- include ... -%}`. ra11y currently fires `document/page-titled`, `semantics/landmark-main`, `navigation/skip-link`, `parsing/html-has-lang` on these files as if they were full pages. Detect the fragment shape (file under `_includes/`, `_layouts/`, `components/`, or file without matching `<html>`/`<body>`/`<head>` structure) and suppress full-page-required rules with a structured `fragment_detected` signal (review-candidate, not suppression — the agent can still manually check that the composed layout has `<main>`). Alternative: emit an `analysisCoverage.fragmentFiles: [paths]` counter so the agent knows partials were scanned but page-level rules didn't apply. **Confirmation (2026-04-21 second pass):** 2.4.5 finder still flags page-titled sibling cases (Q4-PARTIAL-PAGE-TITLED already closed handles page-titled; the broader fragment-detection gap persists for landmark-main + skip-link). Still open.
- [x] **Q4-TEMPLATE-DIRECTIVE-CLASSIFIER-DRIFT** (dfd9ed5) Fixed in `src/mcp/analysis-coverage.ts` (agent confirmed real location; `src/input/analysis-coverage.ts` does not exist). Classifier now accumulates per-file family sets and performs per-file majority-vote rather than a global accumulation, eliminating cross-file tag bleed. All existing template tests pass; added targeted round-trip test for Liquid + Jinja coexistence. 107 lines changed.
- [x] **Q4-XMLLANG-LANG-MISMATCH** `test/source/_layouts/default.html:7` has `<html xmlns="..." xml:lang="en" lang="en-us">` — both present, differ (`en` vs `en-us`). No rule validates the consistency. Extend `document/lang-attribute` or add `document/lang-xmllang-mismatch`. WCAG 3.1.1.
- [x] **Q4-CROSS-INCLUDE-LANDMARK-COMPOSITION** SSG layouts compose as `top.html` (head + opening body) + `{{ content }}` + `footer.html`. Each file parses in isolation; `semantics/landmark-main` fires on `error.html` (no `<main>`) but stays silent on `default.html` even though the composed page also has no `<main>`. Two options: (a) teach the scanner to follow `{% include %}` directives for in-tree composition (complex, cross-file, may produce false positives); (b) document the cross-file gap in a `couldBeWrongBecause: ["partial_or_layout_file_requires_composed_check"]` on the relevant rules. (b) is the honest minimum; (a) is a larger design.
- [x] **Q4-LANGUAGE-CLASS-WARNING** No headline-level warning when `skippedByExtension` shows a content-language dominance that ra11y can't parse. An SSG scan drops 307 `.md`/`.markdown` files, 160 `.rb`, 18 `.scss` silently under a generic `extensions_skipped_no_parser` code — an agent skimming the top-level fields concludes "20 findings, scan had teeth" when the reality is "the actual content layer was never seen." Emit structured codes: `content_files_skipped: { count, exts }` when markdown/rst count > 50; `scss_files_skipped` until Q4-SCSS-DISCOVERY-WIRE lands; `source_language_unsupported: { language }` on detection of Ruby/Python/Go/PHP dominance. Doctrine: "Zero-output success is ambiguous failure" — sibling case for "near-zero output on the wrong-shape project." Note: `scss_files_skipped` code intentionally NOT added — `.scss` is in PARSEABLE_EXTENSIONS and never reaches `skippedByExtension`. (6431452)
- [x] **Q4-PROPOSE-CONFIG-ECOSYSTEM** (d8fdce5) `propose_config` emits a `@ra11y/core` TypeScript config into any project — including one whose root has `Gemfile` / `Pipfile` / `go.mod` and no `package.json`. No warning that the consumer may not want to add a Node toolchain. Fix: probe the repo root for ecosystem markers (`package.json`, `Gemfile`, `pyproject.toml`, `go.mod`, `Cargo.toml`) and either (a) emit `warnings: ["foreign_ecosystem_detected: ruby"]` alongside the config, (b) propose an alternative invocation (`npx @ra11y/core scan` rather than a committed `ra11y.config.ts`), or (c) ask the agent to confirm via `nextStep` before writing config. Do NOT suppress the config — the user may genuinely want to add it — but surface the context.
- [x] **Q4-BOOTSTRAP-CI-SNIPPET-ECOSYSTEM** `bootstrap` CI snippet hardcodes `actions/setup-node@v4` in the suggested workflow. For Ruby/Python/Go projects, the consumer's existing CI almost certainly uses `ruby/setup-ruby` / `actions/setup-python` / `actions/setup-go`. Two options: (a) detect the ecosystem and pick the matching setup action; (b) emit the snippet as additive — "add this job alongside your existing CI" — with a prose preface acknowledging the existing toolchain. (b) is less fragile. **Confirmation (2026-04-21 second pass):** still open; an SSG re-scan confirmed the `setup-node@v4` hardcode is what surfaces on a Ruby project.
- [x] **Q4-SSG-BUILD-HINT** (95c87a5) Detect static-site-generator signals at the repo root (`_config.yml` → Jekyll; `hugo.toml`/`config.toml` → Hugo; `astro.config.*` → Astro; `.eleventy.js` → Eleventy; `gatsby-config.*` → Gatsby; `mkdocs.yml` → MkDocs) and add a `nextStep` suggestion: "for full coverage, build the site and point `additionalPaths` at the rendered output (`_site/`, `public/`, `dist/`)." Structured hint: `meta.detectedFramework: { name, buildOutput, buildCommand }`. This is the discoverable answer to "what does ra11y CAN'T do on my static site." Pairs with existing `analysisCoverage.hints` prose.
- [x] **Q4-ADDITIONALPATHS-REDUNDANT** `scan_project` with `additionalPaths: ["docs/_includes"]` returns `additionalPathsScanned.filesAdded: 0` when those files were already discovered by default enumeration. No signal to the agent that the flag was redundant (not ignored — just redundant). Emit `warnings: ["redundant_additional_paths"]` when the requested paths' files are already in the default-discovered set. Companion to Q3-ADDITIONAL-PATHS-SKIP-REASON (caller asked for N, got M_new, (N - M_new) were already scanned or dropped — distinguish).
- [x] **Q4-RULE-IFRAME-TITLE** [dup-Q3-RULE-IFRAME-TITLE] `<iframe>` without `title` or with generic title (`"YouTube video"`, `"iframe"`). Evidence: `_tutorials/video-walkthroughs.md:10` + `_docs/assets.md`. WCAG 4.1.2 + 2.4.1. Roll into Q3 ticket.
- [x] **Q4-RULE-TABLE-CAPTION-SCOPE** [dup-Q3-RULE-TABLE-CAPTION + Q3-RULE-TABLE-TH-SCOPE] Table caption + th-scope. Evidence: `_docs/front-matter.md:51-200`, `_docs/permalinks.md:50-170`, `_docs/variables.md`, `_tutorials/csv-to-table.md:179-204` — repeated pattern across 20+ tables. Roll into Q3 tickets. Covered by Q3-RULE-TABLE-CAPTION + Q3-RULE-TABLE-TH-SCOPE (77db3b5); `src/rules/semantics/table-caption-missing.ts` + `src/rules/semantics/table-th-scope-missing.ts` evaluate HTML and fire on these tables through the shared HTML parser path. No SSG-specific work required.
- [x] **Q4-RULE-NOTE-ROLE-FROM-CLASS** New `aria/role-from-class-only`. Admonition patterns (`.note`, `.warning`, `.alert`, `.tip`, `.info`) — HTML `<div class="note warning">` or kramdown IAL `{: .note .warning}` — without `role="note"` / `role="alert"` / textual severity prefix. WCAG 1.3.3 + 1.4.1 + 4.1.2. SSG docs sites use this pattern ~30 times across `_docs/`, all invisible today (even without markdown support, the pattern will apply to every SSG). Distinct from Q3-RULE-COLOR-SOLE-INDICATOR which is about status-colored text anywhere — this is admonition-widget-specific.
- [x] **Q4-RULE-EXTERNAL-LINK-NEW-WINDOW** New `navigation/link-target-blank-announcement`. `<a target="_blank">` without `rel="noopener noreferrer"` + no new-window announcement (visible text, sr-only span, or `aria-label` containing "opens in new window"). WCAG 3.2.5. Evidence: `showcase.html:14` + `_tutorials/navigation.md` have the pattern. (713c7fe+a362448)
- [x] **Q4-RULE-DUPLICATE-LANDMARK-UNLABELED** New `semantics/duplicate-landmark-unlabeled`. Two or more `<nav>` / `<main>` / `<aside>` / `<form>` landmarks on a rendered page, none or fewer-than-N with `aria-label` / `aria-labelledby`. WCAG 1.3.1 + 2.4.1. Evidence: `header.html` ships two `<nav>` elements (mobile + desktop), neither labeled. Requires cross-file composition awareness (Q4-CROSS-INCLUDE-LANDMARK-COMPOSITION) to fully catch. **Confirmation (2026-04-21 second pass):** in-file single-nav variant also worth covering — `header.html` on the second pass surfaced a single unlabeled `<nav>` in a fragment file; widen the rule's fragment-file single-nav eligibility alongside the multi-nav case.
- [x] **Q4-RULE-SECTION-ACCESSIBLE-NAME** New `semantics/section-accessible-name-missing`. `<section>` used as a content wrapper without `aria-label` / `aria-labelledby` / enclosing heading. WCAG 1.3.1 / 4.1.2. `<section>` without name is semantically a generic `<div>` for AT. Low priority (many `<section>` usages are fine as-is per spec — only flag when section is a direct child of `<body>` or sibling to other landmarks).
- [x] **Q4-RULE-PLACEHOLDER-ALT** New `media/alt-text-placeholder`. `<img alt="...">` / `alt="image"` / `alt="screenshot"` / `alt="TODO"` / `alt="placeholder"` — generic alt that teaches bad patterns when copy-pasted. WCAG 1.1.1. Evidence: `_tutorials/` docs + `pages/showcase.html` + `_docs/posts.md` teach `![My helpful screenshot](...)` as canonical — docs propagate weak patterns to every user. (0177360)
- [x] **Q4-RULE-PAGINATION-GLYPH-NAME** New review finder `navigation/pagination-glyph-accessible-name`. Pagination controls using `«` / `»` / `‹` / `›` as sole link text without `aria-label` or sr-only expansion. WCAG 2.4.4 + 4.1.2. Evidence: `_docs/pagination.md:112-169` teaches the glyph pattern; similar patterns across design-system docs.
- [x] **Q4-RULE-DISABLED-LINK-SURROGATE** New `aria/disabled-link-surrogate`. `<span class="disabled">` / `<span class="previous">` used as a disabled control where the live state is a `<a>`. Without `aria-disabled="true"`, AT announces the span as static text, losing the disabled-state signal. WCAG 4.1.2.
- [x] **Q4-RULE-XMLLANG-LANG-MATCH** [cross-ref Q4-XMLLANG-LANG-MISMATCH] Same rule — the tightening doubles as a new rule if `document/lang-attribute` doesn't already cover `xml:lang`. (Redundant: `document/lang-attribute.ts` already reads `xml:lang`, validates BCP 47 agreement at line 69-85, emits a tailored violation when the two disagree; Q4-XMLLANG-LANG-MISMATCH already closed this gap)
- [x] **Q4-WARNING-DOWNGRADE-NOISE** (623c4c42) `warnings: ["template_files_parsed_as_literal"]` fires on every Liquid-templated scan — it's a constant on any Jekyll/Hugo/Eleventy repo. Consider demoting to a `meta.templateDirectivesFound` hint when no findings hit lines containing directives (i.e. the literal-parse didn't actually pollute results). Keep the warning when it matters; drop it when it's constant noise.
- [x] **Q4-RULES-EVALUATED-COMPOSITE** (bc29957) `rulesEvaluated: 52` reports total loaded rules, not rules-with-eligible-inputs. On a pure-HTML/SSG project, many React-specific rules show `filesEligible: 0` in `perRuleCoverage` — they didn't fire because they couldn't. Replace the single count with a split: `rulesEvaluated: { loaded, withEligibleInputs, fired }` so an agent sees "52 loaded, 18 had eligible inputs on this file set, 9 fired." Doctrine: "Composite headline counts are dishonest."
- [x] **Q4-PARTIAL-PAGE-TITLED** `document/page-titled` fires on `top.html` (a `<head>` fragment) because the rule sees `{% seo %}` as literal text, not a rendered `<title>`. Pairs with Q4-FRAGMENT-DETECTION — the rule should skip fragment files, or at minimum emit `couldBeWrongBecause: ["title_may_be_template_injected"]` when the file lacks `<html>`/`<body>`.
- [x] **Q4-PARSE-ERROR-DETAIL** `parse_errors_present` / `parseErrorFileCount: 1` surfaced on `docs/_includes/top.html` with no detail — which parser failed, where, why. Pair the top-level boolean with a structured `analysisCoverage.parseErrors: [{ path, parser, reason }]` so an agent has a fix-pivot beyond "one file didn't parse." Doctrine: verbose meta is scan-confidence signal; the bare boolean is the silent-failure shape. Distinct from Q3-PARSE-ERROR-FILE-CONTRADICTION (that item distinguishes total vs partial failure; this item populates the detail payload for both classes). Landed as `{ path, parser, reason }` entries on both existing buckets (`parseErrorFiles` invisible + `partialParseFiles` partial), always shipped when non-empty (no verboseMeta gate) — the split is the honest shape the prior Q3 work established; enriching both with the per-entry detail is the minimal additive fix.
- [x] **Q4-SCANNED-BUILD-ARTIFACTS-REASON** `warnings: ["scanned_build_artifacts_present"]` fires with paths but no per-path classification reason. Under the doctrine's "provable from code" bar (`ai-first-consumer.md` §labeled-buckets), the label has to survive inspection — `scannedBuildArtifacts` should be `[{ path, reason: "contains-data-url-gradient" | "minified" | "dist-path" | "hashed-filename" | "sourcemap-sibling" }]` so the agent can triage without reading each file. Pairs with Q-SHARED-BUILD-ARTIFACT-SASS-PARTIAL (that item fixes the classifier's predicate; this item makes the classifier's verdict inspectable). Structured over `warnings: string[]` — reason is a per-item payload, not a response-level code.
- [x] **Q4-REVIEW-1.4.5-SR-ONLY-SIBLING** `header.html:7` logo image flagged for `wcag22:1.4.5` (images of text) but the reason doesn't mention the `<span class="sr-only">` sibling inside the same `<a>`. The sibling is a strong dismissal signal: the image is the visual rendering of the text, and an SR-only text equivalent is already carried in the DOM. Extend the 1.4.5 finder to detect `sibling_sr_only_text_present` when the flagged image shares a parent with an element carrying `.sr-only` / `.visually-hidden` / `aria-label` on the parent anchor; enrich `reason` with "note: a visually-hidden text sibling (`.sr-only` span) is present — if the image is the textual logotype and the sibling is the SR-accessible equivalent, this is the documented logotype pattern." Reason-text enrichment only — candidate stays visible.
- [x] **Q4-REVIEW-SANITIZE-LIQUID-FINDERS** `showcase.html:16` 1.4.5 reason literally quotes the Liquid template expression `{{ entry.name }}` as the repeated surrounding text. Q4-LIQUID-TEXT-LITERAL stripped Liquid in the rule-side text-node paths, but the fix didn't propagate to `src/review/finders/**` — the same `template_directive_stripped` sanitizer must run on text the finders harvest for reason-building. Apply the existing stripper helper at the finder-side surrounding-text extraction sites; add a fixture under `tests/fixtures/real-world/liquid-review-finder/` with a `{{ entry.name }}` repetition asserting the reason shows the rendered placeholder form (or omits the directive entirely) rather than the source token.
- [x] **Q4-RELEVANCE-REASON-PARSE-COVERAGE-CAVEAT** `likelyIrrelevant` on 1.2.x criteria reports "no `<video>` or `<audio>` elements" as if the file set were fully inspected, when 307 `.md`/`.markdown` files were silently skipped. When `skippedByExtension` includes content extensions above a high-confidence threshold (`.md`, `.markdown`, `.rst`, `.adoc` — authored-content extensions that commonly embed media), enrich `relevanceReason` with a parse-coverage caveat: "note: X files in content extensions ({exts}) were skipped — if the site embeds `<video>` / `<audio>` in markdown content, the media check is under-covered." Doctrine: `likelyIrrelevant` is one of the few labels that must be provable from the code (`ai-first-consumer.md` §labeled-buckets); the bucket claim is only honest when the input set was actually inspected. Pairs with Q4-MARKDOWN-IMPL — once markdown parsing lands, the caveat vanishes automatically for those repos.
- [x] **Q4-TEMPLATE-CLASSIFIER-LIQUID-AS-MUSTACHE-SINGLE-FILE** Q4-TEMPLATE-DIRECTIVE-CLASSIFIER-DRIFT fixed the cross-file majority-vote drift, but a single-file variant remains: an SSG `_layouts/default.html` (pure Liquid with `{%- include -%}`, `{%- comment -%}`, `{{- content -}}`) is classified as `handlebars-or-mustache` because the per-file majority-vote drops `{%- ... -%}` Liquid-only tokens and counts `{{ ... }}` tokens — which the two families share. Fix: treat `{%- ... -%}` / `{% capture %}` / `{% if %}` as high-confidence Liquid-only evidence that decisively wins the per-file vote even when `{{ ... }}` tokens outnumber them. Pairs with Q3-TEMPLATE-DIRECTIVE-STARLIKE-MISDETECT (the Astro/YAML half). Doctrine: verbose meta integrity — wrong template-directive classification leads the agent to reach for the wrong strip rules downstream.
- [x] **Q4-WARNING-DETAILS-CROSS-SURFACE-UNIFY** `warningsDetails.extensions_skipped_no_parser` is populated on `coverage` responses but absent from `scan_project` responses — same warning code, two shapes. Website-template scans reproduce the same pattern for `scanned_build_artifacts_present`, `parse_errors_present` (populated on some surfaces, bare code elsewhere). Fix: ensure every response carrying `warnings: [code]` also carries `warningsDetails: { [code]: { ... } }` when details exist for that code; when no details exist, omit the key rather than shipping empty. Typed-test the cross-surface matrix: for each warning code, it must appear or not appear consistently across `scan_project` / `scan_file` / `coverage` / `checklist`. Doctrine: cross-surface consistency + present-when-meaningful.
- [x] **Q4-LABEL-IN-NAME-LIQUID-STRIP-MISSING** Q4-LIQUID-TEXT-LITERAL was marked closed but `semantics/label-in-name` specifically still quotes raw Liquid `{% for ... %}` as "visible text" on `docs_contents_mobile.html:2` (and two sibling files). Reach: the strip helper was applied to some rules but `label-in-name` never picked it up. Fix: audit every rule under `src/rules/semantics/**` + `src/rules/navigation/**` that harvests visible-text strings, ensure each calls the `stripTemplateDirectives` helper before comparing, and add a rule-inventory regression test that mounts a fixture with `{% for %}` / `{{ var }}` tokens and asserts NO rule emits a finding quoting the raw directive. Pair with Q4-REVIEW-SANITIZE-LIQUID-FINDERS (finder-side already tracked).
- [x] **Q4-SUGGEST-FIX-PROPAGATES-POISONED-VISIBLE-TEXT** `suggest_fix` primary proposal for `semantics/label-in-name` on `docs_contents_mobile` findings builds an `aria-label="{% for section in site.data.docs_nav %}..."` — pasting the raw Liquid template expression into a static ARIA attribute. If an agent applied this mechanically, the page would ship a broken accessible name on every render. Root cause: `suggest_fix` reads the rule's `visibleText` field, not a sanitized version. Fix: run `stripTemplateDirectives` on any text that `suggest_fix` interpolates into `newText`; when the result is empty or nonsense, fall through to a guidance-only suggestion with `fixClass: "guidance"`. Doctrine: verbose meta integrity — `suggest_fix.safety: "safe"` on a proposal that would break the page is a false safety claim.
- [x] **Q4-DOCUMENT-PAGE-TITLED-LIQUID-STRIP** `document/page-titled` fires "empty `<title>`" on `test/source/_layouts/default.html:10` where `<title>` contains `{{ page.title }}`. Q4-LABEL-IN-NAME-LIQUID-STRIP-MISSING (closed) audited `src/rules/semantics/**` + `src/rules/navigation/**`; `src/rules/document/**` was out of scope. Widen the audit to `document/*` rules that consume visible text (page-titled, lang-attribute's xml:lang cross-check, charset/viewport text — any rule whose predicate reads rendered-text content). Fix: run `stripTemplateDirectives` before the emptiness check; when post-strip text is empty AND `templateDirectivesFound` is non-empty on the file, emit a candidate (not a violation) with reason "title is template-interpolated — verify the rendered output carries a non-empty title." Satisfies wcag22:2.4.2.
- [x] **Q4-AUTOCOMPLETE-MISSING-IMPLICIT-BUTTON-TYPE** `forms/autocomplete-missing` fires on `<button>` elements missing `type="button"` in SSG-rendered layouts — the button is adjacent to form-like patterns but the rule conflates "form-adjacent control" with "form field." Fix: gate the rule on input-shaped elements (`<input>`, `<select>`, `<textarea>`); emit a sibling review candidate for `<button>` without explicit `type` inside a `<form>` under a distinct rule (`forms/button-type-explicit`) — two distinct bugs, two distinct rules, no cross-firing. Satisfies wcag22:1.3.5 + 3.3.2.
- [x] **Q4-LIQUID-ROOT-LAYOUT-PARSE-RECOVERY** `docs/_layouts/default.html` fails the HTML parser with "Stray closing tag at top level" because Liquid `{%- include top.html -%}` at line 1 wraps the opening `<html>` (which lives in `top.html`), so the literal top of the layout file is `<body>` + `</html>` without a matching open. `scan_file` on the file runs 64 rules with `fired: 0` (telemetry reads "clean" — dishonest because the DOM was truncated before rules ran). Every `jekyll new` site inherits this root-layout shape. Fix: when the HTML parser encounters a top-level stray closing tag AND the file's first non-whitespace line is a Liquid `{%- include ... -%}` directive (OR the file lives under `_layouts/`/`_includes/`), switch to fragment-mode parsing instead of triggering stray-tag recovery. Pair with a `partialParseFiles` entry noting the shape. Alternative: expose a `parseShape: "fragment-in-included-layout"` hint so downstream rules can adapt. Pairs with Q4-FRAGMENT-DETECTION (closed) and Q3-PARSE-ERROR-FILE-CONTRADICTION (closed). Satisfies wcag22 coverage: unblocks ~all rules on any Liquid-based root layout.
- [x] **Q4-LIQUID-LINE-DRIFT-SASS-SELECTOR** 2.3.1 flash-threshold candidate cites `docs/_sass/_docsearch.scss:119` for the `:valid ~ .searchbox__reset` animation selector, but the actual selector lives at line 133 — a 14-line drift. Verified by reading the file. The finder's selector-to-line mapping is picking the first token match rather than the structural selector match. Fix: walk the CSS AST by position, not by first-token regex; ensure the reported line is the opening line of the ruleset containing the flagged declaration. Add fixture `tests/fixtures/real-world/ssg-docsearch-scss-line-drift/` with sibling-selector ambiguity, asserting the finder points at the correct line. Pair with Q2R2-FORM-TIMING regression guard (similar "evidence line != finder-reported line" shape). Satisfies wcag22:2.3.1.
- [x] **Q4-AUTOCOMPLETE-1.3.5-EXCLUDE-SEARCH-INPUT** `wcag22:1.3.5 Input Purposes` finder fires on a site-search `<input>` (e.g. `docs/_includes/header.html` docsearch-input with `type="search"` + `aria-label="Search"`). WCAG 1.3.5 lists 53 Input Purposes — `name`, `email`, `tel`, `cc-number`, `bday`, etc. — "search" is explicitly NOT one of them. Fix: scope the finder out when ANY of (a) `type="search"` OR `role="searchbox"` OR (b) `name | id | aria-label` matches `/search|query|q$/i`. The autocomplete-token set doesn't apply to free-form query inputs; flagging them is spec-incorrect. Satisfies wcag22:1.3.5.
- [x] **Q4-HEADING-HIERARCHY-PARTIAL-ENRICH-REASON** `semantics/heading-hierarchy` fires 32× on `_docs/*.md` partials because each partial's first heading is `<h5>` while the composed layout supplies `<h1>` via `page.title` front-matter. Q4-FRAGMENT-DETECTION (closed) skips page-level rules on structural fragments, but heading-hierarchy's eligibility is "any file with ≥1 heading" regardless of fragment-ness. Fix: when the rule fires on a file whose first non-whitespace token is a Liquid/MDX template directive (OR the file lives under known partial paths `_docs/`, `_includes/`, `_layouts/`, `_posts/`, `_partials/`), enrich reason with "note: partial/layout file — heading hierarchy depends on the composed layout. Verify the rendered page has `<h1>` before acting." Reason-enrichment only — candidate stays in the primary list so the agent reads the composed layout. Pairs with Q4-PARTIAL-PAGE-TITLED (closed, `document/page-titled` variant) — same enrichment shape, different rule. Satisfies wcag22:1.3.1 + 2.4.6.
- [x] **Q4-SCAN-FILE-PARSE-ERROR-LIMITATIONS-FIELD** `scan_file` on a parse-error file (`docs/_layouts/default.html` per Q4-LIQUID-ROOT-LAYOUT-PARSE-RECOVERY above) returns `fired: 0`, `findings: []`, `warnings: [parse_errors_present]` — but no top-level signal that the scan was degraded. Agent has to cross-read `analysisCoverage.parseErrorFiles` to learn the DOM was truncated. Add a top-level `limitations: [{ reason: "parse_error" | "partial_parse", file, parser, detail }]` on any `scan_file` response where the parse failed or recovered partially — mirrors how runtime-only checks surface their limitation in other responses. Scan_project's per-file entries should carry the same per-file `limitations` when that file was parse-errored. Pairs with Q4-PARSE-ERROR-DETAIL (closed, populates the detail payload); this item surfaces the detail as a load-bearing top-level signal so `fired: 0` + `limitations: [...]` reads as "scan ran but output is degraded" not "scan ran clean."
- [x] **Q4-LIKELY-REDUCIBLE-SIBLING-IMAGE-DEDUPE** `a README with 10 sponsor logos` — ten `wcag22:1.4.5` candidates, one per sponsor logo (`<a><img alt="Sponsor N"/></a>`), each with identical "alt text repeated in surrounding text" shape. When ≥4 adjacent images share a structural pattern (same parent, same `<a><img/></a>` wrapping, alt-text differing only by enumerated tokens), emit ONE consolidated candidate with `siblingOccurrences: [{ line, alt, href }]` instead of N individual ones. Per surface-don't-suppress doctrine, this is honest pattern aggregation — the agent reads one candidate carrying the full sibling list, not N near-identical ones. Pairs with Q6-ICON-FONT-HIDDEN-GROUP-DEDUP-CLASS-PATTERN (closed, same shape, different rule) and Q6-PATTERN-FINGERPRINT-CROSS-TEMPLATE (open, cross-file variant). Satisfies wcag22:1.4.5.

## Track Q5 — Vanilla HTML/CSS/JS field test


Owner: main session + general-purpose. Source: first pass against a hand-authored vanilla-JS tree — 50 pedagogical vanilla `.html` + `.css` + `.js` bundles, no framework, no build step, no template directives. Exercises ra11y on the shape most "starter" tutorials propagate to learners; findings that stick here ship to every hobbyist project that copies these patterns. Triaged against CLAUDE.md §1 — all items are rule/finder correctness or new detection on patterns statically provable from the source.

Why this axis matters: most field tests so far (Q3 design-system, Q4 SSG, Q6 bulk templates) exercise framework-heavy or bundled shapes. A vanilla-JS tree is the control — clean, hand-authored, no build pipeline confounds. When a rule misfires here, the miss is on bare markup the author wrote.

- [x] **Q5-LABEL-ADJACENT-UNASSOCIATED** `src/rules/forms/labels-required.ts` fires on `<input id="length">` when a `<label>` sits one line above, but the `<label>` has no `for=` attribute. Pervasive pattern (≥21 instances across the repo). The rule's fix text ignores the existing adjacent label. Two paths: (a) widen `labels-required` to skip when an immediately-preceding `<label>` sibling is present (match browser-less-strict label association — the accessibility-inspector-friendly authoring pattern) and enrich `suggestion` to "add `for=\"<id>\"` to the adjacent label at line N"; (b) add a sibling rule `forms/label-adjacent-unassociated` firing specifically on the adjacent-but-no-for shape, with a mechanical fix that inserts `for="<id>"`. Prefer (b) — the fix is mechanical, and the existing rule's "no label at all" reason should stay clean for the truly-missing case. Satisfies wcag22:1.3.1 + 3.3.2 + 4.1.2.
- [x] **Q5-BUTTON-NAME-ICON-GLYPH-MAP** `src/rules/semantics/button-name.ts` correctly fires on `<button><i class="fa-*"></i></button>` but the fix text says "add visible text" as the primary. When the only child is `<i class="fa-X">` with a well-known glyph name, the primary fix should be `aria-label="<derived-from-glyph-name>"` (small glyph→label map: `fa-bars` → "Menu", `fa-times`/`fa-xmark` → "Close", `fa-arrow-left` → "Previous", `fa-arrow-right` → "Next", `fa-search`/`fa-magnifying-glass` → "Search", `fa-bell` → "Notifications", `fa-user` → "Account"); secondary stays "add visible text." ≥5 instances in typical scans; the same pattern hits every icon-framework-using project. Extends V1-FIX-BUTTON-NAME with the Font Awesome glyph lookup. Satisfies wcag22:4.1.2. (db7a1c0)
- [x] **Q5-CAROUSEL-SETINTERVAL-PAUSE-MISSING** `image-carousel/script.js:9` has `setInterval(run, 2000)` with no pause UI anywhere in the DOM or script body — the candidate surfaces under `wcag22:2.2.1` with a generic reason. When the setInterval callback mutates DOM state (style/class/attribute change on a known-visual target), escalate the reason to cite `wcag22:2.2.2` "Pause, Stop, Hide" and name the missing pause UI. Reason-text enrichment only — candidate stays in the primary list; agent reads and decides. Pairs with Q3-RULE-PAUSE-STOP-INLINE-STYLE and Q3-FINDER-CAROUSEL-PATTERN. Satisfies wcag22:2.2.1 + 2.2.2.
- [x] **Q5-SETINTERVAL-SHORT-DURATION-SEIZURE** `blurry-loading/script.js:6` has `setInterval(blurring, 30)` — ~33Hz visual-state mutation. The existing 2.2.1 finder doesn't connect fast intervals to `wcag22:2.3.1` (Three Flashes or Below Threshold). Enrich the finder's `reason` when the callback mutates a visual property (class/style affecting opacity, transform, filter, color, background) AND the duration literal is below 333ms: "note: callback runs at ~{rate}Hz — if the visual effect is a flash/blink, verify against SC 2.3.1 (must not flash more than 3× per second)." Additive context — no threshold-based candidate filtering (doctrine: numeric thresholds are suppression). The duration/rate goes in the reason text as evidence for the agent; the candidate always surfaces. Pairs with R-2.3.1-FLASHING (closed) — that finder already exists; this is integrating the timing-finder output with the flashing-finder criterion so one callback-with-short-interval finding carries both SCs when evidence supports it.
- [x] **Q5-LANDMARK-MAIN-HEADING-UNDERCOUNT-AUDIT** 52 files with `<!DOCTYPE html>` + `<body>` structure produced only 1 `semantics/landmark-main` finding and 6 `heading-hierarchy` findings. Suggests over-permissive fragment-or-layout heuristic in the rule's eligibility predicate. Audit `src/rules/semantics/landmark-main.ts` + `src/rules/semantics/heading-hierarchy.ts` against these files: compare which files are considered "full pages" vs "fragments/layouts" and whether the distinction matches the DOCTYPE + html/body presence. If files with a clear full-page shape are being skipped, tighten the fragment heuristic to require the fragment file to genuinely lack `<html>`/`<body>` (not merely look small). Add 3-4 fixtures under `tests/fixtures/real-world/50p-vanilla-doctype/` covering the actual-shape distribution. Candidate root-cause for the scan looking "clean" when real violations are present. (f5fa5a0 — fixture RED by design per CLAUDE.md §7; rule tightening in looksLikeFullPage is follow-up work)
- [x] **Q5-LANDMARK-FULLPAGE-HEURISTIC-TIGHTEN** Follow-up to Q5-LANDMARK-MAIN-HEADING-UNDERCOUNT-AUDIT. `src/rules/semantics/landmark-main.ts::looksLikeFullPage` currently gates on presence of `<header>`/`<nav>`/`<footer>`/`<aside>`; a file with `<!DOCTYPE html>` + `<html>` + `<body>` (no other landmarks) is mis-classified as a fragment and the rule never fires. The naive fix (accept DOCTYPE + `<html>` + `<body>` as sufficient full-page evidence) was tried inline and reverted — it broke 8 independent tests across integration/CLI/MCP/bootstrap test suites because ~30 good-path HTML fixtures under `tests/fixtures/good/**/*.html` share that minimal full-page shape without a `<main>` landmark (they were authored under the old heuristic). A correct tightening needs a stronger signal than DOCTYPE+html+body alone — candidates: body descendant count (≥N interactive/content elements), form/interactive-element density, multiple-sibling structural content, or a content-vs-fixture classifier that reads file size / script count / title presence. Whatever heuristic is chosen, budget for either (a) updating ~30 good-path fixtures to add `<main>` wrappers, or (b) passing a "fixture mode" flag from the real-world harness that skips the rule. Evidence fixtures at `tests/fixtures/real-world/50p-vanilla-doctype/` (f5fa5a0); the failing assertion there is currently COMMENTED OUT pending this design decision. WCAG 1.3.1 / 2.4.1.
- [x] **Q5-RULE-ICON-FONT-ARIA-HIDDEN** New `aria/icon-font-hidden` rule (note severity). `<i class="fas fa-arrow-left">` / `<i class="fa-*">` / `<span class="material-icons">` inside a labeled parent (button with aria-label, link with text sibling) should carry `aria-hidden="true"` to avoid AT double-announcement. ~10+ instances in typical scans; same pattern across every icon-font project (Font Awesome, Material Icons, Bootstrap Icons, Ionicons). Satisfies wcag22:4.1.2 + 1.3.1. Distinct from button-name / link-descriptive-text — those fire when the parent has no name at all; this rule fires when the parent is named AND the icon child is unannotated. Fixtures under `tests/fixtures/{good,bad}/icon-font-hidden/`. Mechanical fix. (b704a95+e24f64f+d0776c5+1deeab0)
- [x] **Q5-NAV-ROLE-SINGLE-PAGE-REASON** `wcag22:2.4.5 Multiple Ways` fires on every single-page HTML fragment with an identical boilerplate reason. The spec scopes 2.4.5 to "sets of Web pages" — single-page standalone files are a documented exception. Tighten the finder's `reason` when exactly one HTML file is being scanned OR the scan root has no multi-page signals (no router config, no `pages/` directory, no `<a href>` chain into sibling HTML files): "note: SC 2.4.5 applies to sets of Web pages — if this is a single-page app or standalone file, the criterion may not apply; verify whether the scanned file is part of a multi-page set." Reason-text enrichment only — candidate stays visible per surface-don't-suppress. Distinct from Q4-FRAGMENT-DETECTION (that item is about partials/layouts within a larger template system; this item is about standalone single-file HTML projects).
- [x] **Q5-RADIO-GROUP-NAKED-NAME-SHARING** (already covered by Q3-RULE-RADIO-GROUP-FIELDSET, 5777e89+27083fc) Verified no-op — `forms/radio-group-without-fieldset` already fires on naked-name-sharing via `summarizeKinds` → `no-wrapper`. Unit test (`tests/unit/rules/forms/radio-group-without-fieldset.test.ts:7-24`) + bad fixture (`no-wrapper.html`) + real-world fixture (`tests/fixtures/real-world/50p-radio-group-naked-name-sharing/`, commit 1c37465) all green today; the signal that motivated this item was stale MCP subprocess state, not a rule gap. No widen needed.
- [x] **Q5-OTP-INPUT-CLUSTER-DETECTION** `verify-account-ui/index.html` has a cluster of 6 `<input type="number" maxlength="1">` for a 6-digit OTP; `forms/labels-required` fires 6× individually but nothing detects the cluster pattern. New review finder `forms/otp-input-cluster`: detect ≥4 sibling single-character inputs (`type="number"`/`type="text"` with `maxlength="1"` or similar) within a common parent; recommend `autocomplete="one-time-code"` on at least one, plus a single group-level `<label>` / `aria-labelledby` covering the cluster. Candidate, not assertion — cluster heuristic may false-positive on non-OTP patterns; agent reads surrounding code to verify. WCAG 1.3.1 + 1.3.5 + 3.3.2.
- [x] **Q5-DUPLICATE-LINK-TEXT-SAME-HREF** `split-landing-page/index.html` has two `<a>Buy Now</a>` both targeting `href="#"` with no distinguishing accessible name. `navigation/link-descriptive-text` fires on keyword-literal generic phrases but not on same-accessible-name-on-≥2-links. Extend the rule with a same-page pass: when ≥2 anchors share the same normalized accessible name and share the same `href`, flag all occurrences with reason "N links on this page use the same accessible name and href; a screen reader user navigating by link list cannot distinguish them." WCAG 2.4.4 + 2.4.9 (AAA). Reason-enrichment path if the hrefs differ but the names match: separate candidate tier (same-name-different-href is lower-confidence — legitimate pattern in some nav designs).
- [x] **Q5-DECORATIVE-IMG-WITH-ADJACENT-MEANING** `feedback-ui-design/index.html` has emoji images marked `alt=""` adjacent to `<small>Unhappy</small>` / `<small>Happy</small>` — the image IS the visual indicator of the named affect, so `alt=""` removes meaning the sibling text doesn't replace (the text names the affect; the image shows the affect). Distinct from 1.4.5 images-of-text. New finder `media/decorative-img-with-adjacent-meaning`: when an `<img alt="">` is the sole image child of a parent that also contains meaning-bearing text referring to an affect/status/value (via a short dictionary: "happy", "sad", "warning", "success", etc.), surface as a review candidate — "image marked decorative but an adjacent sibling names the visual's meaning; verify the image is truly redundant or populate `alt=`." WCAG 1.1.1. (committed d337137b)
- [x] **Q5-NUMERIC-ONLY-LABEL-CONTENT** `custom-range-slider/index.html` has `<label for="range">50</label>` — `forms/non-empty-label` passes because the label has text content, but "50" is a value display, not a semantic name. Enrich the rule's reason when the label's `textContent` matches a value-shape regex (`^\d+(\.\d+)?$` / currency / percentage / date / time patterns): "label text `{text}` looks like a value, not a name — verify whether this element labels the control or displays its current value; a labelling element should name what the control controls." Reason-text enrichment only — candidate stays in the primary list (the distinction is context-dependent; agent reads surrounding code). WCAG 2.4.6 + 3.3.2.
- [x] **Q5-KEYBOARD-HANDLER-EXTERNAL-JS-ADDEVENTLISTENER** (5da32248) `keyboard/handler-missing` never fires when click handlers live in external `.js` files — the rule only inspects inline `onClick=` on HTML elements. Reproduced across expanding-cards (`script.js:4` attaches click to 5 `<div class="panel">`, zero findings despite `rulesEvaluated.fired: 2` showing the rule ran), drag-n-drop, drawing-app, button-ripple-effect. Biggest single coverage hole on vanilla-JS trees. Fix: extend the rule to detect `.addEventListener('click', …)` and `element.onclick = …` patterns on JS/TSX AST that are NOT paired with a sibling `keydown`/`keyup` listener on the same target, emitting the candidate on the element selector resolved in the HTML input (match by `id=`/`class=` to the query-selector string) or on the JS line when HTML resolution fails. Pair with Q3-RULE-KEYBOARD-HANDLER-ATTR (closed, `data-bs-toggle`/inline `onclick=`) — same rule, widened substrate. Satisfies wcag22:2.1.1. Cross-refs "Needs judgment" J-7 — this is the static-analysis path (matching handler presence on the same file), not the full cross-file identifier resolution. Doctrine: detect what's statically provable; let the agent resolve what isn't.
- [x] **Q5-POINTER-GESTURES-SUBSTRING-FALSE-POSITIVE** `wcag22:2.5.1` finder flags `const panels = …` because the identifier "panels" contains the substring "pan" (treated as pan-gesture evidence). Classic identifier-substring match without word-boundary or richer signal. Fix: require word-boundary match (`/\bpan(?:ning|Gesture)?\b/i`) AND a companion signal — a `touchstart`/`touchmove`/`pointermove` listener OR a pointer-event library import (hammer.js, use-gesture, @use-gesture). When neither signal is present, the identifier match alone is insufficient evidence; drop the candidate. Reproduced on a vanilla-JS expanding-cards demo. Satisfies wcag22:2.5.1.
- [x] **Q5-RULE-CUSTOM-WIDGET-KEYBOARD-INOPERABLE** New rule `keyboard/widget-inoperable`. `<canvas>` / `<div>` / `<span>` elements with click handlers but no `role`, no `tabindex`, no keyboard handler. drawing-app's `<canvas id="canvas">` plus sibling `#decrease`/`#size`/`#clear` buttons — ra11y flags the canvas for `media/alt-text-missing` but misses the widget-level keyboard-inoperability. Fix: rule fires when (a) element is a generic/presentational tag (`div`/`span`/`canvas`/`p`/`section`/`header`/`footer`) AND (b) rule-local evidence shows an attached click handler (inline or detected via Q5-KEYBOARD-HANDLER-EXTERNAL-JS-ADDEVENTLISTENER pass) AND (c) no `tabindex` AND (d) no `role` that implies interactivity (`button`, `link`, `checkbox`, `tab`, `menuitem`) AND (e) no paired keyboard handler. Mechanical fix: "add `role=\"button\"` + `tabindex=\"0\"` + `onKeyDown` that invokes the same click handler, OR replace with `<button type=\"button\">`." Satisfies wcag22:2.1.1 + 4.1.2. Pairs with Q5-KEYBOARD-HANDLER-EXTERNAL-JS-ADDEVENTLISTENER (prerequisite). **Superseded by Q5-KEYBOARD-HANDLER-EXTERNAL-JS-ADDEVENTLISTENER (5da32248).** Scope-diagnosis confirmed `keyboard/handler-missing` already fires on every generic/presentational tag in the enumerated set, on both inline and external-JS click handlers, and crucially does NOT exempt on role or tabindex presence (line 186 of `src/rules/keyboard/handler-missing.ts` documents this explicitly). Implementing widget-inoperable as a new rule would either duplicate handler-missing or silently narrow coverage by requiring both role and tabindex to be absent — the latter a silent-miss per AI-first doctrine. Residual delta is fix-suggestion wording (generic-rendering-tag-aware enrichment inside `handler-missing`'s `buildSuggestion`) — tracked separately under Q5-HANDLER-MISSING-GENERIC-TAG-SUGGESTION below.
- [x] **Q5-FOCUS-OUTLINE-VISIBLE-BUTTON-SEVERITY** `focus/outline-visible` fires on 3d-boxes `.magic:focus { outline: none }` at `severity: "info"`, `confidence: "low"` — even though `.magic` selects a real `<button>` element, not a class that might apply to non-interactive wrappers. Bare `outline: none` on a `<button>` selector without a replacement focus indicator in the same block is a high-confidence 2.4.7 violation. Fix: upgrade severity to `"error"` + confidence to `"high"` when (a) the selector resolves to a concrete interactive element (`button`, `a[href]`, `input`, `select`, `textarea`, `[role="button"]`) in the scanned HTML AND (b) the same CSS block contains no `outline:` replacement declaration AND (c) no sibling selector in the same stylesheet adds `box-shadow`/`border`/`text-decoration` as a focus indicator. Reproduced on button-ripple-effect (correct severity) vs 3d-boxes (low-info); the difference is whether Tailwind focus-ring cross-reference fired (on this tree it didn't). Satisfies wcag22:2.4.7.
- [x] **Q5-HANDLER-MISSING-GENERIC-TAG-SUGGESTION** Refinement of `keyboard/handler-missing`'s `buildSuggestion`/`buildAttributeSuggestion` for generic rendering tags (`canvas`, `section`, `header`, `footer`, `p`). Current suggestion text is `<button>`-centric ("change to `<button type=\"button\">`") — accurate for `<div>`/`<span>` but awkward for `<canvas>` (the rendering surface is the point, not the button-ness) or for structural landmarks where the right answer is to move the handler to a child button. Add a tag-specific branch: for `canvas`, suggest wrapping a child `<button>` or adding `role="button"` + `tabindex="0"` + `onKeyDown`; for `section`/`header`/`footer`, suggest moving the click handler to a child interactive element rather than making the landmark itself a widget. Mechanical refinement; satisfies wcag22:2.1.1 follow-up. Spawned from the Q5-RULE-CUSTOM-WIDGET-KEYBOARD-INOPERABLE scope-diagnosis.
- [x] **Q5-HEADLINE-COUNT-DRIFT-THREE-TOTALS** Three different violation totals from one vanilla-JS scan: `plan.violations: 222`, `sum(perRuleCoverage.findingsEmitted): 242`, `sum(paginated files[*].findings) across all offsets: 227`. The delta `242 − 222 = 20` matches `plan.notes: 20` (notes excluded from plan.violations but included in perRuleCoverage), but 227 from paginated files reconciles with neither. Likely: `groupKey` dedup is applied in `plan.violations`, not applied in `perRuleCoverage.findingsEmitted`, partially applied in the paginated file entries. Fix: pick ONE canonical count for violations and document the delta at the response shape level — `plan.violationsCanonical` (one number) + sibling `meta.countsBySurface: { plan, perRuleCoverage, paginated }` when they disagree (tripwire for future drift). Add cross-surface invariant test: on a non-truncated scan, `sum(files[*].findings.length)` equals `plan.violations + (plan.notes ?? 0)` minus a documented dedup delta (or zero). Pairs with Q-SHARED-RULES-EVALUATED-SSOT (closed) — same cross-surface-consistency doctrine, different counter.
- [x] **Q5-PAGINATION-SOFT-CAP-REASON** `limit: 100` on returns 29 files at `offset: 25` (nextOffset 54), then 24 files at `offset: 54` (nextOffset 78), then 2 files at `offset: 78` (no `truncated` field on the last page → implicitly false). Soft cap below requested limit on every page with no structured reason code. Either the density cap is per-page (should emit `effectiveLimit` + `response_token_budget_truncated` on every clipped page) or the page was bounded by available files-at-that-offset (should emit an `exhaustedAtOffset` signal). Fix: every paginated response carries `effectiveLimit` on every page, AND a `pageClipReason: "token_density" | "end_of_results" | "per_criterion_cap"` when the returned count is less than the requested limit. Pairs with Q-SHARED-LIMIT-REQUEST-VS-EFFECTIVE (open cross-repo companion) — same honesty shape, explicitly per-page.
- [x] **Q5-COVERAGE-CONFIDENCE-HONESTY-CROSS-FILE-BLINDSPOT** `keyboard/handler-missing` still reports `coverageConfidence: "high"` on HTML files even though the rule architecturally cannot resolve cross-file `addEventListener` wiring (inline-only for HTML substrate). Q5-KEYBOARD-HANDLER-EXTERNAL-JS-ADDEVENTLISTENER (closed) widened detection on the JS side, but the HTML-scoped `scan_file` still claims "high coverage" when the evidence-gathering is bounded by single-file substrate. Fix: every rule whose evaluation scope is "one file" but whose target spec spans cross-file wiring must downgrade `coverageConfidence` to `"medium"` on HTML-only scans with `reason: "cross_file_listener_resolution_limited_on_this_input"`. Audit: `keyboard/handler-missing`, `aria/labelledby-target-exists`, `navigation/skip-link-target-valid`, `forms/error-message-not-associated`, `pointer/drag-alternative` — any rule whose predicate could match on evidence in a sibling file we didn't walk in THIS call. Pairs with J-7 (open doctrine decision about cross-file identifier resolution). Doctrine: coverage confidence is scan-confidence telemetry — dishonest when the rule can't see the evidence.
- [x] **Q5-TYPE-SMITH-CROSS-FILE-CONFIDENCE-PRIMITIVE** Widen `PerRuleCoverage.coverageConfidence` to `'high' | 'medium' | 'low'` and add `Rule.crossFileCapable?: boolean`. Type-smith-only change; no finding-emission logic. Unblocks Q5-COVERAGE-CONFIDENCE-HONESTY-CROSS-FILE-BLINDSPOT. Widen the union, update `buildPerRuleCoverage` builders + `ruleCoverage` derivatives + any tests hardcoding the literal union. Then Q5-COVERAGE-CONFIDENCE-HONESTY-CROSS-FILE-BLINDSPOT can re-dispatch to apply the flag + gate the downgrade. (50d1ed62)
- [x] **Q5-MOTION-PAUSE-STOP-SC-23-VS-22-CONFLATION** `motion/pause-stop-hide` emits findings under `wcag22:2.2.2` on vanilla HTML/JS projects, but all 28 hits are CSS `transition:` declarations inside `:hover`/`:focus`/`:active` pseudo-class contexts (user-triggered motion) — the correct SC is `wcag22:2.3.3 Animation from Interactions` (AAA). 2.2.2 targets auto-updating content (`setInterval`, `<marquee>`, auto-advancing carousel); 2.3.3 targets animation from user interactions. Fix: differentiate at rule-evaluation time. Build two variants on the same rule family: (a) emit under 2.2.2 when the animation trigger is timer-based OR scroll-based OR auto-playing (`setInterval`, `animation:` keyframes with no user-trigger selector, `data-bs-ride="carousel"`); (b) emit under 2.3.3 when the trigger is a user pseudo-class (`:hover`, `:focus`, `:active`) OR a user-event JS handler. Each finding carries a single `satisfies` criterion matching the trigger. Pairs with J-1 (open doctrine decision about signal-vs-noise aggregation) — the spec-correctness fix is independent of the aggregation question. Satisfies wcag22:2.2.2 + 2.3.3 (separate lanes).
- [x] **Q5-LANDMARK-MAIN-INCONSISTENT-PER-FILE** `semantics/landmark-main` fires on `drink-water/index.html` (no `<main>`) but does NOT fire on `expanding-cards/index.html` (also no `<main>`, same DOCTYPE+html+body shape) on the same scan. Predicate is inconsistent — some eligibility short-circuit fires on one file but not the other. Audit `src/rules/semantics/landmark-main.ts::looksLikeFullPage` with both files as real-world fixtures; the inconsistency suggests a silent fall-through based on presence/absence of some other landmark the predicate treats as "already has landmarks, skip the main check" gate. Pairs with Q5-LANDMARK-FULLPAGE-HEURISTIC-TIGHTEN (open) — likely the same heuristic tightening closes both. Add fixture `tests/fixtures/real-world/50p-landmark-main-per-file-inconsistency/` with both files asserting the rule fires on both OR on neither.
- [x] **Q5-DRAG-ALTERNATIVE-KEYBOARD-COMPOSE** `pointer/drag-alternative` fires on `<div draggable="true">` in `drag-n-drop/index.html:11` but does NOT compose with `keyboard/handler-missing` for the same element. Both rules apply: 2.5.7 (drag alternative) AND 2.1.1 (keyboard accessible). Two honest findings; currently only one. Investigate whether the rule-runner dedupes findings from different rules on the same element (probably doesn't — verify) OR whether `keyboard/handler-missing`'s eligibility predicate short-circuits when `draggable="true"` is present (probably does — tighten). Fix: draggable elements without keyboard wiring fire BOTH rules; the agent sees two distinct criteria mapped to one element and two distinct fix paths. Satisfies wcag22:2.1.1 + 2.5.7.

## Track Q6 — Bulk-template catalog field test


Owner: main session + general-purpose. Source: first pass against a 40+ bulk-template tree. Each template is a pre-built marketing/landing-page bundle shipping jQuery-era vendor CSS/JS (bootstrap.css, font-awesome.css, jquery.fancybox.pack.js) alongside authored HTML. Exercises ra11y at scale — the scan hit the MCP token ceiling, surfaced several rule misfires against vendor bundles, and exposed at-scale quality gaps in the proposed config + build-artifact warning path.

Why this axis matters: a bulk template catalog is the "buy a template kit" shape that a huge fraction of small-business and freelance sites ship. The vendor-CSS noise ratio and jQuery-era link antipatterns (`href="javascript:void(0)"`, icon-only social links) are not framework-authoring problems — they are the default ra11y-on-real-purchased-templates experience. When the token budget overruns, legitimate findings never reach the agent.

Three cross-repo items moved out: `PROPOSE_CONFIG_EMITS_ABSOLUTE_PATHS_AND_TRAILING_COMMA` folds into `Q-SHARED-PROPOSE-CONFIG-RELATIVE-PATHS` (Track Q3 cross-repo section). Two Q6 items consolidate under one roof each: Q6-1 + Q6-13 → `Q6-BUDGET-UNDER-VENDOR-NOISE`; Q6-10 + Q6-11 + SSG-4 share scope with Q4-SCANNED-BUILD-ARTIFACTS-REASON (cross-refs inline).

- [x] **Q6-BUDGET-UNDER-VENDOR-NOISE** Default `limit: 25` collapsed to `effectiveLimit: 1` via the density cap when vendor files dominated — `bootstrap.css` emitted 5,458 findings and `font-awesome.css` emitted 8,940 on a single scan, exhausting the 88K-char budget on one file. Two compounding gaps: (a) the token-density cap (V1-SIZE-RESPONSE-BUDGET-DENSITY-2) held the envelope but starved the response to one file's worth of findings; (b) a single criterion can carry 200+ review candidates on one file (see Q6-MEANINGFUL-SEQUENCE-UTILITY-CLASSES — `.order-0..13` utilities flagged 200+ times). Two additive fixes: per-file finding cap (hard limit ≈100 findings per file per response, with `perFileTruncated: true` + `findingsTotalInFile` on the file's entry) to prevent one file hoarding the budget; per-criterion byte cap on checklist responses (clip to first 3 candidates per criterion when response bytes exceed threshold, with `perCriterionClipped: true`). Both caps are additive labels — findings are not suppressed, they are paginated per scope. Consolidates Q6-1 + Q6-13. Pairs with `scanned_build_artifacts_present` — vendor bundles drive the budget overrun. Doctrine: pagination is honest; suppression is not.
- [x] **Q6-PLAN-TOTALFINDINGS-VIOLATIONS-VS-NOTES** `plan.totalFindings: 24772` buries `notes: 1079` under the same counter (violations: 23693). Composite headline count per `ai-first-consumer.md` §composite-headline-counts. Either rename to `plan.totalFindingsComposite` with a clearer breakdown elsewhere, or split into `plan.violationsCount` + `plan.notesCount` at the same level (matches the shape `ScanSubset` already carries post-V1-COMP-BOOTSTRAP-SCAN-TOTAL). The `--format agent` CLI summary already breaks out "N violations + M notes"; align the MCP plan counter with the same split.
- [x] **Q6-CHECKLIST-IFRAME-FINDER-JS-STRING-LITERALS** Review candidates for 1.2.x / 4.1.2 iframe criteria include `jquery.fancybox.pack.js:4` with snippet `"<iframe id=\"fancybox-frame{rnd}\"..."`. The iframe-review finder line-scans `.js` files as if the literal string contents were HTML elements on the page. Restrict the finder to HTML-family inputs: `.html` / `.htm` / `.jsx` / `.tsx` / `.vue` / `.svelte` (the files ra11y actually parses as DOM). JS string literals containing `<iframe>` are not rendered iframes — they are library code building DOM at runtime; the agent reads the surrounding file to verify. Mirrors the TSX-text-corpus fix in V1-FP-IMAGES-OF-TEXT-SVG-DATA-URI's shape: the text-matching pass must run on DOM-origin text only, not on string-literal payloads inside source code.
- [x] **Q6-MEANINGFUL-SEQUENCE-UTILITY-CLASSES** 1.3.2 review candidates fire on every `.order-0` .. `.order-13` utility-class selector in `bootstrap.css` / authored compiled CSS — 14 selectors × ~40 templates = ~560 candidates per scan, all pointing at the same utility definitions rather than at any consumer. Finder must operate on HTML/JSX *consumers* that apply `class="order-N"`, not on the CSS selector definition site. Tighten: lift the 1.3.2 finder from the CSS-selector scope to the HTML/JSX usage scope (match `class="...order-N..."` / `className="...order-N..."` on elements); silence on the CSS definition when no consumer in the scan applies the class. When the finder *does* hit a CSS-only definition, enrich the reason with "note: utility-class definition; verify consumers apply this class intentionally." Doctrine: the finder's premise (sequence-dependent visual order) is real; the current location (CSS definition) is wrong.
- [x] **Q6-RULE-HREF-JAVASCRIPT-VOID** New `navigation/href-javascript-void` rule. 120 occurrences of `<a href="javascript:void(0)">` across 10 sampled files; zero findings today. Covers `href="javascript:void(0)"`, `href="javascript:;"`, and `href="#"` when no click-handler delegation is detectable (e.g. no matching jQuery `.on("click")` selector, no event delegation parent). All three forms are `<button>` roles masquerading as links; AT announces "link," keyboard Tab lands with no navigation. Satisfies wcag22:4.1.2 + 2.1.1. Mechanical fix: "change `<a href=\"javascript:void(0)\">` to `<button type=\"button\">` — this control mutates state, not navigates." Fixtures under `tests/fixtures/{good,bad}/href-javascript-void/`. (0b942fc)
- [x] **Q6-LINK-ICON-ONLY-ACCNAME** `src/rules/navigation/link-descriptive-text.ts` does not flag `<a><i class="fa fa-twitter"></i></a>` — the anchor has no text, no `aria-label`, no labeling child. Extend the rule to compute the full accessible name: ignore presentational descendants (`<i class="fa-*">`, `<span class="material-icons">`, decorative `<img role="presentation">`), then flag when the computed name is empty. Pairs with Q5-RULE-ICON-FONT-ARIA-HIDDEN — that rule enriches labeled parents with icon children; this rule flags unlabeled parents with only icon children. Satisfies wcag22:2.4.4 + 4.1.2. (2ce309f+8932f04+481c86f)
- [x] **Q6-AUTODETECT-WRAPPERS-LEAKS-JS-IDENTIFIER-NOISE** `autoDetectWrappers: true` at scale returned `opaqueCustomComponentNames: ["AG.y","B","H.length","J","J.length","Math.abs"]`. The extractor walks `.js` files grepping for identifier expressions; single-letter variables, member-access expressions (`J.length`), and built-ins (`Math.abs`) are not component names. Restrict extraction to JSX/TSX element tag positions only — `<ComponentName ... />` / `<ComponentName>...</ComponentName>` where the tag name starts with an uppercase letter and is a bare identifier (no `.`, no single-char unless explicitly allowlisted). Skip the `.js` extension entirely for JSX-tag harvesting (plain `.js` has no JSX syntax). Paired with Q3-OPAQUE-COMPONENTS-TOP-EMPTY-DISHONEST under opaque-component quality, but the extraction-source filter is the root-cause fix.
- [x] **Q6-PLACEHOLDER-IN-FIX-SUGGESTION** Minor refinement on `forms/labels-required`: when the rule fires AND the offending `<input>` has a `placeholder="..."` attribute, include the placeholder verbatim in the fix suggestion ("`<label for=\"email\">Email address</label>` — placeholder text `\"Enter email\"` may carry the intent; verify it's accurate before using as label copy"). Pairs with Q3-RULE-PLACEHOLDER-AS-LABEL (new rule for placeholder-as-only-label antipattern) — this item is the fix-suggestion enrichment for the already-firing `labels-required` rule when the placeholder is additive evidence for the correct label text. Extends V1-FIX-NON-EMPTY-LABEL's placeholder path.
- [x] **Q6-SCANNED-BUILD-ARTIFACTS-GROUP-BY-BASENAME** Companion to Q4-SCANNED-BUILD-ARTIFACTS-REASON. When `scannedBuildArtifacts` surfaces 301 entries (bootstrap.css: 45, font-awesome.css: 80, etc.), the flat list is unactionable — the agent sees 301 individual paths and cannot tell that one `exclude` glob covers most of them. Group by basename with a suggested glob: `scannedBuildArtifacts: { grouped: [{ basename, count, pathHint, suggestedGlob }], ungrouped: [paths] }`. `suggestedGlob` is inline-ready for `propose_config`. Additive shape over the existing flat `scannedBuildArtifacts: [paths]` array (consumers that iterate the flat list still work; the grouped form is a sibling field). Pairs with Q-SHARED-PROPOSE-CONFIG-RELATIVE-PATHS — collapsing 301 paths to ~5 glob patterns.
- [x] **Q6-MOTION-PAUSE-STOP-PER-FILE-AGGREGATION** `motion/pause-stop-hide` produced 8,940 findings (36% of the scan's total) against vendor `bootstrap.css` alone. Every CSS animation keyframe / transition declaration is technically a 2.2.2 concern; vendor bundles carry hundreds. When a single rule fires >N× in one file AND that file is in `scannedBuildArtifacts`, add `perRuleCoverage[ruleId].vendorConcentration?: { file, count }` pointing at the densest vendor file with its total count. Findings still emit individually under `files[]` per surface-don't-suppress (pairs with V1-NOISE-RULE-PER-FILE-ROLLUP, which rolls up the same signal for non-vendor files above a concentration threshold). Additive label — zero information loss, one more field for the agent's triage. The V1 item handled the non-vendor case; this item extends the same shape to vendor bundles explicitly so the agent sees "most of this rule's fire is one vendor file" without having to sort findings themselves.
- [x] **Q6-NEXTSTEP-AVOIDS-VENDOR-CSS** `nextStepStructured.file` pointed at `bootstrap.css:365` — vendor CSS is rarely the file the agent should act on first. When the top-ranked violation or next-step target sits in `scannedBuildArtifacts`, rank the suggestion toward a non-vendor file with a finding of the same rule family (if one exists in the same response); fall back to the vendor file only when every violation sits in vendor code. Doctrine-adjacent to "surface-don't-suppress": the vendor finding stays in `files[]`, but the agent's first action should land where they can actually edit. Reason-text when this rerouting applies: "note: highest-severity finding in this scan is in `vendor/bootstrap.css`; next-step points at `authored/site.css` where a same-family fix is applicable." Additive — when no rerouting applies, `nextStepStructured` behaves exactly as today.
- [x] **Q6-RULE-ARIA-REF-MISMATCH-WIDEN-ATTRS** Website-template `coffee-shop/index.html:162` has `aria-labelledby="myModalLabel"` pointing at an id that does not exist anywhere in the document. Q3-RULE-LABELLEDBY-TARGET (closed) covers `aria-labelledby` / `aria-describedby` / `aria-controls` but did NOT fire here — first investigate whether this is rule-registry drift (see Q-SHARED-RULES-EVALUATED-SSOT); once the registry is fixed and the rule fires, widen coverage to all eight ARIA ID-ref attributes: `aria-labelledby`, `aria-describedby`, `aria-controls`, `aria-owns`, `aria-activedescendant`, `aria-errormessage`, `aria-flowto`, `aria-details`. Deterministic, zero-false-positive check. Fixtures under `tests/fixtures/{good,bad}/aria-ref-widen/` covering each of the eight attributes. WCAG 4.1.2.
- [x] **Q6-RULE-INVALID-ID-SHAPE** Website-template `agile-agency/index.html:60` has `id="#top"` — the `#` is an invalid id-start character per HTML-living-standard (ids may not contain whitespace but are otherwise any non-empty string; however, `id="#top"` is the author confusing id syntax with href-fragment syntax). `parsing/duplicate-id` only fires on duplicates, not on malformed shapes. Add a companion `parsing/invalid-id-shape` rule (or extend duplicate-id): flag ids that start with `#`, contain whitespace, are empty-string, or collide with well-known CSS-escape-required characters when used unescaped. Fixtures under `tests/fixtures/{good,bad}/invalid-id-shape/`. WCAG 4.1.1 (parsing) — note: WCAG 2.2 removed 4.1.1 as a testable SC; keep the rule under `parsing/` as best-practice with `satisfies: [wcag21:4.1.1]` (parsing-failure in WCAG 2.1) only, not 2.2.
- [x] **Q6-ICON-FONT-LABELED-PARENT-REGRESSION** Q5-RULE-ICON-FONT-ARIA-HIDDEN (closed) should fire on `<button><i class="fa-bars"></i></button>` patterns in coffee-shop (lines 59, 82, 86), but the rule emitted zero findings. Investigate: (a) registry drift (covered by Q-SHARED-RULES-EVALUATED-SSOT); (b) the rule's "labeled parent" predicate may not cover `<button>` wrapping `<i>` without any other child text when the parent's accessible name comes solely from an `aria-label` rather than a text node — re-read the rule and add `tests/fixtures/real-world/bulk-template-icon-button/` asserting the rule fires. If (b), widen the labeled-parent signal to include `aria-label` / `aria-labelledby` / `aria-describedby` on the parent. WCAG 4.1.2 + 1.3.1.
- [x] **Q6-ANCHOR-ICON-ONLY-NAME-REGRESSION** Q6-LINK-ICON-ONLY-ACCNAME (closed) should fire on `<a href="#home"><img src="logo.png" alt="..."/></a>` patterns in coffee-shop (`index.html:35`), but the deeper scan emitted `media/alt-text-missing` at the `<img>` level only — not a secondary finding at the anchor level for missing accessible name. When the only child of an `<a>` is a decorative/presentational element (icon `<i>`, `<img role="presentation">`, `<svg aria-hidden>`) AND the anchor has no `aria-label`, the anchor has no accessible name regardless of the child's alt. Investigate: registry drift (see Q-SHARED-RULES-EVALUATED-SSOT) vs rule predicate gap (img with non-empty alt counted as giving the anchor a name, masking the distinction between "anchor name = alt" vs "anchor unnamed"). WCAG 2.4.4 + 4.1.2.
- [x] **Q6-MINIFIED-FILE-SNIPPET-COLUMN-ENRICHMENT** Website-template scans cited `bootstrap.min.js:6` and `col 1` eight times for setTimeout review candidates on a 6-line minified file — every match points at the same line/col because the entire file is one line. The agent cannot locate the specific call site. When the cited file matches minified heuristics (single line > 1000 chars, `.min.` infix, sibling sourcemap present), enrich `reason` with (a) byte-column offset within the single line (so `col 47201` disambiguates multiple matches), (b) a pointer to the unminified sibling if one exists (`authored: "src/bootstrap.js"` when `src/bootstrap.js` exists alongside `dist/bootstrap.min.js`), and (c) the ~80-char context window around the match. Additive reason enrichment — findings remain in `files[]`; the agent gets a usable pointer. Doctrine: "The tool's job is to point" — a line:col pointer to a minified file that resolves to "the whole file" is a dishonest pointer. Shipped (a) byte col + (c) ~80-char context window via `src/review/finders/timing-minified.ts`; sibling resolution (b) dropped per backlog "if sibling-resolution gets expensive, skip" clause — node-scoped finders can't see other files without a scope widening that's out of proportion with the pointer-disambiguation value.
- [x] **Q6-SCSS-PARSING-DISCLOSURE** Website-template `filesByExtension` reports 584 `.scss` files, but `perRuleCoverage.filesEvaluated` numbers don't cleanly account for them (`1637 ≠ 1053 + 584`). Response does not disclose whether `.scss` is parsed as CSS (via `extensionMatches` alias) or skipped. Add `analysisCoverage.sassParsingMode: "css-alias" | "skipped" | "native"` (or equivalent) so the agent knows whether a `.scss`-heavy repo was fully inspected by CSS-family rules. Alternative: enrich `filesByExtension` entries with a `parsedAs` field (`"css"` / `"skipped"` / `"native"`). Doctrine: "Ambiguous field shapes are dishonest" — a filesByExtension count that doesn't match the per-rule file count forces the agent to guess coverage.
- [x] **Q6-RULE-LABEL-ADJACENT-UNASSOCIATED-NO-IDS** `forms/label-adjacent-unassociated` never fires on the canonical orphan-label shape `<label>Text Input</label><input class="form-control">` — 12 instances at `sb-admin/forms.html:142,148,158,163,230-ish` (and similar counts across other templated forms). The rule's predicate requires the `<input>` to already carry an `id` so it can match against the absent `for=`; real templates routinely omit the id entirely. Without it, `forms/labels-required` takes over with generic guidance and the richer "add `for=\"<id>\"` + add `id=\"<id>\"`" mechanical pair never surfaces. Fix: adjacency predicate fires on `<label>` immediately preceding an input-shaped element regardless of whether either carries an `id`; mechanical fix proposes a synthesized id and both attributes in one edit. Most common form-a11y bug in the wild; rule misses it. Satisfies wcag22:1.3.1 + 3.3.2 + 4.1.2. Cross-refs Q5-LABEL-ADJACENT-UNASSOCIATED (closed, different shape) and Q4-LABELS-REQUIRED-VS-LABEL-ADJACENT-OVERLAP (open dedup policy).
- [x] **Q6-FINDINGID-COLLISION-SAMEFILE-SAMELINE** `navigation/link-descriptive-text` emits both "not descriptive" AND "N duplicates" findings on the same anchor elements at the same file:line — 8 findings for 4 links on `super-simple-site/index.html:14-17`, with same `findingId` hash (`87dd7b97d2eb`) appearing twice at L14. Both findings are legitimate (the rule satisfies 2.4.4 for descriptiveness AND 2.4.9 for duplicate names), but the `findingId` collision breaks the agent's dedup and the suppress-by-findingId flow. Root cause: the hash input excludes the rule's sub-variant discriminator ("not descriptive" vs "duplicates"). Fix: include the variant key (e.g. `reasonKind`/`messageKey`) in the `findingId` hash computation in `src/engine/rule-runner.ts` so two findings with distinct sub-variants get distinct ids. Add a cross-invariant test: `new Set(violations.map(v => v.findingId)).size === violations.length` per file-level response. Doctrine: the `findingId` is a load-bearing primary key for the suppress + dedup flows.
- [x] **Q6-ICON-FONT-HIDDEN-GROUP-DEDUP-CLASS-PATTERN** `aria/icon-font-hidden` emits 180 findings on sb-admin — the bulk are identical `<i class="fa fa-*">` patterns inside sibling buttons. `groupKey` dedup operates at the per-element level but doesn't collapse structurally-identical occurrences within one file. Per surface-don't-suppress, the candidates stay visible — but a per-site concentration summary (`perRuleCoverage[ruleId].concentration: { file, count, classPattern }`) would let the agent triage 180 into "one fix pattern, applied site-wide." Fix: compute per-file-per-class-pattern concentration on rules that fire >N× on one file with identical `class` evidence; emit `concentration` as a meta field, not a filter. Pairs with V1-NOISE-RULE-PER-FILE-ROLLUP (same shape, different rule family). Satisfies wcag22:4.1.2 (unchanged scope).
- [x] **Q6-CONFIG-CONTEXT-TRIPLE-READOUT** Three fields re-emit the same "no config found" signal: `configSource: null`, `configSearchedFrom: "<path>"`, `configNote: "No ra11y.config found at … — using built-in defaults"` (200-char boilerplate). All three appear together on every response when no config exists; `configNote` duplicates what the warning + `configSource: null` already say, and the 200-char repetition inflates response size on tiny-repo scans. Fix: collapse to `configSource: null` (present on every response) + `configSearchedFrom: <path>` (present-when-meaningful — omit when identical to cwd); drop `configNote` as a user-facing field, or demote to `_meta.configNote` for debugging only. Pairs with Q-SHARED-NO-CONFIG-WARNING-TINY-REPO (whether the warning itself should fire). Doctrine: present-when-meaningful.
- [x] **Q6-CONTRAST-INLINE-STYLE-REGRESSION-AUDIT** Evidence: `vteam.../index.html:89 <strong style="color:#FFF317">` (1.17:1 yellow-on-white), `street-life/index.html:13 <h1>Street <span style="color:#FFEA00">Life</span></h1>` (1.25:1). `perRuleCoverage.remediation` still says `"add CSS source files or additionalPaths"` — wrong guidance when the color lives in `style=""`. `Q-SHARED-INLINE-STYLE-CONTRAST` (marked [x]) prescribed an inline-style evaluation path, but symptoms reproduce on this scan. Three possibilities: (1) closure shipped but this subprocess is serving a pre-closure bundle (check Q3-MCP-RESTART-HINT-SUBPROCESS-RACE), (2) closure was narrower than the spec (only `<style>` blocks, not `style=""` attrs), (3) closure covered HTML scans but per-rule coverage text wasn't updated to match. Investigation path: `git log src/rules/contrast/minimum.ts` + `git log src/input/parsers/html.ts`; confirm against a fresh-subprocess `scan_file` on `vteam.../index.html`. Fix the smaller delta revealed. Add a real-world fixture `tests/fixtures/real-world/inline-style-contrast-yellow-on-white/` asserting the rule fires on `<strong style="color:#FFF317">Text</strong>`. Satisfies wcag22:1.4.3.
- [x] **Q6-PATTERN-FINGERPRINT-CROSS-TEMPLATE** No cross-template pattern dedupe at scale. `<button class="navbar-toggle" data-toggle="collapse">` appears in ≥8 sibling template dirs and emits 8 distinct `findingId`/`groupKey` values per rule per template. At 174 templates × 3 rules firing on this vendor-snippet = ~500 findings from one canonical pattern copied across the catalog. Agents have no affordance for "dismiss this pattern across all siblings" — each finding gets a distinct id. Fix: add `patternId` as a sibling field on each finding, computed as `hash(ruleId + canonicalize(snippet))` where canonicalize strips attribute order, trivial whitespace, unique id/class tokens, and template/SSG interpolation placeholders. Agents can bulk-dismiss or bulk-fix by `patternId` across sibling template copies. Surface-don't-suppress: findings still appear individually; `patternId` is additive affordance, not a filter. Pairs with Q6-ICON-FONT-HIDDEN-GROUP-DEDUP-CLASS-PATTERN (closed, single-file pattern rollup) — this item is the cross-file sibling. Satisfies wcag22 all.
- [x] **Q6-CATALOG-REPO-SIBLING-HINT** `scan_project` at scale (174 top-level sibling dirs, each carrying its own `index.html` + `css/` + `js/`) treats the whole tree as one flat repo — `detectedFramework: "jekyll"`, `parseErrorFileCount: 501`, `opaqueCustomComponentNames: ["AG.y","B",...]` are all at-scale fallout. Angle-2 confirmed per-subdir scans DO recover signal (per-template `rulesFired`, `parseErrorFileCount` 2-6 instead of 501, `opaqueCustomComponentNames` collapses to 0-1). Add a meta-signal: when the root has ≥N (say 5) top-level siblings each carrying a recognizable site-entry-point shape (`index.html` + sibling `css/` or `images/` or `assets/`), emit `meta.catalogHint: { topLevelSiblings: N, exampleSiblings: [names] }` + a `nextStep` suggestion: "this looks like a catalog of N sibling sites; consider `scan_project({ cwd: \"<subdir>\" })` for per-site signal." Pairs with Q6-NEXTSTEP-AVOIDS-VENDOR-CSS (closed) — same routing shape, different trigger. Doctrine: "One tool call should answer 'what next?'" — catalog-shape detection gives the agent the right second call.
- [x] **Q6-FRAMEWORK-DETECT-JEKYLL-FALSE-POSITIVE** `meta.detectedFramework: "jekyll"` fires at scale because a stray `_config.yml` at the root triggers the Jekyll detection. No `_layouts/`, no `_includes/`, no Gemfile, no `Gemfile.lock`. Single-filename evidence is insufficient. Fix (tighten heuristic in `src/mcp/framework-detection.ts` or equivalent): the Jekyll detection requires `_config.yml` AND at least one of (a) `_layouts/` dir, (b) `_includes/` dir, (c) a Gemfile mentioning `jekyll`, (d) a top-level `_posts/` or `_drafts/` dir. When only `_config.yml` is present, emit `frameworkCandidate: "jekyll" (weak)` as a candidate rather than a confident detection, OR omit the detection entirely and let the agent inspect. Per-subdir scans (angle-2 confirmed) correctly emit `detectedFramework: null` because no `_config.yml` is present in the subdirs. Pairs with Q3-TEMPLATE-DIRECTIVE-STARLIKE-MISDETECT (closed) — same class of over-confident detection from weak evidence.
- [x] **Q6-OPAQUE-COMPONENTS-MINIFIED-JS-REGRESSION** (e8e1a7d7) `opaqueCustomComponentNames: ["AG.y","B","H.length","J","J.length","Math.abs"]` on a at-scale full scan. `Q6-AUTODETECT-WRAPPERS-LEAKS-JS-IDENTIFIER-NOISE` (marked [x]) restricted wrapper extraction to JSX/TSX tag positions with active unit-test coverage (`tests/unit/mcp/analysis-coverage.test.ts`), but symptoms reproduce. Root cause is most likely the **reopened** Q-SHARED-TSX-PARSER-FALSE-JSX-CONTEXTS (now `[~]` above): 501 `.js` files routed through the TSX fallback error-parse, contributing fake tag positions to the wrapper extractor that the JSX-tag-position filter then accepts as legitimate. Two-layer fix: (a) the upstream TSX-parser fix stops `.js` from entering JSX mode at all — closes most of the bleed; (b) belt-and-braces, wrapper extraction must skip files in `parseErrorFiles` AND `scannedBuildArtifacts` regardless of how they got there. Add an invariant test: `for every name in opaqueCustomComponentNames, the source file must not be in parseErrorFiles`. Pairs with Q3-OPAQUE-COMPONENTS-TOP-EMPTY-DISHONEST (closed, single-file shape) and Q3-BOOTSTRAP-WRAPPER-DETECT-EMPTY-REASON (closed, MDX/Astro case). Track this item against (b) specifically; (a) lands via the parent TSX item.
- [x] **Q6-CONTRAST-VENDOR-CSS-CROSS-FILE-DEDUPE** `contrast/minimum` fires 5458× across a at-scale scan, with the vast majority on vendor `bootstrap.css` / `animate.css` duplicated across 100+ template subdirs. One fix to `bootstrap.css` in one subdir does not propagate; the agent has no affordance to say "this is the same finding as N siblings." Add per-basename cross-file dedupe: when the same `(selector, declaration)` pair in files sharing the same basename (`bootstrap.css`) fires the same rule across sibling dirs, collapse to one canonical finding with `vendorOccurrences: [{ path, line }]` sibling list. Pairs with Q6-PATTERN-FINGERPRINT-CROSS-TEMPLATE (open, element-level) and Q6-SCANNED-BUILD-ARTIFACTS-GROUP-BY-BASENAME (closed, meta-level). Surface-don't-suppress: findings still enumerable via the occurrences list; the headline is deduped. Satisfies wcag22:1.4.3.
- [x] **Q6-SUGGEST-FIX-MECHANICAL-VS-GUIDANCE-DRIFT** `suggest_fix` returns `kind: "guidance"` on `navigation/href-javascript-void` findings at scale despite the scan's `plan.fixesByClass.mechanical` counting them as mechanical (and the rule's docs advertise a trivial `<button type="button">` replacement). Cross-surface contradiction: the plan says "mechanical," the fix call says "guidance." Root cause candidates: (1) `suggest_fix` degrades mechanical → guidance when the specific finding's replacement text depends on surrounding context it can't statically resolve; (2) the fix handler reads a different class lookup than the plan. Fix: when `suggest_fix(ruleId, file, line)` can produce a well-formed `newText` for a rule the scan-plan counts as mechanical, return `kind: "edit"` with populated `newText`; when the context makes the replacement ambiguous, emit `kind: "guidance"` BUT also emit `meta.mechanicalInPrinciple: true` so the agent knows the rule family supports a mechanical path. Pairs with Q-SHARED-SUGGEST-FIX-GUIDANCE-PRIMARY (closed) — different fix call shape. Doctrine: cross-surface consistency.

## Track Q7 — Real-world OSS field test


Owner: main session + general-purpose. Source: 2026-04-24 multi-repo OSS field test — 20 scan agents (5 waves × 4 anonymized open-source codebases) probing detection accuracy, false-negative coverage, checklist quality, fix-suggestion quality, and response-shape adherence to AI-first doctrine. Sites span four shapes: a CSS-framework + docs tree, an SSG-templated content site, a vanilla HTML/CSS/JS demo collection, and a bulk-template catalog (≈4k files). Wave 5 is the doctrine cross-check; Waves 1-4 are correctness/coverage.

Cross-cutting themes (≥3-of-4-site recurrence) drive the P0 items below. Items with strong overlap against prior tracks are marked as cross-track shared rather than re-listed. All accepted items are correctness, surface-don't-suppress, or shape-honesty fixes — no heuristic suppression accepted (re-affirmed against doctrine).

- [x] **Q7-SUGGEST-FIX-EDIT-LANE-UNREACHABLE** Despite scans reporting hundreds-to-thousands of `mechanical` findings (266 / 8 / 7 / 2036 across the four sites), every `suggest_fix(ruleId, file, line)` call across 32 attempts returned `kind: "guidance"` — never `kind: "edit"`. The mechanical lane the docstring promises is unreachable through the documented input shape. Either the rule fixers don't ship `oldText`/`newText` payloads, or `suggest_fix` ignores them. Fix: audit every rule whose `fixClass: "mechanical"` and confirm it emits `Violation.fix.{oldText,newText}` (or equivalent fix-path payload), then verify `suggest_fix` returns `kind: "edit"` with both fields populated when the inline data is present. Cross-surface contradiction with `plan.fixesByClass.mechanical` headline is the canonical dishonest-shape — agents budget against the headline and never reach the lane. Pairs with Q6-SUGGEST-FIX-MECHANICAL-VS-GUIDANCE-DRIFT (closed, contradiction shape) and Q-SHARED-SUGGEST-FIX-GUIDANCE-PRIMARY (closed, primary/alternatives gap).
- [x] **Q7-SUGGEST-FIX-NONE-NEAREST-FINDING** `suggest_fix` returns `kind: "none"` when `(file, line)` doesn't match a known finding (paginated scans drift the line, agents lose the original line, rule renames). Today the response carries no `nearestFinding`, no `didYouMean`, no `lineHint` — dead-end response forcing the agent to re-scan. Fix: when `kind: "none"` would emit, walk the per-file findings within ±10 lines of the requested line; if exactly one same-rule finding sits in that window, attach `nearestFinding: { ruleId, line }`; if multiple, attach `didYouMean: [{ ruleId, line }]` (top 3). Doctrine: ambiguous field shapes are dishonest — `kind:"none"` with no breadcrumb is the canonical dead-end shape.
- [x] **Q7-SUGGEST-FIX-VENDOR-CONTEXT** `suggest_fix` proposes editing vendor files (third-party CSS frameworks, icon-font stylesheets, minified vendor JS bundles) directly — even when the file is already labeled in `scannedBuildArtifacts`. Half of the assessed cases on one bulk-template site landed inside vendor files; one suggestion proposed a CSS color edit inside a third-party stylesheet with a copyright banner; another proposed editing a minified template literal inside a packed vendor JS bundle (would corrupt the bundle). Fix: when the target file is in `scannedBuildArtifacts` OR matches a vendor-banner heuristic (top-of-file copyright + `min.css`/`min.js` infix), attach a structured `vendorContext: { signal, redirectTo: "consumer-override" }` field to the response; rewrite the primary fix to "override the failing selector in your own stylesheet (loaded after) — do not modify the vendor file"; demote the in-vendor-file edit to an alternative with explicit "vendor-file edit; consumer-override is preferred." Pairs with Q6-NEXTSTEP-AVOIDS-VENDOR-CSS (closed, file-routing axis) — this is the suggest_fix-content axis. Doctrine: surface-don't-suppress applies to the vendor finding; vendor-context labeling helps the agent route without losing the candidate.
- [x] **Q7-CHECKLIST-META-PARITY** `checklist` response routinely ships only `stale_mcp_subprocess` in `warnings` even when the same scan corpus had `scan_project` emitting `template_files_parsed_as_literal`, `parse_errors_present`, `extensions_skipped_no_parser`, `source_language_unsupported` — the checklist consumer in isolation has no idea the scan was degraded. On one bulk-template site `checklist` returned no `meta` key at all. Fix: when `checklist` is invoked against a previously-cached scan or a freshly-run scan, propagate the same `warnings[]` + `meta` block (configSource, scanned.root, rulesEvaluated, filesByExtension) that `scan_project` would emit on the same input. Cross-surface meta + warnings consistency is a doctrine invariant for tools sharing a scan basis. Pairs with Q4-WARNING-DETAILS-CROSS-SURFACE-UNIFY (closed, scan_project ↔ coverage axis) and Q-SHARED-PASS-RATE-COMPOSITE (closed). Doctrine: cross-surface drift forces wasted round trips.
- [x] **Q7-CRITERION-ID-FIELD-NAME-DRIFT** Same concept under two field names across tools: `checklist` items use `criterionId`, `coverage.failingAutomatedCriteria` entries use `id`. Same scan corpus, same concept, two field names — agents joining the two surfaces by criterion silently mis-match. Fix: pick one canonical name (`criterionId` matches the namespaced-id convention used elsewhere — `wcag22:1.4.3`); rename the other; accept the legacy name as a deprecated alias for one minor with a `warnings: ["deprecated_field_id_renamed_criterionId"]` annotation. Doctrine: cross-tool meta + warnings consistency.
- [x] **Q7-SCANNED-BUILD-ARTIFACTS-REASON-MISLABEL** `scannedBuildArtifacts[].reason: "minified"` shipped on hand-authored long-line files (template-literal islands inside JSX-flavored SSG sources, prop-bundle wrappers, SCSS function bodies with long type signatures) — the heuristic is dressed as a deterministic fact, the label is wrong. Q3-BUILD-ARTIFACT-SINGLE-LONG-LINE-SECOND-PROBE (closed) tightened the `minified` predicate; this item is the cross-cutting recurrence (4/4 sites observed mis-labeled entries on at least one file class). Fix: audit every `reason` token emitted by `src/mcp/build-artifacts.ts` against the doctrine "label provable from code" bar — drop reason-tokens whose predicate is heuristic; surface the underlying signal (line-length distribution, sourcemap-sibling presence, hashed-filename pattern) as additive evidence the agent can read without the deterministic-sounding label. Cross-ref Q4-SCANNED-BUILD-ARTIFACTS-REASON (closed) for the per-path classification reason field. Doctrine: heuristic-mislabeled meta sub-fields are dishonest.
- [x] **Q7-BYPRIORITY-ZERO-INFORMATION-FIELD** Closed as duplicate of V1-CHECKLIST-PRIORITY-AXIS-DEGENERATE. `summary.byPriority` is already fully removed from `src/mcp/tool-checklist.ts` (see drop-rationale comment around :443-450), and `tests/integration/mcp-tools.test.ts:1089` guards `expect(body.summary).not.toHaveProperty("byPriority")`. The earlier fix took path (b) (drop entirely) rather than path (a) (present-when-meaningful) because `priorityFor()` returned `"high"` uniformly across all A/AA criteria — even the conditionally-spread high-only variant would have shipped a degenerate single-key object. Per-candidate `confidence` is the honest signal-bearing axis. No new code change needed.
- [x] **Q7-AAA-RULE-LOADER-SILENT-NORUN** (2/4 second-sweep targets) Rules whose `satisfies` list contains only AAA criteria silently fail to fire even when `level: 'AAA'` is requested. Concrete observation: an `<a href="..." target="_blank">` rule (registered in `list_rules`) was absent from `perRuleCoverage` on every scan; `meta.rulesEvaluated.loaded: 74` vs `list_rules` total of 75 — exact off-by-one. Either the level=AAA codepath does not load rules whose `satisfies` is AAA-only, or eligibility filtering elides them. Either way, the agent has no way to discover that a rule exists but isn't running. Fix: (a) audit the level=AAA loader codepath against `rule.satisfies` so AAA-only rules actually run when level:'AAA' is requested; (b) when a rule is loaded but skipped by level filtering, surface a structured `perRuleCoverage` entry with `skipReason: "gated_by_level", requiredLevel: "AAA", requestedLevel: "AA"` rather than silent omission. Doctrine: silent under-surfacing is the asymmetric failure mode; the agent reading the response cannot distinguish "rule clean" from "rule never ran." Add a regression test scanning a fixture and asserting the AAA-only rule fires when level:'AAA' is requested.
- [x] **Q7-PARTIAL-PARSE-STRUCTURAL-RULE-DEGRADATION** (2/4) When a file enters `parseErrorFiles` / `partialParseFiles` state, structural rules that depend on full-document scope (`semantics/landmark-main`, `semantics/heading-hierarchy`, `document/page-titled`, `parsing/html-has-lang`, `document/lang-attribute`) silently return 0 findings — indistinguishable from "rule ran clean." The agent reading `perRuleCoverage` sees `findingsEmitted: 0` with no degradation signal. Fix: when a file is in `partialParseFiles`, structural rules either (a) run with a `coverageConfidence: "low"` decoration on the per-file `perRuleCoverage` entry, or (b) get explicitly listed with `skipReason: "partial_parse — likely impacted"`. Bites hardest on layout/include files which are precisely where landmark-main matters most. Pairs with Q7-PERRULECOVERAGE-EXTENSION-PRESENT-OUT-OF-SCOPE-SUBKIND (silent-low-confidence shape, different cause). — Shipped via `applyParseErrorAdjustment` in coverage-confidence slice 3 (04db9e0b, turn 3 pick 1).
- [x] **Q7-PLAN-VIOLATIONS-COMPOSITE** (1/4) `plan.violations: 323` ships as a top-level integer next to `plan.fixesByClass: { mechanical, guidance, runtimeOnly, verifyInSource }`. Sums categorically different lanes (8 mechanical + 244 verify-in-source + 63 guidance + 8 runtime-only = 323) — agents budget against the headline as if it were 323 actionable edits. Same shape as the `plan.totalFindings` precedent (deleted 2026-04-24) and `plan.safeEditsAvailable` (deleted 2026-04-24). Fix: drop `plan.violations` as a top-level integer; `plan.fixesByClass` is the structured tally. The plan summary prose echoes the same number — same fix applies there. Doctrine: composite headline counts are dishonest. Pairs with Q-SHARED-SAFE-EDITS-VS-MECHANICAL-DISAGREEMENT (closed precedent).
- [x] **Q7-CHECKLIST-PASS-RATE-COMPOSITE** (1/4) `checklist.summary.automatedCoverage.automatedCriteriaPassRate: 45` is a single composite number that mixes "rule fired clean" with "rule never had eligible inputs" with "rule found violations." Pass-rate composites that conflate ran-clean with no-eligible-inputs misrepresent coverage. Fix: drop the composite headline; the structured `meta.perRuleCoverage` already distinguishes `coverageConfidence: "high"` with `findingsEmitted: 0` from `coverageConfidence: "low"` with `reason: "no files matching ..."`. If a rate is wanted, split into two counters (`criteriaWithRulesAllClean` + `criteriaWithoutEligibleInputs`) — split rather than summed. Doctrine: composite headline counts are dishonest.
- [x] **Q7-PERRULECOVERAGE-EMPTY-ELIGIBLE-COLLAPSE** (3/4) Project-level scans ship `perRuleCoverage` with full entries — including remediation prose — for rules where `filesEvaluated: 0` AND `filesEligible: 0`. On HTML-only or CSS-light scans, ~30 of ~70 entries fit this shape, each ~200 chars of boilerplate. Concrete observation on a single-subdir scope: 347KB response was dominated by these entries. Fix: collapse rules with `filesEvaluated: 0` AND `filesEligible: 0` into a single `meta.rulesNotEvaluatedDueToInputType: { count: N, byExtension: {".css": 12, ".tsx": 8, ...} }` entry; keep the full `perRuleCoverage` shape only for rules that had eligible inputs. The agent reads the by-extension counter once and decides whether to widen scope. Doctrine: verbose meta is signal, but boilerplate repeated across rules with zero eligible inputs is not telemetry. Pairs with Q7-RESPONSE-TOKEN-BUDGET-DETAIL (different lever; this is the upstream payload reduction).
- [x] **Q7-DETECT-NATIVE-WRAPPERS-EMPTY-REASON-DISCRIMINATOR** (1/4) `detect_native_wrappers` returns `emptyReason: "no-jsx-onclick-candidates-found-but-opaque-components-present"` on a pure-HTML codebase containing zero JSX/TSX files (verified: zero `.tsx`/`.jsx`/`.svelte`/`.vue`). The discriminator-sounding token then propagates a wrong `nextStep` ("open the opaque components listed under analysisCoverage.opaqueCustomComponentNames") against a list that doesn't exist for this codebase. Fix: compute `emptyReason` from `filesByExtension` first — when there are zero entries for `.tsx/.jsx/.svelte/.vue`, emit `no-parseable-files`; when JSX/TSX files exist but none have PascalCase component usage, emit `no-pascalcase-components`; only emit `no-jsx-onclick-candidates-found-but-opaque-components-present` when scanned files DID contain PascalCase components. Add a fixture covering pure-HTML projects to the regression suite. Doctrine: heuristic-mislabeled meta sub-fields are dishonest; an `emptyReason` that lies misroutes the agent silently.
- [x] **Q7-RESPONSE-TOKEN-BUDGET-DETAIL** `warnings: ["response_token_budget_truncated"]` fires on bulk scans but the structured detail (`warningsDetails`) is bare — `requestedLimit: 25 → effectiveLimit: 5` doesn't explain *which* finding was the budget-blower (a long fix description, a wide snippet, a deep `criteria` array). Fix: extend `warningsDetails.response_token_budget_truncated` with `{ requestedLimit, effectiveLimit, reason: "fix_description_long" | "criteria_breadth" | "snippet_width" | "meta_array_size", topContributorRule?, topContributorByteCount? }` so the agent knows whether to retry with a narrower scope or a different surface. Pairs with Q-SHARED-LIMIT-REQUEST-VS-EFFECTIVE (closed, the `effectiveLimit` surface axis).
- [x] **Q7-SCAN-ONE-FILE-PER-PAGE-PATHOLOGY** Bulk-template scans paginated with `limit: 25` returned exactly 1 file per page on multiple sites — `totalFilesWithFindings: 1793` translates to 1793 sequential calls. The token-density cap (V1-SIZE-RESPONSE-BUDGET-DENSITY-2) is doing its job, but at scale the per-call throughput hits a degenerate floor. Fix: when the density cap clips a page to ≤ 2 files AND `totalFilesWithFindings > 100`, emit a structured `nextStep` that nudges toward `findings_by_rule` or a per-rule narrowing (`scan_project({ ruleIds: [topRuleId] })`) rather than continuing to page file-by-file. Pairs with V1-TOOL-FINDINGS-BY-RULE (open) — that item adds the `findings_by_rule` primitive; this item adds the routing hint.
- [x] **Q7-LANDMARK-MAIN-REASON-IDENTICAL** `semantics/landmark-main` reason text is identical across every fire on a single scan (18 fires, one identical sentence). Fix: enrich with file-context evidence ("document has only `<body>` root with no landmark elements; a single `<main>` wrapping primary content would establish landmark structure for AT" plus the body's child-element kinds — "body contains [n] sibling sections / divs / forms"). Reason-text enrichment only — candidate stays in primary list. Pairs with the broader doctrine that reason text should encode dismissal/triage signal per-finding, not boilerplate.
- [x] **Q7-NAVIGATION-HREF-RULE-RENAME** `navigation/href-javascript-void` rule ID is misleading: the rule fires on `href="javascript:void(0)"` (the literal name implies) AND on `href="#"` (placeholder href, no fragment target). On `href="#"` the message reads "no fragment target" — correct, but the rule ID suggests the JS-scheme variant. Fix: split into two rules — `navigation/href-javascript-scheme` (matches `href="javascript:..."`) and `navigation/href-empty-fragment` (matches `href="#"` and `href=""`) — both `satisfies wcag22:2.1.1 + 4.1.2`. Migration: ship the new IDs alongside the old for one minor with the legacy ID emitting a `warnings: ["deprecated_rule_id_navigation_href_javascript_void"]` annotation. Doctrine: rule IDs that mislead the agent triaging by ID are dishonest. Pairs with Q6-RULE-HREF-JAVASCRIPT-VOID (closed, the rule landing).
- [x] **Q7-DUPLICATE-INPUT-SIBLING-COLLAPSE** `forms/labels-required` (and any input-shape rule) emits N findings on N visually-grouped inputs sharing identical class+type+attributes inside one parent (one-time-code clusters, quiz radio-button sets, day-of-week checkbox sets). Six identical OTP-digit inputs produced six findings with identical `groupKey`. When ≥3 input siblings share `(class, type, attributes-modulo-id)` inside one parent, collapse to ONE finding with `siblingInstances: [{ line, id? }]`. Surface-don't-suppress applies — the consolidated finding carries the full sibling list; agent reads one row, not N. Pairs with Q4-LIKELY-REDUCIBLE-SIBLING-IMAGE-DEDUPE (closed, image variant) + Q5-OTP-INPUT-CLUSTER-DETECTION (closed, OTP review finder) — those address the cluster-detection axis; this is the rule-emission dedupe axis. Implementation: optional `siblingInstances?: readonly { line: number; id?: string }[]` on `Violation` + `EmittedViolation` stamped through `stampViolation` with conditional spread; collapse logic lives in `src/rules/forms/_label-sibling-collapse.ts` (rule-local helper, NOT engine code) and is consumed by `forms/labels-required` for both HTML and JSX intrinsic input/select/textarea siblings; threshold = 3; canonical finding's own (line, id?) sits at `siblingInstances[0]` so consumers iterate without a second lookup; AgentFinding + JSON formatter forwarders pass the field through with the same conditional spread. Real-world fixture at `tests/fixtures/real-world/verify-account-otp-cluster-collapse/` locks the canonical 6-OTP repro; unit tests cover the count contract (6 → 1 with siblingInstances of length 6, threshold floor at 3, mixed-shape per-parent grouping, JSX parity).
- [x] **Q7-FIXDESCRIPTIONREF-PAGINATION-DICT** (classification_mismatch — invariant tests added) `fixDescriptionRef.hash` saves bytes via shared-hash dedup, but the `fixDescriptions` lookup table is only inlined on the page where the first finding using each hash appears. Out-of-order paged retrieval (offset-jump, parallel page fetch) sees `fixDescriptionRef` referencing a hash that isn't in the response's lookup. Fix: every paginated response carries the full lookup table for hashes referenced by *that page's* findings (compute the union per page); duplicate-content cost is bounded by per-page hash count, never larger than re-inlining. Cross-ref V1-REF-DEDUPE (closed) and Q-SHARED-FIXDESCREF-SAME-GROUP-INLINE-DEDUPE (closed).
- [x] **Q7-MOTION-FINDING-SELECTOR-LINE** `motion/pause-stop-hide` cites the line where the `animation:` declaration appears, but the underlying violation is the entire ruleset (the selector at the rule's opening line is the structural anchor). One observed case: an icon-spinner selector opens at line 80; unprefixed `animation:` declaration at line 84; finding cites line 84 — the agent reading the file sees the declaration but the selector context (which determines whether the rule is interaction-triggered or auto-playing) is 4 lines up. Fix: for selector-scoped findings, report the rule selector start line as `line`, the declaration line as `decline`. Pairs with J-1 (open, the spec-correctness narrowing question) — independent of that.
- [x] **Q7-FRAGMENT-FILE-HEADING-HIERARCHY** (3/4 second-sweep targets) `semantics/heading-hierarchy` "Document has no `<h1>`" branch fires on partial / fragment files: component fragments without root `<html>`/`<body>`/`<main>` (rendered inside parent layouts), content-fragment files with front-matter (H1 supplied by the build-time layout), and partial includes under conventional partials directories (`_includes/`, `_layouts/`, `partials/`, `components/`). Treating each parseable file as a standalone document is a category error for any template/SSG/component-framework repository — observed across three target shapes. Fix: detect "fragment files" via either (a) absence of top-level `<html>`/`<body>`/`<head>` root, (b) presence of front-matter delimiter block, OR (c) path matching conventional partials globs. Either skip the no-h1 branch on fragments OR demote to info severity with reason "fragment file; assumes parent supplies `<h1>`." Pairs with Q7-RULE-LANDMARK-MAIN-FRAGMENT-SCOPE (same scope tightening, different rule). Same scope-tightening should propagate to any structural rule that asserts top-level outline shape.
- [x] **Q7-MARKDOWN-FENCED-CODE-BLOCK-SCOPE** (1/4 — high volume: 800+ findings on one site) DOM-shape rules (`navigation/href-placeholder`, `navigation/link-descriptive-text`, `aria/expanded-on-disclosure`) evaluate HTML inside ` ```html ... ``` ` fenced code blocks in markdown / MDX as live markup. The hint string "Markdown files parsed as HTML residue" is in the response, but rule fires concentrate in MDX docs files where the HTML inside fenced blocks is *prose for the reader*, not rendered DOM. On one target this dominated the high-frequency rule counts (single rule emitted 801 findings; another 608). Fix: in the MDX/markdown ingest path, exclude content nested in ` ``` `-fenced code blocks from rule evaluation. Live MDX expressions (`<Example code={`...`} />` template strings) are similarly literal-not-live; same treatment. Doctrine: don't duplicate capability the agent already has — the agent reading the file distinguishes fenced blocks from live JSX in one read. Pairs with Q-SHARED-TSX-PARSER-FALSE-JSX-CONTEXTS (parser-side fix) and Q7-HTML-SHAPE-RULES-GATE-NON-JSX-JS (rule-side gate) — this is the markdown ingest variant.
- [x] **Q7-OPAQUE-COMPONENT-HEAD-DELEGATION** (1/4) `document/page-titled` and `document/charset-first-1024-bytes` fire on layout files whose `<head>` is delegated to opaque PascalCase components (`<Head title={title} />` or similar). The component is opaque to the scanner; the rule sees no literal `<title>`/`<meta>` and asserts they're missing. Symmetrical to Q7-RULE-LANDMARK-MAIN-FRAGMENT-SCOPE for the head-metadata rule family. Fix: when `<head>` contains only opaque PascalCase components and zero literal `<title>`/`<meta>`, demote severity to info with reason "head delegated to opaque component `<Name>`; verify `<title>` is set inside the component." Same family covers `document/lang-attribute` when `<html>` is itself wrapped in an opaque component. Doctrine: don't claim absence-of-evidence as evidence-of-absence when an opaque component is the named child.
- [x] **Q7-OPAQUE-COMPONENT-SKIP-LINK** (1/4) `navigation/skip-link` predicate requires literal `<nav>` in-file; layouts that compose nav inside an opaque PascalCase component (`<Header />`, `<Topbar />`, `<Sidebar />`, `<AppBar />`, `<Chrome />`) silently pass the predicate even though the rendered page does have navigation that needs a skip link. Concrete observation: `BaseLayout` renders `<body>` → `<Header />` → `<main>` directly with no skip link before `<Header />`; rule fires zero times. Symmetrical bug to the head-delegation false-positive: same opaque-component blind spot, opposite direction (false-negative here, false-positive there). Fix: when a layout file's first `<body>` child is an opaque PascalCase component named like `/header|nav|chrome|topbar|appbar|sidebar/i`, treat as "navigation present, opaque" and apply the skip-link predicate (or emit info-severity candidate with reason "first `<body>` child is opaque component `<Name>`; verify a skip link is the first focusable descendant"). Pairs with Q7-OPAQUE-COMPONENT-HEAD-DELEGATION + Q7-RULE-LANDMARK-MAIN-FRAGMENT-SCOPE (same opaque-component cluster).
- [x] **Q7-SKIP-LINK-NESTED-NAV-DETECTOR** (1/4) `navigation/skip-link`'s "primary nav" detector misses the `<body><header><nav>...</nav></header>...</body>` pattern (nav nested one level inside header). Concrete observation: rule fired on a sibling page with top-level `<body><nav>` shape but produced zero findings on a page with `<body><header><nav>` shape — same SC 2.4.1 obligation, different DOM shape. Fix: loosen the primary-nav detector to include `<nav>` directly inside `<body>` OR one level inside `<body>`'s first `<header>` child. Equivalently: when no skip-link-shaped anchor precedes the first `<nav>` on the page (regardless of nesting depth ≤2), fire the warning. Add a fixture asserting the rule fires on the nested shape.
- [x] **Q7-CHECKLIST-CANDIDATE-SNIPPET-ANCHOR** (2/4) `checklist` candidates ship `snippet` containing the surrounding paragraph or a fixed-width window around the line, NOT the literal sentence/clause containing the matched evidence. Concrete observations: (a) sensory-characteristics finder cites line N with phrase "button below" but `snippet` shows a *different* sentence on the same line (the matcher knew the offset but didn't anchor the snippet to it); (b) sponsor-image finder ships ~80-char window when only one image matched. Agent must search the file to find the actual match — the location pointer's snippet contradicts the location's claim. Fix: anchor `line` and `snippet` at the literal occurrence of the matched evidence (the matcher already knows the byte offset; emit it). When multiple matches exist in one file, emit one candidate per match. Pairs with Q6-MINIFIED-FILE-SNIPPET-COLUMN-ENRICHMENT (closed, vendor-snippet variant) — same shape, different cause.
- [x] **Q7-CHECKLIST-CANDIDATE-PREDICATE-FALSE-EVIDENCE** (1/4) `color/meaning-by-color-only` review finder ships reason text "no visible text, icon, or aria-label" on elements where text is demonstrably present (`<span class="badge bg-success">New</span>` — text "New" is the badge body). The predicate checks for *external* sibling text rather than the element's own innerText. False reason text trains the agent to mistrust the candidate even on real misuse: the agent reads "no visible text," sees clear text, dismisses, and the same dismissal pattern propagates to legitimate cases. Fix: predicate must read the element's own normalized textContent first; reason text should reflect what's actually there ("badge text \"New\" is generic; verify a status word would help colorblind users" vs "no visible text"). Same shape applies to any predicate whose reason text is computed without consulting innerText. Pairs with Q7-RULE-COLOR-TEXT-CONTAINMENT-CHECK (rule-side fix; this is the finder-side recurrence). already-closed-by-prior-commit (verified by general-purpose specialist 2026-04-26): commit f8c86c19 'fix(review): use-of-color reason honors textContent presence' shipped the textContent-aware predicate; subsequent commits 1200a030, d089d9ed, 3a9bc232, 3374cf30 hardened with empty-body gate, status-word enrichment, JSX expression-as-content sentinel. Rule-side companion at 31f02b4d. Literal phrase 'no visible text, icon, or aria-label' does not appear anywhere in src/ or tests/.
- [x] **Q7-DETECT-NATIVE-WRAPPERS-INLINE-OPAQUE-LIST** (1/4) When `detect_native_wrappers` returns `emptyReason: "no-jsx-onclick-candidates-found-but-opaque-components-present"`, the response does NOT include `opaqueCustomComponentNames` inline — the agent must call `scan_project` first to get the list, then come back. Two-call dance. The detector already walked the AST to count them; surfacing the names costs nothing. Fix: when `emptyReason` references opaque components, INCLUDE the `opaqueCustomComponentNames` array inline in the response. Doctrine: one tool call should answer "what next?" — if the next call's input is data this call already computed, ship it. Pairs with Q7-DETECT-NATIVE-WRAPPERS-EMPTY-REASON-DISCRIMINATOR (the discriminator correctness; this is the data-co-location).
- [x] **Q7-SCAN-FILE-PERRULECOVERAGE-EXTENSION-SCOPE** (3/4) `scan_file` returns a `perRuleCoverage` array with 70+ entries; roughly half say `coverageConfidence: "low"` / `reason: "no files matching .css were scanned"` (when the scanned file is `.html`) or analogous for `.tsx`/`.js` etc. The agent passing `path.html` already knows the extension — telling them "no CSS files were scanned" on a single-`.html`-file call is structural noise. For a fix-verify loop where the agent edits → scans → re-edits repeatedly, this baseline overhead dwarfs the actionable content (response routinely exceeds 80,000 chars on a small file). Fix: on `scan_file`, restrict `perRuleCoverage` to rules whose eligible inputs match the scanned file's extension(s); collapse the rest into a single `rulesSkippedExtensionMismatch: N` counter. Sharpen the existing `verboseMeta: false` contract to gate `perRuleCoverage` (currently it only gates `analysisCoverage`). Pairs with Q7-PERRULECOVERAGE-EMPTY-ELIGIBLE-COLLAPSE (project-scan variant).
- [x] **Q7-WARNINGS-DETAILS-SCHEMA-DISCIPLINE** (1/4) `warnings: string[]` declared 8 codes (`stale_mcp_subprocess`, `no_config_found`, `template_files_parsed_as_literal`, `scanned_build_artifacts_present`, `extensions_skipped_no_parser`, `parse_errors_present`, `source_language_unsupported`, `response_token_budget_truncated`) but `warningsDetails` carried payload for only 1 (`response_token_budget_truncated`). The other 7 sit in "I have a problem but no payload" state — agent has no way to know which extensions were skipped, which language was unsupported, etc. Asymmetric discipline: half the codes are paired with details, half are not. Fix: pick one shape — either (a) every warning code in `warnings[]` has a corresponding key in `warningsDetails` (use `{}` for codes that genuinely have no further detail), or (b) restructure `warnings` to `Array<{ code, details? }>` where details is present-when-meaningful. The current free-floating string-list with selective-details map is a typed-poorly union. Doctrine: ambiguous field shapes are dishonest.
- [x] **Q7-RULE-MOTION-DURATION-AND-TRIGGER-GATE** Cross-cutting theme #1 (4/4 sites). `motion/pause-stop-hide` fires on every CSS `animation:` / `transition:` regardless of duration or trigger; produced 8,940 / 55 / hundreds of findings on the four sites with most below 1s and many user-trigger-pseudo-class scoped. WCAG 2.2.2 normatively applies to motion lasting >5s. Fix: parse the duration from the shorthand, gate the rule's "violation" emission on parsed duration AND trigger evidence — `animation: ... infinite` OR `animation: ... [duration > 5s]` OR auto-playing carousel attribute (`data-bs-ride`, `data-ride`, JS-init slider classes `.flexslider`/`.camera_wrap`/`.sl-slider-wrapper`) emits at 2.2.2; transitions inside `:hover`/`:focus`/`:active` selectors OR animations with duration ≤5s and no infinite loop emit at 2.3.3 (Animation from Interactions, AAA). Encode the parsed duration in `reason` text as additive evidence ("animation duration 0.6s, trigger: user-pseudo-class :hover"). Build on Q3-RULE-PAUSE-STOP-INLINE-STYLE (closed, inline-style scanning) + Q5-MOTION-PAUSE-STOP-SC-23-VS-22-CONFLATION (closed, two-lane split). Cross-cutting evidence: 4/4 sites in the 2026-04-24 sweep. Blocks J-1 resolution.
- [x] **Q7-RULE-EXPANDED-DISCLOSURE-NAMESPACE-AUDIT** Cross-cutting theme #2 (1 site, 169 findings — single-site recurrence but high volume warrants action). `aria/expanded-on-disclosure` overgeneralizes the toggle-attribute namespace: tooltips, popovers, and button-pressed are NOT disclosure widgets (popover/tooltip closed in Q3-RULE-POPOVER-NOT-DISCLOSURE + Q3-RULE-TOOLTIP-NOT-DISCLOSURE, but the broader audit is open); tabs use `aria-selected`, modals use `aria-haspopup="dialog"`. Fix: audit the disclosure-toggle predicate against the WAI-ARIA Authoring Practices §disclosure pattern — restrict to genuine disclosure shapes (`collapse`, `dropdown`, `offcanvas`, classList-toggle predicates with no role override) and document the exclusion list. Add separate companion rules for the tab-pattern (`aria/tab-pattern-roles`) and modal-pattern (`aria/modal-trigger-haspopup`) so the toggle-attribute namespace's other widget kinds get correct findings under the right axes.
- [x] **Q7-RULE-COLOR-TEXT-CONTAINMENT-CHECK** `color/meaning-by-color-only` fires on elements whose visible text already contains the status keyword (`<button class="btn btn-danger">Danger</button>`) — the rule's reason text contradicts itself when textContent literally is the status word. Q3-COLOR-MEANING-READS-TEXT-CONTENT (closed) added the textContent pre-check on the design-system axis; the cross-cutting recurrence (≥65 instances on one site, dozens on another) confirms the closure didn't propagate. Re-audit: when the element body's normalized textContent contains the status keyword associated with the color class (`text-danger` → `danger`/`error`/`alert`/...), emit the candidate at lower confidence with reason "text already carries status word — verify color is not the *sole* signal for users who cannot distinguish color"; when textContent is absent or doesn't carry the keyword, fire at default confidence. Pairs with Q3-RULE-COLOR-SOLE-INDICATOR (closed). Cross-cutting evidence: 2/4 sites in the 2026-04-24 sweep. **classification_mismatch: rule-side fix already shipped in commit 31f02b4d (color/meaning-by-color-only reads own textContent). Finder-side recurrence routed to Q7-FINDER-USE-OF-COLOR-CANDIDATE-TEXTCONTENT-CONTAINMENT (added below).**
- [x] **Q7-FINDER-USE-OF-COLOR-CANDIDATE-TEXTCONTENT-CONTAINMENT** `wcag22:1.4.1 Use of Color` finder fires on elements whose textContent already carries the status keyword associated with the color class (Q7-RULE-COLOR-TEXT-CONTAINMENT-CHECK is the rule-side version, closed at 31f02b4d). Apply the same predicate on `src/review/finders/use-of-color.ts`: when textContent contains the status keyword associated with the color class, enrich reason with that evidence so the agent dismisses with one read ("badge text "Success" carries the status word — verify color is not the sole signal"). Pairs with Q7-CHECKLIST-CANDIDATE-PREDICATE-FALSE-EVIDENCE (finder false-evidence axis). Cross-cutting recurrence from 2026-04-24 sweep; rule-side closure did not propagate to the finder.
- [x] **Q7-HTML-SHAPE-RULES-GATE-NON-JSX-JS** Cross-cutting theme #4 (1 site dominant — `.js` was 1808 of 4043 scanned files). HTML-shape rules (`document/iframe-title`, `navigation/href-javascript-void`, `tooltip/dismissable`, etc.) fire on JavaScript string-template literals inside packed plugins, treating raw substring matches as rendered DOM. Fix: gate every HTML-shape rule's `appliesTo` to `.html`/`.htm`/`.jsx`/`.tsx`/`.vue`/`.svelte` (DOM-origin) inputs only; emit on bare `.js` only when the parser identifies a JSX element node (not on regex/substring matches against string literals). Pairs with Q-SHARED-TSX-PARSER-FALSE-JSX-CONTEXTS (closed/reopened — that item is the parser-side fix; this is the rule-side gate). Belt-and-braces: even if the parser-fix lands cleanly, the rule-side gate on `appliesTo` extension is the durable invariant. Cross-cutting evidence: 1/4 sites at high volume.
- [x] **Q7-RULE-INLINE-STYLE-CONTRAST-WIDEN** `contrast/minimum` and `contrast/non-text` are gated on `.css`-file presence; HTML files with inline `style="color:#FFF317; background:#FFF"` declarations produce zero contrast findings. Q-SHARED-INLINE-STYLE-CONTRAST (closed) added the inline-`<style>` block path; the cross-cutting recurrence confirms `style=""` attribute scanning is still missing on multiple sites. Fix: synthesize a per-element style block from the parsed `style=` attribute and feed it into the contrast rules' CSS-rule pipeline (mirrors the inline-`<style>` closure). Add a real-world fixture asserting `<strong style="color:#FFF317">Text</strong>` on a white background fires `contrast/minimum`. Cross-ref Q6-CONTRAST-INLINE-STYLE-REGRESSION-AUDIT (closed). Cross-cutting evidence: 2/4 sites. _(classification_mismatch — already shipped via Q-SHARED-INLINE-STYLE-CONTRAST; guard fixture added)_
- [x] **Q7-RULE-MEDIA-PLACEHOLDER-DIMENSION-SRC** `media/alt-text-missing` (and any rule that quotes the `<img>` identifier) emits `<img>` `'700x400'` when the image src is `http://placehold.it/700x400` — the dimension token from the URL is treated as a meaningful identifier. Fix: when `src` filename matches `/^\d+x\d+$/`, has no extension, or is a known-placeholder host (`placehold.it`, `via.placeholder.com`, `picsum.photos/<dim>x<dim>`), drop the quoted identifier from the reason and add "src looks like a placeholder image" as additive context. Reason-enrichment only — candidate stays. Pairs with Q4-RULE-PLACEHOLDER-ALT (closed, placeholder alt text) — different axis: the alt is the issue elsewhere; here the reason text quotes the wrong identifier.
- [x] **Q7-RULE-LANDMARK-MAIN-FRAGMENT-SCOPE** `semantics/landmark-main` fires on demo / visual-test HTML pages (single-component test pages under `tests/visual/`, `examples/`, `demos/`) where the page is intentionally a one-component rendering with no surrounding chrome. Cross-cutting recurrence: dozens of identical fires on one site, every-page on another. Fix: when the scan source includes `scanned_build_artifacts_present` for the file's path tree OR the page's `<body>` contains ≤2 immediate-child elements (single component + maybe a script), surface the candidate with `couldBeWrongBecause: ["isolated_component_demo_page"]` rather than as a confident violation. Reason-enrichment + couldBeWrongBecause additive — surface stays. Pairs with Q3-RULE-NESTED-INTERACTIVE-ROLE-WRAPPER (closed, similar role-context narrowing).
- [x] **Q7-RULE-ROLE-FROM-CLASS-CHILD-LABEL** `aria/role-from-class-only` fires on admonition wrappers (`<div class="note info"><h5>Tip</h5><p>...</p></div>`) where the immediate child `<h5>` carries the severity label — the rule claims "no severity word" but the child heading IS the label. Fix: when the flagged element's first child is a heading (`<h2>`-`<h6>`), check the heading's textContent against the severity-word dictionary; when present, treat it as supplying the wrapper's accessible label and demote the finding to a candidate with reason "wrapper's first child heading carries severity word — verify the visual/AT mapping rather than adding role." Pairs with Q4-RULE-NOTE-ROLE-FROM-CLASS (closed, the rule landing) — this is the child-label refinement.
- [x] **Q7-RULE-TOOLTIP-DISMISSABLE-TITLE-EQ-TEXT** `tooltip/dismissable` fires on `<a title="front matter">front matter</a>` — the title equals visible text exactly; SC 1.4.13 "content on hover" failure does not apply when no additional content is conveyed. Fix: when the flagged element's trimmed `textContent` (case-insensitively) equals the `title` attribute value, drop the finding or downgrade it to a `parsing/redundant-title-attribute` note (best-practice nudge, not a 1.4.13 violation). Pairs with V1-TOOLTIP-DISMISSABLE-VISIBLE-TEXT-EQ-TITLE (closed, the suggest_fix-side fix that drops redundant alternatives) — that closed the alternatives; this closes the fact-of-firing.
- [x] **Q7-RULE-MEDIA-SVG-ACCESSIBLE-NAME** Cross-cutting theme #15 (3/4 sites). Inline `<svg>` accessible-name semantics not modeled — `<title>`, `role="img" + aria-label`, `<use xlink:href>` references — produce zero findings on codebases with hundreds of inline SVGs. New rule `media/svg-accessible-name`: `<svg>` used non-decoratively (any of: parent has interactive role, parent is `<button>`/`<a>`, sibling text references it as content) without (a) child `<title>` element, (b) `role="img"` + `aria-label`/`aria-labelledby`, (c) `<use xlink:href="#id">` where `#id` resolves to a named symbol. Decorative case: `<svg aria-hidden="true">` (or absent role with no labeling parent) silent. WCAG 1.1.1. Pairs with Q5-RULE-ICON-FONT-ARIA-HIDDEN (closed) + Q6-LINK-ICON-ONLY-ACCNAME (closed) — those cover icon-font and anchor-name axes; this is the inline-SVG axis.
- [x] **Q7-RULE-ARIA-PRESENTATION-ON-SEMANTIC-ANCESTOR** New rule `aria/presentation-on-semantic-ancestor`. WCAG F92: `role="presentation"` (or `role="none"`) on an element whose descendants include focusable controls — the role removes semantics the descendants depend on. Single-site evidence (n≥6) but spec-deterministic rule with high signal. WCAG 4.1.2.
- [x] **Q7-RULE-FORMS-SELECT-ONCHANGE-CONTEXT-CHANGE** New rule `forms/select-onchange-context-change`. `<select onchange="...navigate|location|window.open|submit...">` — selecting an option triggers context change without user activation. WCAG 3.2.2. Single-site evidence (n=3) but pervasive antipattern; rule is statically deterministic. Pairs with Q-SHARED-FORM-SUBMIT-ONCHANGE (covered in untargeted-but-groundable below).
- [x] **Q7-RULE-VISUAL-DISABLED-NON-CONTROL** New rule `semantics/visual-disabled-non-control`. `<span class="prev disabled">Back</span>` — visual-disabled appearance on a non-interactive element with no semantics; AT users see no disabled-state signal. WCAG 1.3.1 + 4.1.2. Pairs with Q4-RULE-DISABLED-LINK-SURROGATE (closed, anchor variant) — this is the broader span/div variant.
- [x] **Q7-RULE-FORMS-ASTERISK-REQUIRED-MARKER** New rule `forms/asterisk-required-marker`. Label/placeholder text ending `*` on an input that has no `required`/`aria-required` attribute and no document-level legend explaining the convention. WCAG 3.3.2. Pairs with Q2R2-FORM-REQ (closed, wrapper-forwards-required-without-marker axis) — different direction: that detects the wrapper-forwards-without-marker case; this detects the marker-without-attribute case.
- [x] **Q7-RULE-LINK-NAME-ONLY-SYMBOL** New rule `navigation/link-name-only-symbol`. Anchor whose normalized accessible name is ≤2 punctuation/HTML-entity characters (`&lsaquo;`, `&times;`, `«`, `»`). WCAG 2.4.4. Pairs with Q4-RULE-PAGINATION-GLYPH-NAME (closed, pagination-specific finder) — this is the broader rule that fires regardless of pagination context.
- [x] **Q7-RULE-DUPLICATE-LANDMARK-HEADER-FOOTER** Extend `semantics/duplicate-landmark-unlabeled` to cover multiple `<header>` / `<footer>` siblings (banner / contentinfo landmarks). Currently covers `<nav>`/`<main>`/`<aside>`; adds `<header>`/`<footer>` to the family with the same labeling-or-collision predicate. WCAG 1.3.1.
- [x] **Q7-RULE-MULTIPLE-H1-BRANCH** (1/4 second-sweep targets) `semantics/heading-hierarchy` has no branch for "two `<h1>` elements on a single page" — multiple top-level outline anchors (e.g. `<h1 class="logo">Site Name</h1>` at top + `<h1>Page Title</h1>` mid-page). Existing branches cover "no `<h1>`" and "level skipped"; multiple-h1 is an outline concern many style guides flag. Fix: add a multiple-h1 branch citing both line numbers and noting that the document outline has two top-level headings. Severity: warning. WCAG 1.3.1.
- [x] **Q7-RULE-COLOR-STATE-CLASS-COLOR-ONLY** (1/4) `color/meaning-by-color-only` scopes only to status-utility class names (`text-danger`, `alert-success`, `btn-warning`); misses tutorial / hand-authored CSS that uses non-utility class names like `.current`, `.active`, `.selected`, `.correct`, `.incorrect` and changes ONLY `color` between paired state classes (no text-decoration, no font-weight change, no border, no icon swap). Concrete observation: `.nav.active a { color: #000 }` paired with `.nav a:hover { color: #c0392b; font-weight: bold }` — the active state is color-only. Fix: widen the rule to also flag selectors that toggle ONLY `color` between paired state classes (sibling selectors differing only in state-class suffix), OR add a 1.4.1 review-candidate finder that points at color-toggling state classes and asks the agent to verify a non-color signal also changes. WCAG 1.4.1. Pairs with Q7-FINDER-1.4.1-COLOR-CLASS-CROSSREF (related cross-class finder — different angle: that targets `.correct/.incorrect`-style class definitions; this targets `.foo` vs `.foo.active` paired definitions).
- [x] **Q7-RULE-ANIMATION-ITERATION-COUNT-GATE** (1/4) `motion/three-flashes-or-below-threshold` (or whichever rule emits 2.3.1 candidates) synthesizes a per-second cycle rate from `animation-duration` alone, treating one-shot transitions (`animation: slideOff 0.3s linear forwards;` with no `infinite` and default iteration count of 1) as candidate-flashing. WCAG 2.3.1 requires three flashes per second sustained — a single-cycle 300ms animation is mathematically inapplicable. Same false positive on `animation: hide 0.2s ease-out` (single-shot reveal). Fix: gate the cycles/s synthesis on `animation-iteration-count: infinite` (or numeric > 1 with a high-enough product). When `forwards`/`backwards`/`both` is the only fill specifier and iteration count is the default 1, treat as a one-shot transition and don't synthesize a frequency. Single-cycle animations should not produce a 2.3.1 candidate at all. Doctrine note: numeric-threshold heuristics are suppression — but iteration-count is provable from code (deterministic), not heuristic; this is correctness, not threshold-suppression. WCAG 2.3.1. (already landed in 932dcbb0; verified 2026-04-25 by /continue turn 2 audit)
- [x] **Q7-RULE-TABLE-TH-SCOPE-IMPLICIT-COL-NARROWING** (1/4) `semantics/table-th-scope-missing` fires 29× on simple `<thead><tr><th>...</th></tr></thead><tbody>...</tbody>` tables. Per HTML5, `<th>` cells inside `<thead>` containing only header cells receive implicit `scope=col`, so these tables are programmatically associated. WCAG technique H63 calls explicit scope "good practice" but H43 (id/headers) covers complex tables; SC 1.3.1 does not require explicit scope on simple thead+tbody tables. Fix: tighten predicate to fire only when (a) the table has multiple header rows in `<thead>`, (b) `<th>` appears in a `<tr>` outside `<thead>` without scope, OR (c) the table mixes row-headers and column-headers. Trivial single-row `<thead>` with column-only `<th>` children should not fire — implicit scope=col covers it. Alternative: emit at info severity not warning. WCAG 1.3.1. (already landed in 68720778; verified 2026-04-25 by /continue turn 5 audit)
- [x] **Q7-RULE-TOOLTIP-DISMISSABLE-TITLE-IS-ACCNAME** (1/4) `tooltip/dismissable` fires on icon-only links where `title` is the sole accessible-name source (no inner text, no aria-label, no labeled child) — e.g. an icon-only social link with only an `<i>` child plus `title="Facebook"`. SC 1.4.13 targets supplementary content shown on hover/focus; when `title` IS the element's primary accessible name, the rule misclassifies the name carrier as supplementary tooltip content. Cross-rule disagreement compounds: `navigation/link-descriptive-text` accepts the same `title` as the name (does not fire); `tooltip/dismissable` rejects it (fires). Fix: suppress `tooltip/dismissable` on interactive elements where `title` is the sole accessible-name source (no inner text, no aria-label/labelledby, no labeled child). Cross-reference the same name-resolution predicate `link-descriptive-text` uses. Alternative: keep firing but reframe message — current "native browser tooltips are not dismissable" is misleading when title is the name carrier. WCAG 1.4.13. Pairs with Q7-RULE-TOOLTIP-DISMISSABLE-TITLE-EQ-TEXT (the redundant-title variant) and Q7-RULE-NAME-RESOLUTION-TITLE-FALLBACK-CONSISTENCY (the cross-rule consistency angle).
- [x] **Q7-RULE-FORMS-ALERT-CONTAINER-PREDICATE** (1/4) `forms/error-message-not-associated` recognizes `.invalid-feedback` (newer-CSS-framework convention), `.error-message` (generic), and `[role=alert]` as candidate error containers, but misses the older `.alert.alert-error` / `.alert.alert-success` / `.alert.alert-danger` / `.alert.alert-warning` family plus id-anchored containers (e.g. `id="contactError"` / `id="contactSuccess"`). Templates using these patterns place the alert div adjacent to a form but never wire it via `aria-describedby` — and the rule never flags it. Fix: extend the predicate to recognize `.alert.alert-{error,danger,warning,success,info}` and conventional id patterns (`*Error`, `*Success`, `*Message`) as candidate containers. Surface as info severity if the association is uncertain. WCAG 3.3.1. Pairs with Q3-RULE-ARIA-INVALID-MISSING (closed, different surface).
- [x] **Q7-RULE-FORM-LANDMARK-NAME-MISSING** (1/4) New rule `semantics/form-landmark-name-missing`. `<form role="form">` (or `<form>` rendered as a landmark by browser heuristics) with no accessible name — no `aria-label`, no `aria-labelledby`, no labeled child. Per ARIA 1.2 a form landmark with no accessible name is removed from the landmark list, undermining the role declaration. `semantics/section-accessible-name-missing` covers `<section>` but not `<form>`. Fix options: (a) widen `semantics/section-accessible-name-missing` to also evaluate `<form role="form">` and `<form>` elements rendered as landmarks (sitting alongside other landmarks); (b) add sibling rule `semantics/form-landmark-name-missing` with the same heuristic. The "section sits alongside other landmarks" predicate translates directly to forms. WCAG 1.3.1 + 4.1.2.
- [x] **Q7-RULE-PAGINATION-ANCHOR-FIX-DIRECTION** (1/4) `navigation/href-placeholder` (formerly `href-javascript-void`) fires on `<a href="#">1</a>`, `<a href="#">2</a>` pagination markers and suggests "change to `<button type="button">`" — wrong direction. Pagination should remain anchors with real `href` values pointing at paginated routes (`href="?page=2"`). The rule technically detects a real defect (no fragment target), but the suggested remediation steers toward the wrong element type for paginated navigation. Fix: when the placeholder anchor sits inside a list with class containing `pagination`/`pager` OR has sibling text content of 1/2/3 numerals, prefer the navigation-with-real-href branch in the fix suggestion ("`href="?page=2"` pointing at an actual paginated route") and de-emphasize the button-conversion branch. Currently both paths read as equally weighted in the guidance.
- [x] **Q7-RULE-LINK-DESCRIPTIVE-CROSS-LANDMARK-DUPS** (1/4) `navigation/link-descriptive-text` flags identical-name + identical-href anchor pairs as confusing duplicates when they sit in different landmarks (top `<header><nav>` + bottom `<footer><nav>`). SC 2.4.5 explicitly calls helpful redundancy across regions a feature; treating header-nav/footer-nav duplicates as confusing inverts the spec. The rule cannot distinguish "helpful redundancy across regions" from "two ambiguously-similar in-content links." Fix: when the two duplicates sit in different landmarks (one inside `<nav>`/`<header>`, the other inside `<footer>`) and target the same href, downgrade or suppress — that's a navigation-redundancy pattern, not the screen-reader-rotor confusion pattern SC 2.4.4/2.4.9 targets. Keep firing when both duplicates are inside the same content region (e.g. two "Read More" in the same article). Encode the landmark-context in the reason text rather than dropping the candidate.
- [x] **Q7-FINDER-3.3.1-FORM-REQUIRED-ATTRS** `wcag22:3.3.1 Error Identification` — `<form>` blocks containing `<input required>` / `pattern` / `minlength` / `aria-invalid` are deterministically present in source. Surface as grounded review candidates pointing at the field, not as untargeted prompts. Pairs with Q3-RULE-ARIA-INVALID-MISSING (closed, the missing-aria-invalid rule) — this is the broader finder for the criterion.
- [x] **Q7-FINDER-3.3.8-PASSWORD-INPUTS** `wcag22:3.3.8 Accessible Authentication` — every `<input type="password">` is a candidate location. Promote to grounded finder citing the input element and the form's other auth fields.
- [x] **Q7-FINDER-1.4.1-COLOR-CLASS-CROSSREF** `wcag22:1.4.1 Use of Color` — CSS class definitions like `.correct{color:green}` / `.incorrect{color:red}` cross-referenced with HTML usage of those classes (deterministic). Surface as candidates pointing at usage sites; reason names the color-only convention.
- [x] **Q7-FINDER-2.5.1-TOUCH-POINTER-HANDLERS** `wcag22:2.5.1 Pointer Gestures` — `touchstart`/`touchmove`/`pointermove` handlers and pointer-event-library imports (hammer.js, use-gesture, @use-gesture) in JS, plus inline `ontouchstart`/`onpointermove` attributes in HTML. Static evidence. Pairs with Q5-POINTER-GESTURES-SUBSTRING-FALSE-POSITIVE (closed) — that closed the false-positive on bare identifier substring; this widens the legitimate evidence sources.
- [x] **Q7-CHECKLIST-IFRAME-FINDER-ELEMENT-VS-PROSE** Iframe-as-media finders for 1.2.x cite prose mentions of `<iframe>` (text content of documentation pages) instead of actual elements. Fix: finder must traverse the parsed DOM for `<iframe>` element nodes, not regex against text content. Pairs with Q-SHARED-VIDEO-CAPTIONS-IFRAME-EMBEDS (closed, the rule extension) — different surface: that's the rule firing on real iframes; this is the finder distinguishing real iframes from prose mentions. (behavior already correct — guard fixture added)
- [x] **Q7-CHECKLIST-1.3.3-LOCATIVE-CALLOUT-PROSE** `wcag22:1.3.3 Sensory Characteristics` finder fires on developer documentation prose ("right side", "button below") inside Callout / Note / Tip blocks — that prose describes UI to developers, not user-facing UI instructions. Fix: when the matched text sits inside a known prose-callout container (`<div class="note|tip|warning|callout">` / `<aside class="...">`) AND no nearby element-level reference resolves the locative, drop the candidate (developer prose is not a user-facing UI instruction). Pairs with Q3-SENSORY-CHARACTERISTIC-LOCATIVE-WORD-SCOPE (closed, locative-word tightening) — this is the callout-container scope tightening.
- [x] **Q7-CHECKLIST-AUDIO-VS-VIDEO-CRITERION-FAN** `<audio>` elements fan out across `wcag22:1.2.3` and `wcag22:1.2.5` (Audio Description / Synchronized Media) — but those criteria are scoped to *synchronized media* (video tracks), not audio-only. Six audio elements emitted 18 candidates across 3 criteria. Fix: when the source element is `<audio>` (no `<video>`/`<source type="video/...">`/iframe-with-video-host), include only `wcag22:1.2.1` (Audio-only and Video-only Prerecorded) in the criterion fan-out; exclude 1.2.3 and 1.2.5. Doctrine: spec-correctness on which criteria a candidate belongs to.
- [x] **Q7-CHECKLIST-CRITERION-FAN-MULTI-CRITERION-CANDIDATE** (2/4 second-sweep targets) When the same evidence (path + line + element shape) supports multiple criteria, the checklist emits N separate candidates with identical reason text — observed: iframe-as-media finders emitting 6 candidates from 2 unique iframes across 1.2.1 + 1.2.3 + 1.2.5; audio-only finders emitting 18 candidates from 6 elements × 3 criteria. The agent dismisses the same dismissal N times. Fix: when the same evidence supports multiple criteria, emit ONE candidate with `criteria: [...]` array and a single deduplicated `reason` text. The summary headline counter then reflects unique evidence locations, not duplicated dismissals. Pairs with Q7-CHECKLIST-AUDIO-VS-VIDEO-CRITERION-FAN (different angle: that's about which criteria the candidate belongs to; this is about the dedup shape when it does belong to multiple).

## Track Q8 — Multi-repo external scan field test (2026-04-25 round)


Owner: main session + `rule-implementer` (rules) + general-purpose. Source: 4 parallel scan agents against four unrelated public codebases — a static-template subproject set, a docs-site fragment tree, a vanilla-JS tutorial-exercise collection, and a bulk-template catalog (≈4k files / 1.8k vendor-JS files). All accepted items are correctness, surface-don't-suppress, or shape-honesty fixes per CLAUDE.md §1 + the AI-first-consumer doctrine; recurring-across-≥2-scans themes drive the P0 priorities.

Cross-cutting themes seen across ≥2 of the 4 scans: response-token-budget overruns even on medium repos; `.js` files routed through the tsx parser flooding `parseErrorFiles` with fake JSX errors; HTML files reported as `partialParseFiles` with "Stray closing tag at top level" on browser-renderable input; heuristic-mislabeled meta sub-fields recurring on `scannedBuildArtifacts[].reason`, `templateDirectivesFound`, and `coverageConfidence` reasons; cross-surface count drift between scan / checklist / coverage; composite-headline strings in checklist summaries.

- [x] **Q8-RESPONSE-TRUNCATED-OVERSIZED-ENVELOPE** When `scan_project` exceeds the MCP host token ceiling (observed twice in one scan at 472KB and 365KB; observed at 87KB on a 429-file repo crashing the limit), the agent gets nothing actionable from the canonical entrypoint — effectively zero-output disguised as too-big. Fix: degrade to a minimum-honest envelope `{ filesScanned, totalFindings, plan.fixesByClass, warnings: ["response_truncated_oversized"], nextStep: { tool: "scan_project", args: { summaryOnly: true, … }}}` rather than dropping the whole response. Pairs with V1-TOOL-VERBOSE-META-INVERTED-DEFAULT (the structural inversion) and V1-TOOL-SCAN-PROJECT-SUMMARY-MODE (the lean entrypoint).
- [x] **Q8-PARSER-ROUTING-JS-AS-TSX** `.js` files are routed through the tsx parser, producing 538-entry `parseErrorFiles[]` (truncated to 50 shown) on one scan with reasons like "Unclosed JSX element `<h>`", "`<g.top>`", "`<this.options.itemsCustom.length>`" — every plain JS or minified vendor JS file emits a fake JSX parse error that floods the response and dominates the token-budget overrun. Fix: route `.js` through a JS-only parser path (no JSX expectation) by default; only re-route to the tsx parser when the file contains a JSX-emitting return path or is named `.jsx`. Per-file `parseErrorFiles[].reason` should never read "Unclosed JSX element" on a file the scanner itself didn't expect to be JSX. Owner: `parser-author`.
- [x] **Q8-PARSE-ERROR-FILES-BY-PARSER-SPLIT** `analysisCoverage.parseErrorFilesTruncated: { shown: 50, total: 538 }` silently elides 488 entries. When the parser-routing bug above flips `.js`-as-tsx to the right thing, the cardinality drops; until then, agents need to see the structural shape — every `.js` file failing — without paging. Fix: emit `parseErrorsByParser: { tsx: 538, css: 12, html: 2 }` summary and split the per-file list by parser kind so the agent sees "every .js file failed under tsx" without expanding 538 entries. Pairs with V1-COVERAGE-PARSE-ERROR-FILES-UNCAPPED (the array-uncapped axis) and Q8-PARSER-ROUTING-JS-AS-TSX (root cause).
- [x] **Q8-HTML-PARSER-STRAY-CLOSING-TAG-PARTIAL-PARSE** 208 HTML files reported as `partialParseFiles` with reason "Stray closing tag at top level" — including browser-renderable `index.html` files that end with whitespace + `</body></html>`. The HTML5 parser is not in tag-soup mode for legitimate input; partial parses hide real findings on every affected file. Fix: switch the HTML parser to HTML5 tag-soup mode for top-level whitespace tolerance, OR document the predicate that produces "Stray closing tag at top level" and tighten it so common-case authored HTML doesn't fall in. Add a fixture asserting an `index.html` ending with `</body></html>\n\n` parses cleanly. Owner: `parser-author`.
- [x] **Q8-EXTENSIONS-SKIPPED-NO-PARSER-IMAGE-FILTER** `warnings.extensions_skipped_no_parser` lists `.jpg`/`.png`/`.gif`/`.eot`/`.ttf` — images and fonts are not text source files; their inclusion clouds the actually-actionable subset (e.g. `.vue`, `.svelte`, `.md`) and inflates the warnings-details payload. Fix: filter the skipped-extensions list to text-source extensions only (drop image / font / audio / video / archive extensions); when binary assets are present, fold their count under `meta.binaryAssetCount` rather than treating them as parser-skipped.
- [x] **Q8-RESPONSE-META-TRUNCATED-FIELD-DETAIL** `warnings: ["response_meta_truncated"]` ships generically — the agent can't tell which meta sub-field was elided. Fix: extend `warningsDetails.response_meta_truncated.fields: ["scannedBuildArtifacts[]", "perRuleCoverage[]", …]` so the agent knows which structured field to retry under `verboseMeta: true`. Pairs with Q7-RESPONSE-TOKEN-BUDGET-DETAIL (same telemetry axis).
- [x] **Q8-PARSE-ERRORS-PRESENT-SUBCODE** `parse_errors_present` warning code conflates parser-bailed-zero-findings (file produced no AST) with partial-parse (file produced a usable AST that stopped at line N). Agents triaging coverage cannot distinguish the two from the warning alone. Fix: add `parser_bailed_zero_findings` as a sibling warning code; emit when the file contributed no AST nodes to any rule. Pairs with V1-PARTIAL-PARSE-FILES-WARNING-CODE (open) — that adds the partial-parse code; this adds the bailed-zero code.
- [x] **Q8-CROSS-SURFACE-UNTARGETED-COUNT-INVARIANT** `scan_project.plan.untargetedCriteria` / `coverage.untargetedCriteria` / `checklist.summary.untargetedCriteria` disagree on the same cwd (one scan: 17 / 15 / 15). Add an integration test asserting the three surfaces report the same untargeted-criteria count for the same input; resolve the off-by-2 in the originating computation. Pairs with Q2R2-UNTARGETED-NAME (closed, naming) — this is the count-invariant axis.
- [x] **Q8-CONFIGSEARCHEDFROM-ECHO-RECURRENCE** `meta.configSearchedFrom` echoes `scan_file`'s parent directory or `scanned.root` when no config was found — Q6-CONFIG-CONTEXT-TRIPLE-READOUT closed this on `scan_project`, but the recurrence on `scan_file` and on docs-site fragment scans suggests the conditional-spread isn't applied uniformly. Fix: audit every tool that emits `configSearchedFrom` and confirm it omits the field when its value would just echo `cwd` or `scanned.root` already in the response.
- [x] **Q8-CHECKLIST-SUMMARY-COMPOSITE-PROSE** Checklist summary string still ships as `"N actionable · M untargeted · K likely irrelevant"` — a composite headline string consumers read first, even though the structured per-kind tally is already present. Fix: drop the prose composite from the summary string entirely; the per-kind counters are the honest signal. Per CLAUDE.md §1 "Composite headline counts are dishonest" worked precedent (`plan.totalFindings` deletion).
- [x] **Q8-SCANNED-BUILD-ARTIFACTS-DATA-URL-GRADIENT-LABEL** `scannedBuildArtifacts.ungrouped[].reason: "contains-data-url-gradient"` fires on hand-authored 1350-line SCSS files with embedded `url(data:...)` gradient declarations — a `data:` gradient is a common authoring pattern, not a build-artifact signal. Fix: drop `"contains-data-url-gradient"` as a deterministic-sounding `reason` token; if data-url density is meaningful evidence, surface it as `dataUrlByteCount` additive context the agent reads without the build-artifact label. Per CLAUDE.md §1 "Heuristic-mislabeled meta sub-fields are dishonest."
- [x] **Q8-SCANNED-BUILD-ARTIFACTS-MINIFIED-SVG-LABEL** `scannedBuildArtifacts[].reason: "minified"` fires on 6 single-line hand-authored brand SVG files whose entire content is one well-formed `<svg>` element. Single-line SVGs are an authoring norm. Pairs with Q3-BUILD-ARTIFACT-SINGLE-LONG-LINE-SECOND-PROBE (closed, line-length axis); this item is the SVG-specific recurrence. Fix: when extension is `.svg`, the single-long-line predicate must require additional minification evidence (no whitespace, no comments, hashed-filename) before stamping `"minified"`.
- [x] **Q8-TEMPLATE-DIRECTIVES-HANDLEBARS-OR-MUSTACHE-DIALECT** `templateDirectivesFound: ["handlebars-or-mustache"]` fires on codebases using non-handlebars `{{ }}` interpolation dialects (Liquid / Jinja / Vue / Angular share the same braces), and on `${{ ... }}` GitHub-Actions expressions in `.yml` files. The token implies block-helper detection (`{{#if}}`, `{{>partial}}`) but the predicate matches bare `{{ }}` interpolation common to many dialects. Fix: drop the deterministic-sounding token; surface the matched evidence shape (`templateInterpolationFound: { token: "{{x}}", count: N }`) and let the agent disambiguate the dialect.
- [x] **Q8-COVERAGECONFIDENCE-MEDIUM-UNCONDITIONAL-DEFAULT** `perRuleCoverage[].coverageConfidence: "medium"` ships with reason `"cross_file_idref_resolution_limited_on_this_input"` on every single-file scan — even when the file is structurally self-contained. The label reads as a deterministic verdict but is an unconditional default. Fix: tighten the predicate so `medium` fires only when the file actually references an external id/idref; otherwise `coverageConfidence: "high"`. Pairs with V1-PERRULE-COVERAGE-CONFIDENCE-PARSE-ERROR-CORPUS (corpus-aggregation axis).
- [x] **Q8-DETECT-NATIVE-WRAPPERS-NO-JSX-INAPPLICABLE** `detect_native_wrappers` returns prose `nextStep` on a tree with zero `.tsx`/`.jsx`/`.svelte`/`.vue` files. Fix: when no JSX-bearing files exist, return a structured `inapplicable: { reason: "no_jsx_in_tree", filesByExtension }` signal so agents route once on the tool's own response rather than re-discovering. Builds on Q7-DETECT-NATIVE-WRAPPERS-EMPTY-REASON-DISCRIMINATOR (open).
- [x] **Q8-DUPLICATE-LANDMARK-UNLABELED-EMITS-ON-SPECULATION** `semantics/duplicate-landmark-unlabeled` reason text reads "When composed into a layout, will likely sit alongside another `<form>`" — pure speculation about composition the static parser cannot observe. 73 emits on this assumption in one scan. Per the new doctrine bullet (heuristic emission is the symmetric twin of heuristic suppression), the rule must not emit when the predicate depends on guessed composition. Fix: gate emission on confirmed in-file sibling evidence; otherwise move the predicate to a review-candidate finder. Owner: `rule-implementer`.
- [x] **Q8-COLOR-MEANING-BY-COLOR-ONLY-SEVERITY-CONTRADICTION** `color/meaning-by-color-only` emits at `error` severity (107 fires in one scan) while the reason text concedes the visible text already carries the status word. Per the new doctrine bullet (reason text and severity must agree), pick one closure: (a) suppress emission on the conceding branch, (b) downgrade to candidate, (c) rephrase reason to surface what is actually still in question (e.g. "color may still be the *sole* cue for users who can't read the keyword"). Pairs with Q7-RULE-COLOR-TEXT-CONTAINMENT-CHECK (closed, rule-side fix) and Q7-FINDER-USE-OF-COLOR-CANDIDATE-TEXTCONTENT-CONTAINMENT (open, finder-side); this item asserts the severity/reason agreement closure.
- [x] **Q8-HEADING-HIERARCHY-FRAGMENT-EMISSION** `semantics/heading-hierarchy` and `semantics/landmark-main` fire identically on files already classified as fragments (`analysisCoverage.fragmentFiles` populated; reason text mentions composition). Either gate emission on fragment classification or move both rules to soft review-candidate on fragments. Pairs with Q7-FRAGMENT-FILE-HEADING-HIERARCHY (open) and V1-HEADING-HIERARCHY-MARKDOWN-FIRES-DESPITE-HINT (open) — convergent doctrine fix on this rule family.
- [x] **Q8-FINDER-2.2.1-VENDOR-PATH-AND-DURATION-CLASSIFICATION** `review:wcag22:2.2.1` setTimeout/setInterval finder fires on minified/vendor JS and on short animation ticks (1ms / 30ms / 150ms). Per CLAUDE.md §1 numeric-threshold-heuristics rule, do NOT suppress; instead surface additive evidence: `vendorPathHint?: boolean`, `durationLiteralMs?: number | "non-literal"` so the agent dismisses a vendor-bundled debounce in one read. Pairs with V1-FINDER-2.2.1-SETTIMEOUT-VENDOR-FILE-GATE (closed, downgrade lane); this is the structured-reason axis.
- [x] **Q8-FINDER-1.4.5-PER-ACCNAME-DEDUP** `review:wcag22:1.4.5` fires N times for N near-identical sponsor / avatar / logo images with sequential alt prefix. Fix: dedupe per accessible-name pattern with `sourceCount: N` instead of N candidates; one row carrying the location list per pattern. Pairs with V1-FINDER-1.4.5-IMAGES-OF-TEXT-MARKDOWN-ADJACENT-LINK-FP (closed) and Q7-VENDOR-DISMISSAL-KEY-FINGERPRINT (open) — this is the within-scan dedup axis.
- [x] **Q8-NAVIGATION-HREF-JAVASCRIPT-VOID-EXAMPLE-CONTAINER-HINT** `navigation/href-javascript-void` reason text floods on docs example code blocks (1284 emits in one scan, mostly inside `<Example>` component wrappers). Per the AI-first markdown-fenced-code-block rule (Q7-MARKDOWN-FENCED-CODE-BLOCK-SCOPE), the parser-side fix is the durable answer; in the meantime, add `containingElementHint?: "Example" | "CodeBlock" | "pre"` to the reason so the agent dismisses an example-context fire in one read. Reason-enrichment only — no severity change.
- [x] **Q8-LINK-DESCRIPTIVE-TEXT-TEMPLATE-AWARE-REASON** `navigation/link-descriptive-text` lacks template-aware reason text on Liquid/Jinja/handlebars-style interpolated content; sibling rule `semantics/empty-heading` already honors template-stripped content. Cross-rule consistency: extend the same template-caveat to link-descriptive-text reason text so the rule pair behaves uniformly on template-bearing files. Pairs with Q7-TEMPLATE-DIRECTIVE-CHILD-ONLY-CONTENT (open).
- [x] **Q8-FORMS-AUTOCOMPLETE-MISSING-NAVIGATION-SELECT** `classification_mismatch_not_reproducible` (verified by rule-implementer specialist 2026-04-25): `forms/autocomplete-missing` only iterates `<input>` and has never fired on `<select>` — premise is false against current main. Field report likely misattributed; `forms/select-onchange-context-change` already covers the navigation-`<select>` shape correctly. If a separate rule-widening item is needed (extend autocomplete-missing to `<select>` + `<textarea>` per HTML spec), file a fresh entry framed as widening, not as a false-positive closure.
- [x] **Q8-RULE-SVG-TITLE-INLINE-MARKUP** `media/svg-title-missing` only fires on `.svg` files; inline `<svg>` in HTML / Astro / MDX with no `<title>` and no `aria-hidden="true"` is silently missed. Extend to inline `<svg>` in markup-bearing extensions. Pairs with Q7-RULE-MEDIA-SVG-ACCESSIBLE-NAME (closed) — convergent extension; this is the inline-SVG title-element axis.
- [x] **Q8-RULE-DUPLICATE-ID-PARSER-HTML** `semantics/duplicate-id` is gated on `.html` extension; Astro / MDX / JSX files ingested via `parser='html'` are eligible but never evaluated. Fix: derive eligibility from the parser registry kind, not extension. Owner: `rule-implementer`.
- [x] **Q8-RULE-MOTION-ATTRIBUTE-DRIVEN-CAROUSEL** `motion/pause-stop-hide` did not fire on attribute-driven auto-rotating carousel patterns (`data-ride="carousel"`, `data-bs-ride="carousel"`, `data-interval=…`) in any of the four scans. WCAG 2.2.2: auto-playing motion >5s requires a pause control. Fix: extend the rule to recognize attribute-driven carousel idioms as auto-playing-motion evidence. Owner: `rule-implementer`.
- [x] **Q8-RULE-SKIP-LINK-HEADER-FALLBACK** `navigation/skip-link` only fires when `<nav>` element exists; bypass-blocks gap when navigation is implemented as `<header>` containing sibling anchors with no `<nav>`. Loosen the trigger to `<header>` containing anchor-list children. Pairs with Q7-OPAQUE-COMPONENT-SKIP-LINK (open) and Q7-SKIP-LINK-NESTED-NAV-DETECTOR (open) — convergent rule-scope widening.
- [x] **Q8-RULE-HREF-JAVASCRIPT-PSEUDO-PROTOCOL** New rule `navigation/href-javascript-scheme` (or extend `navigation/href-javascript-void`). Detect `href="javascript:void(0)"`, `href="javascript:;"`, `href="javascript:"` — currently the rule only matches `href="#"`. Single scan observed 140 hits unflagged. WCAG 2.1.1 + 4.1.2. Pairs with Q7-NAVIGATION-HREF-RULE-RENAME (closed) which proposed the rename; this item is the predicate widening. Owner: `rule-implementer`. (classification_mismatch: predicate already widened by commit 6c2e81d3 'feat(rules): split href-placeholder into js-scheme + empty-fragment'; rule navigation/href-javascript-scheme already matches /^javascript\\s*:/i case-insensitively.)
- [x] **Q8-RULE-CROSS-FILE-CLICK-HANDLER-NON-INTERACTIVE** New review finder `keyboard/cross-file-click-handler-on-non-interactive`. `el.addEventListener('click', …)` attached at runtime to a plain `<div>`/`<span>`/`<li>`/`<img>` (no `role`, no `tabindex`, no `onkeydown`) — the dominant FN pattern in vanilla-JS codebases (≥20 hits across two scans). Static-deterministic when the AST resolves the element selector to a non-interactive tag. WCAG 2.1.1 + 4.1.2. Owner: `rule-implementer`.
- [x] **Q8-RULE-AUDIO-VIDEO-NO-CONTROLS** New rule `media/audio-video-no-controls`. `<audio>`/`<video>` element without `controls` attribute and no JS-driven control wiring (no `play()` / `pause()` reference in sibling scripts). 6 hits in one scan. WCAG 1.4.2 + 2.1.1. Owner: `rule-implementer`.
- [x] **Q8-RULE-IN-PAGE-LINK-FRAGMENT-MISSING** New rule `navigation/in-page-link-fragment-missing`. `href="#fragment-id"` where the fragment id does not exist in the same document. 6 hits in one scan. WCAG 2.4.1. Owner: `rule-implementer`.
- [x] **Q8-RULE-DRAGGABLE-NO-KEYBOARD-ALT** New rule `pointer/draggable-no-keyboard-alt`. `draggable="true"` element with no keyboard-equivalent reorder/move mechanism (no `onkeydown` listening for arrow / home / end). WCAG 2.5.7 + 2.1.1. Owner: `rule-implementer`.
- [x] **Q8-RULE-FORM-NO-SUBMIT** New rule `forms/no-submit-control`. `<form>` containing one or more text inputs but no `<input type="submit">`, `<button type="submit">`, or `<button>` lacking explicit type (defaults to submit). Catches broken-stub contact forms. WCAG 3.3.2. Owner: `rule-implementer`.
- [x] **Q8-RULE-LAYOUT-TABLE-PRESENTATION-ROLE** New rule `semantics/layout-table-no-presentation-role`. `<table>` containing only `<td>` cells (no `<th>`, no `scope` attributes, no `<caption>`) used purely for layout — recommend `role="presentation"`. 102 hits in one scan. WCAG 1.3.1. Owner: `rule-implementer`.
- [x] **Q8-RULE-IMG-EMPTY-ALT-IN-CLICKABLE-GROUP** New rule `media/img-empty-alt-in-clickable-group`. `<img alt="">` on images that are the sole visual differentiator inside a clickable widget (rating tile, gallery thumb, button child). 3+ hits in one scan. WCAG 1.1.1. Owner: `rule-implementer`.
- [x] **Q8-RULE-ICON-CHILD-MISSING-ARIA-HIDDEN** New rule `aria/icon-child-missing-aria-hidden`. `<i>` icon-font element inside a `<button>`/`<a>` with no `aria-hidden="true"` AND no accessible-name source on the parent — pairs with `semantics/button-name`; lone `aria-hidden` enrichment. 8 hits in one scan. WCAG 1.3.1 + 4.1.2. Owner: `rule-implementer`.
- [x] **Q8-RULE-FORM-LANDMARK-NAME-MISSING-COUSIN** Already shipped (verified 2026-04-25 by /continue rule-implementer specialist): `semantics/form-landmark-name-missing` rule exists at src/rules/semantics/form-landmark-name-missing.ts (commit b57ce5fe), with 12 unit tests, fixtures, registry entry, and KB doc (commit 31b96a94). Implementation cedes the multi-form case to `semantics/duplicate-landmark-unlabeled` per AI-first heuristic-emission doctrine.
- [x] **Q8-RULE-TARGET-BLANK-NO-WARNING** New review finder `navigation/target-blank-no-warning`. `target="_blank"` link with no warning text in the link body or visually-adjacent prose, and no `rel="noopener"`. WCAG 3.2.5 (Change on Request) advisory — finder, not rule, since "warning text" requires reading semantics. Owner: `rule-implementer`.
- [x] **Q8-FORMS-LABELS-REQUIRED-INCONSISTENT-INPUT-TYPES** `forms/labels-required` fires inconsistently between `<input type="text">` and `<input type="number">`, and possibly skips lone search-style inputs (no surrounding form context). Investigate: predicate may be branching on `type=` value rather than treating all text-bearing input types uniformly. Fix: audit predicate against the WHATWG input-types table; assert all text-input types (text/search/email/tel/url/password/number/date/time/datetime-local/month/week) emit identically when unlabeled.
- [x] **Q8b-PARSE-ERROR-FILES-DOWNGRADE-COVERAGE** Files appearing in `analysisCoverage.parseErrorFiles[]` still report `coverageConfidence: "high"` for every rule on that path — confidence label contradicts recorded parse error. Honest shape: when `parseErrorFiles[].path == file`, every `perRuleCoverage[].filesEvaluated` entry crediting that file must downgrade to `"low"` with `reason: "parse_failed"`. Same invariant for `partialParseFiles[]` → `"medium" + reason: "partial_parse"`. Mirror of the parser-side "reason text and severity must agree" rule. Owner: `parser-author` / engine. Slice 2 (per-finding propagation) verified-already-shipped by worktree-agent-ab29d2b6ec3c88183 on 2026-04-26 — no separate commit needed.
- [x] **Q8b-FRAGMENT-FILE-COVERAGE-DOWNGRADE** Files on `analysisCoverage.fragmentFiles[]` (e.g. `_includes/`, `_partials/`, README.md residue) report `document/*` rule coverage at `confidence: "high"` regardless — fragment classification doesn't propagate to per-rule confidence. Fix: when file is a known fragment, downgrade `landmark-main` / `heading-hierarchy` / `page-titled` / `lang-attribute` per-rule confidence to `"medium"` with `reason: "fragment_input_no_document_envelope"`.
- [x] **Q8b-PERRULECOVERAGE-CONFIDENCE-REASON-EVIDENCE-GATE** `perRuleCoverage[ruleId].coverageConfidence.reason` ships `"cross_file_idref_resolution_limited_on_this_input"` / `"cross_file_listener_resolution_limited_on_this_input"` / `"cross_file_custom_property_resolution_limited_on_this_input"` / `"cross_file_click_alternative_resolution_limited_on_this_input"` even when the input contains zero idref attributes / zero inline listeners on non-interactive elements / zero `var(--…)` references / zero draggable attributes. Extends Q8-COVERAGECONFIDENCE-MEDIUM-UNCONDITIONAL-DEFAULT (which names the idref case) to the full reason family. Fix: gate every confidence-downgrade reason on the rule actually observing ≥1 candidate token it failed to resolve. With zero candidates, `confidence: "high"`.
- [x] **Q8b-CONTRAST-INHERITANCE-REASON-VS-SEVERITY** `contrast/minimum` ships at `severity: "error"` while reason text concedes the inheritance assumption may be wrong (`"verify this rule's element actually renders inside that ancestor"`). Per the doctrine "reason text and severity must agree": demote to `warning` (or candidate) on the inheritance-uncertain branch; reserve `error` for the case where the cascading background+foreground are both determined within the same rule. Pairs with Q8-COLOR-MEANING-BY-COLOR-ONLY-SEVERITY-CONTRADICTION (sibling rule on same axis).
- [x] **Q8b-ACTIONABLE-MANUAL-COUNT-CROSS-SURFACE-INVARIANT** `actionableManualItems` disagrees across `scan_project.plan` / `checklist.summary.actionable` / `coverage` on the same cwd (observed 10/7/7, 6/5/5, 12/9 across corpora). Sibling to Q8-CROSS-SURFACE-UNTARGETED-COUNT-INVARIANT but on a distinct counter. Fix: shared finder-ranker pass that all three response builders consume; add an integration test pinning `scan.actionableManualItems === checklist.summary.actionable === coverage.actionableManualItems` on the same cwd.
- [x] **Q8b-PARSE-ERROR-FILE-COUNT-CROSS-SURFACE-INVARIANT** `analysisCoverage.parseErrorFiles[].length` differs between `scan_project` and `coverage` on the same cwd (observed 546 vs 538). No surface explains the delta; agent reading both responses cannot reconcile. Fix: cache+share parse results across project-rooted tools; integration test asserting `parseErrorFileCount` agrees cross-tool.
- [x] **Q8b-COUNTSBYSURFACE-MULTI-DISAGREEMENT-HEADLINE** `scan_project.meta.countsBySurface` ships three disagreeing finding totals (observed `plan:316`, `perRuleCoverage:328`, `filesSurface:316`) plus `plan.violations:310` — a 4-way spread within one response. Field name implies these should agree (`countsBySurface` reads as "cross-surface reconciliation"). Either remove `countsBySurface` (callers don't need cross-surface reconciliation as headline) or annotate each entry with what it counts (`{ plan: { count, includes }, perRuleCoverage: { count, includes }, filesSurface: { count, includes } }`).
- [x] **Q8b-PLAN-SUMMARY-EMBEDDED-COUNTS-DRIFT-VS-STRUCTURED** `scan_project.plan.summary` prose embeds 5+ different counts (e.g. "310 findings, 10/45/51/204 fixesByClass, 6 notes, 7 actionable, 17 untargeted") that duplicate structured siblings and rot independently. The structured fields are authoritative; the prose is the drift surface. Fix: drop `plan.summary` entirely (or strip all bare numbers from it), letting the structured siblings carry the data without a duplicate human-readable composite. Per-doctrine "composite headline counts are dishonest" worked precedent.
- [x] **Q8b-RULE-NESTED-INTERACTIVE-ARIA-ROLES** `semantics/nested-interactive` checks descendant native interactive elements but skips ARIA interactive roles. An anchor wrapping `<div role="progressbar">` / `<span role="slider">` / `<button role="checkbox">` / nested `role="link"` in `<a>` is silently allowed. Extend the descendant scan to recognize ARIA interactive roles (`button`, `link`, `checkbox`, `radio`, `combobox`, `slider`, `spinbutton`, `progressbar`, `switch`, `tab`) as nested-interactive evidence.
- [x] **Q8b-RULE-FOCUS-OUTLINE-VISIBLE-BOX-SHADOW-CREDIT** `focus/outline-visible` does not credit a paired `box-shadow` as a focus-indicator replacement, producing avoidable noise on `:focus / :focus-visible { outline: 0; box-shadow: 0 0 0 2px … }` — a canonical replacement pattern. Fix: when a `box-shadow` declaration coexists in the same `:focus` / `:focus-visible` rule block as the `outline: 0`, treat it as a valid replacement and suppress emission. Surface-don't-suppress-only when the box-shadow pattern is genuinely missing.
- [x] **Q8b-RULE-DIALOG-ROLE-MISSING-ON-MODAL-SHELL** New rule `aria/dialog-role-missing`. Container with `tabindex="-1"` + `aria-labelledby` + `aria-hidden` + class-name token in `{modal, dialog, drawer, offcanvas, popover}` but missing `role="dialog"` / `role="alertdialog"` (and missing `aria-modal`). Different from V1-FINDER-MODAL-FOCUS-MGMT (focus-management lane) — this is the role-presence lane. WCAG 4.1.2.
- [x] **Q8b-RULE-TAB-CONTROLS-RECIPROCAL-PAIR** New rule `aria/tab-controls-missing`. `[role="tab"]` element missing `aria-controls` referencing its panel id; symmetric `[role="tabpanel"]` missing `aria-labelledby` back to a tab id. Reciprocal-pair predicate; resolve idrefs in the same document. WCAG 4.1.2 + 1.3.1.
- [x] **Q8b-RULE-TAB-WIDGET-ROLES** New rule `aria/tab-widget-roles`. A list-of-anchors-or-buttons with attribute idiom matching tab-widget patterns (`data-toggle="tab"` / `data-bs-toggle="tab"` / `role="tab"` ancestor) that lacks the full role triple (`tablist` / `tab` / `tabpanel`) plus `aria-selected` on every member. Composite-widget rule family per the lens-A doctrine gap. WCAG 4.1.2.
- [x] **Q8b-RULE-ANCHOR-BUTTON-NOT-FOCUSABLE** New rule `keyboard/anchor-button-not-focusable`. `<a>` carrying `role="button"` OR class-name token in `{btn, button}` AND no `href` AND no `tabindex="0"` — not keyboard-reachable yet announces as button. WCAG 2.1.1 + 4.1.2.
- [x] **Q8b-RULE-LAYOUT-CONTAINER-OVERFLOW-NO-KEYBOARD** New rule `layout/horizontal-scroll-no-keyboard`. Element with computed `overflow-x: auto|scroll` (or `overflow: auto|scroll`) wrapping a `<table>` or other wide content, with no `tabindex="0"` + `aria-label` on the wrapper. Keyboard users cannot scroll the region. WCAG 2.1.1.
- [x] **Q8b-RULE-INPUT-IMAGE-EMPTY-ALT-NAME-SOURCE** `classification_mismatch_already_double_covered` (verified by rule-implementer specialist 2026-04-25): predicate already fires under TWO existing rules — `media/alt-text-missing` (WCAG 1.1.1; tests/unit/rules/media/alt-text-missing.test.ts:22-27,86-90) AND `semantics/button-name` (WCAG 4.1.2; tests/unit/rules/semantics/button-name.test.ts:125-178). Adding a third rule for the same predicate would create a triple-emission directly conflicting with the open Q8b-SAME-ELEMENT-DOUBLE-COUNT-MERGE item one line below. The two existing rules' satisfies lists already span 1.1.1 + 4.1.2.
- [x] **Q8b-FIX-DESCRIPTION-REF-ALWAYS-RESOLVABLE** Per-finding `fix.description` group-dedup leaves entire `groupKey` groups with NO representative carrying either inline `fix.description` OR a resolvable `fixDescriptionRef.hash` — agent retrieves nothing actionable on those findings. Violates invariant #5 (every violation has a context-aware fix suggestion). Fix: scan-builder must guarantee at least one representative per group carries either inline or ref; integration test asserting every emitted `fix.description` (or `fixDescriptionRef.hash`) resolves in `referenceGuide`.
- [x] **Q8c-CONTRAST-MINIMUM-HOVER-PSEUDO-OVERSTATEMENT** `contrast/minimum` fires at `severity: error, confidence: high` on selectors containing `:hover`, `:focus`, `:active` (e.g. `.text-warning:hover` ratio 3.73:1). Resting-state contrast is not the failing case; hover/focus contrast is governed by SC 1.4.11 (UI components) when applicable, not 1.4.3 AA (text). Fix: when the selector contains a user-state pseudo-class, downgrade severity to `warning` AND rephrase reason to scope the failure to the active state (e.g. `":hover state has 3.73:1 — verify this is acceptable for the brief duration of the user state"`). Pairs with Q8b-CONTRAST-INHERITANCE-REASON-VS-SEVERITY (cascade-uncertainty axis) — this is the user-state-pseudo axis on the same rule.
- [x] **Q8c-CONTRAST-NON-TEXT-BORDER-EQUALS-BG-FALSE-POSITIVE** `contrast/non-text` fires at `1.00:1` on `border: 1px <color> solid` against `background: <same-color>` — but a same-color border is not a "UI component boundary" failing 1.4.11; the button's perimeter is conveyed by other means (background different from page bg). Fix: when border-color matches element's own background-color, the border is invisible by intent — defer to "is the element's silhouette visible against the *parent* bg?" Surface as additive evidence (`borderEqualsBackground: true`) rather than a 1.00:1 finding. Pairs with V1-CSS-CONTRAST-CASCADE-INHERITED (closed, cascade walk) — this is the same-color-border false-positive surface.
- [x] **Q8c-FOCUS-OUTLINE-VISIBLE-UNIVERSAL-SELECTOR-FALSE-NEGATIVE** `focus/outline-visible` fires on `:focus { outline: none }` declarations but does NOT fire on `* { outline: none }` (universal selector) — the latter is the more dangerous pattern, killing focus indicator on every element. Observed in one corpus on a top-of-stylesheet declaration. Fix: extend the predicate to match `*` selectors AND any selector whose specificity catches focusable elements; treat universal `outline: none` as the most severe case. Pairs with Q8b-RULE-FOCUS-OUTLINE-VISIBLE-BOX-SHADOW-CREDIT (box-shadow replacement axis) — this is the missing universal-selector predicate axis.
- [x] **Q8c-RULE-AUDIO-NO-CONTROLS-NO-TRANSCRIPT** Bare `<audio src="…"></audio>` (no `controls`, no `<track>`, no neighboring `<a href>` to a transcript) currently surfaces only as a manual-review candidate. The static signal is deterministic — element with no controls + no text alternative — and warrants a rule-side emission. Fix: extend `media/audio-track-needed` (or new rule `media/audio-controls-or-transcript-missing`) to fire on `<audio>` with no `controls` AND no `<track>` AND no sibling transcript anchor. WCAG 1.1.1 + 1.4.2. Pairs with Q8-RULE-AUDIO-VIDEO-NO-CONTROLS (open, controls-only axis) — this is the broader transcript-or-controls axis.
- [x] **Q8c-RULE-DISCLOSURE-VS-TAB-VALUE-CARVE-OUT** `aria/expanded-on-disclosure` fires on framework `data-*-toggle="tab"` attributes calling them "disclosure-style" — but `toggle="tab"` is the WAI-ARIA tabs pattern (uses `aria-selected` + `role="tab"` + `tablist`/`tabpanel`), NOT a disclosure pattern (which uses `aria-expanded`). Adding `aria-expanded` to a tab is wrong. Reason text hedges "If this control is not a disclosure trigger, the detection is wrong" — exact severity-vs-reason disagreement. Fix: carve out `tab`/`pill`/`list` toggle values from the disclosure-rule predicate; route them to the new tab-widget rule (Q8b-RULE-TAB-WIDGET-ROLES) instead. Pairs with Q8b-RULE-TAB-WIDGET-ROLES (open) — this is the disclosure/tab disambiguation closure.
- [x] **Q8c-RULE-INLINE-DISPLAY-NONE-ON-FOCUSABLE** New rule `semantics/inline-display-none-on-focusable` (or extend `aria/hidden-focus`). Element with inline `style="display:none"` AND a focusable descendant (or itself focusable: `<a>`, `<button>`, `[tabindex]`). Pattern observed: `<li style="display:none;"><a href="#topnav">HOME</a></li>` (dead nav code or progressive-disclosure that needs aria treatment). Static-deterministic; no runtime ambiguity. WCAG 2.4.3 + 4.1.2. Pairs with V1-FP-HIDDEN-FOCUS-MODAL (closed, modal-overlay axis) — this is the inline-style axis on the focusable-hidden surface.
- [x] **Q8c-RULE-DOC-TITLE-ELEMENT-LITERAL-FROM-FILENAME** New rule (or `document/page-titled` widening). When `<title>` content is a verbatim filename token (`"index"`, `"page1"`, `"untitled"`, `"document"`, `"Page Title"`) emit a candidate — these are scaffold/template default titles never customized. Bulk catalog: dozens of templates with literal placeholder titles. WCAG 2.4.2. Pairs with V1-FIX-DOC-TITLE (closed, fix-text axis) — this is the placeholder-detection axis.

## Track Q9 — Multi-corpus AI-first sweep (2026-04-25 evening, 4 corpora × 5 angles)


Field-test follow-ups from a 20-agent multi-corpus probe (CSS framework + static-site generator + vanilla mini-projects + bulk template gallery). Each item below was observed across ≥1 corpus during the round; recurrence counts noted inline. Items are bucketed by failure class. Net-new against Q8/Q8b/Q8c/V1; recurrence of already-named items folded into the existing rows there.

- [x] **Q9-PERFINDING-COULDBEWRONG-EMPTY-WHEN-PERRULE-DEGRADED** `keyboard/handler-missing` per-finding entries ship with `confidence: "high"` and `couldBeWrongBecause: []` while the same response's `perRuleCoverage["keyboard/handler-missing"].coverageConfidence === "medium"` carries `reason: "cross_file_listener_resolution_limited_on_this_input"`. Recurring 7/7 in one vanilla corpus, 4/9 in another, 6/9 contrast/minimum in a third. Add an integration-test invariant: when `perRuleCoverage[r].coverageConfidence !== "high"`, every per-finding emission of rule `r` in the same response must either downgrade `confidence` to match OR include the per-rule reason in `couldBeWrongBecause`. Per AI-first doctrine "Per-finding confidence must reflect per-rule coverage limitations." Promotion-to-guard-test of an existing doctrine rule.
- [x] **Q9-FRAGMENT-CLASSIFICATION-NO-PER-RULE-DOWNGRADE** When `analysisCoverage.fragmentFiles[]` includes a file, `document/landmark-main` / `document/page-titled` / `parsing/html-has-lang` / `semantics/empty-heading` still report `coverageConfidence: "high"` with no `fragment_input_no_document_envelope` reason. Recurring across 2 corpora. Fix per Q8b-FRAGMENT-FILE-COVERAGE-DOWNGRADE (open) + the doctrine `Parser-failure invalidates per-file confidence` rule which already names the fragment-files variant. Pin with integration test asserting per-rule confidence downgrade for the document-shaped rule family on every fragment file.
- [x] **Q9-SUGGEST-FIX-NO-CRITERION-ID-BRIDGE** `checklist` surfaces criterion IDs (`wcag22:X.Y.Z`); `suggest_fix({ruleId: "wcag22:X.Y.Z"})` returns `"Rule not found. Call list_rules."` — manual-review candidates have no working handoff to suggest_fix. Recurring across all 4 corpora. Cross-tool gap. Fix: suggest_fix accepts criterion IDs as an alias for "fetch fix guidance for any rule satisfying this criterion"; when multiple rules satisfy, return guidance for the most-specific rule with a `disambiguation_note: ["alt-text/missing matches", "media/alt-text-placeholder also matches"]`. Per AI-first doctrine "One tool call should answer 'what next?'" — the broken handoff forces 3+ tool calls instead of 1.
- [x] **Q9-RULE-FOCUS-OUTLINE-UNIVERSAL-SELECTOR** Already shipped via Q8c-FOCUS-OUTLINE-VISIBLE-UNIVERSAL-SELECTOR-FALSE-NEGATIVE (commit d355d9b8 / 3b787545 / 045c8c8d on 2026-04-25) — duplicate item closed by /continue planner sweep. The universal-subject branch was added to the `focus/outline-visible` predicate.
- [x] **Q9-RULE-MOTION-INFINITE-ANIMATION-NO-MEDIA** `classification_mismatch_premise_false_and_spec_incorrect` (verified by rule-implementer specialist 2026-04-25): `motion/pause-stop-hide` at WCAG 2.2.2 already fires on `.spinner { animation: spin 2s infinite; }` via the `hasInfinite` gate (src/rules/motion/pause-stop-hide.ts). Separately, the proposed sibling rule satisfying WCAG 2.3.3 is spec-incorrect — 2.3.3 normative text governs animation triggered by user interaction (`:hover`/`:focus`/`:active`), not auto-running infinite animation. `motion/animation-from-interactions` correctly owns 2.3.3 in the interaction-gated lane. Closing as not-actionable.
- [x] **Q9-RULE-MOTION-AA-LEVEL-REDUCED-MOTION-CANDIDATE** `motion/*` family is gated to AAA only; AA-default scans never evaluate `prefers-reduced-motion` presence. Vanilla corpus, `theme-clock/style.css` `animation: grow 0.6s linear` without `@media`. Fix: add an AA-level review-candidate for prefers-reduced-motion presence on any file containing `animation:` or `transition:` declarations; the candidate frames the question without claiming a violation. Satisfies wcag22:2.3.3 (AAA reframed at AA as a review-candidate).
- [x] **Q9-RULE-CSS-HOVER-ONLY-NO-FOCUS-MIRROR** No rule fires on `:hover` declarations that mutate `transform` / `opacity` / `visibility` / `display` without a paired `:focus` / `:focus-within` mirror. Vanilla corpus, `movie-app/style.css:110` `.movie:hover .overview { transform: translateY(0); }` reveals an overlay only on hover. Fix: add a CSS rule detecting `:hover`-only declarations that mutate visual layout properties without `:focus` / `:focus-within` mirror. Satisfies wcag22:1.4.13 + 2.1.1.
- [x] **Q9-RULE-ALT-TEXT-PLACEHOLDER-SRC-BASENAME-RESTATEMENT** `media/alt-text-placeholder` token list catches `pic` / `image` / `placeholder` etc. but misses the canonical "alt restates src basename" antipattern (e.g. `<img src="balloons.gif" alt="balloons">`). Bulk corpus. Fix: case-insensitive check that the alt text equals (or is contained in) the src URL's basename-stem. Satisfies wcag22:1.1.1. Pairs with V1-ALT-TEXT-PLACEHOLDER-SEQUENTIAL-SLIDE (closed, sequential-slide axis) — convergent token-list extension.
- [x] **Q9-RULE-LINK-TARGET-BLANK-NEVER-FIRES** `classification_mismatch_already_correctly_gated` (verified by rule-implementer specialist 2026-04-25): rule is correctly AAA-gated (satisfies wcag22:3.2.5, level AAA) and the prescribed regression test already lives at `tests/integration/per-rule-coverage-end-to-end.test.ts:361-428` proving the rule fires under `level: "AAA"` AND surfaces a structured `perRuleCoverage[].skipReason: "gated_by_level"` row under AA. The corpus reporter saw 0 findings because their scan was at AA default — the gated-by-level row already provides the remediation signal. Adding `wcag22:4.1.2` to satisfies would be spec-incorrect (4.1.2 governs programmatic determinability, not context-change announcement).
- [x] **Q9-RULE-FOCUS-NOT-OBSCURED-FP-MODAL-BACKDROP** `focus/not-obscured` fires on `.modal` / `.modal-backdrop` containers in vendor stylesheets (bulk corpus, ≥dozen hits) — predicate misreads modal/backdrop as persistent chrome obscuring focus during scroll, but modals are conditional (display:none default until shown). Fix: gate predicate on absence of `display: none` default OR presence of a `:not(.show)` / `:not([open])` qualifier in the selector chain. Per AI-first doctrine "Heuristic emission is the symmetric twin of heuristic suppression."
- [x] **Q9-RULE-KEYBOARD-HANDLER-FP-NATIVE-BUTTON-CREATE-ELEMENT** `keyboard/handler-missing` FP on `const btn = document.createElement('button'); btn.addEventListener('click', ...)`. Same-file resolution gap — rule didn't track local var creation. Vanilla corpus. Fix: same-file backward resolution of the addEventListener receiver; when the receiver var is bound from `document.createElement('button'|'a'|'input'|...)`, the receiver is a native interactive element and the rule must not fire. Pairs with Q9-SUGGEST-FIX-NATIVE-BUTTON-ADDS-KEYDOWN-HANDLER (suggest_fix-side closure).
- [x] **Q9-RULE-HREF-EMPTY-FRAGMENT-FP-IN-DOCS-EXAMPLE-PROP** Illustrative `<a href="#">` placeholders inside JSX template-literal `code` props (`<Example code={\`<a href="#">…\`}/>` in `.mdx` docs files) fire `navigation/href-empty-fragment` as if production. CSS-framework corpus. Per AI-first doctrine, prefer reason-enrichment over carve-out. Fix: enrich reason with `containingElementHint?: "Example" | "CodeBlock" | "code prop in JSX template literal"` so the agent dismisses the example-context fire in one read. Reason-only — no severity change. Pairs with Q8-NAVIGATION-HREF-JAVASCRIPT-VOID-EXAMPLE-CONTAINER-HINT (open, sibling rule).
- [x] **Q9-EMPTY-HEADING-TEMPLATE-EXPRESSION-CONTENT** `semantics/empty-heading` fires at `error`/`high` on `<h4>{{ section.title }}</h4>` because the parser strips the templating directive before the rule runs. Static-site corpus. See Q9-EMPTY-HEADING-TEMPLATE-EXPRESSION-AT-ERROR for the severity-axis closure; this item is the predicate-axis closure: when the heading's only child is a template directive (`{{ }}`, `{% %}`, `${ }`, `<%= %>`, ERB), the rule must not fire as a violation — promote to review-candidate with reason framing the binding-resolves question.

