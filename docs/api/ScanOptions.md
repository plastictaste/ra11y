# ScanOptions

Interface export from `@ra11y/core`.

Options for `scan()`:

- `paths`: files or directories to scan.
- `standards`: optional standard ids. Defaults to `["wcag22"]`.
- `level`: optional WCAG level gate. Defaults to `"AA"`.
