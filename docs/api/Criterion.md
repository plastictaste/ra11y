# Criterion

Type export from `@ra11y/core`.

Describes one requirement inside an accessibility standard, including its criterion id, title, level, references, and automation metadata. `Standard.criteria` is an array of `Criterion`.
