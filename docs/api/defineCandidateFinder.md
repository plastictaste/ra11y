# defineCandidateFinder

Function export from `@ra11y/core/plugin`.

Identity helper for assisted manual-review candidate finders. It returns the same object and preserves literal types while checking the finder shape.
