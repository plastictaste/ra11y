# defineConfig

Function export from `@ra11y/core` and `@ra11y/core/plugin`.

Identity helper for `ra11y.config.ts` files. It returns the supplied config unchanged while giving editors type checking and completion.
