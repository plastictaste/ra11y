# ra11y backlog

The `/continue` skill reads this file and dispatches work to specialist subagents. Each item should be small enough that one specialist can finish it in under 20 minutes. When an item would produce more work, split it in place before dispatching.

Legend: `[ ]` open · `[~]` in progress · `[!]` blocked (reason in comment).

Closing an item = deleting its `- [ ] **<ID>**` line in the same commit that lands the work, with a `Closes: <ID>` (or `Drops: <ID>`) trailer in the commit message. The commit body carries the rationale; look it up later via `git log --grep "Closes: <ID>"` or `bun scripts/show-closed.ts <ID>`. See CLAUDE.md §9.7. Only the three states in the legend above are valid; `scripts/check-commit.ts` rejects untrailered deletions and any other state.

---

## Track Q15 — Multi-corpus AI-first sweep (2026-05-02)


---

## Track Q16 — Multi-corpus AI-first sweep (2026-05-05)

Second multi-corpus AI-first sweep on four shape-distinct corpora (CSS-framework distribution with documentation site; static-site-generator with mixed Liquid/ERB templates; vanilla-stack catalog of ~50 sibling demo subdirectories; vendor-template bulk catalog of ~100 sibling subdirectories with extensive duplicated vendor JS/CSS). 54 raw findings → 22 after cross-corpus dedupe → 18 actionable after filtering against existing Track Q15 items.

---

## Track Q17 — Multi-corpus AI-first sweep (2026-05-06)

Third multi-corpus AI-first sweep on four shape-distinct corpora (vendor-heavy CSS framework with SCSS + JS; Ruby static-site generator with template directives; tutorial-style HTML/CSS/JS collection; bulk HTML template catalog with vendor-heavy admin theme). 5 angles per corpus (scan_project / checklist / coverage / suggest_fix / bootstrap+detect_native_wrappers) returning structured JSON. ~80 raw findings → ~14 actionable after dedupe against Q15 + Q16 (the bulk of repeat findings — empty warningsDetails payloads, cross-surface meta drift, truncation reporter overlap, per-finding fixClass-vs-kind drift, severity-vs-conceded-uncertainty — confirmed reproduction on all four corpora and remain captured under the open Q16 items).

---

## Track Q18 — Targeted gap audit (2026-05-07)

Targeted gap audit across `src/mcp/`, `src/reports/`, response-shape sibling files, and the integration test suite — looking for AI-first consumer-doctrine drift not yet captured under Q15/Q16/Q17 and for invariant rules in the doctrine that ship without a pinning integration test. Findings ground out at four shape-level violations (concrete file:line evidence) and two invariant pins (named doctrine rules with no pinning test in `tests/integration/`).

---

## Track Q19 — Multi-corpus AI-first sweep (2026-05-07)

Fourth multi-corpus AI-first sweep on four shape-distinct corpora (a vendor-heavy CSS framework with SCSS + JS sources; a Ruby static-site generator with mixed template directives; a tutorial-style HTML/CSS/JS collection of standalone lesson folders; a bulk HTML template catalog with vendor-heavy admin theme). 5 angles per corpus (scan_project / checklist / coverage / suggest_fix / bootstrap+detect_native_wrappers) returning structured JSON. ~93 raw findings → 12 actionable after dedupe against open Q16 + Q17 items (the bulk of repeat findings — empty warningsDetails payloads on present-when-meaningful BinaryPresenceMarker codes, cross-surface meta drift, truncation reporter overlap, per-finding fixClass-vs-kind drift, severity-vs-conceded-uncertainty — confirmed reproduction on all four corpora and remain captured under the existing open items).



---

## Track Q20 — Multi-corpus AI-first sweep (2026-05-08)

Fifth multi-corpus AI-first sweep on four shape-distinct corpora (vendor-heavy CSS framework with SCSS + JS + Astro doc site; static-site-generator with Liquid / ERB templates and a `_layouts/` envelope tree; vanilla-stack catalog of ~50 sibling demo subdirectories; bulk vendor-template catalog of ~170 sibling subdirectories ~567MB on disk). 5 angles per corpus (`scan_project` / `checklist` / `coverage` / `suggest_fix` / `bootstrap` + `detect_native_wrappers`) returning structured JSON. ~58 raw findings → 18 actionable after dedupe against existing AI-first-consumer doctrine bullets and closed Q15/Q16/Q17/Q18/Q19 items. Two real-world fixtures landed RED (`scss-state-class-line-drift-on-nested-ampersand`, `document-rules-skipped-on-layout-envelope`) pinning the two false-position / false-negative reproducers most cheaply repro'd from sanitized synthetic source. One new doctrine bullet landed: "NextStep prose and structured channels must agree."

- [ ] **Q20-CODE-PROP-LITERAL-OVER-EMISSION** Rules emit at `severity:error, confidence:high` inside JSX `<Example code={`...`}/>` template literals: 1108 `navigation/href-empty-fragment` + 452 `navigation/link-no-href` on a doc-site corpus where the `jsx_code_demo_prop_parsed_as_live_dom` warning fires (72 .mdx files affected, propNames=['code']). Reason text hedges `inside code prop in JSX template literal — likely demonstration code, verify before fixing` while severity stays at `error`. Inconsistency: `forms/autocomplete-missing` fires on `<input>` nodes inside the same code-prop literals but `forms/placeholder-as-label` is silent — rule-family suppression-uniformity drift. Closure: when `jsx_code_demo_prop_parsed_as_live_dom` is in `warnings[]` AND emission's parent JSX prop is `code`, uniformly downgrade `severity → info` (or move to `reviewCandidates` surface) across rule families.
- [ ] **Q20-PER-FINDING-CONFIDENCE-NOT-PROPAGATING-PER-RULE-LIMITATION** `keyboard/handler-missing` ships per-rule `coverageConfidence: medium` with reason `cross_file_listener_resolution_not_attempted_by_rule`, but per-finding emissions ship at `severity:warning, confidence:medium` regardless. Observed 43/43 emissions on a vanilla-JS corpus where every selector resolved to a real `<button>` element wired by sibling `.js` — 100% false-positive rate by `confidence:medium`. Per "Per-finding confidence must reflect per-rule coverage limitations": propagate the per-rule degradation to per-finding `confidence` (or echo the reason as additive context in `couldBeWrongBecause`). The `applyPerRuleConfidenceDowngrade` cascade may exist but is not wired through `keyboard/handler-missing`'s emission path.
- [ ] **Q20-SUPPRESS-RECOMMENDED-MISUSED-AS-GUIDANCE** `kind: 'suppress-recommended'` ships when primary advice is an actionable edit rather than a conceded N/A: (a) `aria/dropdown-toggle-triple-aria-missing` returns `kind='suppress-recommended'` with `primary.approach='Add aria-haspopup="menu" and aria-controls=...'` — a real attribute edit; (b) `semantics/heading-hierarchy` ships `kind='suppress-recommended'` on a standalone HTML page with primary advice `Insert an <h1>`; (c) `plan.fixesByClass.suppressRecommended.source: 13` but `suggest_fix` on those findings returns `kind='guidance'`. Per "Suppress-recommended is a distinct discriminator from guidance": tighten the kind-discriminator predicate so `suppress-recommended` requires the rule's evidence model to have conceded the criterion may not apply (e.g. `fragment_input_no_document_envelope`, `parent_layout_supplies_envelope`); when primary advice is a positive edit, route to `kind: 'verify-in-source'` or `kind: 'guidance'`.
- [ ] **Q20-CROSS-SURFACE-ACTIONABLE-COUNT-DRIFT** Silent 1-count disagreement on the actionable-manual-items counter across project-rooted surfaces on identical cwd: `scan_project=23`, `bootstrap=23`, `checklist=22`, `coverage=22`. The cross-surface count invariant requires shared computation; observed drift is on the same conceptual counter rendered through two different code paths. Closure: shared tally helper as canonical source consumed by all four surfaces; pin via integration test on identical cwd.
- [ ] **Q20-VENDOR-PRIORITY-ASYMMETRIC-ACROSS-RULES** Vendor-classification (`vendor-library-version-detected: jquery`, `definite-vendor-distribution`) propagates uniformly to timer rules — `setTimeout` / `setInterval` review-candidates dim to `priority: 'low'` with `vendorPathHint: true` in reason — but does NOT propagate to `.innerHTML` / `.textContent` candidates emitted from the SAME vendor file. Those ship at `priority: 'high'` with no vendor-aware downgrade. Asymmetric handling on identical evidence is dishonest by rule family. Closure: shared vendor-aware-priority helper consumed by every candidate emitter; when `scanKind: 'buildArtifact'` AND vendor classification is confirmed, every candidate's priority dims uniformly.
- [ ] **Q20-BOOTSTRAP-EXCLUDE-RECOMMENDATION-DRIFT** `bulk_catalog_detected.suggestedExcludes` (5 globs) and `bootstrap.suggestedConfig.exclude` (582 entries) disagree on the same scope. Both ship from project-rooted surfaces classifying the same corpus; the agent that pastes the smaller list gets a vastly less-strict exclude than the larger. Closure: shared exclude-recommendation helper with one canonical output; the two surfaces format it differently (globs vs. explicit paths) but enumerate the same files.
- [ ] **Q20-PERRULE-REMEDIATION-WRONG-TEMPLATE** `perRuleCoverage[ruleId]` ships `skipReason: 'gated_by_level'` (rule requires AAA, scan requested AA) with `remediation: 'files of this extension exist at cwd but were pruned by additionalPaths / exclude / .gitignore / default build-dir skips; rerun with broader scope or unset the path filter that excluded them'` — that remediation describes a different `skipReason` (path-filtering) entirely. Affects `contrast/enhanced`, `motion/animation-from-interactions`, `navigation/link-target-blank-announcement`, `pointer/target-size-enhanced`. Closure: `skipReason → remediation` map fix so `gated_by_level` produces remediation like `re-run with --level AAA` and only `path-filtered` produces the current text.
- [ ] **Q20-PROJECTKIND-COLLAPSES-ASTRO-AND-MDX-INTO-JSX** `detect_native_wrappers.projectKind` and `bootstrap.wrappers.projectKind` ship `'jsx'` on a corpus that is overwhelmingly `.scss` + `.astro` + `.mdx` (a doc site). The classifier collapses Astro / MDX into `jsx`, so an agent reading `projectKind: jsx` assumes a React-style project and may mis-route subsequent calls (e.g. asking about React component-wrapper detection on an Astro corpus). Closure: extend `projectKind` to distinguish `astro` and `mdx` (or `static-site-generator` for the Liquid case) so the discriminator is honest.
- [ ] **Q20-FRAGMENT-CLASSIFICATION-SIGNALS-ALL-FALSE-BUT-KIND-ASSERTED** `meta.fragmentFiles[].fragmentClassificationSignals` ships all-false (`hasHtmlOpener: false, hasLayoutDirective: false, inLayoutsDir: false`) yet `kind: 'layout_include_partial'` is asserted on the same entry. The actual predicate that fired (path-prefix match on `_includes/`) is hidden from the surfaced signals. Affects 14 fragment classifications on a static-site-generator corpus. Per "Heuristic-mislabeled meta sub-fields are dishonest": every asserted `kind` must be derivable from at least one true signal in `fragmentClassificationSignals` (so add `pathSuggestsIncludePartial: true` for the `_includes/` predicate, etc.).
- [ ] **Q20-PARTIAL-PARSE-VS-FRAGMENT-INPUT-REASON-COLLISION** `partialParseFiles[].reason: 'Unclosed <html>'` fires on `_includes/top.html` — but the file is also in `fragmentFiles[]` with `kind: 'layout_include_partial'`, so the `<html>` opener absence is fragment-shape-expected, not parser-bailed. The `partial-parse` token reads as parser-failure when the cause is "fragment input lacks document envelope by design." Closure: distinct reason `fragment_input_no_document_envelope` for files also classified as fragments; reserve `partial-parse` for genuine parser bails on document-shaped sources.
- [ ] **Q20-SKIP-EXTENSION-WARNING-DROPS-AND-DOUBLE-REPORTS** `.mkdn` (1), `.xhtml` (1), `.php` (1) files appear in `detect_native_wrappers.inapplicable.filesByExtension` but are silently absent from `text_source_skipped` and `parser_routable_extensions_skipped` warnings. Separately, `.coffee` (21 files) is double-reported in both `text_source_skipped.parserRoutableExtensions` and `parser_routable_extensions_skipped`. Per "Per-tool lane and warning-set classification must agree" and "Truncation reporters must reconcile across warnings": every parser-rejected text-source extension reaches exactly one canonical skip warning; the other surfaces it via `seeAlso`. Files visible to one classification path must be visible to all parallel paths.
- [ ] **Q20-NO-CONFIG-FOUND-EMPTY-DETAILS** `warnings[]` ships `no_config_found` on all four project-rooted surfaces with `warningsDetails.no_config_found: {}` (empty object). Agent reads the warning name with zero specifics — no `searchedPaths` array, no `cwd` echo. Per "Empty `warningsDetails.<code>: {}` is dishonest": populate with `{ searchedPaths: [...], cwd: <resolved> }` so the agent can decide whether to bootstrap or whether the search missed an existing config.
- [ ] **Q20-PAIR-RULE-DOUBLE-EMISSION-ON-SAME-ELEMENT** `pointer/drag-alternative` AND `pointer/draggable-no-keyboard-alt` both fire at `severity: warning` on the same `<div draggable="true">` with overlapping criteria (`wcag22:2.5.7` + `wcag22:2.1.1`). The two emissions duplicate fix prose. Worse: `pointer/draggable-no-keyboard-alt` ships `fixClass: 'suppress-recommended'` on a real drag-only-no-keyboard-alternative violation — suggesting suppression where the predicate genuinely fails. Closure: when two rules with overlapping criteria fire on the same element, coalesce into one emission, OR have one rule suppress when its co-rule has already fired with the same criterion set.
- [ ] **Q20-CHECKLIST-NO-MIN-ENVELOPE-FALLBACK** `checklist` transport-fails on a 50-sibling vanilla-demo corpus (response 86260 chars, host-rejected) while `coverage` on identical cwd correctly emits a slim envelope (`warnings: ['response_dropped_files_oversize']`, `items[]` dropped, `nextStep` recommends scope-down). Per "Per-tool lane and warning-set classification must agree": the minimum-honest envelope fallback at `scan_project` and `coverage` must also exist at `checklist`. Otherwise the agent's first call to `checklist` on a bulk-vendor or bulk-demo corpus dies with no actionable diagnostic.
- [ ] **Q20-SCAN-FILE-FINDINGS-EMPTY-ARRAY-AMBIGUOUS** `scan_file` returns `findings: []` (empty array shape) alongside `findingsArrayDropped: true`, `truncated: true`, `totalFindings: 72`. The empty-array form is shape-identical to a clean-zero-finding scan; the truncation discriminator lives only in sibling booleans, not in the field's own shape. Per "Truncated containers must rename or sentinel": rename to `findingsTruncated: { totalFindings, returned: 0 }`, OR ship a sentinel `[{ truncated: true, reason: 'response_dropped_files_oversize' }]` so the field itself signals "no contents shipped."
- [ ] **Q20-BOOTSTRAP-DOES-NOT-PROPOSE-FIXTURE-EXCLUDES** `bootstrap.suggestedConfig.exclude` lists only build-output globs (`dist/**`, `js/dist/**`) while the same response's `warningsDetails.linked_stylesheet_local_unresolved.files` enumerates 14 visual-test fixture files under `js/tests/visual/*.html` and `js/tests/integration/index.html`. The classifier saw the fixtures (the local-unresolved warning fired on them) but did not propose to exclude. Pasting the suggested config leaves the fixtures in scope and the next scan re-emits hundreds of fixture-driven findings the user did not intend. Closure: when fixture-shaped paths surface in a partial-parse-fragility warning, propose them as `excludeCandidates` (separate field from `exclude` so the user opts in rather than auto-excluding test trees that may be authored).

---

## Track Q21 — Multi-corpus AI-first sweep (2026-05-08)

Sixth multi-corpus AI-first sweep on the same four shape-distinct corpora as Q20 (vendor-heavy CSS framework with SCSS + JS + Astro doc site; static-site-generator with Liquid / ERB templates and a `_layouts/` envelope tree; vanilla-stack catalog of ~50 sibling demo subdirectories; bulk vendor-template catalog of ~170 sibling subdirectories). Four parallel scan agents, each running 5 angles (`scan_project` / `checklist` / `coverage` / `bootstrap` + `detect_native_wrappers` / `scan_file` + `suggest_fix`) with hand-inspection of 3–5 representative files per corpus. ~85 raw findings → 26 actionable after dedupe against open Q15/Q16/Q17/Q18/Q19/Q20 items and against existing AI-first-consumer doctrine bullets. Two real-world fixtures landed RED (`navigation-link-no-href-on-href-empty-fragment`, `svg-webfont-title-missing-false-positive`) pinning the two cleanest false-positive reproducers from sanitized synthetic source.

- [ ] **Q21-PLACEHOLDER-IMAGE-HOST-FALSE-NEGATIVE** `media/alt-text-placeholder` predicate matches placeholder-shaped filenames but does NOT match placeholder image hosts. `<img src="http://placehold.it/700x400">` / `<img src="https://via.placeholder.com/300">` / `<img src="https://picsum.photos/200">` / `<img src="https://dummyimage.com/600x400">` all slip past at clean scan because the URL host carries the placeholder evidence rather than the filename. Closure: extend the predicate to recognize known placeholder image hosts (`placehold.it`, `via.placeholder.com`, `picsum.photos`, `dummyimage.com`, `placekitten.com`, `placebeard.it`) as the same evidence as placeholder-filename infixes.
- [ ] **Q21-VENDOR-CLASSIFICATION-MIN-INFIX-ONLY** Build-artifact classifier keys vendor classification on the `.min.*` infix only. `bootstrap.min.css` → `buildArtifact` lane (correct); identical-content sibling `bootstrap.css` (un-minified vendor copy in the same directory) → `source` lane (wrong). Observed 84 `contrast/minimum` errors on the un-minified vendor copy shipping at `severity:error` source-budget while the minified sibling correctly de-prioritized. Vendor-library identity is already a known signal (`vendor-library-version-detected: bootstrap`/`jquery`/`owl-carousel`); same logic should apply regardless of minification. Closure: extend the build-artifact classifier — when a file's basename matches a known library name AND a `.min.*` sibling exists in the same directory, classify as `buildArtifact` regardless of minification.
- [ ] **Q21-LINKED-STYLESHEET-RESOLVED-FILES-FLAGGED** `linked_stylesheet_local_unresolved` warning flags `<link href="…">` targets that DO exist on disk. Sample of 8 hand-checked HTML files: 28 hrefs whose targets are stat-able resolve to real files; the warning still flagged them. The resolver appears to flag any href whose target is outside the parsed scan set rather than files genuinely missing on disk. False-positive rate: ~80% on a vendor-heavy bulk corpus where most href targets sit in vendor subtrees the parser skipped. Closure: stat the resolved path against the filesystem before flagging; if it resolves, don't emit. Alternatively, rename the warning to disambiguate "target not in parsed scan set" from "target missing on disk" — they're different signals with different action hints.
- [ ] **Q21-LINKED-STYLESHEET-VS-TEMPLATE-EXPRESSION-COFIRE** `linked_stylesheet_local_unresolved` co-fires on hrefs that are visibly template expressions for which `template_expression_in_href` already fires. Same href (`{{ 'css/screen.css' | relative_url }}`, `<%= asset_path 'screen.css' %>`) ships under both warning codes. The unresolved-stylesheet claim is dishonest because the resolved value is unknowable statically — the template channel is the right home. Closure: gate `linked_stylesheet_local_unresolved` to skip hrefs containing template tokens (`{{ }}` / `{% %}` / `<%= %>` / `<%# %>` / `${}` for ERB / Liquid / Handlebars / template-literal interpolation); `template_expression_in_href` covers them honestly.
- [ ] **Q21-DOCUMENT-RULES-IGNORE-IN-FILE-TITLE-DIRECTIVES** `document/page-titled` fires on files whose in-file directives inject `<title>` at render time. Observed: a layout/include partial with `{% seo %}` (a canonical static-site-generator helper that emits `<title>`) on line 14 still flagged as "HTML document is missing a `<title>` element". The rule's reason text already concedes "title is template-injected (e.g. `{% seo %}`)" as a possibility yet still emits. Symmetric to existing `landmark-main` fragment handling. Closure: add `template_directive_provides_title` couldBeWrongBecause code; on detection of `{% seo %}` / `<%= yield :title %>` / `<title>{{ ... }}</title>` / `<title>{{= ... =}}</title>` / `<title>${...}</title>` in-file, gate the rule to a review candidate or suppress emission.
- [ ] **Q21-LANDMARK-MAIN-REASON-LEAKS-NO-BODY** `semantics/landmark-main` reason text says `looks like a layout wrapper or template partial (no <body>, ...)` on files where `<body>` IS present (e.g. `<body class="wrap">` on line 3). The boilerplate parenthetical clause leaks unconditionally regardless of actual body presence. Per "Reason text and severity must agree" applied to reason templating: conditionalize the `(no <body>, …)` parenthetical on actual `<body>` absence; render the bodyless-fragment evidence only when `<body>` is genuinely missing.
- [ ] **Q21-TEMPLATE-FILES-LITERAL-FALSE-FIRE-ON-BARE-FRONTMATTER** `template_files_parsed_as_literal` warning fires on pure-markdown files with frontmatter + ATX headings + bullet lists and ZERO `{{ }}` or `{% %}` directives. Predicate appears to fire on any `.md` + frontmatter shape rather than actual template-directive presence. Frontmatter strip is a different signal (already covered by `hasFrontmatterFence`). Closure: gate `template_files_parsed_as_literal` emission on `analysisCoverage.templateInterpolationFound.length > 0` so the warning code reflects its name.
- [ ] **Q21-TEMPLATE-FILES-LITERAL-MISSES-AUTHORED-INCLUDES** `template_files_parsed_as_literal.files[]` under-reports the affected file set on canonical fragment subtrees. Observed: 1 of 21 `_includes/*.html` files cited (the test-fixture sibling), the other 20 authored authored-fragment partials with `{{ }}` Liquid interpolation absent from the warning's payload. Predicate appears gated on rule-emission rather than directive-detection, so files where rules don't fire fall out of the warning's audit list — but the warning's job is signaling parse-as-literal happened, not which-rule-fired. Closure: predicate should fire on every file whose `meta.analysisCoverage.templateInterpolationFound[]` is non-empty, regardless of rule emission.
- [ ] **Q21-BULK-CATALOG-TRIGGER-DRIFTS-ACROSS-SURFACES** `bulk_catalog_detected.trigger` token differs across project-rooted surfaces on identical cwd: `scan_project` + `bootstrap` ship `slow_and_vendor_heavy` (with populated `durationMs`); `checklist` + `coverage` ship `bulk_and_vendor_heavy` (with `durationMs: null`). Same warning code, two predicates, four surfaces. Per "Per-tool lane and warning-set classification must agree": the bulk-catalog discriminator must agree across consumers. Closure: factor the bulk-catalog classifier into one shared helper run on every project-rooted tool; either ship a single trigger token or split into two distinct codes (`slow_and_vendor_heavy`, `bulk_and_vendor_heavy`) that fire by independent predicates instead of disagreeing under one code name.
- [ ] **Q21-COVERAGE-VS-SCAN-PROJECT-FILES-WITH-FINDINGS-DRIFT** `coverage.warningsDetails.response_dropped_files_oversize.totalFilesWithFindings: 113` and `scan_project.totalFilesWithFindings: 110` disagree by 3 on identical cwd. The cross-surface count invariant requires shared computation; observed drift on the same conceptual counter rendered through two different code paths. Closure: drive both counts through the shared scan-confidence helper that already computes `filesByExtension`; pin equality with an integration test on identical cwd.
- [ ] **Q21-WARNING-PROPAGATION-CORPUS-TO-PER-FILE** Corpus-level warnings (`jsx_code_demo_prop_parsed_as_live_dom`, `js_innerhtml_template_literal_unparsed`) ship affected-file lists on `scan_project` but the same warning is silently absent on `scan_file` for the listed files. Agent calling `scan_file` on a file the corpus-level warning identified gets zero signal that the per-file evidence model is degraded. Per the cross-surface count invariant extended to warnings whose predicate is per-file: every per-file warning must reach every consumer of that path. Closure: plumb per-file warnings (`jsx_code_demo_prop_parsed_as_live_dom`, `js_innerhtml_template_literal_unparsed`, `template_files_parsed_as_literal` on per-file scope) into the per-file warning emit path; the shared scan-confidence helper is the right home.
- [ ] **Q21-PER-RULE-CONFIDENCE-OUTRANKS-PER-FINDING** `perRuleCoverage[ruleId].coverageConfidence: "high"` ships while every emitted finding for that rule on the same response carries `confidence: "medium"` with a `couldBeWrongBecause` code (e.g. `template_directive_interpolation_unresolved`, `cross_file_evidence_bounded_not_attempted_by_rule`). Per-rule label outranks every honest per-finding label. Symmetric to the closed "Parser-failure invalidates per-file confidence" doctrine bullet but driven from per-finding evidence rather than parse failures. Closure: when all emitted findings of a rule on a response carry uniform per-finding downgrade reasons, per-rule `coverageConfidence` propagates the worst per-finding ceiling.
- [ ] **Q21-PER-RULE-FINDINGS-EMITTED-VS-INVENTORY** `perRuleCoverage[ruleId].findingsEmitted: 1, fired: true` on a rule whose `findings[]` array contains zero entries for that rule within the SAME response. Per-rule tally and per-finding inventory disagree internally. Observed on `navigation/link-descriptive-text` with `verboseMeta: true`. Closure: audit the perRuleCoverage emission accumulator for off-by-one between review-candidate and finding paths; pin invariant: forEach `perRuleCoverage` row with `findingsEmitted: N`, `count(findings[].ruleId == row.ruleId) == N` (excluding review candidates which count separately).
- [ ] **Q21-SCAN-FILE-NEXTSTEP-SELF-LOOP** `scan_file.nextStepStructured: { tool: "scan_file", args: { path: <same-path> } }` echoes the caller's request on responses where all findings ship at info-severity. Self-loop with no progression toward `suggest_fix`, `checklist`, or scope-narrowing. Distinct from Q20's prose-vs-structured-channel disagreement: here the structured channel routes back at the caller's exact args. Closure: on `scan_file` with no error/warning-severity findings, route `nextStepStructured` to `checklist` (when review-candidates exist) or `suggest_fix` on the highest-priority finding; never echo the caller's own arguments.
- [ ] **Q21-SCANNED-MINIFIED-FILE-INLINE-LIST-OVERSIZE** `scanned_minified_file` warning ships a full inline file list (~72KB / 767 paths on a canonical bulk corpus) while the larger-population sibling `scanned_build_artifacts_present` (1199 files) trims to `top: [10]` (~1.4KB). Inconsistent ship policy: the smaller-population warning is a primary slim-envelope-pressure source. Closure: trim `scanned_minified_file` the same way `scanned_build_artifacts_present` does — `count` + `topPath` + `top: [10]` paths — and ship full lists only behind `verboseMeta: true` or a paginated cursor.
- [ ] **Q21-METAFIELDSDROPPED-VS-SURVIVING-PAYLOAD** `metaFieldsDroppedForSummary: ["scannedBuildArtifacts", "perRuleCoverageSummary"]` lists fields as dropped while the same data ships intact under `warningsDetails.scanned_build_artifacts_present.top[]` (the build-artifacts data the drop-list claimed dropped is reachable elsewhere). Drop-list disagrees with surviving payload; agent reading the drop-list thinks data is gone when it's reachable through a parallel surface. Closure: reconcile `metaFieldsDroppedForSummary` against `warningsDetails`; entries in the drop-list must name fields that are actually unreachable from the response (or link via `seeAlso` so the agent can find the data on the surviving surface).
- [ ] **Q21-METAFIELDSDROPPED-SILENT-FIELDS-NO-SENTINEL** `response_dropped_files_oversize.metaFieldsDropped` names 10 dropped meta keys (e.g. `perRuleCoverageSummary`, `scannedBuildArtifacts`, `detectedFramework`, `catalogHint`, `filesByExtension`, `rulesEvaluated`, `analysisCoverage`); the `meta` block actually shipped retains its 9 surviving keys with NO per-field `{truncated: true}` sentinel. Agent reading `meta` sees `standards`/`filesScanned`/`durationMs` and cannot distinguish "no scan-confidence concerns" from "meta block was clipped to 9 keys." Per "Truncated containers must rename or sentinel": replace each dropped meta key with `{ truncated: true, reason: "response_dropped_files_oversize" }` sentinel, OR rename the parent block (`metaTruncated`) when sub-fields are stripped.
- [ ] **Q21-BOOTSTRAP-VIOLATIONS-COUNT-PRE-TRUNCATION** `bootstrap.scan.violationsCount` reports the pre-truncation tally even when `truncated_files_dropped` warning fires. Observed: `droppedFileCount: 86` of 156 scanned, `violationsCount: 297` (matches un-truncated `scan_project.violationsByScanKind.source: 297`). No twin `violationsCountAfterTruncation`; agent reading the headline can't tell if it reflects post-drop subset or unclipped scan. Per "Composite headline counts are dishonest": split into `violationsCountPreTruncation` + `violationsCountAfterTruncation` (or rename the single field to `violationsCountFromUnclippedScan` to make scope explicit).
- [ ] **Q21-SUGGEST-FIX-APPROACH-TRAILING-ELLIPSIS** `suggest_fix.primary.approach` is systematically truncated with `…` glyph and no truncation discriminator (e.g. `Add an accessible name to the link: set aria-label="…" on the <a>, or add a…`). Agent reading `approach` cannot distinguish authored ellipsis from clipping — the same ambiguous-empty-string failure mode at a different field. Closure: rename the truncated form to `approachHeadline` and document that the full prose lives in `explanation`, OR carry full prose without truncation. Trailing-ellipsis-without-sentinel must not appear in shipped fields.
- [ ] **Q21-PARSER-BAIL-CLASSIFICATION-SPLIT** `scan_file_parser_bail_no_findings` warning fires alongside `analysisCoverage.parseErrorFileCount: 0` + `partialParseFileCount: 0`, and `perRuleCoverageSummary` lists 88 rules at full confidence — the bail is announced via `warnings[]` only and never reaches the coverage classification arrays. The "Parser-failure invalidates per-file confidence" doctrine bullet requires per-rule confidence downgrade on parse failure; current code has the warning channel firing without the classification-array hookup. Closure: route bail emissions through the same path that populates `parseErrorFiles[]`/`partialParseFiles[]` (or a dedicated `parserBailFiles[]` array); downgrade per-rule confidence to `low` with `reason: "parser_bailed"` for all rules on the bailed file.
- [ ] **Q21-PER-FILE-EMISSION-COUNT-MISSING-TWIN** `scan_file.plan.actionableManualItemsBySource: { source: 1 }` headlines 1 actionable manual criterion but `reviewCandidates[]` ships 42 entries (all of one criterion). The headline counts criteria; the array counts emissions; no per-file twin field exposes the emission count. Agent reading `actionableManualItemsBySource: 1` budgets against 1 candidate while the response carries 42. Per "Composite headline counts are dishonest" applied to per-file scope: add `plan.actionableEmissionsForFile` (the per-file mirror of `checklist.summary.actionable.emissionsTotal`) so the criteria headline and the candidate-array length reconcile on every per-file response.
- [ ] **Q21-NESTED-SCSS-LINE-DRIFT-MULTI-RULE** Line-position drift in nested SCSS sources is not specific to `color/state-class-color-only` (Q20-SCSS-LINE-DRIFT). `motion/pause-stop-hide` reports the same off-by-N drift relative to the actual selector position on the same parser shape — selector `.placeholder-glow .placeholder` reported at the line of an earlier sibling block, actual selector dozens of lines below. Suggests the regression is in the shared SCSS line-counter / `walkCssRules` traversal, not rule-specific. Closure: audit every CSS-rule-emitting rule on nested-`&` selectors with multi-block sibling preambles; the helper-bound fix should propagate to all of them when Q20-SCSS-LINE-DRIFT lands.
- [ ] **Q21-DYNAMIC-IMG-CREATION-NO-ALT-FALSE-NEGATIVE** Plain-JS files calling `document.createElement("img")` (or `new Image()`) with no downstream `.alt = …` set return `findings: []` and ship `nextStep` prose claiming `Automated checks clean.` even though the constructed `<img>` will reach the DOM with no accessible name. The corpus-level warning `js_innerhtml_template_literal_unparsed` flags one related pattern but not this one. Closure: add a finder that surfaces `document.createElement("img")` / `new Image()` without subsequent `.alt =` / `.setAttribute("alt", …)` as a high-priority review candidate; mandatory when `scan_file_parser_bail_no_findings` fires the response cannot ship `Automated checks clean.` prose.

---

## Track V — v1.0.0 readiness

- [~] **V1-CHANGELOG-V1** Draft `## [1.0.0]` entry landed (1d784b7). Date placeholder `YYYY-MM-DD` stays until user says ship. Sections populated: Breaking Changes (exit-code freeze), Added (13 sub-groups), Changed (9 items), Deprecated (2 aliases), Fixed (19 items), Deferred (the four ADR 0018 items). New empty `## [Unreleased]` sits at top. Ready for `/release 1.0.0` when the user triggers.
