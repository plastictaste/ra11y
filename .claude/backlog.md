# ra11y backlog

The `/continue` skill reads this file and dispatches work to specialist subagents. Each item should be small enough that one specialist can finish it in under 20 minutes. When an item would produce more work, split it in place before dispatching.

Legend: `[ ]` open · `[x]` done · `[~]` in progress · `[!]` blocked (reason in comment)

## Ship state

- **v0.1.0 — ready to tag.** Code-complete: 54 rules, 4 standards, 9 formatters, 10 MCP tools, 4 reports, CLI wired, release workflow configured. Remaining work is the demo + the tag + publish (Track D).
- **v0.2.0 — in flight.** MCP hardening (Track M), review-candidate coverage + focus-ring cross-ref (Track R), real-world fixture corpus (Track F). Target: 3–4 weeks post-0.1.0.
- **v0.3.0+ — staged, not started.** MCP sampling (Track S), ecosystem integrations + public benchmark (Track E). Phase 20 work is deliberately deferred until after 0.2.0 ships and user feedback tells us which sampling-backed tool matters most. See ADR 0005 for the sampling architecture.
- **v1.0.0 — readiness (Track V).** Conformance capstone work is done in Track C. Remaining v1.0 gates: (a) five **detection gaps** found in the 2026-04-19 four-agent third-pass audit — alt-text walker scope (SVG image / role=img / canvas), button-name SVG `<title>` traversal, `<input type="image">` title recognition, contenteditable labels, `background-image` contrast surfacing; (b) nine **shape-honesty null sentinels** to conditional-spread; (c) eleven **fix-suggestion context-aware rewrites** (V1-FIX-AUDIT `[~]` acceptance gate); (d) **KB final sweep** to drain `GUIDANCE_BY_ID`; (e) **V1-CHANGELOG-V1** date-stamp on ship; (f) **V1-REGISTRY-AGGREGATE** layer-boundary hardening so ADR 0019's frozen `defineRule`/`defineStandard`/`defineCandidateFinder` exports function end-to-end instead of shipping as inert type-inference stubs (ADR 0022); (g) **V1-RESPONSE-ASSEMBLER** scan-family MCP response-shape seam so the AI-first consumer doctrine is enforced by one assembler call rather than 24 hand-rolled handlers before v1.0 freezes the shape (ADR 0024, proposed). Derived from 2026-04-19 three-agent + third-pass four-agent gap analyses plus 2026-04-20 architectural audit.

## Dispatch model (parallel tracks, not sequential phases)

Tracks below are independent. `/continue` picks the next open item from each of up to 3 active tracks per turn and dispatches them in parallel (details in `.claude/skills/continue/SKILL.md`). Within a track, items run in order — some tracks have sequencing; cross-track work is always parallelizable.

Active tracks: **D** (docs/release) · **M** (MCP hardening) · **R** (rules + review candidates) · **F** (real-world fixtures) · **S** (MCP sampling) · **E** (ecosystem/evals) · **Q2** (agent-consumer feedback round 3) · **Q3** (Bootstrap design-system field test, 2026-04-20) · **Q4** (Jekyll static-site-generator field test, 2026-04-20) · **Q5** (50projects50days vanilla HTML/CSS/JS field test, 2026-04-21) · **Q6** (website-templates bulk-template catalog field test, 2026-04-21). Track Q (rounds 1-2) closed 2026-04-17.

Staged tracks: **C** (conformance-claim gaps — v0.3.0 foundation + v1.0.0 capstone). Tracks S and E were promoted on 2026-04-17 after the user directed "go all the way without releasing until finalized" — M/R/F are complete, so the remaining pre-release work spans S and E. ADR 0005 §Follow-up work still applies to the speculative tool choices inside S; foundation items (sampling.ts, capability, prompt library, KB docs) are safe to build.

Track Q was added on 2026-04-17 in response to a 10-agent independent eval brief — MCP shape honesty + silent-failure elimination, all derived from real consumer pain on an external React codebase.

Track C was added on 2026-04-17 from a gap analysis on "what's missing to let an agent fully claim WCAG 2.1 AA." Four gaps: runtime-evidence ingest, attestation ledger, process-level scope, conformance statement. None widen detection (no new rules); they widen what an agent can defensibly *say* after using ra11y.

---

## Track D — Docs & release

Owner: `release-captain` + `doc-writer`. Blocks nothing; can ship independently.

### v0.1.0

- [ ] asciinema demo recording embedded in README (needs user: `brew install asciinema`, then interactive `asciinema rec docs/demo.cast` of a real terminal session scanning a sample project; can't be automated inside /continue because it requires a TTY)
- [x] Version bump confirmation (package.json already reads `0.1.0`; verify + commit a release-prep chore if anything else drifts) (a0a805b — README status note, terminal version example, config.md stale forward-ref, writing-a-rule.md afterProject note; no drift in CHANGELOG date, llms.txt absent)
- [ ] Tag `v0.1.0` and push (triggers `release.yml` → npm publish with provenance)
- [ ] GitHub release with changelog excerpt

### v0.2.0

- [x] Release notes for 0.2.0 — drafted under `## [Unreleased]` in CHANGELOG.md (1eabf39). Covers M/R/F/S/E deltas; dated header waits for release.
- [x] Migration notes if any track introduces a breaking MCP shape (b2ed366 — warranted; four hard breaks in response-output shapes: suggest_fix suggestion→fixPaths+kind, checklist.summary.automatedCoverage trimmed to gloss, untargetedCriteria rename, activeNativeWrappers string[]→tagged-object-list; filePath param alias and configure dispatch are input-side compat, not output-side; docs/migrations/0.1-to-0.2.md created)

---

## Track M — MCP hardening

Owner: main session. Internal ordering: baseline first (unlocks scan-diff), then apply-fix, then prompts/resources/capability, then structured errors, then tests last.

### v0.2.0

- [x] `src/mcp/tool-baseline.ts` — create/check/update a baseline from within an MCP session (pairs with Phase 14 baseline work)
- [x] `src/mcp/tool-scan-diff.ts` — compare current scan vs a baseline; emit only new findings
- [x] `src/mcp/tool-apply-fix.ts` — take a `suggest_fix` result, apply the search/replace, re-scan, return the delta (opt-in via `--allow-write` session flag)
- [x] Prompt templates under `src/mcp/prompts/` — reusable `prompts/list` entries for triage / fix / audit / VPAT narrative workflows (pure strings, no template engine)
- [x] `resources/list` support — expose `docs/kb/**` as MCP resources so attached agents can retrieve KB entries without filesystem access
- [x] Capability declaration polish: `logging`, `completions` where meaningful, `roots` so `scan_project` respects the host's declared project boundaries
- [x] Structured errors across tools — replace text-only error payloads with `structuredContent` where the result is machine-consumable (coverage, checklist, list_rules, review_candidates)
- [x] `tests/integration/mcp-resources.test.ts` — resources/list + resources/read round-trip
- [x] `tests/integration/mcp-prompts.test.ts` — prompts/list + prompts/get round-trip

---

## Track R — Rules + review-candidate coverage

Owner: `rule-implementer` (rules) + main session (review finders). Rules within the track are independent; dispatch 3 in parallel when you have 3 open items.

### v0.2.0

- [x] `src/review/finders/consistent-navigation.ts` — wcag22:3.2.3. Compare `<nav>` children structure/order across route files; flag divergent routes as candidates.
- [x] `tests/unit/review/finders/consistent-navigation.test.ts` — ≥3 positive, ≥3 negative, ≥1 edge case against fixtures under `tests/fixtures/review/consistent-navigation/`.
- [x] Tailwind focus-ring cross-reference for `focus/outline-visible`. When `.classname:focus-visible { outline: none }` currently downgrades to `info`, cross-reference against `tailwind.ts` class output: if an element with that className also carries `focus-visible:ring-*` / `focus-visible:outline-*` / `focus-visible:shadow-*` utilities, auto-resolve the info candidate. Not heuristic suppression — a named class-token link is concrete evidence. Extends existing parser output; no new architecture.
- [x] `src/rules/aria/conflicting-role.ts` — wcag22:4.1.2. Flag explicit `role` values that contradict the native element's implicit role: `<button role="link">`, `<a role="button">` without href/handler coupling, `<nav role="main">`, etc. Real 4.1.2 failure — AT announces one thing, browser behaves like another. `docs/kb/concepts/aria-valid-roles.md` already promises this rule exists; implementation fills the gap. Explicitly NOT covering *redundant* role (`<a role="link">`) — pure ARIA code-smell with no WCAG hook; revisit when a best-practices standard lands. Needs implicit-role table in `src/engine/ast-helpers.ts` or a co-located map; pairs with `tests/fixtures/{good,bad}/aria-conflicting-role/` and ≥3 positive/≥3 negative unit tests.

### v0.3.0

- [x] **R-2.3.1-FLASHING** `src/review/finders/flashing-content.ts` — wcag22:2.3.1 + wcag21:2.3.1 (Three Flashes or Below Threshold, Level A). Surface review candidates for static signals of flashing/high-motion content: `<video autoplay>` without a `prefers-reduced-motion` guard, `<canvas>` + `requestAnimationFrame`/animation-library calls outside `@media (prefers-reduced-motion: reduce)`, high-frequency CSS `@keyframes` (opacity/transform cycles faster than ~3Hz) not reduced-motion-gated, `<marquee>` / `<blink>` legacy tags. Follows the `motion-actuation.ts` / `pointer-input.ts` finder shape: reason-text frames the question; agent reads the file and verifies the physical thresholds (3/s flash rate, red-flash area) that static analysis can't. Closes the last A+AA criterion with no static signal (gap surfaced during 2026-04-17 WCAG 2.1 AA coverage audit — all other manual-only A+AA SCs already ship finders or have tracked work). Fixtures under `tests/fixtures/review/flashing-content/`; ≥3 positive, ≥3 negative, ≥1 edge case; kb entry via `/fix-drift`.

---

## Track F — Real-world fixture corpus

Owner: `fixture-curator` + `test-author`. **Sequenced: ADR → harness prototype → fixture backfill (9 items in parallel).** The 9 fixture cases cannot start until the harness lands.

### v0.2.0

- [x] ADR 0006 `docs/adr/0006-real-world-fixture-harness.md` — resolve the five design questions (assertion shape, per-fixture config, sanitization policy, golden-output generation, harness architecture). Chosen positions go in the ADR.
- [x] Harness prototype against `tests/fixtures/real-world/tsx-generics/` validating the `FixtureAssertions` primitives from the ADR before committing to the shape.
- [x] Extend `fixture-curator` subagent definition in `.claude/agents/fixture-curator.md` with the new `tests/fixtures/real-world/` conventions once the ADR lands.
- [x] `real-world/tsx-generics/` — Pick<T,K>, ForwardRefRenderFunction<...>, generic function calls. Guards commit `2968d87`. Assertion: zero-parse-errors.
- [x] `real-world/spa-shell-vite/` — Vite-style `<div id="root">` + module script index.html. Guards `bc3aae4`. Assertion: wcag22:2.4.5 candidate reason includes "SPA index shell".
- [x] `real-world/tailwind-coverage/` — 60 JSX files with utility-class strings, zero CSS. Guards `4700a13`. Assertion: analysisCoverage.hints includes "Tailwind usage detected" + `additionalPaths: ["dist/assets"]`.
- [x] `real-world/logotype-annotation/` — `<img className="site-logo" alt="Acme">`. Guards `71b9954`. Assertions: wcag22:1.4.5 reason contains "logotype exemption"; wcag22:1.4.9 reason does NOT.
- [x] `real-world/timing-role-hints/` — useDebouncedCallback, authManager, telemetryService. Guards `3ada44a`. Assertion: wcag22:2.2.1 reason contains "likely not user-facing" (updated live: now guards "session-keepalive / debounce / animation" + reverted-enrichment absent).
- [x] `real-world/template-directives/` — Jinja `{% extends %}` + `{{ x }}`. Guards `a554d27`. Assertion: analysisCoverage.templateDirectiveHandling includes "parsed as literal".
- [x] `real-world/opaque-components-top/` — 10 PascalCase components with varied call-site counts. Guards `0ee28e1`. Assertion: analysisCoverage.opaqueCustomComponentsTop is a ranked list of length ≤5.
- [x] `real-world/suppression-reason-slot/` — pragma with `: reason` syntax. Guards `d820186`. Assertion: meta.suppressions contains an entry with `reason: "..."`.
- [x] `real-world/autodetect-attribution/` — autoDetectWrappers scan; assert sessionNativeWrappers is absent (regression on `6821b77`).
- [x] Policy change: update `CLAUDE.md` §7 and §17 — when fixing a real-world bug (not adding a new rule from spec), add a sanitized repro to `tests/fixtures/real-world/<case>/` FIRST. Unit tests are for invariants; behavior-rehearsals migrate to real-world fixtures on touch.

---

## Track S — MCP sampling

Owner: `parser-author` + main session. Promoted from staged 2026-04-17. Foundation items (sampling.ts, capability plumbing, prompt library, KB docs) land freely; the three speculative LLM-backed tools (`resolve-component`, `verdict-candidate`, `draft-vpat-narrative`) still need a concrete use-case selection per ADR 0005 §Follow-up work — surface them as review candidates before wiring.

### v0.2.0 (foundation)

- [x] `src/mcp/sampling.ts` — client helper calling `sampling/createMessage` on the host with timeout + max-tokens budget
- [x] Server capability declaration: read host `sampling` capability from `initialize.params.capabilities`; graceful fallback to "return the prompt for the agent to run" via `SamplingNotSupportedError` on hosts that decline
- [!] `src/mcp/tool-resolve-component.ts` — verifies PascalCase wrappers via host-sampled source read. ADR 0005 §Follow-up: pick 1–2 speculative tools based on user feedback before wiring; this one is a candidate.
- [x] `src/mcp/tool-verdict-candidate.ts` — pass/fail with reasoning for a review candidate + its `reviewPrompt`. ADR 0005 §Follow-up: same caveat. (edbe3d1, turn 21)
- [x] `src/mcp/tool-draft-vpat-narrative.ts` — drafts the VPAT "Remarks and explanations" cell per criterion. ADR 0005 §Follow-up: same caveat. (c733c12, turn 24)
- [!] `src/mcp/tool-triage-findings.ts` — pure (no sampling) triage that labels each finding. Scope of the label schema is ambiguous without downstream consumers (the three tools above are its consumers); define + pick one before dispatch.
- [x] Prompt library under `src/mcp/prompts/` — checksum registry for version-pinning (9cfc547). Surfaced on `prompts/list` + `prompts/get` via `_meta.checksum`. Substitution was already in place.
- [x] `tests/unit/mcp/sampling.test.ts` with a fake host recording sampling requests
- [x] `tests/integration/mcp-sampling.test.ts` with a scripted host adapter — scripted host exercises `verdict_candidate` end-to-end; deadlock fix (ff2869d) in `server.ts` unblocks stdio read loop. (a37f154, turn 23)
- [x] Docs: `docs/kb/architecture/mcp-sampling.md`
- [x] Docs: `docs/mcp/prompts.md`
- [x] `/audit` MCP prompt template: end-to-end workflow (scan → triage → verdict → VPAT draft) — (f4a960e, turn 26, 2026-04-21) blocked on the three speculative tools landing; template already exists as a prompt (`audit.ts`), the "MCP prompt template" wording refers to the end-to-end wiring.

---

## Track E — Ecosystem & public benchmark

Owner: `doc-writer` + main session. Promoted from staged 2026-04-17. Items expand the agent-host matrix and establish the public quality story.

### v0.2.0

- [x] Prompt evals harness in `tests/evals/` — measure each sampling prompt's accuracy against a labeled fixture set; CI-gated against a scripted host (no real LLM calls). Paired with Track S. (committed d00535b)
- [!] `examples/ra11y-in-claude-code/` — reference `.mcp.json` + sample `CLAUDE.md` section showing triage → verdict → draft-VPAT inside Claude Code. Blocked on the Track S speculative tools landing (the workflow references them).
- [!] `examples/ra11y-in-cursor/` — blocked externally on Cursor's MCP host shipping sampling.
- [x] VS Code extension skeleton under `integrations/vscode/` — wraps the MCP server for IDE-native findings (f0508dd). Sibling project with isolated toolchain; no runtime deps leak into @ra11y/core. Explicit non-goals (marketplace, EDH smoke test, CodeActionProvider, per-file scan-on-save, streaming, multi-root, reconnection) listed in README for follow-up.
- [x] Public benchmark: `benchmarks/a11y-tool-comparison.md` — scaffold (2f5539a) + ra11y-side numbers via `scripts/benchmark-run.ts` (f3878e3). Cold start 41.3ms, 10/100/1000 files 3.4/28.3/235.9ms, real-world corpus 72.4ms for 9 fixtures. Competitor columns remain "pending" until those tools are added as isolated dev deps.

---

## Track Q — Agent-consumer feedback

Owner: main session + general-purpose. Source: 10 independent agent runs against an external React codebase (2026-04-17 eval brief). All accepted items are MCP shape/honesty fixes — none touch detection logic. Dispatch in parallel; each touches a different surface.

### v0.2.0 — accepted (P0/P1)

- [x] **P0-E** Top-level `warnings: string[]` for silent-failure modes on `scan_project` / `scan` responses (31e040f). Codes wired: `scanned_zero_files`, `root_source_defaulted` (scan_project only), `no_config_found`, `tailwind_detected_css_undercounted`, `template_files_parsed_as_literal`. Conditional-spread; never `warnings: []`.
- [x] **P0-C** Populate `snippet` on every `review_candidates` and `checklist` candidate (9ba1c2b). ±3 lines, de-indented, 300-char cap, omitted (not `""`/`null`) when file:line is absent. Per-response source cache so each file is read at most once.
- [x] **P0-D** Widen `suggest_fix.oldText` to a unique anchor window (50b4514). Three-step ladder (opening-tag cluster → ±1 line → bracket expansion), 200-char cap, falls back to original narrow edit + `caveat` when no unique window fits. `apply_fix`'s existing literal-match path consumes the widened pair without changes. Bonus: extracted `buildSuggestFixPayload` to its own file to keep `tools.ts` under the 500-line budget.
- [x] **P1-F** AST-verify auto-detected native wrappers (c725cd0). One-hop probe: matches PascalCase basename to a `*.{tsx,jsx,ts,js}` file and inspects the first JSX element's tag name; native interactive root → `confirmed`, anything else → `assumed`. Only `confirmed` enter `activeNativeWrappers` and silence findings; `assumed` are surfaced under `bySource.fromAutoDetect.assumed` but findings on them are NOT silenced. No name-pattern matching, no transitive following.
- [x] **P1-J** Stable `findingId: string` on every Violation (49f9227). Truncated sha256 of (ruleId, relativeFilePath, lineContextHash); 12 hex chars. Line numbers NOT in the hash by design (drift resilience); ±3-line source context IS. Path normalization to relative-form so IDs match across machines. Baseline cross-run matching now uses findingId; legacy SHA1 helper kept as `fingerprintOf` for any straggling callers.
- [x] **P1-M** `plan.actionableManualItems` + `plan.untargetedCriteria` both top-level; summary leads with the actionable count (ab35222). Old composite `manualReviewRequired` removed.
- [x] **P1-H** `plan.mechanicalEditsAvailable` + `plan.guidanceFixesAvailable` both top-level (ab35222). Old composite `fixSuggestionAvailable` removed.
- [x] **P1-K** `nextStepStructured: { tool, args }` alongside prose `nextStep` (ab35222). Both omitted together when the next step is generic; aligned pair by construction.
- [x] **P1-L** Concrete `editCandidate` for `label-in-name` non-contiguous fixes (53ab36b). Synthesizes `aria-label="<visible text>: <remaining aria-label words>"` only when diagnosis is `isInterleavedExpansion`; case-insensitive dedupe of overlapping tokens. Field is `editCandidate` (NOT `edit`) so kind stays `guidance` — agent decides whether to apply.

### v0.2.0 — accepted (P2)

- [x] **P2-V** `criteriaTitles: string[]` aligned index-for-index with `criteria` (70a9cd7). Standard-filter looks up titles from the criterion registry; scanner stamps both at violation construction; formatters and MCP `formatFinding` forward.
- [x] **P2-N** `plan.limitations` emitted on every scan response, not only clean ones (turn 7). Prevents agents from overclaiming conformance on mixed-result responses.
- [x] **P2-P** `opaqueCustomComponents.names` inlined on every response when count ≤ 50 (c1f7e6b). Above 50 keep the current top-5 + `verboseMeta` pattern. Named constant `OPAQUE_COMPONENT_INLINE_NAMES_MAX = 50` with size-budget rationale.
- [x] **P2-R** `file` is canonical across suggest_fix + apply_fix; `filePath` accepted as deprecated alias (c2ea3b5). Using the alias fires `warnings: ["deprecated_param_filepath"]`; passing both raises a structured error.

### v0.2.0 — accepted (from 20-run combined brief, 2026-04-17)

Net-new items from round 2 of the consumer eval (round 2 explicitly probed edge cases — bogus `cwd`, `changedOnly`, subpath scans, large-directory overflow). 4 unanimous round-2 issues + several single-observer R2 finds; all are correctness or honest-shape, none are heuristic suppression.

- [x] **P0-F** Hard-error envelope on silent-failure paths (6a50836). `code: "cwd-not-found"` on `scan_project` with nonexistent `cwd`; `code: "scan-paths-not-found"` on `scan` when every requested path is missing. Discriminator: did the user point at something that exists? Yes → soft signal (warnings: ["scanned_zero_files"]); no → hard error envelope. Categorically different cases now have categorically different shapes.
- [x] **P1-CHG** `changedOnly: true` silently falls back to a full scan with no staged files (7824f81). Picked the hybrid: hard-error envelope (`code: "no_staged_files"`) for the in-repo case, honest fallback signal (`scanMode: "full-fallback"`, `fallbackReason`) for the not-in-a-git-repo case. Same lying-field bug exists in `tool-scan-diff.ts` per agent report — follow-up below.
- [x] **P1-CFG** Superseded by **P0-F** (6a50836). A bogus `cwd` now returns a structured `cwd-not-found` error envelope rather than a successful-looking empty scan, so the schema-presence-check failure mode no longer applies — the envelope shape is categorically different from the success shape, and agents branch on `isError: true` + `code`.
- [x] **P1-OVF** scan_project pagination (ab23bd5). `limit` (default 200, clamped [1, 2000]) + `offset` (default 0) input params; response carries `truncated: true` + `nextOffset` + `totalFilesWithFindings` when the cap truncates, else omits them. Offset-based (stable across rescans). Scan still runs over everything — only the emitted `files` array is capped; `plan.totalFindings` reports the full pre-truncation tally so page 1 doesn't mislead.
- [x] **P1-DUP** Dedupe `reviewPrompt` text in `review_candidates` (496b2e1). Top-level `prompts: { [criterionId]: { text } }` keyed map; candidates omit the per-item `reviewPrompt`. Field omitted when zero prompts apply. Lossless.
- [x] **P1-FILE-ENV** `scan_file` envelope parity (70a9cd7). Now carries `meta.configSource`, `activeNativeWrappers`, `rulesEvaluated`, `nextStep`, `opaqueCustomComponents`. Reused scan_project helpers rather than duplicating.
- [x] **P1-FILE-DEDUP** `scan_file` cross-standard dedup (70a9cd7). Same finding under multiple loaded standards collapses to one with `criteria: [...]`, matching scan_project. Shared dedup pass in `review-candidate-dedup.ts`.
- [x] **P1-ACCT** `analysisCoverage.opaqueCustomComponentsExcludedByAutoDetect` surfaces the subtraction count when auto-detect contributed (457635d). Agents computing "with-flag count + excluded = without-flag count" now see the scan-scope of the flag in the same field it affects.
- [x] **P1-IGN** Subpath scans now honor root `.gitignore` (389ba58). Walks up to git root, collects ancestor `.gitignore` files in order, translates patterns to scan-root-relative form, then enumerates from `cwd`. Per-scan memoization. Edge cases handled: `**/`-leading patterns, anchored-at-scan-root, anchored-outside-scan-root (dropped), FS-root termination. `!`-negation re-includes still unsupported (pre-existing glob.ts limitation, called out in docs).
- [x] **P1-CONF** Typed `confidence` on every manual-review candidate (3d258a3). `"high" | "medium" | "low"`, same enum as automated findings. Each finder picks its default; `priority: "high"` on checklist items stays (different axis).
- [x] **P2-BASE** `meta.baselineStatus: { exists, path, lastModified }` on scan_project when `.ra11y-baseline.json` exists at the scan root (457635d). Omitted when absent. Prevents re-proposing fixes for grandfathered violations without the baseline-check round-trip.
- [x] **P2-BUILD** `meta.scannedBuildArtifacts: [paths]` + `warnings: ["scanned_build_artifacts_present"]` when scanned set includes compiled-CSS / bundler-output (c2ea3b5). Findings on those files STILL surface — this is labeling, not filtering. Detection is deterministic (escape-bracket utility selectors, size threshold, /dist/ /build/ /static/assets/ path markers).

### Considered and rejected (per CLAUDE.md §1)

- **P0-A** Heuristic pre-filter for `wcag22:2.2.1` setTimeout/setInterval candidates by filename (`hooks/useDebounce*`, `telemetry/`, `auth/`), enclosing-function name regex, and duration threshold. → rejected per **§1 "Numeric-threshold heuristics are suppression"** + **"No heuristic suppression, even for spec carve-outs"** + **§17** "Adding a numeric-threshold gate." A 4.5s debounce and a 4.5s session timeout are indistinguishable from static analysis. The reason-text enrichment shipped under Track F (`real-world/timing-role-hints`) — "session-keepalive / debounce / animation" — is the correct mechanism. If agents still struggle, sharpen the reason text further; do not filter the candidate list. **Re-affirmed in 20-run brief (P0-A there, 20/20 agreement on the noise — but the noise is real signal the agent dismisses, per CLAUDE.md §1.**
- **P0-B** Tighten `wcag22:3.2.2 On Input` to skip `onChange` handlers whose body is `(e) => setX(e.target.value)`. → rejected for the same reason. The agent reading the handler body is the only correct arbiter; a controlled-input setter and a `navigate()` call are both `onChange` from the AST. Enrich the `reason` text with the detected handler shape ("body calls a single React setter") as additive context if the existing reason is thin; do not drop the candidate.
- **P1-I (10-run brief)** SPA-mode / file-class awareness — auto-suppress `2.4.5 Multiple Ways` / `1.4.5 Images of Text` / `2.4.1 Bypass Blocks` on server-template SPA shells. → rejected per **§1 "No heuristic suppression, even for spec carve-outs."** Whether a template is "the SPA shell" or "the start of a content site" cannot be statically determined. The deterministic disable pragma (`<!-- ra11y-disable wcag22:2.4.5 -->`) is the durable mechanism. Reason-text enrichment ("template parsed as literal — verify whether navigation is owned by the SPA") is acceptable; suppression is not.
- **P2-W (10-run brief) / P2-Y (20-run brief)** Severity downgrade for `aria/hidden-focus` when descendants are `disabled` + container has `pointer-events-none`. → rejected per **§1 "Don't downgrade priority to hide things."** Severity is for sorting; downgrading hides the candidate from agents that filter by severity. The reason text already explains state-dependent nature; that is enough. If a `caveat` field would help, add it as additive metadata at the same severity.
- **P1-R (20-run brief)** Tailwind utility-class awareness on `layout/reflow-hardcoded-width` — skip or downgrade when the rule fires inside `.w-\[Npx\]` selectors on compiled-CSS paths and no JSX consumer exists. → rejected per **§1 "No heuristic suppression"**. Path-pattern + selector-shape suppression replaces honest "please verify" with false confidence; the agent reading the file can confirm "this is a Tailwind utility-class definition" in one read. Pair with **P2-BUILD** above (the build-artifact warning) so the agent gets the labeling signal without losing the candidate.

### Deferred (worth doing eventually, not in this batch)

- **P1-G** Auto-detect compiled-CSS output paths via vite/next/tsup config. The current `additionalPaths: ["dist/assets"]` hint is wrong for many projects. Worth doing properly (read config, surface as `meta.inferredBuildOutput`), but scope is larger than the rest of Track Q. Park for later sprint.
- **P2-Q** Batch variant of `suggest_fix({ findings: [...] })`. Nice-to-have; round-trip reduction is real but not urgent.
- **P2-S** `plan.candidateCountsByCriterion: { ... }` histogram. Cheap, but no agent in the brief said they were blocked on it. Park.
- **P2-T** Gate `unusedNativeWrappers` on full-scan only. Single-observer (run #8 only); the existing `unusedNativeWrappersNote` already disclaims. Low ROI.
- **P2-U** Promote `absentDeclaredWrappers` to `meta.configHealth.staleWrappers` on `scan_project`. Single-observer; nice but not urgent.

---

## Track Q2 — Agent-consumer feedback (round 3, 10-agent eval)

Owner: main session + general-purpose. Source: a 10-agent parallel eval against an external React/Vite/TS/Tailwind codebase — each agent exercised a different MCP slice in a "you-find, I-fix" workflow. Round 3 ran after Q (rounds 1-2) closed, on the post-Q shape. All accepted items are shape-honesty, batch-primitive, or workflow-gap fixes — none are heuristic suppression.

### v0.2.0 — accepted (P0)

- [x] **Q2-PROSE** Top-level `referenceGuide.suppressPlacement: { [ext]: prose }` on scan / scan_file / scan_project / scan_diff responses; findings no longer carry `suppressPlacement` inline. Keyed by file extension (`tsx`, `css`, `html`, `htm`, `default`), populated only with extensions that actually have findings, omitted entirely when no findings exist (honest conditional-spread). `suppressWith` stays inline per finding — short, rule-ID-specific, ready-to-paste, not worth hoisting. apply_fix/baseline still use the old per-finding shape (small response bodies, no hoist benefit); documented as intentional scope. New module `src/mcp/reference-guide.ts` owns the builder + language mapping. `analysisCoverage.rulesByExtension` is already top-level — not actually per-finding prose, so nothing to hoist there.
- [x] **Q2-FIXKIND** Per-finding `fixClass` discriminator inline on every violation (bbbecf1 + 0da8a45). `mechanical | guidance | runtime-only | verify-in-source`. Stamped by the engine from required Rule.fixClass metadata; forwarded through MCP formatFinding. Distribution: 15 mechanical, 8 guidance, 2 runtime-only, 24 verify-in-source. ADR 0007. Named `fixClass` not `fix.kind` to avoid collision with existing `Violation.fix` auto-fixer shape and `suggest_fix.kind` 2-value axis.
- [x] **Q2-REASON** Bare `ra11y-disable` pragmas (no `: reason`) emit a review candidate under `suppression/no-reason` (d69175f). New `ra11y:` process-rule namespace with one criterion (`ra11y:suppression-no-reason`); engine activates `RA11Y_PROCESS_CRITERION_IDS` unconditionally so the finder fires regardless of enabled standards. Suppression semantics unchanged — pragma still honored; additive accountability signal.
- [x] **Q2-RULEDETAILS** `includeRuleDetails: "none" | "unique" | "all"` accepted on `scan` + `scan_project`; when set, response carries `ruleCatalog: { [ruleId]: { description, rationale, normativeQuote, goodExample, badExample, references } }` at the top level. `unique` is filtered to ruleIds that actually fired in the scan; `all` includes every loaded rule for first-contact caching. Default `none` keeps the baseline shape (field omitted entirely, not `{}`). New module `src/mcp/rule-catalog.ts` owns the builder, `collectFiredRuleIds`, the shared JSON-Schema fragment, and a `ruleCatalogField` conditional-spread helper so both tools share one code path. Mirrors the Q2-PROSE inline-once/reference-from-findings architecture.

### v0.2.0 — accepted (P1)

- [x] **Q2-HUNK** `scan_diff` now accepts `hunksOnly: boolean` + `comparisonRef` (ff21fa5). Shells out to `git diff --unified=0 <ref>` via `spawnSync` with array args (zero-dep, no shell injection); parses hunk headers per file; filters findings to `(filePath, line)` inside a hunk. Baseline path is the default and untouched. Error envelope: `not-a-git-repo` + `unknown-ref`; soft signal `warnings: ["no_hunks_in_comparison"]` when both succeed but diff is empty. macOS symlink realpath normalization applied so `/var/` vs `/private/var/` doesn't break path comparisons.
- [x] **Q2-RESOLVED** `scan_diff` baseline mode now carries `resolved: ResolvedFinding[]` + `resolvedCount: number` (9d85885). `ResolvedFinding = { filePath, ruleId, message }` — mirrors the baseline entry minus the opaque hash. Both fields emitted even at zero in baseline mode (meaningful: "this scan fixed nothing"). In `hunksOnly: true` mode the fields are **omitted entirely** (not empty-array) — concept doesn't apply without a baseline. `nextStep` surfaces the resolved count when non-zero and nudges toward `baseline` mode: "update" to prune.
- [x] **Q2-GROUPKEY** `groupKey: string` stamped on every Violation (217d97c + ce8d6ac + 9184f6d + b979244). 12-hex-char truncated SHA-256 of `ruleId + "\0" + describeNodeShape(node)`. Helper `describeNodeShape` lives in `src/engine/ast-helpers.ts`; attribute NAMES and children-structure coarsened to `[text]`/`[expr]`/`[element]`/`[empty]`/`[mixed]` — values stripped, positions excluded. Stamped by engine (scanner + rule-runner), forwarded through MCP + JSON + agent + SARIF (`partialFingerprints.groupKey`) formatters. ADR 0008. Constant `GROUP_KEY_HEX_LENGTH = 12` mirrors `FINDING_ID_LENGTH` for symmetric read.
- [x] **Q2-CHECKLIST-LIMIT** Checklist pagination (df8946c + 73a545b). Inputs: `limit` (default 200, clamp [1,2000]), `offset` (default 0, ≥0), `maxCandidatesPerCriterion` (default 10, clamp [1,100]). `totalCandidates` always emitted. `truncated: true` + `nextOffset` conditionally spread when global `limit` clipped the flat stream; `perCriterionClipped: true` conditionally spread when any criterion hit the per-criterion cap. Orthogonal signals — either, both, or neither. Named constants; no magic numbers. Scan_project pagination helper extraction deferred as follow-up.
- [x] **Q2-SKIPCRIT** `skipCriterion: string[]` accepted on `checklist` + `scan_project`. Findings / checklist entries whose entire `criteria` set is contained in the skip list are dropped; entries tied to un-skipped criteria stay. Caller's filter of its own result — not tool suppression — surfaced as `summary.skippedByCaller` (checklist) and `meta.skippedByCaller` (scan_project). Shared schema + `skippedByCallerField` helper live in `src/mcp/skip-criterion.ts`. Filter applied in `runScanAndFormat` via new `applyCriterionSkip` pass so plan counts stay honest. Also consolidated `referenceGuideField` + `baselineStatusField` helpers into their owning modules to free file-budget headroom.
- [x] **Q2-LISTSUPP** `list_suppressions` MCP tool (d0a017c + 60df697). Input `{ cwd?, additionalPaths? }`; output `{ suppressions: [{ file, line, ruleId|null, criterionId|null, reason?, wildcard }], meta, nextStep }`. Does NOT run rules — enumerates pragmas only via the existing parser. Wildcard pragmas emit one entry with `ruleId: null, criterionId: null, wildcard: true`; multi-token pragmas emit one entry per token. `reason` field is conditionally spread (omitted for bare pragmas). Registered as tool #17.

### v0.2.0 — accepted (P2)

- [x] **Q2-WRAPMAP** Config accepts `nativeWrappers` as `{ Button: "button", Link: "a" }` alongside the legacy `string[]` form (4e40f10). Loader normalizes to two honest LoadedConfig fields: `nativeWrappers: string[]` (names — every existing consumer keeps working unchanged) and `nativeWrapperElements: Record<string, string>` (new, populated only when the object form was supplied, empty `{}` otherwise). Tests cover all three input shapes (array, object, absent). Rule opt-in shipped as Q2-WRAPMAP-RULES (3f7a07f + 7c7e43b + 713506a); compound-component extension shipped as Q2R2-COMPOUND (e1ca368). MCP tool wiring of `nativeWrapperElements` is the last remaining wedge — promoted as its own item below.
- [x] **Q2-WRAPMAP-MCP** Thread `LoadedConfig.nativeWrapperElements` through the MCP handlers so `configure` / `sessionConfigure` / `scan` / `scan_project` surface the element mapping alongside the flat names list (92dc714). `meta.activeNativeWrapperElements` emitted when map non-empty, omitted otherwise; `sessionConfigure` accepts both flat-array and object forms via `oneOf`. New `src/mcp/scan-assembly.ts` extracted plan/meta assemblers to stay under the line budget.
- [x] **Q2-WRAPGLOB** Glob patterns now accepted on `nativeWrappers` — `*Button`, `Icon*`, `*Card*`, `Icon*Button`. `*` expands to `[A-Za-z0-9]*` so globs stay inside identifier boundaries (no cross-dash or cross-whitespace match). Centralized in `src/mcp/wrapper-matcher.ts` (`matchesWrapperPattern`, `nameMatchesAnyWrapper`, `wrapperPatternToTagRegexSource`) and threaded through the three consumer sites: `dropWrapperNoise` (silence findings), `collectUsedWrappers` (unusedWrappers detection via AST walk), `findWrappersInExcludedSources` (text-search widening into stories/dev-tools). `unusedNativeWrappers` now tracks which PATTERNS matched — `*Button` counts as "used" when any variant appears; `Ghost*` surfaces as unused when nothing matches. No `?` / `[abc]` / `!` — just `*`, by design.
- [x] **Q2-WRAPMAP-RULES** `Rule.wrapperTreatsAsElement?: string` opt-in (3f7a07f + 7c7e43b). Context builder filters `LoadedConfig.nativeWrapperElements` against the rule's declared target tag and exposes `ctx.wrappersForElement: ReadonlySet<string>`. Rules that don't set the field see an empty set — identical to pre-Q2-WRAPMAP-RULES. Wired on 3 rules: `navigation/link-descriptive-text` (`"a"`), `media/alt-text-missing` (`"img"`), `forms/labels-required` (`"input"`). `forms/labels-required` also teaches implicit-label detection to accept mapped wrappers. CLI scan threads `fileConfig.nativeWrapperElements` into `runScan`. MCP tool wiring deferred — task guardrails excluded `src/mcp/` during concurrent work.
- [x] **Q2-WRAPPATH** `detect_native_wrappers` candidates now carry `definitionFile: string | null` (6930fce). One-hop basename probe (matches `ComponentName` against `{ComponentName}.{tsx,jsx,ts,js}`) via the existing `indexFilesByComponentName` helper P1-F already uses — no import-graph traversal, no transitive barrel-following. Barrels are invisible to the probe because the basename still matches the defining file. `null` is emitted (not omitted) when the probe fails — the attempt is meaningful.
- [x] **Q2-SNIPPET-KIND** Reason-driven snippet widening (bd94a21 + b0e26cc + 11a9472). `CROSS_LINE_REASON_PATTERNS` matches cues like "defined outside this line" / "referenced function"; matched candidates get a wide snippet instead of the ±3-line default. For TSX/JSX/TS/JS, `findEnclosingBlock` does a regex-free brace-balance walk (string/comment/template-expression aware via a per-state machine) to bound the enclosing function/block; over-cap blocks honestly fall back to the 10-line window rather than silently truncating. HTML/CSS keep the 10-line fallback. Snippet remains a plain string — no shape change.
- [x] **Q2-SESSIONCFG** Canonical tool name is now `sessionConfigure`; `configure` is a dispatch-only backward-compat alias not listed in `tools/list`. Calls via the legacy name still land on the same handler but get `warnings: ["deprecated_tool_name_configure"]` layered onto the response so agents can migrate on their own schedule. Description reframed to lead with the ephemeral session-only semantics and point at `ra11y.config.ts` for durable committed config. New `src/mcp/deprecation-warning.ts` owns the warning-layering helper (will be reusable when we rename other tools). Pattern mirrors P2-R's `file`/`filePath` alias.
- [x] **Q2-PRUNE** `ra11y baseline prune [--dry-run]` subcommand (ebbe753). Pure `pruneBaseline(entries, fileExists)` helper in `src/engine/baseline.ts`; CLI wrapper in `src/cli/commands/baseline.ts`. Reuses the existing `writeBaseline` serializer so metadata and retained-entry order round-trip. Missing/malformed baseline → exit 2 with stderr pointer to `ra11y baseline create`. No new exit codes.
- [x] **Q2-VERIFYCMD** `verifyCommand` + `verifyCommandStructured` on every `suggest_fix` response (100a0fa). Prose + structured pair, always populated (no conditional-spread — no suggest_fix response is meaningful without a re-verify). Structured form: `{ tool: "scan_file", args: { file, ruleId? } }`. Chose `scan_file` (narrowest deterministic verify) over `scan_project`; `ruleId` is advisory so the agent can post-filter the re-scan output.
- [x] **Q2-SARIF-DOCS** SARIF output + exit codes documented (d987876). `docs/cli.md` gets a `## SARIF output` section with field-mapping table sourced from `src/output/formatters/sarif.ts`; `docs/ci.md` is new — exit-code table, `--fail-on` reference, GitHub Actions (blocking + Security-tab upload via `github/codeql-action/upload-sarif@v3`), GitLab CI with artifact export, baseline adoption in CI.

### Considered and rejected (per CLAUDE.md §1)

- **Promote `editCandidate` to `kind: "edit"`** when the synthesis looks concrete (e.g. label-in-name's heuristic rewrite). → rejected per **§1 "Ambiguous field shapes are dishonest"**. `kind: "edit"` is a contract that the oldText/newText pair is a mechanical swap — the rule emitted it with certainty. `editCandidate` at `kind: "guidance"` is an LLM-synthesized guess from visible text + aria-label tokens (per P1-L). Promoting the guess to `kind: "edit"` would make the contract lie; the agent would batch-apply candidates that aren't verified. Current shape is correct.
- **Heuristic auto-dismissal** for "obvious debounces" (timer duration < 1s, pure-value-bubbleup onChange). → rejected per **§1 "Numeric-threshold heuristics are suppression"** + **"No heuristic suppression"**. Same reasoning as the original Q P0-A rejection. A 800ms animation and a 800ms auth-retry back-off are indistinguishable from static analysis; the agent reading the surrounding code is the only correct arbiter. Enrich `reason` text further if needed; do not filter the candidate list.
- **Weight cross-line signals** (`pointer-events-none`, `disabled`, `focus-visible:ring-*`) in `suggest_fix` ranking. → rejected per **§1 "Don't duplicate capability the agent already has"**. The agent reading the file sees those signals in one pass; a heuristic weighting in-tool produces output the agent can't tell to mistrust. The `focus-visible:ring-*` cross-reference IS already shipped for the `focus/outline-visible` rule (Track R) where the evidence is a concrete class-token link — but fix-ranking heuristics are the fuzzier form and belong to the agent.
- **Framework-version awareness in `suggest_fix`** (detect React 18 vs 19 from `package.json`, etc.). → rejected per **§1 "Don't duplicate capability"**. Agents read `package.json` trivially; building a detector in-tool is the kind of in-process inference that can be confidently wrong (monorepos, overrides, multiple React versions). Better: the agent reads the config and compose the fix itself.
- **Flatten `files[].findings[]` to `findings[]` with `file` inlined** when total < 10. → deferred (not rejected). Token-saving but introduces a shape branch on response-size — consumers that iterate by-file now have to handle both shapes. The current nested shape is fine; optimize only with a measured token budget goal.

### Deferred (worth doing eventually, not in this batch)

- **Rule-catalog reorganization** — resolve `parsing/duplicate-id` + `parsing/html-has-lang` vs `document/lang-attribute`; clarify `semantics/label-in-name` vs `forms/labels-required` vs `forms/non-empty-label`. Renames need a deprecation path (alias old IDs for one major release). Costs a semver major.
- **Server-side typecheck/parse verification** on `suggest_fix` suggestions. Expensive (spins up a parse per suggestion); might be worth it for high-stakes mechanical fixes but not across the board.
- **SARIF output for GitHub annotations** — already emit SARIF; "::error" annotation mapping is a small transform. Tied to Q2-SARIF-DOCS; promote if demand surfaces.

### v0.2.0 — round 2 retriage (added 2026-04-17)

Source: the 10-agent round-2 eval aggregation (`AGGREGATED.md`). Most items were *not* folded into Track Q (rounds 1-2, closed 2026-04-17) — the round-2 aggregation was produced around the same time but separately. Triage below retriages each item against CLAUDE.md §1 doctrine and existing Q/Q2 shipped work.

#### Accepted (P0 — structural/bug/shape parity)

- [x] **Q2R2-DIR-NEXT** `scan` (directory mode) now emits `nextStep` + `nextStepStructured` at parity with `scan_project` and `scan_file`. Uses the shared `buildNextStep` helper; `iterativeTip` left empty (scan has no scope-dimension to nudge about, unlike scan_project full vs changed mode). Empty-files branch still skips the pair — same as scan_project — because `warnings: ["scanned_zero_files"]` carries the "nothing scanned" signal and a `checklist` suggestion would be dishonest when no files were actually scanned. [round 2, agent 5]
- [x] **Q2R2-FIX-DEDUPE** When a finding's inline `fix` (now `fixClass`-typed, bbbecf1) already carries primary + alternatives + context, drop the `suggest_fix` suggestion from `nextStep`/`nextStepStructured`. Pairs with Q2-VERIFYCMD — the verify step still applies, but the "now call suggest_fix" handoff is redundant. Saves ~600 tokens per finding in tight fix loops. [round 2, agent 1] — 14e3d9a
- [x] **Q2R2-SCANNED-KEY** Canonical `scanned: { mode: "project" | "dir" | "file", root?, paths?, file? }` envelope now flows through scan_project / scan / scan_file / scan_diff / detect_native_wrappers / propose_config / propose_baseline / bootstrap / wrapper_introspect — nine MCP tools in total (the original 7 plus tool-bootstrap + tool-wrapper-introspect, which landed after the stash and shared the same `scannedRoot` smell). Stash@{14} (`unrelated-mcp-wip`) was applied, conflicts with the later `applyMetaCacheMode` refactor resolved by porting the stash intent onto HEAD rather than `--theirs`-ing the refactor away. Tests rewritten across tests/integration/mcp-{tools,roots,meta-cache}.test.ts + tests/unit/mcp/{tool-bootstrap,tool-propose-baseline,tool-propose-baseline-invariants,tool-propose-config,tool-wrapper-introspect,tools}.test.ts. CHANGELOG Breaking entry added. Full verify green. [round 2, agent 5]
- [x] **Q2R2-UNTARGETED-NAME** One canonical field name for the untargeted-criteria count (today: `plan.untargetedCriteria` on scan_project, `untargeted` on checklist, `manualUntargetedCount` on coverage, top-level `untargetedCriteria` on scan_file — four names, same concept). Same canonicalization pattern as P2-R (file/filePath). [round 2, agent 9]
- [x] **Q2R2-WRAPPER-SOURCES** Collapse `activeNativeWrappers` + `activeNativeWrappersBySource` + `sessionNativeWrappers` into one tagged list: `activeNativeWrappers: [{name, source: "config"|"autoDetect"|"session", confirmed?}]`. `confirmed` folds in P1-F. [round 2, agent 9]
- [x] **Q2R2-COVERAGE-CHECKLIST** ADR 0010 landed (fe962f6) + execution (3093b67, 27e12d0). `checklist.summary.automatedCoverage` trimmed to `{ standardId, automatedCriteriaPassRate }`; `criteriaAutomatable` + `criteriaAutomatablePassing` dropped. `coverage` cross-points to `checklist` when `manualWithCandidates` non-empty, to `scan_project` when only failing-automated remain, omitted when clean. `checklist` cross-points to `coverage` when `actionable === 0`, to `checklist` (paging) when truncated, omitted otherwise. Both tools conditional-spread `nextStep`+`nextStepStructured` as one unit. 4 new integration tests cover scalar parity + args round-trip + aligned-pair discipline. Merge with verbosity knob still deferred to v1.0.0 behind a deprecation alias path. [round 2, agent 7]
- [x] **Q2R2-META-CACHE** Meta-cache skeleton + wiring landed across scan / scan_file / scan_project / scan_diff (14c35a4 + 608cda7 + a2d7b57). Input flag is `metaMode: "full" | "delta"`, default `"full"` so legacy callers see zero shape change. Under `metaMode: "delta"`, first call returns full meta stamped with `sessionRef: "<tool>-<8hex>"`; repeat call with same signature returns `{ sessionRef, delta: {...}, removedFields? }` (removedFields omitted when empty, honest conditional-spread). Signature hash covers tool name + cwd + paths + standards/level + additionalPaths; excludes `metaMode` itself + pagination so a paged walk stays on one ref. Unit + integration tests committed (2ae7baa + c14e137). Extension shipped across checklist / coverage / list_suppressions — three out of the four follow-up tools wired; list_rules deferred because its response is pure enumeration data (no scan-confidence telemetry, no cwd/filesScanned signal to cache) and the meta block would be trivial. For checklist + coverage the `meta` block is opt-in per `metaMode: "delta"` so legacy callers see zero shape change (neither tool had `meta` historically); list_suppressions' existing meta gets wrapped by `applyMetaCacheMode` directly. [round 2, agent 1]

#### Accepted (P1 — load-bearing capability)

- [x] **Q2R2-WRAPPER-INTROSPECT** ADR 0012 (e47ec5d) + execution (81707b1, 5bf4c00, a16a3a9). `wrapper_introspect` MCP tool returns per-wrapper records with `observedRoot ∈ {button|a|input|div|opaque|unknown}` + `confidence ∈ {confirmed|assumed|unresolved}`; textarea/select/input collapse onto `"input"` to match the ADR's DOM bucket set. Per-file-hash cache keyed via `WeakMap<McpSession>`; natural invalidation on content change. One-hop basename probe only (no transitive import following). `nextStep` routes to `propose_config` when all records are confirmed, else `detect_native_wrappers`. 11 tests. Per ADR, introspection is an audit/discovery signal — never silently rewrites `LoadedConfig.nativeWrappers`. [round 2, agent 2]
- [x] **Q2R2-DRIFT** New rule `wrapper/drift` — component declared in `nativeWrappers` (or auto-detected) whose definition no longer renders the expected native element. Fires at the DEFINITION file. Closes the "fixing a wrapper is silently safe; breaking it is silently catastrophic" asymmetry — the breakage surfaces the moment the wrapper is edited. [round 2, agent 2]
- [x] **Q2R2-INHERITED** ADR 0014 `docs/adr/0014-inherited-findings.md` (a271077) + implementation `src/engine/inherited-findings.ts` + `tests/unit/engine/inherited-findings.test.ts` (landed earlier; ADR now documents the decisions the impl already encodes). Cap decision: **all call sites, no cap** — per CLAUDE.md §1 "Surface, don't suppress" + "Labeled buckets are suppression too"; agents dedup via groupKey, suppress via source pragma. Rollout: **always on, no flag** — the signal is not something an agent can opt into discovering. `confidence: "inherited"` extends the existing enum (single axis); fresh findingId/groupKey per call site so each is an independent baseline/attestation target. Interaction with polymorphic-dispatch + baseline + attestations (ADR 0013) documented in §Consequences. [round 2, agent 2]
- [x] **Q2R2-POLYMORPHIC** Resolve literal `as` / `asChild` prop values and re-dispatch rules against the resolved element. `<Button as="a" href=...>` re-runs `link-no-href`, `link-descriptive-text`. Only literals — dynamic `as={Something}` stays unresolved (honest — agent reads and decides). Engine surface shipped (0b130dd: `ctx.resolvePolymorphic`, link-descriptive-text opted in); follow-up opt-ins landed (b07455b: alt-text-missing, labels-required, semantics/button-name). [round 2, agent 2]
- [x] **Q2R2-COMPOUND** Typed compound-component mapping in config: `nativeWrappers: { Card: { Header: "div", Body: "div" }, Composer: { SendButton: "button" } }`. Extends Q2-WRAPMAP object form with nested dotted-path keys. [round 2, agent 2]
- [x] **Q2R2-RULE-COV** Per-rule coverage confidence on scan responses: `{ ruleId, filesEvaluated, filesEligible, coverageConfidence: "high"|"low", reason?, remediation? }`. Derivative split: `confidentlyClean: [ruleIds]` vs `lowConfidenceClean: [ruleIds]`. Tailwind-pre-build is the acute case (`contrast/minimum` runs against 2 CSS files, 0 findings ≠ clean). Distinct from Q2-RULEDETAILS (rule *catalog* inline) — this is per-rule *trust* per scan. [round 2, agent 6]
- [x] **Q2R2-CWBB** Shape + forwarders landed (2df787a + ADR 0009, 3e63715 for json/sarif). Optional `readonly couldBeWrongBecause?: readonly string[]` on `Violation`; wired through MCP `formatFinding`, `agent` formatter, `json` (passthrough), and SARIF (`Result.properties.couldBeWrongBecause`) with conditional-spread so empty/absent reads identically to consumers. Text-contrast opt-in shipped (02b8bf8): `contrast/minimum` + `contrast/enhanced` emit `tailwind_class_on_consumer` when a `text-*`/`bg-*` utility on the consumer would override the failing declaration; rules promoted to project scope; helpers in `src/rules/contrast/_shared.ts`. Non-text contrast opt-in shipped: `contrast/non-text` emits `tailwind_class_on_consumer` when a `border-*` / `outline-*` / `ring-*` utility on the consumer would override the failing declaration (boundary-vs-background axis — distinct family set from the text-contrast opt-in, same reason code); rule promoted to project scope; family sets exposed as `TEXT_CONTRAST_OVERRIDE_FAMILIES` / `NON_TEXT_CONTRAST_OVERRIDE_FAMILIES` in `_shared.ts`; `divide-*` intentionally excluded (applies between children, not the element itself). **Deferred** (rejected by the implementing agent as a fuzzy signal): `forms/required-indicator-missing` → `replacement_indicator_in_sibling_file` — "required"/"Required" tokens fire on nearly every form-heavy codebase (react-hook-form, zod, formik, test assertions); needs a tighter signal before it earns the opt-in. `estimatedFpRate` numeric remains rejected per §1 "Numeric-threshold heuristics are suppression." [round 2, agent 8]
- [x] **Q2R2-STORYBOOK-PRESET** MVP shipped (6dfa231 + aee3134). `Config.preset: "storybook"` accepted + validated; `isStorybookStoryFile` matches `*.stories.{tsx,jsx,ts,js}` + `*.story.*`; `DiscoverOptions.includeStoryFiles` drops only story-file patterns when the preset fires (tests/mocks still excluded); Storybook primitives (`Meta`, `StoryObj`, `StoryFn`, `Story`) exempt from opaque-component classification inside story files only (product `<Meta/>` stays opaque); `warnings: ["storybook_preset_active"]` emitted when preset engages. Args-binding follow-up landed (0bb8842 + 0fa523d + 55ba1f7 + 2a95a48 + 6c878dd): `parseTsx(source, { filePath })` runs a Storybook synthesis pass that appends a virtual `<ComponentName attr=... />` JsxElement for each `StoryObj` / `StoryFn` / `Story` / `Meta`-typed declarator with literal `args`, so rules that need to evaluate the rendered shape (alt-text-missing, button-name, labels-required, etc.) get a chance to fire on story files. Each synthesized element carries `synthesized: { source: "storybook-args", storyName }` for honest provenance. Spreads, callbacks, identifier-reference values, dynamic templates, and unresolvable components all skip synthesis (no half-known elements). [round 2, agent 4]
- [x] **Q2R2-FORM-REQ** New rule `forms/required-indicator-missing` — component forwards `required` to a native input but renders no visible marker and no `aria-required`. Cites wcag22:3.3.2. Fires at the wrapper DEFINITION. [round 2, agent 3]
- [x] **Q2R2-FORM-TIE** Review finder `forms/server-error-untied` under wcag22:3.3.1. `<p role="alert">` / `<div aria-live>` sibling of a field where the field has no `aria-invalid` / `aria-describedby` pointing at the error's id. Finder, not rule — cross-element proof requires inspection agents do better. [round 2, agent 3]
- [x] **Q2R2-FORM-TIMING** Review finder `forms/validation-timing` under wcag22:3.3.3 / 3.3.4. Surfaces `onChange` validation handlers that fire per-keystroke. Reason-text enrichment only — agent decides whether `onBlur` is appropriate; no auto-downgrade. [round 2, agent 3]

#### Accepted (P2 — orchestration / DX)

- [x] **Q2R2-BOOTSTRAP** `bootstrap` MCP tool — composes `detect_native_wrappers` + `propose_config` + `scan_project` + `baseline create` + returns a CI snippet in one call. Interrogation note (§1 "Interrogate the problem"): is this a docs gap or a genuine composition primitive? Round-2 signal from two independent agents suggests the latter; build after Q2R2-PROPOSE-CFG lands. [round 2, agents 7, 10]
- [x] **Q2R2-SUPPRESS-TOOL** `suppress` MCP tool — input `{ file, line, ruleId, reason }`. Inserts the appropriate source pragma (block-comment for HTML, JSX-comment for TSX). Required reason (rejects bare — mirrors Q2-REASON source-rule). [round 2, agent 7]
- [x] **Q2R2-CFG-SNIPPET** `detect_native_wrappers` response adds `suggestedConfigSnippet: string` as a structured field. Today it's buried in `nextStep`'s English; agents parse prose to extract config. [round 2, agent 9]
- [x] **Q2R2-PROPOSE-CFG** `propose_config` MCP tool — synthesizes `ra11y.config.ts` from scan state (wrappers + exclude + commented rules stub). Deterministic; no LLM. [round 2, agent 10]
- [x] **Q2R2-PROPOSE-BASE** `propose_baseline` MCP tool — categorizes each would-be entry with a machine-readable reason code (`wrapper-undetected` / `third-party-html` / `legacy-route` / `design-system-internal`). Reviewers triage by category. [round 2, agent 10]

#### Accepted (P3 — polish)

- [x] **Q2R2-INTENTIONAL** `@ra11y-intentional` JSDoc tag on a component declaration — intentional-failure-demo marker (Storybook "bad example" pattern). Functionally a file-scoped `ra11y-disable *` pragma but syntactically part of docs markup. Required reason. [round 2, agent 4]
- [x] **Q2R2-SCAN-MIXED-DOC** Document that `scan` accepts `paths: string[]` of mixed files + dirs. The primitive exists; it's non-obvious. One-line in `tools/list` description + a `docs/mcp/tools.md` note. [round 2, agent 5]

#### Considered and rejected (round 2 retriage)

- **Glob-ignore `*.stories.tsx` as default** → rejected per §1 "Default-exclude globs are suppression too." Story-file zero-findings is an honest signal (structurally unanalyzable). Fix: the `preset: "storybook"` that scans what stories exercise (Q2R2-STORYBOOK-PRESET above), not a filename carve-out. Severity-downgrade variants fail for the same reason.
- **Auto-suppress when `couldBeWrongBecause` escape hatch is provably present** → rejected per §1 "No heuristic suppression." The structured field is informational (Q2R2-CWBB); the agent reads the file and decides. Baking inference into the tool creates the silent-miss mode the doctrine exists to prevent.
- **`estimatedFpRate: number` on every finding** → rejected per §1 "Numeric-threshold heuristics are suppression." Any consumer filtering on `fp_rate > X` reintroduces silent-miss. Reason-text + `couldBeWrongBecause` carry the same information without the threshold.
- **`reportFalsePositive` as a persistent out-of-tree dismissal** → rejected as new surface; **superseded by C-ATTEST-TOOL** (Track C). Attestation ledger is the durable out-of-tree evidence channel with the evidence-slot the doctrine requires. Track here as "duplicates Track C."
- **Surface `limitations` once at session level** → rejected. `limitations` is a per-scan honest signal; the specific scan's inability to verify runtime criteria is what the agent needs for that specific claim. Session caching creates a "was this scan's limits the session's, or did they change?" gap — silent-miss risk. Keep per-response.

---

## Track Q3 — Design-system field test (Bootstrap, 2026-04-20)

Owner: main session + general-purpose. Source: 10-agent parallel eval against the Bootstrap repo (twbs/bootstrap, /tmp/bootstrap). Slices: components MDX (×4 agents), forms MDX, layout/utilities/helpers/content MDX, getting-started/about/customize/extend MDX, Astro pages/layouts/components, raw HTML test fixtures under `js/tests/`, and an MCP surface critique. All findings triaged against CLAUDE.md §1 and the AI-first-consumer doctrine.

Bootstrap is a useful stress-test because (a) it's a design-system doc site, the shape ra11y agents see most often; (b) it has genuine a11y content (accessibility.mdx, prose-embedded ARIA patterns); (c) it ships MDX + Astro + raw HTML + SCSS in one tree — four file classes at once.

**Parser-lifecycle discovery (process issue, not code gap):** `dist/cli.js` was rebuilt at 22:30 but the MCP subprocesses were started at 22:13 and kept running the pre-rebuild bundle. All 10 agents saw `.mdx` / `.astro` return `file-unsupported` and `filesAdded: 0` — a behavior that evaporates once the host re-spawns the MCP subprocess against the fresh bundle. The parser code itself (`src/input/parsers/{mdx,astro,scss}.ts` + `src/mcp/session.ts:440-457` dispatch + `src/utils/path.ts` allow-list) is correct. Items below are the findings that remain valid AFTER the MCP is restarted — the stale-process symptom is folded into a single lifecycle item.

### v0.2.0 — accepted (P0)

- [x] **Q3-MCP-RESTART-HINT** New `src/mcp/stale-subprocess.ts` records the bundle file's mtime at `startMcpServer` and re-stats on every `tools/call` dispatch. When on-disk mtime is strictly greater than the recorded baseline, `annotateStaleSubprocess` merges `stale_mcp_subprocess` into the response's `warnings[]` (deduped, prepended so it's first) and adds a sibling `staleSubprocessHint` prose field ("rebuild detected; reconnect the MCP to pick up parser and rule changes"). Leaves `isError` and non-JSON payloads untouched. 13 unit tests in `tests/unit/mcp/stale-subprocess.test.ts` cover record-idempotency, merge-with-existing-warnings, dedup, non-string filter, non-object payload bypass, error-envelope bypass, empty-content, and trailing-content preservation.
- [x] **Q3-SCAN-FILE-ERROR-EXT-DRIFT** (d44363f) `src/utils/path.ts` now exports `PARSEABLE_EXTENSIONS` + `parseableExtensions()`; `src/mcp/tools.ts` derives the `scan_file` `file-unsupported` remediation from the registry via a module-level `SCAN_FILE_UNSUPPORTED_REMEDIATION` constant, so `.mdx` / `.astro` / `.scss` are no longer invisibly omitted and the message stays in lockstep as the parser registry grows. Unit test asserts every current extension is present.
- [x] **Q3-ADDITIONAL-PATHS-SKIP-REASON** (aa23b13) `scan_project.additionalPathsScanned.skipped: [{ path, reason }]` now surfaces per-path skip reasons — `"not-found"` / `"unsupported-extension"` / `"excluded-by-glob"`. New `src/mcp/additional-paths-classifier.ts` owns `classifyAdditionalPathSkips` + `additionalPathsScannedField`; directory-exists-with-zero-parseable-files deliberately NOT a fourth reason (response-level `extensions_skipped_no_parser` warning already carries that signal). `skipped` conditional-spread (omitted when every additional path contributed files). Four tests cover all three reasons + omission.

### v0.2.0 — accepted (P1 — shape honesty)

- [x] **Q3-GUIDANCE-FIELD-NAME** Superseded by **V1-COMP-PLAN-GUIDANCE-FIXES** (82f41e5). The misnamed `plan.guidanceFixesAvailable` composite was replaced with structured `plan.fixesByClass: { mechanical, guidance, runtimeOnly, verifyInSource }` — each lane counts one kind of thing per the doctrine. `mechanicalEditsAvailable` retained as an orthogonal "actionable inline edit" counter. No rename path for the old misnomer — agents read the structured lane directly.
- [x] **Q3-PLAN-SUMMARY-PROSE** Superseded by **V1-SHAPE-FIXCLASS-HEADLINE** (33a1978) + **V1-SHAPE-CLI-AGENT-HEADLINE** (331a805). The violations parenthetical now breaks down by `fixClass` lane (`mechanical` / `guidance` / `runtime-only` / `verify-in-source`) rather than summing prose-only items under a single "guidance fixes" label; zero-count lanes omitted. Both the MCP `plan.summary` and CLI `--format agent` summary route through the shared `buildFixClassBreakdown` helper. No separate rename required — the composite is gone from both surfaces.
- [x] **Q3-FIX-PAYLOAD-EMPTY** (1699abe) Investigation: the `fix: { safety: "safe" }` alone shape the Bootstrap field report described CANNOT occur on current main. `buildFix` in `src/output/agent-response/build-finding.ts` always populates `description` in both emitting branches (mechanical: `v.suggestion ?? v.fixPaths.primary.label` — label is required on FixPath; guidance: requires non-empty suggestion). `rewriteFinding` in `src/mcp/reference-guide.ts` atomically strips `description` + adds `fixDescriptionRef: { hash }`. Previously closed by **075b64c** (atomic hoist) + **6b6349c** (null-sentinel sweep) + **2a26c25** (hoist invariants) + **ea6dd5c** (snippet collapse). Added regression tests: `AgentFix-shape-is-honest` helper in `tests/unit/output/agent-response/build-finding.test.ts` covers every buildFix branch + mechanical-without-suggestion + guidance-only + empty-suggestion + direct-construction corner case; parallel invariant in `tests/unit/mcp/reference-guide-hoist.test.ts` covers the post-hoist pipeline.
- [x] **Q3-DEFAULT-LIMIT-BUDGET** Superseded by **V1-SIZE-RESPONSE-BUDGET-DENSITY-2** (66a9147 + 9c73266 + 538e392). ADR 0021 amended with the token-density secondary budget; `src/mcp/token-budget.ts` exposes `applyTokenBudget` + `DEFAULT_TOKEN_BUDGET_CHARS = 88000` (~22000 tokens, ~3k headroom under the 25k ceiling). Wired through `scan` / `scan_project` / `scan_diff`. Post-merge measurement on bootstrap: 25k MCP ceiling held with ~3.5k-token envelope headroom; `truncated: true`, `nextOffset` advances on `scan_project`, `response_token_budget_truncated` warning fires. Secondary token-density cap kicks in before the file-count cap when needed.
- [x] **Q3-CHECKLIST-NEXTSTEP** (050cf88) `buildChecklistNextStep` now emits the pair in the previously-empty actionable-items-no-truncation branch. Structured hint routes to `scan_project { cwd }` (closed-form, directly callable); prose names `attest` as the verdict-recording step. `attest` is kept out of the structured form because its required `reason` + `evidenceSource` come from the agent's per-item investigation — pre-seeding would fabricate provenance. Pair-invariant (both fields together or neither) preserved across all three branches.
- [x] **Q3-BOOTSTRAP-NEXTSTEP-SELFLOOP** (573fcec) `buildNextStepStructured` in `tool-bootstrap.ts` no longer self-loops. When violations exist and baseline has not been written: route to `detect_native_wrappers({ cwd: root })` if the detect leg surfaced PascalCase+onClick candidates, else `scan_project({})`. baseline-written branch still emits `baseline check`; clean branch still emits `scan_project`. Prose untouched — still names `writeBaseline: true` as the grandfather alternative (legitimate English-tier guidance, not a structured self-loop).
- [x] **Q3-PROPOSE-CONFIG-FIELD-DRIFT** (275d4bb) `bootstrap` response now emits `suggestedConfig` as the canonical key alongside the legacy `proposedConfig` for one release (both populated together on a successful propose leg; both absent together when the leg degrades). Canonical name matches `propose_config.suggestedConfig` + `detect_native_wrappers.suggestedConfigSnippet` — two of three surfaces already used `suggested` prefix. Output-side alias (no `warnings` code — no input call site to warn about); transition plan documented in CHANGELOG `[Unreleased]` → `### Deprecated`.
- [x] **Q3-DETECT-WRAPPERS-EMPTY-REASON** (808940e) `detect_native_wrappers` now stamps `emptyReason: "no-parseable-files"` on the `files.length === 0` early return and `emptyReason: "no-pascalcase-onclick-components"` when `collectWrapperCandidates` returned empty on a non-empty file set; omitted via conditional-spread when candidates are populated. Chose one combined token for the zero-candidate case — `collectWrapperCandidates` checks PascalCase AND onClick together, so splitting would require reshaping the collector. Tool description advertises the field; three tests cover both variants + omission.

### v0.3.0 — accepted (P1 — rule gaps + tightening)

Rule folders with known issues (verified via source-read against /Users/van/dev/ra11y/src/rules/):

- [x] **Q3-RULE-NESTED-INTERACTIVE-ROLE-WRAPPER** `src/rules/semantics/nested-interactive.ts` over-fires on role-only wrappers. Fires on `<a href>` inside `<div role="tab">` even though the outer div is not materially interactive (no handler, no tabindex, not a native interactive tag). 7 false positives across `js/tests/visual/{collapse,modal}.html` on Bootstrap. Tighten: require the outer element to be (a) a native interactive tag, (b) have `tabindex >= 0`, or (c) have an actual click/key handler — role alone should not qualify.
- [x] **Q3-RULE-LINK-HREF-HASH** (3d5dbc0) `src/rules/navigation/link-no-href.ts` now fires on `<a href="">` and `<a href="#">` identically to missing href (whitespace trimmed first). Fragment navigation `<a href="#non-empty-id">` stays silent. `satisfies` + WCAG citation unchanged — same 1.3.1 / 4.1.2 criteria. `docs.description` widened to "missing, empty, or placeholder (#) href." Three new fixtures (empty-href.html, hash-only-href.html, fragment-nav.html) + ≥3 positive + ≥2 negative unit tests across HTML + JSX.
- [x] **Q3-RULE-EXPANDED-DISCLOSURE-RANGE** `src/rules/aria/expanded-on-disclosure.ts` never fires on Bootstrap's canonical disclosure pattern — `<button aria-expanded="false" data-bs-toggle="dropdown">` without `aria-controls`. Verify rule coverage matches the spec; the Bootstrap pattern is exactly what WAI-ARIA Authoring Practices §disclosure describes.
- [x] **Q3-RULE-KEYBOARD-HANDLER-ATTR** (77c0378, turn 25, 2026-04-21) `keyboard/handler-missing` widened to flag `data-bs-toggle`, `data-bs-dismiss`, `data-bs-ride`, `data-bs-target` on non-interactive elements (div, span, p, section, etc.) and inline `onclick="..."` attributes. 8 new HTML fixtures (bad + good), expanded unit tests. `satisfies` kept at wcag22:2.1.1 only (2.1.3 is AAA+manual per agent judgment). KB drift check passed without regen — rule-kb entry updated in-place.
- [x] **Q3-RULE-PAUSE-STOP-INLINE-STYLE** (288c039, turn 26, 2026-04-21) `src/rules/motion/pause-stop-hide.ts` only scans `.css` files, not inline `<style>` blocks or `style=` attributes. Bootstrap carousel fixtures use inline `transition-duration` + `data-bs-ride="carousel"` for auto-advance — zero findings today. Widen to inline-style scanning; `data-bs-ride="carousel"` itself is a static signal for WCAG 2.2.2.

New rules (ordered by ROI):

- [x] **Q3-RULE-TABLE-CAPTION** New `semantics/table-caption-missing`. `<table>` without `<caption>` or `aria-label`/`aria-labelledby`. WCAG 1.3.1 (A). No table-family rule exists today; `semantics/table-headers` covers `<th scope>` but not the wrapper-level label.
- [x] **Q3-RULE-TABLE-TH-SCOPE** (77db3b5) New `semantics/table-th-scope-missing`. `<th>` in multi-row/multi-col tables without `scope="col"`/`"row"`. WCAG 1.3.1 (A).
- [x] **Q3-RULE-IFRAME-TITLE** New `media/iframe-title-missing`. `<iframe>` without `title` or with generic titles (`"YouTube video"`, `"iframe"`). WCAG 4.1.2 + 2.4.1. No `iframe/` folder today.
- [x] **Q3-RULE-PROGRESSBAR-VALUE-RANGE** New `aria/progressbar-value-range`. Validate `role="progressbar"` has coherent `aria-valuenow` / `aria-valuemin` / `aria-valuemax` and `valuemin ≤ valuenow ≤ valuemax`. WCAG 4.1.2 (A). Bootstrap's progress.mdx has 15+ instances that pass but no rule enforces the shape. (b0cefd5, turn 23)
- [x] **Q3-RULE-LABELLEDBY-TARGET** New `aria/labelledby-target-exists` (mirror of `forms/label-for-id-mismatch` for ARIA). `aria-labelledby` / `aria-describedby` / `aria-controls` must reference existing element IDs in the same document. WCAG 4.1.2. (b39f502, turn 24)
- [x] **Q3-RULE-SKIPLINK-TARGET** Widen `src/rules/navigation/skip-link.ts` or add `navigation/skip-link-target-valid`. `<a href="#content">Skip to main content</a>` must resolve to an element with `id="content"`. WCAG 2.4.1. Bootstrap's own accessibility.mdx documents `href="#content"` as the canonical pattern while the docs' BaseLayout renders `<main>` without that id in most routes. (fb66578, turn 32.2)
- [x] **Q3-RULE-STRETCHED-LINK-TOPOLOGY** New `pointer/stretched-link-multiple-in-container`. Two or more `.stretched-link` (or equivalent full-card-overlay links) in the same positioned ancestor. WCAG 2.4.4. Bootstrap's own `helpers/stretched-link.mdx` documents the antipattern in prose but ships example markup that triggers it. (a4834e8, turn 32.3)
- [x] **Q3-RULE-HEADING-CLASS-NONHEADING** (9da0ff1, turn 27) New `semantics/heading-class-on-nonheading`. `<p class="h1">` / `<span class="h2">` / `<div class="display-1">` — visual heading, non-heading element. WCAG 1.3.1. Bootstrap's typography.mdx teaches this as a first-class pattern.
- [x] **Q3-RULE-COLOR-SOLE-INDICATOR** New `color/meaning-by-color-only`. `.text-danger` / `.text-success` / `.text-warning` as the only signal for status with no icon sibling, no SR-only label, no prose prefix. WCAG 1.4.1. Bootstrap's accessibility.mdx admits the gap in prose; the matching rule would substantiate or falsify the claim. (bc76f1d, turn 32.5)
- [x] **Q3-RULE-ARIA-INVALID-MISSING** (8903e67, turn 28) New `forms/aria-invalid-missing`. Field carries `.is-invalid` / `:invalid` styling to convey error but never sets `aria-invalid="true"` — color-only error feedback. WCAG 1.4.1 + 4.1.3.
- [x] **Q3-RULE-ERROR-MESSAGE-ASSOC** New `forms/error-message-not-associated`. `.invalid-feedback` / `[role="alert"]` sibling of an input where the input has no `aria-describedby` pointing at the error's `id`. WCAG 3.3.1 + 3.3.3. (3f2c160+04f0259, turn 29)
- [x] **Q3-RULE-PLACEHOLDER-AS-LABEL** New `forms/placeholder-as-label`. Input has `placeholder` but no visible label, `aria-label`, `aria-labelledby`, or wrapping `<label>` — or the placeholder text duplicates what a CSS-styled float-label would show (Bootstrap's floating-label antipattern). WCAG 3.3.2 + 1.3.1. (e163c12, turn 30)
- [x] **Q3-RULE-RADIO-GROUP-FIELDSET** New `forms/radio-group-without-fieldset` (stronger than existing `fieldset-legend`). ≥2 `<input type="radio">` sharing a `name=` without an ancestor `<fieldset><legend>` or `role="radiogroup"` + `aria-labelledby`. WCAG 1.3.1 + 3.3.2 + 4.1.2. (5777e89+b3dc3af+8fc3be4+27083fc, turn 31)
- [ ] **Q3-RULE-LIVE-REGION-PRE-EXISTENCE** New review finder (not rule — runtime state) `aria/live-region-pre-existence`. `role="alert"` / `role="status"` / `aria-live` on an element hidden at initial render (`display: none`, `.d-none`, `hidden`). WCAG 4.1.3. Toasts.mdx documents the gotcha in prose.
- [ ] **Q3-RULE-CHARSET-DECLARATION** New `document/charset-first-1024-bytes`. `<meta charset>` must be the first child of `<head>` within the first 1024 bytes (HTML spec; pairs with `document/viewport-zoom`). Low priority but mechanical.

### v0.3.0 — accepted (P2)

- [ ] **Q3-FINDER-FORM-TIMING-BOOTSTRAP** Bootstrap validation docs teach `.needs-validation novalidate` + per-keystroke visual feedback. The existing `forms/validation-timing` finder (Q2R2-FORM-TIMING) should also recognize this Bootstrap pattern when parsing restores. Verify coverage after parser restart.
- [ ] **Q3-FINDER-CAROUSEL-PATTERN** New review finder `motion/carousel-pattern`. Surface `data-bs-ride="carousel"` + role=region/aria-roledescription="carousel" + accessible name checks. WCAG 2.2.2. Companion to Q3-RULE-PAUSE-STOP-INLINE-STYLE.
- [x] **SCAFFOLDER-IMPORT-POSITION** `scripts/scaffold-rule.ts` appends the new rule's import at the end of `src/rules/index.ts`'s import block instead of inserting alphabetically within the correct path-group cluster (`forms/`, `navigation/`, `media/`, etc.). Observed independently by three rule-implementer agents in turn 29/30 (Q4-RULE-EXTERNAL-LINK, Q4-RULE-PLACEHOLDER-ALT, Q3-RULE-PLACEHOLDER-AS-LABEL) — each had to hand-reorder or ship a biome-cleanup `fix(rules): import position` follow-up commit, and cross-turn integration conflicts on `src/rules/index.ts` are almost entirely driven by the tail-append collision. Fix: teach the scaffolder to parse the existing import block, find the right path-group cluster for the new rule's path, and insert in alphabetical order inside that cluster. Fallback for unknown paths: append at end (current behavior). (12da16e — insertIntoRegistry uses module-path key for imports and identifier key for BUILTIN_RULES/exports; unit test exercises aria cluster insertion + identifier-block ordering)

### Considered and rejected (per CLAUDE.md §1)

- **Filter `.mdx` template-literal HTML out of findings by default** (one agent suggested we treat `<Example code={``}>` as "docs-only, skip") → rejected per §1 "No heuristic suppression" + "Labeled buckets are suppression too." The HTML in those template strings is rendered to users on the docs site — real live markup. Canonical docs-site surface; failing to scan it is a silent miss.
- **`aria/hidden-focus` severity downgrade on Bootstrap `.modal` patterns** (one agent noted Bootstrap toggles `aria-hidden` at runtime, so static analysis sees a false state) → rejected per §1 "Don't downgrade priority to hide things." The rule already emits honest reason text; additive `couldBeWrongBecause` metadata is appropriate (see Q2R2-CWBB), severity-downgrade is not.
- **Deduplicate `forms/autocomplete-missing` when it fires 28× on one file** (concentrated in `floating-label.html`) → rejected per §1 "Labeled buckets are suppression too." The meta `concentration: {file, count}` already surfaces the density honestly; letting the agent see 28 is correct. A `meta.rulePatternSummary` extension could enrich reason text, but the 28 candidates must stay individually visible.
- **Restore `<Example code={``}>` parsing via a Starlight-specific extractor** → deferred, not rejected. The Astro parser fix (Q3-MCP-RESTART-HINT unblocking the existing parseAstro wiring) closes the surrounding `.astro` case; a Starlight-specific content-prop extractor for `.mdx` is a separate engineering effort with a narrow scope (only helps docs sites using the `<Example>` pattern). Re-evaluate after Bootstrap docs actually parse end-to-end.

### Second-pass additions (2026-04-21)

Source: second Bootstrap-repo pass after turn-28 items landed. Findings triaged against CLAUDE.md §1 — most are correctness/false-positive tightening or shape-honesty on fields the first pass didn't exercise. Three Bootstrap items dedupe cross-repo: `PROPOSED_CONFIG_ABSOLUTE_PATH` merges with Jekyll-2 + Q6-9 under `Q-SHARED-PROPOSE-CONFIG-RELATIVE-PATHS`; `VARIABLES_SCSS_FALSE_BUILD_ARTIFACT` merges with Jekyll-3 under `Q-SHARED-BUILD-ARTIFACT-SASS-PARTIAL`; the `.md` skipped-by-extension item is a deep-dup with Q4-MARKDOWN-IMPL (cross-ref added below, no new item).

- [x] **Q3-RULE-NESTED-INTERACTIVE-ROLE-REGRESSION** Regression audit on `src/rules/semantics/nested-interactive.ts`. Q3-RULE-NESTED-INTERACTIVE-ROLE-WRAPPER marked closed but the rule still fires on role-only wrappers: outer `<div role="tab">` with no click handler / tabindex / native interactive tag triggers findings at `js/tests/visual/modal.html:63,78,92` and `collapse.html:17,32,46,60` (seven false positives). Determine whether the closure regressed or the originally-landed closure never matched these fixtures; add a real-world fixture under `tests/fixtures/real-world/bootstrap-role-tab-wrapper/` before fixing per CLAUDE.md §7 bug-fix workflow. WCAG 2.1.1 + 4.1.2. (5aa703d, turn 32 — fixture GREEN on current main; regression guard against re-introduction)
- [x] **Q3-RULE-POPOVER-NOT-DISCLOSURE** `src/rules/aria/expanded-on-disclosure.ts` fires on `data-bs-toggle="popover"` as if popovers were disclosure widgets; evidence `popover.html:13,17,21,25,29` + `modal.html:53`. Popovers are tooltip-shaped (`role="tooltip"` + `aria-describedby`), not disclosure (`aria-expanded`). Remove `"popover"` from the disclosure-toggle allow-list; keep `"dropdown"`, `"collapse"`, and the JS-classList-toggle predicates. Add a good-fixture under `tests/fixtures/good/expanded-on-disclosure/popover-not-disclosure.html`. WCAG 4.1.2. (cfc089f, turn 32.2)
- [x] **Q3-RULE-PAUSE-STOP-HTML-INLINE-STYLE-MISS** Q3-RULE-PAUSE-STOP-INLINE-STYLE (closed turn 26) missed the canonical carousel case: `carousel.html:10,20` has a `<style>.carousel-item { transition: transform 2s ease }</style>` block alongside `data-bs-ride="carousel"` and the rule emits zero findings. Verify that inline `<style>` scanning runs against HTML documents at the scanner's root (not only when `.css` files are parsed separately); add a fixture under `tests/fixtures/bad/motion-pause-stop-hide/html-inline-style.html` asserting the rule fires on the transition. Pairs with Q3-FINDER-CAROUSEL-PATTERN — the `data-bs-ride` signal itself is also a 2.2.2 candidate source. WCAG 2.2.2. (de4e32c, turn 32.3 — fixture-only; rule-fix follow-up in next invocation)
- [x] **Q3-OPAQUE-COMPONENTS-TOP-EMPTY-DISHONEST** (f793550) `assembleOpaqueComponentBlock` now omits `opaqueCustomComponentsTop` entirely when the interactive filter drops every candidate, instead of shipping `[]` alongside a populated `opaqueCustomComponentNames`. `buildHints` elides the `(top: ...)` parenthetical when examples is empty, so the prose no longer renders `"(top: )"` with nothing after the colon. Two new tests lock both codepaths.
- [x] **Q3-PLAN-MECHANICAL-EDITS-COUNTER-DRIFT** `plan.mechanicalEditsAvailable: 14` co-occurred with `plan.fixesByClass.mechanical: 0` — the 14 counted are all `verify-in-source`, not mechanical edits. Either rename `plan.mechanicalEditsAvailable` to `plan.safeEditsAvailable` (covers mechanical + verify-in-source — the two lanes an agent can act on with local edits) or align the counter to `fixesByClass.mechanical` only. Pick one definition; document the chosen semantic in CHANGELOG under `[Unreleased]`. Doctrine: "Composite headline counts are dishonest" — the counter currently sums two distinct lanes under a name that promises one.
- [x] **Q3-PARSE-ERROR-FILE-CONTRADICTION** `analysisCoverage.parseErrorFiles: [modal.mdx]` was emitted alongside 14 findings on that same `modal.mdx` with live line numbers. The current shape conflates "file wholly failed to parse" with "file partially parsed and some rules fired." Split: keep `parseErrorFiles` for total-parse-failure (zero findings possible), emit a separate `partialParseFiles: [{ path, reason }]` for files where the parser recovered and downstream rules ran on the recovered slice. Agents currently read `parseErrorFiles` as "these files are invisible" and miss the findings that did emerge.
- [ ] **Q3-TEMPLATE-DIRECTIVE-STARLIKE-MISDETECT** Bootstrap is Astro/Starlight, not a Handlebars/Mustache project, but `analysisCoverage.templateDirectivesFound: ["handlebars-or-mustache"]` fired. The `{{...}}` hits came from `.github/workflows/*.yml` GitHub Actions expressions (`${{ github.event.pull_request.number }}`) and Astro JSX props `overrides={{ body: bodyProps }}`. Tighten the detector: require a closing `}}` followed by non-JSX context, or drop the family entirely when an SSG has already been detected at the scan root. Pair with adding `"starlight"` as a recognized `ssg` value in the Q4-SSG-BUILD-HINT path so Astro + Starlight repos are named honestly. Doctrine: the classifier is already per-file majority-vote (Q4-TEMPLATE-DIRECTIVE-CLASSIFIER-DRIFT); this tightens what counts as evidence of a template family in the first place.
- [ ] **Q3-BOOTSTRAP-WRAPPER-DETECT-EMPTY-REASON** `detect_native_wrappers.wrappers.candidates: []` surfaced alongside a scan carrying 15 opaque PascalCase components in `opaqueCustomComponentNames`. The detector requires an `onClick` prop — MDX/Astro components rarely carry inline `onClick`. Two paths: (a) lower the bar in MDX/Astro to include components with any interactive-looking prop (`href`, `onClick`, `onPress`) or any rendered-as-element signal; (b) emit a structured `wrappers.emptyReason: "no-jsx-onclick-candidates-found-but-opaque-components-present"` so the agent sees the detector's lower-bound premise failed even though opaque components exist. Prefer (b) as an additive honest-signal shim; (a) risks widening false positives. Extends Q3-DETECT-WRAPPERS-EMPTY-REASON with the Astro/MDX-specific case. Doctrine: "Zero-output success is ambiguous failure."
- [ ] **Q3-IMAGES-OF-TEXT-SVG-DATA-URI-SRC-CORPUS** `wcag22:1.4.5` candidate at `js/tests/integration/index.html:31` claims "alt text repeated in surrounding text" when the `<img alt="First slide">` is adjacent to a `<text>First slide</text>` node inside an SVG `data:` URI sitting in the same element's own `src=` attribute. Surrounding-text corpus should exclude the flagged element's own `src` / `data-*` attribute values (they are not visible text on the rendered page). Extend the corpus-building helper to skip attribute values that contain `data:image/svg+xml`; alternatively, name the repetition's match location in the reason text so the agent can tell the text was inside the same element's `src`. Distinct from V1-FP-IMAGES-OF-TEXT-SVG-DATA-URI (that item enriches the reason when the `<img>` itself has a data-SVG src; this item is about the SVG text payload polluting the surrounding-text corpus for a different element or the same element).
- [ ] **Q3-TEXT-SUCCESS-CANDIDATE-IGNORES-TEXTCONTENT** `wcag22:1.4.1` candidate says `text-success` "with no visible text" on `<strong class="... text-success-emphasis">Design</strong>` — the element's `textContent` is "Design," which is visible. Finder must pre-check `element.textContent.trim().length > 0` before emitting the "no visible text" variant; when text is present, emit the simpler variant that frames the question as "color-only indicator check — verify the state is not conveyed by `text-success` alone." Pairs with (but does not block) Q3-RULE-COLOR-SOLE-INDICATOR. WCAG 1.4.1.

### Deeper-pass additions (2026-04-21, 10x)

Source: 23-scan deeper pass against the bootstrap repo as part of the four-repo 10x field test. Load-bearing discovery: the MCP subprocess serving the session was spawned before a mid-session `dist/cli.js` rebuild, and Q3-MCP-RESTART-HINT silently failed to fire on any of 23 responses across 90+ minutes. Several items below are symptoms whose underlying rule/shape fix is already on disk — but the detection failure itself is a bug, and it cascades into the rule-registry drift tracked under Q-SHARED-RULES-EVALUATED-SSOT.

- [ ] **Q3-MCP-RESTART-HINT-ERROR-ENVELOPE** `annotateStaleSubprocess` short-circuits on `result.isError === true`, so stale-subprocess + `file-unsupported` on `.md` is indistinguishable from "extension truly unsupported." Highest-risk path for silent miss: an agent that hits an error envelope on a stale subprocess never learns the tool needs a restart. Fix: merge `stale_mcp_subprocess` into `warnings[]` regardless of `isError` — the warning is about the subprocess, not the individual call's success. Three tests: `isError: true` + warnings merge, non-string filter on error envelope, error-envelope preserves `structuredContent` when present.
- [ ] **Q3-MCP-RESTART-HINT-SUBPROCESS-RACE** Q3-MCP-RESTART-HINT was closed in turn 32 but failed to fire across a full 90-min session even though dist mtime advanced mid-session. Root-cause three candidates: (a) `recordSubprocessStart` records baseline at module-init and never re-stats; (b) `import.meta.url` resolution returns a stale path when the subprocess is spawned before the rebuild and the path becomes a different inode; (c) the one-shot recording is cached and never retried. Reproduce with a deterministic test (spawn subprocess → rewrite dist → call tool → assert warning fires), not just added coverage. The fix self-heals ≥40% of the enumerated BUGs in the 10x pass triage.
- [ ] **Q3-META-BUILD-PROVENANCE** Add `meta.ra11yVersion: string`, `meta.commitHash?: string`, and `meta.bundleMtime: string` (ISO) to every MCP response so an agent can detect drift between expected and actual tool versions without relying on `stale_mcp_subprocess` heuristic alone. Conditional-spread `commitHash` per the AI-first consumer model (non-git deployments ship without it). Pairs with Q3-MCP-RESTART-HINT-SUBPROCESS-RACE — provenance is the positive signal; stale-warning is the negative signal; both needed. Additive meta field; no new tool parameters.
- [ ] **Q3-RULES-BY-EXTENSION-UNDERCOUNT** `.scss` scan reports `rulesByExtension: {".scss": ["focus/outline-visible", "wrapper/drift"]}` (2 rules), but `perRuleCoverage` on the same scan shows 8+ CSS-family rules with `filesEvaluated: 1` on that extension. Two surfaces disagreeing on the same input. Either `rulesByExtension` is a subset view (document + rename to `rulesByExtensionPrimary`) or it's a bug (align counts with `perRuleCoverage`). Pick one. Doctrine: cross-surface consistency — two fields naming "rules run on this extension" must agree or carry differentiating names.

### Cross-repo shared (2026-04-21)

- [x] **Q-SHARED-PROPOSE-CONFIG-RELATIVE-PATHS** `propose_config` now pipes build-artifact paths through `normalizeExcludes(paths, root)` before emitting. Every emitted path is POSIX-style root-relative (`dist/compiled.css`, never `/tmp/bootstrap/scss/_variables.scss`), paths outside the scan root are dropped, and groups with 3+ entries sharing a top-level directory collapse to a single `<dir>/**` glob (motivating case: website-templates 301 entries under one `dist/` tree). Two-entry groups stay itemized so a small `assets/` pair reads clearer than a wildcard. Collapse threshold pulled out as `EXCLUDE_GLOB_COLLAPSE_THRESHOLD = 3` with rationale. Three new tests (relative-only invariant, 3-file collapse, 2-file preserve) locked in alongside the existing exclude-branch test. Bootstrap inherits the fix transparently via its propose_config leg. Consolidates Bootstrap-8 + Jekyll-2 + Q6-9.
- [x] **Q-SHARED-BUILD-ARTIFACT-SASS-PARTIAL** `scannedBuildArtifacts` classifier fires on Sass partials (`_variables.scss`, `_style.scss`) because the filename starts with `_`. That prefix is the Sass partial convention (authored source) — not a generated-artifact signal. Key the classifier off deterministic markers instead: `.min.` infix, `dist/` / `build/` / `_site/` / `public/` path ancestry, hashed-filename patterns (`.[a-f0-9]{8,}.`), or a sibling sourcemap. Consolidates Bootstrap-9 + Jekyll-3. Doctrine: the `scannedBuildArtifacts` label itself is honest (findings still surface per Q4-SCSS-DISCOVERY-WIRE), but mis-labeling authored Sass as artifact misleads the agent's triage — the label must be provable from the file shape.

### Cross-repo shared (2026-04-21 deeper 10x pass)

Source: aggregated four-repo 10x deeper field test (bootstrap, jekyll, 50projects50days, website-templates — 95 MCP scan operations total). Items here reproduce across ≥2 repos. Single-repo deeper-pass additions live under each track's `Deeper-pass additions (2026-04-21, 10x)` sub-section.

- [ ] **Q-SHARED-MD-DISPATCH-UNIFY** `scan_file` on `.md`/`.markdown` returns `{code: "file-unsupported", remediation: "...astro, .css, .htm, .html, .js, .jsx, .mdx, .scss, .ts, .tsx."}` — `.md` and `.markdown` elided from the advertised set — while `session.parseFile` and `tool-apply-fix-internals.ts` both dispatch correctly. Root cause: commit `38e86f5` claimed "route .md/.markdown through parseMarkdown end-to-end" but `src/cli/commands/scan.ts::parseFor` has no `.md`/`.markdown` branch, and the MCP `SCAN_FILE_UNSUPPORTED_REMEDIATION` constant is not rebuilt against the live registry at call time. Reproduced in bootstrap (13 `.md` skipped), jekyll (307 `.md`/`.markdown` invisible to CLI), 50projects50days (`README.md` routed to `skippedByExtension`). Fix: land the CLI `parseFor` `.md`/`.markdown` branch; derive `SCAN_FILE_UNSUPPORTED_REMEDIATION` from `parseableExtensions()` at call time (not module-init); golden fixture (sanitized jekyll-style `_docs/assets.md` with embedded HTML). Folds Q4-MD-CLI-PARSEFOR-MISSING + Q4-SCAN-FILE-UNSUPPORTED-REMEDIATION-STALE filed by the deeper-pass Jekyll scan. Doctrine: "Ambiguous field shapes are dishonest" — remediation prose claims a single source of truth that doesn't match runtime.
- [ ] **Q-SHARED-RULES-EVALUATED-SSOT** `rulesEvaluated` disagrees across MCP surfaces on the same `cwd`: `scan_project` reports 53, `propose_config` reports 54, `list_rules` reports 54 (50projects50days deeper pass; same three-way drift reproduced on website-templates where `navigation/href-javascript-void` sits in source + is imported but absent from running registry). Likely cause: JSX/TSX-only rule counted-as-evaluated when JSX input is absent differs between tools; stale subprocess serving a 53-rule bundle against a 54-rule on-disk bundle compounds. Fix: centralize "rules evaluated on this input" in one helper consumed by every surface; add cross-surface invariant test asserting `list_rules` rule set == keys in any scan's `perRuleCoverage`. Pairs with the "Needs judgment" question on JSX/TSX-only rules counted as evaluated on non-JSX inputs. Doctrine: "Verbose meta is signal, not clutter" + cross-surface consistency.
- [ ] **Q-SHARED-NEXTSTEP-SCHEMA-DISPATCH-TEST** `nextStepStructured.args.file` on a scan response names `file` as the parameter, but `scan_file`'s schema declares `path`; `suggest_fix.verifyCommandStructured` emits a non-existent `ruleId` parameter on `scan_file` args. An agent that pipes `args` through verbatim hits `InputValidationError` — the structured-next-step contract is broken by silent drift. Fix: typed dispatch test in `tests/unit/mcp/` asserting every `nextStepStructured` and `verifyCommandStructured` `args` object validates against the target tool's input schema at build time (any `args` key not in the target's JSONSchema fails the test). Doctrine: "One tool call should answer 'what next?'" — the structured hint must be directly callable.
- [ ] **Q-SHARED-SCAN-PROJECT-INLINE-REVIEW-CANDIDATES** `scan_project` returns `actionableManualItems: N` with `files: []` when automated findings are zero on the scanned root. Agent has no inline file:line pointer and must make a separate `checklist` call. Reproduced in jekyll (`test/source` sub-scan returned `actionableManualItems: 1` + empty files array) and website-templates (`plugins/` sub-scan returned `actionableManualItems: 1, files: []`). Fix: add `reviewCandidateFiles?: [{ path, count }]` (or inline `reviewCandidates?: [{file, line, criterion, reason}]` capped by the same `limit`) on `scan_project` per-file entries or as a top-level array, conditional-spread when automated-findings files exist. Doctrine: "One tool call should answer 'what next?'" — currently forces a wasted round trip.
- [ ] **Q-SHARED-SCAN-FILE-ERROR-DISCRIMINATION** `scan_file` uses `code: "file-unsupported"` for both unsupported-extension and file-not-found. Bootstrap confirmed: `scan_file` on a nonexistent `.mdx` path (extension IS supported, file doesn't exist) returns `file-unsupported`; `scan_project` has a distinct `cwd-not-found` code. Split: `file-not-found` (path doesn't exist) / `unsupported-extension` (extension not in `PARSEABLE_EXTENSIONS`) / `file-unreadable` (permission / IO). Pairs with Q-SHARED-MD-DISPATCH-UNIFY — an agent hitting `file-unsupported` on a `.md` input needs to distinguish "extension truly unsupported" from "not-supported-by-this-subprocess" from "not found on disk." Doctrine: "Ambiguous field shapes are dishonest."
- [ ] **Q-SHARED-DISCLOSURE-REASON-ENRICHMENT** `aria/expanded-on-disclosure` fires on patterns where the reason is insufficiently annotated: bootstrap MDX popover examples (Q3-RULE-POPOVER-NOT-DISCLOSURE closed, but 103 lingering findings on attribute combos the closure didn't widen for), website-templates collapse button with `<span class="sr-only">Toggle navigation</span>` accessible name. When the rule fires on an element carrying `.sr-only` / `.visually-hidden` / `aria-label` children, enrich `reason` with "note: element has a visually-hidden text child — if that is the disclosure label, verify the accessible name is complete." Reason-text enrichment only — candidate stays in the primary list. Doctrine: "Surface don't suppress" — enrich reason so agent dismisses faster without heuristic suppression.

---

## Track Q4 — Static-site-generator field test (Jekyll, 2026-04-20)

Owner: main session + general-purpose. Source: 10-agent parallel eval against the Jekyll repo (jekyll/jekyll, /tmp/jekyll). Slices: `_layouts/`, `_includes/`, `pages/`, `_docs/` (alphabetical split), `_posts/` + `_tutorials/`, CSS/SCSS, raw `test/source/*.html`, `lib/` scaffold templates, Ruby source tree, and an MCP surface critique from a non-React-project angle.

Jekyll is the canonical static-site-generator shape: HTML templates with Liquid (`{% ... %}`, `{{ ... }}`), YAML frontmatter, `.md`/`.markdown` content, SCSS, ERB fixtures, Ruby source. Where Q3 (Bootstrap) probed the docs-site MDX/Astro axis, Q4 probes the Ruby/SSG/Liquid axis — the failure modes are different. Cross-referenced against Q3 to avoid duplication; items that overlap are tagged as such.

**MCP freshness:** one MCP subprocess spawned post-rebuild (pid 16243, started 23:07 vs dist rebuilt at 22:30), so findings reflect current bundle behavior — the Q3 stale-process confound does not apply here.

### v0.2.0 — accepted (P0 — parser correctness, load-bearing)

- [x] **Q4-LIQUID-ATTR-QUOTE-BUG** HTML parser's attribute-value tokenizer breaks on nested-quote Liquid. `lang="{{ site.lang | default: "en-US" }}"` tokenizes as `lang="{{ site.lang | default: "` — the inner `"` terminates the attribute value, leaving `en-US" }}"` as stray content. Triggers FALSE `parsing/html-has-lang` findings on every Jekyll scaffold template (`lib/blank_template/_layouts/default.html:2`, `lib/theme_template/...`) — every `jekyll new` site inherits the false positive. Fix: teach the attribute-value tokenizer to recognize `{{ ... }}` / `{% ... %}` spans and skip their contents when searching for the closing quote. Load-bearing: until this is fixed, every Liquid-templated HTML project gets confident wrong findings.
- [x] **Q4-LIQUID-TEXT-LITERAL** Rules that consume visible text (`semantics/label-in-name`, `semantics/list-structure`, `document/page-titled`, `navigation/link-descriptive-text`) treat Liquid template expressions as literal visible text, producing FALSE positives when the rendered output would be fine. Examples: `docs_contents_mobile.html:2` (Liquid `{% for %}` loop inside `<select>` tagged as the accessible-name string); `docs_contents.html:9` (`{% capture %}` block reinserts `<a>` inside `<li>` at a later line — `list-structure` sees naked `<a>`-in-`<ul>`). Fix: strip or placeholder-ize Liquid/ERB/Jinja directives in the parser's text-node output so rules operate on the rendered-text shape, not the source shape. Pair with a `template_directive_stripped` signal in `reason` text so an agent knows the check ran against the stripped text. Doctrine: not heuristic suppression — the rule's premise (visible text) is what's being computed, and Liquid directives are not visible text.
- [x] **Q4-SCSS-DISCOVERY-WIRE** Audited the full path — the wiring was already complete at the time the item was filed: `feat(input): route .scss through the scss parser end-to-end` (96620ec) landed all four plumbing pieces (`PARSEABLE_EXTENSIONS`, `parseForExtension` dispatch, `extensionMatches` `.scss → .css` alias, apply-fix `Ext` discriminator). Reproduced against current `main` via a direct `scan_file` handler call and an MCP-subprocess JSON-RPC round-trip: both parse `.scss` cleanly and fire `contrast/minimum` on authored Sass (ratio 4.48:1 flagged). `discoverFilesWithDiagnostics` accepts `.scss` and emits no `skippedByExtension` entry. The Jekyll field report was observing a stale bundle despite the turn-10 freshness check. Added two regression tests in `tests/unit/mcp/tools.test.ts` — one asserting `scan_file` on a `.scss` file parses end-to-end and fires `contrast/minimum` with `wcag22:1.4.3`, one asserting `scan_project` on a mixed `.scss`+`.css` directory counts every `.scss` under `filesByExtension` and does NOT report them as `skippedByExtension`. Future refactors that drop any link in the chain (discovery allow-list → parser dispatcher → rule eligibility) will trip the tripwires.
- [x] **Q4-MARKDOWN-SUPPORT** ADR-only phase done in ADR 0025 (4e0bc95); leaving item open for Q4-MARKDOWN-IMPL. `.md` (157) and `.markdown` (150) return `file-unsupported` in Jekyll — every `_docs/*.md`, `_posts/*.markdown`, `_tutorials/*.md`, `pages/*.md`, and `README.markdown` is invisible. Raw HTML embedded in markdown (tables, iframes, admonition divs, inline `<a>`/`<img>`) never reaches any rule. Options: (a) full CommonMark parser (large); (b) lightweight HTML-in-markdown extractor that strips `#`-headings + fenced-code blocks, feeds HTML-block residue to `parseHtml`, scans `![alt](url)` for alt-text, and maps kramdown IAL (`{: .note .warning}`) to `class=` on the preceding block. (b) is ~300 LOC, unlocks static-site-generator ecosystem (Jekyll, Hugo, MkDocs, Docusaurus, Astro content collections). Decision: explore (b); ADR before implementation.
- [x] **Q4-MARKDOWN-IMPL** Implement Option B per ADR 0025 — ~300 LOC HTML-residue extractor at src/input/parsers/markdown.ts, discovery wiring, analysisCoverage hint. Blocks on nothing. Medium. **Cross-ref (2026-04-21 Bootstrap second-pass):** Bootstrap repo skipped 13 `.md` files as "extensions_skipped_no_parser" despite having no MDX/Liquid syntax — those are plain-HTML-in-markdown just like Jekyll `.md`. When implementing, consider a fast-path: route `.md` through the `.mdx` parser when no MDX syntax is detected (no `import`/`export`, no JSX element opens), emit `md_files_skipped_no_parser` warning if the parser declines. Same implementation; two caller sites validate it. (a1a0e3b+a7b169e, turn 31)

### v0.2.0 — accepted (P1 — template-awareness and fragment detection)

- [ ] **Q4-FRAGMENT-DETECTION** Jekyll `_includes/` partials (top.html, footer.html, header.html, error.html) are fragments — they have no `<html>`, no `<head>`, start mid-document with `{%- include ... -%}`. ra11y currently fires `document/page-titled`, `semantics/landmark-main`, `navigation/skip-link`, `parsing/html-has-lang` on these files as if they were full pages. Detect the fragment shape (file under `_includes/`, `_layouts/`, `components/`, or file without matching `<html>`/`<body>`/`<head>` structure) and suppress full-page-required rules with a structured `fragment_detected` signal (review-candidate, not suppression — the agent can still manually check that the composed layout has `<main>`). Alternative: emit an `analysisCoverage.fragmentFiles: [paths]` counter so the agent knows partials were scanned but page-level rules didn't apply. **Confirmation (2026-04-21 second pass):** 2.4.5 finder still flags page-titled sibling cases (Q4-PARTIAL-PAGE-TITLED already closed handles page-titled; the broader fragment-detection gap persists for landmark-main + skip-link). Still open.
- [x] **Q4-TEMPLATE-DIRECTIVE-CLASSIFIER-DRIFT** (dfd9ed5, turn 25, 2026-04-21) Fixed in `src/mcp/analysis-coverage.ts` (agent confirmed real location; `src/input/analysis-coverage.ts` does not exist). Classifier now accumulates per-file family sets and performs per-file majority-vote rather than a global accumulation, eliminating cross-file tag bleed. All existing template tests pass; added targeted round-trip test for Liquid + Jinja coexistence. 107 lines changed.
- [x] **Q4-XMLLANG-LANG-MISMATCH** `test/source/_layouts/default.html:7` has `<html xmlns="..." xml:lang="en" lang="en-us">` — both present, differ (`en` vs `en-us`). No rule validates the consistency. Extend `document/lang-attribute` or add `document/lang-xmllang-mismatch`. WCAG 3.1.1.
- [ ] **Q4-CROSS-INCLUDE-LANDMARK-COMPOSITION** Jekyll layouts compose as `top.html` (head + opening body) + `{{ content }}` + `footer.html`. Each file parses in isolation; `semantics/landmark-main` fires on `error.html` (no `<main>`) but stays silent on `default.html` even though the composed page also has no `<main>`. Two options: (a) teach the scanner to follow `{% include %}` directives for in-tree composition (complex, cross-file, may produce false positives); (b) document the cross-file gap in a `couldBeWrongBecause: ["partial_or_layout_file_requires_composed_check"]` on the relevant rules. (b) is the honest minimum; (a) is a larger design.

### v0.2.0 — accepted (P1 — MCP shape for non-React projects)

- [x] **Q4-LANGUAGE-CLASS-WARNING** No headline-level warning when `skippedByExtension` shows a content-language dominance that ra11y can't parse. Jekyll scan drops 307 `.md`/`.markdown` files, 160 `.rb`, 18 `.scss` silently under a generic `extensions_skipped_no_parser` code — an agent skimming the top-level fields concludes "20 findings, scan had teeth" when the reality is "the actual content layer was never seen." Emit structured codes: `content_files_skipped: { count, exts }` when markdown/rst count > 50; `scss_files_skipped` until Q4-SCSS-DISCOVERY-WIRE lands; `source_language_unsupported: { language }` on detection of Ruby/Python/Go/PHP dominance. Doctrine: "Zero-output success is ambiguous failure" — sibling case for "near-zero output on the wrong-shape project." Note: `scss_files_skipped` code intentionally NOT added — `.scss` is in PARSEABLE_EXTENSIONS and never reaches `skippedByExtension`. (6431452, turn 24)
- [x] **Q4-PROPOSE-CONFIG-ECOSYSTEM** (d8fdce5, turn 26, 2026-04-21) `propose_config` emits a `@ra11y/core` TypeScript config into any project — including one whose root has `Gemfile` / `Pipfile` / `go.mod` and no `package.json`. No warning that the consumer may not want to add a Node toolchain. Fix: probe the repo root for ecosystem markers (`package.json`, `Gemfile`, `pyproject.toml`, `go.mod`, `Cargo.toml`) and either (a) emit `warnings: ["foreign_ecosystem_detected: ruby"]` alongside the config, (b) propose an alternative invocation (`npx @ra11y/core scan` rather than a committed `ra11y.config.ts`), or (c) ask the agent to confirm via `nextStep` before writing config. Do NOT suppress the config — the user may genuinely want to add it — but surface the context.
- [ ] **Q4-BOOTSTRAP-CI-SNIPPET-ECOSYSTEM** `bootstrap` CI snippet hardcodes `actions/setup-node@v4` in the suggested workflow. For Ruby/Python/Go projects, the consumer's existing CI almost certainly uses `ruby/setup-ruby` / `actions/setup-python` / `actions/setup-go`. Two options: (a) detect the ecosystem and pick the matching setup action; (b) emit the snippet as additive — "add this job alongside your existing CI" — with a prose preface acknowledging the existing toolchain. (b) is less fragile. **Confirmation (2026-04-21 second pass):** still open; Jekyll re-scan confirmed the `setup-node@v4` hardcode is what surfaces on a Ruby project.
- [x] **Q4-SSG-BUILD-HINT** (95c87a5, turn 28) Detect static-site-generator signals at the repo root (`_config.yml` → Jekyll; `hugo.toml`/`config.toml` → Hugo; `astro.config.*` → Astro; `.eleventy.js` → Eleventy; `gatsby-config.*` → Gatsby; `mkdocs.yml` → MkDocs) and add a `nextStep` suggestion: "for full coverage, build the site and point `additionalPaths` at the rendered output (`_site/`, `public/`, `dist/`)." Structured hint: `meta.detectedFramework: { name, buildOutput, buildCommand }`. This is the discoverable answer to "what does ra11y CAN'T do on my static site." Pairs with existing `analysisCoverage.hints` prose.
- [ ] **Q4-ADDITIONALPATHS-REDUNDANT** `scan_project` with `additionalPaths: ["docs/_includes"]` returns `additionalPathsScanned.filesAdded: 0` when those files were already discovered by default enumeration. No signal to the agent that the flag was redundant (not ignored — just redundant). Emit `warnings: ["redundant_additional_paths"]` when the requested paths' files are already in the default-discovered set. Companion to Q3-ADDITIONAL-PATHS-SKIP-REASON (caller asked for N, got M_new, (N - M_new) were already scanned or dropped — distinguish).

### v0.3.0 — accepted (P1 — rule gaps + tightenings)

Deduped against Q3 where applicable. Items marked `[dup-Q3]` overlap and should share implementation.

- [x] **Q4-RULE-IFRAME-TITLE** [dup-Q3-RULE-IFRAME-TITLE] `<iframe>` without `title` or with generic title (`"YouTube video"`, `"iframe"`). Jekyll `_tutorials/video-walkthroughs.md:10` + `_docs/assets.md`. WCAG 4.1.2 + 2.4.1. Roll into Q3 ticket.
- [ ] **Q4-RULE-TABLE-CAPTION-SCOPE** [dup-Q3-RULE-TABLE-CAPTION + Q3-RULE-TABLE-TH-SCOPE] Table caption + th-scope. Jekyll's `_docs/front-matter.md:51-200`, `_docs/permalinks.md:50-170`, `_docs/variables.md`, `_tutorials/csv-to-table.md:179-204` — repeated pattern across 20+ Jekyll tables. Roll into Q3 tickets.
- [x] **Q4-RULE-NOTE-ROLE-FROM-CLASS** New `aria/role-from-class-only`. Admonition patterns (`.note`, `.warning`, `.alert`, `.tip`, `.info`) — HTML `<div class="note warning">` or kramdown IAL `{: .note .warning}` — without `role="note"` / `role="alert"` / textual severity prefix. WCAG 1.3.3 + 1.4.1 + 4.1.2. Jekyll docs use this pattern ~30 times across `_docs/`, all invisible today (even without markdown support, the pattern will apply to every SSG). Distinct from Q3-RULE-COLOR-SOLE-INDICATOR which is about status-colored text anywhere — this is admonition-widget-specific.
- [x] **Q4-RULE-EXTERNAL-LINK-NEW-WINDOW** New `navigation/link-target-blank-announcement`. `<a target="_blank">` without `rel="noopener noreferrer"` + no new-window announcement (visible text, sr-only span, or `aria-label` containing "opens in new window"). WCAG 3.2.5. Jekyll's `showcase.html:14` + `_tutorials/navigation.md` have the pattern. (713c7fe+a362448, turn 29)
- [ ] **Q4-RULE-DUPLICATE-LANDMARK-UNLABELED** New `semantics/duplicate-landmark-unlabeled`. Two or more `<nav>` / `<main>` / `<aside>` / `<form>` landmarks on a rendered page, none or fewer-than-N with `aria-label` / `aria-labelledby`. WCAG 1.3.1 + 2.4.1. Jekyll's `header.html` ships two `<nav>` elements (mobile + desktop), neither labeled. Requires cross-file composition awareness (Q4-CROSS-INCLUDE-LANDMARK-COMPOSITION) to fully catch. **Confirmation (2026-04-21 second pass):** in-file single-nav variant also worth covering — `header.html` on the second pass surfaced a single unlabeled `<nav>` in a fragment file; widen the rule's fragment-file single-nav eligibility alongside the multi-nav case.
- [ ] **Q4-RULE-SECTION-ACCESSIBLE-NAME** New `semantics/section-accessible-name-missing`. `<section>` used as a content wrapper without `aria-label` / `aria-labelledby` / enclosing heading. WCAG 1.3.1 / 4.1.2. `<section>` without name is semantically a generic `<div>` for AT. Low priority (many `<section>` usages are fine as-is per spec — only flag when section is a direct child of `<body>` or sibling to other landmarks).
- [x] **Q4-RULE-PLACEHOLDER-ALT** New `media/alt-text-placeholder`. `<img alt="...">` / `alt="image"` / `alt="screenshot"` / `alt="TODO"` / `alt="placeholder"` — generic alt that teaches bad patterns when copy-pasted. WCAG 1.1.1. Jekyll's `_tutorials/` docs + `pages/showcase.html` + `_docs/posts.md` teach `![My helpful screenshot](...)` as canonical — docs propagate weak patterns to every user. (0177360, turn 30)
- [ ] **Q4-RULE-PAGINATION-GLYPH-NAME** New review finder `navigation/pagination-glyph-accessible-name`. Pagination controls using `«` / `»` / `‹` / `›` as sole link text without `aria-label` or sr-only expansion. WCAG 2.4.4 + 4.1.2. Jekyll's `_docs/pagination.md:112-169` teaches the glyph pattern; similar patterns across design-system docs.
- [ ] **Q4-RULE-DISABLED-LINK-SURROGATE** New `aria/disabled-link-surrogate`. `<span class="disabled">` / `<span class="previous">` used as a disabled control where the live state is a `<a>`. Without `aria-disabled="true"`, AT announces the span as static text, losing the disabled-state signal. WCAG 4.1.2.
- [x] **Q4-RULE-XMLLANG-LANG-MATCH** [cross-ref Q4-XMLLANG-LANG-MISMATCH] Same rule — the tightening doubles as a new rule if `document/lang-attribute` doesn't already cover `xml:lang`. (turn 32.5 — redundant: `document/lang-attribute.ts` already reads `xml:lang`, validates BCP 47 agreement at line 69-85, emits a tailored violation when the two disagree; Q4-XMLLANG-LANG-MISMATCH already closed this gap)

### v0.3.0 — accepted (P2)

- [ ] **Q4-WARNING-DOWNGRADE-NOISE** `warnings: ["template_files_parsed_as_literal"]` fires on every Liquid-templated scan — it's a constant on any Jekyll/Hugo/Eleventy repo. Consider demoting to a `meta.templateDirectivesFound` hint when no findings hit lines containing directives (i.e. the literal-parse didn't actually pollute results). Keep the warning when it matters; drop it when it's constant noise.
- [x] **Q4-RULES-EVALUATED-COMPOSITE** (bc29957, turn 27) `rulesEvaluated: 52` reports total loaded rules, not rules-with-eligible-inputs. On a pure-HTML/SSG project, many React-specific rules show `filesEligible: 0` in `perRuleCoverage` — they didn't fire because they couldn't. Replace the single count with a split: `rulesEvaluated: { loaded, withEligibleInputs, fired }` so an agent sees "52 loaded, 18 had eligible inputs on this file set, 9 fired." Doctrine: "Composite headline counts are dishonest."
- [x] **Q4-PARTIAL-PAGE-TITLED** `document/page-titled` fires on `top.html` (a `<head>` fragment) because the rule sees `{% seo %}` as literal text, not a rendered `<title>`. Pairs with Q4-FRAGMENT-DETECTION — the rule should skip fragment files, or at minimum emit `couldBeWrongBecause: ["title_may_be_template_injected"]` when the file lacks `<html>`/`<body>`.

### Considered and rejected (per CLAUDE.md §1)

- **Auto-suppress React-specific rules on non-React projects** → rejected per §1 "Labeled buckets are suppression too." The rules correctly no-op when `filesEligible: 0`; the agent sees that in `perRuleCoverage`. Don't filter — surface. Q4-RULES-EVALUATED-COMPOSITE is the right fix (make the empty-eligible-set state legible at the headline level, keep the per-rule data honest).
- **Auto-exclude `_site/`, `vendor/bundle/`, `node_modules/` in `propose_config`** → **accepted in a narrower form**: these are definitionally build output / vendor dumps where findings are not the user's to fix. Fold into `DEFAULT_EXCLUDED_PATTERNS` (see `src/input/discover.ts`) rather than `propose_config` — the exclude belongs at discovery, not config. CLAUDE.md §1 "Default-exclude globs are suppression too" permits this because findings in those paths are "definitionally wrong for any consumer." Add `_site/`, `.jekyll-cache/`, `vendor/bundle/`, `_build/` (Hugo), `public/` (Gatsby — though `public/` is ambiguous). Tracked as **Q4-DEFAULT-EXCLUDE-BUILD-DIRS** in P2 above if landed.
- **Heuristic "this looks like a Jekyll include, suppress skip-link finding"** → rejected as heuristic suppression. Q4-FRAGMENT-DETECTION is the principled alternative: detect partial/fragment structurally, not by path-pattern guessing.
- **Full markdown CommonMark parser as a P0** → deferred. Q4-MARKDOWN-SUPPORT option (b) — a lightweight HTML-in-markdown extractor — covers 80% of real findings at ~15% of the implementation cost. Revisit full parser after the extractor ships.
- **Auto-detect and scan `_site/` output when Jekyll is detected** → rejected as "tool should not run build toolchains." The agent reads `_config.yml` + `Gemfile` + knows `bundle exec jekyll build`; ra11y should point at the output, not produce it. Q4-SSG-BUILD-HINT is the right mechanism.

### Second-pass additions (2026-04-21)

Source: second Jekyll-repo pass after turn-28 items landed. Confirms three previously-open Q4 items (fragment detection, Bootstrap-CI-snippet ecosystem, duplicate-landmark) still fire on the current build — notes appended inline above rather than filed as new items. The four items below are net-new honest-signal gaps. Two Jekyll findings (`BOOTSTRAP-EXCLUDE-ABSOLUTE-PATH`, `BOOTSTRAP-EXCLUDES-SCSS-BECAUSE-BUILD-ARTIFACT`) moved into `Q-SHARED-PROPOSE-CONFIG-RELATIVE-PATHS` / `Q-SHARED-BUILD-ARTIFACT-SASS-PARTIAL` above.

- [ ] **Q4-PARSE-ERROR-DETAIL** `parse_errors_present` / `parseErrorFileCount: 1` surfaced on `docs/_includes/top.html` with no detail — which parser failed, where, why. Pair the top-level boolean with a structured `analysisCoverage.parseErrors: [{ path, parser, reason }]` so an agent has a fix-pivot beyond "one file didn't parse." Doctrine: verbose meta is scan-confidence signal; the bare boolean is the silent-failure shape. Distinct from Q3-PARSE-ERROR-FILE-CONTRADICTION (that item distinguishes total vs partial failure; this item populates the detail payload for both classes).
- [ ] **Q4-SCANNED-BUILD-ARTIFACTS-REASON** `warnings: ["scanned_build_artifacts_present"]` fires with paths but no per-path classification reason. Under the doctrine's "provable from code" bar (`ai-first-consumer.md` §labeled-buckets), the label has to survive inspection — `scannedBuildArtifacts` should be `[{ path, reason: "contains-data-url-gradient" | "minified" | "dist-path" | "hashed-filename" | "sourcemap-sibling" }]` so the agent can triage without reading each file. Pairs with Q-SHARED-BUILD-ARTIFACT-SASS-PARTIAL (that item fixes the classifier's predicate; this item makes the classifier's verdict inspectable). Structured over `warnings: string[]` — reason is a per-item payload, not a response-level code.
- [ ] **Q4-REVIEW-1.4.5-SR-ONLY-SIBLING** `header.html:7` logo image flagged for `wcag22:1.4.5` (images of text) but the reason doesn't mention the `<span class="sr-only">Jekyll</span>` sibling inside the same `<a>`. The sibling is a strong dismissal signal: the image is the visual rendering of the text, and an SR-only text equivalent is already carried in the DOM. Extend the 1.4.5 finder to detect `sibling_sr_only_text_present` when the flagged image shares a parent with an element carrying `.sr-only` / `.visually-hidden` / `aria-label` on the parent anchor; enrich `reason` with "note: a visually-hidden text sibling (`.sr-only` span) is present — if the image is the textual logotype and the sibling is the SR-accessible equivalent, this is the documented logotype pattern." Reason-text enrichment only — candidate stays visible.
- [ ] **Q4-REVIEW-SANITIZE-LIQUID-FINDERS** `showcase.html:16` 1.4.5 reason literally quotes the Liquid template expression `{{ entry.name }}` as the repeated surrounding text. Q4-LIQUID-TEXT-LITERAL stripped Liquid in the rule-side text-node paths, but the fix didn't propagate to `src/review/finders/**` — the same `template_directive_stripped` sanitizer must run on text the finders harvest for reason-building. Apply the existing stripper helper at the finder-side surrounding-text extraction sites; add a fixture under `tests/fixtures/real-world/jekyll-liquid-review-finder/` with a `{{ entry.name }}` repetition asserting the reason shows the rendered placeholder form (or omits the directive entirely) rather than the source token.
- [ ] **Q4-RELEVANCE-REASON-PARSE-COVERAGE-CAVEAT** `likelyIrrelevant` on 1.2.x criteria reports "no `<video>` or `<audio>` elements" as if the file set were fully inspected, when 307 `.md`/`.markdown` files were silently skipped. When `skippedByExtension` includes content extensions above a high-confidence threshold (`.md`, `.markdown`, `.rst`, `.adoc` — authored-content extensions that commonly embed media), enrich `relevanceReason` with a parse-coverage caveat: "note: X files in content extensions ({exts}) were skipped — if the site embeds `<video>` / `<audio>` in markdown content, the media check is under-covered." Doctrine: `likelyIrrelevant` is one of the few labels that must be provable from the code (`ai-first-consumer.md` §labeled-buckets); the bucket claim is only honest when the corpus was actually inspected. Pairs with Q4-MARKDOWN-IMPL — once markdown parsing lands, the caveat vanishes automatically for those repos.

### Deeper-pass additions (2026-04-21, 10x)

Source: 27-scan deeper pass against the jekyll repo as part of the four-repo 10x field test. Load-bearing discovery: commit `38e86f5` ("route .md/.markdown through parseMarkdown end-to-end") overstated scope — the MCP side works, the CLI side does not (see Q-SHARED-MD-DISPATCH-UNIFY). Items below are Jekyll-specific residue after the cross-repo Q-SHARED items are filed above.

- [ ] **Q4-TEMPLATE-CLASSIFIER-LIQUID-AS-MUSTACHE-SINGLE-FILE** Q4-TEMPLATE-DIRECTIVE-CLASSIFIER-DRIFT fixed the cross-file majority-vote drift, but a single-file variant remains: jekyll `_layouts/default.html` (pure Liquid with `{%- include -%}`, `{%- comment -%}`, `{{- content -}}`) is classified as `handlebars-or-mustache` because the per-file majority-vote drops `{%- ... -%}` Liquid-only tokens and counts `{{ ... }}` tokens — which the two families share. Fix: treat `{%- ... -%}` / `{% capture %}` / `{% if %}` as high-confidence Liquid-only evidence that decisively wins the per-file vote even when `{{ ... }}` tokens outnumber them. Pairs with Q3-TEMPLATE-DIRECTIVE-STARLIKE-MISDETECT (Bootstrap half on Astro/YAML). Doctrine: verbose meta integrity — wrong template-directive classification leads the agent to reach for the wrong strip rules downstream.
- [ ] **Q4-WARNING-DETAILS-CROSS-SURFACE-UNIFY** `warningsDetails.extensions_skipped_no_parser` is populated on `coverage` responses but absent from `scan_project` responses — same warning code, two shapes. Website-templates reproduces the same pattern for `scanned_build_artifacts_present`, `parse_errors_present` (populated on some surfaces, bare code elsewhere). Fix: ensure every response carrying `warnings: [code]` also carries `warningsDetails: { [code]: { ... } }` when details exist for that code; when no details exist, omit the key rather than shipping empty. Typed-test the cross-surface matrix: for each warning code, it must appear or not appear consistently across `scan_project` / `scan_file` / `coverage` / `checklist`. Doctrine: cross-surface consistency + present-when-meaningful.
- [ ] **Q4-LABEL-IN-NAME-LIQUID-STRIP-MISSING** Q4-LIQUID-TEXT-LITERAL was marked closed but `semantics/label-in-name` specifically still quotes raw Liquid `{% for ... %}` as "visible text" on `docs_contents_mobile.html:2` (and two sibling files). Reach: the strip helper was applied to some rules but `label-in-name` never picked it up. Fix: audit every rule under `src/rules/semantics/**` + `src/rules/navigation/**` that harvests visible-text strings, ensure each calls the `stripTemplateDirectives` helper before comparing, and add a rule-inventory regression test that mounts a fixture with `{% for %}` / `{{ var }}` tokens and asserts NO rule emits a finding quoting the raw directive. Pair with Q4-REVIEW-SANITIZE-LIQUID-FINDERS (finder-side already tracked).
- [ ] **Q4-SUGGEST-FIX-PROPAGATES-POISONED-VISIBLE-TEXT** `suggest_fix` primary proposal for `semantics/label-in-name` on the Jekyll `docs_contents_mobile` findings builds an `aria-label="{% for section in site.data.docs_nav %}..."` — pasting the raw Liquid template expression into a static ARIA attribute. If an agent applied this mechanically, the page would ship a broken accessible name on every render. Root cause: `suggest_fix` reads the rule's `visibleText` field, not a sanitized version. Fix: run `stripTemplateDirectives` on any text that `suggest_fix` interpolates into `newText`; when the result is empty or nonsense, fall through to a guidance-only suggestion with `fixClass: "guidance"`. Doctrine: verbose meta integrity — `suggest_fix.safety: "safe"` on a proposal that would break the page is a false safety claim.

---

## Track Q5 — Vanilla HTML/CSS/JS field test (50projects50days, 2026-04-21)

Owner: main session + general-purpose. Source: first pass against `bradtraversy/50projects50days` — 50 pedagogical vanilla `.html` + `.css` + `.js` bundles, no framework, no build step, no template directives. Exercises ra11y on the shape most "starter" tutorials propagate to learners; findings that stick here ship to every hobbyist project that copies these patterns. Triaged against CLAUDE.md §1 — all items are rule/finder correctness or new detection on patterns statically provable from the source.

Why this axis matters: most field tests so far (Q3 Bootstrap, Q4 Jekyll, Q6 website-templates) exercise framework-heavy or bundled shapes. 50projects50days is the control — clean, hand-authored, no build pipeline confounds. When a rule misfires here, the miss is on bare markup the author wrote.

### v0.3.0 — accepted (P1)

- [ ] **Q5-LABEL-ADJACENT-UNASSOCIATED** `src/rules/forms/labels-required.ts` fires on `<input id="length">` when a `<label>` sits one line above, but the `<label>` has no `for=` attribute. Pervasive pattern (≥21 instances across the repo). The rule's fix text ignores the existing adjacent label. Two paths: (a) widen `labels-required` to skip when an immediately-preceding `<label>` sibling is present (match browser-less-strict label association — the accessibility-inspector-friendly authoring pattern) and enrich `suggestion` to "add `for=\"<id>\"` to the adjacent label at line N"; (b) add a sibling rule `forms/label-adjacent-unassociated` firing specifically on the adjacent-but-no-for shape, with a mechanical fix that inserts `for="<id>"`. Prefer (b) — the fix is mechanical, and the existing rule's "no label at all" reason should stay clean for the truly-missing case. Satisfies wcag22:1.3.1 + 3.3.2 + 4.1.2.
- [x] **Q5-BUTTON-NAME-ICON-GLYPH-MAP** `src/rules/semantics/button-name.ts` correctly fires on `<button><i class="fa-*"></i></button>` but the fix text says "add visible text" as the primary. When the only child is `<i class="fa-X">` with a well-known glyph name, the primary fix should be `aria-label="<derived-from-glyph-name>"` (small glyph→label map: `fa-bars` → "Menu", `fa-times`/`fa-xmark` → "Close", `fa-arrow-left` → "Previous", `fa-arrow-right` → "Next", `fa-search`/`fa-magnifying-glass` → "Search", `fa-bell` → "Notifications", `fa-user` → "Account"); secondary stays "add visible text." ≥5 instances in the repo; the same pattern hits every icon-framework-using project. Extends V1-FIX-BUTTON-NAME with the Font Awesome glyph lookup. Satisfies wcag22:4.1.2. (db7a1c0, turn 32.2)
- [ ] **Q5-CAROUSEL-SETINTERVAL-PAUSE-MISSING** `image-carousel/script.js:9` has `setInterval(run, 2000)` with no pause UI anywhere in the DOM or script body — the candidate surfaces under `wcag22:2.2.1` with a generic reason. When the setInterval callback mutates DOM state (style/class/attribute change on a known-visual target), escalate the reason to cite `wcag22:2.2.2` "Pause, Stop, Hide" and name the missing pause UI. Reason-text enrichment only — candidate stays in the primary list; agent reads and decides. Pairs with Q3-RULE-PAUSE-STOP-INLINE-STYLE and Q3-FINDER-CAROUSEL-PATTERN. Satisfies wcag22:2.2.1 + 2.2.2.
- [ ] **Q5-SETINTERVAL-SHORT-DURATION-SEIZURE** `blurry-loading/script.js:6` has `setInterval(blurring, 30)` — ~33Hz visual-state mutation. The existing 2.2.1 finder doesn't connect fast intervals to `wcag22:2.3.1` (Three Flashes or Below Threshold). Enrich the finder's `reason` when the callback mutates a visual property (class/style affecting opacity, transform, filter, color, background) AND the duration literal is below 333ms: "note: callback runs at ~{rate}Hz — if the visual effect is a flash/blink, verify against SC 2.3.1 (must not flash more than 3× per second)." Additive context — no threshold-based candidate filtering (doctrine: numeric thresholds are suppression). The duration/rate goes in the reason text as evidence for the agent; the candidate always surfaces. Pairs with R-2.3.1-FLASHING (Track R, closed) — that finder already exists; this is integrating the timing-finder output with the flashing-finder criterion so one callback-with-short-interval finding carries both SCs when evidence supports it.
- [x] **Q5-LANDMARK-MAIN-HEADING-UNDERCOUNT-AUDIT** 52 files with `<!DOCTYPE html>` + `<body>` structure produced only 1 `semantics/landmark-main` finding and 6 `heading-hierarchy` findings. Suggests over-permissive fragment-or-layout heuristic in the rule's eligibility predicate. Audit `src/rules/semantics/landmark-main.ts` + `src/rules/semantics/heading-hierarchy.ts` against these files: compare which files are considered "full pages" vs "fragments/layouts" and whether the distinction matches the DOCTYPE + html/body presence. If files with a clear full-page shape are being skipped, tighten the fragment heuristic to require the fragment file to genuinely lack `<html>`/`<body>` (not merely look small). Add 3-4 fixtures under `tests/fixtures/real-world/50p-vanilla-doctype/` covering the actual-shape distribution. Candidate root-cause for the scan looking "clean" when real violations are present. (f5fa5a0, turn 32 — fixture RED by design per CLAUDE.md §7; rule tightening in looksLikeFullPage is follow-up work)
- [ ] **Q5-LANDMARK-FULLPAGE-HEURISTIC-TIGHTEN** Follow-up to Q5-LANDMARK-MAIN-HEADING-UNDERCOUNT-AUDIT. `src/rules/semantics/landmark-main.ts::looksLikeFullPage` currently gates on presence of `<header>`/`<nav>`/`<footer>`/`<aside>`; a file with `<!DOCTYPE html>` + `<html>` + `<body>` (no other landmarks) is mis-classified as a fragment and the rule never fires. The naive fix (accept DOCTYPE + `<html>` + `<body>` as sufficient full-page evidence) was tried inline and reverted — it broke 8 independent tests across integration/CLI/MCP/bootstrap because ~30 good-path HTML fixtures under `tests/fixtures/good/**/*.html` share that minimal full-page shape without a `<main>` landmark (they were authored under the old heuristic). A correct tightening needs a stronger signal than DOCTYPE+html+body alone — candidates: body descendant count (≥N interactive/content elements), form/interactive-element density, multiple-sibling structural content, or a content-vs-fixture classifier that reads file size / script count / title presence. Whatever heuristic is chosen, budget for either (a) updating ~30 good-path fixtures to add `<main>` wrappers, or (b) passing a "fixture mode" flag from the real-world harness that skips the rule. Evidence fixtures at `tests/fixtures/real-world/50p-vanilla-doctype/` (f5fa5a0); the failing assertion there is currently COMMENTED OUT pending this design decision. WCAG 1.3.1 / 2.4.1.
- [x] **Q5-RULE-ICON-FONT-ARIA-HIDDEN** New `aria/icon-font-hidden` rule (note severity). `<i class="fas fa-arrow-left">` / `<i class="fa-*">` / `<span class="material-icons">` inside a labeled parent (button with aria-label, link with text sibling) should carry `aria-hidden="true"` to avoid AT double-announcement. ~10+ instances in the repo; same pattern across every icon-font project (Font Awesome, Material Icons, Bootstrap Icons, Ionicons). Satisfies wcag22:4.1.2 + 1.3.1. Distinct from button-name / link-descriptive-text — those fire when the parent has no name at all; this rule fires when the parent is named AND the icon child is unannotated. Fixtures under `tests/fixtures/{good,bad}/icon-font-hidden/`. Mechanical fix. (b704a95+e24f64f+d0776c5+1deeab0, turn 29)
- [ ] **Q5-NAV-ROLE-SINGLE-PAGE-REASON** `wcag22:2.4.5 Multiple Ways` fires on every single-page HTML fragment with an identical boilerplate reason. The spec scopes 2.4.5 to "sets of Web pages" — single-page standalone files are a documented exception. Tighten the finder's `reason` when exactly one HTML file is being scanned OR the scan root has no multi-page signals (no router config, no `pages/` directory, no `<a href>` chain into sibling HTML files): "note: SC 2.4.5 applies to sets of Web pages — if this is a single-page app or standalone file, the criterion may not apply; verify whether the scanned file is part of a multi-page set." Reason-text enrichment only — candidate stays visible per surface-don't-suppress. Distinct from Q4-FRAGMENT-DETECTION (that item is about partials/layouts within a larger template system; this item is about standalone single-file HTML projects).

### Deeper-pass additions (2026-04-21, 10x)

Source: ~30-scan deeper pass against the 50projects50days repo as part of the four-repo 10x field test. Vanilla HTML is the clean control — misses here ship to every hobbyist project that copies these patterns.

- [ ] **Q5-RADIO-GROUP-NAKED-NAME-SHARING** Q3-RULE-RADIO-GROUP-FIELDSET (closed turn 31) covers `<fieldset>` + empty `<legend>` variants; the naked-name-sharing case still passes silently. `quiz-app/index.html` has four `<input type="radio" name="answer">` with zero group-level name — no `<fieldset>`, no `role="radiogroup"`, no `aria-labelledby`. Widen `forms/radio-group-without-fieldset` (or add a companion `forms/radio-group-no-group-name`) to fire on ≥2 radios sharing a `name` attribute when there is no ancestor `<fieldset>`, no `role="radiogroup"`, and no `aria-labelledby` on any common ancestor. Reason text: "radio group `{name}` has N inputs but no group-level accessible name — add `<fieldset><legend>...</legend>` or `role="radiogroup" aria-labelledby="..."`." Fixtures under `tests/fixtures/{good,bad}/radio-group-naked-name-sharing/`. WCAG 1.3.1 + 3.3.2 + 4.1.2.
- [ ] **Q5-OTP-INPUT-CLUSTER-DETECTION** `verify-account-ui/index.html` has a cluster of 6 `<input type="number" maxlength="1">` for a 6-digit OTP; `forms/labels-required` fires 6× individually but nothing detects the cluster pattern. New review finder `forms/otp-input-cluster`: detect ≥4 sibling single-character inputs (`type="number"`/`type="text"` with `maxlength="1"` or similar) within a common parent; recommend `autocomplete="one-time-code"` on at least one, plus a single group-level `<label>` / `aria-labelledby` covering the cluster. Candidate, not assertion — cluster heuristic may false-positive on non-OTP patterns; agent reads surrounding code to verify. WCAG 1.3.1 + 1.3.5 + 3.3.2.
- [ ] **Q5-DUPLICATE-LINK-TEXT-SAME-HREF** `split-landing-page/index.html` has two `<a>Buy Now</a>` both targeting `href="#"` with no distinguishing accessible name. `navigation/link-descriptive-text` fires on keyword-literal generic phrases but not on same-accessible-name-on-≥2-links. Extend the rule with a same-page pass: when ≥2 anchors share the same normalized accessible name and share the same `href`, flag all occurrences with reason "N links on this page use the same accessible name and href; a screen reader user navigating by link list cannot distinguish them." WCAG 2.4.4 + 2.4.9 (AAA). Reason-enrichment path if the hrefs differ but the names match: separate candidate tier (same-name-different-href is lower-confidence — legitimate pattern in some nav designs).
- [ ] **Q5-DECORATIVE-IMG-WITH-ADJACENT-MEANING** `feedback-ui-design/index.html` has emoji images marked `alt=""` adjacent to `<small>Unhappy</small>` / `<small>Happy</small>` — the image IS the visual indicator of the named affect, so `alt=""` removes meaning the sibling text doesn't replace (the text names the affect; the image shows the affect). Distinct from 1.4.5 images-of-text. New finder `media/decorative-img-with-adjacent-meaning`: when an `<img alt="">` is the sole image child of a parent that also contains meaning-bearing text referring to an affect/status/value (via a short dictionary: "happy", "sad", "warning", "success", etc.), surface as a review candidate — "image marked decorative but an adjacent sibling names the visual's meaning; verify the image is truly redundant or populate `alt=`." WCAG 1.1.1.
- [ ] **Q5-NUMERIC-ONLY-LABEL-CONTENT** `custom-range-slider/index.html` has `<label for="range">50</label>` — `forms/non-empty-label` passes because the label has text content, but "50" is a value display, not a semantic name. Enrich the rule's reason when the label's `textContent` matches a value-shape regex (`^\d+(\.\d+)?$` / currency / percentage / date / time patterns): "label text `{text}` looks like a value, not a name — verify whether this element labels the control or displays its current value; a labelling element should name what the control controls." Reason-text enrichment only — candidate stays in the primary list (the distinction is context-dependent; agent reads surrounding code). WCAG 2.4.6 + 3.3.2.

---

## Track Q6 — Bulk-template catalog field test (website-templates, 2026-04-21)

Owner: main session + general-purpose. Source: first pass against a 40+ bulk-template catalog (`website-templates/` repo). Each template is a pre-built marketing/landing-page bundle shipping jQuery-era vendor CSS/JS (bootstrap.css, font-awesome.css, jquery.fancybox.pack.js) alongside authored HTML. Exercises ra11y at scale — the scan hit the MCP token ceiling, surfaced several rule misfires against vendor bundles, and exposed catalog-scale quality gaps in the proposed config + build-artifact warning path.

Why this axis matters: website-templates is the "buy a template kit" shape that a huge fraction of small-business and freelance sites ship. The vendor-CSS noise ratio and jQuery-era link antipatterns (`href="javascript:void(0)"`, icon-only social links) are not framework-authoring problems — they are the default ra11y-on-real-purchased-templates experience. When the token budget overruns, legitimate findings never reach the agent.

Three cross-repo items moved out: `PROPOSE_CONFIG_EMITS_ABSOLUTE_PATHS_AND_TRAILING_COMMA` folds into `Q-SHARED-PROPOSE-CONFIG-RELATIVE-PATHS` (Track Q3 cross-repo section). Two Q6 items consolidate under one roof each: Q6-1 + Q6-13 → `Q6-BUDGET-UNDER-VENDOR-NOISE`; Q6-10 + Q6-11 + Jekyll-4 share scope with Q4-SCANNED-BUILD-ARTIFACTS-REASON (cross-refs inline).

### v0.3.0 — accepted (P0 — response budget + signal-to-noise)

- [ ] **Q6-BUDGET-UNDER-VENDOR-NOISE** Default `limit: 25` collapsed to `effectiveLimit: 1` via the density cap when vendor files dominated — `bootstrap.css` emitted 5,458 findings and `font-awesome.css` emitted 8,940 on a single scan, exhausting the 88K-char budget on one file. Two compounding gaps: (a) the token-density cap (V1-SIZE-RESPONSE-BUDGET-DENSITY-2) held the envelope but starved the response to one file's worth of findings; (b) a single criterion can carry 200+ review candidates on one file (see Q6-MEANINGFUL-SEQUENCE-UTILITY-CLASSES — `.order-0..13` utilities flagged 200+ times). Two additive fixes: per-file finding cap (hard limit ≈100 findings per file per response, with `perFileTruncated: true` + `findingsTotalInFile` on the file's entry) to prevent one file hoarding the budget; per-criterion byte cap on checklist responses (clip to first 3 candidates per criterion when response bytes exceed threshold, with `perCriterionClipped: true`). Both caps are additive labels — findings are not suppressed, they are paginated per scope. Consolidates Q6-1 + Q6-13. Pairs with `scanned_build_artifacts_present` — vendor bundles drive the budget overrun. Doctrine: pagination is honest; suppression is not.
- [ ] **Q6-PLAN-TOTALFINDINGS-VIOLATIONS-VS-NOTES** `plan.totalFindings: 24772` buries `notes: 1079` under the same counter (violations: 23693). Composite headline count per `ai-first-consumer.md` §composite-headline-counts. Either rename to `plan.totalFindingsComposite` with a clearer breakdown elsewhere, or split into `plan.violationsCount` + `plan.notesCount` at the same level (matches the shape `ScanSubset` already carries post-V1-COMP-BOOTSTRAP-SCAN-TOTAL). The `--format agent` CLI summary already breaks out "N violations + M notes"; align the MCP plan counter with the same split.

### v0.3.0 — accepted (P1 — rule/finder correctness)

- [ ] **Q6-CHECKLIST-IFRAME-FINDER-JS-STRING-LITERALS** Review candidates for 1.2.x / 4.1.2 iframe criteria include `jquery.fancybox.pack.js:4` with snippet `"<iframe id=\"fancybox-frame{rnd}\"..."`. The iframe-review finder line-scans `.js` files as if the literal string contents were HTML elements on the page. Restrict the finder to HTML-family inputs: `.html` / `.htm` / `.jsx` / `.tsx` / `.vue` / `.svelte` (the files ra11y actually parses as DOM). JS string literals containing `<iframe>` are not rendered iframes — they are library code building DOM at runtime; the agent reads the surrounding file to verify. Mirrors the TSX-text-corpus fix in V1-FP-IMAGES-OF-TEXT-SVG-DATA-URI's shape: the text-matching pass must run on DOM-origin text only, not on string-literal payloads inside source code.
- [ ] **Q6-MEANINGFUL-SEQUENCE-UTILITY-CLASSES** 1.3.2 review candidates fire on every `.order-0` .. `.order-13` utility-class selector in `bootstrap.css` / authored compiled CSS — 14 selectors × ~40 templates = ~560 candidates per scan, all pointing at the same Bootstrap utility definitions rather than at any consumer. Finder must operate on HTML/JSX *consumers* that apply `class="order-N"`, not on the CSS selector definition site. Tighten: lift the 1.3.2 finder from the CSS-selector scope to the HTML/JSX usage scope (match `class="...order-N..."` / `className="...order-N..."` on elements); silence on the CSS definition when no consumer in the scan applies the class. When the finder *does* hit a CSS-only definition, enrich the reason with "note: utility-class definition; verify consumers apply this class intentionally." Doctrine: the finder's premise (sequence-dependent visual order) is real; the current location (CSS definition) is wrong.
- [x] **Q6-RULE-HREF-JAVASCRIPT-VOID** New `navigation/href-javascript-void` rule. 120 occurrences of `<a href="javascript:void(0)">` across 10 sampled files; zero findings today. Covers `href="javascript:void(0)"`, `href="javascript:;"`, and `href="#"` when no click-handler delegation is detectable (e.g. no matching jQuery `.on("click")` selector, no event delegation parent). All three forms are `<button>` roles masquerading as links; AT announces "link," keyboard Tab lands with no navigation. Satisfies wcag22:4.1.2 + 2.1.1. Mechanical fix: "change `<a href=\"javascript:void(0)\">` to `<button type=\"button\">` — this control mutates state, not navigates." Fixtures under `tests/fixtures/{good,bad}/href-javascript-void/`. (0b942fc, turn 30)
- [x] **Q6-LINK-ICON-ONLY-ACCNAME** `src/rules/navigation/link-descriptive-text.ts` does not flag `<a><i class="fa fa-twitter"></i></a>` — the anchor has no text, no `aria-label`, no labeling child. Extend the rule to compute the full accessible name: ignore presentational descendants (`<i class="fa-*">`, `<span class="material-icons">`, decorative `<img role="presentation">`), then flag when the computed name is empty. Pairs with Q5-RULE-ICON-FONT-ARIA-HIDDEN — that rule enriches labeled parents with icon children; this rule flags unlabeled parents with only icon children. Satisfies wcag22:2.4.4 + 4.1.2. (2ce309f+8932f04+481c86f, turn 31)
- [ ] **Q6-AUTODETECT-WRAPPERS-LEAKS-JS-IDENTIFIER-NOISE** `autoDetectWrappers: true` on the website-templates scan returned `opaqueCustomComponentNames: ["AG.y","B","H.length","J","J.length","Math.abs"]`. The extractor walks `.js` files grepping for identifier expressions; single-letter variables, member-access expressions (`J.length`), and built-ins (`Math.abs`) are not component names. Restrict extraction to JSX/TSX element tag positions only — `<ComponentName ... />` / `<ComponentName>...</ComponentName>` where the tag name starts with an uppercase letter and is a bare identifier (no `.`, no single-char unless explicitly allowlisted). Skip the `.js` extension entirely for JSX-tag harvesting (plain `.js` has no JSX syntax). Paired with Q3-OPAQUE-COMPONENTS-TOP-EMPTY-DISHONEST under opaque-component quality, but the extraction-source filter is the root-cause fix.
- [ ] **Q6-PLACEHOLDER-IN-FIX-SUGGESTION** Minor refinement on `forms/labels-required`: when the rule fires AND the offending `<input>` has a `placeholder="..."` attribute, include the placeholder verbatim in the fix suggestion ("`<label for=\"email\">Email address</label>` — placeholder text `\"Enter email\"` may carry the intent; verify it's accurate before using as label copy"). Pairs with Q3-RULE-PLACEHOLDER-AS-LABEL (new rule for placeholder-as-only-label antipattern) — this item is the fix-suggestion enrichment for the already-firing `labels-required` rule when the placeholder is additive evidence for the correct label text. Extends V1-FIX-NON-EMPTY-LABEL's placeholder path.

### v0.3.0 — accepted (P2 — build-artifact rollup + signal routing)

- [ ] **Q6-SCANNED-BUILD-ARTIFACTS-GROUP-BY-BASENAME** Companion to Q4-SCANNED-BUILD-ARTIFACTS-REASON. When `scannedBuildArtifacts` surfaces 301 entries (bootstrap.css: 45, font-awesome.css: 80, etc.), the flat list is unactionable — the agent sees 301 individual paths and cannot tell that one `exclude` glob covers most of them. Group by basename with a suggested glob: `scannedBuildArtifacts: { grouped: [{ basename, count, pathHint, suggestedGlob }], ungrouped: [paths] }`. `suggestedGlob` is inline-ready for `propose_config`. Additive shape over the existing flat `scannedBuildArtifacts: [paths]` array (consumers that iterate the flat list still work; the grouped form is a sibling field). Pairs with Q-SHARED-PROPOSE-CONFIG-RELATIVE-PATHS — collapsing 301 paths to ~5 glob patterns.
- [ ] **Q6-MOTION-PAUSE-STOP-PER-FILE-AGGREGATION** `motion/pause-stop-hide` produced 8,940 findings (36% of the scan's total) against vendor `bootstrap.css` alone. Every CSS animation keyframe / transition declaration is technically a 2.2.2 concern; vendor bundles carry hundreds. When a single rule fires >N× in one file AND that file is in `scannedBuildArtifacts`, add `perRuleCoverage[ruleId].vendorConcentration?: { file, count }` pointing at the densest vendor file with its total count. Findings still emit individually under `files[]` per surface-don't-suppress (pairs with V1-NOISE-RULE-PER-FILE-ROLLUP, which rolls up the same signal for non-vendor files above a concentration threshold). Additive label — zero information loss, one more field for the agent's triage. The V1 item handled the non-vendor case; this item extends the same shape to vendor bundles explicitly so the agent sees "most of this rule's fire is one vendor file" without having to sort findings themselves.
- [ ] **Q6-NEXTSTEP-AVOIDS-VENDOR-CSS** `nextStepStructured.file` pointed at `bootstrap.css:365` — vendor CSS is rarely the file the agent should act on first. When the top-ranked violation or next-step target sits in `scannedBuildArtifacts`, rank the suggestion toward a non-vendor file with a finding of the same rule family (if one exists in the same response); fall back to the vendor file only when every violation sits in vendor code. Doctrine-adjacent to "surface-don't-suppress": the vendor finding stays in `files[]`, but the agent's first action should land where they can actually edit. Reason-text when this rerouting applies: "note: highest-severity finding in this scan is in `vendor/bootstrap.css`; next-step points at `authored/site.css` where a same-family fix is applicable." Additive — when no rerouting applies, `nextStepStructured` behaves exactly as today.

### Deeper-pass additions (2026-04-21, 10x)

Source: 15-scan deeper pass against the website-templates repo as part of the four-repo 10x field test. 3,973 files / 24,772 findings / 20.5 s internal scan time (see "Needs judgment" section below for the performance-budget call). Items here are residue after the cross-repo Q-SHARED items and the existing Q6 caps (Q6-BUDGET-UNDER-VENDOR-NOISE et al.) are addressed.

- [ ] **Q6-RULE-ARIA-REF-MISMATCH-WIDEN-ATTRS** Website-templates `coffee-shop/index.html:162` has `aria-labelledby="myModalLabel"` pointing at an id that does not exist anywhere in the document. Q3-RULE-LABELLEDBY-TARGET (closed turn 24) covers `aria-labelledby` / `aria-describedby` / `aria-controls` but did NOT fire here — first investigate whether this is rule-registry drift (see Q-SHARED-RULES-EVALUATED-SSOT); once the registry is fixed and the rule fires, widen coverage to all eight ARIA ID-ref attributes: `aria-labelledby`, `aria-describedby`, `aria-controls`, `aria-owns`, `aria-activedescendant`, `aria-errormessage`, `aria-flowto`, `aria-details`. Deterministic, zero-false-positive check. Fixtures under `tests/fixtures/{good,bad}/aria-ref-widen/` covering each of the eight attributes. WCAG 4.1.2.
- [ ] **Q6-RULE-INVALID-ID-SHAPE** Website-templates `agile-agency/index.html:60` has `id="#top"` — the `#` is an invalid id-start character per HTML-living-standard (ids may not contain whitespace but are otherwise any non-empty string; however, `id="#top"` is the author confusing id syntax with href-fragment syntax). `parsing/duplicate-id` only fires on duplicates, not on malformed shapes. Add a companion `parsing/invalid-id-shape` rule (or extend duplicate-id): flag ids that start with `#`, contain whitespace, are empty-string, or collide with well-known CSS-escape-required characters when used unescaped. Fixtures under `tests/fixtures/{good,bad}/invalid-id-shape/`. WCAG 4.1.1 (parsing) — note: WCAG 2.2 removed 4.1.1 as a testable SC; keep the rule under `parsing/` as best-practice with `satisfies: [wcag21:4.1.1]` (parsing-failure in WCAG 2.1) only, not 2.2.
- [ ] **Q6-ICON-FONT-LABELED-PARENT-REGRESSION** Q5-RULE-ICON-FONT-ARIA-HIDDEN (closed turn 29) should fire on `<button><i class="fa-bars"></i></button>` patterns in coffee-shop (lines 59, 82, 86), but the rule emitted zero findings. Investigate: (a) registry drift (covered by Q-SHARED-RULES-EVALUATED-SSOT); (b) the rule's "labeled parent" predicate may not cover `<button>` wrapping `<i>` without any other child text when the parent's accessible name comes solely from an `aria-label` rather than a text node — re-read the rule and add `tests/fixtures/real-world/bulk-template-icon-button/` asserting the rule fires. If (b), widen the labeled-parent signal to include `aria-label` / `aria-labelledby` / `aria-describedby` on the parent. WCAG 4.1.2 + 1.3.1.
- [ ] **Q6-ANCHOR-ICON-ONLY-NAME-REGRESSION** Q6-LINK-ICON-ONLY-ACCNAME (closed turn 31) should fire on `<a href="#home"><img src="logo.png" alt="..."/></a>` patterns in coffee-shop (`index.html:35`), but the deeper scan emitted `media/alt-text-missing` at the `<img>` level only — not a secondary finding at the anchor level for missing accessible name. When the only child of an `<a>` is a decorative/presentational element (icon `<i>`, `<img role="presentation">`, `<svg aria-hidden>`) AND the anchor has no `aria-label`, the anchor has no accessible name regardless of the child's alt. Investigate: registry drift (see Q-SHARED-RULES-EVALUATED-SSOT) vs rule predicate gap (img with non-empty alt counted as giving the anchor a name, masking the distinction between "anchor name = alt" vs "anchor unnamed"). WCAG 2.4.4 + 4.1.2.
- [ ] **Q6-MINIFIED-FILE-SNIPPET-COLUMN-ENRICHMENT** Website-templates cited `bootstrap.min.js:6` and `col 1` eight times for setTimeout review candidates on a 6-line minified file — every match points at the same line/col because the entire file is one line. The agent cannot locate the specific call site. When the cited file matches minified heuristics (single line > 1000 chars, `.min.` infix, sibling sourcemap present), enrich `reason` with (a) byte-column offset within the single line (so `col 47201` disambiguates multiple matches), (b) a pointer to the unminified sibling if one exists (`authored: "src/bootstrap.js"` when `src/bootstrap.js` exists alongside `dist/bootstrap.min.js`), and (c) the ~80-char context window around the match. Additive reason enrichment — findings remain in `files[]`; the agent gets a usable pointer. Doctrine: "The tool's job is to point" — a line:col pointer to a minified file that resolves to "the whole file" is a dishonest pointer.
- [ ] **Q6-SCSS-PARSING-DISCLOSURE** Website-templates `filesByExtension` reports 584 `.scss` files, but `perRuleCoverage.filesEvaluated` numbers don't cleanly account for them (`1637 ≠ 1053 + 584`). Response does not disclose whether `.scss` is parsed as CSS (via `extensionMatches` alias) or skipped. Add `analysisCoverage.sassParsingMode: "css-alias" | "skipped" | "native"` (or equivalent) so the agent knows whether a `.scss`-heavy repo was fully inspected by CSS-family rules. Alternative: enrich `filesByExtension` entries with a `parsedAs` field (`"css"` / `"skipped"` / `"native"`). Doctrine: "Ambiguous field shapes are dishonest" — a filesByExtension count that doesn't match the per-rule file count forces the agent to guess coverage.

### Considered and rejected (per CLAUDE.md §1)

- **Auto-exclude `bootstrap.css` / `font-awesome.css` / `jquery*.js` via `DEFAULT_EXCLUDED_PATTERNS`** → rejected per §1 "Default-exclude globs are suppression too." Vendor-bundle findings ARE real findings — contrast in bootstrap.css is a real contrast issue on the rendered page, just not one the consumer edits. The existing `scannedBuildArtifacts` label + proposed rollups (Q6-SCANNED-BUILD-ARTIFACTS-GROUP-BY-BASENAME + Q6-MOTION-PAUSE-STOP-PER-FILE-AGGREGATION) give the agent the signal without discarding the findings. Pairing with the build-artifact-aware next-step rerouting (Q6-NEXTSTEP-AVOIDS-VENDOR-CSS) closes the "agent lands on unactionable file" failure mode.
- **Default-downgrade vendor findings to `severity: "note"`** → rejected per §1 "Don't downgrade priority to hide things." Severity is for sorting; a downgrade hides real contrast violations from agents that filter by severity. The `scannedBuildArtifacts` membership is the honest label — agents decide triage; the tool does not pre-decide severity.

---

## Needs judgment — 2026-04-21 field test

Source: aggregated four-repo 10x deeper field test (bootstrap, jekyll, 50projects50days, website-templates). Items below are open strategy/doctrine questions surfaced during triage that need a user-level decision before the corresponding backlog work can land. They are not tracked as dispatchable work — they are blockers on specific items above. Once answered, each resolves into an existing or new backlog item.

- **J-1 (pause-stop-hide signal-vs-noise strategy).** `motion/pause-stop-hide` produced 8,940 findings on website-templates and 55 on 50projects50days. Most are user-triggered hover/focus transitions, not the auto-playing motion WCAG 2.2.2 targets. Current reason text doesn't distinguish. Is this (a) over-surface that needs reason-text enrichment only ("auto-playing?" / "user-triggered?" signal, let the agent decide), (b) spec-correctness matter where the rule should narrow to auto-playing-only, (c) both? Surface-don't-suppress argues (a); narrowing risks silent-miss on genuine 2.2.2 auto-playing violations. Blocks Q6-MOTION-PAUSE-STOP-PER-FILE-AGGREGATION scoping decision.

- **J-2 (vendor CSS noise, primary mitigation choice).** Website-templates: 69% of 24,772 findings = 3 rules firing on vendor CSS. Doctrine forbids default-excluding vendor paths. Primary mitigation options: (a) fix the `scannedBuildArtifacts` classifier so agents can filter by it with confidence, (b) add a per-file finding cap (Q6-BUDGET-UNDER-VENDOR-NOISE), (c) both, (d) something else (e.g., surface rule-per-file concentration aggressively and trust the agent to route). Currently Q6-BUDGET and Q6-SCANNED-BUILD-ARTIFACTS-GROUP-BY-BASENAME are both open; the question is whether they land independently or one supersedes the other. Blocks prioritization of both items.

- **J-3 (plan.totalFindings: rename vs remove).** Every `scan_project` ships a top-level `plan.totalFindings: N` that sums violations + notes. Doctrine forbids composite headline counts. Agents may already depend on the field for aggregation. Remove entirely (breaking — v1.0 gate) or rename to `plan.totalFindingsComposite` with explicit "sum of violations+notes, prefer the split" prose (additive, non-breaking)? Blocks Q6-PLAN-TOTALFINDINGS-VIOLATIONS-VS-NOTES implementation shape.

- **J-4 (rulesEvaluated definition: loaded-vs-fired).** `rulesEvaluated` disagrees cross-surface on the same cwd: `scan_project: 53` vs `propose_config: 54` vs `list_rules: 54` (50projects50days). Likely cause: JSX/TSX-only rule counted as "evaluated on 0 applicable files" differs by tool. Which is correct: "evaluated" = "the rule ran on 0 applicable files" (54) or "the rule didn't run because no applicable files" (53)? Either is honest; they must agree cross-surface. Blocks Q-SHARED-RULES-EVALUATED-SSOT shape.

- **J-5 (likelyIrrelevant bucket: parse-coverage caveat).** `likelyIrrelevant` for 1.2.x is honest on website-templates ("no video/audio in scanned files"). On Jekyll it's technically honest but 307 `.md`/`.markdown` files were never parsed — under the doctrine rule that bucket labels must be provable, is "no video in scanned files" sufficient when the scan missed the content layer? Options: (a) enrich `likelyIrrelevant` reason with the skipped-content caveat automatically (covered by Q4-RELEVANCE-REASON-PARSE-COVERAGE-CAVEAT), (b) consider it the agent's job to reconcile `likelyIrrelevant` with `extensions_skipped_no_parser` separately, (c) suppress `likelyIrrelevant` entirely when content extensions are skipped. Blocks Q4-RELEVANCE-REASON-PARSE-COVERAGE-CAVEAT scoping.

- **J-6 (performance budget at 4k-file vendor-heavy scale).** Website-templates full-scan ran 20.5 s internal (~32 s wall-clock) against 3,973 files — 1.7× over linear extrapolation of the 1000-file 3 s budget. Options: (a) add a new bench scenario at 3-4k vendor-heavy files with an explicit budget, (b) document a "precommit use not supported beyond N files" scope limit, (c) default to `--changed`-only discovery on repos > 1000 files with a `largeRepoHint` warning, (d) accept the slowdown and document it. Blocks performance-track decisions + CLAUDE.md §11 table update.

- **J-7 (static-analysis limit vs cross-file handler resolution).** `<a href="#">` / `<li>`-acting-as-tab / `<div>`-acting-as-button patterns with click handlers in separate `.js` files (50projects50days + website-templates common). `navigation/href-javascript-void` (registry-drift notwithstanding) catches raw `href="#"`; the broader "custom interactive widget with JS-only handler" class requires cross-file resolution. Read sibling `.js` files to resolve handlers (expensive, cross-file complexity, risks wrong-identifier matches) OR accept as an inherent static-analysis limitation and document it in `limitations`? Doctrine: "don't duplicate capability the agent already has" argues for the latter — the agent can Read+Grep across files. Blocks any rule/finder that would widen predicates to cross-file handler presence.

- **J-8 (scan_file error discrimination split).** Bootstrap confirmed `scan_file` conflates `file-unsupported` (extension not in set) with file-not-found. Split options: (a) `file-not-found` / `unsupported-extension` / `file-unreadable` (three distinct codes), (b) fold not-found into a single `file-unreadable` (two codes: "can't read this file" + "can't parse this extension"), (c) keep `file-unsupported` as umbrella with a `details.reason` sub-discriminator. Doctrine prefers explicit codes (a); (b) is more compact; (c) keeps the existing discriminator stable. Blocks Q-SHARED-SCAN-FILE-ERROR-DISCRIMINATION implementation shape.

---

## Track C — Conformance-claim gaps

Owner: main session + `spec-researcher` + `doc-writer`. Staged; do not dispatch alongside active tracks. ra11y today produces *signals* (findings, review candidates, coverage hints). It does not produce a *claim*. Track C closes the four gaps that separate the two — attestation ledger, runtime-evidence ingest, process-level scope, conformance statement. **No new rules**; the track widens what an agent can defensibly say after using the tool.

Sequencing (cross-gap, soft): attestations first (unlocks evidence semantics everywhere else), then axe ingest (wide-reach runtime coverage), then process scope, then conformance capstone. Within each gap items are ordered by the ADR-then-foundation-then-surface pattern.

### v0.3.0 — attestation ledger (foundation)

- [x] **C-ATTEST-ADR** ADR 0011 (evidence as first-class primitive) + ADR 0012 (rule-scoped attestations) cover this slot.
- [x] **C-ATTEST-STORE** `.ra11y/attestations.jsonl` append-only store with validated envelope landed in `src/config/attestation-store.ts`.
- [x] **C-ATTEST-TOOL** `attest` MCP tool landed; accepts `criterionId`, `ruleIds?`, `verdict`, `scope`, `location?`, `reason`, `by`; stamps commit hash; rejects bare verdicts.
- [x] **C-ATTEST-LIST** `list_attestations` MCP tool landed with git-backed staleness probe + `staleProbeUnavailable` signal for non-repo environments.
- [x] **C-ATTEST-PRUNE** `ra11y attestations prune [--dry-run]` subcommand landed; pure function + injectable `fileExists` predicate.
- [x] **C-ATTEST-CHECKLIST** Checklist items spread per-criterion `attestation?: { verdict, stale?, evidence, by? }` with conditional-spread discipline.
- [x] **C-SUPPRESSION-ATTEST** Bare pragmas emit `verdict: "pending"` attestations; reasoned pragmas emit `"pass"`; `suppression/no-reason` finder retained as complementary signal.

### Rejected — runtime evidence bridge (2026-04-19)

A prior plan proposed ingesting vendor runtime results (axe-core JSON → normalized shape → ledger) as the path to close runtime-only WCAG criteria. Rejected: vendor-specific ingest adapters duplicate capability agents already have via their own test harnesses, and the tool's job is to point at the source — not at another tool's output. The ingest shape is also fragile to vendor schema drift, and the normalized layer re-buckets findings in ways the reading agent can't re-audit. Agents that run runtime checks bridge their results through the existing `attest` tool — the reason text is the evidence, and the attestation ledger is the durable, vendor-neutral channel.

Items struck: C-RUNTIME-ADR, C-AXE-SCHEMA, C-AXE-INGEST, C-RUNTIME-MAP. C-COVERAGE-MERGE is re-scoped below.

- [x] **C-COVERAGE-MERGE** Coverage report integrates attestations; per-criterion row carries optional `attested: { verdict, stale? }`. Manual criteria with fresh pass/n-a attestations count toward covered. Stale/pending/fail don't flip static verdicts.

### v0.3.0 — process-level scope

- [x] **C-PROCESS-ADR** ADR 0016 (process-level scope) landed.
- [x] **C-PROCESS-CONFIG** `processes: [{ name, pages }]` config primitive + schema validation + loader wiring.
- [x] **C-PROCESS-SCAN** `scan_process` MCP tool landed; orchestrates per-page scan with process-level frame.
- [x] **C-PROCESS-CONSISTENCY** `consistent-navigation` finder honors declared processes; heuristic path retained as fallback with a reason-text nudge to declare the primitive.
- [x] **C-PROCESS-IDENT** `consistent-identification` finder for wcag22:3.2.4; emits zero candidates when no processes config (honest needs-config gap).

### v1.0.0 — conformance capstone

- [x] **C-PROFILE-WCAG21AA** `ConformanceProfile` primitive + 8 built-ins (wcag21-a/aa, wcag22-a/aa/aaa, section508, en301549, ada); `--profile` CLI flag with explicit-override warning. Profile→reports scope filtering wired in turn 8.
- [x] **C-CONFORM-ADR** ADR 0017 (conformance-statement-output) landed.
- [x] **C-CONFORM-STATEMENT** `conformance_statement` MCP tool + `src/reports/conformance.ts` landed; refuses when coverage is incomplete; blocker list cites missing criteria.
- [x] **C-CONFORM-SIGN** SHA-256 digest over (commit hash + attestations + in-scope criteria + config fingerprint); canonical JSON; `verifyConformanceBundle` for drift detection.
- [x] **C-CONFORM-DOC** `docs/conformance.md` end-to-end agent guide: scan → checklist → attest → coverage → signed statement.

### Dependencies + interactions with Track S

Track S speculative tools (`verdict-candidate`, `draft-vpat-narrative`, `resolve-component`) become concretely useful once **C-ATTEST-TOOL** lands — their output is evidence the agent writes to the ledger. Track C resolves Track S's "pick 1–2 speculative tools" decision (ADR 0005 §Follow-up): `verdict-candidate` is the natural first pick because its output IS an attestation entry. Promote Track S `[!]` items to `[ ]` once C-ATTEST-TOOL is merged.

### Considered and rejected

- **Embed axe-core as a runtime dep.** Rejected per **§3 invariant 1** (zero runtime deps). Parse axe's JSON output; don't pull axe-core into the install. Agents already run axe in their test harness; they hand us the JSON path.
- **In-tool headless browser (Playwright / Puppeteer).** Rejected per zero-deps + MCP-first-consumer framing — the agent already runs the browser in its test harness. We ingest; we don't drive.
- **Blanket "ra11y verifies conformance" marketing.** Rejected — the conformance statement will cite evidence sources; ra11y is the aggregator, the attestation is the agent's (or human's) word, and the signed bundle is what survives audit. Honesty > reach.

---

## Track V — v1.0.0 readiness

Owner: main session + `doc-writer` + `fixture-curator` + `test-author` + specialists as indicated per item. Source: 2026-04-19 three-agent gap analysis (rule coverage vs WCAG 2.1/2.2 A+AA, MCP shape discipline, backlog + semver + CI infra). The conformance capstone in Track C is shipped; these are the remaining gates for the "find all violations" bar.

No item here proposes new rule detection logic — Track V is about honesty surfaces, decision closure, and coverage verification. Items that WOULD add detection get logged as Track R or Track C items.

### v1.0.0 — certification claim blockers (final sweep, 2026-04-19)

Source: two independent pre-ship subagent sweeps (spec-researcher normative angle + general-purpose tooling/reproducibility angle). Both flagged overlapping blockers that would sink a real WCAG/VPAT/Section 508 certification submission. Each item has a concrete file:line anchor; together these are the gate to `/release 1.0.0`. Dispatch in parallel where non-overlapping; B1 and B6 block on B2's shape work.

- [x] **V1-CERT-STATEMENT-SHAPE** `src/reports/conformance.ts:101-128` `ConformanceStatement` is missing four of six W3C WCAG §5.3.1 required claim fields: `guidelinesTitle`, `guidelinesVersion`, `guidelinesUri`, `scope.files`, `technologiesReliedUpon`, `technologiesNotReliedUpon`. ADR 0017 designed these but implementation diverged. Accept ADR 0017 as the source of truth; reshape the type + `renderConformanceMarkdown` (line 308) to emit all six; update ADR 0017 status from "Proposed" → "Accepted". Blocker for B1 + B3 + B6. Medium. [579be54, e9732df — all six WCAG §5.3.1 required fields now populated on `ConformanceStatement`; `scope.commitHash` and `scope.configSnapshot` conditional-spread per the AI-first consumer model (no `""` or `{}` sentinel); `technologiesReliedUpon` defaults to `["HTML", "CSS", "ECMAScript", "WAI-ARIA"]`; ADR 0017 promoted Proposed → Accepted with an Implementation section documenting the present-when-meaningful deviations; MCP tool forwards `files[].filePath` + session-config snapshot; `renderConformanceMarkdown` now emits date / guidelines / conformance-level / scope / technologies sections in a WCAG §5.3.1 layout; nine new unit tests cover the shape.]
- [x] **V1-CERT-STATEMENT-SIGN** [bb79517, 15469b9, 5e434a1, b428896 — SignatureInput extended with per-file SHA-256 manifest (relative path keys), toolVersion, and a full configFingerprint covering standards/level/profile/rules/nativeWrappers/processes/additionalPaths; each new axis has its own ConformanceVerificationReason drift code (file-manifest-mismatch, tool-version-mismatch, config-profile-mismatch, config-rules-mismatch, config-native-wrappers-mismatch, config-processes-mismatch, config-additional-paths-mismatch); MCP tool computes commitHash via headSha (skip-with-warning `non_git_repo_signature_omitted` outside git), collects durable attestations, builds the file manifest over parsed sources, assembles the config fingerprint from LoadedConfig (falling back to session values on config-load failure), passes VERSION as toolVersion; 16 new tests cover reordering invariance, per-axis drift routing, full round-trip, minimal round-trip, and MCP-level signed-scan round-trip + tamper detection.]
- [x] **V1-CERT-DOCS-DRIFT** `docs/conformance.md:197-223` advertises `scope.files`, `scope.commitHash`, `scope.configSnapshot`, `technologiesReliedUpon`, `evidence.findingIds`, `evidence.attestationIds` — fields absent from the emitted type. After V1-CERT-STATEMENT-SHAPE reshapes the type, reconcile the doc so it matches what the tool emits. Also fix the minor cross-reference error in `docs/kb/criteria/wcag22/2.4.12-focus-not-obscured-enhanced.md` frontmatter (references `wcag21:2.4.12` which doesn't exist; WCAG 2.1 AA stops at 2.4.9). Blocks on V1-CERT-STATEMENT-SHAPE. Small. [d60e6cd — removed evidence.findingIds/attestationIds (not on type), replaced date/conformanceLevel with generatedAt/profile, added full field reference table with always-present/conditional notes, fixed end-to-end transcript, corrected 2.4.12 frontmatter to reference wcag22:2.4.11 instead of nonexistent wcag21:2.4.12.]
- [x] **V1-CERT-STALE-ATTEST** [40cb5b9, bf03eac — builder now consults `createGitStalenessProbe(cwd)` against every attested-pass/n/a source, flipping criteria to `stale-attestation` blockers (carrying `staleAttestedAt`) when the probe says the scope intersects the changed-files set; indeterminate probe results surface as a top-level `stale_probe_unavailable` warning (response-level, conditional-spread) rather than fabricating staleness; added `missing-process-config` blocker for the ADR-0016 process-level criteria (2.4.5 / 3.2.3 / 3.2.4) when `processes` is undeclared — structural gap surfaces before downstream `no-evidence` hides the root cause; MCP tool hands the probe + loaded config's `processes` through, merges the new warning with `non_git_repo_signature_omitted`, and routes the `nextStep` hint on the new reasons; builder split into `resolveScope` / `evaluateCriteria` / `classifyOneCriterion` / `probeAttestedSource` to stay under the complexity budget; 11 new unit tests cover both variants + indeterminate probe + markdown rendering + priority of missing-process-config over staleness.]
- [x] **V1-CERT-ATTEST-INTEGRITY** ADR 0020 (`docs/adr/0020-attestation-ledger-integrity.md`) + implementation already shipped. Option (b) adopted: `ra11y attestations verify` subcommand uses git as the trust root — flags `removed-since-head`, `backdated-attestation`, `future-attestation-before-commit`; `uncommitted` is informational. Pure helper `verifyAttestationIntegrity` in `src/config/attestation-store.ts:275`; CLI wrapper in `src/cli/commands/attestations.ts`; unit tests at `tests/unit/cli/commands/attestations-verify.test.ts`. Backlog flip was lost in the 2026-04-19 history rewrite; restored here.
- [x] **V1-CERT-CLI-PATH** [6f01a0c + edd0a69 + 508465a + 86be52b + df3ba43 — `ra11y attest <criterionId>` wraps `appendAttestation` with mandatory `--reason`, criterion + rule-ID validation, and scope↔`--location` coupling; `ra11y conformance` wraps `buildConformanceStatement` + `renderConformanceMarkdown` (Markdown default, JSON via `--output`), signing the bundle when inside a git repo and emitting `non_git_repo_signature_omitted` otherwise; `ra11y conformance --verify <bundle.json>` wraps `verifyConformanceBundle`, reconstructing the SignatureInput from the current tree and reporting the drift reason on failure. `parseCliArgs` refactored to dispatch flag-commands + subcommand keywords via declarative arrays (kept under cognitive-complexity cap). Both paths wire through `ExitCode.OK` / `ExitCode.USER_ERROR`. 10 attest + 11 conformance integration tests cover argv → disk round-trip, validation branches, and verify drift detection. `docs/cli.md` documents both subcommands with flags, exit codes, and examples.]
- [x] **V1-CERT-VERSION-CONST** (926a839 — `src/version.ts` reads `package.json.version` at module load via `node:fs.readFileSync` + `new URL("../package.json", import.meta.url)`; all 8 call sites (sarif TOOL_VERSION, vpat EVALUATOR, baseline ra11yVersion, help VERSION, json ra11y.version, terminal banner, agent TOOL_VERSION, mcp SERVER_VERSION) import from it; formatters snapshot test updated to compare against the derived constant; new `tests/unit/version.test.ts` guards drift.)
- [x] **V1-CERT-VPAT-25** (c4fdf5d + 3a43276 + 95a9104 + 9d94e3c — MCP `vpat` tool parity added 2026-04-19: mirrors `conformance_statement` shape — structured `standards[].entries[]` per-criterion, `nextStep` + `nextStepStructured` pair routing to `scan_project` / `attest` / `conformance_statement`, honest warnings (`scanned_zero_files`, `no_config_found`, `product_metadata_placeholders_in_use` when empty product metadata surfaces `<Product Name>` / `<Product Version>`), `format: "markdown"` attaches `markdownRendering`, `format: "json"` omits it. Seven-scenario integration test covers happy path, missing-param error envelope, media-absent Not Applicable, markdown/json split, empty-directory warning, placeholder emission, and `additionalPaths` round-trip. docs/mcp/tool-reference.md documents the new tool.) (c4fdf5d — VPAT builder retargeted at the ITI VPAT 2.5 Rev template, expanding to "VPAT 2.5 Rev INT" whenever EN 301 549 is in scope. `VpatReport` now carries `product: VpatProductMetadata` (Name / Version required, Contact Email / Organization, Evaluation Methods, Notes optional) with `<Product Name>` / `<Product Version>` template placeholders when unset so gaps stay visible to the VPAT reader; `buildVpatReport` accepts a `VpatBuildOptions` bag while retaining the legacy `(generatedAt, candidates)` signature. `buildEntry` now invokes `detectApplicability` via `isLikelyIrrelevant` + `irrelevanceReason`, so media SCs (1.2.1-1.2.9, 1.4.2) emit "Not Applicable" with the deterministic absence reason instead of "Not Evaluated"; `runVpat` threads applicability through from the parsed file set. Remarks rewritten for a procurement audience — each row states its verdict, SC local ID + title + level, and failing rule IDs; no references to terminal output, JSON report, or --checklist. Applicable-chapters block lists Chapters 3-7 with scope notes (ra11y is a Chapter 5 source). Product metadata surfaces via `RA11Y_VPAT_PRODUCT_NAME`, `_PRODUCT_VERSION`, `_CONTACT_EMAIL`, `_CONTACT_ORGANIZATION`, `_EVALUATION_METHODS`, `_NOTES` env vars. Documented in `docs/cli.md`; `docs/certification/vpat-mapping.md` now points at `src/mcp/manual-applicability.ts` as the presence-check source of truth.)
- [x] **V1-CERT-E2E-TEST** [a9f2419 — tests/integration/conformance-e2e.test.ts exercises scan → attest → conformance_statement → verifyConformanceBundle against real on-disk git repos with deterministic GIT_AUTHOR_DATE / GIT_COMMITTER_DATE stamps. Five scenarios: (a) happy-path signed bundle round-trips valid; (b) post-attestation commit flips attested-pass criteria into reason: "stale-attestation" blockers carrying staleAttestedAt; (c) mutating a scanned file post-sign returns file-manifest-mismatch from verifyConformanceBundle; (d1) rewriting a committed attestation line surfaces removed-since-head from `ra11y attestations verify` (ADR 0020) with non-zero exit; (d2) forging a post-sign attestation flips the signature verifier to attestation-set-mismatch. No mocks; git shelled out via node:child_process.]

### v1.0.0 — coverage honesty

- [x] **V1-COVERAGE-MATRIX** Authoritative matrix published at `docs/kb/standards/coverage.md` (92f6070 generator + c7d41fa doc + check-kb-drift extension). 252 unique criteria across 5 standards: 116 rule · 105 finder · 25 attestation-only · 6 gap. All six gaps are AAA-only (animation-from-interactions, link-purpose-link-only, target-size AAA) where the AA equivalent already ships. Drift check green.
- [x] **V1-CRITERION-KB** Full acceptance reached (26158e7 + fca13f1). Attestation-only half: 25 KB entries landed (ca35661) — 13 wcag22 + 10 wcag21 + 1 section508 + 1 en301549. Finder-backed half: 7 new KB entries added via V1-KB-FINDER-GUIDANCE (26158e7); wcag22:3.1.3 was already covered. GUIDANCE_BY_ID fully removed from src/reports/checklist.ts (fca13f1). Every criterion in the coverage matrix now has a KB file.
- [x] **V1-FIX-AUDIT** Acceptance reached (2026-04-19 run): all 11 generic-fix-text rules shipped context-aware builders. Resolved via: V1-FIX-DOC-LANG (746ecee), V1-FIX-DOC-TITLE (1a639b3), V1-FIX-TABINDEX-POSITIVE (7a24546), V1-FIX-NON-EMPTY-LABEL (0da9c7e), V1-FIX-VIDEO-CAPTIONS (03f9896), V1-FIX-LINK-NO-HREF (aeb5ddc), V1-FIX-DUPLICATE-ID (4e0d212), V1-FIX-BUTTON-NAME (cf53e34), V1-FIX-EMPTY-HEADING (b374e12), V1-FIX-LANDMARK-MAIN (06035ee), V1-FIX-TABLE-HEADERS (cb70741). Audit doc (`docs/kb/rules/fix-suggestion-audit.md`) distribution now 52 context-aware · 0 generic · 0 caveat-only · 0 needs-review.
- [x] **V1-FIX-RANK-DOC** `docs/kb/patterns/suggest-fix-ranking.md` published (92f6070). Documents the two axes (fixClass on Violation vs kind on suggest_fix response), batch-routing table for all four fixClass values with per-value rule inventories, primary/alternatives ranking contract, edit vs editCandidate distinction, verifyCommand post-fix workflow. `confidenceRationale?: string` decision: **defer** — existing `primary.label`, `explanation`, `caveat` already carry the signal in human-readable form.

### v1.0.0 — surface discoverability

- [x] **V1-PROMPT-LINK** Prompt templates wired into `nextStep` prose across scan / scan_project / review_candidates / checklist (52f97aa). `ra11y/triage` surfaces on clean+manual and on review_candidates; `ra11y/audit` on clean+zero-manual and on checklist-zero-actionable; `ra11y/fix` on violations-with-fix and all-mechanical; `ra11y/vpat-narrative` on checklist-zero-actionable alongside audit. `nextStepStructured` left as-is per ADR 0010 alignment.

### v1.0.0 — real-world fixture push

Track F shipped 10 fixtures, all guarding scanner infrastructure (wrappers, templates, TSX generics). Zero fixtures exercise rule-territory behavior in production-like shape. Each item below lands one sanitized fixture under `tests/fixtures/real-world/<case>/` with `source/` + `assertions.ts`, following the ADR 0006 harness. Dispatch in parallel — independent fixtures.

- [x] **V1-FIXTURE-FORMS** `tests/fixtures/real-world/forms-validation/` landed (7694cdb). Asserts labels-required, fieldset-legend, required-indicator-missing, autocomplete-missing rules; validation-timing, error-identification, server-error-untied finders. Zero-parse-errors across ContactForm.tsx + PhoneField.tsx.
- [x] **V1-FIXTURE-NAV** `tests/fixtures/real-world/nav-landmarks/` landed (f97ccbf). Asserts landmark-main, skip-link, link-descriptive-text rules + consistent-navigation finder across two divergent route files (home.tsx + about.tsx) and shell.html.
- [x] **V1-FIXTURE-DIALOG** `tests/fixtures/real-world/dialog-modal/` landed (2be0974). Three ConfirmDialog variants + page.tsx; asserts no-keyboard-trap finder fires on `role="dialog"` regardless of labeling; valid ARIA role passes aria/invalid-role; backdrop onClick exempted by keyboard/handler-missing. Drift: no rule today emits `couldBeWrongBecause` on dialogs — documented in fixture README.
- [x] **V1-FIXTURE-TABLE** `tests/fixtures/real-world/data-tables/` landed (aaa94ed). Three variants — SimpleTable (td-only, violation), ScopedHeaderTable (clean), ComplexHeaderTable (clean). Finding: `semantics/table-headers` satisfies wcag22:1.3.1; no table-specific review finder exists (Track R gap).
- [x] **V1-FIXTURE-LIVE-REGION** `tests/fixtures/real-world/live-region-status/` landed (aaa94ed bundle). StatusBanner + ToastRegion + Page; asserts aria/live-region-valid fires on invalid tokens, role/aria-live contradictions, hidden regions. Drift: `couldBeWrongBecause: "runtime_behavior_required"` is NOT emitted by this rule today — documented in fixture README; that enrichment would be a Track R item.
- [x] **V1-FIXTURE-ATTEST** Both fixtures + doc landed (d43358f attest-axe + 1750692 attest-lighthouse + bbcd71a `docs/kb/patterns/bridging-runtime-a11y.md`). Synthetic axe-core JSON + Lighthouse JSON, bridge scripts showing the agent-side mapping to `attest` calls, assertions verifying the parse path. No vendor-specific runtime ingest adapters in src/ — the bridge is a documented PATTERN, not code ra11y runs. (d43358f's commit subject mis-titles the fixture work as "prompt templates"; content is correct.)

### v1.0.0 — deferred-decision closure

Each item closes one deferred ADR or semver-major decision. Acceptance for each: an ADR marked Accepted (or Superseded / Rejected) + the corresponding backlog item flipped.

- [x] **V1-RULE-RENAME-DECIDE** Deferred past v1.0 via ADR 0018 (93e0207). Current rule names stay stable; revisit on user complaints / further catalog-overlap signals.
- [x] **V1-COV-CHECK-MERGE** Deferred past v1.0 via ADR 0018 (93e0207). ADR 0010 stays; coverage + checklist remain separate tools with cross-linked `nextStep` pairs. Revisit on concrete round-trip-cost evidence.
- [x] **V1-PARSER-SUBPKG-DECIDE** Deferred past v1.0 via ADR 0018 (93e0207). ADR 0003 stays; core stays monolithic. Revisit on peer-resolution edge cases or a second non-trivial parser.
- [x] **V1-SAMPLING-TOOL-PICK** Deferred past v1.0 via ADR 0018 (93e0207). Track S `[!]` items remain blocked; `verdict-candidate` is the pre-picked first promotion when a sampling-capable host surfaces a concrete workflow.

### v1.0.0 — plugin API integrity (2026-04-20)

Source: 2026-04-20 architectural audit — two independent Explore passes converged on the same finding, verified by grep. `grep -rln "BUILTIN_RULES\|BUILTIN_STANDARDS" src/` returns 33 files; 31 of those are non-barrel leaks past the existing `src/engine/registry/{criteria,rules,standards}.ts` primitives. The leak means CLAUDE.md §3.8 is aspirational, not enforced, and ADR 0019's frozen `defineRule`/`defineStandard`/`defineCandidateFinder` exports function as type-inference helpers only — user-authored rules have no wiring path to `list_rules`, `suggest_fix`, or any scan. ADR 0022 has the full design and 7-commit migration plan. Dispatch sequential within this subsection: stages 1 → 2 → {3, 5} → 4 → 6 → 7, with 3/4/5 parallelizable across agents once 2 lands. Every stage is additive and reversible until V1-REGISTRY-LINT.

- [x] **V1-REGISTRY-AGGREGATE** (68e1240) New `src/engine/registry/registry.ts` wraps the existing `CriteriaRegistry` + `RulesRegistry` + `StandardsRegistry` primitives via a `Registry` class with `rules`, `standards`, `finders`, `criteria` arrays + `findRule` / `findStandard` / `findCriterion` / `rulesForCriterion` accessors. `rulesForCriterion` closes over the reciprocal equivalence graph — returns `readonly Rule[]` in input-position order so call-site swaps stay deterministic. `createBuiltinRegistry()` is the designated single-call site into `BUILTIN_RULES` / `BUILTIN_STANDARDS` / `BUILTIN_CANDIDATE_FINDERS`; no other `src/engine/**` file references those barrels. 20 unit tests cover accessor round-trips, transitive equivalence (both directions), input-order determinism, and a shipped-rule smoke (`media/alt-text-missing` surfaces under `wcag22:1.1.1` + declared equivalents). Pure addition — zero call-site change; stages 2–7 migrate consumers.
- [x] **V1-REGISTRY-SESSION** (06d6ea5) `McpSession` constructor now instantiates `createBuiltinRegistry()` and exposes it as `readonly registry: Registry`. Zero call-site migration — existing `BUILTIN_*` imports inside `src/mcp/**` untouched so stage 3+ can land incrementally. Three new tests in `tests/unit/mcp/session.test.ts` pin the construction contract + the `findRule("contrast/minimum")` / unknown-ID round-trip.
- [x] **V1-REGISTRY-HELPERS** (9ebfbce) Scope mis-sized. Four of the six call sites sit inside exported module-level helpers with no `session` parameter — `findRule` / `findStandard` / `firstUnknownStandard` / `satisfyingRulesForCriterion`. Dropping the `BUILTIN_*` barrels requires adding a `session` parameter to each, which cascades to ~9 single-line call-site updates across eight stage-4 files (`src/mcp/tools.ts`, `tool-suggest-fix.ts`, `tool-coverage.ts`, `tool-checklist.ts`, `tool-vpat.ts`, `tool-review-candidates.ts`, `tool-conformance-statement.ts`, `tool-attest.ts`). The `Registry` aggregate surface is sufficient — this is a helper-signature constraint, not an accessor gap. Two resolutions: (1) merge stages 3 + 4a into one commit that threads `session` through callers (net diff ≈20 lines outside `tools-helpers.ts`); (2) split stage 3 into 3a (migrate the two session-bearing sites, keep the four helpers reaching `BUILTIN_*` locally) + 3b (drop barrels alongside helper-signature change alongside stage 4). Prefer (1) on 2026-04-21 re-plan. Blocked per the `/continue` scope-lock; re-dispatch with widened scope.
- [x] **V1-REGISTRY-MCP-TOOLS** (6d6d6a8 — 6 atomic commits dd47e19..5d94912) Migrate `src/mcp/tools.ts` + `src/mcp/tool-*.ts` (16 files) to read through `session.registry`. Dispatch in parallel groups by family: {scan + scan-process + review-candidates}, {suggest-fix + apply-fix + apply-fix-internals}, {baseline + propose-baseline + attest}, {coverage + checklist + vpat + conformance-statement + explain-standard + list-suppressions + propose-config}, plus {completions + scan-project-budget + wrappers-meta}. Each group is its own commit under the 400-LOC budget. Blocks on V1-REGISTRY-HELPERS. Owner: main session + parallel agent dispatch. Medium (≈4 commits).
- [x] **V1-REGISTRY-CLI** Landed across three commits (1a57d4b scan-family + 734490d explain-family + 1c46a6c list-family). All eleven `src/cli/commands/*.ts` hits and `src/cli/run.ts` now route through a `registry: Registry = createBuiltinRegistry()` parameter; `attest` dropped its local `criterionExists` / `satisfyingRulesForCriterion` helpers in favor of `registry.findCriterion` + `registry.rulesForCriterion` (equivalence-closure semantics now match MCP attest). Invariant: `rg "BUILTIN_(RULES|STANDARDS|CANDIDATE_FINDERS)" src/cli/` returns zero hits. `V1-REGISTRY-LINT` can now converge once `V1-REGISTRY-MCP-TOOLS` (blocked on `V1-REGISTRY-HELPERS`) lands.
- [x] **V1-REGISTRY-LINT** (f6c133a) New `scripts/check-builtins-scope.ts` modeled on `scripts/check-network-isolation.ts`. Forbids importing `BUILTIN_RULES` / `BUILTIN_STANDARDS` / `BUILTIN_CANDIDATE_FINDERS` outside `src/engine/registry/**`, the three barrel files themselves, and `tests/**`. Added to the `scripts/verify.ts` sequence (CLAUDE.md §4). Acceptance: `bun scripts/check-builtins-scope.ts` green. Blocks on V1-REGISTRY-MCP-TOOLS + V1-REGISTRY-CLI converging (every non-barrel call site must be migrated first). This is the commit that locks the invariant. Owner: main session. Small.
- [x] **V1-REGISTRY-PLUGIN-WIRE** `src/api/plugin.ts` helpers remain identity functions at the type level (ADR 0019 freeze), but `createRegistry({ rules, standards, finders })` now composes builtins with user-authored additions. `McpSession` + CLI bootstrap accept a registry override so a future `ra11y.config.ts` hook or CLI `--plugin` flag can thread one in. Working example under `examples/plugin-rule/` showing a user rule visible to `list_rules` and producing violations through `scan_project`. Integration test covering end-to-end plugin path. ADR 0022 flipped Proposed → Accepted. (b546831, turn 23)

### v1.0.0 — MCP response-assembly seam (2026-04-20)

Source: 2026-04-20 architectural audit (Explore-agent survey of `src/mcp/**` + grep verification). Same class of leak as V1-REGISTRY-AGGREGATE but one layer up: the AI-first consumer doctrine in `docs/kb/architecture/ai-first-consumer.md` (conditional-spread on zero counts, `warnings` on zero-output success, split composite headline counters, present-when-meaningful optional fields, honest `fixesByClass` tally) is currently enforced in 24 separate `src/mcp/tool-*.ts` files by convention + the path-scoped rule at `.claude/rules/mcp-response-shapes.md`. Verified counts on 2026-04-20:

| measurement                                                                 | count |
|-----------------------------------------------------------------------------|------:|
| `src/mcp/tool-*.ts` files                                                   | 24    |
| Tool files routing through `runScanAndFormat` (the existing near-assembler) | **2** (`tool-scan-project.ts`, `tool-scan-diff.ts`) |
| Tool files hand-rolling `meta: {` blocks directly                           | 10    |
| Tool files calling `buildAgentFinding` or composing `AgentFinding` manually | 8+    |
| `scan` + `scan_file` tool definitions                                       | inline in `src/mcp/tools.ts` (625 LOC mixing schemas + handlers) |
| `src/mcp/tools-helpers.ts` current size                                     | 856 LOC (hosts `runScanAndFormat` + fraying) |
| `scripts/check-response-nullability.ts` allowlist entries                   | 1 (catches a narrow slice; doesn't catch field-presence drift) |

The partial assembler `src/mcp/scan-assembly.ts` (`buildScanPlan` / `buildScanMeta` / `buildAnalysisCoverage` / `suppressionsMetaBlock` / `wrappersMetaBlock`) is already the right shape, but it lives underneath a non-authoritative wrapper (`runScanAndFormat`) and is only consumed by 2 of 24 tools. Every new scan-family field (future sampling outputs, new fix-class lanes, new `warnings` codes, new honest-counter splits) currently requires touching ≤24 sites with no CI gate forcing synchrony — silent cross-surface drift is the waiting failure mode, and the invariants called out in `ai-first-consumer.md` ("`scan` says 21, `checklist` says 4") are exactly what this seam prevents.

Why v1.0 blocker: the MCP response shape is part of the public contract v1.0 freezes (ADR 0019 + the migration table in `docs/migrations/0.1-to-0.2.md`). Freezing the shape before codifying the single-path invariant means third-party tooling pattern-matches on hand-rolled examples; changing the pattern later is a semver-breaking event. This is structurally identical to the Registry aggregate work (ADR 0022) — an internal seam that must be in place before the surface it backs becomes immutable.

Design captured in ADR 0024 (Proposed, 2026-04-20). Dispatch sequential within this subsection: stage 1 (assembler) → 2 (handler extract from `tools.ts`) → {3, 4, 5 parallel} → 6 (lint) → 7 (handler-size budget). Stages 3-5 are parallelizable across agents on separate worktrees (no shared files — each touches a disjoint set of `tool-*.ts`). Every stage is additive and reversible until V1-RESPONSE-LINT. Golden snapshots under `tests/snapshots/mcp/**` must stay byte-identical through every refactor commit — any intentional shape change splits into a separate `feat(mcp):` commit with rationale.

- [x] **V1-RESPONSE-ASSEMBLER** (e095fc9) New `src/mcp/response-assembler.ts` exporting `assembleScanFamilyResponse(input: ScanFamilyResponseInput, options?: ScanFamilyResponseOptions): ScanFamilyResponse`. `input` carries `violations, perRuleCoverage, reviewCandidates, discovery, wrappers, config, durationMs, toolName, scopeHint`; `options` carries `hoistReferenceGuide, includeReviewCandidates, tokenBudget`. Internally orchestrates the existing extracted helpers: `buildScanPlan` + `buildScanMeta` + `buildAnalysisCoverage` + `suppressionsMetaBlock` + `wrappersMetaBlock` + `buildAgentFinding` (per violation) + `buildReferenceGuide` + `dedupReviewCandidates` + `tokenBudget` clamping + `warnings` code emission (`scanned_zero_files`, `root_source_defaulted`, `no_config_found`, `tailwind_detected_css_undercounted`, `template_files_parsed_as_literal`). Pure addition — zero call-site change; stages 3-5 migrate consumers. Unit tests mirror the existing assembly-helper tests: honest-counter-split cases, conditional-spread fixesByClass, empty-scan warnings emission, reference-guide dedup, per-scope variants. Blocks on nothing. Owner: `feature-dev:code-architect`. Medium. ≤400 LOC net.
- [x] **V1-RESPONSE-SCAN-CORE** (d32bdb8..7e7a523, turn 20, 2026-04-21) `scan` + `scan_file` migrated through `assembleScanFamilyResponse`; handlers extracted to `src/mcp/tool-scan.ts` + `src/mcp/tool-scan-file.ts`; `src/mcp/scan-collect.ts` added as `runScanAndCollect` seam helper. `scan_process` deferred — per-page `ScanResult[]` shape is structurally different and warrants separate analysis; split into V1-RESPONSE-SCAN-PROCESS below. 4 commits: 1b273e1 64c5f21 ac7f760 61be322.
- [x] **V1-RESPONSE-SCAN-PROCESS** (turn 25, 2026-04-21) Decision: `scan_process` stays OUTSIDE `assembleScanFamilyResponse`. Three converging reasons: (1) `perPageResults: ScanResult[]` is the documented primitive (ADR 0016) future process-level finders cross-index — collapsing N pages into one `files[]` discards the per-page boundary; (2) `meta` is process-level (`processName`, `resolvedProcess`, `missingPages`) not scan-level (`activeNativeWrappers`, `rulesEvaluated`, …) — merging would violate "ambiguous field shapes are dishonest"; (3) looping the assembler per-page would force composing a dishonest composite plan/meta across pages. Rationale lives as a durable docblock at the top of `src/mcp/tool-scan-process.ts` so V1-RESPONSE-LINT's allowlist entry can cite the file by path. No code changes to the handler; no snapshot drift.
- [x] **V1-RESPONSE-SCAN-DERIVATIVE** (7007df8..7b2f49a, turn 21, 2026-04-21) Migrated `checklist` + `coverage` + `conformance_statement` handlers through `buildDerivativeScanWarnings` assembler seam. `buildDerivativeScanWarnings` exported from `src/mcp/response-assembler.ts`; cross-tool invariant test guards `scan.totalFindings === checklist.automatedFindings + checklist.manualReviewRequired` drift class. No snapshot drift (derivative tools use bespoke shapes). 2 commits.
- [!] **V1-RESPONSE-FIX-FAMILY** Original framing rejected by a re-dispatch 2026-04-21 (classification_mismatch): `baseline.check` emits `files` + bespoke `meta` (NO `plan`); `apply_fix` emits bespoke `meta` + `before`/`after` violation slices (NO `files`, NO `plan`); `suggest_fix` emits NO findings-tree at all (single-violation lookup → primary/alternative fix paths). Routing through `assembleScanFamilyResponse` as-is would ADD a `plan` block and widen `meta` — directly contradicting the byte-identical-snapshots acceptance gate. Two paths forward: **(a)** narrow scope to "share the assembler's `groupByFile` helper for `baseline.check` only" (the only file-grouping site) and drop `apply_fix` + `suggest_fix` from scope — preserves byte-identity. **(b)** accept the shape changes (add `plan`, widen `meta`) and re-cut acceptance onto named-field invariants — this is the ADR 0024 spirit (unify scan-family response shape) but a BREAKING output change that needs a migration note. Pick one before re-dispatch. Blocks on decision. Owner: user / architect decision.
- [ ] **V1-RESPONSE-LINT** New `scripts/check-response-assembly.ts` modeled on `scripts/check-network-isolation.ts` + `scripts/check-response-nullability.ts`. Statically forbids scan-family tool files from defining object literals with a `plan` key or a `meta: {` block without routing through `assembleScanFamilyResponse`. Allowlist: `src/mcp/response-assembler.ts`, `src/mcp/scan-assembly.ts` (the underlying helpers), non-scan-family tools (`attest`, `suppress`, `list_rules`, `list_suppressions`, `explain_*`, `bootstrap`, `detect_native_wrappers`, `wrapper_introspect`, `sessionConfigure`), and `tests/**`. Added to the `scripts/verify.ts` sequence (CLAUDE.md §4). Acceptance: `bun scripts/check-response-assembly.ts` green; dry-run synthetic test (temporarily hand-roll a `plan:` block in `tool-coverage.ts`, confirm the check fails, revert) passes before landing. This is the commit that locks the invariant. Blocks on V1-RESPONSE-SCAN-CORE + V1-RESPONSE-SCAN-DERIVATIVE + V1-RESPONSE-FIX-FAMILY converging. Owner: main session. Small.
- [ ] **V1-RESPONSE-HANDLER-BUDGET** Enforce `src/mcp/tool-*.ts` ≤ 150 LOC after migration via an extension to `scripts/check-limits.ts`. Handlers that still exceed the budget are doing work that belongs in the assembler — keep extracting until they fit (or carry an inline `// ra11y-limits-exempt: <reason>` pragma, matching the existing limits-check escape). Acceptance: `bun scripts/check-limits.ts` green; aggregate `src/mcp/tool-*.ts` LOC drops from 8,485 to ≤ 3,600 (≈150 × 24). Blocks on V1-RESPONSE-LINT. Owner: main session. Small. Final commit of the subsection — flips ADR 0024 status Proposed → Accepted.

### v1.0.0 — release hygiene

- [x] **V1-BENCH-BASELINE** v1.0 performance baseline locked (8e8a564). All four scenarios 79-95% under budget: cold 41ms/200ms, 10-files 5.2ms/100ms, 100-files 28.5ms/500ms, 1000-files 272ms/3000ms. Headroom watch: cold-start grows with CLI module graph; 1000-file throughput scales linearly with rule count.
- [x] **V1-API-STABILITY-AUDIT** v1.0 public surface frozen in ADR 0019. Audit confirmed `check-tsdoc.ts` passes on baseline; no `@experimental` / `@internal` tags on exported names (the one `@internal` marker in the tree sits on a non-exported symbol in `src/output/agent-response/build-finding.ts`); no accidental re-exports of internal types (types not listed in `src/types/index.ts` are deep-import-only and remain free to evolve). ADR enumerates every exported name across both entry points: 8 names from `@ra11y/core` (`Criterion`, `ReportData`, `ScanResult`, `Severity`, `Standard`, `Violation`, `scan`, `ScanOptions`) and 7 from `@ra11y/core/plugin` (`defineRule`, `defineStandard`, `defineFormatter`, `defineConfig`, `defineCandidateFinder`, `FormatterFn`, `Formatter`). Escape-hatch `experimental` column documented but empty at v1.0.
- [~] **V1-CHANGELOG-V1** Draft `## [1.0.0]` entry landed (1d784b7). Date placeholder `YYYY-MM-DD` stays until user says ship. Sections populated: Breaking Changes (exit-code freeze), Added (13 sub-groups), Changed (9 items), Deprecated (2 aliases), Fixed (19 items), Deferred (the four ADR 0018 items). New empty `## [Unreleased]` sits at top. Ready for `/release 1.0.0` when the user triggers.

### v1.0.0 — test coverage gates (second-pass scan, 2026-04-19)

Source: coverage sweep during second-pass gap scan — MCP surface and several CLI commands have severe coverage deficits that contradict the "find all violations" bar. Every item here lands tests against committed behavior; no behavioral changes.

- [x] **V1-TEST-MCP-SERVER** `src/mcp/server.ts` raised from 8.28% → 96.96% lines / 97.22% funcs (eb6fd73 + cb0da5a + d42c6fb). 36 new in-process dispatch tests via a new shared `tests/helpers/mcp-harness.ts` since integration tests spawn subprocesses and don't cross instrumentation. Remaining uncovered lines are defensive/unreachable branches.
- [x] **V1-TEST-MCP-SCAN-DIFF** `src/mcp/tool-scan-diff.ts` raised from 18.12% → 96.90% lines / 95% funcs (c3efd95 + 5d0c99b). 21 in-process dispatch tests cover baseline mode, hunks mode, structured errors, symlink normalization, stable `findingId`/`groupKey`. Remaining uncovered branches are impossible-in-practice race cases.
- [x] **V1-TEST-MCP-APPLY-FIX** `src/mcp/tool-apply-fix.ts` raised from 43.71% → 100% lines / 95.49% branches (120569d + 6f36e30). 26 new unit tests drive the handler in-process (existing integration tests drive the subprocess, which instrumentation doesn't cross). Internals file `tool-apply-fix-internals.ts` rode along to 96.55%/95.91%.
- [x] **V1-TEST-CLI-CMDS** Six CLI commands lifted from 0% to 100% function coverage (b827dc7 + 30644ea + 4342de4). `certification`, `checklist`, `coverage`, `vpat`, `doctor`, `init` all exercised via in-process handler invocations against tmpdir fixtures; `doctor` line-coverage at 87.93% (remaining branches require mocking `process.version`), all others at 100%.
- [x] **V1-TEST-MCP-GLUE** All five target modules raised to 100% lines / 100% branches (235db1a + 28f2224 + 7740a44; logging.test.ts file swept into 5d0c99b by parallel agent). 59 new tests across completions, deprecation-warning, outbound, logging, ra11y-kb resources. Every module confirmed reachable from `src/mcp/server.ts`.
- [x] **V1-TEST-CONFIG-SCHEMA** `src/config/schema.ts` raised from 6.94% → 100% lines / 100% funcs (4dfd9d6). 42 new tests in `tests/unit/config/schema.test.ts` covering accepted shapes, rejection paths, error-path indices, and output normalization.

### v1.0.0 — CI + release pipeline gates (second-pass scan, 2026-04-19)

- [x] **V1-CI-WINDOWS** `windows-latest` added to verify matrix (b0b07cc). Fail-fast stays false so an OS hiccup doesn't cancel the other lanes.
- [x] **V1-RELEASE-VERIFY-FULL** Already satisfied — `.github/workflows/release.yml:43` runs `bun run verify` (full, not trimmed). The second-pass audit that flagged this was against stale state.
- [x] **V1-CI-SARIF-UPLOAD** Self-scan job added to ci.yml (6315a06). Runs `ra11y scan src/ --format sarif --fail-on never` and uploads to GitHub Security via `github/codeql-action/upload-sarif@v3`. Makes ra11y the reference implementation of its own CI integration.
- [x] **V1-CI-DEP-REVIEW** `.github/workflows/dependency-review.yml` added (914d4f1). GitHub's dependency-review action runs on every PR alongside the in-house zero-dep guard; `fail-on-severity: moderate`, `comment-summary-in-pr: on-failure`.
- [x] **V1-EXIT-CODES** Canonical `ExitCode.{OK,VIOLATIONS,USER_ERROR,NEW_VIOLATIONS}` enum landed in `src/cli/exit-codes.ts` (8e8b0ef). Every command now returns via the enum; `ra11y --help` renders the legend from `EXIT_CODE_LEGEND`; `docs/cli.md` cross-links `docs/errors.md`. Frozen at v1.0.
- [x] **V1-MCP-ERRORS-DOC** `docs/errors.md` created (5bfcf34). 26 MCP structured errors indexed (24 from the central union + 2 suppress-local cast-through codes documented as intentional), 4 CLI exit codes indexed. Canonical error + exit-code reference.

### v1.0.0 — code-quality polish (second-pass scan, 2026-04-19)

- [x] **V1-TYPE-ESCAPE** Redundant `as unknown` cast dropped (7979ef8); `coerceAttestationRecord` already accepts `unknown`, the cast widened a narrower type to no purpose.
- [x] **V1-ERROR-MSG-QUALITY** All three throw sites rewritten (7979ef8): sampling.ts now names the actual type returned + links the MCP spec + suggests the host decline the capability; attestation-store.ts enumerates the specific failed field + shows a concrete example record; tool-suggest-fix-internals.ts is marked as an internal invariant with a file-an-issue hint.
- [x] **V1-COMMENT-DRIFT** Hardcoded count removed (456158d); the comment now describes the contents (excludes 4.1.1, includes the nine new 2.2 criteria) rather than asserting a drifting count.
- [x] **V1-README-PLUGIN-LINK** Per-plugin-kind authoring guides now linked inline in the Plugin API section (3379e03). The prior scan's claim of a broken `docs/plugin-authoring.md` link did not match the current README — the three `docs/plugins/authoring-a-*.md` guides are now cross-linked alongside the examples + architecture deep-dive.
- [x] **V1-DOCS-CLI-COMMANDS** Per-command reference section added to `docs/cli.md` (192cf0d). Each command gets a subsection with purpose + representative invocation + key non-global flags; `baseline prune` + `attestations prune` subcommands now documented; `--profile` flag added; usage synopsis updated to reflect command dispatch.
- [x] **V1-MIGRATION-0.2-TO-1.0** `docs/migrations/0.2-to-1.0.md` landed (2f9af2e). TL;DR: v1.0 is additive for most consumers. 4 deferred items (from ADR 0018) + 1 breaking change (exit-code freeze) + 2 soft deprecations (`configure` → `sessionConfigure`, `filePath` → `file`). Cross-linked from CHANGELOG.md + docs/getting-started.md.

### v1.0.0 — detection gaps (third-pass scan, 2026-04-19)

Source: "find all violations" bar re-raised 2026-04-19. Four-agent gap audit against WCAG 2.2 A+AA surface. Each item widens detection on an existing rule or adds a small new primitive — none reshape the engine. Gaps are where a real violation on common production patterns walks past our rules undetected. Dispatch in parallel — independent files.

- [x] **V1-DETECT-ALT-WIDEN** (db325c9, a174d62) `src/rules/media/alt-text-missing.ts` today only walks `<img>` and `<input type="image">` (line 13 header). Real violation patterns miss: (a) SVG `<image src="…">` inside `<svg>`; (b) `role="img"` on arbitrary elements (`<div role="img">`, `<span role="img">`); (c) `<canvas>` with no fallback text + no aria-label/aria-labelledby. Widen element walk + apply existing `hasAccessibleName*` logic. Satisfies wcag22:1.1.1, wcag21:1.1.1. Fix scope: rule extension; add three walkers with the same accessible-name check that runs today on `<img>`. Tests: `tests/fixtures/{good,bad}/alt-text-missing/` gains svg-image, role-img-div, canvas-no-fallback cases. Effort: small.
- [x] **V1-DETECT-BUTTON-NAME-SVG** (2af5caf) `src/rules/semantics/button-name.ts:130` uses `htmlTextContent` which walks HtmlElement + HtmlText but does NOT descend into SVG `<text>` / `<title>` children. `<button><svg><title>Close</title></svg></button>` computes as name-less today but is accessibly named in every major AT. Same blind spot for `<a>`/interactive controls. Extend `hasAccessibleNameHtml`/`hasAccessibleNameJsx` to walk SVG `<title>` + `<text>` as direct children of interactive controls. Satisfies wcag22:4.1.2. Effort: small.
- [x] **V1-DETECT-BUTTON-NAME-IMAGE-INPUT** (2af5caf) `src/rules/semantics/button-name.ts:155-173` filters `["button", "submit", "reset"]` only; `<input type="image" src=... title="Submit">` is never checked. Alt-text rule covers the `alt` path but not the `title`-as-name path — so a `title`-named image input reports no violation correctly, but a name-less one also reports no violation (false negative). Add `type="image"` to the button-name filter and recognize `alt` OR `title` OR `aria-label` as valid name sources. Satisfies wcag22:4.1.2, wcag22:1.1.1. Effort: small.
- [x] **V1-DETECT-LABELS-CONTENTEDITABLE** (e8ca208) `src/rules/forms/labels-required.ts:38` `LABELABLE_TAGS = ["input","select","textarea"]`. `<div contenteditable="true">` acts as a form control (Slack-style composers, Notion-style editors) and has the same labeling requirement under WCAG 1.3.2 + 3.3.2 but is not checked. Extend the element set to include `contenteditable="true"` (not `false` or `inherit`). Effort: small.
- [x] **V1-DETECT-CONTRAST-BG-IMAGE** (431cd1b, 6c49c2b) `src/rules/contrast/_shared.ts:114` resolves only `background-color` and `background` (color-only shorthand). Text over `background-image: url(…)` / `background: linear-gradient(…)` currently skips contrast evaluation silently — no violation, no `couldBeWrongBecause` signal. Two paths: (a) emit `couldBeWrongBecause: "background_image_unresolvable"` as additive signal for affected declarations (honest surfacing, no false positives); (b) defer luminance heuristic for gradients to a follow-up. Path (a) is the v1.0 scope. Effort: small.

### v1.0.0 — shape-honesty sweep (third-pass scan, 2026-04-19)

Source: same audit. Verified file:line hits on shape-honesty violations that survived Q/Q2/Q2R2. Every item below is a null-sentinel that CLAUDE.md §1 "Ambiguous field shapes are dishonest" calls out directly — forcing agents to disambiguate "field unavailable" vs "field empty."

- [x] **V1-SHAPE-NULL-SWEEP** (6b6349c) Replace 9 response-field null/empty sentinels with conditional-spread:
    - `src/mcp/tool-explain-standard.ts:65` `publisher: standard.publisher ?? null`
    - `src/mcp/tool-explain-standard.ts:66` `url: standard.url ?? null`
    - `src/mcp/tool-explain-standard.ts:68` `levelFilterApplied: level ?? null`
    - `src/mcp/tool-explain-standard.ts:75` `url: c.url ?? null`
    - `src/mcp/tool-explain-standard.ts:76` `equivalentTo: c.equivalentTo ?? []`
    - `src/mcp/rule-catalog.ts:77` `normativeQuote: rule.docs.normativeQuote ?? null`
    - `src/mcp/tool-review-candidates.ts:177` `title: criterion?.title ?? null`
    - `src/mcp/tool-review-candidates.ts:178` `level: criterion?.level ?? null`
    - `src/mcp/next-step.ts:147` `singleFilePath: options.singleFilePath ?? null`
  Exclude `detect-wrappers-core.ts:107` (`definitionFile`) — intentional per Q2-WRAPPATH ("`null` is emitted, not omitted — the attempt is meaningful"). Use conditional-spread: `...(x ? { field: x } : {})`. Audit `tests/integration/mcp-*.test.ts` for any snapshot assertions relying on the null — those are the contract tests to update. Effort: small.
- [x] **V1-SHAPE-NULL-AUDIT** Lint check shipped at `scripts/check-response-nullability.ts` (472e18b) and wired into `scripts/verify.ts` as `response-nullability`. Uses the TypeScript compiler API (not regex) to walk ancestors for `textResult(...)` / `errorResult(...)` calls — input-param normalization passes structurally, no carve-out needed. Catches `?? null`, `?? []`, `?? ""`, `?? 0`, `?? \`\``. Pre-loaded allowlist for the Q2-WRAPPATH `definitionFile` exception. 10 unit tests at `tests/unit/scripts/check-response-nullability.test.ts`. Backlog flip was lost in the 2026-04-19 history rewrite; restored here.

### v1.0.0 — fix-suggestion context-aware sweep (third-pass scan, 2026-04-19)

Source: V1-FIX-AUDIT (`[~]` above) enumerates 11 rules with generic fix text. Acceptance requires zero generic rows before v1.0 tag. Each item is sized for one ≤400-LOC commit per CLAUDE.md §9. Plans derived from fix-suggestion-audit.md. Dispatch in parallel — independent rules.

- [x] **V1-FIX-DOC-LANG** `src/rules/document/lang-attribute.ts:71-72` — inspect `<meta charset>` + sibling pages' lang values to propose a concrete lang candidate ("Add lang=\"en\" (detected from <meta charset=\"utf-8\">); if the page is French, use lang=\"fr\""). Small. [746ecee]
- [x] **V1-FIX-DOC-TITLE** `src/rules/document/page-titled.ts:68-86` — extract `<h1>` text + `<meta name="description">` as title candidate ("`<title>Contact Information — Acme</title>` — candidate from existing `<h1>` text"). Small. [1a639b3 — three-branch ladder (h1-text / meta-description truncated / fallback) inlines source line numbers and strips trailing punctuation from the meta candidate; 5 new tests cover all branches + empty-title re-use; audit row flipped to `context-aware`.]
- [x] **V1-FIX-TABINDEX-POSITIVE** `src/rules/focus/tabindex-positive.ts:210-223` — inline the offending tabindex value + branch on tag/role ("Remove tabindex=\"5\" from `<div>`; if focus is needed, use tabindex=\"0\" with role=\"button\""). Small. [7a24546 — three-branch ladder (natively focusable / contenteditable host / non-focusable fallback) inlines tabindex="N" + tag; keeps declared role or proposes role="button"; 9 new tests cover all branches in HTML+JSX; audit row flipped in b66f00e's drift-resolution.]
- [x] **V1-FIX-NON-EMPTY-LABEL** `src/rules/forms/non-empty-label.ts:96-129` — read `for`/`htmlFor` + locate matching id ("Label references id=\"email\"; move the email-input description text inside the label"). Small. [0da9c7e — four-branch ladder (no-target / matching control + hint / matching control no hint / unmatched id) inlines `for="id"` (or `htmlFor="id"`), `<tag type="…">` descriptor, control line number, and a candidate derived from `type` (13 semantic types → canonical labels), `placeholder` verbatim, or humanized `name` (`firstName` → "First name", `phone_number` → "Phone number"); 11 new unit tests cover all branches in HTML+JSX; audit row flipped to `context-aware`.]
- [x] **V1-FIX-VIDEO-CAPTIONS** `src/rules/media/video-captions-missing.ts:116-131` — extract video src basename + document lang for a concrete VTT path and srclang ("Add `<track kind=\"captions\" src=\"launch.vtt\" srclang=\"en\">` inside `<video src=\"launch.mp4\">`"). Small. [03f9896 — ladder: video `src` → strip-dir + swap-ext into concrete `src="<stem>.vtt"`; no `src` but `<source>` child → first-source basename; neither → `captions.vtt` with child-of-`<video>` phrasing. `srclang` inlined from nearest `<html lang="…">` (HTML root or JSX root-layout shape); fallback `en`. Common primary subtags humanize into `label="English captions"` etc. Captions-vs-subtitles guidance included in every suggestion.]
- [x] **V1-FIX-LINK-NO-HREF** `src/rules/navigation/link-no-href.ts:115-117` — inspect onClick expression body for navigation vs mutation keywords (navigate/route/history vs toggle/set/open) and branch the fix. Needs small new helper in `src/engine/ast-helpers.ts` for JSX expression keyword probe. Medium. [d76dcfb + aeb5ddc — new `describeJsxExpressionIntent(src: string | null)` helper is text-level and deterministic: navigation identifiers (navigate/router/history/redirect/location/goto/useNavigate/useRouter), `window.location`/`document.location`, and member-call `.push(` / `.replace(` / `.go(`; mutation identifiers (toggle/open/close/show/hide/dispatch/mutate/delete/save/submit/setState), React setter convention `set[A-Z]…(`, and camelCase verb-prefix form (`toggleMenu`, `openDialog`, `closeModal`); navigation wins ties; unknown honest-fallback keeps weak-evidence cases out of heuristic suppression. Rule now emits a three-branch suggestion: navigation → real `href="..."` + `preventDefault()` interception, mutation → `<button type="button">`, unknown → "decide the intent" prose. 8 new rule tests + 16 new helper tests across the three branches and the navigation-tie rule.]
- [x] **V1-FIX-DUPLICATE-ID** `src/rules/parsing/duplicate-id.ts:64-66` — inline `first.loc` line/tag + propose a numeric suffix candidate ("id=\"mainContent\" duplicates `<main>` at line 12 — use id=\"mainContent2\" or remove"). Small. [4e0d212 — pre-scans the document so the proposed suffix is guaranteed free; trailing-digit run stripped before incrementing (`section2`→`section3`, `item42`→`item43`); names aria-labelledby / aria-describedby / aria-controls / label[for] / `href="#id"` as the silent-first-match reference hooks; six new unit tests covering all branches.]
- [x] **V1-FIX-BUTTON-NAME** `src/rules/semantics/button-name.ts:304-319, 223-239` — detect icon-only child (SVG/img) and extract svg `<title>` / img alt as concrete aria-label candidate ("Button contains only `<svg><title>Close</title></svg>`; add aria-label=\"Close\""). Pairs with V1-DETECT-BUTTON-NAME-SVG — ship after that extension lands so the fix text cites the same helpers. Medium. [cf53e34 — `buildIconAwareSuggestion` branches on host subject + icon-child shell: SVG-icon branch names the `<svg><title>…</title></svg>` fix alongside `aria-label`; `<img>`-icon branch inlines a Title-Cased filename subject as both `alt=` and `aria-label=` candidates; `<input type="image">` branch never says "wraps an <img>" and cites HTML §4.10.5.1.18 that value is not a name source; empty `<input type="submit/button/reset">` branch proposes `value="…"` / `aria-label="…"` (void element, no nested children); empty `<button>` / `<a>` / `[role="button"]` keeps the visible-text example anchored on the actual host tag; 8 new unit tests covering HTML svg / img / empty-button / image-input-with-src / image-input-no-src / submit-empty-value / JSX image-input / JSX empty-button fix-text assertions.]
- [x] **V1-FIX-EMPTY-HEADING** `src/rules/semantics/empty-heading.ts:165-171, 174-180` — walk document for nearest preceding heading at adjacent level ("Empty `<h3>` follows \"Contact Information\" (h2); continue the hierarchy"). Small. (b374e12)
- [x] **V1-FIX-LANDMARK-MAIN** `src/rules/semantics/landmark-main.ts:78-91, 95-112` — when multiple `<main>` exist, inline each main's id/class for disambiguation ("Document has `<main id=\"app-main\">` (line 5) and `<main id=\"legacy-main\">` (line 45); demote one to `<section>`"). Small. (06035ee)
- [x] **V1-FIX-TABLE-HEADERS** `src/rules/semantics/table-headers.ts:154-157, 212-215` — inspect first-row `<tr>` children, detect header-shaped text (short, title-cased), suggest concrete td→th rewrite. Small. [cb70741 — `looksHeaderShaped` gates on ≤40 chars, starts-upper, non-numeric (currency/percent/signed rejected), and rejects ALL-CAPS multi-word shouts. `classifyHeaders` requires ≥2 cells and ≥60% header-shaped to populate each axis. Four-branch ladder in `buildSuggestionFromDetection`: (1) both axes → inline row + column candidates and mention scope="col" / scope="row" plus scope="colgroup" for complex tables; (2) row-only → inline detected column headers (e.g. `Name`, `Email`, `Role`, `Last Login`) and recommend scope="col"; (3) col-only → inline detected row labels and recommend scope="row"; (4) no signal → fallback naming both scopes and the role="presentation" layout escape. Five new unit tests cover row-only inline, col-only inline, numeric-only fallback, both-axes + colgroup mention, and JSX parity. Audit row was flipped during the link-no-href commit's index race (aeb5ddc).]

### v1.0.0 — criterion KB final sweep

- [x] **V1-KB-FINDER-GUIDANCE** 7 KB entries added (26158e7) + GUIDANCE_BY_ID removed (fca13f1): wcag22:1.2.1, 1.2.3, 1.4.1, 2.1.2, 2.3.1, 3.2.1, 3.2.2. wcag22:3.1.3 already had a KB entry. Map fully drained; checklist builder now uses criterion.description directly.

### v1.0.0 — fourth-pass external-codebase scan (2026-04-20)

Source: `scan_project` run against the twbs/bootstrap repo (125 parseable files, 131 findings, 114 ms). Bootstrap is 104 `.astro` + 122 `.scss` + 47 `.css` + 14 `.html` + js/ts; it is documentation-site-shaped (Astro) with a traditional JS-test HTML fixture corpus. Two classes of gap surfaced: (a) the discovery pipeline silently drops unknown extensions — 226 of 351 source files vanished without a warning; (b) the response clocked 147 KB and overran the MCP token ceiling on the first call even with `limit: 50`, driven by one rule echoing user-authored label text twice per finding. Five shape/coverage-honesty items + one response-size item. Each has a concrete file:line anchor. Dispatch in parallel — independent files.

- [x] **V1-DETECT-SILENT-EXT** `src/utils/path.ts:20-28` `PARSEABLE_EXTENSIONS` is `{.ts,.tsx,.js,.jsx,.html,.htm,.css}`. `hasParseableExtension` + `src/input/discover.ts` silently skip every other extension — no surfacing anywhere in the scan meta. On Bootstrap 226 source files (104 `.astro`, 122 `.scss`) were dropped at discovery; `filesScanned: 125` reads as "tool covered everything" when the tool actually ignored 64% of the repo. Landed: `discoverFilesWithDiagnostics` + `parseFilesWithDiagnostics` + warning code `extensions_skipped_no_parser` + `meta.analysisCoverage.skippedByExtension` wired through `runScanAndFormat`; `scan_project` threads the diagnostic. Follow-up: migrate remaining scan-surface tools (scan, scan_file, scan_diff) to `parseFilesWithDiagnostics` so they also emit the warning.
- [x] **V1-SHAPE-SNIPPET-EMPTY** `src/output/agent-response/build-finding.ts` + `src/output/agent-response/types.ts` — `buildSnippet` returned `{ before: [], highlighted: v.snippet ?? "", after: [] }`; `before` / `after` had zero writers anywhere in src/. Landed: `AgentFinding.snippet` is now `string | undefined`, omitted via conditional-spread when absent; `AgentSnippet` type removed; snapshot tests + CHANGELOG Breaking Changes entry updated.
- [x] **V1-SIZE-LABEL-ECHO-CAP** `truncateForEcho(text, max = 200)` helper added to `src/engine/ast-helpers.ts` (dda1414); single U+2026 ellipsis. Threaded through 13 rule files (227725b) — the three backlog-named offenders plus 10 additional sites discovered in the audit grep: `media/alt-text-missing` (4 sites), `semantics/button-name`, `document/iframe-title`, `tooltip/dismissable` (replaced bespoke truncator), `media/video-captions-missing`, `layout/reflow-hardcoded-width`, `parsing/duplicate-id`, `parsing/html-has-lang`, `document/lang-on-parts`, `document/meta-refresh`, `motion/pause-stop-hide`, `navigation/skip-link`. Read-only / controlled-vocabulary hits explicitly skipped (ARIA enums, table cell ≤40 cap, accesskey single chars). Helper unit tests + invariant assertions on label-in-name. `pointer/target-size` retains its own 40-char cap (intentional, tighter than default).
- [x] **V1-SIZE-RESPONSE-BUDGET** ADR 0021 (`docs/adr/0021-scan-response-size-budget.md`) accepted option (a): lower `DEFAULT_PAGE_LIMIT` 200 → 25 in `src/mcp/tool-scan-project.ts` (c1945fa). Calibration from a 16-fixture measurement corpus (real-world + synthetic): bytes-per-file is stable ~2.9–6.5 KB across profiles; 25 keeps typical responses under 100 KB and caps worst-case label-heavy at ~163 KB with honest `truncated` + `nextOffset` signals. 200-file responses were 1.24 MB — factor-of-8 cut, not marginal tuning. New unit test asserts default-cap triggers truncation at 25 on a 30-file fixture. Token-aware budget (option b) rejected to preserve existing `limit`/`offset` contract semantics consumers already understand.
- [x] **V1-SHAPE-FIXCLASS-HEADLINE** `src/mcp/scan-assembly.ts` plan-summary builder — Bootstrap scan returned `plan.summary: "129 violations (129 guidance fixes). 2 notes… 4 actionable manual review items + 17 untargeted criteria"`. The parenthetical sums four categorically different `fixClass` sub-buckets (`mechanical` 31, `guidance` 30, `runtime-only` 24, `verify-in-source` 46) under one "guidance fixes" label. Directly violates `@docs/kb/architecture/ai-first-consumer.md` §"Composite headline counts are dishonest" — agents budget against the summary string and get four distinct work kinds conflated. Fix: either stop summing in the prose ("129 violations (31 mechanical, 30 guidance, 24 runtime-only, 46 verify-in-source)") or drop the parenthetical and rely on the per-finding `fixClass` discriminator (already shipped in Q2-FIXKIND). Effort: small. Landed `33a1978` (Option A — per-lane breakdown, zero-count lanes omitted). Follow-ups: (1) rename top-level `mechanicalEditsAvailable`/`guidanceFixesAvailable` fields to avoid the fixClass enum-name collision; (2) `src/output/agent-response/build-plan.ts` CLI `--format agent` summary uses the same composite "mechanical edits / guidance fixes" pattern and warrants the same split.
- [x] **V1-SHAPE-RULECOV-COUNT** `findingsEmitted: number` added to `PerRuleCoverage` (`src/types/violation.ts:261-268`) and stamped on every entry in `buildPerRuleCoverage` via a new `countFindingsByRule` linear pass over `allViolations` (f035908). Always present, zero-is-meaningful per the AI-first consumer doctrine. Scanner now passes `allViolations` (pre-severity-filter) to keep telemetry honest about what the engine observed. Unit + integration tests assert `findingsEmitted === violation-stream-count` invariant; existing test fixtures updated to populate the new required field. Counter was NOT pre-computed (backlog hint inaccurate); the linear pass adds negligible cost.

### v1.0.0 — fifth-pass external-codebase scan (2026-04-20)

Source: second pass of `scan_project` against twbs/bootstrap (today's HEAD, 125 parseable files, 131 findings) after the V1-SHAPE-* / V1-SIZE-* fourth-pass items landed. Whole re-scan confirms four of the six fourth-pass fixes are wired through the MCP path. Two new items: (a) V1-SHAPE-FIXCLASS-HEADLINE follow-up (2) — the CLI `--format agent` summary — promoting from a buried follow-up line into a first-class item; (b) V1-SIZE-RESPONSE-BUDGET's shipped file-count cap does not bind on label-dense codebases where findings cluster in few files, so the MCP-ceiling overrun symptom the ADR intended to prevent still fires on bootstrap.

- [x] **V1-SHAPE-CLI-AGENT-HEADLINE** (331a805 — option (c): `buildFixClassBreakdown` extracted to neutral `src/output/agent-response/fix-class-breakdown.ts`; `src/mcp/plan-summary.ts` re-imports from the neutral location (MCP→output was already legitimate direction, inverse would have violated layering). `build-plan.ts:buildSummary` now tallies violations by `v.fixClass` and routes the parenthetical through the shared helper. `AgentPlan` field names (`mechanicalEditsAvailable`/`guidanceFixesAvailable`) unchanged — parent item's follow-up (1) stays separate. Three new snapshot tests: mixed-lane breakdown, zero-count lane omission, all-four-lanes fixture. CHANGELOG entry under `## [Unreleased]`.)

- [x] **V1-SIZE-RESPONSE-BUDGET-DENSITY** Option (b) landed (402e546 + 2a26c25 — `src/mcp/reference-guide.ts`: `hashFixDescription` / `hoistFixDescriptions` / `applyFixDescriptionHoist` / `hoistAndBuildReferenceGuide` extend the Q2-PROSE hoist; duplicates keyed by `(ruleId, 12-hex SHA-256 hash)` so two distinct descriptions under one rule both survive (verified on `semantics/label-in-name` which emits a 466-char and 632-char verdict independently). Threshold: hoist when `(ruleId, hash)` repeats ≥2 in the response; singletons stay inline. Wired through scan / scan_file / scan_project / scan_diff (matches Q2-PROSE coverage; suggest_fix / apply_fix / baseline skipped per small response bodies). `AgentFix.description` → optional + `AgentFinding.fixDescriptionRef: { hash }` replaces it on hoisted findings — never both. 11 unit tests in `tests/unit/mcp/reference-guide-hoist.test.ts` + 1 integration round-trip. Breaking CHANGELOG entry under `[Unreleased]`. Measurement on `/tmp/bootstrap` MCP response: 124,624 → 107,036 bytes (**14.1% / ≈ 17.6 KB saved**). Partial win flagged honestly: **100 KB MCP-ceiling target not met** — post-hoist response ≈ 107 KB (~26.7k tokens vs 25k ceiling). Remaining per-finding bloat: `message` ≈ 22 KB (per-finding context, not dedupable), `fix` ≈ 13 KB, `suppressWith` ≈ 7.6 KB, `criteriaTitles` ≈ 3.8 KB. Options (a) token-aware cap and (c) shift full prose to `suggest_fix` are now promoted to the follow-up item below.

- [x] **V1-SIZE-RESPONSE-BUDGET-DENSITY-2** Option (a) landed (66a9147 + 9c73266 + 538e392 — ADR 0021 amended with the token-density secondary budget; `src/mcp/token-budget.ts` exposes `applyTokenBudget` + `CHARS_PER_TOKEN_PROXY = 4` + `DEFAULT_TOKEN_BUDGET_CHARS = 88000` (~22000 tokens, ~3k headroom under the 25k ceiling); scan-tool wiring lives in `scan-project-budget.ts` + `scan-diff-budget.ts` to keep the scan-tool files under the 500-effective-line budget; wired through `scan` / `scan_project` / `scan_diff` — `scan_file` is a single-file shape with no trailing-file axis to trim, so it's not wired through, matching the V1-SIZE-RESPONSE-BUDGET-DENSITY (b) hoist's own skip); new `response_token_budget_truncated` warning code disambiguates density truncation from the existing file-count cap; `truncated: true` composes cleanly between the two caps, `totalFilesWithFindings` persists, `nextOffset` advances on `scan_project`, omitted on `scan`/`scan_diff` (no `limit`/`offset` contract there); progress guarantee keeps ≥1 file entry even when a single file exceeds the budget. Tests: `tests/unit/mcp/token-budget.test.ts` (helper invariants) + `tests/unit/mcp/token-budget-scan-project.test.ts` (50-file label-dense integration fixture). Bootstrap measurement (post-merge, in-process handler): body 107,036 B / ~26.7k tokens → 78,167 B / ~19.5k tokens; envelope 86,098 B / ~21.5k tokens; `truncated: true`, `nextOffset: 8`, `response_token_budget_truncated` fires; 25k MCP ceiling held with ~3.5k-token envelope headroom. ADR 0021 amendment updated with the post-merge measurement table. Option (c) remains on the table only if a future measurement shows the density cap can't hold typical codebases under 25k tokens — density cap holds bootstrap under budget today.

### v1.0.0 — sixth-pass external-codebase scan (2026-04-20)

Source: third `scan_project` pass against twbs/bootstrap HEAD after fourth- and fifth-pass items landed (125 parseable files, 131 findings, 115 ms, response body 78 KB truncated at nextOffset 8 — density budget held). This pass confirms three rule-accuracy false positives: two are rule-logic bugs (select/option composition; modal runtime-toggle pattern), one is reason-text thinness on a widely-used framework idiom. A fourth item captures a per-file noise ratio that suggests rollup hints. Each has a concrete file:line anchor + a verbatim finding from the scan. Dispatch in parallel — independent files.

- [x] **V1-FP-LABEL-IN-NAME-SELECT** `src/rules/semantics/label-in-name.ts:34-41` — `<select>` is in `HTML_INTERACTIVE_TAGS`, so the rule harvests its descendant text as "visible label." On `twbs/bootstrap` at `site/src/assets/examples/floating-labels/floating-label.html:77`, `<select class="form-select" id="floatingSelect" aria-label="Floating label select example">` with three `<option>` children fired 18× (13.7% of the whole scan): "visible text 'Open this select menu One Two Three' … not contained in aria-label". `<select>`'s accessible-name computation does **not** include `<option>` descendant text — only the currently-selected option's text is the widget's value, and its accessible name comes from `aria-label` / `<label for>` / none. Fix: exclude `<option>` text when computing the visible-label substring for a `<select>`; or more broadly, use only direct text nodes (not descendant `<option>`s) for `<select>`. Curate `tests/fixtures/real-world/bootstrap-floating-label-select/` with the three-option pattern asserting zero findings.
- [x] **V1-FP-TOOLTIP-DATA-BS-TOGGLE** `src/rules/tooltip/dismissable.ts` — rule has no awareness of JS-enhanced tooltip toggles. On `twbs/bootstrap` at `js/tests/integration/index.html:18`, `<button … data-bs-toggle="tooltip" title="Tooltip on top">` fired 18× across the scan. Bootstrap's tooltip JS **replaces** the native `title` with an ARIA-aware tooltip widget at runtime (`aria-describedby` + role=tooltip + keyboard dismiss); the `title` attribute is a source string, not a rendered native tooltip. Surface-don't-suppress per §1: keep the finding but enrich `reason` when sibling attrs `data-bs-toggle="tooltip"|"popover"` or `data-toggle="tooltip"|"popover"` (BS4 legacy) or `data-tippy-content` are present — "note: a JS tooltip library appears to enhance this element; the native `title` may be an input to a runtime widget. Verify the runtime behavior at the call site." Curate `tests/fixtures/real-world/bootstrap-data-bs-toggle-tooltip/`.
- [x] **V1-FP-HIDDEN-FOCUS-MODAL** `src/rules/aria/hidden-focus.ts` — rule cannot observe runtime `aria-hidden` toggles. On `twbs/bootstrap` at `js/tests/integration/index.html:41`, `<div class="modal fade" … aria-hidden="true">` containing focusable `<button class="btn-close">` fired 3× on this scan and is the canonical FP of the Bootstrap/BEM modal family. The `limitations` block already names this class of gap, but the finding still lands at `severity: error` with no framing. Surface-don't-suppress: keep severity, but when `aria-hidden="true"` sits on an element with class containing `modal`, `dialog`, `drawer`, `offcanvas`, `popover`, `toast`, `overlay`, `backdrop`, enrich `reason` with "note: class name suggests a runtime-toggled overlay (`aria-hidden` likely flips when the widget opens); static analysis cannot observe the toggle — verify the open-state runtime behavior." Curate `tests/fixtures/real-world/bootstrap-modal-aria-hidden/`.
- [x] **V1-NOISE-RULE-PER-FILE-ROLLUP** `src/output/agent-response/build-plan.ts` + `src/mcp/scan-assembly.ts` — on bootstrap, `forms/autocomplete-missing` fired 31× with 28 on `site/src/assets/examples/floating-labels/floating-label.html` alone; `semantics/label-in-name` fired 18× all on the same file (entangled with V1-FP-LABEL-IN-NAME-SELECT but the pattern generalizes). When one rule fires >N× on one file (threshold proposal: 10), keep every finding in `files[].findings` (no grouping, no dedup — surface-don't-suppress) but add an honest hint: e.g. `perRuleCoverage[ruleId].concentration?: { file, count }` pointing at the densest file when its share exceeds 50% of that rule's total. Signal for the agent that one file is likely where the idiom lives; zero information loss.

### v1.0.0 — seventh-pass external-codebase scan (2026-04-20)

Source: fourth `scan_project` pass against twbs/bootstrap HEAD after sixth-pass fixes landed (125 parseable files, 131 findings, 123 ms, 78 KB response truncated at nextOffset 8 — density budget held). This pass surfaces a previously-invisible coverage gap: bootstrap has 453 source files, but 328 were silently dropped at ingestion (110 `.mdx`, 114 `.scss`, 104 `.astro`), so `filesScanned: 125` reads as full coverage on 27% of the repo. The `extensions_skipped_no_parser` warning fires but doesn't enumerate the hits, and `parseErrorFiles: [remark.ts]` is not echoed as a structured warning at all. Three parser gaps, two honest-signal gaps, two detection gaps, three reason-text enrichment gaps. Each has a concrete evidence anchor from the scan. Dispatch in parallel — independent files.

- [x] **V1-PARSER-SCSS** Landed (7c9987d parser + 96620ec wiring). `src/input/parsers/scss.ts` + `src/input/parsers/scss-internals.ts` implement a phased preprocess/flatten/substitute pipeline into the existing CSS AST — `$var: <literal>;` inlines to subsequent usages, non-literal RHS preserved un-substituted (no synthesized colors), nested selectors flatten one level, `@mixin`/`@include`/`@import`/`@use`/`@forward` stripped, `//` line comments normalized. `.scss` added to `PARSEABLE_EXTENSIONS` + `extensionMatches` alias to `.css`; routed through cli scan/conformance, mcp session + apply-fix + build-artifacts + agent-response + per-rule-coverage. 26 unit tests; recoverable parse errors emit ParseError rather than silent drop. Downstream contrast rules see SCSS files transparently — no rule code changes required.
- [x] **V1-PARSER-MDX** Landed (6a8977a parser + e6cc850 tests + e88956b wiring + 056d52f tsx-generic-classifier unblock). `src/input/parsers/mdx.ts` strips frontmatter, import/export statements, and code fences, extracts JSX + raw HTML blocks at line-start boundaries, delegates fragments to the existing TSX/HTML parsers. `.mdx` added to `PARSEABLE_EXTENSIONS`; routed through cli scan/conformance, mcp session + apply-fix, agent-response suppression pragmas, per-rule-coverage label. Non-literal expression blocks preserved un-resolved; unparseable fragments emit recoverable parseError. TSX generic-call classifier refuses to cross blank lines (previously swallowed JSX blocks whose preceding line ended in an identifier).
- [x] **V1-PARSER-ASTRO** Landed (39182db parser + a5c37a2 wiring). `src/input/parsers/astro.ts` strips the `---` frontmatter fence (line-preserving whitespace replacement), delegates the remaining template to the existing HTML parser. Opaque component tags (`<Icon>`, `<Header>`) flow through as HTML elements; `{expr}` literal text is honest (unresolved). `.astro` added to `PARSEABLE_EXTENSIONS` with `.html` extensionMatches alias; routed through cli scan/conformance, mcp session + apply-fix, agent-response suppression (HTML-comment form), per-rule-coverage "Astro" label. 21 unit tests cover line-preservation, unterminated fence, CRLF, garbage input, opaque-tag templates.
- [x] **V1-WARN-PARSE-ERRORS** (b6167be) `parse_errors_present` added to the `ScanWarningCode` union + emitted via `hasParseErrors(analysisCoverage)` inside the shared `computeScanWarnings` helper. Conditional-spread (never `[]`) — propagates through every consumer of `warningsField` / `warningsFromScanMeta` (scan_project, coverage, checklist, review_candidates, suggest_fix). Three new unit tests in `tests/unit/mcp/warnings.test.ts`; `docs/errors.md` unchanged (that file indexes structured-error codes + CLI exit codes, not MCP scan-warning codes).
- [x] **V1-WARN-EXT-ENUMERATE** Landed (430ee69 ADR 0023 + a549d5c impl). Option 2 chosen — bare `warnings: string[]` stays the stable triage surface; parallel `warningsDetails` sibling carries structured payloads keyed by code, conditional-spread so presence-only warnings add no noise. New `ScanWarningDetails` type + `computeScanWarningDetails` + `summarizeSkippedExtensions` helpers in `src/mcp/warnings.ts`; `warningsField` / `warningsFieldFromScanMeta` co-emit the sibling so every consumer (scan, scan_project, coverage, checklist, review_candidates, suggest_fix) picks it up. 13 new tests (6 payload builder + 5 composite shape + 2 integration on `tool-coverage`). CHANGELOG entry under `### Added` (additive, non-breaking).
- [x] **V1-RULE-ARIA-EXPANDED-DISCLOSURE** Landed (2d24801 rule + 9ca3f0d tests + b82dfd0 kb regen). `src/rules/aria/expanded-on-disclosure.ts` fires on `<button>` / `<a>` / `[role="button"]` matching any of three predicates (aria-controls target, data-toggle value in disclosure set, onclick classList.toggle/add/remove with visibility tokens) that lack `aria-expanded`. Native `<details>/<summary>` exempt. `data-bs-*` / `data-toggle` are additive evidence hints only; no framework branches. 21 unit tests, 6 fixtures, satisfies wcag22:4.1.2 + wcag21:4.1.2.
- [x] **V1-FINDER-MODAL-FOCUS-MGMT** (c65db1d + f6dbbfd) New `src/review/finders/modal-focus-management.ts` flags modal-shaped containers (`role="dialog"`/`role="alertdialog"` or word-boundary class literal containing `modal`/`drawer`/`offcanvas`) that show zero static focus-management hints in the same file. Hint signals (any one silences all modals in the file): container `tabindex="-1"`, descendant `autofocus` / `data-*autofocus*`, or source-text hit on `.focus(`, `trapFocus`, `focusTrap`, `focus-trap`, `FocusTrap`, `useFocusTrap`, `createFocusTrap`. Confidence `medium`; reason text makes the single-file-check framing explicit. Four criteria emitted per candidate: wcag22:2.4.3, wcag21:2.4.3, wcag22:2.4.11, wcag22:2.4.12. 6 fixtures (3 bad, 3 good), 14 unit tests. Coverage matrix regenerated — WCAG 2.2 2.4.12 promoted from attestation-only → finder.
- [x] **V1-FP-NESTED-INTERACTIVE-NONFOCUSABLE-OUTER** (c3227a4) When the outer element's explicit role is in the ARIA 1.2 composite-widget-delegating set (`tab`, `menuitem`, `option`, `treeitem`, `gridcell`, `row`), the reason text is enriched with a composite-widget-delegates-focus note citing the tablist-roving-tabindex expectation. Severity and finding set unchanged; candidate stays visible per surface-don't-suppress. 7 new unit tests cover the three delegating-role cases plus the non-delegating controls (tablist container, native-outer, `<a href>` inside `<button>`).
- [x] **V1-FP-HEADING-HIERARCHY-CITE-PREV** (21742b4) `reportSkippedLevels` now tracks the full previous `HeadingEntry`; message reads "previous was `<h1>` at line 14, this is `<h5>`. Skipped 3 level(s)." and the suggestion echoes the same line. Scope-note: `Violation` has no `detail` field and `src/types/` was out of scope, so the line is inlined in `message`/`suggestion` — strictly additive, no schema change. Zero detection change (same condition, same findings); three new test assertions cover line-citation, suggestion echo, and most-recent-prior-heading selection.
- [x] **V1-FP-AUTOCOMPLETE-CITE-TRIGGER** (4ae6c7a) `expectedToken` refactored to `matchPurpose` returning `{ expected, trigger }` tagged union (`{ kind: "type" | "name" | "id", ... }`); `describeTrigger()` renders the evidence into the message (`type="email" triggers this rule` / `name attribute contains "email"` / `id attribute contains "email"`). Type beats name/id when both match (most concrete signal); `name` vs `id` distinguished in citation. Fixture-dismissal scenario from backlog covered: `name="floatingInput"` stays silent; `name="emailInput"` fires with name-path citation (no false `type="email"`). Zero detection change.

### v1.0.0 — eighth-pass external-codebase scan (2026-04-20)

Source: 10-agent parallel MCP-tool audit against twbs/bootstrap HEAD after the seventh-pass items landed. Each agent targeted a distinct tool lens (scan_project envelope, checklist, coverage, review_candidates, suggest_fix, explain_rule/standard, bootstrap composer, detect_native_wrappers, list_rules/propose_*, attest/vpat/conformance). `suggest_fix` and `detect_native_wrappers` came back clean — fix text is context-aware, wrapper detection returns honest empties. Seven categories of gap across the other eight tools: composite-headline counts, envelope/meta honesty, onboarding-tool shape, filter/correctness bugs, rule-metadata drift against spec, review-finder reason-text enrichment, new-finder coverage, and conformance-claim honesty. Each has a concrete evidence anchor from the run. Dispatch in parallel — independent files.

Composite-headline counts (doctrine: "composite headline counts are dishonest — must count one kind of thing"):

- [x] **V1-COMP-PLAN-GUIDANCE-FIXES** (82f41e5) `plan.guidanceFixesAvailable` replaced with structured `plan.fixesByClass: { mechanical, guidance, runtimeOnly, verifyInSource }` — each key counts one kind of thing per the doctrine; `mechanicalEditsAvailable` retained as an orthogonal "actionable inline edit" counter. `next-step.ts` `fixable` now reads from `fixesByClass.mechanical + .guidance` to preserve prose-only-suggestion coverage that the old composite incidentally provided. New `tests/unit/output/agent-response/build-plan.test.ts` covers per-lane emission; `fixesByClass` conditional-spread when violations > 0.
- [x] **V1-COMP-CHECKLIST-MANUAL-REVIEW** (b60abc5) `checklist.summary.manualReviewRequired` dropped — the canonical dishonest-composite in `docs/kb/architecture/ai-first-consumer.md`. Consumers should read the existing `summary.actionable` + `summary.untargetedCriteria` split (also in `summary.headline`). No `ChecklistSummary`/`ChecklistResponse` type existed (ad-hoc shape at assembly site); integration tests adjusted to derive the total from the split when needed; no non-test consumer read the field. CHANGELOG Breaking entry added under `[Unreleased]`.
- [x] **V1-COMP-BOOTSTRAP-SCAN-TOTAL** Landed (46fd94b). `ScanSubset` now carries `violationsCount` + `notesCount` (both required) and present-when-meaningful `fixesByClass: { mechanical, guidance, runtimeOnly, verifyInSource }` + `mechanicalEditsAvailable` — all forwarded verbatim from upstream `scan_project` plan. `totalFindings` removed (bootstrap was the sole caller; split is strictly more information — CHANGELOG `### Breaking Changes` under `[Unreleased]`). `buildNextStep` / `buildNextStepStructured` read `violationsCount`; headline prose reads "N violation(s) + M note(s)". New focused test asserts per-lane numeric + lanes sum to `violationsCount` invariant.

Envelope honesty (doctrine: "verbose meta is scan-confidence signal; warnings are the silent-failure channel"):

- [x] **V1-ENV-LIST-RULES-BARE** (a83fe44) `list_rules` response now carries `meta: { rulesTotal, rulesMatched, standardsLoaded, standards: string[] }` + `nextStep` prose + `nextStepStructured: { tool: "scan_project", args }` (filter pre-fills `standard`). No metaMode — list_rules is pure registry enumeration, no scan-confidence delta across calls. Next-step builder extracted into `src/mcp/list-rules-next-step.ts` to keep `tools.ts` under line budget. Four new tests cover meta + nextStep branches (filter vs no-filter). Filter echo + matchedOf already landed in c868a39.
- [x] **V1-ENV-COVERAGE-NO-ANALYSIS-COVERAGE** (128c247) `coverage` tool response now carries `analysisCoverage` + `warnings` at the top level, unconditional (not gated behind metaMode). `parseFilesWithDiagnostics` swapped in so discovery diagnostics flow; `buildAnalysisCoverage` reused from scan-assembly; local `countFilesByExtension` helper mirrors the private one so the same `extensions_skipped_no_parser` + Tailwind-undercount codes fire on parity with `scan_project`. `src/reports/coverage.ts` untouched (kept data-pure). 4 new unit tests cover warning-fires / warning-absent / scanned-zero-files / cross-tool parity.
- [x] **V1-ENV-BOOTSTRAP-STRIPS-LIMITATIONS** (fa5220c) Optional `limitations?: readonly string[]` added to `ScanSubset`; `extractScanSubset` copies it verbatim from upstream `scan_project`'s `plan.limitations` (conditional-spread — omitted when upstream emits no prose). The bootstrap composer's response now surfaces the static-analysis caveat to CI readers pasting the `ciSnippet`. `buildCiSnippet()` deliberately unmodified — the optional CI-caveat expansion overlaps V1-ENV-CISNIPPET-DRY-RUN-DISHONEST which is scoped separately.
- [x] **V1-ENV-CISNIPPET-DRY-RUN-DISHONEST** Landed (65a9adf). `buildCiSnippet` takes `{ baselineExists, writeBaseline, baselineWrittenByThisCall }`; handler probes `${root}/${BASELINE_FILENAME}` via `existsSync`. Three content branches: (1) baseline on disk → plain `--baseline check` (steady state); (2) baseline absent + this call wrote it → `--baseline check` with a `# commit .ra11y-baseline.json before pushing` comment; (3) pure dry-run (no baseline, writeBaseline:false, or writeBaseline failure) → prepend `# create ... first:` + a `- run: npx @ra11y/core --baseline create` step before `--baseline check`. Top-level response shape unchanged (`ciSnippet: string`). Three new tests in `tool-bootstrap.test.ts`.
- [x] **V1-WARN-TOKEN-TRUNCATION-NO-LIMIT-ECHO** Landed (992cab2). `ScanWarningDetails` union extended with `response_token_budget_truncated?: { requestedLimit, effectiveLimit }`; new `tokenBudgetTruncatedDetailsField()` helper returns a spreadable fragment. `scan-project-budget.ts` + `scan-diff-budget.ts` thread pre-density page size (`requestedLimit`) and post-density page size (`effectiveLimit`) into `mergeBudgetedFields`. New `scan-budget.ts` module extracted the `scan` tool's merge to stay under line budget; `tools.ts` delegates to `mergeScanTokenBudget`. Tests cover truncation-fires + omission on default-budget passthrough. Shape follows ADR 0023 (option 2 — bare `warnings[]` stable, structured payload under `warningsDetails`).
- [x] **V1-META-RULES-EVALUATED-COVERAGE-DRIFT** (92ba8cc) `buildPerRuleCoverage` now iterates every active+filter-passing rule (not just tracker entries) and synthesizes a project-scoped branch for rules whose only lifecycle is `afterProject` (`focus/outline-visible`, `wrapper/drift`). `rulesEvaluated` derives from `perRuleCoverage.length` so the two cannot drift again. Zero-file scans honestly surface "no files were scanned; project-scoped rule had nothing to evaluate" instead of silent absence. Reused existing `"low"` confidence + structured `reason` (no enum widening needed). Four new invariant tests + one existing test updated. CHANGELOG `### Changed` entry added.

Filter/correctness bugs:

- [x] **V1-LIST-RULES-MATCHED-COUNT** (c868a39) `list_rules` handler now emits `matchedOf: { total, matched }` always and `filter: { standard }` via conditional-spread — agents can distinguish no-filter / no-op filter / narrowing filter from the response alone. Four new unit tests cover all three states plus the shape contract (`"filter" in data` absent when unset, `matched === total` on wcag22, `matched < total` on wcag21). Filter semantics under the current 53 rule set: `wcag22:` → 53 (universal), `wcag21:` → 50 (genuine subset), `section508`/`en301549` → 0 via direct-prefix match (their coverage flows through `equivalentTo`, not this handler's `startsWith` filter — the `matched: 0` is now a structured signal rather than an empty list; filter logic itself unchanged). Envelope additions (`meta`, `nextStep`) left for V1-ENV-LIST-RULES-BARE.
- [x] **V1-PROPOSE-BASELINE-DUP-FINDINGID** (acd6e61) `proposed[]` deduped by `findingId` — first-seen wins, order preserved. `tallyReasons` now runs over the deduped list so `counts.unclassified` and the `proposed.length == sum(counts)` invariant stay honest. Two new unit tests: three `<button title="…">` on one line collapse to one entry (shared findingId via identical line-context hash); invariant assertion that no two `proposed[]` entries share a `findingId` under any scan shape. No engine or findingId-generation changes.
- [x] **V1-SESSION-WRAPPERS-CWD-BLEED** (3b89f8c) Honesty patch (option b) shipped: `McpSession.configure()` records the anchor cwd (explicit `cwd` param preferred, falls back to `process.cwd()`) on first wrapper-landing call — first-write-wins. `session.sessionWrappersMismatchCwd(root)` + new `session_wrappers_configured_for_different_cwd` warning code wired into `scan_project` and `list_suppressions`. `sessionConfigure` schema gains optional `cwd` param; `docs/mcp/tool-reference.md` updated. Full correctness filter (option a — drop wrappers when roots mismatch) deferred as follow-up; 13 test-file cascade was past the turn budget.

Rule-metadata drift against spec (CLAUDE.md §3 invariant 4 — every rule cites every criterion it checks):

- [x] **V1-RULE-HIDDEN-FOCUS-ADD-2.1.1** (84a57b2 + 9f52268) `satisfies` widened to `[wcag22:4.1.2, wcag21:4.1.2, wcag22:2.1.1, wcag21:2.1.1]`; equivalence rollup now credits the rule under section508:2.1.1 + en301549:9.2.1.1. Header cites both SCs (Name-Role-Value + Keyboard); rationale paragraph added for the keyboard-silence failure mode. No behavior change — pure declarative coverage widening.
- [x] **V1-SC-2.4.11-AUTOMATABLE-MISMATCH** (3697e35 + 2079bb4) SC 2.4.11 retagged `automatable: "manual"` → `"partial"`; rationale comment cites `focus/not-obscured` as the auto-checking rule. No rule/satisfies changes (rule was already correct). wcag21/Section 508/EN 301 549 unchanged — 2.4.11 is wcag22-only. KB `docs/kb/wcag/2-4-11.md` + coverage matrix regenerated.
- [x] **V1-RULE-HEADING-H1-OVERCLAIM** (e2aad8f + 60efbba) Softened `docs.description`, `docs.rationale`, missing-h1 `suggestion`, and the file-header block comment: dropped "must have an `<h1>`" overclaim; new wording frames SC 1.3.1 as non-mandating but surfaces the missing-top-of-document-landmark concern and points at the disable pragma for fragment/layout cases. Detection logic / satisfies / severity / message strings unchanged.
- [x] **V1-RULE-AUTOCOMPLETE-MODAL** (575862b + 5d27ca8) `docs.description` and `docs.rationale` rewritten to cite SC 1.3.5 AA directly and reference the 53 input-purpose tokens; per-finding `message` + `suggestion` strings also updated to must-modality. Trigger-citation from the earlier 4ae6c7a commit preserved via merge-resolution ("appears to collect information about the user but has no autocomplete attribute — {describeTrigger}. WCAG 2.2 SC 1.3.5 (AA) requires …"). Detection unchanged; `satisfies` unchanged.

Review-finder reason-text enrichment:

- [x] **V1-FP-TIMING-CITE-DURATION** (d398092) `reason` now carries the call's duration argument (`this._config.interval`, `emulatedDuration`, etc.) and enclosing function/method name via text-level lookback. Honest omission when unknown — no guessed names. 11 new tests, zero detection change. Commit oversize driven by biome cognitive-complexity decomposition helpers.
- [x] **V1-FP-IMAGES-OF-TEXT-SVG-DATA-URI** (a2b8ee0) Additive note when `<img src>` is `data:image/svg+xml,...` and decoded payload contains neither `<text` nor `<tspan` — "text-baked-in concern is provably lower, but verify the SVG isn't rendering text glyphs directly in path data." Base64 SVGs skipped (not inspected). 6 new tests covering svg-with-text/svg-with-tspan/non-svg/base64/JSX parity.
- [x] **V1-FP-MULTIPLE-WAYS-CITE-COUNTS** (5afba2d) `reason` appends nav-signal counts (`<nav>`, `<a>`, `<input type="search">`, breadcrumb, sitemap link) derived by `summarizeHtmlSignals` / `summarizeJsxSignals`. Preserves existing SPA-shell annotation after em-dash. 4 new tests; zero detection change.

New review finders (detection gaps — zero candidates on strong static evidence):

- [x] **V1-FINDER-POINTER-GESTURES** (349e489 + b57c2f8) Two new detection paths on `src/review/finders/pointer-input.ts`: (a) co-occurrence — when `touchstart`+`touchmove` OR `pointerdown`+`pointermove` both appear as `addEventListener` in one file, emits one path-based candidate anchored at the first `touchmove`/`pointermove` offset and dedupes against the standalone candidate at the same location; (b) name-pattern — left-word-boundary regex `/\b(swipe|pan|pinch|rotate)/i` matched against file basename + class/function/const/let/var identifier declarations, skipping comment/JSDoc lines. `swipe.js`, `class PinchZoom`, `function rotate(`, `const panHandler =`, `panelSlide` fire; `span`, `planet`, `expanded`, `plan` stay silent. Confidence: `"high"` on the deterministic pair pattern, `"medium"` on name-pattern hits. 29 new tests + 6 fixtures (3 bad, 3 good). Surface-don't-suppress — no thresholds, no heuristic suppression.
- [x] **V1-FINDER-FOCUS-ORDER-NEGATIVE-TABINDEX** (802013d + ed2eca8 + 8197d84) New `src/review/finders/focus-order.ts` fires on `tabindex="-1"` (string or numeric expression) applied to natively-focusable controls (`<input>`, `<textarea>`, `<select>`, `<button>`, `<a href>`) OR elements with widget roles (`button`, `link`, `textbox`, `checkbox`, `radio`, `combobox`, `slider`, `spinbutton`, `switch`). Walks both HTML and TSX/JSX ASTs. `<a>` without `href` correctly skipped (not natively focusable); `<div>` without widget role skipped. Confidence `"high"` — deterministic predicate. Reason text frames the a11y-dialog-pattern vs. accidental-suppression question. Six fixtures (3 bad, 3 good across HTML + TSX), 17 unit tests, KB coverage matrix regenerated.

Conformance-claim honesty (the surfaces agents use to decide "should I claim this?"):

- [x] **V1-CONFORMANCE-LIMITATIONS-FIELD-MISSING** (d53eab0 + 72fd5e2 + 718ef76) `ConformanceBlocker.status` widens with `"undetermined"`; `ConformanceBlockerReason` gains `"runtime-evidence-required"`; top-level `limitations: string[]` emitted on the conformance statement (present-when-non-empty, omitted when zero undetermined criteria). New `src/reports/runtime-evidence-criteria.ts` — spec-derived enumeration of 22 WCAG SCs (1.4.3, 1.4.10, 1.4.11, 1.4.12, 1.4.13, 2.1.1, 2.4.3, 2.4.6, 2.4.7, 2.5.7, 2.5.8, 3.3.7, 3.3.8, 3.3.9 + WCAG 2.1 aliases) with inline comments citing the normative basis for each. Runtime-dependent SCs that reach the builder with zero fail-evidence + no attested/sampled source now emit `status: "undetermined"` / `reason: "runtime-evidence-required"` instead of the silent-miss `status: "pass", reason: "no-evidence"`. Attestations still clear the blocker; static failures still route to `"failing"`. Markdown renderer emits a `## Limitations` section when present, omits when absent. Ledger `EvidenceStatus` unchanged — undetermined lives only at claim level. CHANGELOG Breaking entry added.
- [x] **V1-VPAT-RUNTIME-ONLY-PARTIALLY-SUPPORTS** (e807012) Runtime-evidence-required criteria (22 SCs from the spec-derived `RUNTIME_EVIDENCE_REQUIRED_CRITERIA`) now route to `conformance: "Not Evaluated"` on clean scans with no fresh attestation, instead of collapsing into `"Partially Supports"` via `automatable: "partial"` metadata. Fresh `pass`/`fail`/`n/a` attestations release the criterion to its automatable default; pending attestations ignored; proven static failures still route to `"Does Not Support"` (conservative default — honest negative evidence preserved). Attestations threaded through a new optional `attestations` field on `VpatBuildOptions`; MCP `vpat` tool forwards its already-loaded durable store. **2.5.3 / 2.4.9 left on the static path** — both are `automatable: "partial"` with real rule coverage (`label-in-name`, `link-purpose`) and NOT in `RUNTIME_EVIDENCE_REQUIRED_CRITERIA`; inventing a parallel manual-review-only constant would create two sources of truth for the runtime-vs-manual distinction. 5 new unit tests + 1 MCP integration test; `docs/certification/vpat-mapping.md` updated.
- [x] **V1-ATTEST-EVIDENCE-SOURCE-UNSTRUCTURED** (fb789f0 + 539fbb2) `evidenceSource: "runtime_tool" | "manual_review" | "human_study" | "declaration"` now required on the TS type + on write (`requireValidRecord` rejects missing); absent on read coerces to `"declaration"` for backward compat, unrecognized values skip the record. Optional `toolName` / `runUrl` / `observedAt` (ISO-8601 via new `src/utils/iso-timestamp.ts` zero-dep validator) conditional-spread at every surface. Pragma-derived records stamp `"manual_review"` unconditionally. Attestation summary splits `totalCount` + `bySource` (no composite headline). Downstream: `conformance_statement` markdown + `list_attestations` + VPAT remarks cell all surface evidenceSource + toolName (present-when-meaningful). Two commits; 29 files touched.

Generator-script footgun (tooling, surfaced repeatedly by agents running /fix-drift inside worktrees):

- [x] **V1-TOOL-KB-GEN-PRESERVE-HANDWRITTEN** (b85940b) Option (c1) landed — the unlink decision extracted into a pure `chooseFilesToUnlink` helper in `scripts/generate-rule-kb.ts`. Preserves `index.md` / `README.md`, slugs listed in exported `HAND_AUTHORED_KB_SLUGS` (`fix-suggestion-audit`, `coverage`), and any filename already in the expected-write set. Script body wrapped in `if (import.meta.main)` per repo convention so tests import the helper without triggering regeneration. New `tests/unit/scripts/generate-rule-kb.test.ts` — 9 cases covering index/README preservation, stale-slug removal, overwrite path, both hand-authored filenames from the bug report, allowlist coverage, full-wipe case. Allowlist mirrors `check-kb-drift.ts` `knownRuleExtras` / `knownExtras` sets from the opposite direction; follow-up to hoist the shared constant noted but out of scope.

---

### Considered but not elevated to Track V

- **Implement the remaining manual-only criteria as finders.** The coverage matrix (V1-COVERAGE-MATRIX) will enumerate actual gaps — per finder review the finder list confirms most are already covered. If V1-COVERAGE-MATRIX surfaces genuine gaps, each becomes a Track R `R-<criterion>-<topic>` item, NOT a Track V item. Track V verifies; Track R implements detection.
- **Widen real-world fixtures beyond the five above.** SPA routing, internationalization, dark-mode contrast, reduced-motion — all worth fixtures, none are "find all violations" gates. Log as Track F `F-V1.x` items if the v1.0 bar rises.
- **OSSF Scorecard / npm audit workflows.** Nice-to-have for supply-chain posture; zero-dep invariant already covers most of the surface. Skip unless a real signal arrives.

---

## History — Phases 0–17 complete

Historical record; do not modify. The `/continue` skill does not walk this section.

- **Phase 0** — Autonomous Claude Code infrastructure (hooks, agents, skills, settings).
- **Phase 1** — Guard + generator scripts; `scripts/verify.ts` single entrypoint.
- **Phase 2** — Core types, registries, scanner skeleton, AST helpers, public API surface.
- **Phase 3** — WCAG 2.2 standard module; 86 active criteria + historical 4.1.1.
- **Phase 4** — In-house utilities (logger, assert, fs, path, glob, git, ansi, string-width, wrap, color, contrast, args, index).
- **Phase 5** — Parsers: tsx, html, css, tailwind + theme resolver + discover; unit + fuzz tests.
- **Phase 6** — Rule engine + first 5 rules (alt-text-missing, contrast/minimum, link-descriptive-text, focus/outline-visible, parsing/duplicate-id).
- **Phase 7** — Scanner + terminal/plain/json formatters + theme + snapshot tests.
- **Phase 8** — CLI: scan, list-rules, list-standards, explain, coverage, checklist, vpat, certification, init, doctor.
- **Phase 9** — WCAG 2.1 standard module (reuses rules via equivalentTo).
- **Phase 10** — Section 508 + EN 301 549 standard modules (thin equivalentTo wrappers).
- **Phase 11** — Remaining rules across contrast, focus, keyboard, aria, semantics, forms, pointer, navigation, layout, tooltip, document domains. 54 rules total.
- **Phase 12** — Alternative formatters: sarif, junit, markdown, html, agent.
- **Phase 13** — Reports: coverage, checklist, vpat, certification.
- **Phase 14** — Config, inline disables, baseline mode (create/check/update), monorepo `projects: []` support.
- **Phase 15** — Plugin API (`defineRule`, `defineStandard`, `defineFormatter`, `defineConfig`) + three example plugins + CI smoke job.
- **Phase 16** — Long-form docs: getting-started, CLI, configuration, architecture, KB (architecture + patterns + concepts + gotchas + glossary), certification guides, plugin authoring, MCP docs, ADRs 0001–0005.
- **Phase 17** — CI workflows (ci.yml, release.yml), issue + PR templates, dependabot.
- **Phase 18 partial** — README with real output snapshots + multi-standard-scan integration test + mcp-session integration test coverage audit. Remaining demo/tag/publish items live in Track D above.
- **Phase 19 partial** — In-house JSON-RPC 2.0 MCP server (~200 LoC), session state + AST cache by mtime, 10 live tools, session config for `nativeWrappers`, `.mcp.json` auto-attach, integration tests. Remaining hardening items live in Track M above.
- **Phase 22 partial** — Review finders shipped: use-of-color, error-identification, headings-and-labels, on-input-body (landed as reason-text tiers on on-input-change per CLAUDE.md §1), `tool-audit` meta-tool. Remaining finder + finder tests live in Track R.
