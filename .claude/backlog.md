# ra11y backlog

The `/continue` skill reads this file and dispatches work to specialist subagents. Each item should be small enough that one specialist can finish it in under 20 minutes. When an item would produce more work, split it in place before dispatching.

Legend: `[ ]` open · `[x]` done · `[~]` in progress · `[!]` blocked (reason in comment)

## Ship state

- **v0.1.0 — ready to tag.** Code-complete: 54 rules, 4 standards, 9 formatters, 10 MCP tools, 4 reports, CLI wired, release workflow configured. Remaining work is the demo + the tag + publish (Track D).
- **v0.2.0 — in flight.** MCP hardening (Track M), review-candidate coverage + focus-ring cross-ref (Track R), real-world fixture corpus (Track F). Target: 3–4 weeks post-0.1.0.
- **v0.3.0+ — staged, not started.** MCP sampling (Track S), ecosystem integrations + public benchmark (Track E). Phase 20 work is deliberately deferred until after 0.2.0 ships and user feedback tells us which sampling-backed tool matters most. See ADR 0005 for the sampling architecture.
- **v1.0.0 — readiness (Track V).** Conformance capstone work is done in Track C. Remaining v1.0 gates: (a) five **detection gaps** — alt-text walker scope (SVG image / role=img / canvas), button-name SVG `<title>` traversal, `<input type="image">` title recognition, contenteditable labels, `background-image` contrast surfacing; (b) nine **shape-honesty null sentinels** to conditional-spread; (c) eleven **fix-suggestion context-aware rewrites** (V1-FIX-AUDIT `[~]` acceptance gate); (d) **KB final sweep** to drain `GUIDANCE_BY_ID`; (e) **V1-CHANGELOG-V1** date-stamp on ship; (f) **V1-REGISTRY-AGGREGATE** layer-boundary hardening so ADR 0019's frozen `defineRule`/`defineStandard`/`defineCandidateFinder` exports function end-to-end instead of shipping as inert type-inference stubs (ADR 0022); (g) **V1-RESPONSE-ASSEMBLER** scan-family MCP response-shape seam so the AI-first consumer doctrine is enforced by one assembler call rather than 24 hand-rolled handlers before v1.0 freezes the shape (ADR 0024, proposed).

## Dispatch model (parallel tracks, not sequential phases)

Tracks below are independent. `/continue` picks the next open item from each of up to 3 active tracks per turn and dispatches them in parallel (details in `.claude/skills/continue/SKILL.md`). Within a track, items run in order — some tracks have sequencing; cross-track work is always parallelizable.

Active tracks: **D** (docs/release) · **M** (MCP hardening) · **R** (rules + review candidates) · **F** (real-world fixtures) · **S** (MCP sampling) · **E** (ecosystem/evals) · **Q2** (agent-consumer feedback round 3) · **Q3** (design-system field test) · **Q4** (static-site-generator field test) · **Q5** (vanilla HTML/CSS/JS field test) · **Q6** (bulk-template field test) · **Q7** (multi-repo OSS field test). Track Q (rounds 1-2) closed 2026-04-17.

Staged tracks: **C** (conformance-claim gaps — v0.3.0 foundation + v1.0.0 capstone). Tracks S and E were promoted on 2026-04-17 after the user directed "go all the way without releasing until finalized" — M/R/F are complete, so the remaining pre-release work spans S and E. ADR 0005 §Follow-up work still applies to the speculative tool choices inside S; foundation items (sampling.ts, capability, prompt library, KB docs) are safe to build.

Track Q was added on 2026-04-17 in response to a 10-agent independent eval brief — MCP shape honesty + silent-failure elimination, all derived from real consumer pain on an external React codebase.

Track C was added on 2026-04-17 from a gap analysis on "what's missing to let an agent fully claim WCAG 2.1 AA." Four gaps: runtime-evidence ingest, attestation ledger, process-level scope, conformance statement. None widen detection (no new rules); they widen what an agent can defensibly *say* after using ra11y.

---

## Track D — Docs & release

Owner: `release-captain` + `doc-writer`. Blocks nothing; can ship independently.

### v0.1.0

- [x] Demo recording for README — switched from asciinema (`.cast`, doesn't render inline on github.com or npm.js) to charmbracelet/vhs (declarative `.tape` script → reproducible GIF; 2026-04-25 research turn). Two-part: (a) `/continue` agent scaffolds `docs/demo.tape` — declarative VHS script: `Set FontSize 18`, `Type "ra11y scan"`, `Sleep 2s`, `Enter`, `Output docs/demo.gif` — and a `.github/workflows/vhs.yml` using `charmbracelet/vhs-action` so the GIF regenerates on tape/CLI changes. (b) User runs once: `brew install vhs` (pulls `ttyd`+`ffmpeg`), `vhs docs/demo.tape`, commits `docs/demo.gif`, embeds `![ra11y demo](docs/demo.gif)` in README. CI then keeps the GIF fresh — no re-record loop needed when output drifts. Tape file IS automatable inside /continue; only the first render requires user-side install.
- [x] Version bump confirmation (package.json already reads `0.1.0`; verify + commit a release-prep chore if anything else drifts) (a0a805b — README status note, terminal version example, config.md stale forward-ref, writing-a-rule.md afterProject note; no drift in CHANGELOG date, llms.txt absent)
- [x] Tag `v0.1.0` and push — **superseded** by Track V (v1.0.0 readiness). The local `v0.1.0` tag (commit `e365b5ea`, 2026-04-13) is already 1,674 commits stale; the in-flight work (CHANGELOG `## [1.0.0]` draft, plugin-API additions, MCP response-assembler seam, breaking exit-code freeze) is structurally a v1.0 release, not a v0.1 patch. Per /continue research-turn 2026-04-26: defer until Track V hits 0 open, then `/release 1.0.0`. User-confirmation gate on releases per CLAUDE.md §13 + memory directive remains in force.
- [x] GitHub release with changelog excerpt — superseded with the tag item above; v1.0.0 release notes draft already lives under `## [1.0.0]` in CHANGELOG.md and will be the release body.

### v0.2.0

- [x] Release notes for 0.2.0 — drafted under `## [Unreleased]` in CHANGELOG.md (1eabf39). Covers M/R/F/S/E deltas; dated header waits for release.
- [x] Migration notes if any track introduces a breaking MCP shape (b2ed366 — warranted; four hard breaks in response-output shapes: suggest_fix suggestion→fixPaths+kind, checklist.summary.automatedCoverage trimmed to gloss, untargetedCriteria rename, activeNativeWrappers string[]→tagged-object-list; filePath param alias and configure dispatch are input-side compat, not output-side; docs/migrations/0.1-to-0.2.md created)

---

## Track M — MCP hardening

Owner: main session. Internal ordering: baseline first (unlocks scan-diff), then apply-fix, then prompts/resources/capability, then structured errors, then tests last.

### v0.2.0

- [x] `src/mcp/tool-baseline.ts` — create/check/update a baseline from within an MCP session (pairs with Phase 14 baseline work)
- [x] `src/mcp/tool-scan-diff.ts` — compare current scan vs a baseline; emit only new findings
- [x] `src/mcp/tool-apply-fix.ts` — take a `suggest_fix` result, apply the search/replace, re-scan, return the delta (opt-in via `--allow-write` session flag)
- [x] Prompt templates under `src/mcp/prompts/` — reusable `prompts/list` entries for triage / fix / audit / VPAT narrative workflows (pure strings, no template engine)
- [x] `resources/list` support — expose `docs/kb/**` as MCP resources so attached agents can retrieve KB entries without filesystem access
- [x] Capability declaration polish: `logging`, `completions` where meaningful, `roots` so `scan_project` respects the host's declared project boundaries
- [x] Structured errors across tools — replace text-only error payloads with `structuredContent` where the result is machine-consumable (coverage, checklist, list_rules, review_candidates)
- [x] `tests/integration/mcp-resources.test.ts` — resources/list + resources/read round-trip
- [x] `tests/integration/mcp-prompts.test.ts` — prompts/list + prompts/get round-trip

---

## Track R — Rules + review-candidate coverage

Owner: `rule-implementer` (rules) + main session (review finders). Rules within the track are independent; dispatch 3 in parallel when you have 3 open items.

### v0.2.0

- [x] `src/review/finders/consistent-navigation.ts` — wcag22:3.2.3. Compare `<nav>` children structure/order across route files; flag divergent routes as candidates.
- [x] `tests/unit/review/finders/consistent-navigation.test.ts` — ≥3 positive, ≥3 negative, ≥1 edge case against fixtures under `tests/fixtures/review/consistent-navigation/`.
- [x] Tailwind focus-ring cross-reference for `focus/outline-visible`. When `.classname:focus-visible { outline: none }` currently downgrades to `info`, cross-reference against `tailwind.ts` class output: if an element with that className also carries `focus-visible:ring-*` / `focus-visible:outline-*` / `focus-visible:shadow-*` utilities, auto-resolve the info candidate. Not heuristic suppression — a named class-token link is concrete evidence. Extends existing parser output; no new architecture.
- [x] `src/rules/aria/conflicting-role.ts` — wcag22:4.1.2. Flag explicit `role` values that contradict the native element's implicit role: `<button role="link">`, `<a role="button">` without href/handler coupling, `<nav role="main">`, etc. Real 4.1.2 failure — AT announces one thing, browser behaves like another. `docs/kb/concepts/aria-valid-roles.md` already promises this rule exists; implementation fills the gap. Explicitly NOT covering *redundant* role (`<a role="link">`) — pure ARIA code-smell with no WCAG hook; revisit when a best-practices standard lands. Needs implicit-role table in `src/engine/ast-helpers.ts` or a co-located map; pairs with `tests/fixtures/{good,bad}/aria-conflicting-role/` and ≥3 positive/≥3 negative unit tests.

### v0.3.0

- [x] **R-2.3.1-FLASHING** `src/review/finders/flashing-content.ts` — wcag22:2.3.1 + wcag21:2.3.1 (Three Flashes or Below Threshold, Level A). Surface review candidates for static signals of flashing/high-motion content: `<video autoplay>` without a `prefers-reduced-motion` guard, `<canvas>` + `requestAnimationFrame`/animation-library calls outside `@media (prefers-reduced-motion: reduce)`, high-frequency CSS `@keyframes` (opacity/transform cycles faster than ~3Hz) not reduced-motion-gated, `<marquee>` / `<blink>` legacy tags. Follows the `motion-actuation.ts` / `pointer-input.ts` finder shape: reason-text frames the question; agent reads the file and verifies the physical thresholds (3/s flash rate, red-flash area) that static analysis can't. Closes the last A+AA criterion with no static signal (gap surfaced during 2026-04-17 WCAG 2.1 AA coverage audit — all other manual-only A+AA SCs already ship finders or have tracked work). Fixtures under `tests/fixtures/review/flashing-content/`; ≥3 positive, ≥3 negative, ≥1 edge case; kb entry via `/fix-drift`.

---

## Track F — Real-world fixture corpus

Owner: `fixture-curator` + `test-author`. **Sequenced: ADR → harness prototype → fixture backfill (9 items in parallel).** The 9 fixture cases cannot start until the harness lands.

### v0.2.0

- [x] ADR 0006 `docs/adr/0006-real-world-fixture-harness.md` — resolve the five design questions (assertion shape, per-fixture config, sanitization policy, golden-output generation, harness architecture). Chosen positions go in the ADR.
- [x] Harness prototype against `tests/fixtures/real-world/tsx-generics/` validating the `FixtureAssertions` primitives from the ADR before committing to the shape.
- [x] Extend `fixture-curator` subagent definition in `.claude/agents/fixture-curator.md` with the new `tests/fixtures/real-world/` conventions once the ADR lands.
- [x] `real-world/tsx-generics/` — Pick<T,K>, ForwardRefRenderFunction<...>, generic function calls. Guards commit `2968d87`. Assertion: zero-parse-errors.
- [x] `real-world/spa-shell-vite/` — Vite-style `<div id="root">` + module script index.html. Guards `bc3aae4`. Assertion: wcag22:2.4.5 candidate reason includes "SPA index shell".
- [x] `real-world/tailwind-coverage/` — 60 JSX files with utility-class strings, zero CSS. Guards `4700a13`. Assertion: analysisCoverage.hints includes "Tailwind usage detected" + `additionalPaths: ["dist/assets"]`.
- [x] `real-world/logotype-annotation/` — `<img className="site-logo" alt="Acme">`. Guards `71b9954`. Assertions: wcag22:1.4.5 reason contains "logotype exemption"; wcag22:1.4.9 reason does NOT.
- [x] `real-world/timing-role-hints/` — useDebouncedCallback, authManager, telemetryService. Guards `3ada44a`. Assertion: wcag22:2.2.1 reason contains "likely not user-facing" (updated live: now guards "session-keepalive / debounce / animation" + reverted-enrichment absent).
- [x] `real-world/template-directives/` — Jinja `{% extends %}` + `{{ x }}`. Guards `a554d27`. Assertion: analysisCoverage.templateDirectiveHandling includes "parsed as literal".
- [x] `real-world/opaque-components-top/` — 10 PascalCase components with varied call-site counts. Guards `0ee28e1`. Assertion: analysisCoverage.opaqueCustomComponentsTop is a ranked list of length ≤5.
- [x] `real-world/suppression-reason-slot/` — pragma with `: reason` syntax. Guards `d820186`. Assertion: meta.suppressions contains an entry with `reason: "..."`.
- [x] `real-world/autodetect-attribution/` — autoDetectWrappers scan; assert sessionNativeWrappers is absent (regression on `6821b77`).
- [x] Policy change: update `CLAUDE.md` §7 and §17 — when fixing a real-world bug (not adding a new rule from spec), add a sanitized repro to `tests/fixtures/real-world/<case>/` FIRST. Unit tests are for invariants; behavior-rehearsals migrate to real-world fixtures on touch.

---

## Track S — MCP sampling

Owner: `parser-author` + main session. Promoted from staged 2026-04-17. Foundation items (sampling.ts, capability plumbing, prompt library, KB docs) land freely; the three speculative LLM-backed tools (`resolve-component`, `verdict-candidate`, `draft-vpat-narrative`) still need a concrete use-case selection per ADR 0005 §Follow-up work — surface them as review candidates before wiring.

### v0.2.0 (foundation)

- [x] `src/mcp/sampling.ts` — client helper calling `sampling/createMessage` on the host with timeout + max-tokens budget
- [x] Server capability declaration: read host `sampling` capability from `initialize.params.capabilities`; graceful fallback to "return the prompt for the agent to run" via `SamplingNotSupportedError` on hosts that decline
- [x] `src/mcp/tool-resolve-component.ts` — **REJECTED** (2026-04-21 research decision). Doctrine violation: "Don't duplicate capability the agent already has" — `wrapper_introspect` (deterministic AST probe) and `detect_native_wrappers` (one-hop probe with `confirmed`/`assumed`) already cover honest cases; a sampling-backed third layer adds probabilistic guesses on top of facts. Sampling belongs where the host can't trivially reproduce via Read+Grep; that doesn't apply here. If a future pure-IDE host without filesystem reach emerges, revisit.
- [x] `src/mcp/tool-verdict-candidate.ts` — pass/fail with reasoning for a review candidate + its `reviewPrompt`. ADR 0005 §Follow-up: same caveat. (edbe3d1)
- [x] `src/mcp/tool-draft-vpat-narrative.ts` — drafts the VPAT "Remarks and explanations" cell per criterion. ADR 0005 §Follow-up: same caveat. (c733c12)
- [x] `src/mcp/tool-triage-findings.ts` — **REJECTED** (2026-04-21 research decision). "Labeled buckets are suppression too": root-cause hypothesis labels are heuristic by construction, not provable from code, so a triage bucket would be honest only for deterministic facts (not available here). With `resolve_component` rejected above and `verdict_candidate` + `draft_vpat_narrative` already shipping without consumer-labels, there is no downstream consumer to shape the schema. Prefer reason-text enrichment on review candidates. If an external consumer publishes a concrete need before v1.0, reconsider.
- [x] Prompt library under `src/mcp/prompts/` — checksum registry for version-pinning (9cfc547). Surfaced on `prompts/list` + `prompts/get` via `_meta.checksum`. Substitution was already in place.
- [x] `tests/unit/mcp/sampling.test.ts` with a fake host recording sampling requests
- [x] `tests/integration/mcp-sampling.test.ts` with a scripted host adapter — scripted host exercises `verdict_candidate` end-to-end; deadlock fix (ff2869d) in `server.ts` unblocks stdio read loop. (a37f154)
- [x] Docs: `docs/kb/architecture/mcp-sampling.md`
- [x] Docs: `docs/mcp/prompts.md`
- [x] `/audit` MCP prompt template: end-to-end workflow (scan → triage → verdict → VPAT draft) — (f4a960e) blocked on the three speculative tools landing; template already exists as a prompt (`audit.ts`), the "MCP prompt template" wording refers to the end-to-end wiring.

---

## Track E — Ecosystem & public benchmark

Owner: `doc-writer` + main session. Promoted from staged 2026-04-17. Items expand the agent-host matrix and establish the public quality story.

### v0.2.0

- [x] Prompt evals harness in `tests/evals/` — measure each sampling prompt's accuracy against a labeled fixture set; CI-gated against a scripted host (no real LLM calls). Paired with Track S. (committed d00535b)
- [x] `examples/ra11y-in-claude-code/` — reference `.mcp.json` + sample `CLAUDE.md` section showing **deterministic v1 workflow** inside Claude Code (scan_project → checklist → suggest_fix → verdict_candidate → draft_vpat_narrative). Unblocked 2026-04-21: Track S speculative tools rejected; ship with the existing deterministic surface. Rewrite the `/audit` MCP prompt template to omit the rejected tools. Precedent: ESLint MCP ships a narrow deterministic tool surface; ra11y's existing surface is already richer than that v1 bar. (committed 15223877)
- [x] `examples/ra11y-in-cursor/` — ship sampling-optional v1 (2026-04-21 research decision). Cursor's MCP host supports Tools, Prompts, Resources, Roots, Elicitation as of April 2026; sampling is a feature request (forum.cursor.com/t/mcp-sampling-support/149604) not implemented. ra11y's deterministic surface (`scan_project`, `checklist`, `suggest_fix`, `detect_native_wrappers`, `explain_rule`, `explain_standard`, `verdict_candidate`, `draft_vpat_narrative`) all work without host-sampling — the sampling-backed tools gracefully degrade via `SamplingNotSupportedError`. Example should lead with the deterministic flow; add sampling variants as annotated optional paths.
- [x] VS Code extension skeleton under `integrations/vscode/` — wraps the MCP server for IDE-native findings (f0508dd). Sibling project with isolated toolchain; no runtime deps leak into @ra11y/core. Explicit non-goals (marketplace, EDH smoke test, CodeActionProvider, per-file scan-on-save, streaming, multi-root, reconnection) listed in README for follow-up.
- [x] Public benchmark: `benchmarks/a11y-tool-comparison.md` — scaffold (2f5539a) + ra11y-side numbers via `scripts/benchmark-run.ts` (f3878e3). Cold start 41.3ms, 10/100/1000 files 3.4/28.3/235.9ms, real-world corpus 72.4ms for 9 fixtures. Competitor columns remain "pending" until those tools are added as isolated dev deps.

---

## Track Q — Agent-consumer feedback

Owner: main session + general-purpose. Source: 10 independent agent runs against an external React codebase (2026-04-17 eval brief). All accepted items are MCP shape/honesty fixes — none touch detection logic. Dispatch in parallel; each touches a different surface.

_25 closed item(s) archived to [`docs/backlog-history/q-rounds.md`](../../docs/backlog-history/q-rounds.md)._

## Track Q2 — Agent-consumer feedback (round 3, 10-agent eval)

Owner: main session + general-purpose. Source: a 10-agent parallel eval against an external React/Vite/TS/Tailwind codebase — each agent exercised a different MCP slice in a "you-find, I-fix" workflow. Round 3 ran after Q (rounds 1-2) closed, on the post-Q shape. All accepted items are shape-honesty, batch-primitive, or workflow-gap fixes — none are heuristic suppression.

_45 closed item(s) archived to [`docs/backlog-history/q-rounds.md`](../../docs/backlog-history/q-rounds.md)._

## Track Q3 — Design-system field test

Owner: main session + general-purpose. Source: 10-agent parallel eval against a design-system docs repo. Slices: components MDX (×4 agents), forms MDX, layout/utilities/helpers/content MDX, getting-started/about/customize/extend MDX, Astro pages/layouts/components, raw HTML test fixtures under `js/tests/`, and an MCP surface critique. All findings triaged against CLAUDE.md §1 and the AI-first-consumer doctrine.

A design-system doc-site tree is a useful stress-test because (a) it's the shape ra11y agents see most often; (b) it has genuine a11y content (accessibility.mdx, prose-embedded ARIA patterns); (c) it ships MDX + Astro + raw HTML + SCSS in one tree — four file classes at once.

**Parser-lifecycle discovery (process issue, not code gap):** `dist/cli.js` was rebuilt at 22:30 but the MCP subprocesses were started at 22:13 and kept running the pre-rebuild bundle. All 10 agents saw `.mdx` / `.astro` return `file-unsupported` and `filesAdded: 0` — a behavior that evaporates once the host re-spawns the MCP subprocess against the fresh bundle. The parser code itself (`src/input/parsers/{mdx,astro,scss}.ts` + `src/mcp/session.ts:440-457` dispatch + `src/utils/path.ts` allow-list) is correct. Items below are the findings that remain valid AFTER the MCP is restarted — the stale-process symptom is folded into a single lifecycle item.

_80 closed item(s) archived to [`docs/backlog-history/q-rounds.md`](../../docs/backlog-history/q-rounds.md)._

## Track Q4 — Static-site-generator field test

Owner: main session + general-purpose. Source: 10-agent parallel eval against a static-site-generator repo. Slices: `_layouts/`, `_includes/`, `pages/`, `_docs/` (alphabetical split), `_posts/` + `_tutorials/`, CSS/SCSS, raw `test/source/*.html`, `lib/` scaffold templates, Ruby source tree, and an MCP surface critique from a non-React-project angle.

A canonical static-site-generator tree: HTML templates with Liquid (`{% ... %}`, `{{ ... }}`), YAML frontmatter, `.md`/`.markdown` content, SCSS, ERB fixtures, Ruby source. Where Q3 probed the docs-site MDX/Astro axis, Q4 probes the Ruby/SSG/Liquid axis — the failure modes are different. Cross-referenced against Q3 to avoid duplication; items that overlap are tagged as such.

**MCP freshness:** one MCP subprocess spawned post-rebuild (pid 16243, started 23:07 vs dist rebuilt at 22:30), so findings reflect current bundle behavior — the Q3 stale-process confound does not apply here.

_44 closed item(s) archived to [`docs/backlog-history/q-rounds.md`](../../docs/backlog-history/q-rounds.md)._

### v0.3.0 — accepted (P2)

- [ ] **Q4-LABELS-REQUIRED-VS-LABEL-ADJACENT-OVERLAP** (follow-up from Q5-LABEL-ADJACENT-UNASSOCIATED) The new `forms/label-adjacent-unassociated` rule can fire alongside `forms/labels-required` on the same input when there is an adjacent `<label>` without `for=`. Both emissions are honest per AI-first surface-don't-suppress doctrine, but field use may reveal whether the overlap is readable or noisy. Observation path: if field reports cite "same input flagged twice," consider de-overlapping by making `labels-required` silent when a label sibling exists (and let `label-adjacent-unassociated` be the canonical voice for that shape). Do NOT preemptively suppress — wait for evidence that the dual-emission confuses consumers. WCAG 1.3.1 / 3.3.2 / 4.1.2. Cross-ref commits 5fdc228+a3f04657.

## Track Q5 — Vanilla HTML/CSS/JS field test

Owner: main session + general-purpose. Source: first pass against a hand-authored vanilla-JS tree — 50 pedagogical vanilla `.html` + `.css` + `.js` bundles, no framework, no build step, no template directives. Exercises ra11y on the shape most "starter" tutorials propagate to learners; findings that stick here ship to every hobbyist project that copies these patterns. Triaged against CLAUDE.md §1 — all items are rule/finder correctness or new detection on patterns statically provable from the source.

Why this axis matters: most field tests so far (Q3 design-system, Q4 SSG, Q6 bulk templates) exercise framework-heavy or bundled shapes. A vanilla-JS tree is the control — clean, hand-authored, no build pipeline confounds. When a rule misfires here, the miss is on bare markup the author wrote.

_25 closed item(s) archived to [`docs/backlog-history/q-rounds.md`](../../docs/backlog-history/q-rounds.md)._

## Track Q6 — Bulk-template catalog field test

Owner: main session + general-purpose. Source: first pass against a 40+ bulk-template tree. Each template is a pre-built marketing/landing-page bundle shipping jQuery-era vendor CSS/JS (bootstrap.css, font-awesome.css, jquery.fancybox.pack.js) alongside authored HTML. Exercises ra11y at scale — the scan hit the MCP token ceiling, surfaced several rule misfires against vendor bundles, and exposed at-scale quality gaps in the proposed config + build-artifact warning path.

Why this axis matters: a bulk template catalog is the "buy a template kit" shape that a huge fraction of small-business and freelance sites ship. The vendor-CSS noise ratio and jQuery-era link antipatterns (`href="javascript:void(0)"`, icon-only social links) are not framework-authoring problems — they are the default ra11y-on-real-purchased-templates experience. When the token budget overruns, legitimate findings never reach the agent.

Three cross-repo items moved out: `PROPOSE_CONFIG_EMITS_ABSOLUTE_PATHS_AND_TRAILING_COMMA` folds into `Q-SHARED-PROPOSE-CONFIG-RELATIVE-PATHS` (Track Q3 cross-repo section). Two Q6 items consolidate under one roof each: Q6-1 + Q6-13 → `Q6-BUDGET-UNDER-VENDOR-NOISE`; Q6-10 + Q6-11 + SSG-4 share scope with Q4-SCANNED-BUILD-ARTIFACTS-REASON (cross-refs inline).

_28 closed item(s) archived to [`docs/backlog-history/q-rounds.md`](../../docs/backlog-history/q-rounds.md)._

## Track Q7 — Real-world OSS field test

Owner: main session + general-purpose. Source: 2026-04-24 multi-repo OSS field test — 20 scan agents (5 waves × 4 anonymized open-source codebases) probing detection accuracy, false-negative coverage, checklist quality, fix-suggestion quality, and response-shape adherence to AI-first doctrine. Sites span four shapes: a CSS-framework + docs tree, an SSG-templated content site, a vanilla HTML/CSS/JS demo collection, and a bulk-template catalog (≈4k files). Wave 5 is the doctrine cross-check; Waves 1-4 are correctness/coverage.

Cross-cutting themes (≥3-of-4-site recurrence) drive the P0 items below. Items with strong overlap against prior tracks are marked as cross-track shared rather than re-listed. All accepted items are correctness, surface-don't-suppress, or shape-honesty fixes — no heuristic suppression accepted (re-affirmed against doctrine).

_64 closed item(s) archived to [`docs/backlog-history/q-rounds.md`](../../docs/backlog-history/q-rounds.md)._

### v0.2.0 — accepted (P0 — shape honesty / silent-failure)

- [ ] **V1-SUGGEST-FIX-MECHANICAL-LANE-REMAINING-RULES** (extends Q7-SUGGEST-FIX-EDIT-LANE-UNREACHABLE) Audit and wire kind: "edit" emission for remaining ~17 rules tagged fixClass: "mechanical": document/lang-attribute, media/alt-text-missing, plus ~15 others. Either populate Violation.fix.{oldText,newText} or re-tag fixClass to verifyInSource if the edit is content-dependent. Cross-references V1-FIX-LANG-AUTOCOMPLETE-ALT-MECHANICAL-DOWNGRADE (turn 6) and V1-SUGGEST-FIX-MECHANICAL-LANE-EMIT-EDIT (turn 7).

### v0.2.0 — accepted (P1 — shape honesty)

- [ ] **Q7-PARTIAL-PARSE-PARSED-THROUGH-LINE** (1/4) `partialParseFiles[]` entries ship `{ path, parser, reason }` but no indication of WHERE parsing stopped — e.g. `parsedThroughLine: N` or a byte/char range. The agent has no way to bound trust in per-rule findings on the file: the rule may have visited the first 5 lines or the first 500. Without the stop point, the agent must read every partial-parse file in full to verify coverage, defeating the static-scanner premise. Fix: emit `parsedThroughLine: N` (or `parsedThroughByte`/`parsedThroughOffset`) on every `partialParseFiles` entry. 12 partial-parse files on one observed scan — non-trivial fraction. Pairs with Q7-PARTIAL-PARSE-STRUCTURAL-RULE-DEGRADATION (different lever; this is metadata, that is rule-emission shape).
- [ ] **Q7-RULE-FINDING-STRUCTURED-EVIDENCE-FIELDS** (1/4 — high volume) High-density rules bury the discriminating evidence in prose: `semantics/list-structure` (59 fires) does not name `offendingChildTag` / `offendingChildClass` as structured fields, so an agent triaging cannot distinguish "intentional `<hr>`-as-divider sibling under `<ul>`" from "real list misnesting" without reading source. `aria/expanded-on-disclosure` (169 fires) does not surface which predicate-branch fired (`branch.kind: "data-toggle" | "class-token" | ...`). For any rule whose finding density is >50, expose the discriminating evidence as a structured field, not buried in the prose `reason` text. Fix: audit the high-density rules, add `evidence: { offendingChildTag?, offendingChildClass?, predicateBranch? }` substructure to their findings; promote the discriminator from prose. Doctrine: agents read structured fields fast; prose is a fallback. Pairs with Q7-LANDMARK-MAIN-REASON-IDENTICAL (different rule, same structural-evidence pattern).
- [ ] **Q7-PERRULECOVERAGE-EXTENSION-PRESENT-OUT-OF-SCOPE-SUBKIND** (1/4) `perRuleCoverage` entries with `coverageConfidence: "low"` conflate two different gaps: (a) `extension-absent` — the scan tree contains zero files of this extension (current message is correct); (b) `extension-present-but-out-of-scope` — files of this extension exist at the cwd but `additionalPaths`/`exclude` pruned them out (current message wrongly steers the agent toward "add additionalPaths for compiled output" when the actual issue is "narrow additionalPaths excluded source"). Fix: differentiate via `subkind: "extension-absent" | "extension-present-but-out-of-scope"`; emit `(b)` with remediation "files of this extension exist at cwd but were pruned by `additionalPaths`/`exclude`; rerun with broader scope or unset the path filter." Doctrine: present-when-meaningful structured data; agent decides remediation from the structured discriminator, not by guessing the right call.
- [ ] **Q7-RESPONSE-TOKEN-BUDGET-SORT-ORDER** (1/4) `warnings: ["response_token_budget_truncated"]` ships `warningsDetails: { requestedLimit: 25, effectiveLimit: 5 }` but does NOT document the sort order on truncation. Agent paginating doesn't know if `nextOffset: 5` orders by alphabetical / finding-density / severity; with N files the page-walk strategy depends on the order. Fix: extend `warningsDetails.response_token_budget_truncated` with `sortOrder: "<order>"` so the agent's page-walk strategy is informed. Pairs with Q7-RESPONSE-TOKEN-BUDGET-DETAIL (different metadata axis: that's the budget-blower; this is the iteration order).
- [ ] **Q7-CATALOG-SCAN-GROUPBY-DIRECTORY** (1/4) Bulk-template repos with N parallel sub-project subdirs at the top level have no first-class workflow for "scan all sub-projects but aggregate per-sub-project." Current options: (a) one whole-tree scan that exceeds token budget and conflates findings across templates; (b) N hand-scoped `scan_project` calls with `additionalPaths` set to each sub-project (174 round trips, 174 separate result blobs to merge); (c) per-file `scan_file` in a loop (loses rule-coverage telemetry's per-project meaning). Fix: add `groupBy: "directory" | "firstChildDir" | "extension"` to `scan_project`; on `groupBy: "firstChildDir"`, the response carries `summary.byGroup: { "<dir>": { violations: N, mostCommonRule, ... }, ... }` — one response with N rows. The full `files[]` payload can be lazily fetched per group via follow-up calls (offset/limit already supports paging — extend with group filter). Pairs with Q6-CATALOG-REPO-SIBLING-HINT (closed, the catalog *detection*) — this is the *aggregation* response shape. Doctrine-aligned: additive surface, no bucket-then-filter.
- [ ] **Q7-CHECKLIST-PERCRITERION-CLIPPED-HEADLINE** (1/4) When `perCriterionClipped: true`, summary headline `actionable: 2` reads as "2 things to verify" — but the actual unclipped count under each criterion can be 10+ (e.g. `maxCandidatesPerCriterion: 10` × 2 criteria = 20 unread, with more elided beyond the 10-cap). Composite headline that conflates "criteria with candidates" and "candidates" under one number. Fix: when `perCriterionClipped` is true, surface per-criterion uncapped count: `summary.actionable: { criteria: 2, candidatesUncapped: 24, candidatesReturned: 11 }`. Also: when `totalCandidates > limit*0.8`, emit `nextStepStructured: { tool: "checklist", args: { offset: <n>, limit: <n> } }` rather than today's generic "iterate items[]" prose. Doctrine: composite headline counts are dishonest; clipped pages hidden behind a complete-sounding count is silent under-surfacing.
- [ ] **Q7-VENDOR-DISMISSAL-KEY-FINGERPRINT** (1/4) Logo-image candidates emit identical reason text on every brand mark — observed 174× on a bulk-template tree, every dismissal a fresh decision because attestation is per-criterion, not per-candidate-pattern. Same issue applies to any high-cardinality candidate family with structurally identical evidence (sponsor avatars, contributor lists, vendor-CSS contrast violations sharing one selector). Fix: compute `candidate.dismissalKey: hash(rule, filename-pattern, src-basename-pattern)` (or analogous discriminator); the agent records one verdict ("logo images are brand marks, dismissed under 1.4.5 logotype exemption") keyed on the hash, and subsequent matches auto-apply via `attest`. Workflow scaffolding rather than rule-suppression — fully consistent with surface-don't-suppress doctrine. Pairs with Q7-CHECKLIST-1.4.5-SR-ONLY-DEDUPE (in-page contiguous-range axis; this is the cross-page pattern axis).
- [ ] **Q7-TEMPLATE-DIRECTIVE-CHILD-ONLY-CONTENT** (1/4) Heading and link rules that infer "empty" / "no accessible name" fire on elements whose only descendant is a template-directive interpolation (`{{...}}`, `<%= ... %>`, `{expr}`, etc.). Concrete observations: `<h2 itemprop="headline"><a href="{{ post.url }}">{{- post.title -}}</a></h2>` flagged by `semantics/empty-heading` and `navigation/link-descriptive-text` simultaneously — the rules say "empty" / "no accessible name," but the body is interpolated content the static parser cannot resolve. Fix: when the only descendant text node is a parsed template directive, demote severity to info OR rephrase to "heading/link content is interpolated (template directive); verify rendered output is non-empty/descriptive." Do not assert absence-of-content as a static fact when the static parser has no evidence of it. Pairs with Q7-FRAGMENT-FILE-HEADING-HIERARCHY (different angle on template/SSG content; both depend on reading the template-directive disposition correctly).

### v0.3.0 — accepted (P1 — rule correctness)

- [ ] **Q7-RULE-IMG-ALT-REPEATS-PROSE-SC-MISMAP** (2/4) 1.4.5 "Images of Text" finder fires when alt text repeats a sibling label or surrounding prose: `<img alt="fly">` next to `<p>Fly</p>`; sponsor-avatar grids with sequential alt-prefix-plus-index. SC 1.4.5 governs whether the image's PIXELS render text — orthogonal to alt-prose redundancy (which is a 1.1.1 concern). Concrete observations across two targets: 10+ candidates each on pictorial-image clusters and sponsor avatars. The criterion mismatch trains the agent to mistrust 1.4.5 candidates uniformly. Fix: either (a) drop the alt-repeats-prose finder for 1.4.5 entirely — the actual signal (text rendered in pixels) is invisible to static analysis without OCR, and the agent reading the alt+visible-prose pair is the only reliable arbiter; OR (b) keep the finder but reframe the reason as a 1.1.1 "redundant alternative text" candidate suggesting `alt=""` since the visible label already names the image. The 1.4.5 evidence (filename-suggests-text, e.g. `button.png`/`banner-headline.svg`) stays under 1.4.5; alt-repeats-prose moves to 1.1.1. Doctrine: spec-correctness on which criterion a candidate belongs to. Pairs with Q7-CHECKLIST-1.4.5-SR-ONLY-DEDUPE (different lever; that's contiguous-range dedup, this is criterion mapping).
- [ ] **Q7-RULE-NAME-RESOLUTION-TITLE-FALLBACK-CONSISTENCY** (1/4) Two rules treat the `title` attribute differently: `navigation/link-descriptive-text` accepts `title` as accessible name unconditionally (so icon-only social links with only `<i>` child + `title="Facebook"` silently pass); `tooltip/dismissable` treats the same `title` as supplementary tooltip content (and fires). The rules disagree on the same attribute on the same element. Per ARIA 1.2 `title` is a last-resort name source — many screen readers (NVDA at default verbosity, VoiceOver in some modes) suppress it. Fix: pick one model and apply it across all name-resolution rules: either (a) treat `title` as accessible name only when no other source exists AND emit info-severity "name-via-title-fallback" candidates so the agent can verify SR support, or (b) reject `title` as name and require aria-label / inner text / sr-only span. The current schism produces silent under-fire on the canonical icon-only social-link pattern (the textbook 2.4.4 risk surface). Pairs with Q7-RULE-TOOLTIP-DISMISSABLE-TITLE-IS-ACCNAME (same observation, complementary fix lane).
- [ ] **Q7-RULE-1.3.6-VS-1.3.5-CANDIDATE-VIOLATION-DEDUP** (1/4) 1.3.6 "Identify Purpose" review candidate fires on inputs that ALREADY trigger `forms/autocomplete-missing` (a deterministic 1.3.5 violation) at the same line. Concrete observation: `<input>` at line 88 emits both a `forms/autocomplete-missing` violation (criterion 1.3.5) AND a 1.3.6 review candidate; the candidate adds nothing the violation didn't already say. SC 1.3.5 is a subset signal of SC 1.3.6 — the candidate is implied. Fix: per-line dedup — when an automated violation already covers a criterion subset, skip the parent-criterion review candidate at the same line. Maintain a per-line set of fired rule criteria; skip review candidates whose criteria union is a subset of an already-fired violation's criteria.
- [ ] **Q7-RULE-SC-3.2.5-LOADER-AUDIT** (2/4) Companion to Q7-AAA-RULE-LOADER-SILENT-NORUN. The new `navigation/link-target-blank-announcement` rule (Q4-RULE-EXTERNAL-LINK-NEW-WINDOW closed) is registered in `list_rules` but does not appear in any scan response's `perRuleCoverage` — observed across two of four targets. `meta.rulesEvaluated.loaded: 74` vs `list_rules` total 75. Either the rule is not wired into the engine's HTML rule-runner path, or its eligibility check filters out plain HTML files. Result: every `<a target="_blank">` without an announcement on every HTML template silently passes. Fix: audit the rule registry to confirm `navigation/link-target-blank-announcement` is in the HTML-applicable rule list; add a regression test scanning a fixture `<a href="..." target="_blank">X</a>` and asserting the rule fires (both at default level for AAA-loaded sessions and explicitly with `level: "AAA"`). WCAG 3.2.5.

### v0.3.0 — accepted (P2 — finder coverage on untargeted-but-groundable criteria)

Cross-cutting theme #18 (4/4 sites). Several criteria currently ship as `untargetedCriteria` (bare-criterion prompts) when the deterministic grounding evidence sits in the parsed source. Each item below promotes one criterion to a grounded finder.

- [ ] **Q7-FINDER-3.2.2-ONCHANGE-CONTEXT-MUTATION** `wcag22:3.2.2 On Input` — JS handlers (`onchange`, `oninput`, `addEventListener('change'|'input')`) whose body mutates `location`, `window.open`, `innerHTML`, or calls a `navigate*`/`router.push*` identifier. Static evidence is grep-able. Pairs with Q7-RULE-FORMS-SELECT-ONCHANGE-CONTEXT-CHANGE (above) — different shape: that rule fires deterministically on the `<select onchange="...navigate...">` subset; this finder surfaces the broader candidate set for review.
- [ ] **Q7-FINDER-1.3.2-CSS-VISUAL-ORDER** `wcag22:1.3.2 Meaningful Sequence` — CSS `flex-direction: row-reverse` / `order: N` / `direction: rtl` on layout containers is grep-able. Cross-ref Q6-MEANINGFUL-SEQUENCE-UTILITY-CLASSES (closed) — that fixed the false-positive on utility-class definitions; this is the consumer-side finder that fires on the actual layout-reordering call sites.

### v0.3.0 — accepted (P2 — checklist quality)

- [ ] **Q7-CHECKLIST-1.4.5-SR-ONLY-DEDUPE** `wcag22:1.4.5 Images of Text` finder fires N times per page on adjacent sponsor-avatar / contributor-list image clusters — N near-identical candidates from contiguous line ranges. Fix: when ≥4 candidates share `(ruleId, criterionId, parent-element-shape)` within a contiguous line range, collapse to one entry with `siblingOccurrences: [{ line, alt }]` + reason "and N similar at lines X-Y." Pairs with Q4-LIKELY-REDUCIBLE-SIBLING-IMAGE-DEDUPE (closed, image-rule dedupe) and Q6-PATTERN-FINGERPRINT-CROSS-TEMPLATE (closed, cross-file). This is the within-page contiguous-range axis.
- [ ] **Q7-CHECKLIST-1.3.3-LOCATIVE-WITH-NOUN** (1/4) 1.3.3 sensory finder fires on prose like "Fill out the form below with some info" because of substring "form below" — but the position word is followed by a noun (`form`) that names the target. WCAG 1.3.3 prohibits sensory-only / position-only references when there is no other identifier; when a noun anchors the reference, the position is supplementary, not the sole locator. Fix: either (a) drop the candidate when a noun follows the position word (the noun anchors the reference), OR (b) rephrase reason to "verify users who linearize content (screen readers) can locate '<noun>' by its name — '<position>' is a layout cue, not a name." Pairs with Q7-CHECKLIST-1.3.3-LOCATIVE-CALLOUT-PROSE (different angle: that's about prose-callout containers; this is about position-with-noun).
- [ ] **Q7-CHECKLIST-2.4.5-DISJUNCTIVE-PREDICATE** (1/4) 2.4.5 "Multiple Ways" review candidate emits "Likely root layout has no search, sitemap, breadcrumb, or 3-link navigation signal" on a page that DOES have a 3-link `<nav>` with 17 anchors. The reason text says "1 `<nav>`, 17 `<a>`, no `<input type='search'>`, no breadcrumb, no sitemap link" — the heuristic conjuncts all four mechanisms; SC 2.4.5 is disjunctive (any one mechanism satisfies). Fix: revise the heuristic to disjunctive — accept ANY of: search input present, sitemap link present, breadcrumb structure detected, OR primary `<nav>` with ≥3 unique links. Or rephrase reason to "Has primary navigation but no secondary mechanism (search/sitemap/breadcrumb); SC 2.4.5 is satisfied by any one — verify your nav links are unique destinations, or consider a search box for sites with deeper hierarchies." The current text reads as a false alarm on a page that probably already meets 2.4.5.
- [ ] **Q7-CHECKLIST-2.4.5-FRAGMENT-FILE-CONTEXT** (1/4) 2.4.5 finder labels a `<head>`-only partial as "Likely root layout" because of `0 <nav>, 0 <a>` evidence. A `<head>`-only file (no `<body>`) cannot be a root layout — the conclusion is heuristic and wrong. Fix: the "Likely root layout" classifier should require the file to contain `<body>` OR be the only top-level `<html>` document in its directory tree. A `<head>`-only file should be classified as "head fragment" and skipped for 2.4.5 — the rule semantically applies to renderable pages with navigable content. Pairs with Q7-FRAGMENT-FILE-HEADING-HIERARCHY (same fragment-classification surface).

## Track Q8 — Multi-repo external scan field test (2026-04-25 round)

Owner: main session + `rule-implementer` (rules) + general-purpose. Source: 4 parallel scan agents against four unrelated public codebases — a static-template subproject set, a docs-site fragment tree, a vanilla-JS tutorial-exercise collection, and a bulk-template catalog (≈4k files / 1.8k vendor-JS files). All accepted items are correctness, surface-don't-suppress, or shape-honesty fixes per CLAUDE.md §1 + the AI-first-consumer doctrine; recurring-across-≥2-scans themes drive the P0 priorities.

Cross-cutting themes seen across ≥2 of the 4 scans: response-token-budget overruns even on medium repos; `.js` files routed through the tsx parser flooding `parseErrorFiles` with fake JSX errors; HTML files reported as `partialParseFiles` with "Stray closing tag at top level" on browser-renderable input; heuristic-mislabeled meta sub-fields recurring on `scannedBuildArtifacts[].reason`, `templateDirectivesFound`, and `coverageConfidence` reasons; cross-surface count drift between scan / checklist / coverage; composite-headline strings in checklist summaries.

_63 closed item(s) archived to [`docs/backlog-history/q-rounds.md`](../../docs/backlog-history/q-rounds.md)._

### v0.2.0 — accepted (P0 — heuristic-mislabeled meta sub-fields)

- [~] **Q8-CSS-COVERAGE-SCSS-ELIGIBILITY** `perRuleCoverage` CSS eligibility check requires `.css` extension when the `parser='css'` registry already accepts `.scss`/`.sass`/`.less`. On scss-only file scans the per-rule `filesEligible` reads `0` and the rule reports "no css files." Fix: derive eligibility from the parser registry, not a hard-coded extension list, so scss-only scans report honest CSS coverage. Pairs with V1-RULE-CONTRAST-MINIMUM-SCSS-ELIGIBILITY (closed for the contrast rules). — classification_mismatch_not_reproducible (verified by general-purpose specialist 2026-04-26): per-rule eligibility for `.scss` files already passes through `EXTENSION_ALIASES` in src/utils/path.ts:107-118 (.scss → [.css], .less → [.css]) used by `extensionMatches` in `runRulesForFile`/`bumpTracker`. Probe via runScan() with parseScss-routed `.scss` files showed `text-spacing` (declares only `[".css"]`) reports `filesEligible: 1, filesEvaluated: 1, coverageConfidence: "high"` — premise false against current main. If field-report corpus was `.sass` indented syntax (no parser), that's a parser-coverage gap not an eligibility gap; file fresh entry framed as parser widening.

### v0.2.0 — accepted (P1 — finder reason-text enrichment)

- [ ] **Q8-FINDER-2.4.5-CROSS-FILE-EVIDENCE** `review:wcag22:2.4.5` (Multiple Ways) fires on every standalone single-page demo without context. Add cross-file evidence to the reason — `scan covered N distinct directory roots with no cross-anchors` — so the agent dismisses standalone-page cases in one read. Pairs with V1-FINDER-2.4.5-MULTIPLE-WAYS-REQUIRE-BODY (closed) — different lever (gating); this is reason-enrichment.
- [ ] **Q8-FINDER-2.3.1-ITERATION-COUNT-PREDICATE** `review:wcag22:2.3.1` (motion/three-flashes) heuristic equates animation-duration with frequency, firing on single-shot 150ms transitions where `animation-iteration-count: 1`. Fix predicate to require iteration-count > 1 (or `infinite`) before synthesizing a per-second cycle rate; encode iteration count as additive reason text otherwise. Pairs with V1-MOTION-2.3.1-CYCLES-PER-SECOND-MATH (closed) and Q7-RULE-ANIMATION-ITERATION-COUNT-GATE (open) — convergent fix; this is the finder-axis closure assertion.

### v0.2.0 — accepted (P2 — finder fix-suggestion polish)

- [ ] **Q8-TOOLTIP-DISMISSABLE-MARKDOWN-LINK-TITLE** `tooltip/dismissable` fires on markdown-link `[text](url "title")` syntax (which renders as an anchor with `title` attribute) and currently suggests "make the tooltip dismissable" — wrong direction; the redundant `title` should be removed since markdown-link `text` already names the anchor. Fix: when the source is a markdown link with a literal title and the title duplicates or shadows the link text, suggest removing the title attribute rather than adding dismiss semantics. Pairs with V1-TOOLTIP-DISMISSABLE-VISIBLE-TEXT-EQ-TITLE (closed) and Q7-RULE-TOOLTIP-DISMISSABLE-TITLE-EQ-TEXT (open) — this is the markdown-syntax variant.
- [ ] **Q8-SECTION-ACCESSIBLE-NAME-NEAREST-HEADING** `semantics/section-accessible-name-missing` reason text doesn't surface the nearest visible heading. The fix is mechanical when the heading is included in reason ("section sits below `<h2>Author bio</h2>`; consider `aria-labelledby` pointing at that heading's id"). Fix: enrich reason with `nearestVisibleHeading?: { text, line, idIfPresent? }` so the agent composes the fix without reading the file. Pairs with V1-RULE-SECTION-ACCESSIBLE-NAME-NEAREST-HEADING-PROPOSE (open) — same gap, this item locks the reason-enrichment shape.

### v0.2.0 — accepted (Q8b — 2nd-pass field test, 2026-04-25)

Source: 5-lens × 4-corpus replication pass over the same four codebases as Q8. Each repo received 5 parallel scout subagents (lenses A=coverage gaps, B=output correctness, C=parser/scanner, D=heuristic-mislabeled meta, E=response-shape drift). Aggregator deduped against existing Q8/V1 items. Net-new items below; Q8 confirmations and doctrine additions captured separately (the latter folded into `docs/kb/architecture/ai-first-consumer.md`).

#### Q8b — accepted (P0 — token-budget honesty / silent-failure)

- [ ] **Q8b-PARSE-ERROR-REASON-NAMES-WRONG-CULPRIT** Every `.js` parse-error entry reads `reason: "Unclosed JSX element <foo>"` because `.js` routes to tsx — technically true but reads as "this JS file has a JSX bug" when the actual signal is "we ran the JSX parser on non-JSX." Until Q8-PARSER-ROUTING-JS-AS-TSX lands, reframe the reason to `"tsx_parser_on_non_jsx_input"` and surface the offending token as additive evidence (`triggerToken: "<r.length>"`); don't echo the JSX-element name as if it were ground truth. Pairs with Q8-PARSER-ROUTING-JS-AS-TSX (root cause); proposed fix is the honest-reason carry-over until that lands.
- [ ] **Q8b-HTML-STRAY-TAG-REASON-NAMES-SCOPE** `partialParseFiles[].reason: "Stray closing tag at top level"` fires on documents whose strays are nested inline (well-formed at root). The "top level" claim is the misdiagnosis — agents reading the reason waste a read confirming the file's root closes cleanly. Fix: reformulate as `"Mismatched <tag> close at line N (inside <ancestor>)"` with the actual stray + enclosing scope; reserve `"top level"` for true root strays. Pairs with Q8-HTML-PARSER-STRAY-CLOSING-TAG-PARTIAL-PARSE (the parser-mode fix); this is the reason-text honesty fix.
- [ ] **Q8b-LAYOUT-TAIL-ELISION-FALSE-DIAGNOSIS** Layout-tail elision diagnosis (`reason: "Elided layout-tail </html> — file opens with a {% include %} directive whose sibling partial closes this root tag"`) fires on files whose last lines are literally `</body>\n</html>`. Predicate doesn't re-check for closing tags after the include preamble. Fix: only emit the elision diagnosis when no `</html>` token appears in the file; otherwise the parse error is something else.

#### Q8b — accepted (P0 — heuristic-mislabeled meta sub-fields)

- [ ] **Q8b-MINIFIED-LABEL-NEEDS-DISCRIMINATOR-AXIS** `scannedBuildArtifacts[].reason: "minified"` is a single composite token over multiple independent signals (single-long-line, hashed-filename, sourcemap-sibling, `.min.` token, license-header-only). On any one false-positive (hand-authored shim, glyph-font SVG, debug-build with banner), the agent can't tell which sub-signal fired. Fix: emit the discriminator that fired (`reason: "single-long-line-no-min-token"` / `"sourcemap-sibling-present"` / `"hashed-filename"`); stop synthesizing `"minified"` as a deterministic label. Builds on Q8-SCANNED-BUILD-ARTIFACTS-MINIFIED-SVG-LABEL — that is the SVG-extension carve-out; this is the structural axis covering all extensions.
- [ ] **Q8b-PERRULECOVERAGE-CONFIDENCE-REASON-SUFFIX-RENAME** Coverage-confidence reasons ending in `_on_this_input` read as a per-input verdict but fire as unconditional defaults on every scan. The suffix is the dishonest framing — when an evidence-gated reason fails to find triggering tokens, the rule isn't "limited on this input," it's structurally inapplicable. Rename surviving evidence-gated cases to `_inherent_to_rule` or drop the suffix entirely; keep `_on_this_input` only when the predicate genuinely re-evaluates per-input.
- [ ] **Q8b-EXTENSIONS-SKIPPED-NO-EXT-BUCKET-NAMES-PATHS** `warningsDetails.extensions_skipped_no_parser.extensions` includes `(no-ext)` lumping LICENSE / COPYING / NOTICE / Makefile / Dockerfile etc. into one bucket alongside dotted-but-binary skips. Agents triaging coverage cannot tell whether the bucket holds source-shaped files. Fix: name well-known textual filenames inline (`(no-ext): ["LICENSE", "Makefile", "Dockerfile"]`) and treat them as a separate sub-bucket from binary-extension skips so the agent can route once.

#### Q8b — accepted (P0 — heuristic-emission / reason-severity disagreement)

- [ ] **Q8b-TOOLTIP-DISMISSABLE-JS-ENHANCER-DOWNGRADE** `tooltip/dismissable` reason text already carries `couldBeWrongBecause: ["tooltip_js_enhancer_present"]` (when sibling attrs like `data-bs-toggle="tooltip"` / `data-tippy-content` are observed) but severity stays `warning` — the reason concedes uncertainty; the severity doesn't. Downgrade to `info` / candidate when the `tooltip_js_enhancer_present` evidence is set; reserve warning for native-only tooltip cases.
- [ ] **Q8b-MOTION-PAUSE-STOP-USER-TRIGGERED-CITATION** `motion/pause-stop-hide` cites `wcag22:2.2.2` on user-interaction-driven CSS transitions; SC 2.2.2's normative gate requires auto-start, >5s, parallel content — none satisfied for hover/focus transitions. Citation-honesty issue distinct from severity. Fix: split branches — auto-playing animation cites 2.2.2; user-triggered transition cites 2.3.3 (AAA) or drops the citation entirely. Pairs with Q8-RULE-MOTION-ATTRIBUTE-DRIVEN-CAROUSEL (auto-rotating carousel scope) and J-1 (user input on signal-vs-noise strategy).

#### Q8b — accepted (P0 — cross-surface drift)


#### Q8b — accepted (P1 — finder reason-text enrichment)

- [ ] **Q8b-CONFIGSOURCE-NULL-EMITS-NO-CONFIG-FOUND-WARNING** `meta.configSource: null` ships across multiple tools without a `warnings: ["no_config_found"]` companion — doctrine names this code as the canonical signal for the "config not resolved" state but emission is inconsistent. Audit every tool that can emit `configSource: null` and unconditionally pair with `warnings: ["no_config_found"]` + `warningsDetails.no_config_found.searchedFrom: cwd`. Closes a recurring zero-output-success ambiguity surface.
- [ ] **Q8b-FINDER-AUTOCOMPLETE-LABEL-INTERPOLATION** `forms/autocomplete-missing` emits the same one-paragraph reason verbatim for every `type="email"` regardless of label/context; agent cannot dismiss recipient-email vs user-email cases in one read. Fix: interpolate the resolved `<label>` / `aria-label` / `placeholder` text into the reason (`"input labelled 'Recipient email' — autocomplete='email' applies if this collects the user's own email; otherwise 'off'"`).
- [ ] **Q8b-FINDER-LANDMARK-MAIN-PROBABLE-CANDIDATE** `semantics/landmark-main` reason text is identical across page-shaped HTML files with no per-file context. Enrich with `probableMainHint?: { tag, line, selectorHint }` — identify the largest non-header/footer/nav block in the file and surface as a candidate to wrap or relabel. Reason-enrichment only; no severity change. Pairs with V1-FIX-LANDMARK-MAIN (closed) — that fix-text only triggered on multi-`<main>` files; this is the zero-`<main>` axis.
- [ ] **Q8b-FINDER-CAPTION-PRECEDING-HEADING-AS-LABELLEDBY-CANDIDATE** `semantics/table-caption-missing` reason and fix are identical across emits regardless of context. When a heading element precedes the table within the same section, propose `aria-labelledby` pointing at the heading's id (auto-id the heading if needed) as the mechanical-edit alternative to inserting a `<caption>`.
- [ ] **Q8b-FINDER-SCANNED-BUILD-ARTIFACTS-OVERSIZE-NEXT-STEP** When `scannedBuildArtifacts.grouped.length` is large AND `totalFilesWithFindings > threshold`, the canonical `nextStep` should swap from `suggest_fix` (which points at the first vendor-CSS finding) to `"add suggestedGlob entries to ra11y.config.ts exclude"` or `"rerun scan_project with additionalPaths narrowed"`. Otherwise the agent's first-tool-call routes into vendor noise instead of the structural fix.

#### Q8b — accepted (P1 — rule scope widening)


#### Q8b — accepted (P1 — new rules)


#### Q8b — accepted (P2 — finder fix-suggestion polish)

- [ ] **Q8b-SAME-ELEMENT-DOUBLE-COUNT-MERGE** Same anchor / same input emits two findings under different rule ids: `<a href="#"><i class="fa..."></i></a>` fires both `navigation/href-javascript-void` (error) AND `navigation/link-descriptive-text` (warning); `<input>` with placeholder fires both `forms/labels-required` AND `forms/placeholder-as-label`. One fix closes both. Inflates headline counts and forces redundant triage. Fix: when two rules fire on the same element with overlapping fixes, merge into a composite finding citing both criteria — or fold the secondary rule into the primary's reason text. No information loss.
- [ ] **Q8b-FIX-SUGGESTION-SENTENCE-SPLITTER-ELLIPSIS-BREAK** `suggest_fix` `approach` field is truncated mid-word on findings whose `explanation` contains the literal `"— e.g."` — sentence splitter breaks on the period inside the abbreviation. Single-character bug, observed across rules whose fix prose uses common abbreviations. Fix: sentence splitter must not break on `"e.g."`, `"i.e."`, `"vs."`, `"etc."` mid-clause; or use a length-based fallback rather than period-counting.

#### Q8 confirmations — cross-corpus signal (2nd-pass)

These existing Q8 items reproduced across multiple lenses/corpora in the 2nd-pass field test, signaling high priority for prompt closure (no new work — duplicate evidence, durable closure target):

- Q8-PARSER-ROUTING-JS-AS-TSX — confirmed by 4 corpora (lenses C across all 4 reports).
- Q8-COVERAGECONFIDENCE-MEDIUM-UNCONDITIONAL-DEFAULT — confirmed by 4 corpora (lens D across all 4 reports — idref / listener / custom-property / click-alternative variants).
- Q8-CROSS-SURFACE-UNTARGETED-COUNT-INVARIANT — confirmed by 4 corpora (lens E across all 4 reports — drift on every cwd).
- Q8-SCANNED-BUILD-ARTIFACTS-MINIFIED-SVG-LABEL — confirmed by 3 corpora (brand SVGs, glyph-font SVG, compact-distribution shim).
- Q8-RESPONSE-TRUNCATED-OVERSIZED-ENVELOPE — confirmed by 3 corpora (86KB, 88KB, post-clip 18× over host cap on bulk catalog).
- Q8-CHECKLIST-SUMMARY-COMPOSITE-PROSE — confirmed by 3 corpora (`criteriaManualReviewRequired` + `plan.summary` composite-headline class).
- Q8-COLOR-MEANING-BY-COLOR-ONLY-SEVERITY-CONTRADICTION — confirmed by 2 corpora (same conceding-reason / error-severity contradiction).
- Q8-RULE-CROSS-FILE-CLICK-HANDLER-NON-INTERACTIVE — confirmed by 2 corpora (vanilla-JS handler-in-sibling-`.js` pattern recurs).

### v0.2.0 — accepted (Q8c — 3rd-pass field test, 2026-04-25)

Source: 5-lens × 4-corpus replication pass over the same four codebases as Q8/Q8b. Each scout ran lenses A=coverage gaps, B=output correctness, C=parser/scanner, D=heuristic-mislabeled meta, E=response-shape drift; aggregator deduped against existing Q8/Q8b/V1 entries. Net-new items below; doctrine additions captured separately in `docs/kb/architecture/ai-first-consumer.md`.

Cross-cutting themes seen across ≥2 of the 4 scans this round: minified/build-artifact label leaks onto un-minified vendor distribution sources; per-finding `confidence: high` does not propagate the per-rule cross-file `coverageConfidence: medium` limitation, so `keyboard/handler-missing` ships hundreds of error-severity findings on native `<button>` elements wired up by sibling `.js`; `nextStep` routes the agent into the alphabetically-first finding (often a vendor file or scaffold dir) on truncated-response and bulk-corpus scans; review candidates emit at `priority: high` against files already classified as build-artifacts; the `scanned_build_artifacts_present` + `parse_errors_present` warning pair overlaps when minified vendor files reach the parser before the artifact classifier rules them out; HTML-residue parse classification on `.md` files routes every HTML-document rule across README/docs prose, producing identical "no `<h1>`" / "no `<main>`" findings the agent dismisses by reading the file once.

#### Q8c — accepted (P0 — token-budget honesty / silent-failure)

- [ ] **Q8c-NEXTSTEP-FIRST-BY-FILENAME-VENDOR-ROUTING** On truncated/bulk-corpus scans, `scan_project.nextStepStructured` points at the alphabetically-first finding — observed routing to a visual-regression fixture file, an underscore-prefixed scaffold directory's `index.html`, and a vendor stylesheet path already present in `scannedBuildArtifacts`. Agent following the canonical `nextStep` wastes a `suggest_fix` call on non-actionable input. Fix: when the response is truncated OR the candidate file is in `scannedBuildArtifacts`, route `nextStep` to (a) the highest-firing rule's first non-vendor finding, OR (b) a "scope down via `additionalPaths`" structured suggestion. Pairs with Q8b-FINDER-SCANNED-BUILD-ARTIFACTS-OVERSIZE-NEXT-STEP — this is the cross-cutting recurrence on truncation/scaffold filename axes.
- [ ] **Q8c-PARSER-JS-INNERHTML-TEMPLATE-LITERAL-WARNING** `.js` files containing `innerHTML = …` / `insertAdjacentHTML(…)` / `document.write(…)` / `el.html('…')` template-literal HTML islands are silently dropped — no rule recurses into the HTML payload. Doctrine names `js_innerhtml_template_literal_unparsed` as the canonical warning code for this class but no surface emits it. Observed across all 4 corpora — dynamically-injected interactive controls, vendor-bundled DOM-construction strings, and runtime-populated content-placeholder shells. Fix: emit the named warning whenever a `.js` source contains a template-literal HTML pattern AND the routed parser produced no findings on that file; surface in `warningsDetails.js_innerhtml_template_literal_unparsed.fileSamples[]`. Symmetric twin of suppression doctrine — under-parsing a content surface is the same silent-miss failure mode as under-emitting.
- [ ] **Q8c-EXTENSION-SKIP-BINARY-VS-TEXT-SPLIT** `warningsDetails.extensions_skipped_no_parser` lumps binary-asset extensions (`.png`, `.jpg`, `.eot`, `.ttf`, `.woff`, `.mp3`, `.psd`, `.ico`) with text-source extensions the scanner could plausibly support (`.php`, `.coffee`, `.htc`, `.xhtml`, `.mkdn`, `.rmd`, `.erb`, `.hbs`). On a bulk catalog the `topExtension: ".jpg"` (1835) buries 30 `.php` files under `totalSkipped: 5341`; the agent reads the top extension and concludes "binary noise" while real text-bearing source goes silently unscanned. Fix: split `extensions_skipped_no_parser` into `binary_assets_skipped` (image/font/audio/video/archive) + `text_source_skipped` (scanner could plausibly route — split out so agent sees the actionable subset). Pairs with Q8-EXTENSIONS-SKIPPED-NO-PARSER-IMAGE-FILTER (filter axis) — this is the symmetric split that surfaces actionable subset rather than dropping it.
- [ ] **Q8c-WARNINGS-SCANNED-BUILD-ARTIFACTS-PARSE-ERRORS-OVERLAP** `scanned_build_artifacts_present` + `parse_errors_present` overlap silently when minified vendor files (jquery.min.js, html5shiv.js, etc.) reach the tsx parser before the build-artifact classifier rules them out — the same files appear in both `scannedBuildArtifacts.grouped[]` and `parseErrorFiles[]` with reasons that contradict each other ("minified" vs "Unclosed JSX element <r.length>"). Agent reading the warnings list cannot tell which is the symptom and which the root cause. Fix: route every input through the artifact classifier BEFORE parser dispatch; when classified as artifact, skip parse entirely and never enter `parseErrorFiles[]`. Pairs with Q8-PARSER-ROUTING-JS-AS-TSX (root cause) and Q8b-MINIFIED-LABEL-NEEDS-DISCRIMINATOR-AXIS (label honesty) — this is the ordering invariant that prevents contradictory dual-classification.
- [ ] **Q8c-FRAGMENTFILES-KIND-DISCRIMINATOR** `analysisCoverage.fragmentFiles[]` is presented as a single classification, but observed members include three categorically-different shapes: HTML partials (`_includes/`, `_partials/`), Markdown-residue (README parsed as HTML), and standalone-document non-HTML (`.svg` icon files, brand-mark SVGs). Each warrants different downstream rule-skipping (HTML-doc rules irrelevant to standalone SVGs; document-shaped rules irrelevant to layout-composing partials; both irrelevant to README markdown). Fix: add `fragmentFiles[].kind: "html_partial" | "markdown_residue" | "svg_standalone"` discriminator; document-shaped rules read the discriminator before deciding eligibility. Pairs with Q8b-FRAGMENT-FILE-COVERAGE-DOWNGRADE (per-rule confidence axis) — this is the per-file shape axis upstream of the confidence call.

#### Q8c — accepted (P0 — heuristic-mislabeled meta sub-fields)

- [ ] **Q8c-MINIFIED-LABEL-VENDOR-DISTRIBUTION-FALSE-POSITIVE** `scannedBuildArtifacts[].reason: "minified"` fires on UN-minified vendor distributions: a 138KB readable jQuery source whose first lines are `/*! jQuery v1.10.2 ... */` and `//@ sourceMappingURL=jquery.min.map`; a `wow.js` shipped alongside its `wow.min.js` sibling; a `prettify.js` with single-line top + multi-line body; a UMD-bundled `livereload.js` with no `.min` infix. Each is a release artifact distributed alongside its minified sibling but is itself the readable source. Mislabeling routes the agent to skip findings on the source while the actually-minified sibling gets the same label. Fix: `vendor_distribution` and `minified` are distinct labels — `vendor_distribution` requires sibling `.min.<ext>` OR sourcemap reference pointing at a `.min` sibling OR banner-comment-with-version; `minified` requires line-length p99 above N OR sourcemap-target shape. Pairs with Q8-SCANNED-BUILD-ARTIFACTS-MINIFIED-SVG-LABEL (SVG-specific) and Q8b-MINIFIED-LABEL-NEEDS-DISCRIMINATOR-AXIS (sub-signal axis) — this is the structural false-positive that recurs on every legacy-vendor-bearing corpus.
- [ ] **Q8c-PERRULECOVERAGE-PARSER-FIELD-NAME-AMBIGUOUS** `parseErrorFiles[].parser: "tsx"` reads as a content classification ("this file IS tsx") but is actually a routing decision (".js was sent to the tsx parser"). Agent reading a `.js` file with `parser: "tsx"` may infer "this codebase uses TSX" and re-route fix suggestions accordingly. Fix: rename to `parserAttempted` or `routedTo`; OR add a sibling `naturalParser` (per-extension default) so the agent can see the routing mismatch in one read. Pairs with Q8b-PARSE-ERROR-REASON-NAMES-WRONG-CULPRIT (reason text on parse error) — this is the field-naming axis on the meta record.
- [ ] **Q8c-PARSEMODE-MD-AS-HTML-LABEL** `parseModeByExtension['.md']: "html"` reads as "the file IS HTML" but is actually "Markdown parsed as HTML residue." Agent reading the field cannot tell the difference between native HTML routing and the markdown-residue downgrade pass. Fix: rename to `"markdown-html-residue"` or surface a `parseModeByExtension['.md']: { mode: "html-residue", caveat: "markdown-source-not-natively-parsed" }` discriminator. Pairs with V1-HEADING-HIERARCHY-MARKDOWN-SELF-CONTRADICTION (closed) — that closed the rule-eligibility side; this is the meta-label honesty axis on the parseMode field.
- [ ] **Q8c-COVERAGECONFIDENCE-REASON-SUFFIX-FRAMING** `perRuleCoverage[].coverageConfidence.reason: "cross_file_X_resolution_limited_on_this_input"` reads as "we tried this input and were limited" — the actual situation is "the rule never crosses files at all." The token frames a permanent rule-design limitation as an input-specific hiccup; agents may infer "maybe a different input would resolve it" and waste a re-scan. Fix: when the cross-file resolution is structurally not attempted by the rule, rename to `"cross_file_X_resolution_not_attempted_by_rule"` (or drop the `_on_this_input` suffix per Q8b-PERRULECOVERAGE-CONFIDENCE-REASON-SUFFIX-RENAME). Pairs with Q8b-PERRULECOVERAGE-CONFIDENCE-REASON-SUFFIX-RENAME (suffix-rename axis) — this is the framing-honesty closure for the cases where the rule provably never attempts the resolution.
- [ ] **Q8c-TEMPLATE-DIRECTIVE-CODE-FENCE-FALSE-POSITIVE** `templateDirectivesFound: ["erb-or-ejs"]` / `["jinja-or-liquid"]` fires when the directive token appears inside Markdown fenced code blocks or inline-code spans (e.g. a docs page quoting `<%= Time.now %>` in prose; release-notes markdown mentioning template syntax). The detector tokenizes everywhere in the source. Fix: when scanning a `.md`/`.markdown` file for template-family directives, exclude content inside triple-backtick fences AND inline-code spans before running the matcher. Pairs with V1-TEMPLATE-CLASSIFIER-MARKDOWN-PROSE-FALSE-POSITIVE (closed) — recurrence at the field-emission axis on a different corpus.

#### Q8c — accepted (P0 — heuristic-emission / reason-severity disagreement)

- [ ] **Q8c-KEYBOARD-HANDLER-MISSING-NATIVE-BUTTON-CROSS-FILE** `keyboard/handler-missing` fires at `severity: error, confidence: high` on native `<button id="X">` elements wired up via sibling `.js` (`document.getElementById('X').addEventListener('click', …)`). Native `<button>` handles Enter/Space without JS. Per-rule `coverageConfidence: medium` with `reason: "cross_file_listener_resolution_limited_on_this_input"` is recorded but per-finding confidence stays `high` — exact severity-reason disagreement called out by doctrine. Recurs across 2 corpora as the dominant FP pattern (≥25 hits in one scan, all on native interactive elements). Fix: propagate the per-rule confidence to per-finding (downgrade to medium); OR exempt native-interactive elements (`<button>`, `<a href>`, `<input type="submit/button/reset">`) from the rule entirely on the cross-file path. Pairs with Q8-RULE-CROSS-FILE-CLICK-HANDLER-NON-INTERACTIVE (the inverse axis: non-interactive elements with handlers) — this is the native-interactive false-positive closure.
- [ ] **Q8c-NAVIGATION-HREF-VOID-FRAGMENT-TARGET-REASON-MISMATCH** `navigation/href-javascript-void` reason text reads "has no fragment target" on `<a href="#component-id" role="button" data-slide="prev">` — the href IS a non-empty fragment target; the rule may be tripping on `role="button"` short-circuiting the predicate. Reason text contradicts the input. Fix: the reason text must accurately reflect the matched predicate (empty fragment vs non-empty fragment-with-role-button vs `javascript:` scheme); audit the rule's branching to ensure each branch's reason names the actual evidence. Pairs with V1-RULE-NAVIGATION-HREF-VOID-RENAME (closed, predicate widening) — this is the post-rename reason-text honesty fix.
- [ ] **Q8c-MEDIA-ALT-PLACEHOLDER-DIMENSION-FROM-URL** `media/alt-text-missing` reason text identifies the `<img>` by `'700x400'` (extracted from `placehold.it/700x400`) and proposes `alt="700x400"` as the fix — pixel dimensions are not image content; the suggested alt is actively wrong. Fix: detect placeholder URL patterns (`placehold.it`, `via.placeholder.com`, `picsum.photos`, `dummyimage.com`) and propose `alt=""` (decorative) as the fix, OR a generic "placeholder image; describe the eventual content" prompt. Pairs with Q7-RULE-MEDIA-PLACEHOLDER-DIMENSION-SRC (open, reason-text axis) — this is the wrong-fix-text axis on the same identifier.

#### Q8c — accepted (P0 — cross-surface drift)

- [ ] **Q8c-FILESCANNED-OVERSTATES-RULE-REACH** `meta.filesScanned: 472` is the headline but per-rule `filesEvaluated` ranges from 6 (SVG-only rules) to 472 (focus-outline) — typical eligibility 327 (HTML-shape rules) with 145 files (SCSS + plain JS) evaluated by NO rule for that bucket. Headline overstates actual rule reach by ≥30% on multiple scans. Fix: surface `filesWithAnyRuleEvaluated: N` and `filesWithZeroRuleEvaluation: M` alongside `filesScanned`; agent can read in one pass how much of the input was a no-op. Pairs with V1-FILES-BY-EXTENSION-GROUND-TRUTH-UNDERCOUNT (open, denominator-honesty axis) — this is the rule-reach denominator companion.
- [ ] **Q8c-PERRULECOVERAGE-FIRED-FLAG-DERIVATION** `meta.rulesEvaluated: { loaded: 77, withEligibleInputs: 73, fired: 48 }` says 48 rules fired; `perRuleCoverage[]` lists 73 entries (matches `withEligibleInputs`). To verify the 48 number an agent must count entries with `findingsEmitted > 0`. Fix: add an explicit `fired: boolean` flag per `perRuleCoverage[]` entry, OR surface a sibling `notFired: ruleId[]` list, OR list only fired rules in `perRuleCoverage[]` with a separate `rulesWithEligibleInputsButNoFindings: ruleId[]`. Pairs with V1-META-RULES-EVALUATED-COVERAGE-DRIFT (closed, the count-derivation axis) — this is the agent-side derivation cost on the same surface.
- [ ] **Q8c-PERRULECOVERAGE-RULES-WITH-NO-ELIGIBLE-INPUTS-DROPPED** `perRuleCoverage[]` silently drops rules with zero eligible inputs (`navigation/link-target-blank-announcement`, `motion/animation-from-interactions`, `contrast/enhanced`, `parsing/invalid-id-shape` observed missing on a docs-site scan). Agent reading `perRuleCoverage[]` cannot distinguish "rule ran on 0 inputs" from "rule did not load" — the rules are simply gone. Fix: include them with `filesEvaluated: 0` + `reason: "no_eligible_inputs"`, OR surface a sibling `rulesWithNoEligibleInputs: ruleId[]` list. The corpus DOES have eligible inputs for `navigation/link-target-blank-announcement` (3 confirmed sites with `target="_blank"`), suggesting the eligibility predicate itself is buggy on `.html` partials without `<html>` envelope. Pairs with Q8c-PERRULECOVERAGE-FIRED-FLAG-DERIVATION (sibling shape axis) — this is the missing-from-list axis.
- [ ] **Q8c-WARNINGS-CROSS-SURFACE-DRIFT-3-WAY** `warnings: string[]` differs across `scan_project` / `checklist` / `coverage` on the same cwd: scan_project carries `["scanned_build_artifacts_present", "response_token_budget_truncated"]` not present on checklist/coverage; coverage drops `response_meta_truncated`; each surface ships a partial slice of the same scan-time facts. Agent calling all three sees three different warning lists describing one scan. Fix: response-instance warnings (`response_meta_truncated`, `response_token_budget_truncated`) belong to the originating tool; scan-time warnings (`extensions_skipped_no_parser`, `parse_errors_present`, `template_files_parsed_as_literal`, `scanned_build_artifacts_present`, `no_config_found`) must be identical cross-surface — compute via a shared helper consumed by every tool that loads the same scan basis. Pairs with Q7-CHECKLIST-META-PARITY (closed, scan_project↔checklist meta axis) — this is the warnings-list cross-surface invariant on top of the meta parity.

#### Q8c — accepted (P1 — finder reason-text enrichment)

- [ ] **Q8c-FINDER-2.2.1-VENDOR-ARTIFACT-PRIORITY-DOWNGRADE** `review:wcag22:2.2.1` setTimeout finder emits at `priority: high` against findings whose target file is already in `scannedBuildArtifacts` (e.g. minified `respond.min.js`, internal debounce inside legacy jQuery) — quotes obfuscated single-letter variable names (`setTimeout(p)`) as the duration. Reason concedes "minified file — match at byte col …" but priority stays high. Fix: when the candidate's file is in `scannedBuildArtifacts`, drop priority from `high` to `medium` AND surface `vendorContext: { signal, redirectTo: "consumer-override" }`. Surface-don't-suppress (still emit; agent reads context once). Pairs with Q8-FINDER-2.2.1-VENDOR-PATH-AND-DURATION-CLASSIFICATION (reason-text axis) — this is the priority-honesty axis on the same finder.
- [ ] **Q8c-FINDER-LOGOTYPE-CONCEDED-PREDICATE-PRIORITY** `review:wcag22:1.4.5` candidate emits at `priority: high` while the reason text concedes the logotype-pattern is satisfied (e.g. "a visually-hidden text sibling is present in the same parent — if the image is the textual logotype and the sibling is the SR-accessible equivalent, this is the documented logotype pattern"). Severity contradicts conceded predicate at the candidate-priority level — same shape as the rule-side rule (Q8-COLOR-MEANING-BY-COLOR-ONLY-SEVERITY-CONTRADICTION) but on the finder/priority axis. Fix: when the reason concedes the predicate may be satisfied, downgrade priority to `medium` (or `low`); reserve `high` for cases where evidence supports an unsatisfied predicate. Pairs with Q8-COLOR-MEANING-BY-COLOR-ONLY-SEVERITY-CONTRADICTION (rule-side severity axis) — this is the candidate-priority axis on the same shape.
- [ ] **Q8c-FINDER-2.4.5-HEAD-ONLY-FRAGMENT-PRIORITY** `review:wcag22:2.4.5` (Multiple Ways) emits at `priority: high` on a 19-line `<head>`-and-meta partial file — the criterion does not apply to head-fragments. Reason concedes "if this is a standalone single-page file or SPA, the criterion may not apply." Fix: when the file structurally cannot host page navigation (no `<body>`, no anchors, no nav elements), drop priority to `low` OR exclude the file from finder eligibility. Pairs with Q7-CHECKLIST-2.4.5-FRAGMENT-FILE-CONTEXT (closed, body-presence requirement) — this is the priority-axis closure for the head-fragment subset.
- [ ] **Q8c-FINDER-IFRAME-MEDIA-EXCLUDE-JS-STRING-LITERALS** Iframe-media review candidates fire on `.js` file string literals (`"<iframe id=\"frame\">…"`), treating the string as a rendered iframe. JS string literals containing `<iframe>` are library code building DOM at runtime, not iframes on the page. Fix: restrict iframe-finder eligibility to HTML-family inputs (`.html`, `.jsx`, `.tsx`, `.vue`, `.svelte`, `.astro`, `.mdx`, `.md`-as-residue); skip `.js`/`.ts` even when the parser exposed a string-literal HTML island. Pairs with Q6-CHECKLIST-IFRAME-FINDER-JS-STRING-LITERALS (closed, JS-source iframe-string axis) — this is the recurrence audit on the open finder family.
- [ ] **Q8c-FINDER-AUTOCOMPLETE-CROSS-FILE-FN-RESOLVED-HINT** `review:wcag22:3.2.2` (`onchange` on `<select>`) reason hedges `"low confidence, body not inline"` and asks the agent to "open the referenced fn and check for router/location/submit" — but the function name (e.g. `navigateToUrl`) is statically resolvable in this corpus (defined inline at sibling layout file). Cross-file resolution is the agent's job per doctrine; surface the function-name link so the agent can jump in one call. Fix: when the handler is a named function call, surface `handlerFunctionName: "navigateToUrl"` as additive evidence so the agent's next Read targets the right symbol. Pairs with Q8-FORMS-AUTOCOMPLETE-MISSING-NAVIGATION-SELECT (rule-side axis) — this is the candidate-side enrichment.
- [ ] **Q8c-FINDER-LANDMARK-MAIN-DEMO-FIXTURE-REASON** `semantics/landmark-main` fires at `severity: warning` uniformly on tiny single-component demo HTML files (test-fixture-shaped: one `<button>` + a styled background, ≤30 lines, no header/footer text). Doctrine forbids filename-pattern suppression but the structural shape is provable from the file (no `<header>`, no `<footer>`, ≤N text-tokens, ≤1 interactive control). Fix: when the file shape is provably "isolated component demo" (deterministic structural predicate), enrich `reason` with `couldBeWrongBecause: ["isolated_component_demo_page"]` so the agent dismisses faster. Reason-enrichment only — keep the candidate; surface evidence the agent verifies. Pairs with Q7-RULE-LANDMARK-MAIN-FRAGMENT-SCOPE (open, fragment-scope axis) — this is the demo-page-shape axis on the same rule.
- [ ] **Q8c-FINDER-EMPTY-HEADING-SKELETON-LOADER-COULDBEWRONG** `semantics/empty-heading` fires at `severity: error` on empty `<h3>` inside content-placeholder/skeleton-loader UIs (filled at runtime via `setTimeout(…, 'header.innerHTML = …')`). Reason doesn't acknowledge SPA loading patterns; severity remains error/high. Fix: when the file's same-shape sibling JS contains `innerHTML = …` / `textContent = …` writes targeting the empty-heading's id/class, enrich reason with `couldBeWrongBecause: ["runtime_innerhtml_population"]`. Reason-enrichment only — keep the finding. Pairs with V1-LIVE-REGION-RUNTIME-MUTATION-CANDIDATE (closed, status-message axis) — this is the heading-emptiness sibling axis.

#### Q8c — accepted (P1 — rule scope widening)

- [ ] **Q8c-RULE-INTERACTIVE-DIV-ROLE-MISSING-CROSS-FILE** When a `.js` file binds a click handler to a `<div>` / `<span>` / `<li>` / `<img>` resolved by selector, AND the target element in HTML has no `role` / `tabindex` / `aria-label`, emit a rule-side finding (not just a candidate). Observed pattern: a non-interactive element with a state-class receives a `dblclick` or `onclick` handler from a sibling script. Predicate is static-deterministic when the AST resolves the selector to a non-interactive tag. WCAG 4.1.2 + 2.1.1. Pairs with Q8-RULE-CROSS-FILE-CLICK-HANDLER-NON-INTERACTIVE (open, finder axis) — this is the rule-emission upgrade for the deterministic resolved-selector case.
- [ ] **Q8c-RULE-COLOR-CLASS-ACTIVE-STATE-UNANNOUNCED** Class-name idiom `.active`, `.selected`, `.checked` (toggled-state styling) on elements lacking `aria-pressed` / `aria-selected` / `aria-checked` is a 1.4.1 candidate the rule does not fire on. Observed pattern: an element with a state-class signaling color-only active state, no aria-state attribute, no text-state sibling. Fix: extend `color/meaning-by-color-only` to surface a review candidate when an element carries a state-class (`active`/`selected`/`checked`) AND no aria-state attribute AND no text-state sibling. WCAG 1.4.1 + 4.1.2. Pairs with Q8-COLOR-MEANING-BY-COLOR-ONLY-SEVERITY-CONTRADICTION (severity-axis closure) — this is the new state-class predicate.
- [ ] **Q8c-RULE-EXPANDED-DISCLOSURE-CLASS-COLLAPSED-PREDICATE** `aria/expanded-on-disclosure` fires on elements with framework `data-*-toggle="collapse"` attributes but misses class-only signal (`.collapsed` class without any data-* attribute) — common pattern in template catalogs. Fix: extend the rule predicate to recognize class-name `collapsed` / `expanded` toggling on `<button>` / `<a>` as disclosure evidence (additive to the data-* path), surface as candidate if not already a rule fire. WCAG 4.1.2. Pairs with V1-RULE-ARIA-EXPANDED-DISCLOSURE (closed, predicate axis) — this is the class-only widening.
- [ ] **Q8c-RULE-NESTED-INTERACTIVE-LINK-WITH-TEXT-AND-ICON** `navigation/link-descriptive-text` reason "every child is presentational (icon)" misclassifies `<a href="…"><i class="<icon>"></i> User Profile</a>` — text node sibling exists after the icon. Fix: the "every child is presentational" predicate must walk past the leading icon to the text node; when text is present, suppress the rule. Pairs with V1-FP-NESTED-INTERACTIVE-NONFOCUSABLE-OUTER (closed) — different rule, same composite-content predicate axis.

#### Q8c — accepted (P1 — new rules)

- [ ] **Q8c-RULE-PHP-MIXED-HTML-PARSER** Add `.php` to `PARSEABLE_EXTENSIONS` with HTML routing + PHP-island stripping (analogous to ERB / Liquid handling). Bulk catalog scan: 30 `.php` files with substantial HTML markup (DOCTYPE, body, nav, anchors) silently dropped via `extensions_skipped_no_parser`. PHP server-page templates carry a11y-relevant content. Fix: parser strips `<?php … ?>` / `<?= … ?>` blocks (literal text), routes the residue through the HTML parser; emit `php_islands_stripped` warning; rule-eligibility derives from parser registry (Q8-RULE-DUPLICATE-ID-PARSER-HTML pattern). Pairs with V1-PARSER-ERB (closed, sibling extension) and Q8-PARSER-ROUTING-JS-AS-TSX (parser-routing axis) — this is the new-extension addition.
- [ ] **Q8c-RULE-SVG-USE-XLINK-HREF-ICON-NAME** New rule `aria/svg-use-decorative-or-named`. Inline `<svg><use xlink:href="#icon-id" /></svg>` (or `<use href>`) with neither `aria-hidden="true"` nor accessible name (`<title>`, `aria-label`, `aria-labelledby`). Currently `semantics/svg-title-missing` only fires on standalone `.svg` files; inline `<svg><use>` icons in HTML are silently missed. Multiple corpora carry dozens. WCAG 1.1.1 + 4.1.2. Pairs with Q8-RULE-SVG-TITLE-INLINE-MARKUP (open, inline-svg axis) — this is the `<use>` element specialization.
- [ ] **Q8c-RULE-ALL-FORMS-LANDMARK-NAME** Cousin to `semantics/duplicate-landmark-unlabeled` (which fires speculatively per Q8-DUPLICATE-LANDMARK-UNLABELED-EMITS-ON-SPECULATION). Once the speculative composite is gated on confirmed sibling evidence, the genuine "every form on this page is unlabeled" case becomes a real finding worth a deterministic rule. Fix: surface `semantics/all-forms-unlabeled` rule for the in-file count ≥ 2 + zero `aria-label`/`aria-labelledby` deterministic case. WCAG 1.3.1 + 4.1.2. Pairs with Q8-DUPLICATE-LANDMARK-UNLABELED-EMITS-ON-SPECULATION (closure target) — this is the deterministic-emission cousin.

#### Q8c — accepted (P2 — finder fix-suggestion polish)

- [ ] **Q8c-FIX-DESCRIPTION-MARKDOWN-FOR-MD-FILES** `aria/role-from-class-only` finding's `suppressWith` field shows a JS line-comment pragma (`// ra11y-disable-next-line aria/role-from-class-only`) on a `.md` (markdown) file — JS-comment syntax renders as literal text in markdown output and would corrupt the rendered docs. Fix: when the file extension is `.md`/`.markdown`/`.mdx`, the suggested suppression pragma must be HTML-comment form (`<!-- ra11y-disable-next-line ... -->`) or JSX-comment form (`{/* ra11y-disable-next-line ... */}` for MDX). Pairs with the existing pragma syntax docs — this is the per-extension routing axis on `suppressWith`.
- [ ] **Q8c-PARSE-ERROR-REASON-LEAKS-FILE-CONTENT** SCSS parse-error reason text leaks raw multi-line file content as the "selector" token: `"SCSS nested selector \"/* Icon rotations & flippi*/\\n/* -----------------------*/\\n\\n.#\" under \"\" is too complex to flatten"`. The reason includes a comment block + newlines; agent reading the reason gets corrupted error context. Fix: sanitize selector tokens in error messages (strip newlines, truncate to ≤80 chars with `…` ellipsis). Pairs with V1-SIZE-LABEL-ECHO-CAP (closed, generic truncation helper) — this is the parse-error reason-text application of the same helper.
- [ ] **Q8c-EXTENSIONS-SKIPPED-NO-EXT-SCHEMA-SPLIT** `warningsDetails.extensions_skipped_no_parser.extensions: [..., "(no-ext)"]` mixes real extensions with the parenthesized sentinel for "no extension" files (LICENSE, COPYING, Makefile). Agent parsing the array as a glob/extension filter has to special-case the parenthesized form. Fix: split into `extensions: string[]` (dotted tokens only) + `noExtensionFiles: string[]` (well-known textual filenames inline: LICENSE / Makefile / Dockerfile) so the array stays type-honest. Pairs with Q8b-EXTENSIONS-SKIPPED-NO-EXT-BUCKET-NAMES-PATHS (bucket-naming axis) — this is the schema-shape axis on the same field.

---

## Track Q9 — Multi-corpus AI-first sweep (2026-04-25 evening, 4 corpora × 5 angles)

Field-test follow-ups from a 20-agent multi-corpus probe (CSS framework + static-site generator + vanilla mini-projects + bulk template gallery). Each item below was observed across ≥1 corpus during the round; recurrence counts noted inline. Items are bucketed by failure class. Net-new against Q8/Q8b/Q8c/V1; recurrence of already-named items folded into the existing rows there.

_13 closed item(s) archived to [`docs/backlog-history/q-rounds.md`](../../docs/backlog-history/q-rounds.md)._

### Parser routing + coverage gaps

- [ ] **Q9-PARSER-INNERHTML-TEMPLATE-LITERAL-ISLANDS-IN-JS** `innerHTML = \`<button>...\`` / `insertAdjacentHTML(\`<i class="fa…">\`)` / `document.write(\`<h2>…\`)` template-literal HTML islands inside `.js` are silently unparsed across all 4 corpora (≥7 distinct files in one vanilla-JS corpus alone, plus inline-script blocks inside browser-shipped HTML). No `js_innerhtml_template_literal_unparsed` warning fires; rules like `semantics/button-name`, `media/alt-text-missing`, `aria/icon-font-hidden`, `semantics/heading-hierarchy`, `semantics/list-structure` produce zero findings on injected widgets. Fix: recurse the tsx/JS parser into string-literal arguments to `innerHTML` / `insertAdjacentHTML` / `document.write` / `outerHTML` and route the substring through the HTML parser; emit a structured `js_innerhtml_template_literal_unparsed` warning whenever the recursion declines so the agent can route. Per AI-first doctrine "Routing skips that drop content are the symmetric twin of suppression." Pairs with Q8-PARSER-ROUTING-JS-AS-TSX (sibling routing-honesty fix).
- [ ] **Q9-PARSER-INLINE-SCRIPT-BLOCK-IN-HTML-NOT-JOINED** `<script>…</script>` inline blocks inside `.html` files are not joined to surrounding markup for cross-element handler resolution; `keyboard/handler-missing` lists itself as eligible in `perRuleCoverage` but the inline body is never treated as a JS context. Recurring across CSS-framework + static-site corpora. Fix: parse inline `<script>` bodies and feed `addEventListener` / `onclick` resolutions back into the HTML rule pass; emit `inline_script_in_html_unparsed_for_handler_resolution` when the join is skipped. Pairs with Q9-PARSER-INNERHTML-TEMPLATE-LITERAL-ISLANDS-IN-JS (cross-context resolution axis).
- [ ] **Q9-EXTENSIONS-SKIPPED-NO-PARSER-TOP-N-TRUNCATION** `warningsDetails.extensions_skipped_no_parser.extensions` caps at top-5 entries; bulk corpora skip 8+ distinct extension classes (`.php`, `.coffee`, `.ttf`, `.woff`, `.woff2`, `.eot`, `.otf`, `.psd`, `.htc`, `.ai`, `.pdf`) but only top-5 surface in the warning while the others remain silent in `meta.skippedByExtension`. Recurring across bulk corpora. Fix: enumerate ALL skipped extensions in the warning array (or add a `silentExtensionCount: N` sentinel naming the surplus). Per AI-first doctrine "Routing skips that drop content are the symmetric twin of suppression." Pairs with Q8-EXTENSIONS-SKIPPED-NO-PARSER-IMAGE-FILTER (filter-axis closed) — this is the truncation-axis follow-up.
- [ ] **Q9-PARSER-PHP-DROPS-EMBEDDED-HTML** `.php` files containing real HTML markup (forms, headings, images outside `<?php ?>` islands) are routed to `file-unsupported`; in one bulk corpus 30 `.php` files contain renderable HTML that scan_file silently rejects. Fix: when a `.php` file contains substantial HTML outside of `<?php ?>` blocks, strip the PHP islands and route the remainder through the HTML parser; alternatively add `.php` to the unsupported-with-context warning so the agent knows real HTML was dropped. Per AI-first doctrine "Routing skips that drop content are the symmetric twin of suppression."
- [ ] **Q9-PARSER-XHTML-MKDN-SILENTLY-DROPPED** `.xhtml` is rejected as `file-unsupported` while sibling `.html` is accepted; `.mkdn` is rejected while `.markdown` is accepted. Both are equivalent input shapes the user could plausibly want scanned. Recurring across static-site corpus. Fix: route `.xhtml` through the HTML parser (XML-serialized HTML), `.mkdn` through the markdown parser, OR list both explicitly in `extensions_skipped_no_parser` with a sentinel reason rather than silently dropping.
- [ ] **Q9-MAP-SOURCEMAP-SILENT-EXCLUSION** `.map` sourcemap files (48 in one CSS-framework corpus) are silently dropped — not in `filesByExtension`, not in `extensions_skipped_no_parser`. Sourcemap exclusion is conventionally correct but should be declared as a structured warning so the agent can audit. Fix: add `sourcemap_files_excluded` warning code with `count` + `topPaths` + the exclusion rationale.
- [ ] **Q9-MIN-LINE-HEURISTIC-MISSES-MIN-JS-BUNDLES** `.min.js` single-line megabundles (e.g. `bootstrap.bundle.min.js` shipped in a CSS-framework corpus' `dist/`) escape `scannedBuildArtifacts` classification because the min-line predicate appears scoped to text/SVG only. Fix: extend the single-long-line classifier to `.js` files (one-long-line + size threshold + `.min.` filename token). Pairs with Q9-VENDOR-COPYRIGHT-BANNER-CLASSIFICATION (sibling vendor-detection axis).
- [ ] **Q9-VENDOR-COPYRIGHT-BANNER-CLASSIFICATION** Unminified vendor bundles with `/*!` copyright banners (Bootstrap CSS, jQuery 1.x, Modernizr, jquery-scrolltofixed) escape `scannedBuildArtifacts` classification — no `.min.` infix, but the file head is a `/*! Library vN.M Copyright/Released under … */` banner. Recurring across 2 corpora; in bulk corpora drives 150+ contrast findings emitted against vendor stylesheets. Fix: classify as build artifact when first N bytes contain `/*!` banner with `Copyright|License|Released under|MIT|Apache|GPL` tokens. suggest_fix on these has no awareness it's editing vendor.
- [ ] **Q9-SCAN-FILE-MIN-CSS-NO-CLASSIFICATION** `scan_file` on `.min.css` produces findings WITH NO `scannedBuildArtifacts` classification or `scanned_minified_file` warning, even though `scan_project` on the same file flags it as a build artifact. Cross-tool routing inconsistency. Fix: scan_file must apply the same min-line classifier and emit the same warnings as scan_project on identical input. Per AI-first doctrine "Cross-surface count invariant" (warning-telemetry analogue).
- [ ] **Q9-LINKED-MIN-CSS-NOT-CONSULTED-FROM-HTML** `.html` files linking `.min.css` are not transitively pulled into contrast-rule resolution (CSS-framework corpus: 14 `.html` files link bootstrap.min.css; HTML scan does not consult the linked sheet for token resolution). No warning emitted that the linked stylesheet was not consulted. Fix: emit `linked_stylesheet_not_resolved_for_contrast` warning naming the unresolved `<link href>` paths so the agent can scope a follow-up; deferring full resolution is acceptable, silent omission is not.
- [ ] **Q9-PARSER-BAILED-NON-JSX-IN-TSX-WARNING-ABSENT** Doctrine names `parser_bailed_on_non_jsx_in_tsx_route` as the canonical telemetry code but no warning fires when `.js` is routed through the tsx parser cleanly (no bail). Vanilla-JS corpora confirm `parseModeByExtension['.js'] === 'tsx'` with no warning emission. Fix: emit the warning whenever a `.js` file successfully parses via the tsx route — the agent can then decide whether to trust the result or scope around it. Telemetry-only; no behavioral change. Per AI-first doctrine "Routing skips that drop content are the symmetric twin of suppression."
- [ ] **Q9-SCSS-UNRESOLVED-VARIABLES-NO-COVERAGE-DOWNGRADE** `scss_unresolved_variables.files` (31 `.scss` files in one CSS-framework corpus) does NOT propagate to per-rule `coverageConfidence` downgrades for color-token / focus-token-driven rules (`contrast/*`, `focus/outline-visible`). Fix: when a file appears in `scss_unresolved_variables.files`, downgrade `perRuleCoverage[contrast/*][file].coverageConfidence` to `"medium"` with `reason: "scss_unresolved_variables"`. Same shape as parse-failed propagation. Per AI-first doctrine "Parser-failure invalidates per-file confidence" (extends to value-resolution failure).

### Per-rule / per-finding confidence drift

- [ ] **Q9-PERRULECOVERAGE-PARSE-ERROR-PER-FILE-NOT-CORPUSWIDE** A single parse-error file (1 of 429 in static-site corpus, 0.23%) blanket-degrades EVERY rule's per-rule confidence to `"low"` with `reason: "file-parse-error"`, regardless of which file each rule actually ran on; per-finding confidence on emitted rules stays `"high"/"error"`. The corpus-wide degradation is dishonest in two directions at once — over-pessimistic on rules that didn't touch the bad file, under-honest on rules that did. Fix: per-rule confidence must be per-rule-per-file (`perRuleCoverage[r].byFile[path].confidence`), not corpus-aggregated; the response-level scalar should fold from those.
- [ ] **Q9-META-PERRULECOVERAGE-DROPPED-NO-SENTINEL** When `meta.perRuleCoverage[]` is silently dropped under `response_meta_truncated`, the agent cannot distinguish "no per-rule coverage exists" from "meta was clipped." CSS-framework corpus observed. Fix: when a meta sub-field is dropped under truncation, replace it with a sentinel (`{truncated: true, fieldDroppedReason: "..."}` or pair with `warningsDetails.response_meta_truncated.fields: ["perRuleCoverage[]"]`) so absence is visible. Pairs with Q8-RESPONSE-META-TRUNCATED-FIELD-DETAIL (the warning-detail axis is open already; this is the in-place sentinel axis).
- [ ] **Q9-FRAGMENT-CLASSIFICATION-CROSS-SURFACE-DRIFT** `scan_file` returns `fragmentFiles[]: true` on `_includes/header.html` etc.; `scan_project` on the same files returns `fragmentFiles[]: false` (or omits the file). Recurring across CSS-framework + static-site corpora. Same root invariant as cross-surface count drift. Fix: shared fragment-classifier helper consumed by both surfaces; integration test pinning equality on identical input. Per AI-first doctrine "Cross-surface count invariant."

### Cross-surface count + warning drift

- [ ] **Q9-PARSE-COVERAGE-FILE-RESHUFFLE-BULK-ONLY** Bulk corpus shows `parseErrorFileCount` and `partialParseFileCount` reshuffling between buckets across surfaces (scan_project: 100/105 ; checklist+coverage: 92/113) with totals matching (205==205) — files migrate between bucket labels even though the union is stable. Narrow-scope agrees. Fix: shared parse-coverage helper consumed by both surfaces; integration test pinning per-bucket equality on the same cwd. Per AI-first doctrine "Cross-surface count invariant" (per-bucket axis).
- [ ] **Q9-COUNTSBYSURFACE-3-WAY-DISAGREEMENT** `scan_project.meta.countsBySurface` ships three disagreeing finding totals (`{plan:85, perRuleCoverage:86, filesSurface:85}`) within ONE response on a vanilla corpus. The 3-way variant of the worst-case 4-way spread the doctrine names. No narration of which is canonical. Fix: either reconcile the three (a single shared finding-count helper) or annotate each entry with what it includes (`{plan: {count, includes}, perRuleCoverage: {count, includes}, filesSurface: {count, includes}}`). Pairs with Q8b-COUNTSBYSURFACE-MULTI-DISAGREEMENT-HEADLINE (open, 4-way axis); this is the 3-way recurrence.
- [ ] **Q9-BULK-WARNINGS-NOT-CROSS-SURFACE** `scanned_build_artifacts_present` / `scanned_minified_file` / `bulk_catalog_detected` only ship on `scan_project`; `checklist` and `coverage` on the identical bulk-vendor cwd produce neither warning nor `warningsDetails` entry. Agent calling `checklist` first on a bulk corpus has zero signal that 996 minified files exist. Fix: every project-rooted tool emits the same scan-confidence warning telemetry on identical cwd. Per AI-first doctrine, this is the warning-telemetry analogue of the cross-surface count invariant — worth promoting to a sub-rule of that bullet (see Q9-DOCTRINE-CROSS-SURFACE-WARNING-INVARIANT).
- [ ] **Q9-COVERAGE-CRITERIA-AUTOMATABLE-DRIFTS-NARROW-VS-BULK** `coverage.criteriaAutomatable` reports 32 at narrow scope vs 34 at bulk on the same standard/level. Should be standard-fixed metadata, not corpus-derived. Fix: route `criteriaAutomatable` from the standard-module registry, not from `(rules ∩ files)` evaluation; integration test pinning equality across narrow/bulk on the same standard.
- [ ] **Q9-SCAN-PROJECT-OMITS-PARSE-COVERAGE-COUNTERS** `scan_project` omits `parseErrorFileCount` / `partialParseFileCount` / `fragmentFileCount` entirely while `coverage` exposes them. Agent reading `scan_project` alone cannot tell whether 0 parse errors or telemetry-not-collected — same shape as the doctrine `Zero-output success is ambiguous failure` ambiguity but at the field level. Fix: either always populate the three counters on `scan_project.meta` (defaulting to `0` when telemetry was collected) or omit-when-meaningful with conditional spread + a `warnings: ["parse_coverage_telemetry_skipped"]` companion; ambiguous absence is not acceptable. Per AI-first doctrine "Ambiguous field shapes are dishonest."
- [ ] **Q9-DUPLICATE-ID-AND-CRITERIONID-AFTER-RENAME** Every entry in `manualWithCandidates[]` / `untargetedCriteriaList[]` / `likelyIrrelevantCriteria[]` / `failingAutomatedCriteria[]` carries both `id` AND `criterionId` equal — the `deprecated_field_id_renamed_criterionId` warning fires but every entry duplicates. Recurring across corpora; inflates payload ~6KB on bulk. Fix: drop the deprecated field at the alias-removal version (consistent with semver alias contract); short-term, dedupe at serialization with a single conditional spread.
- [ ] **Q9-RULESBYEXTENSION-DUP-PAYLOAD** `coverage.rulesByExtension` and `coverage.rulesFiredByExtension` ship as identical 6-10KB payloads after rename — both populated identically; deprecated_field warning fires but duplicate ships. Same shape as Q9-DUPLICATE-ID-AND-CRITERIONID-AFTER-RENAME at a different field. Fix: drop the deprecated field at alias-removal; short-term dedupe at serialization.
- [ ] **Q9-STALE-MCP-SUBPROCESS-WARNING-EVERY-RESPONSE** `stale_mcp_subprocess` warning ships on every response (vanilla corpus); agent cannot tell trust impact — the field is self-diagnostic about test-environment freshness, not about the user's codebase. Fix: when the dist-staleness gate (V1-MCP-DIST-STALE-CI-GATE, shipped) confirms freshness, drop the warning; alternatively rename to `mcp_subprocess_freshness_check_needed` so the field names what it actually signals. Per AI-first doctrine "Surface, don't suppress" — but a warning that fires in 100% of responses carries no information.
- [ ] **Q9-OVERSIZE-MITIGATION-DOES-NOT-ENGAGE-PRE-SERIALIZATION** Bulk-corpus 101836-char response evaporated at host transport boundary; the tool's internal mitigation (clip files, set `truncated: true`) didn't engage because it ran post-clip and the post-truncation payload still exceeded the host cap. Fix: add a pre-serialization size estimator; when the estimate crosses the sentinel (e.g. 24KB), fall back to the minimum-honest envelope (`plan + meta + warnings: ["response_dropped_files_oversize"] + nextStep`) BEFORE serialization, dropping `files[]` rather than the whole response. Per AI-first doctrine "Oversize-success is ambiguous failure" — extends that bullet's mitigation guidance to the pre-serialization site.
- [~] **Q9-VERIFYINSOURCE-COMPOSITE-HEADLINE-DISHONESTY** `plan.fixesByClass.verifyInSource: 254` ships next to `plan.actionableManualItems: 7` (vanilla corpus); `verifyInSource` sums all rule emissions whose fix lane is guidance-only — agent reading the headline budgets against 254 not the ~50 grounded checklist totalCandidates. Same shape as the documented `fixesByClass.mechanical:266` vs `safeEditsAvailable:14` case (closed by deletion). Fix: verify `verifyInSource` measures one concept; if it sums actionable-with-file-line + non-actionable-prose, split into two siblings (`verifyInSourceGrounded` + `verifyInSourcePromptOnly`) and stop summing. Per AI-first doctrine "Composite headline counts are dishonest" worked precedent. **classification_mismatch_premise_false (verified by general-purpose specialist 2026-04-26):** the proposed Grounded/PromptOnly split keys on fixPaths.primary.edit presence — the exact payload-availability axis the deleted plan.safeEditsAvailable composite measured (closed by Q-SHARED-SAFE-EDITS-VS-MECHANICAL-DISAGREEMENT, CHANGELOG.md line 50). Re-introducing it under verifyInSource{Grounded,PromptOnly} would re-create the dishonest shape the doctrine deleted: two siblings keyed off different axes (rule-demanded lane vs. payload presence) framed as 'how much verifyInSource work.' Premise that verifyInSource 'sums all rule emissions whose fix lane is guidance-only' is also factually inaccurate — it sums emissions whose rule declares fixClass: 'verify-in-source', including the 5/69 rules that DO ship inline fixPaths.primary.edit (lang-attribute, button-name, inline-display-none-on-focusable, expanded-on-disclosure, hidden-focus). fixesByClass answers 'which remediation lane does the rule route into?' — one kind per key; payload availability is orthogonal and intentionally NOT surfaced after the safeEditsAvailable deletion. Closure: TSDoc enrichment on FixesByClass.verifyInSource pre-empting the 'apply-fix can act on each' misreading is the durable answer if any closure is needed.

### suggest_fix shape + correctness bugs

- [ ] **Q9-SUGGEST-FIX-MISSING-ALTERNATIVES-ARRAY** `suggest_fix` returns `kind: "guidance"` / `spec_restatement` with NO `alternatives` array; schema promises primary+alternatives and only primary returned. Explanation duplicates the rule's own `message` verbatim — no per-call enrichment. Recurring across CSS-framework + static-site corpora. Fix: when `kind === "guidance"`, populate `alternatives` with at least one per-call enrichment shape (suppression pragma form, citation link, "verify by reading" prompt) so the slot isn't a phantom; OR omit the field via conditional spread when no alternatives exist. Per AI-first doctrine "Ambiguous field shapes are dishonest."
- [ ] **Q9-SUGGEST-FIX-LINE-DRIFT-VS-SCAN** Scan finds `keyboard/handler-missing` at file `X` line `N`; `suggest_fix({ruleId, file: X, line: N})` returns `"No violation for keyboard/handler-missing at line N."` Same scan, same response, same predicate — disagrees on whether the finding exists. Vanilla corpus. Fix: suggest_fix's lookup predicate must match the rule's emission predicate exactly (likely a normalization mismatch on line number — 0-indexed vs 1-indexed, or post-strip vs pre-strip line). Add an integration test asserting every emitted finding's `(ruleId, file, line)` triple successfully resolves via suggest_fix in the same response.
- [ ] **Q9-SUGGEST-FIX-WRONG-ATTRIBUTE-ARIA-EXPANDED-VS-CONTROLS** `aria/expanded-on-disclosure` fires "missing aria-expanded"; suggest_fix returns "Add aria-controls" — rule predicate name and fix attribute don't agree. Element already carries `aria-expanded="false"` in some cases. CSS-framework corpus, dropdown trigger. Fix: assert rule emission and suggested fix attribute name match for the disclosure-attribute family (`aria-expanded` / `aria-controls` / `aria-haspopup`); when the rule fires on the absence of `aria-expanded`, the suggested fix must add `aria-expanded`, not `aria-controls`.
- [ ] **Q9-SUGGEST-FIX-NESTED-INTERACTIVE-A-TO-BUTTON-IGNORES-PARENT** `suggest_fix` recommends changing `<a>` to `<button>` inside `<ul class="dropdown-menu">` (or any ancestor whose role contract requires anchor-shaped descendants). Replacing the anchor would break documented dropdown structure and ARIA semantics. CSS-framework corpus. Fix: before recommending an element-tag swap, walk ancestors and gate the suggestion on absence of a structural-ancestor constraint (`<ul role="menu">`, `<select>`, `<datalist>`, `<table>` etc.); when a constraint exists, surface it in the reason and propose the within-constraint alternative.
- [ ] **Q9-SUGGEST-FIX-NATIVE-BUTTON-ADDS-KEYDOWN-HANDLER** `keyboard/handler-missing` FP on `const btn = document.createElement('button'); btn.addEventListener('click', ...)`; suggest_fix recommends adding a keydown handler with `preventDefault` on Space — would break native Space activation and introduces a NEW violation. Vanilla corpus. Fix: suggest_fix must check whether the addEventListener receiver resolves to a native interactive element (button, a[href], input, select, textarea); when it does, refuse to recommend a keyboard-handler addition and either suggest dismissal-via-pragma or surface "no fix needed — native element handles keyboard" as the primary path. Pairs with Q9-RULE-KEYBOARD-HANDLER-FP-NATIVE-BUTTON-CREATE-ELEMENT (rule-side closure).
- [ ] **Q9-SUGGEST-FIX-PLACEHOLDER-LABEL-IGNORES-VISIBLE-LABEL** `forms/labels-required` fires; suggest_fix uses placeholder `<label for="field">Label</label>` instead of the visible adjacent `<label>Email</label>` already present in the same DOM subtree. Vanilla corpus, multiple form-input demos. Fix: when suggesting `for=` wiring, scan adjacent siblings (within parent + immediate-prev/immediate-next) for an existing `<label>` element with usable text content and use that text in the suggested fix; reserve generic "Label" placeholder for the case where no visible label exists.
- [ ] **Q9-SUGGEST-FIX-VENDOR-CONTRAST-RECOLOR** `contrast/minimum` fires on vendor `bootstrap.css`; suggest_fix recommends recoloring `.text-muted` in the unminified vendor stylesheet. No awareness the file is third-party. Bulk corpus. Fix: gate suggest_fix on `meta.scannedBuildArtifacts` membership — when the target file is classified as a build artifact, return guidance shaped as "this file is third-party; consider overriding the rule in your own stylesheet OR upgrading the vendor library" rather than a direct edit. Pairs with Q9-VENDOR-COPYRIGHT-BANNER-CLASSIFICATION (vendor detection axis) — fix follows once classification is honest.
- [ ] **Q9-SUGGEST-FIX-WRONG-FOR-TEMPLATE-DIRECTIVE-CONTENT** `semantics/empty-heading` on `<h4>{{ section.title }}</h4>` produces suggest_fix "fill `<h4>` with primary section title" (already filled via the template binding); `parsing/duplicate-id` on a markdown fragment suggests "Add id=output" when `#output` is generated by the markdown adapter from a later `## Output` heading. Static-site corpus, 6+ cases. Fix: suggest_fix must detect templating-directive presence in the target node (`{{ }}`, `{% %}`, `${ }`, `<%= %>`, ERB, JSP) and reframe the primary path as "verify the binding resolves to non-empty text at render time" rather than "hard-code a value." Pairs with the existing template-aware reason-text rules on `semantics/empty-heading`.

### Heuristic-mislabeled meta sub-fields

- [ ] **Q9-MINIFIED-LABEL-MISLABELS-SVG-FONTS** `scanned_minified_file` mislabels SVG fonts (FontAwesome-style: hand-authored vector glyph paths with long lines). Recurring across 3 corpora; bulk corpus 122/996 minified-flagged files (12%) are SVG fonts. Fix: refine heuristic — SVG content with `<font>` element, `<glyph>` elements, or pretty-printed line-count > 100 and authored copyright header should NOT be classified `"minified"`. Per AI-first doctrine "Heuristic-mislabeled meta sub-fields are dishonest." Pairs with Q8-SCANNED-BUILD-ARTIFACTS-MINIFIED-SVG-LABEL (the inline-SVG-extension carve-out) — this is the SVG-FONT-specific recurrence.
- [ ] **Q9-MINIFIED-LABEL-MISLABELS-LONG-LINE-SCSS** `scannedBuildArtifacts[].reason: "minified"` (or `likely-vendored-data-url-css`) fires on hand-authored 1350-line SCSS files (e.g. `_style.scss` opening with `/* Base */` block and a single inline `data:image/...` marker triggering the data-url heuristic). CSS-framework corpus. Fix: require token-cooccurrence (data-url + line-stats + sourcemap-sibling-presence) instead of any single-marker firing; surface the underlying signals (`dataUrlByteCount`, `medianLineLength`, `sourcemapSiblingExists`) as additive evidence the agent can read without the deterministic-sounding label. Pairs with Q8-SCANNED-BUILD-ARTIFACTS-DATA-URL-GRADIENT-LABEL (open, the gradient-token axis) and Q8b-MINIFIED-LABEL-NEEDS-DISCRIMINATOR-AXIS (open, the discriminator axis).
- [ ] **Q9-SCANNED-BUILD-ARTIFACTS-TOP-N-WITH-REASONS-AT-DEFAULT** `scanned_build_artifacts_present` ships `{count: 997, topPath: "...jquery-1.10.2.js"}` with no array, no per-file reason at default verbosity; `verboseMeta: true` unlocks the array but default-verbosity callers cannot audit reason tokens. Bulk corpus. Fix: ship top-10 `{path, reason}` inline at default verbosity — sufficient for the agent to dismiss "vendor-and-vendor-only" cases in one read; the long-tail array stays gated behind verboseMeta. Pairs with V1-TOOL-VERBOSE-META-INVERTED-DEFAULT (open, the verbose-default inversion).
- [ ] **Q9-TEMPLATE-DIRECTIVES-LIQUID-AS-HANDLEBARS** `templateDirectivesFound: ["handlebars-or-mustache"]` fires on Liquid `{{ }}` interpolation (which shares brace syntax with handlebars/mustache but is a distinct dialect). Static-site corpus. Fix: token co-occurrence required (`{% ... %}` Liquid tag tokens vs `{{# ... }}` handlebars block-helper tokens) before stamping a dialect label; absent disambiguating evidence, surface `templateInterpolationFound: { token: "{{x}}", count: N }` and let the agent disambiguate. Per AI-first doctrine "Heuristic-mislabeled meta sub-fields are dishonest." Pairs with V1-TEMPLATE-CLASSIFIER-LIQUID-PIPE-FILTER-EVIDENCE (closed, the pipe-evidence axis) and Q8-TEMPLATE-DIRECTIVES-HANDLEBARS-OR-MUSTACHE-DIALECT (open).

### Reason-severity mismatches (recurring)

- [ ] **Q9-TOOLTIP-DISMISSABLE-RUNTIME-CONCESSION-AT-WARNING** `tooltip/dismissable` ships at `severity: "warning"` while reason text reads `"the native title may be an input to a runtime ARIA-aware widget. Verify the runtime behavior at the call site before fixing."` — concedes uncertainty; severity claims certainty. Recurring across 3 corpora. Closure: move to review-candidate, OR downgrade to `info`, OR rewrite reason to surface what's actually still in question. Per AI-first doctrine "Reason text and severity must agree" (conceded-uncertainty extension). Pairs with Q8b-TOOLTIP-DISMISSABLE-JS-ENHANCER-DOWNGRADE (open, sibling axis).
- [ ] **Q9-MOTION-PAUSE-STOP-RUNTIME-CONCESSION-AT-WARNING** `motion/pause-stop-hide` reason concedes `"verify keyboard focus + announcement carry pause semantics before dismissing"` while severity stays `"warning"`. Recurring. Closure: move to review-candidate. Per AI-first doctrine "Reason text and severity must agree." Pairs with Q8b-MOTION-PAUSE-STOP-USER-TRIGGERED-CITATION (open, the citation-honesty axis).
- [ ] **Q9-COLOR-MEANING-VISUAL-STYLE-AT-ERROR** `color/meaning-by-color-only` emits at `error` on `<button class="btn-success">Search</button>` and `<button class="btn-danger">Take this action</button>` — visual class names without status semantics; in the second case, the parent `<div role="alert" class="alert alert-danger">` already conveys danger context. Recurring across 4 corpora (2-5 cases per corpus). Closure: rewrite reason to "color may still be the *sole* cue for users with dark/high-contrast themes that override class colors" OR gate predicate on absence of parent live-region / role=alert announcement. Per AI-first doctrine "Reason text and severity must agree."
- [ ] **Q9-EMPTY-HEADING-TEMPLATE-EXPRESSION-AT-ERROR** `semantics/empty-heading` reason: `"the only rendered content was a template expression (Liquid/Jinja/ERB) stripped by the parser - verify the expression resolves to non-empty text at render time"` ships at `severity: "error"` `confidence: "high"`. Static-site corpus, 6 cases. Closure: downgrade severity to `suggestion` OR move to review-candidate when only-child of a heading is a template expression. Per AI-first doctrine "Reason text and severity must agree."
- [ ] **Q9-HEADING-HIERARCHY-MARKDOWN-STRIPPED-AT-WARNING** `semantics/heading-hierarchy` reason: `"Markdown ATX-syntax headings were stripped by the markdown adapter before this rule ran...The rendered document outline may be well-formed once the ATX headings come back"` ships at `severity: "warning"`. Predicate self-concedes. Closure: move to review-candidate. Per AI-first doctrine "Reason text and severity must agree."
- [ ] **Q9-IN-PAGE-LINK-FRAGMENT-FRAGMENT-FILE-CONCESSION** `navigation/in-page-link-fragment-missing` reason: `"anchor links to fragment #X but no element with that id exists in this document. Verify the id is added or correct the href."` ships at `severity: "warning"` even when the host file is fragment-classified (`_includes/_partials/`) and the id may be supplied by the parent layout. Closure: move to review-candidate when host file is fragment-classified (composes with Q9-FRAGMENT-CLASSIFICATION-NO-PER-RULE-DOWNGRADE).
- [ ] **Q9-EXPANDED-ON-DISCLOSURE-SR-ONLY-CHILD-CONCESSION** `aria/expanded-on-disclosure` reason: `"(note: element has a visually-hidden text child (.sr-only) — if that is the disclosure label, verify the accessible name is complete..."` ships at `severity: "error"`. Bulk corpus. Closure: rewrite reason to surface what is *actually* still in question, OR move to review-candidate. Per AI-first doctrine "Reason text and severity must agree."

### Rule predicate gaps (false negatives)

- [ ] **Q9-RULE-FOCUS-OUTLINE-COMPOUND-SELECTOR-BRANCH** `focus/outline-visible` misses `:focus` selectors that share a compound rule with `:hover` (`&:hover, &:focus { outline: 0 }`). CSS-framework corpus, `_carousel.scss:109`. Fix: split compound selectors and evaluate each branch independently for the rule's `:focus`-zeroing predicate. Satisfies wcag22:2.4.7. Owner: `rule-implementer`.
- [ ] **Q9-RULE-FOCUS-OUTLINE-BOXSHADOW-VAR-UNRESOLVED** `focus/outline-visible` trusts `box-shadow: 0 0 0 var(--focus-width)` as a sufficient replacement focus indicator even when the CSS variable is unresolved (no token resolution available). CSS-framework corpus, `_navbar.scss:166`. Fix: when a `box-shadow` value contains an unresolved `var(...)`, degrade `coverageConfidence` for the rule on that file to `medium` with `reason: "focus_replacement_uses_unresolved_css_var"`; do not silently credit the unresolved var as a valid replacement. Per AI-first doctrine "Heuristic emission is the symmetric twin of heuristic suppression" (over-trusting evidence is the same predicate-strength mistake).
- [ ] **Q9-RULE-POPOVER-TOOLTIP-TRIGGER-DISCLOSURE** No rule fires on popover/tooltip triggers (`data-bs-toggle="popover"`, `data-bs-toggle="tooltip"`, and other component-library toggle attributes) missing `aria-haspopup` / `aria-expanded`. `aria/expanded-on-disclosure` is gated to `data-bs-toggle="dropdown"` / `"collapse"`. CSS-framework corpus. Fix: extend the disclosure predicate or add a new rule `aria/popover-trigger-missing-haspopup` covering the tooltip/popover toggle vocabulary. Satisfies wcag22:4.1.2.
- [ ] **Q9-RULE-ARIA-LIVE-MISSING-ON-INNERHTML-TARGET** No rule pairs `innerHTML` / `textContent` mutations in `setInterval` / `setTimeout` / event handlers with the target element's missing `aria-live` / `role="status"` / `role="alert"`. Vanilla corpus, `insect-catch-game` updates `#score` / `#time` via `innerHTML` in `setInterval` but neither node has live-region semantics. Fix: add a rule targeting `<el id=X>` where sibling JS contains `document.getElementById(X).innerHTML =` (or `.textContent =`) inside a recurring scheduler; require `aria-live` / `role=status` / `<output>` on the target. Satisfies wcag22:4.1.3 (Status Messages). Pairs with Q9-PARSER-INNERHTML-TEMPLATE-LITERAL-ISLANDS-IN-JS (parser-recursion axis required for cross-context resolution).
- [ ] **Q9-RULE-CROSS-FILE-CLICK-HANDLER-DYNAMIC-NON-INTERACTIVE** No rule detects clickable-div pattern when the click target is created dynamically and wired in a separate JS context (e.g. `insect.addEventListener('click', catchInsect)` on dynamically-created `<div class="insect">`). Vanilla corpus. Fix: cross-check JS-attached click target's resolved kind in the rendered markup (or via cross-file resolution), not only the static-text absence-of-keyboard-sibling signal. Pairs with Q8-RULE-CROSS-FILE-CLICK-HANDLER-NON-INTERACTIVE (open, the static-DOM axis); this is the dynamic-creation extension. Satisfies wcag22:2.1.1 + 4.1.2.
- [ ] **Q9-FINDER-2.5.7-DRAG-EVENTS** No finder fires on SC 2.5.7 (Dragging Movements) for elements wiring `dragstart` / `dragenter` / `dragover` / `drop` event listeners. Vanilla corpus, `drag-n-drop` exercise has 6 native HTML5 drag handlers; SC 2.5.7 absent from `items` / `untargetedCriteriaList` / `likelyIrrelevantCriteria`. Fix: add a review-candidate finder for SC 2.5.7 detecting `addEventListener('dragstart'|'drop'|...)` calls; reason frames the question (`single-pointer alternative — verify a click/keyboard alternative exists`). Satisfies wcag22:2.5.7.
- [ ] **Q9-FINDER-2.5.1-MOUSE-ONLY-PATTERNS** No finder fires on SC 2.5.1 (Pointer Gestures) for `mousedown` / `mousemove` / `mouseup` patterns lacking `touchstart` / `pointerdown` fallbacks. Vanilla corpus, `drawing-app` uses mouse-only events. SC 2.5.1 absent from candidates. Fix: add a review-candidate finder detecting mouse-event listener patterns without sibling pointer/touch listeners; reason frames the question (`pointer-event compatibility — verify touch/pointer fallback exists`). Satisfies wcag22:2.5.1.

### Rule predicate too-broad (false positives)

- [ ] **Q9-RULE-KEYBOARD-HANDLER-FP-CROSS-FILE-NATIVE-BUTTON** `keyboard/handler-missing` FP on cross-file resolved native button (e.g. `document.querySelector('#send')` resolves to `<button id="send">` in sibling `.html`). Confidence:high + severity:error. 4/4 cases tested in vanilla corpus. Per Q9-PERFINDING-COULDBEWRONG-EMPTY-WHEN-PERRULE-DEGRADED — paired drift class. Fix: when cross-file listener resolution is degraded (per `perRuleCoverage`), per-finding emissions on candidate native-element targets must downgrade `confidence` to `medium` and surface the limitation in `couldBeWrongBecause`.
- [ ] **Q9-RULE-CROSS-TEMPLATE-3.2.3-HEURISTIC** Cross-template `wcag22:3.2.3` heuristic treats N unrelated sub-template directories as one site (bulk corpus: 174 sub-template-dirs evaluated jointly). The reason itself flags the heuristic. Fix: gate cross-template comparison on a configured `processes: []` config primitive OR a sibling-detection threshold (max N templates per cross-comparison), and either suppress the predicate or move to review-candidate when the heuristic is unsafe.

### nextStep routing dishonesty

- [ ] **Q9-NEXTSTEP-CWD-ECHOES-CALLER-AFTER-DROP** Bulk-scope `nextStepStructured.args.cwd` echoes the caller's `cwd` unchanged after `response_dropped_files_oversize`. Prose says `"narrower scope"`; structured args identical. Recurring across multiple bulk-scope tests. Fix: when `response_dropped_files_oversize` fires, populate `nextStepStructured.args` with a narrower target — `cwd: dirname(topRules[0].topFile)` OR `restrictToPaths: [<top-non-vendor-subdir>]` — so the structured next-call differs from the failing call. Per AI-first doctrine "NextStep prioritization on truncated/bulk responses must avoid first-by-filename routing" (extends to "must not echo the failing scope verbatim").
- [ ] **Q9-NEXTSTEP-FIRST-BY-FILENAME-RECURRENCE** `nextStep` defaults to picking the alphabetically-first finding when truncated or bulk-vendor-heavy: routes to `_project_starter_/` scaffold (vanilla corpus), `js/tests/integration/` test fixture (CSS-framework), vendor stylesheet path already in `scannedBuildArtifacts` (bulk corpus). Recurring across 3 corpora. The doctrine bullet already names this; this item promotes it to a guard test. Fix: integration test asserting that on a synthetic corpus mixing scaffold + fixture + vendor + real-source, `nextStep` selects from real-source first; failing the test fails CI. Per AI-first doctrine "NextStep prioritization on truncated/bulk responses must avoid first-by-filename routing."
- [ ] **Q9-TRUNCATED-FILES-DROPPED-COUNT-WARNING** Truncated responses drop file-level findings without a per-rule index of what was dropped. Static-site corpus: `warnings: ["response_meta_truncated"]` ships but no `truncated_files_dropped_count` code names how many files lost coverage; topline counts disagree with returned file findings, forcing scan_file follow-ups. Fix: add a structured `truncated_files_dropped` warning naming `{droppedFileCount, ruleFamiliesAffected: ["..."], topDroppedRules: [{ruleId, droppedCount}]}` so the agent can decide whether to re-scope or re-call with `verboseMeta: true`. Per AI-first doctrine "Oversize-success is ambiguous failure" (file-level analogue).

### Templating-driven false positives

- [ ] **Q9-LINK-DESCRIPTIVE-TEXT-TEMPLATE-EXPRESSION-CONTENT** `navigation/link-descriptive-text` fires on `<a>{{ post.title }}</a>` (template-directive-only content). Same shape as Q9-EMPTY-HEADING-TEMPLATE-EXPRESSION-CONTENT, recurring across 4 files in one static-site corpus. Fix: when the anchor's only child is a template directive, promote to review-candidate with reason framing the binding-resolves question. Per AI-first doctrine — cross-rule consistency with the `semantics/empty-heading` template-aware fix. Pairs with Q8-LINK-DESCRIPTIVE-TEXT-TEMPLATE-AWARE-REASON (open, the reason-text axis).

### Pragma extension mismatch

- [ ] **Q9-SUGGESTWITH-PER-EXTENSION-PRAGMA-FORM** `suggestWith` ships only the `<!-- ra11y-disable -->` HTML-comment pragma form; candidates emit on `.scss` / `.css` / `.js` files where HTML-comment syntax is invalid and would corrupt source. Recurring across 3 corpora. Fix: extend `suggestWith` to ship per-extension pragma forms — `/* ra11y-disable */` for `.css` / `.scss` / `.sass` / `.less` / `.js` / `.ts`, `{/* ra11y-disable */}` for `.jsx` / `.tsx` / `.mdx`, `<!-- ra11y-disable -->` for `.html` / `.htm` / `.xhtml` / `.markdown` / `.md`. Pairs with Q8c-FIX-DESCRIPTION-MARKDOWN-FOR-MD-FILES (open, markdown-axis already named) — this is the across-file-extensions superset.

## Track Q10 — Multi-corpus AI-first sweep (2026-04-26 round, 4 corpora × 5 angles)

Field-test follow-ups from a 20-agent multi-corpus probe (CSS framework + static-site generator + vanilla mini-projects + bulk template gallery). Each item below is **net-new** against Q9; the substantial fraction of this round's findings that recurred without behavior change against existing Q9 closure framings folded as recurrence counts under the Q9 "2026-04-26 round recurrences" subsection above. Items are bucketed by failure class.

### Parser routing + coverage gaps

- [ ] **Q10-PARSER-ASTRO-ROUTED-AS-HTML-FRONTMATTER-BAILS** `.astro` files routed to the html parser bail with `Stray closing tag at top level` because the `---`-delimited TS frontmatter contract is not consumable by the html parser; the JSX-flavoured tsx parser handles them. Document-shaped rules silently degrade against a malformed AST. Fix: add `.astro` to the tsx route table (or a dedicated astro pre-strip + html route); emit `parser_route_corrected_for_extension` if the route changes. Per AI-first doctrine "Routing skips that drop content are the symmetric twin of suppression."
- [ ] **Q10-FRAGMENTFILES-CLASSIFIES-FULL-PAGE-LAYOUT-AS-FRAGMENT** Full-page layout files whose source opens with frontmatter-only-then-markup are tagged `fragmentFiles` even though they render as full HTML documents at build time; document-shaped rules silently downgrade. The fragment label is a heuristic dressed as a deterministic classification. Fix: require token co-occurrence (no `<html>` opener AND no layout-include directive AND not in a known layouts dir) before stamping the fragment label; surface the underlying signals as additive evidence. Per AI-first doctrine "Heuristic-mislabeled meta sub-fields are dishonest."
- [ ] **Q10-SCSS-PARTIALS-MISLABELED-AS-PARSE-ERROR-NOT-FRAGMENT** SCSS partials (`_*.scss` declaring `&.foo` selectors intended for inclusion in another file) ship as `parseErrorFiles` rather than fragment-classified, inflating parse-error counts and routing the agent toward "fix the parse error" instead of "this is a fragment, route around it." Fix: classify SCSS partials (`_`-prefixed basename + presence of dangling `&` parent-references) as fragments; add a `scss_partial_input` reason on per-rule confidence rather than treating the bail as a hard parse error. Per AI-first doctrine "Heuristic-mislabeled meta sub-fields are dishonest."
- [ ] **Q10-LINK-TARGET-BLANK-APPLIESTO-EXCLUDES-ERB-MD-RESIDUE** `navigation/link-target-blank-announcement` `appliesTo.fileExtensions` is hardcoded to `['.html','.htm','.tsx','.jsx']`; ERB and markdown files with embedded `<a target="_blank">` parse as html but cannot be evaluated by the rule. Fix: extend `appliesTo.fileExtensions` to every extension whose `parseModeByExtension` resolves to `html`. Per AI-first doctrine "Routing skips that drop content."
- [ ] **Q10-IE-CONDITIONAL-HTML-LANG-NOT-FLAGGED** `document/lang-attribute` silently does not fire on files whose `<html lang="...">` is declared only inside `<!--[if IE]>` conditional comments — the parser elides the conditional but the rule does not surface a `couldBeWrongBecause: ["html_element_inside_ie_conditional_comment"]`. Fix: detect the IE-conditional `<html>` pattern and either fire the rule (real false negative — assistive tech receives the bare `<html>`) or emit a finding with the cited `couldBeWrongBecause` token so the agent can investigate. Per AI-first doctrine "Routing skips that drop content."

### Per-rule / per-finding confidence drift

- [ ] **Q10-COVERAGE-PERRULECOVERAGE-EMPTY-VS-SCAN-FILE-FULL** `coverage` with `verboseMeta: true` ships an empty `perRuleCoverage[]` while `scan_file` on the same input ships ~95 rows; the doctrine's parser-failure-invalidates-confidence invariant is unauditable from the canonical confidence-dashboard tool. Fix: shared per-rule-coverage helper consumed by both surfaces; integration test pinning row-count parity on identical input. Per AI-first doctrine "Cross-surface count invariant."

### Cross-surface count + warning drift

- [ ] **Q10-PLAN-SUMMARY-SUM-VS-COUNTSBYSURFACE-DISAGREE** `plan.fixesByClass` sums to N (e.g. 14+42+4+285=345) while the same response's `meta.countsBySurface.plan` ships a different value (e.g. 359) and the `nextStep` prose echoes the lower number — three values for the same conceptual "plan-eligible findings" count in one response. Fix: derive `meta.countsBySurface.plan` from the same shared helper that drives `plan.fixesByClass`; integration test asserting `sum(plan.fixesByClass) === meta.countsBySurface.plan` on every response. Per AI-first doctrine "Composite headline counts are dishonest." Pairs with Q9-COUNTSBYSURFACE-3-WAY-DISAGREEMENT (the cross-internal-surface axis).
- [ ] **Q10-RULES-FIRED-BY-EXTENSION-MISLABELS-ELIGIBILITY** `rulesFiredByExtension['.md']` credits 96+ rules to `.md` (including `media/audio-video-no-controls`, `semantics/landmark-main`, `parsing/duplicate-id`) when no rule actually fired on the only `.md` file in the corpus (a fragment README). The label reads "fired by extension" but the values are "eligible to fire." Fix: rename to `rulesEligibleByExtension` OR ship two siblings `rulesFiredByExtension` + `rulesEligibleByExtension`; the deterministic-sounding "fired" label currently lies. Per AI-first doctrine "Heuristic-mislabeled meta sub-fields are dishonest."
- [ ] **Q10-DEPRECATION-WARNING-ASYMMETRIC-ACROSS-SURFACES** `deprecated_field_id_renamed_criterionId` ships on `coverage` but not on `checklist` despite both surfaces emitting the renamed field; the same rename should be narrated identically on every surface that touches the field. Fix: emit deprecation warnings from the shared field-emission helper, not at the surface boundary; integration test pinning warning-set parity across project-rooted tools for any field touched by `rule-aliases.ts`. Per AI-first doctrine "Cross-surface count invariant."
- [ ] **Q10-META-TRUNCATED-FIELD-NAME-RETAINED-WHEN-GUTTED** When `response_meta_truncated` fires, `meta` keeps its field name but is stripped of `analysisCoverage`, `parseErrorFileCount`, `partialParseFileCount`, `scannedBuildArtifacts[]`, `scannedMinifiedFiles[]`, `filesByExtension`, `templateDirectivesFound`, `activeNativeWrappers`, `rulesEvaluated`, `opaqueCustomComponents` — leaving 9 trivial scalars. Agent reading `meta` cannot distinguish "no scan-confidence concerns" from "meta got gutted to fit the envelope." Fix: when truncation drops meta sub-fields, replace each absent field with a `{truncated: true, reason: "..."}` sentinel OR pair with `warningsDetails.response_meta_truncated.fieldsDropped: [...]`. Per AI-first doctrine "Ambiguous field shapes are dishonest" and the new "Truncated containers must rename or sentinel" doctrine bullet (codified in this round).
- [ ] **Q10-RESPONSE-TOKEN-BUDGET-TRUNCATED-WITHOUT-TRUNCATED-FLAG** `scan_project` trips `response_token_budget_truncated` (e.g. requestedLimit 10 → effectiveLimit 1, dropping 9 files of findings) but the top-level `truncated` flag is `null`/absent; `checklist` sets `truncated: true` and offers `nextCursor` on its own per-criterion clip. Fix: `scan_project` must set `truncated: true` whenever `response_token_budget_truncated` or `response_dropped_files_oversize` fires; integration test pinning the invariant. Per AI-first doctrine "Zero-output success is ambiguous failure" (paged-success variant).
- [ ] **Q10-YML-SKIPPED-BUT-TEMPLATE-LITERAL-CO-FIRES-NO-DETAIL** `extensions_skipped_no_parser` reports `.yml` as topExtension while `template_files_parsed_as_literal` ALSO fires; the two warnings co-fire on the same files but mean different things. The `template_files_parsed_as_literal` warning has no `files[]` / `extensions[]` detail to disambiguate. Fix: ship `warningsDetails.template_files_parsed_as_literal: {files: [...], extensions: [...]}` so the agent can tell which `.yml` was skipped vs parsed-as-literal. Per AI-first doctrine "Heuristic-mislabeled meta sub-fields are dishonest."
- [ ] **Q10-DUPLICATE-CRITERIA-CANDIDATES-INFLATE-ACTIONABLE** `scan_file.reviewCandidates` lists the same `(file, line)` evidence under multiple criteria as separate items (e.g. an `<input type="password">` line emits one wcag22:1.3.6 candidate AND one wcag22:3.3.8 candidate); `checklist.items` already de-duplicates via `criteriaIds: []`. The asymmetric de-dup inflates `actionableManualItems` on `scan_file` relative to `checklist`. Fix: apply the same `criteriaIds: []`-collapsing dedup on `scan_file.reviewCandidates`; integration test pinning per-`(file,line)` candidate-count parity across surfaces. Per AI-first doctrine "Cross-surface count invariant."

### suggest_fix shape + correctness bugs

- [ ] **Q10-SUGGEST-FIX-MECHANICALINPRINCIPLE-CONTRADICTS-KIND-GUIDANCE** `suggest_fix` returns `kind: "guidance"` while shipping `meta.mechanicalInPrinciple: true` — two sibling fields under the same response answer "is a mechanical edit available" with opposite values. Recurring across 4 corpora on `document/lang-attribute`, `forms/labels-required`, `aria/icon-child-missing-aria-hidden`, `semantics/section-accessible-name-missing`, `semantics/landmark-main`, `color/meaning-by-color-only`. Fix: when `kind: "guidance"` is correct (no edit producible from static evidence alone), set `meta.mechanicalInPrinciple: false`; when `mechanicalInPrinciple` is genuinely true, the response must materialize `kind: "edit"` with `oldText`/`newText`. Per AI-first doctrine "Composite headline counts are dishonest" (per-class sub-tally must agree with per-call shape) — pairs with Q9-VERIFYINSOURCE-COMPOSITE-HEADLINE-DISHONESTY and codifies into the new "Per-call shape must agree with per-class plan tally" doctrine bullet (codified in this round).
- [ ] **Q10-SUGGEST-FIX-VERIFYCOMMAND-DUPLICATE-PROSE-VS-STRUCTURED** `suggest_fix` ships both `verifyCommand` (prose string) and `verifyCommandStructured` (object with `tool`/`args`/`verifyRuleId`). Drift between the two is silent and the agent cannot tell which is canonical — same shape as the documented triple-readout failures. Fix: drop the prose `verifyCommand` string; keep only `verifyCommandStructured`. Per AI-first doctrine "Verbose meta is signal — but de-duplicate triple readouts."
- [ ] **Q10-SUGGEST-FIX-VENDOR-REDIRECT-COMPOUNDS-MISLABEL** When `suggest_fix` is called on a finding whose file was classified as a build artifact, it returns `kind: "none"` with `vendorContext.redirectTo: "consumer-override"` instead of a fix. Combined with a heuristic mis-classification (Q9-MINIFIED-LABEL-MISLABELS-LONG-LINE-SCSS recurrence), the agent is silently denied fix help on hand-authored source. Two failure modes compound. Fix: gate vendor-redirect on a high-confidence build-artifact classifier (token co-occurrence, not single-marker) AND surface the underlying classification signals in `vendorContext` so the agent can decide whether to trust the redirect. Per AI-first doctrine "Heuristic-mislabeled meta sub-fields are dishonest."
- [ ] **Q10-PARSE-ERROR-REASON-DIRECTION-INVERTED** A `parseErrorFiles[].reason` describing layout-tail elision narrates the wrong direction: claims `</html>` is the elided closer when the file's actual content has `</html>` on its last line and the OPENING `<html>` is what's elided (supplied by an included partial). Fix: parse-error reason text must reflect which tag is actually elided; integration test on a synthetic include-pattern fixture asserting reason matches the elided side. Per AI-first doctrine "Heuristic-mislabeled meta sub-fields are dishonest" (reason-text honesty extension).

### Reason-severity mismatches

- [ ] **Q10-CANDIDATE-REASON-CONCEDES-CRITERION-MAY-NOT-APPLY-AT-PRIORITY-HIGH** Review candidates ship at `priority: "high"` while reason text concedes the criterion may not apply (`"if this is a standalone single-page file or SPA, the criterion may not apply"` for wcag22:2.4.5 candidates; `"flashing only if iteration-count is set to infinite or a value >3"` for wcag22:2.3.1 finder where the static evidence shows iteration-count=1). Same shape as "reason text and severity must agree" extended to candidate priority. Fix: when a candidate's reason concedes the predicate may not apply, downgrade `priority` to `medium` or `low`; integration test parsing reason text for hedging tokens (`may not apply`, `only if`, `verify…before`) and asserting priority degradation. Per AI-first doctrine "Reason text and severity must agree" (candidate-priority extension; codified into the new "Reason/priority/fix-description must agree across all three channels" bullet this round).
- [ ] **Q10-FIX-DESCRIPTION-CONCEDES-PREDICATE-AT-ERROR-SEVERITY** `keyboard/handler-missing` emits at `severity: "error"` while the per-finding `fix.description` literally instructs `"Cross-file check: grep the selector in your HTML to confirm the target isn't already a <button> or <a href>"` — fix description concedes the predicate may not hold; severity claims it does. Same shape as "reason text and severity must agree" applied to the fix-description text channel. Fix: when fix.description hedges with a verify-before-fixing instruction, downgrade severity to `warning` AND populate per-finding `couldBeWrongBecause` with the limitation; OR perform the cross-file check the description prescribes (the scanner already has the HTML AST). Per AI-first doctrine "Reason text and severity must agree."
- [ ] **Q10-ROLE-FROM-CLASS-ONLY-FRAGMENT-FILE-CONCEDED-UNCERTAINTY** `aria/role-from-class-only` fires at `severity: "warning"` `confidence: "medium"` on fragment markdown files containing component-library-style admonition divs (`<div class="note info">`); message asserts deterministic AT-stripping while host file is fragment+template (rendered output may carry role-via-CSS, ARIA injection, or theme wrapper). Reason concedes uncertainty (file is fragment+template); severity does not match. Fix: when host file is fragment-classified AND template directives are present, downgrade to a review candidate OR populate per-finding `couldBeWrongBecause: ["fragment_input_no_document_envelope", "template_directives_present"]`. Per AI-first doctrine "Reason text and severity must agree" (conceded-uncertainty extension).

### Rule predicate gaps (false negatives)

- [ ] **Q10-RULE-VALUE-AS-LABEL-ANTIPATTERN** No rule fires on the `<input type="text" value="Username" />` value-as-label antipattern (default `value=` used as the visible label, often cleared on focus by inline JS). The placeholder-as-label rule captures a sibling antipattern; the value-as-label variant prevalent in older templates is uncovered. Fix: add `forms/value-as-label` (or extend `forms/placeholder-as-label`) detecting `<input type="text|email|search">` elements with `value` attribute equal to a label-shaped string, no `<label>`, no `aria-label`, no `aria-labelledby`. Satisfies wcag22:1.3.1 + 4.1.2.

### Rule predicate too-broad (false positives)

- [ ] **Q10-RULE-TABLE-CAPTION-MISSING-FIRES-ON-MD-FENCED-TABLES** `semantics/table-caption-missing` fires on `<table>` elements rendered from markdown table syntax (kramdown / GFM) — markdown source has no caption mechanism in the syntax, so the rule cannot honestly establish that an authoring choice was made. Fix: when the table's source file is `.md`/`.markdown`/`.mkdn`, OR the file is in `fragmentFiles[]`, attach `couldBeWrongBecause: ["markdown_table_no_caption_syntax_in_md"]` AND downgrade per-finding confidence to `medium`. Per AI-first doctrine "Reason text and severity must agree."

### Finding-emission shape bugs

- [ ] **Q10-DUPLICATE-FINDING-IDS-ACROSS-DISTINCT-EMISSIONS** `forms/placeholder-as-label` emits 6 separate finding entries on consecutive lines that share the same `findingId` AND `groupKey`, while `forms/labels-required` on the exact same input collapses to one entry with `siblingInstances: [...]`. Identical IDs across distinct entries break consumer dedup keyed by `findingId`. Fix: assert `findingId` uniqueness across emissions in the same response; emit one entry with `siblingInstances: []` when multiple consecutive findings collapse. Per AI-first doctrine "Ambiguous field shapes are dishonest" (id-collision is the worst-case ambiguity).

### scan_file surface gaps

- [ ] **Q10-SCAN-FILE-NO-TRUNCATION-NO-OVERSIZE-PROTECTION** `scan_file` has no `limit`/`offset`/`restrictToPaths` and no oversize-success protection; a single dense HTML file produces 68k–84k character responses that blow the host token cap, returning only a transport error with no `truncated` flag, no warning, no minimum-honest envelope. Same silent failure mode as `scan_project` but worse because there's no scope-down lever. Fix: add `limit`/`offset`/`maxBytes` parameters; pre-serialization size estimator; minimum-honest envelope fallback (drop `findings[]` body, keep `meta + warnings + nextStep`) when the post-clip envelope still exceeds the host cap. Per AI-first doctrine "Oversize-success is ambiguous failure" — extends Q9-OVERSIZE-MITIGATION-DOES-NOT-ENGAGE-PRE-SERIALIZATION to the scan_file surface.

### Considered and rejected (per CLAUDE.md §1)

- **AAA→AA promotion of `navigation/link-target-blank-announcement`** — A scanner finding asked for AA-level emission of the rule (citing field evidence of unflagged `<a target="_blank">` patterns). Rejected: WCAG 3.2.5 is normatively AAA. Spec-correct closure already named in Q9-RULE-LINK-TARGET-BLANK-NEVER-FIRES — the AA-axis ask would be spec-incorrect emission and the deterministic escape hatch (source-level pragma) covers the rare case where the agent decides the rule should fire. The Q10-LINK-TARGET-BLANK-APPLIESTO-EXCLUDES-ERB-MD-RESIDUE row above is the legitimate fix (extend AAA-eligible extensions to match `parseModeByExtension`), separable from the level-axis question.

---

## Track Q11 — Multi-corpus AI-first sweep (2026-04-26 round, 4 corpora × 5 angles)

Field-test follow-ups from a 20-agent multi-corpus probe (CSS framework + static-site generator + vanilla mini-projects + bulk template gallery). Each item below is **net-new** against Q9/Q10; recurrences of already-named items folded as recurrence counts under the existing rows there. Items bucketed by failure class.

### Rule predicate gaps (false positives — parent-walk + multi-rule overlap)

- [ ] **Q11-RULE-TAB-WIDGET-PARENT-WALK-STOPS-AT-PRESENTATION** `aria/tab-widget-roles` parent-walk doesn't traverse past a `<li role="presentation">` wrapper when looking for `role="tablist"`; on the canonical WAI-ARIA tabs pattern (`<ul role="tablist"><li role="presentation"><button role="tab">…`) the rule fires "parent has no role=tablist" four times in one fixture-tree. The presentation wrapper IS the documented WAI-ARIA Authoring Practices §tabs intermediate — skipping it is the rule's job. CSS-framework corpus, recurring on every tab demo. Fix: when walking ancestors for a role-triple anchor, treat `role="presentation"` / `role="none"` as transparent — keep walking until a non-presentation ancestor surfaces. Per AI-first doctrine "Reason text and severity must agree" (the rule's reason names a parent that DOES exist if you walk through `presentation`).
- [ ] **Q11-RULE-FORMS-LABELS-REQUIRED-AND-LABEL-ADJACENT-DUAL-FIRE** `forms/labels-required` and `forms/label-adjacent-unassociated` both fire at `severity: "error"` on the same `<input>` whose visible-but-unassociated `<label>` is a structural sibling in the same `<div class="form-group">` parent. Two distinct findings for one defect; both fixable by the same single edit (add `for=`/`id=`). Bulk-template corpus, every contact-form input. Fix: when `forms/label-adjacent-unassociated` fires, suppress `forms/labels-required` on the same `(file, line)` and surface the adjacency evidence as additive context on the unassociated finding (`siblingLabelText: "Name"`). Per AI-first doctrine "Composite headline counts are dishonest" extended to per-defect rule fan-out — agent's attention budget is wasted on the duplicate.
- [ ] **Q11-RULE-EXPANDED-DISCLOSURE-FIX-CONCEDES-MAY-NOT-APPLY-AT-ERROR** `aria/expanded-on-disclosure`'s `fix.description` literally instructs `"If this control is not a disclosure trigger, the detection is wrong — suppress with a source-level pragma"` while ship at `severity: "error"`. The fix-description-channel concedes the predicate may not hold; severity claims it does. Bulk-template corpus, every navbar toggler. Fix: when fix.description hedges with "if this is not a disclosure trigger" or "suppress with a pragma," downgrade severity to `warning` AND populate per-finding `couldBeWrongBecause: ["disclosure_predicate_relies_on_class_token"]`; OR move to a review candidate. Per AI-first doctrine "Reason / priority / fix-description must agree across all three channels" (codified 2026-04-26).

### Missing rules (corpus-driven, recurring across ≥2 corpora)

- [ ] **Q11-RULE-TABLIST-ON-NON-TAB-CONTAINER** No rule fires on `role="tablist"` applied to a tabpanels container (e.g. `<div class="tab-content" role="tablist">` housing `role="tabpanel"` children). This is a real ARIA misuse — assistive tech announces a tablist but the children are panels, not tabs. CSS-framework corpus, `tab.html:35`. Fix: add `aria/tablist-on-non-tab-container` — fires when a `role="tablist"` element's children carry `role="tabpanel"` (or no `role="tab"` descendant at any depth). Satisfies wcag22:1.3.1 + 4.1.2.
- [ ] **Q11-RULE-VISUALLY-HIDDEN-ONLY-NAME-CAROUSEL-CONTROL** No rule fires on `<a href="#prev"><span class="visually-hidden">Previous</span><span aria-hidden="true" class="icon-prev"></span></a>` patterns where the only accessible name is a visually-hidden span and the visible content is icon-only. Currently `media/svg-accessible-name` only addresses the inner-svg shape; the carousel-control sr-only-text + aria-hidden-icon-sibling shape is uncovered. CSS-framework corpus carousel demos, recurring. Fix: add `forms/visually-hidden-only-name` (or extend `media/svg-accessible-name`) — fires when an interactive element's only accessible name source is a `.visually-hidden` / `.sr-only` text node alongside an `aria-hidden="true"` icon sibling. Surface a review-candidate at the SC level rather than auto-flag (the pattern is borderline-correct — the SR-text IS the accessible name; the question is whether a sighted user without icons would understand the affordance). Satisfies wcag22:2.4.4.
- [ ] **Q11-RULE-DROPDOWN-TOGGLE-TRIPLE-ARIA-MISSING** No rule fires on Bootstrap-style dropdown triggers (`<a class="dropdown-toggle" data-toggle="dropdown">` or `data-bs-toggle="dropdown"`) missing the canonical `aria-haspopup="menu"` + `aria-controls` + `aria-expanded` triple. `aria/expanded-on-disclosure` only addresses the expanded-attribute axis on the broader disclosure namespace. Bulk-template corpus, recurring 50+ instances. Pairs with Q9-RULE-POPOVER-TOOLTIP-TRIGGER-DISCLOSURE (popover/tooltip axis); this is the dropdown-trigger axis. Fix: add `aria/dropdown-toggle-triple-aria-missing` — fires when an element has a `data-(bs-)?toggle="dropdown"` attribute and lacks any of `aria-haspopup`, `aria-controls`, or `aria-expanded`. Satisfies wcag22:4.1.2.
- [ ] **Q11-RULE-MULTIPLE-LABEL-FOR-SAME-ID** No rule fires when two `<label for="X">` elements reference the same `id="X"` (e.g. duplicate label declarations across visible + visually-hidden variants). Vanilla-JS corpus, `password-strength-background/index.html:21,31` both `for="email"`. Fix: add `forms/multiple-label-for-same-id` (or extend `parsing/duplicate-id`'s sibling-label scan) — fires when ≥2 `<label for=>` elements in the same document target the same id. Satisfies wcag22:1.3.1 + 4.1.2.
- [ ] **Q11-RULE-DIV-DATASRC-CSS-BACKGROUND-IMAGE-INVISIBLE** No rule fires on `<div data-src="path/to/image.jpg">` patterns used by carousel/lightbox libraries to bind CSS background-image at runtime. The image is content-bearing but `media/alt-text-missing` cannot fire on `<div>` and no `aria-label` finder addresses the data-attribute-driven shape. Bulk-template corpus, every `.camera_wrap`/`.flexslider`/`.owl-carousel` instance. Fix: add a review-candidate finder for `<div data-src=>` / `<div data-bg=>` patterns where the data attribute resolves to an image extension; reason frames the question (`background-image bound at runtime — verify aria-label or sibling text describes the image content`). Satisfies wcag22:1.1.1. Pairs with V1-CONTRAST-BG-IMAGE-SURFACE (open) on the contrast axis.

### Response-shape: ambiguous / type-polymorphic / dangling fields

- [ ] **Q11-FIELD-DURATIONLITERALMS-TYPE-POLYMORPHIC** `wcag22:2.2.1` review-candidate ships `durationLiteralMs: 0 | 200 | 100 | "non-literal"` — the field is a number on resolved literals but a string sentinel `"non-literal"` when the duration is a non-literal expression (member-access, identifier, computed). Agent must type-check before reading. Bulk-template + CSS-framework corpora. Fix: omit the field when the duration is non-literal (conditional spread); ship a sibling `durationExpression?: "this.config.timeout"` for the cases the agent might want to disambiguate. Per AI-first doctrine "Ambiguous field shapes are dishonest" (type-polymorphism is the worst-case ambiguity — agent's downstream comparison/sort silently coerces).
- [ ] **Q11-WARNINGSDETAILS-EMPTY-OBJECT-VALUE-AS-FIELD** `warningsDetails.{scanned_build_artifacts_present, partial_parse_files_present, deprecated_field_id_renamed_criterionId}: {}` ships empty objects as field values when the warning fires but the substantive context is dropped under truncation. Agent cannot distinguish "warning fired with no details" from "details were truncated and gutted." Bulk-template corpus. Same shape as ambiguous-empty-string at the object-value level. Fix: when a warning fires without details, omit the warningsDetails entry entirely and rely on the warning code in the `warnings: []` array; when details existed but were truncated, replace with `{truncated: true, fieldsDropped: ["..."]}` sentinel. Per AI-first doctrine "Ambiguous field shapes are dishonest" (extends to `{}` value alongside `""`/`null`); pairs with Q10-META-TRUNCATED-FIELD-NAME-RETAINED-WHEN-GUTTED.
- [ ] **Q11-REFERENCEGUIDE-DANGLING-POINTER-ON-TRUNCATION** `findings[].fix.descriptionRef.hash` references a top-level `referenceGuide.fixDescriptions[hash]` entry; when truncation drops `referenceGuide` (or clips its body) but findings retain `descriptionRef`, the agent gets dangling pointers that resolve to undefined. Recurring shape in oversize bulk responses. Fix: response-assembly site must pin the invariant — when a `descriptionRef` is emitted, the matching `referenceGuide.fixDescriptions[hash]` must be retained. Either hoist used references AHEAD of the truncation pass, or inline the description string under each finding when truncation forces dropping the guide. Per AI-first doctrine "Truncated containers must rename or sentinel, not retain" extended to cross-field reference invariants.
- [ ] **Q11-RESPONSE-DROPPED-FILES-OVERSIZE-COUNTER-UNDERREPORTS** `warningsDetails.response_dropped_files_oversize` ships `{droppedFileCount: 1, requestedLimit: 25, effectiveLimit: 1}` on a 4936-files-with-findings corpus. The headline `droppedFileCount: 1` reads as "we dropped one file" — what actually happened is "we serialized one of 4936 files-with-findings, the requestedLimit ceiling was 25, effective ceiling was 1." Bulk-template corpus. Fix: rename `droppedFileCount` to `droppedFileCountFromRequestedLimit` AND ship a sibling `totalFilesWithFindings: N` (already shipped on `files.totalFilesWithFindings` — propagate to the warning detail). Per AI-first doctrine "Composite headline counts are dishonest" applied to truncation telemetry.
- [ ] **Q11-COVERAGE-FAILINGAUTOMATEDCRITERIA-NAME-CONFLATES-SEVERITY** `coverage.failingAutomatedCriteria: [...]` lists every criterion with ANY automated finding regardless of severity — a criterion that emits one `severity: "warning"` shows up alongside one with 50 `severity: "error"`. The "failing" label suggests test-runner-style fail/pass; the contents are "has-emission" union. Static-site + bulk-template corpora. Fix: split into `failingAutomatedCriteria` (criteria with ≥1 `error`-severity emission) + `warningAutomatedCriteria` (criteria with only `warning`/`suggestion` emissions); OR rename to `criteriaWithAutomatedEmissions` + provide per-criterion severity rollup. Per AI-first doctrine "Composite headline counts are dishonest."
- [ ] **Q11-COVERAGE-CRITERIATOTAL-NO-LEVEL-QUALIFIER** `coverage.criteriaTotal: 55` is unanchored from the conformance-level qualifier (55 = WCAG 2.2 A+AA cumulative). Same field name shipped alongside `--profile wcag22-a` (30) and `--profile wcag22-aaa` (87) varies — the field's denominator changes silently. Fix: rename to `criteriaTotalForProfile` AND ship a sibling `criteriaByLevel: {A: N, AA: N, AAA: N}` map so the total never disagrees with the level breakdown. Per AI-first doctrine "Ambiguous field shapes are dishonest."

### Cross-surface drift / counter splits

- [ ] **Q11-FIXESBYCLASS-NOT-SPLIT-BY-SCAN-KIND** `plan.violationsByScanKind: {source: 1229, buildArtifact: 147}` correctly splits source vs vendor on `scan_project`; `plan.fixesByClass: {mechanical: 1376, verifyInSource: ..., guidance: ..., none: ...}` does NOT split by scanKind. Agent reading `mechanical: 1376` cannot tell how many target user-editable source vs vendor (where `apply_fix` would edit a build artifact). Bulk-template corpus. Fix: ship `fixesByClass: {mechanical: {source: N, buildArtifact: N}, verifyInSource: ...}` so each lane carries the same scanKind axis as `violationsByScanKind`; integration test pinning `sum(fixesByClass.mechanical) == fixesByClass.mechanicalTotal == violationsByScanKind sum where fixClass=mechanical`. Per AI-first doctrine "Composite headline counts are dishonest" — same-axis decomposition must be available where the parent counter is.
- [ ] **Q11-DEFAULT-EXCLUDED-DIST-NO-SCANNED-BUILD-ARTIFACTS-ENTRY** Repos where `dist/`, `build/`, or `.next/` contain bundle output that is silently filtered by `DEFAULT_EXCLUDED_PATTERNS` produce zero `scannedBuildArtifacts[]` entries — the file-discovery walker dropped them BEFORE classification ran. Agent has no signal that 30+ minified+sourcemap files exist on disk in vendor-shaped directories. CSS-framework corpus, `dist/css/` + `dist/js/` + `js/dist/` (~30 files total) all silent. Fix: emit a `default_excluded_artifact_paths` warning naming `{path, fileCount, sampleFiles}` for every `DEFAULT_EXCLUDED_PATTERNS` glob that matched ≥1 file in the scope; per AI-first doctrine "Default-exclude globs are suppression too" — the agent dismisses build-output paths in one read but cannot un-suppress files that never got walked. Pairs with Q9-BULK-WARNINGS-NOT-CROSS-SURFACE on the propagation axis.
- [ ] **Q11-BULK-CATALOG-SUGGESTEDEXCLUDES-NOT-ADDITIONALPATHS-INVERSE** `bulk_catalog_detected.suggestedExcludes` returns filename-pattern globs (`**/bootstrap.min.js`) shaped for config-file edits; `nextStepStructured` doesn't propagate the inverse — a concrete `additionalPaths: [<top-non-vendor-subdir>]` for the immediate next call. Agent receiving the response after `response_dropped_files_oversize` reads "narrow scope" prose but the structured args repeat the failing cwd. Bulk-template corpus. Fix: when `bulk_catalog_detected` fires, populate `nextStepStructured.args.additionalPaths` (or `restrictToPaths`) with the inverse of `suggestedExcludes` — the single highest-impact non-vendor subtree. Per AI-first doctrine "NextStep prioritization on truncated/bulk responses must avoid first-by-filename routing" (extends to "must propose a concrete in-call narrowing, not just config-file changes"); pairs with Q9-NEXTSTEP-CWD-ECHOES-CALLER-AFTER-DROP.

### Routing edge cases

- [ ] **Q11-PARSER-DOUBLE-EXTENSION-ERB-MARKDOWN-ROUTING** `.md.erb` files (templated markdown — common in static-site themes for theme-template README files) route via the `.erb` half (→html parser) rather than the `.md` half (→markdown parser); content scanned as HTML produces no findings even when real markdown content is present. Static-site corpus, `lib/theme_template/README.md.erb`. Fix: when an extension chain ends in a known templating extension (`.erb` / `.liquid` / `.ejs`), route by the leading extension — strip the templating tail before mode lookup. Per AI-first doctrine "Routing skips that drop content are the symmetric twin of suppression." Edge case but the file class is real.

### 2026-04-26 round recurrences (folded onto existing Q9/Q10 rows)

Each line: `<row id> — N recurrences observed in 2026-04-26 round, evidence summary`. The 2026-04-26 sweep replayed the 4-corpora × 5-angles probe; items below recurred without behavior change against the same Q9/Q10 closure framing, so this round folds them as recurrence counts rather than new rows.

- **Q10-SUGGEST-FIX-MECHANICALINPRINCIPLE-CONTRADICTS-KIND-GUIDANCE** — +4 recurrences (all 4 corpora): `kind: "guidance"` + `meta.mechanicalInPrinciple: true` returned for `document/lang-attribute`, `forms/labels-required`, `aria/icon-child-missing-aria-hidden`, `media/alt-text-placeholder`, `parsing/html-has-lang`, `semantics/button-name`, `aria/expanded-on-disclosure`, `forms/placeholder-as-label`, `forms/autocomplete-missing`, `navigation/href-empty-fragment` — 0/5 mechanical findings probed produced `kind: "edit"` on the CSS-framework corpus.
- **Q9-BULK-WARNINGS-NOT-CROSS-SURFACE** — +4 recurrences (all 4 corpora): `scss_unresolved_variables` ships on `scan_project` only (CSS-framework, 31 SCSS files); `scanned_minified_file` + `scanned_build_artifacts_present` ship on `scan_project` only (static-site, jquery in `docs/js/`); `scanned_build_artifacts_present` ships on `scan_project` only (vanilla-JS); same warnings split asymmetrically across `scan_project`/`checklist`/`coverage` (bulk-template).
- **Q9-PARSER-INNERHTML-TEMPLATE-LITERAL-ISLANDS-IN-JS** — +3 recurrences: tooltip/popover string-concatenated HTML templates inside `.js` (CSS-framework `js/src/tooltip.js:73-76`, `js/src/popover.js:25-29`); innerHTML template-literal `<img alt="${user.name}">` in vanilla-JS exercise `script.js:33-50`; multiple inline `<script>` JS-side template literals across bulk-template corpora — no `js_innerhtml_template_literal_unparsed` warning emitted in any case.
- **Q9-RULE-KEYBOARD-HANDLER-FP-CROSS-FILE-NATIVE-BUTTON** — +2 recurrences: 43 instances on vanilla-JS corpus (every demo's `script.js` resolves the click target to a sibling `<button>` in `index.html`); recurring on bulk-template corpus where `$("a[href='#top']")` selectors resolve to native anchors. Fix.description literally instructs "Cross-file check: grep the selector in your HTML to confirm" — the rule has the AST one hop away.
- **Q10-META-TRUNCATED-FIELD-NAME-RETAINED-WHEN-GUTTED** — +3 recurrences: `coverage(verboseMeta:true).meta.perRuleCoverage` silently absent under truncation while `warningsDetails.response_meta_truncated.fields` doesn't enumerate it (CSS-framework); `meta` retained but stripped of `analysisCoverage`/`scannedBuildArtifacts.ungrouped` (bulk-template); `files: []` retained as empty array under `effectiveLimit: 1` of 4936 (bulk-template — array-shape variant of the doctrine bullet).
- **Q9-NEXTSTEP-FIRST-BY-FILENAME-RECURRENCE** — +2 recurrences: routes to `_project_starter_/index.html` ahead of higher-impact findings on a vanilla-JS corpus mixing scaffold + real source; routes to `js/tests/integration/index.html` (test-fixture path) on a CSS-framework corpus despite production source available. Pairs with Q9-NEXTSTEP-CWD-ECHOES-CALLER-AFTER-DROP (already at 2 recurrences).
- **Q9-PERFINDING-COULDBEWRONG-EMPTY-WHEN-PERRULE-DEGRADED** — +2 recurrences (closed item, evidence of regression): `keyboard/handler-missing` per-rule confidence labeled `medium` with `cross_file_listener_resolution_limited_on_this_input` while every per-finding emission ships `confidence: "high", severity: "error"` on the vanilla-JS corpus (43 instances) and bulk-template corpus. The closure shipped (commit reference in original Q9 row) but field evidence shows the degradation isn't propagating uniformly — re-open if integration test confirms the regression.
- **Q9-EXPANDED-ON-DISCLOSURE-SR-ONLY-CHILD-CONCESSION** — +2 recurrences: `<button class="navbar-toggle"><span class="sr-only">Toggle navigation</span>…</button>` shape recurs on bulk-template + static-site corpora; severity stays `error` while reason concedes "if .sr-only is the disclosure label, accessible name is complete."
- **Q9-EMPTY-HEADING-TEMPLATE-EXPRESSION-AT-ERROR** — +1 recurrence: static-site corpus `_includes/news_item.html:2` and `news_item_archive.html:11` both `<h2>{{ post.title }}</h2>` — Liquid stripped, severity stays `error`.
- **Q9-RULE-HREF-EMPTY-FRAGMENT-FP-IN-DOCS-EXAMPLE-PROP** — +2 recurrences: 1284 fires inside `<Example code={\`...\`}/>` JSX template-literal `code` props on a CSS-framework `.mdx` documentation tree; 44 fires inside `<div class="highlight result" data-proofer-ignore>` rendered-example markup blocks on a static-site corpus tutorial — adjacent shape (attribute-marker container) under the same closure mechanism.
- **Q10-PARSER-ASTRO-ROUTED-AS-HTML-FRONTMATTER-BAILS** — +1 recurrence: 9 `.astro` files on a CSS-framework corpus all in `partialParseFiles[]` with `reason: "Stray closing tag at top level"` — the misleading reason narrative from `parseModeByExtension[".astro"]: "html"` (should be `tsx`).
- **Q9-PARSE-ERROR-REASON-DIRECTION-INVERTED** — +2 recurrences: `_layouts/default.html` and `_layouts/error.html` on a static-site corpus both classified `parseErrorFiles` with `reason` text claiming `</html>` is the elided closer when the file's actual content has `</html>` on its last line and the OPENING `<html>` is what's elided.
- **Q9-VENDOR-COPYRIGHT-BANNER-CLASSIFICATION** — +1 recurrence: bulk-template corpus has many `/*! Library vN.M Copyright/Released under … */` banner-headed unminified vendor stylesheets that escape `scannedBuildArtifacts` classification.
- **Q9-MINIFIED-LABEL-MISLABELS-LONG-LINE-SCSS** — +1 recurrence: SCSS function bodies with long type signatures + inline `data:image/...` markers fire `likely-vendored-data-url-css` on hand-authored sources (CSS-framework `_buttons.scss`/`_grid.scss`).
- **Q9-STALE-MCP-SUBPROCESS-WARNING-EVERY-RESPONSE** — +4 recurrences (all 4 corpora): the warning ships on every response unconditionally including on `suggest_fix` per-call shapes where it's structurally irrelevant.
- **Q9-RULESBYEXTENSION-DUP-PAYLOAD** + **Q9-DUPLICATE-ID-AND-CRITERIONID-AFTER-RENAME** — +2 recurrences each: byte-identical deprecated/renamed field pairs continue to ship; deprecation warnings have no `warningsDetails` entry naming the alias-removal version (per ADR-0021 alias contract).
- **Q9-OVERSIZE-MITIGATION-DOES-NOT-ENGAGE-PRE-SERIALIZATION** — +3 recurrences: 74KB CSS-framework `scan_project(limit=25)` (post-clip envelope still over host cap); 87KB static-site `scan_project` (post-clip 87269 chars on 429 files); 60KB+ vanilla-JS `scan_project` returns InputValidationError instead of minimum-honest envelope. Closure in flight per the original Q9 row but the recurrence count crossed the "promote to integration test" bar.
- **Q9-BULK-CATALOG-DETECTED.SUGGESTEDEXCLUDES-NOT-ADDITIONALPATHS** — already in Q11-BULK-CATALOG-SUGGESTEDEXCLUDES-NOT-ADDITIONALPATHS-INVERSE above (this round's primary surface).
- **J-6 (performance budget at 4k-file vendor-heavy scale)** — +1 recurrence (concrete evidence): bulk-template corpus 4936 files / 26.3s on a hot scan = 75% over linear extrapolation of the 1000-file 3s budget AND 75% over the documented 4000-file 15s budget. CLAUDE.md §11 row warrants update OR the discovery walker needs a `largeRepoHint` warning emission.

### Considered and rejected (per CLAUDE.md §1)

- **Suppress wcag22:1.4.5 candidates on standalone-mini-site corpora where every subdir is a single-page demo** — Rejected per "no heuristic suppression, even for spec carve-outs" doctrine. The single-page-mini-site predicate is a heuristic on weaker evidence than the agent has; the deterministic escape hatch (source-level disable pragma) covers the dismissal cost. Reason-text enrichment (`couldBeWrongBecause: ["isolated_component_demo_page"]`) already applies via Q7-RULE-LANDMARK-MAIN-FRAGMENT-SCOPE for the document-shaped case.
- **Auto-promote `scannedBuildArtifacts`-classified files to a deprioritized bucket so suggest_fix doesn't recommend edits to vendor** — Rejected per "Labeled buckets are suppression too" doctrine. The legitimate fix is Q9-SUGGEST-FIX-VENDOR-CONTRAST-RECOLOR (gate suggest_fix on the classification at the per-call site, not the response-shape bucket); same-shape suppression at the response layer would discard signal the agent uses for its own routing.

---

## Track Q12 — Multi-corpus AI-first sweep (2026-04-26 round, 4 corpora × 5 angles)

Field-test follow-ups from a 4-corpus probe (CSS framework + static-site generator + vanilla mini-projects + bulk-template gallery). Each item below is **net-new** against Q9/Q10/Q11; recurrences fold under the existing rows there. Items bucketed by failure class.

### Per-finding confidence drift (file-scope vs corpus-scope)

- [ ] **Q12-COULDBEWRONG-FILE-PARSE-ERROR-REASON-MISATTACHED-CORPUS-WIDE** Per-finding `couldBeWrongBecause: ["file_parse_error"]` is attached to every finding emitted on files that are NOT in `parseErrorFiles[]` or `partialParseFiles[]`. Observed on a static-site corpus (every `<table>`/`<div>`/anchor finding on `_docs/assets.md` and `_docs/collections.md` carries the reason while only `_layouts/default.html` has a parse error) and on a CSS-framework corpus (every finding on `js/tests/visual/alert.html` and `js/tests/visual/button.html` carries the reason while only 9 `.astro` files have partial parse errors). The reason looks corpus-scoped — fires whenever ANY parse error exists in the scan, not file-scoped. Per AI-first doctrine "Per-finding confidence must reflect per-rule coverage limitations" — applied at the wrong layer here: claims this finding's file had a parse failure when it didn't. Closure: gate the per-finding `file_parse_error` reason on `findings[].path ∈ (parseErrorFiles[].path ∪ partialParseFiles[].path)` membership; integration test pins the invariant on a fixture that mixes one parse-error file with one clean file in the same scan.

### Truncation telemetry honesty (minimum-honest envelope shape)

- [ ] **Q12-MIN-ENVELOPE-DROPS-COUNTERS-WITHOUT-SENTINEL** Under the bulk-corpus minimum-honest envelope (`response_dropped_files_oversize` + `response_token_budget_truncated` co-fire), `totalFilesWithFindings` and `truncated` ship absent (or `null`) at the top level even though `bulk_catalog_detected` records the underlying state in `warningsDetails`. Agent reading the envelope sees `files: []` and cannot distinguish "0 files had findings" from "files were dropped by the oversize protector." Same shape as Q10-META-TRUNCATED-FIELD-NAME-RETAINED-WHEN-GUTTED but with the inverse failure: instead of retaining a name + gutting content, this drops the counter outright, stripping the only signal that would let the agent infer the drop. Closure: under truncation engagement, populate `truncated: true` AND `totalFilesWithFindings: <pre-clip count>` AND a `filesArrayDropped: true` flag (or a structured sentinel `files: { truncated: true, droppedCount: N }`). Per AI-first doctrine "Truncated containers must rename or sentinel, not retain" extended to absent-counter case. Pairs with Q10-RESPONSE-TOKEN-BUDGET-TRUNCATED-WITHOUT-TRUNCATED-FLAG (one recurrence this round; closure attempt was for the `truncated` flag — `totalFilesWithFindings` is the new axis).

- [ ] **Q12-MIN-ENVELOPE-OVER-HOST-CAP-ON-VENDOR-HEAVY-CORPORA** Even with `files[]` dropped, the bulk-corpus minimum-honest envelope serializes to ~89 KB (over typical MCP host caps) because verbose meta entries — `topRules` (10 entries × ~250 chars), `bulk_catalog_detected.suggestedExcludes`, full `partialParseFiles[]` enumeration (113 entries on the bulk-template corpus), `perRuleCoverageSummary.ruleIds` — are retained. The internal `hardCeilingBytes: 96000` setting passes ra11y's check while a strict host returns `InputValidationError`, leaving the agent with a host-level transport error and no envelope. Closure: under the envelope-drop branch, additionally trim verbose meta arrays to `topRules[0..2]` + `partialParseFiles[0..3]` + `perRuleCoverageSummary` reduced to its `ruleCount` scalar; target post-clip envelope ≤ 50 KB (or whatever the smallest documented host cap is). Per AI-first doctrine "Oversize-success is ambiguous failure." Counts as a +1 recurrence on Q9-OVERSIZE-MITIGATION-DOES-NOT-ENGAGE-PRE-SERIALIZATION; the new bullet is the trim-verbose-meta-too axis.

### Vendor-classification schema split

- [ ] **Q12-SCANNED-BUILD-ARTIFACTS-VENDORLIBRARIES-PARALLEL-SURFACES** `scannedBuildArtifacts` ships TWO concurrent vendor-classification surfaces in the same response: `scannedBuildArtifacts.ungrouped[]` (path/min-infix-based, e.g. `html5shiv.min.js` + `respond.min.js`) AND `scannedBuildArtifacts.vendorLibraries[]` (registry/version-based, e.g. `_normalize.scss` 7.0.0). Both classify "vendor" but the schema splits them, leaving an agent reading `scannedBuildArtifacts` to do its own union. The split is also asymmetric across surfaces — `vendorLibraries` rides only on `scan_project`, not on `coverage` or `checklist`. Closure: merge into a single `scannedBuildArtifacts.classified[]` array with a `classification` discriminator (`min-infix` | `path-prefix` | `vendor-library-version-detected`); deprecate the split via the alias table; populate identically on every surface that emits `scannedBuildArtifacts`. Per AI-first doctrine "Composite headline counts are dishonest" extended to vendor-classification fan-out (one concept = one shape) AND "Cross-surface count invariant" (warning + classification telemetry on identical cwd must agree across project-rooted tools). Pairs with Q9-BULK-WARNINGS-NOT-CROSS-SURFACE on the propagation axis.

### 2026-04-26 round recurrences (folded onto existing Q9/Q10/Q11 rows)

Each line: `<row id> — N recurrences observed in this round, evidence summary`. Replays of the 4-corpora probe; items below recurred without behavior change against the same Q9/Q10/Q11 closure framing.

- **Q9-RULE-KEYBOARD-HANDLER-FP-CROSS-FILE-NATIVE-BUTTON** — +1 recurrence: vanilla-corpus `keyboard/handler-missing` count=43 with per-rule `cross_file_listener_resolution_limited_on_this_input` reason while every per-finding emission ships `confidence: "high", severity: "error"` (Q9-PERFINDING-COULDBEWRONG-EMPTY-WHEN-PERRULE-DEGRADED axis still leaking on this rule).
- **Q9-NEXTSTEP-FIRST-BY-FILENAME-RECURRENCE** — +2 recurrences: routes to `_project_starter_/index.html` (vanilla scaffold dir) and to `js/tests/integration/index.html` (CSS-framework test fixture path) ahead of higher-impact authored findings.
- **Q9-OVERSIZE-MITIGATION-DOES-NOT-ENGAGE-PRE-SERIALIZATION** — +1 recurrence: bulk-template `scan_project` returned `preDropBytes: 548283` post-clip 89238 chars, still over the host cap. Closure attempt is in flight; new sub-axis captured as Q12-MIN-ENVELOPE-OVER-HOST-CAP-ON-VENDOR-HEAVY-CORPORA above.
- **Q11-RESPONSE-DROPPED-FILES-OVERSIZE-COUNTER-UNDERREPORTS** — +1 recurrence: bulk-template ships `droppedFileCount: 1, requestedLimit: 2, effectiveLimit: 1` while `totalFilesWithFindings` is absent — agent can't reconcile.
- **Q11-BULK-CATALOG-SUGGESTEDEXCLUDES-NOT-ADDITIONALPATHS-INVERSE** — +1 recurrence: bulk-template `nextStepStructured.args` echoes the failing `cwd` with no `additionalPaths` / `restrictToPaths` proposed despite `bulk_catalog_detected.suggestedExcludes` being populated (5 patterns).
- **Q10-PARSER-ASTRO-ROUTED-AS-HTML-FRONTMATTER-BAILS** — +1 recurrence: 9 `.astro` files in CSS-framework `partialParseFiles[]` all `Stray closing tag at top level` due to `parseModeByExtension[".astro"]: "html"`.
- **Q10-RESPONSE-TOKEN-BUDGET-TRUNCATED-WITHOUT-TRUNCATED-FLAG** — +1 recurrence: bulk-template ships `truncated: null`/absent while `response_token_budget_truncated` AND `response_dropped_files_oversize` AND `bulk_catalog_detected` all fire. New related axis (`totalFilesWithFindings` absence) captured as Q12-MIN-ENVELOPE-DROPS-COUNTERS-WITHOUT-SENTINEL above.
- **Q10-META-TRUNCATED-FIELD-NAME-RETAINED-WHEN-GUTTED** — +2 recurrences: static-site corpus and CSS-framework corpus both ship `response_meta_truncated.fields: ["analysisCoverage.fragmentFiles"]` — closure shipped (`fragmentFilesTruncated: {shown, total}` sentinel populated) but `response_meta_truncated` warning still fires alongside, ambiguous whether more fields than the named one were dropped. Closure: when the only truncation is `fragmentFiles` clipping with sentinel, suppress the warning; emit the warning ONLY when fields are dropped without sentinel.
- **Q9-BULK-WARNINGS-NOT-CROSS-SURFACE** — +2 recurrences: `scss_unresolved_variables` ships on CSS-framework `scan_project` (31 SCSS files); `scanned_minified_file` + `scanned_build_artifacts_present` ship on static-site `scan_project` (`html5shiv.min.js`, `respond.min.js`). Cross-surface propagation to `coverage`/`checklist` not verified this round (host-cap budget exhausted) — re-probe needed if closure work claims completion.
- **Q11-RULE-EXPANDED-DISCLOSURE-FIX-CONCEDES-MAY-NOT-APPLY-AT-ERROR** — +1 recurrence: CSS-framework `aria/expanded-on-disclosure` count=166 across `dropdowns.mdx`, `navs-tabs.mdx` — every finding still at `severity: "error"` despite the closed-bullet doctrine.

### Considered and rejected (per CLAUDE.md §1)

- **Suppress `aria/role-from-class-only` on fragment-classified `.md` files where the admonition `<div class="note info">` shape is rendered through a known kramdown / Liquid include** — Rejected per "no heuristic suppression, even for spec carve-outs" doctrine. The fragment-rendered admonition COULD carry a sibling Liquid `{% capture %}` block that supplies the role — or it could be raw HTML residue with no extra runtime injection. Static evidence cannot distinguish the two, and the deterministic escape hatch (source-level disable pragma) covers the rare case where the agent confirms the fragment runs through an ARIA-injecting layer. Reason-text enrichment via `couldBeWrongBecause: ["fragment_input_no_document_envelope", "kramdown_admonition_class_token"]` is a legitimate additive change; folded into Q10-ROLE-FROM-CLASS-ONLY-FRAGMENT-FILE-CONCEDED-UNCERTAINTY (already open).
- **Down-bucket findings on files in `scannedBuildArtifacts.ungrouped[]` so they sort below authored-source findings in `topRules`** — Rejected per "Labeled buckets are suppression too" doctrine. `topRules.count` is a deterministic count of emissions; sorting by classification would replace honest count with attention-budget heuristic. Q11-FIXESBYCLASS-NOT-SPLIT-BY-SCAN-KIND covers the legitimate split-axis at the structured field; `topRules` summary stays honest.

---

## Needs judgment — 2026-04-21 field test

Open strategy/doctrine questions surfaced during triage that need a user-level decision before the corresponding backlog work can land. They are not tracked as dispatchable work — they are blockers on specific items above. Once answered, each resolves into an existing or new backlog item.

- **J-1 (pause-stop-hide signal-vs-noise strategy).** `motion/pause-stop-hide` produced 8,940 findings at scale and 55 on vanilla HTML/JS projects. Most are user-triggered hover/focus transitions, not the auto-playing motion WCAG 2.2.2 targets. Current reason text doesn't distinguish. Is this (a) over-surface that needs reason-text enrichment only ("auto-playing?" / "user-triggered?" signal, let the agent decide), (b) spec-correctness matter where the rule should narrow to auto-playing-only, (c) both? Surface-don't-suppress argues (a); narrowing risks silent-miss on genuine 2.2.2 auto-playing violations. Blocks Q6-MOTION-PAUSE-STOP-PER-FILE-AGGREGATION scoping decision.

- **J-2 (vendor CSS noise, primary mitigation choice).** Website-template scans: 69% of 24,772 findings = 3 rules firing on vendor CSS. Doctrine forbids default-excluding vendor paths. Primary mitigation options: (a) fix the `scannedBuildArtifacts` classifier so agents can filter by it with confidence, (b) add a per-file finding cap (Q6-BUDGET-UNDER-VENDOR-NOISE), (c) both, (d) something else (e.g., surface rule-per-file concentration aggressively and trust the agent to route). Currently Q6-BUDGET and Q6-SCANNED-BUILD-ARTIFACTS-GROUP-BY-BASENAME are both open; the question is whether they land independently or one supersedes the other. Blocks prioritization of both items.

- **J-3 (plan.totalFindings: rename vs remove).** Every `scan_project` ships a top-level `plan.totalFindings: N` that sums violations + notes. Doctrine forbids composite headline counts. Agents may already depend on the field for aggregation. Remove entirely (breaking — v1.0 gate) or rename to `plan.totalFindingsComposite` with explicit "sum of violations+notes, prefer the split" prose (additive, non-breaking)? Blocks Q6-PLAN-TOTALFINDINGS-VIOLATIONS-VS-NOTES implementation shape.

- **J-4 (rulesEvaluated definition: loaded-vs-fired).** `rulesEvaluated` disagrees cross-surface on the same cwd: `scan_project: 53` vs `propose_config: 54` vs `list_rules: 54` (vanilla-JS tree). Likely cause: JSX/TSX-only rule counted as "evaluated on 0 applicable files" differs by tool. Which is correct: "evaluated" = "the rule ran on 0 applicable files" (54) or "the rule didn't run because no applicable files" (53)? Either is honest; they must agree cross-surface. Blocks Q-SHARED-RULES-EVALUATED-SSOT shape.

- **J-5 (likelyIrrelevant bucket: parse-coverage caveat).** `likelyIrrelevant` for 1.2.x is honest at scale ("no video/audio in scanned files"). On an SSG source tree it's technically honest but 307 `.md`/`.markdown` files were never parsed — under the doctrine rule that bucket labels must be provable, is "no video in scanned files" sufficient when the scan missed the content layer? Options: (a) enrich `likelyIrrelevant` reason with the skipped-content caveat automatically (covered by Q4-RELEVANCE-REASON-PARSE-COVERAGE-CAVEAT), (b) consider it the agent's job to reconcile `likelyIrrelevant` with `extensions_skipped_no_parser` separately, (c) suppress `likelyIrrelevant` entirely when content extensions are skipped. Blocks Q4-RELEVANCE-REASON-PARSE-COVERAGE-CAVEAT scoping.

- **J-6 (performance budget at 4k-file vendor-heavy scale).** A full-scan ran 20.5 s internal (~32 s wall-clock) against 3,973 files — 1.7× over linear extrapolation of the 1000-file 3 s budget. Options: (a) add a new bench scenario at 3-4k vendor-heavy files with an explicit budget, (b) document a "precommit use not supported beyond N files" scope limit, (c) default to `--changed`-only discovery on repos > 1000 files with a `largeRepoHint` warning, (d) accept the slowdown and document it. Blocks performance-track decisions + CLAUDE.md §11 table update.

- **J-7 (static-analysis limit vs cross-file handler resolution).** `<a href="#">` / `<li>`-acting-as-tab / `<div>`-acting-as-button patterns with click handlers in separate `.js` files (common on vanilla-JS trees + at scale). `navigation/href-javascript-void` (registry-drift notwithstanding) catches raw `href="#"`; the broader "custom interactive widget with JS-only handler" class requires cross-file resolution. Read sibling `.js` files to resolve handlers (expensive, cross-file complexity, risks wrong-identifier matches) OR accept as an inherent static-analysis limitation and document it in `limitations`? Doctrine: "don't duplicate capability the agent already has" argues for the latter — the agent can Read+Grep across files. Blocks any rule/finder that would widen predicates to cross-file handler presence.

- **J-8 (scan_file error discrimination split).** Design-system scan confirmed `scan_file` conflates `file-unsupported` (extension not in set) with file-not-found. Split options: (a) `file-not-found` / `unsupported-extension` / `file-unreadable` (three distinct codes), (b) fold not-found into a single `file-unreadable` (two codes: "can't read this file" + "can't parse this extension"), (c) keep `file-unsupported` as umbrella with a `details.reason` sub-discriminator. Doctrine prefers explicit codes (a); (b) is more compact; (c) keeps the existing discriminator stable. Blocks Q-SHARED-SCAN-FILE-ERROR-DISCRIMINATION implementation shape.

---

## Track C — Conformance-claim gaps

Owner: main session + `spec-researcher` + `doc-writer`. Staged; do not dispatch alongside active tracks. ra11y today produces *signals* (findings, review candidates, coverage hints). It does not produce a *claim*. Track C closes the four gaps that separate the two — attestation ledger, runtime-evidence ingest, process-level scope, conformance statement. **No new rules**; the track widens what an agent can defensibly say after using the tool.

Sequencing (cross-gap, soft): attestations first (unlocks evidence semantics everywhere else), then runtime ingest (wide-reach runtime coverage), then process scope, then conformance capstone. Within each gap items are ordered by the ADR-then-foundation-then-surface pattern.

### v0.3.0 — attestation ledger (foundation)

- [x] **C-ATTEST-ADR** ADR 0011 (evidence as first-class primitive) + ADR 0012 (rule-scoped attestations) cover this slot.
- [x] **C-ATTEST-STORE** `.ra11y/attestations.jsonl` append-only store with validated envelope landed in `src/config/attestation-store.ts`.
- [x] **C-ATTEST-TOOL** `attest` MCP tool landed; accepts `criterionId`, `ruleIds?`, `verdict`, `scope`, `location?`, `reason`, `by`; stamps commit hash; rejects bare verdicts.
- [x] **C-ATTEST-LIST** `list_attestations` MCP tool landed with git-backed staleness probe + `staleProbeUnavailable` signal for non-repo environments.
- [x] **C-ATTEST-PRUNE** `ra11y attestations prune [--dry-run]` subcommand landed; pure function + injectable `fileExists` predicate.
- [x] **C-ATTEST-CHECKLIST** Checklist items spread per-criterion `attestation?: { verdict, stale?, evidence, by? }` with conditional-spread discipline.
- [x] **C-SUPPRESSION-ATTEST** Bare pragmas emit `verdict: "pending"` attestations; reasoned pragmas emit `"pass"`; `suppression/no-reason` finder retained as complementary signal.

### Rejected — runtime evidence bridge

A prior plan proposed ingesting vendor runtime results (vendor JSON → normalized shape → ledger) as the path to close runtime-only WCAG criteria. Rejected: vendor-specific ingest adapters duplicate capability agents already have via their own test harnesses, and the tool's job is to point at the source — not at another tool's output. The ingest shape is also fragile to vendor schema drift, and the normalized layer re-buckets findings in ways the reading agent can't re-audit. Agents that run runtime checks bridge their results through the existing `attest` tool — the reason text is the evidence, and the attestation ledger is the durable, vendor-neutral channel.

Items struck: C-RUNTIME-ADR, C-RUNTIME-SCHEMA, C-RUNTIME-INGEST, C-RUNTIME-MAP. C-COVERAGE-MERGE is re-scoped below.

- [x] **C-COVERAGE-MERGE** Coverage report integrates attestations; per-criterion row carries optional `attested: { verdict, stale? }`. Manual criteria with fresh pass/n-a attestations count toward covered. Stale/pending/fail don't flip static verdicts.

### v0.3.0 — process-level scope

- [x] **C-PROCESS-ADR** ADR 0016 (process-level scope) landed.
- [x] **C-PROCESS-CONFIG** `processes: [{ name, pages }]` config primitive + schema validation + loader wiring.
- [x] **C-PROCESS-SCAN** `scan_process` MCP tool landed; orchestrates per-page scan with process-level frame.
- [x] **C-PROCESS-CONSISTENCY** `consistent-navigation` finder honors declared processes; heuristic path retained as fallback with a reason-text nudge to declare the primitive.
- [x] **C-PROCESS-IDENT** `consistent-identification` finder for wcag22:3.2.4; emits zero candidates when no processes config (honest needs-config gap).

### v1.0.0 — conformance capstone

- [x] **C-PROFILE-WCAG21AA** `ConformanceProfile` primitive + 8 built-ins (wcag21-a/aa, wcag22-a/aa/aaa, section508, en301549, ada); `--profile` CLI flag with explicit-override warning. Profile→reports scope filtering wired.
- [x] **C-CONFORM-ADR** ADR 0017 (conformance-statement-output) landed.
- [x] **C-CONFORM-STATEMENT** `conformance_statement` MCP tool + `src/reports/conformance.ts` landed; refuses when coverage is incomplete; blocker list cites missing criteria.
- [x] **C-CONFORM-SIGN** SHA-256 digest over (commit hash + attestations + in-scope criteria + config fingerprint); canonical JSON; `verifyConformanceBundle` for drift detection.
- [x] **C-CONFORM-DOC** `docs/conformance.md` end-to-end agent guide: scan → checklist → attest → coverage → signed statement.

### Dependencies + interactions with Track S

Track S speculative tools (`verdict-candidate`, `draft-vpat-narrative`, `resolve-component`) become concretely useful once **C-ATTEST-TOOL** lands — their output is evidence the agent writes to the ledger. Track C resolves Track S's "pick 1–2 speculative tools" decision (ADR 0005 §Follow-up): `verdict-candidate` is the natural first pick because its output IS an attestation entry. Promote Track S `[!]` items to `[ ]` once C-ATTEST-TOOL is merged.

### Considered and rejected

- **Embed a vendor runtime scanner as a runtime dep.** Rejected per **§3 invariant 1** (zero runtime deps). Don't pull any vendor scanner into the install. Agents already run their preferred runtime harness in their test setup; they bridge results through `attest`.
- **In-tool headless browser (Playwright / Puppeteer).** Rejected per zero-deps + MCP-first-consumer framing — the agent already runs the browser in its test harness. We ingest; we don't drive.
- **Blanket "ra11y verifies conformance" marketing.** Rejected — the conformance statement will cite evidence sources; ra11y is the aggregator, the attestation is the agent's (or human's) word, and the signed bundle is what survives audit. Honesty > reach.

---

## Track V — v1.0.0 readiness

Owner: main session + `doc-writer` + `fixture-curator` + `test-author` + specialists as indicated per item. Source: 2026-04-19 three-agent gap analysis (rule coverage vs WCAG 2.1/2.2 A+AA, MCP shape discipline, backlog + semver + CI infra). The conformance capstone in Track C is shipped; these are the remaining gates for the "find all violations" bar.

No item here proposes new rule detection logic — Track V is about honesty surfaces, decision closure, and coverage verification. Items that WOULD add detection get logged as Track R or Track C items.

### v1.0.0 — certification claim blockers

Source: two independent pre-ship subagent sweeps (spec-researcher normative angle + general-purpose tooling/reproducibility angle). Both flagged overlapping blockers that would sink a real WCAG/VPAT/Section 508 certification submission. Each item has a concrete file:line anchor; together these are the gate to `/release 1.0.0`. Dispatch in parallel where non-overlapping; B1 and B6 block on B2's shape work.

- [x] **V1-CERT-STATEMENT-SHAPE** `src/reports/conformance.ts:101-128` `ConformanceStatement` is missing four of six W3C WCAG §5.3.1 required claim fields: `guidelinesTitle`, `guidelinesVersion`, `guidelinesUri`, `scope.files`, `technologiesReliedUpon`, `technologiesNotReliedUpon`. ADR 0017 designed these but implementation diverged. Accept ADR 0017 as the source of truth; reshape the type + `renderConformanceMarkdown` (line 308) to emit all six; update ADR 0017 status from "Proposed" → "Accepted". Blocker for B1 + B3 + B6. Medium. [579be54, e9732df — all six WCAG §5.3.1 required fields now populated on `ConformanceStatement`; `scope.commitHash` and `scope.configSnapshot` conditional-spread per the AI-first consumer model (no `""` or `{}` sentinel); `technologiesReliedUpon` defaults to `["HTML", "CSS", "ECMAScript", "WAI-ARIA"]`; ADR 0017 promoted Proposed → Accepted with an Implementation section documenting the present-when-meaningful deviations; MCP tool forwards `files[].filePath` + session-config snapshot; `renderConformanceMarkdown` now emits date / guidelines / conformance-level / scope / technologies sections in a WCAG §5.3.1 layout; nine new unit tests cover the shape.]
- [x] **V1-CERT-STATEMENT-SIGN** [bb79517, 15469b9, 5e434a1, b428896 — SignatureInput extended with per-file SHA-256 manifest (relative path keys), toolVersion, and a full configFingerprint covering standards/level/profile/rules/nativeWrappers/processes/additionalPaths; each new axis has its own ConformanceVerificationReason drift code (file-manifest-mismatch, tool-version-mismatch, config-profile-mismatch, config-rules-mismatch, config-native-wrappers-mismatch, config-processes-mismatch, config-additional-paths-mismatch); MCP tool computes commitHash via headSha (skip-with-warning `non_git_repo_signature_omitted` outside git), collects durable attestations, builds the file manifest over parsed sources, assembles the config fingerprint from LoadedConfig (falling back to session values on config-load failure), passes VERSION as toolVersion; 16 new tests cover reordering invariance, per-axis drift routing, full round-trip, minimal round-trip, and MCP-level signed-scan round-trip + tamper detection.]
- [x] **V1-CERT-DOCS-DRIFT** `docs/conformance.md:197-223` advertises `scope.files`, `scope.commitHash`, `scope.configSnapshot`, `technologiesReliedUpon`, `evidence.findingIds`, `evidence.attestationIds` — fields absent from the emitted type. After V1-CERT-STATEMENT-SHAPE reshapes the type, reconcile the doc so it matches what the tool emits. Also fix the minor cross-reference error in `docs/kb/criteria/wcag22/2.4.12-focus-not-obscured-enhanced.md` frontmatter (references `wcag21:2.4.12` which doesn't exist; WCAG 2.1 AA stops at 2.4.9). Blocks on V1-CERT-STATEMENT-SHAPE. Small. [d60e6cd — removed evidence.findingIds/attestationIds (not on type), replaced date/conformanceLevel with generatedAt/profile, added full field reference table with always-present/conditional notes, fixed end-to-end transcript, corrected 2.4.12 frontmatter to reference wcag22:2.4.11 instead of nonexistent wcag21:2.4.12.]
- [x] **V1-CERT-STALE-ATTEST** [40cb5b9, bf03eac — builder now consults `createGitStalenessProbe(cwd)` against every attested-pass/n/a source, flipping criteria to `stale-attestation` blockers (carrying `staleAttestedAt`) when the probe says the scope intersects the changed-files set; indeterminate probe results surface as a top-level `stale_probe_unavailable` warning (response-level, conditional-spread) rather than fabricating staleness; added `missing-process-config` blocker for the ADR-0016 process-level criteria (2.4.5 / 3.2.3 / 3.2.4) when `processes` is undeclared — structural gap surfaces before downstream `no-evidence` hides the root cause; MCP tool hands the probe + loaded config's `processes` through, merges the new warning with `non_git_repo_signature_omitted`, and routes the `nextStep` hint on the new reasons; builder split into `resolveScope` / `evaluateCriteria` / `classifyOneCriterion` / `probeAttestedSource` to stay under the complexity budget; 11 new unit tests cover both variants + indeterminate probe + markdown rendering + priority of missing-process-config over staleness.]
- [x] **V1-CERT-ATTEST-INTEGRITY** ADR 0020 (`docs/adr/0020-attestation-ledger-integrity.md`) + implementation already shipped. Option (b) adopted: `ra11y attestations verify` subcommand uses git as the trust root — flags `removed-since-head`, `backdated-attestation`, `future-attestation-before-commit`; `uncommitted` is informational. Pure helper `verifyAttestationIntegrity` in `src/config/attestation-store.ts:275`; CLI wrapper in `src/cli/commands/attestations.ts`; unit tests at `tests/unit/cli/commands/attestations-verify.test.ts`. Backlog flip was lost in the 2026-04-19 history rewrite; restored here.
- [x] **V1-CERT-CLI-PATH** [6f01a0c + edd0a69 + 508465a + 86be52b + df3ba43 — `ra11y attest <criterionId>` wraps `appendAttestation` with mandatory `--reason`, criterion + rule-ID validation, and scope↔`--location` coupling; `ra11y conformance` wraps `buildConformanceStatement` + `renderConformanceMarkdown` (Markdown default, JSON via `--output`), signing the bundle when inside a git repo and emitting `non_git_repo_signature_omitted` otherwise; `ra11y conformance --verify <bundle.json>` wraps `verifyConformanceBundle`, reconstructing the SignatureInput from the current tree and reporting the drift reason on failure. `parseCliArgs` refactored to dispatch flag-commands + subcommand keywords via declarative arrays (kept under cognitive-complexity cap). Both paths wire through `ExitCode.OK` / `ExitCode.USER_ERROR`. 10 attest + 11 conformance integration tests cover argv → disk round-trip, validation branches, and verify drift detection. `docs/cli.md` documents both subcommands with flags, exit codes, and examples.]
- [x] **V1-CERT-VERSION-CONST** (926a839 — `src/version.ts` reads `package.json.version` at module load via `node:fs.readFileSync` + `new URL("../package.json", import.meta.url)`; all 8 call sites (sarif TOOL_VERSION, vpat EVALUATOR, baseline ra11yVersion, help VERSION, json ra11y.version, terminal banner, agent TOOL_VERSION, mcp SERVER_VERSION) import from it; formatters snapshot test updated to compare against the derived constant; new `tests/unit/version.test.ts` guards drift.)
- [x] **V1-CERT-VPAT-25** (c4fdf5d + 3a43276 + 95a9104 + 9d94e3c — MCP `vpat` tool parity added 2026-04-19: mirrors `conformance_statement` shape — structured `standards[].entries[]` per-criterion, `nextStep` + `nextStepStructured` pair routing to `scan_project` / `attest` / `conformance_statement`, honest warnings (`scanned_zero_files`, `no_config_found`, `product_metadata_placeholders_in_use` when empty product metadata surfaces `<Product Name>` / `<Product Version>`), `format: "markdown"` attaches `markdownRendering`, `format: "json"` omits it. Seven-scenario integration test covers happy path, missing-param error envelope, media-absent Not Applicable, markdown/json split, empty-directory warning, placeholder emission, and `additionalPaths` round-trip. docs/mcp/tool-reference.md documents the new tool.) (c4fdf5d — VPAT builder retargeted at the ITI VPAT 2.5 Rev template, expanding to "VPAT 2.5 Rev INT" whenever EN 301 549 is in scope. `VpatReport` now carries `product: VpatProductMetadata` (Name / Version required, Contact Email / Organization, Evaluation Methods, Notes optional) with `<Product Name>` / `<Product Version>` template placeholders when unset so gaps stay visible to the VPAT reader; `buildVpatReport` accepts a `VpatBuildOptions` bag while retaining the legacy `(generatedAt, candidates)` signature. `buildEntry` now invokes `detectApplicability` via `isLikelyIrrelevant` + `irrelevanceReason`, so media SCs (1.2.1-1.2.9, 1.4.2) emit "Not Applicable" with the deterministic absence reason instead of "Not Evaluated"; `runVpat` threads applicability through from the parsed file set. Remarks rewritten for a procurement audience — each row states its verdict, SC local ID + title + level, and failing rule IDs; no references to terminal output, JSON report, or --checklist. Applicable-chapters block lists Chapters 3-7 with scope notes (ra11y is a Chapter 5 source). Product metadata surfaces via `RA11Y_VPAT_PRODUCT_NAME`, `_PRODUCT_VERSION`, `_CONTACT_EMAIL`, `_CONTACT_ORGANIZATION`, `_EVALUATION_METHODS`, `_NOTES` env vars. Documented in `docs/cli.md`; `docs/certification/vpat-mapping.md` now points at `src/mcp/manual-applicability.ts` as the presence-check source of truth.)
- [x] **V1-CERT-E2E-TEST** [a9f2419 — tests/integration/conformance-e2e.test.ts exercises scan → attest → conformance_statement → verifyConformanceBundle against real on-disk git repos with deterministic GIT_AUTHOR_DATE / GIT_COMMITTER_DATE stamps. Five scenarios: (a) happy-path signed bundle round-trips valid; (b) post-attestation commit flips attested-pass criteria into reason: "stale-attestation" blockers carrying staleAttestedAt; (c) mutating a scanned file post-sign returns file-manifest-mismatch from verifyConformanceBundle; (d1) rewriting a committed attestation line surfaces removed-since-head from `ra11y attestations verify` (ADR 0020) with non-zero exit; (d2) forging a post-sign attestation flips the signature verifier to attestation-set-mismatch. No mocks; git shelled out via node:child_process.]

### v1.0.0 — coverage honesty

- [x] **V1-COVERAGE-MATRIX** Authoritative matrix published at `docs/kb/standards/coverage.md` (92f6070 generator + c7d41fa doc + check-kb-drift extension). 252 unique criteria across 5 standards: 116 rule · 105 finder · 25 attestation-only · 6 gap. All six gaps are AAA-only (animation-from-interactions, link-purpose-link-only, target-size AAA) where the AA equivalent already ships. Drift check green.
- [x] **V1-CRITERION-KB** Full acceptance reached (26158e7 + fca13f1). Attestation-only half: 25 KB entries landed (ca35661) — 13 wcag22 + 10 wcag21 + 1 section508 + 1 en301549. Finder-backed half: 7 new KB entries added via V1-KB-FINDER-GUIDANCE (26158e7); wcag22:3.1.3 was already covered. GUIDANCE_BY_ID fully removed from src/reports/checklist.ts (fca13f1). Every criterion in the coverage matrix now has a KB file.
- [x] **V1-FIX-AUDIT** Acceptance reached (2026-04-19 run): all 11 generic-fix-text rules shipped context-aware builders. Resolved via: V1-FIX-DOC-LANG (746ecee), V1-FIX-DOC-TITLE (1a639b3), V1-FIX-TABINDEX-POSITIVE (7a24546), V1-FIX-NON-EMPTY-LABEL (0da9c7e), V1-FIX-VIDEO-CAPTIONS (03f9896), V1-FIX-LINK-NO-HREF (aeb5ddc), V1-FIX-DUPLICATE-ID (4e0d212), V1-FIX-BUTTON-NAME (cf53e34), V1-FIX-EMPTY-HEADING (b374e12), V1-FIX-LANDMARK-MAIN (06035ee), V1-FIX-TABLE-HEADERS (cb70741). Audit doc (`docs/kb/rules/fix-suggestion-audit.md`) distribution now 52 context-aware · 0 generic · 0 caveat-only · 0 needs-review.
- [x] **V1-FIX-RANK-DOC** `docs/kb/patterns/suggest-fix-ranking.md` published (92f6070). Documents the two axes (fixClass on Violation vs kind on suggest_fix response), batch-routing table for all four fixClass values with per-value rule inventories, primary/alternatives ranking contract, edit vs editCandidate distinction, verifyCommand post-fix workflow. `confidenceRationale?: string` decision: **defer** — existing `primary.label`, `explanation`, `caveat` already carry the signal in human-readable form.

### v1.0.0 — surface discoverability

- [x] **V1-PROMPT-LINK** Prompt templates wired into `nextStep` prose across scan / scan_project / review_candidates / checklist (52f97aa). `ra11y/triage` surfaces on clean+manual and on review_candidates; `ra11y/audit` on clean+zero-manual and on checklist-zero-actionable; `ra11y/fix` on violations-with-fix and all-mechanical; `ra11y/vpat-narrative` on checklist-zero-actionable alongside audit. `nextStepStructured` left as-is per ADR 0010 alignment.

### v1.0.0 — real-world fixture push

Track F shipped 10 fixtures, all guarding scanner infrastructure (wrappers, templates, TSX generics). Zero fixtures exercise rule-territory behavior in production-like shape. Each item below lands one sanitized fixture under `tests/fixtures/real-world/<case>/` with `source/` + `assertions.ts`, following the ADR 0006 harness. Dispatch in parallel — independent fixtures.

- [x] **V1-FIXTURE-FORMS** `tests/fixtures/real-world/forms-validation/` landed (7694cdb). Asserts labels-required, fieldset-legend, required-indicator-missing, autocomplete-missing rules; validation-timing, error-identification, server-error-untied finders. Zero-parse-errors across ContactForm.tsx + PhoneField.tsx.
- [x] **V1-FIXTURE-NAV** `tests/fixtures/real-world/nav-landmarks/` landed (f97ccbf). Asserts landmark-main, skip-link, link-descriptive-text rules + consistent-navigation finder across two divergent route files (home.tsx + about.tsx) and shell.html.
- [x] **V1-FIXTURE-DIALOG** `tests/fixtures/real-world/dialog-modal/` landed (2be0974). Three ConfirmDialog variants + page.tsx; asserts no-keyboard-trap finder fires on `role="dialog"` regardless of labeling; valid ARIA role passes aria/invalid-role; backdrop onClick exempted by keyboard/handler-missing. Drift: no rule today emits `couldBeWrongBecause` on dialogs — documented in fixture README.
- [x] **V1-FIXTURE-TABLE** `tests/fixtures/real-world/data-tables/` landed (aaa94ed). Three variants — SimpleTable (td-only, violation), ScopedHeaderTable (clean), ComplexHeaderTable (clean). Finding: `semantics/table-headers` satisfies wcag22:1.3.1; no table-specific review finder exists (Track R gap).
- [x] **V1-FIXTURE-LIVE-REGION** `tests/fixtures/real-world/live-region-status/` landed (aaa94ed bundle). StatusBanner + ToastRegion + Page; asserts aria/live-region-valid fires on invalid tokens, role/aria-live contradictions, hidden regions. Drift: `couldBeWrongBecause: "runtime_behavior_required"` is NOT emitted by this rule today — documented in fixture README; that enrichment would be a Track R item.
- [x] **V1-FIXTURE-ATTEST** Both bridge fixtures and the bridging-runtime-a11y KB page were ultimately removed (3ad371d dropped one sibling; the second was removed once it was clear the canonical worked example was itself the implicit-endorsement vector). The `attest` flow stays exercised by unit + integration tests directly; ra11y stays vendor-neutral by not naming any specific runtime scanner in the codebase.

### v1.0.0 — deferred-decision closure

Each item closes one deferred ADR or semver-major decision. Acceptance for each: an ADR marked Accepted (or Superseded / Rejected) + the corresponding backlog item flipped.

- [x] **V1-RULE-RENAME-DECIDE** Deferred past v1.0 via ADR 0018 (93e0207). Current rule names stay stable; revisit on user complaints / further catalog-overlap signals.
- [x] **V1-COV-CHECK-MERGE** Deferred past v1.0 via ADR 0018 (93e0207). ADR 0010 stays; coverage + checklist remain separate tools with cross-linked `nextStep` pairs. Revisit on concrete round-trip-cost evidence.
- [x] **V1-PARSER-SUBPKG-DECIDE** Deferred past v1.0 via ADR 0018 (93e0207). ADR 0003 stays; core stays monolithic. Revisit on peer-resolution edge cases or a second non-trivial parser.
- [x] **V1-SAMPLING-TOOL-PICK** Deferred past v1.0 via ADR 0018 (93e0207). Track S `[!]` items remain blocked; `verdict-candidate` is the pre-picked first promotion when a sampling-capable host surfaces a concrete workflow.

### v1.0.0 — plugin API integrity

Source: 2026-04-20 architectural audit — two independent Explore passes converged on the same finding, verified by grep. `grep -rln "BUILTIN_RULES\|BUILTIN_STANDARDS" src/` returns 33 files; 31 of those are non-barrel leaks past the existing `src/engine/registry/{criteria,rules,standards}.ts` primitives. The leak means CLAUDE.md §3.8 is aspirational, not enforced, and ADR 0019's frozen `defineRule`/`defineStandard`/`defineCandidateFinder` exports function as type-inference helpers only — user-authored rules have no wiring path to `list_rules`, `suggest_fix`, or any scan. ADR 0022 has the full design and 7-commit migration plan. Dispatch sequential within this subsection: stages 1 → 2 → {3, 5} → 4 → 6 → 7, with 3/4/5 parallelizable across agents once 2 lands. Every stage is additive and reversible until V1-REGISTRY-LINT.

- [x] **V1-REGISTRY-AGGREGATE** (68e1240) New `src/engine/registry/registry.ts` wraps the existing `CriteriaRegistry` + `RulesRegistry` + `StandardsRegistry` primitives via a `Registry` class with `rules`, `standards`, `finders`, `criteria` arrays + `findRule` / `findStandard` / `findCriterion` / `rulesForCriterion` accessors. `rulesForCriterion` closes over the reciprocal equivalence graph — returns `readonly Rule[]` in input-position order so call-site swaps stay deterministic. `createBuiltinRegistry()` is the designated single-call site into `BUILTIN_RULES` / `BUILTIN_STANDARDS` / `BUILTIN_CANDIDATE_FINDERS`; no other `src/engine/**` file references those barrels. 20 unit tests cover accessor round-trips, transitive equivalence (both directions), input-order determinism, and a shipped-rule smoke (`media/alt-text-missing` surfaces under `wcag22:1.1.1` + declared equivalents). Pure addition — zero call-site change; stages 2–7 migrate consumers.
- [x] **V1-REGISTRY-SESSION** (06d6ea5) `McpSession` constructor now instantiates `createBuiltinRegistry()` and exposes it as `readonly registry: Registry`. Zero call-site migration — existing `BUILTIN_*` imports inside `src/mcp/**` untouched so stage 3+ can land incrementally. Three new tests in `tests/unit/mcp/session.test.ts` pin the construction contract + the `findRule("contrast/minimum")` / unknown-ID round-trip.
- [x] **V1-REGISTRY-HELPERS** (9ebfbce) Scope mis-sized. Four of the six call sites sit inside exported module-level helpers with no `session` parameter — `findRule` / `findStandard` / `firstUnknownStandard` / `satisfyingRulesForCriterion`. Dropping the `BUILTIN_*` barrels requires adding a `session` parameter to each, which cascades to ~9 single-line call-site updates across eight stage-4 files (`src/mcp/tools.ts`, `tool-suggest-fix.ts`, `tool-coverage.ts`, `tool-checklist.ts`, `tool-vpat.ts`, `tool-review-candidates.ts`, `tool-conformance-statement.ts`, `tool-attest.ts`). The `Registry` aggregate surface is sufficient — this is a helper-signature constraint, not an accessor gap. Two resolutions: (1) merge stages 3 + 4a into one commit that threads `session` through callers (net diff ≈20 lines outside `tools-helpers.ts`); (2) split stage 3 into 3a (migrate the two session-bearing sites, keep the four helpers reaching `BUILTIN_*` locally) + 3b (drop barrels alongside helper-signature change alongside stage 4). Prefer (1) on 2026-04-21 re-plan. Blocked per the `/continue` scope-lock; re-dispatch with widened scope.
- [x] **V1-REGISTRY-MCP-TOOLS** (6d6d6a8 — 6 atomic commits dd47e19..5d94912) Migrate `src/mcp/tools.ts` + `src/mcp/tool-*.ts` (16 files) to read through `session.registry`. Dispatch in parallel groups by family: {scan + scan-process + review-candidates}, {suggest-fix + apply-fix + apply-fix-internals}, {baseline + propose-baseline + attest}, {coverage + checklist + vpat + conformance-statement + explain-standard + list-suppressions + propose-config}, plus {completions + scan-project-budget + wrappers-meta}. Each group is its own commit under the 400-LOC budget. Blocks on V1-REGISTRY-HELPERS. Owner: main session + parallel agent dispatch. Medium (≈4 commits).
- [x] **V1-REGISTRY-CLI** Landed across three commits (1a57d4b scan-family + 734490d explain-family + 1c46a6c list-family). All eleven `src/cli/commands/*.ts` hits and `src/cli/run.ts` now route through a `registry: Registry = createBuiltinRegistry()` parameter; `attest` dropped its local `criterionExists` / `satisfyingRulesForCriterion` helpers in favor of `registry.findCriterion` + `registry.rulesForCriterion` (equivalence-closure semantics now match MCP attest). Invariant: `rg "BUILTIN_(RULES|STANDARDS|CANDIDATE_FINDERS)" src/cli/` returns zero hits. `V1-REGISTRY-LINT` can now converge once `V1-REGISTRY-MCP-TOOLS` (blocked on `V1-REGISTRY-HELPERS`) lands.
- [x] **V1-REGISTRY-LINT** (f6c133a) New `scripts/check-builtins-scope.ts` modeled on `scripts/check-network-isolation.ts`. Forbids importing `BUILTIN_RULES` / `BUILTIN_STANDARDS` / `BUILTIN_CANDIDATE_FINDERS` outside `src/engine/registry/**`, the three barrel files themselves, and `tests/**`. Added to the `scripts/verify.ts` sequence (CLAUDE.md §4). Acceptance: `bun scripts/check-builtins-scope.ts` green. Blocks on V1-REGISTRY-MCP-TOOLS + V1-REGISTRY-CLI converging (every non-barrel call site must be migrated first). This is the commit that locks the invariant. Owner: main session. Small.
- [x] **V1-REGISTRY-PLUGIN-WIRE** `src/api/plugin.ts` helpers remain identity functions at the type level (ADR 0019 freeze), but `createRegistry({ rules, standards, finders })` now composes builtins with user-authored additions. `McpSession` + CLI bootstrap accept a registry override so a future `ra11y.config.ts` hook or CLI `--plugin` flag can thread one in. Working example under `examples/plugin-rule/` showing a user rule visible to `list_rules` and producing violations through `scan_project`. Integration test covering end-to-end plugin path. ADR 0022 flipped Proposed → Accepted. (b546831)

### v1.0.0 — MCP response-assembly seam

Source: 2026-04-20 architectural audit (Explore-agent survey of `src/mcp/**` + grep verification). Same class of leak as V1-REGISTRY-AGGREGATE but one layer up: the AI-first consumer doctrine in `docs/kb/architecture/ai-first-consumer.md` (conditional-spread on zero counts, `warnings` on zero-output success, split composite headline counters, present-when-meaningful optional fields, honest `fixesByClass` tally) is currently enforced in 24 separate `src/mcp/tool-*.ts` files by convention + the path-scoped rule at `.claude/rules/mcp-response-shapes.md`. Verified counts on 2026-04-20:

| measurement                                                                 | count |
|-----------------------------------------------------------------------------|------:|
| `src/mcp/tool-*.ts` files                                                   | 24    |
| Tool files routing through `runScanAndFormat` (the existing near-assembler) | **2** (`tool-scan-project.ts`, `tool-scan-diff.ts`) |
| Tool files hand-rolling `meta: {` blocks directly                           | 10    |
| Tool files calling `buildAgentFinding` or composing `AgentFinding` manually | 8+    |
| `scan` + `scan_file` tool definitions                                       | inline in `src/mcp/tools.ts` (625 LOC mixing schemas + handlers) |
| `src/mcp/tools-helpers.ts` current size                                     | 856 LOC (hosts `runScanAndFormat` + fraying) |
| `scripts/check-response-nullability.ts` allowlist entries                   | 1 (catches a narrow slice; doesn't catch field-presence drift) |

The partial assembler `src/mcp/scan-assembly.ts` (`buildScanPlan` / `buildScanMeta` / `buildAnalysisCoverage` / `suppressionsMetaBlock` / `wrappersMetaBlock`) is already the right shape, but it lives underneath a non-authoritative wrapper (`runScanAndFormat`) and is only consumed by 2 of 24 tools. Every new scan-family field (future sampling outputs, new fix-class lanes, new `warnings` codes, new honest-counter splits) currently requires touching ≤24 sites with no CI gate forcing synchrony — silent cross-surface drift is the waiting failure mode, and the invariants called out in `ai-first-consumer.md` ("`scan` says 21, `checklist` says 4") are exactly what this seam prevents.

Why v1.0 blocker: the MCP response shape is part of the public contract v1.0 freezes (ADR 0019 + the migration table in `docs/migrations/0.1-to-0.2.md`). Freezing the shape before codifying the single-path invariant means third-party tooling pattern-matches on hand-rolled examples; changing the pattern later is a semver-breaking event. This is structurally identical to the Registry aggregate work (ADR 0022) — an internal seam that must be in place before the surface it backs becomes immutable.

Design captured in ADR 0024 (Proposed, 2026-04-20). Dispatch sequential within this subsection: stage 1 (assembler) → 2 (handler extract from `tools.ts`) → {3, 4, 5 parallel} → 6 (lint) → 7 (handler-size budget). Stages 3-5 are parallelizable across agents on separate worktrees (no shared files — each touches a disjoint set of `tool-*.ts`). Every stage is additive and reversible until V1-RESPONSE-LINT. Golden snapshots under `tests/snapshots/mcp/**` must stay byte-identical through every refactor commit — any intentional shape change splits into a separate `feat(mcp):` commit with rationale.

- [x] **V1-RESPONSE-ASSEMBLER** (e095fc9) New `src/mcp/response-assembler.ts` exporting `assembleScanFamilyResponse(input: ScanFamilyResponseInput, options?: ScanFamilyResponseOptions): ScanFamilyResponse`. `input` carries `violations, perRuleCoverage, reviewCandidates, discovery, wrappers, config, durationMs, toolName, scopeHint`; `options` carries `hoistReferenceGuide, includeReviewCandidates, tokenBudget`. Internally orchestrates the existing extracted helpers: `buildScanPlan` + `buildScanMeta` + `buildAnalysisCoverage` + `suppressionsMetaBlock` + `wrappersMetaBlock` + `buildAgentFinding` (per violation) + `buildReferenceGuide` + `dedupReviewCandidates` + `tokenBudget` clamping + `warnings` code emission (`scanned_zero_files`, `root_source_defaulted`, `no_config_found`, `tailwind_detected_css_undercounted`, `template_files_parsed_as_literal`). Pure addition — zero call-site change; stages 3-5 migrate consumers. Unit tests mirror the existing assembly-helper tests: honest-counter-split cases, conditional-spread fixesByClass, empty-scan warnings emission, reference-guide dedup, per-scope variants. Blocks on nothing. Owner: `feature-dev:code-architect`. Medium. ≤400 LOC net.
- [x] **V1-RESPONSE-SCAN-CORE** (d32bdb8..7e7a523) `scan` + `scan_file` migrated through `assembleScanFamilyResponse`; handlers extracted to `src/mcp/tool-scan.ts` + `src/mcp/tool-scan-file.ts`; `src/mcp/scan-collect.ts` added as `runScanAndCollect` seam helper. `scan_process` deferred — per-page `ScanResult[]` shape is structurally different and warrants separate analysis; split into V1-RESPONSE-SCAN-PROCESS below. 4 commits: 1b273e1 64c5f21 ac7f760 61be322.
- [x] **V1-RESPONSE-SCAN-PROCESS** Decision: `scan_process` stays OUTSIDE `assembleScanFamilyResponse`. Three converging reasons: (1) `perPageResults: ScanResult[]` is the documented primitive (ADR 0016) future process-level finders cross-index — collapsing N pages into one `files[]` discards the per-page boundary; (2) `meta` is process-level (`processName`, `resolvedProcess`, `missingPages`) not scan-level (`activeNativeWrappers`, `rulesEvaluated`, …) — merging would violate "ambiguous field shapes are dishonest"; (3) looping the assembler per-page would force composing a dishonest composite plan/meta across pages. Rationale lives as a durable docblock at the top of `src/mcp/tool-scan-process.ts` so V1-RESPONSE-LINT's allowlist entry can cite the file by path. No code changes to the handler; no snapshot drift.
- [x] **V1-RESPONSE-SCAN-DERIVATIVE** (7007df8..7b2f49a) Migrated `checklist` + `coverage` + `conformance_statement` handlers through `buildDerivativeScanWarnings` assembler seam. `buildDerivativeScanWarnings` exported from `src/mcp/response-assembler.ts`; cross-tool invariant test guards `scan.totalFindings === checklist.automatedFindings + checklist.manualReviewRequired` drift class. No snapshot drift (derivative tools use bespoke shapes). 2 commits.
- [x] **V1-RESPONSE-FIX-FAMILY** — **DECISION 2026-04-21: pick (a), narrow scope.** Share the assembler's `groupByFile` helper for `baseline.check` only; drop `apply_fix` + `suggest_fix` from scope. Preserves the byte-identical-snapshots acceptance gate that v1.0.0 depends on. `apply_fix` and `suggest_fix` legitimately emit different shapes (before/after slices, primary/alternative fix paths) — forcing them through the assembler would sum categorically different data, which the "composite headline counts are dishonest" rule forbids. ADR 0024's lint gate (scripts/check-response-assembly.ts), LOC budget, and assembler for scan-core + scan-derivative tools all still land. If post-v1 an agent reports cross-tool drift between `baseline` and `scan_project`, revisit — structurally the fix-family responses carry different data.
- [x] **V1-RESPONSE-LINT** New `scripts/check-response-assembly.ts` modeled on `scripts/check-network-isolation.ts` + `scripts/check-response-nullability.ts`. Statically forbids scan-family tool files from defining object literals with a `plan` key or a `meta: {` block without routing through `assembleScanFamilyResponse`. Allowlist: `src/mcp/response-assembler.ts`, `src/mcp/scan-assembly.ts` (the underlying helpers), non-scan-family tools (`attest`, `suppress`, `list_rules`, `list_suppressions`, `explain_*`, `bootstrap`, `detect_native_wrappers`, `wrapper_introspect`, `sessionConfigure`), and `tests/**`. Added to the `scripts/verify.ts` sequence (CLAUDE.md §4). Acceptance: `bun scripts/check-response-assembly.ts` green; dry-run synthetic test (temporarily hand-roll a `plan:` block in `tool-coverage.ts`, confirm the check fails, revert) passes before landing. This is the commit that locks the invariant. Blocks on V1-RESPONSE-SCAN-CORE + V1-RESPONSE-SCAN-DERIVATIVE + V1-RESPONSE-FIX-FAMILY converging. Owner: main session. Small.
- [x] **V1-RESPONSE-HANDLER-BUDGET** Enforce `src/mcp/tool-*.ts` ≤ 150 LOC after migration via an extension to `scripts/check-limits.ts`. Handlers that still exceed the budget are doing work that belongs in the assembler — keep extracting until they fit (or carry an inline `// ra11y-limits-exempt: <reason>` pragma, matching the existing limits-check escape). Acceptance: `bun scripts/check-limits.ts` green; aggregate `src/mcp/tool-*.ts` LOC drops from 8,485 to ≤ 3,600 (≈150 × 24). Blocks on V1-RESPONSE-LINT. Owner: main session. Small. Final commit of the subsection — flips ADR 0024 status Proposed → Accepted.

### v1.0.0 — release hygiene

- [x] **V1-BENCH-BASELINE** v1.0 performance baseline locked (8e8a564). All four scenarios 79-95% under budget: cold 41ms/200ms, 10-files 5.2ms/100ms, 100-files 28.5ms/500ms, 1000-files 272ms/3000ms. Headroom watch: cold-start grows with CLI module graph; 1000-file throughput scales linearly with rule count.
- [x] **V1-API-STABILITY-AUDIT** v1.0 public surface frozen in ADR 0019. Audit confirmed `check-tsdoc.ts` passes on baseline; no `@experimental` / `@internal` tags on exported names (the one `@internal` marker in the tree sits on a non-exported symbol in `src/output/agent-response/build-finding.ts`); no accidental re-exports of internal types (types not listed in `src/types/index.ts` are deep-import-only and remain free to evolve). ADR enumerates every exported name across both entry points: 8 names from `@ra11y/core` (`Criterion`, `ReportData`, `ScanResult`, `Severity`, `Standard`, `Violation`, `scan`, `ScanOptions`) and 7 from `@ra11y/core/plugin` (`defineRule`, `defineStandard`, `defineFormatter`, `defineConfig`, `defineCandidateFinder`, `FormatterFn`, `Formatter`). Escape-hatch `experimental` column documented but empty at v1.0.
- [~] **V1-CHANGELOG-V1** Draft `## [1.0.0]` entry landed (1d784b7). Date placeholder `YYYY-MM-DD` stays until user says ship. Sections populated: Breaking Changes (exit-code freeze), Added (13 sub-groups), Changed (9 items), Deprecated (2 aliases), Fixed (19 items), Deferred (the four ADR 0018 items). New empty `## [Unreleased]` sits at top. Ready for `/release 1.0.0` when the user triggers.

### v1.0.0 — test coverage gates

MCP surface and several CLI commands have severe coverage deficits that contradict the "find all violations" bar. Every item here lands tests against committed behavior; no behavioral changes.

- [x] **V1-TEST-MCP-SERVER** `src/mcp/server.ts` raised from 8.28% → 96.96% lines / 97.22% funcs (eb6fd73 + cb0da5a + d42c6fb). 36 new in-process dispatch tests via a new shared `tests/helpers/mcp-harness.ts` since integration tests spawn subprocesses and don't cross instrumentation. Remaining uncovered lines are defensive/unreachable branches.
- [x] **V1-TEST-MCP-SCAN-DIFF** `src/mcp/tool-scan-diff.ts` raised from 18.12% → 96.90% lines / 95% funcs (c3efd95 + 5d0c99b). 21 in-process dispatch tests cover baseline mode, hunks mode, structured errors, symlink normalization, stable `findingId`/`groupKey`. Remaining uncovered branches are impossible-in-practice race cases.
- [x] **V1-TEST-MCP-APPLY-FIX** `src/mcp/tool-apply-fix.ts` raised from 43.71% → 100% lines / 95.49% branches (120569d + 6f36e30). 26 new unit tests drive the handler in-process (existing integration tests drive the subprocess, which instrumentation doesn't cross). Internals file `tool-apply-fix-internals.ts` rode along to 96.55%/95.91%.
- [x] **V1-TEST-CLI-CMDS** Six CLI commands lifted from 0% to 100% function coverage (b827dc7 + 30644ea + 4342de4). `certification`, `checklist`, `coverage`, `vpat`, `doctor`, `init` all exercised via in-process handler invocations against tmpdir fixtures; `doctor` line-coverage at 87.93% (remaining branches require mocking `process.version`), all others at 100%.
- [x] **V1-TEST-MCP-GLUE** All five target modules raised to 100% lines / 100% branches (235db1a + 28f2224 + 7740a44; logging.test.ts file swept into 5d0c99b by parallel agent). 59 new tests across completions, deprecation-warning, outbound, logging, ra11y-kb resources. Every module confirmed reachable from `src/mcp/server.ts`.
- [x] **V1-TEST-CONFIG-SCHEMA** `src/config/schema.ts` raised from 6.94% → 100% lines / 100% funcs (4dfd9d6). 42 new tests in `tests/unit/config/schema.test.ts` covering accepted shapes, rejection paths, error-path indices, and output normalization.

### v1.0.0 — CI + release pipeline gates

- [x] **V1-CI-WINDOWS** `windows-latest` added to verify matrix (b0b07cc). Fail-fast stays false so an OS hiccup doesn't cancel the other lanes.
- [x] **V1-RELEASE-VERIFY-FULL** Already satisfied — `.github/workflows/release.yml:43` runs `bun run verify` (full, not trimmed). The second-pass audit that flagged this was against stale state.
- [x] **V1-CI-SARIF-UPLOAD** Self-scan job added to ci.yml (6315a06). Runs `ra11y scan src/ --format sarif --fail-on never` and uploads to GitHub Security via `github/codeql-action/upload-sarif@v3`. Makes ra11y the reference implementation of its own CI integration.
- [x] **V1-CI-DEP-REVIEW** `.github/workflows/dependency-review.yml` added (914d4f1). GitHub's dependency-review action runs on every PR alongside the in-house zero-dep guard; `fail-on-severity: moderate`, `comment-summary-in-pr: on-failure`.
- [x] **V1-EXIT-CODES** Canonical `ExitCode.{OK,VIOLATIONS,USER_ERROR,NEW_VIOLATIONS}` enum landed in `src/cli/exit-codes.ts` (8e8b0ef). Every command now returns via the enum; `ra11y --help` renders the legend from `EXIT_CODE_LEGEND`; `docs/cli.md` cross-links `docs/errors.md`. Frozen at v1.0.
- [x] **V1-MCP-ERRORS-DOC** `docs/errors.md` created (5bfcf34). 26 MCP structured errors indexed (24 from the central union + 2 suppress-local cast-through codes documented as intentional), 4 CLI exit codes indexed. Canonical error + exit-code reference.

### v1.0.0 — code-quality polish

- [x] **V1-TYPE-ESCAPE** Redundant `as unknown` cast dropped (7979ef8); `coerceAttestationRecord` already accepts `unknown`, the cast widened a narrower type to no purpose.
- [x] **V1-ERROR-MSG-QUALITY** All three throw sites rewritten (7979ef8): sampling.ts now names the actual type returned + links the MCP spec + suggests the host decline the capability; attestation-store.ts enumerates the specific failed field + shows a concrete example record; tool-suggest-fix-internals.ts is marked as an internal invariant with a file-an-issue hint.
- [x] **V1-COMMENT-DRIFT** Hardcoded count removed (456158d); the comment now describes the contents (excludes 4.1.1, includes the nine new 2.2 criteria) rather than asserting a drifting count.
- [x] **V1-README-PLUGIN-LINK** Per-plugin-kind authoring guides now linked inline in the Plugin API section (3379e03). The prior scan's claim of a broken `docs/plugin-authoring.md` link did not match the current README — the three `docs/plugins/authoring-a-*.md` guides are now cross-linked alongside the examples + architecture deep-dive.
- [x] **V1-DOCS-CLI-COMMANDS** Per-command reference section added to `docs/cli.md` (192cf0d). Each command gets a subsection with purpose + representative invocation + key non-global flags; `baseline prune` + `attestations prune` subcommands now documented; `--profile` flag added; usage synopsis updated to reflect command dispatch.
- [x] **V1-MIGRATION-0.2-TO-1.0** `docs/migrations/0.2-to-1.0.md` landed (2f9af2e). TL;DR: v1.0 is additive for most consumers. 4 deferred items (from ADR 0018) + 1 breaking change (exit-code freeze) + 2 soft deprecations (`configure` → `sessionConfigure`, `filePath` → `file`). Cross-linked from CHANGELOG.md + docs/getting-started.md.

### v1.0.0 — detection gaps

Source: "find all violations" bar re-raised 2026-04-19. Four-agent gap audit against WCAG 2.2 A+AA surface. Each item widens detection on an existing rule or adds a small new primitive — none reshape the engine. Gaps are where a real violation on common production patterns walks past our rules undetected. Dispatch in parallel — independent files.

- [x] **V1-DETECT-ALT-WIDEN** (db325c9, a174d62) `src/rules/media/alt-text-missing.ts` today only walks `<img>` and `<input type="image">` (line 13 header). Real violation patterns miss: (a) SVG `<image src="…">` inside `<svg>`; (b) `role="img"` on arbitrary elements (`<div role="img">`, `<span role="img">`); (c) `<canvas>` with no fallback text + no aria-label/aria-labelledby. Widen element walk + apply existing `hasAccessibleName*` logic. Satisfies wcag22:1.1.1, wcag21:1.1.1. Fix scope: rule extension; add three walkers with the same accessible-name check that runs today on `<img>`. Tests: `tests/fixtures/{good,bad}/alt-text-missing/` gains svg-image, role-img-div, canvas-no-fallback cases. Effort: small.
- [x] **V1-DETECT-BUTTON-NAME-SVG** (2af5caf) `src/rules/semantics/button-name.ts:130` uses `htmlTextContent` which walks HtmlElement + HtmlText but does NOT descend into SVG `<text>` / `<title>` children. `<button><svg><title>Close</title></svg></button>` computes as name-less today but is accessibly named in every major AT. Same blind spot for `<a>`/interactive controls. Extend `hasAccessibleNameHtml`/`hasAccessibleNameJsx` to walk SVG `<title>` + `<text>` as direct children of interactive controls. Satisfies wcag22:4.1.2. Effort: small.
- [x] **V1-DETECT-BUTTON-NAME-IMAGE-INPUT** (2af5caf) `src/rules/semantics/button-name.ts:155-173` filters `["button", "submit", "reset"]` only; `<input type="image" src=... title="Submit">` is never checked. Alt-text rule covers the `alt` path but not the `title`-as-name path — so a `title`-named image input reports no violation correctly, but a name-less one also reports no violation (false negative). Add `type="image"` to the button-name filter and recognize `alt` OR `title` OR `aria-label` as valid name sources. Satisfies wcag22:4.1.2, wcag22:1.1.1. Effort: small.
- [x] **V1-DETECT-LABELS-CONTENTEDITABLE** (e8ca208) `src/rules/forms/labels-required.ts:38` `LABELABLE_TAGS = ["input","select","textarea"]`. `<div contenteditable="true">` acts as a form control (Slack-style composers, Notion-style editors) and has the same labeling requirement under WCAG 1.3.2 + 3.3.2 but is not checked. Extend the element set to include `contenteditable="true"` (not `false` or `inherit`). Effort: small.
- [x] **V1-DETECT-CONTRAST-BG-IMAGE** (431cd1b, 6c49c2b) `src/rules/contrast/_shared.ts:114` resolves only `background-color` and `background` (color-only shorthand). Text over `background-image: url(…)` / `background: linear-gradient(…)` currently skips contrast evaluation silently — no violation, no `couldBeWrongBecause` signal. Two paths: (a) emit `couldBeWrongBecause: "background_image_unresolvable"` as additive signal for affected declarations (honest surfacing, no false positives); (b) defer luminance heuristic for gradients to a follow-up. Path (a) is the v1.0 scope. Effort: small.

### v1.0.0 — shape-honesty sweep

Source: same audit. Verified file:line hits on shape-honesty violations that survived Q/Q2/Q2R2. Every item below is a null-sentinel that CLAUDE.md §1 "Ambiguous field shapes are dishonest" calls out directly — forcing agents to disambiguate "field unavailable" vs "field empty."

- [x] **V1-SHAPE-NULL-SWEEP** (6b6349c) Replace 9 response-field null/empty sentinels with conditional-spread:
    - `src/mcp/tool-explain-standard.ts:65` `publisher: standard.publisher ?? null`
    - `src/mcp/tool-explain-standard.ts:66` `url: standard.url ?? null`
    - `src/mcp/tool-explain-standard.ts:68` `levelFilterApplied: level ?? null`
    - `src/mcp/tool-explain-standard.ts:75` `url: c.url ?? null`
    - `src/mcp/tool-explain-standard.ts:76` `equivalentTo: c.equivalentTo ?? []`
    - `src/mcp/rule-catalog.ts:77` `normativeQuote: rule.docs.normativeQuote ?? null`
    - `src/mcp/tool-review-candidates.ts:177` `title: criterion?.title ?? null`
    - `src/mcp/tool-review-candidates.ts:178` `level: criterion?.level ?? null`
    - `src/mcp/next-step.ts:147` `singleFilePath: options.singleFilePath ?? null`
  Exclude `detect-wrappers-core.ts:107` (`definitionFile`) — intentional per Q2-WRAPPATH ("`null` is emitted, not omitted — the attempt is meaningful"). Use conditional-spread: `...(x ? { field: x } : {})`. Audit `tests/integration/mcp-*.test.ts` for any snapshot assertions relying on the null — those are the contract tests to update. Effort: small.
- [x] **V1-SHAPE-NULL-AUDIT** Lint check shipped at `scripts/check-response-nullability.ts` (472e18b) and wired into `scripts/verify.ts` as `response-nullability`. Uses the TypeScript compiler API (not regex) to walk ancestors for `textResult(...)` / `errorResult(...)` calls — input-param normalization passes structurally, no carve-out needed. Catches `?? null`, `?? []`, `?? ""`, `?? 0`, `?? \`\``. Pre-loaded allowlist for the Q2-WRAPPATH `definitionFile` exception. 10 unit tests at `tests/unit/scripts/check-response-nullability.test.ts`. Backlog flip was lost in the 2026-04-19 history rewrite; restored here.

### v1.0.0 — fix-suggestion context-aware sweep

Source: V1-FIX-AUDIT (`[~]` above) enumerates 11 rules with generic fix text. Acceptance requires zero generic rows before v1.0 tag. Each item is sized for one ≤400-LOC commit per CLAUDE.md §9. Plans derived from fix-suggestion-audit.md. Dispatch in parallel — independent rules.

- [x] **V1-FIX-DOC-LANG** `src/rules/document/lang-attribute.ts:71-72` — inspect `<meta charset>` + sibling pages' lang values to propose a concrete lang candidate ("Add lang=\"en\" (detected from <meta charset=\"utf-8\">); if the page is French, use lang=\"fr\""). Small. [746ecee]
- [x] **V1-FIX-DOC-TITLE** `src/rules/document/page-titled.ts:68-86` — extract `<h1>` text + `<meta name="description">` as title candidate ("`<title>Contact Information — Acme</title>` — candidate from existing `<h1>` text"). Small. [1a639b3 — three-branch ladder (h1-text / meta-description truncated / fallback) inlines source line numbers and strips trailing punctuation from the meta candidate; 5 new tests cover all branches + empty-title re-use; audit row flipped to `context-aware`.]
- [x] **V1-FIX-TABINDEX-POSITIVE** `src/rules/focus/tabindex-positive.ts:210-223` — inline the offending tabindex value + branch on tag/role ("Remove tabindex=\"5\" from `<div>`; if focus is needed, use tabindex=\"0\" with role=\"button\""). Small. [7a24546 — three-branch ladder (natively focusable / contenteditable host / non-focusable fallback) inlines tabindex="N" + tag; keeps declared role or proposes role="button"; 9 new tests cover all branches in HTML+JSX; audit row flipped in b66f00e's drift-resolution.]
- [x] **V1-FIX-NON-EMPTY-LABEL** `src/rules/forms/non-empty-label.ts:96-129` — read `for`/`htmlFor` + locate matching id ("Label references id=\"email\"; move the email-input description text inside the label"). Small. [0da9c7e — four-branch ladder (no-target / matching control + hint / matching control no hint / unmatched id) inlines `for="id"` (or `htmlFor="id"`), `<tag type="…">` descriptor, control line number, and a candidate derived from `type` (13 semantic types → canonical labels), `placeholder` verbatim, or humanized `name` (`firstName` → "First name", `phone_number` → "Phone number"); 11 new unit tests cover all branches in HTML+JSX; audit row flipped to `context-aware`.]
- [x] **V1-FIX-VIDEO-CAPTIONS** `src/rules/media/video-captions-missing.ts:116-131` — extract video src basename + document lang for a concrete VTT path and srclang ("Add `<track kind=\"captions\" src=\"launch.vtt\" srclang=\"en\">` inside `<video src=\"launch.mp4\">`"). Small. [03f9896 — ladder: video `src` → strip-dir + swap-ext into concrete `src="<stem>.vtt"`; no `src` but `<source>` child → first-source basename; neither → `captions.vtt` with child-of-`<video>` phrasing. `srclang` inlined from nearest `<html lang="…">` (HTML root or JSX root-layout shape); fallback `en`. Common primary subtags humanize into `label="English captions"` etc. Captions-vs-subtitles guidance included in every suggestion.]
- [x] **V1-FIX-LINK-NO-HREF** `src/rules/navigation/link-no-href.ts:115-117` — inspect onClick expression body for navigation vs mutation keywords (navigate/route/history vs toggle/set/open) and branch the fix. Needs small new helper in `src/engine/ast-helpers.ts` for JSX expression keyword probe. Medium. [d76dcfb + aeb5ddc — new `describeJsxExpressionIntent(src: string | null)` helper is text-level and deterministic: navigation identifiers (navigate/router/history/redirect/location/goto/useNavigate/useRouter), `window.location`/`document.location`, and member-call `.push(` / `.replace(` / `.go(`; mutation identifiers (toggle/open/close/show/hide/dispatch/mutate/delete/save/submit/setState), React setter convention `set[A-Z]…(`, and camelCase verb-prefix form (`toggleMenu`, `openDialog`, `closeModal`); navigation wins ties; unknown honest-fallback keeps weak-evidence cases out of heuristic suppression. Rule now emits a three-branch suggestion: navigation → real `href="..."` + `preventDefault()` interception, mutation → `<button type="button">`, unknown → "decide the intent" prose. 8 new rule tests + 16 new helper tests across the three branches and the navigation-tie rule.]
- [x] **V1-FIX-DUPLICATE-ID** `src/rules/parsing/duplicate-id.ts:64-66` — inline `first.loc` line/tag + propose a numeric suffix candidate ("id=\"mainContent\" duplicates `<main>` at line 12 — use id=\"mainContent2\" or remove"). Small. [4e0d212 — pre-scans the document so the proposed suffix is guaranteed free; trailing-digit run stripped before incrementing (`section2`→`section3`, `item42`→`item43`); names aria-labelledby / aria-describedby / aria-controls / label[for] / `href="#id"` as the silent-first-match reference hooks; six new unit tests covering all branches.]
- [x] **V1-FIX-BUTTON-NAME** `src/rules/semantics/button-name.ts:304-319, 223-239` — detect icon-only child (SVG/img) and extract svg `<title>` / img alt as concrete aria-label candidate ("Button contains only `<svg><title>Close</title></svg>`; add aria-label=\"Close\""). Pairs with V1-DETECT-BUTTON-NAME-SVG — ship after that extension lands so the fix text cites the same helpers. Medium. [cf53e34 — `buildIconAwareSuggestion` branches on host subject + icon-child shell: SVG-icon branch names the `<svg><title>…</title></svg>` fix alongside `aria-label`; `<img>`-icon branch inlines a Title-Cased filename subject as both `alt=` and `aria-label=` candidates; `<input type="image">` branch never says "wraps an <img>" and cites HTML §4.10.5.1.18 that value is not a name source; empty `<input type="submit/button/reset">` branch proposes `value="…"` / `aria-label="…"` (void element, no nested children); empty `<button>` / `<a>` / `[role="button"]` keeps the visible-text example anchored on the actual host tag; 8 new unit tests covering HTML svg / img / empty-button / image-input-with-src / image-input-no-src / submit-empty-value / JSX image-input / JSX empty-button fix-text assertions.]
- [x] **V1-FIX-EMPTY-HEADING** `src/rules/semantics/empty-heading.ts:165-171, 174-180` — walk document for nearest preceding heading at adjacent level ("Empty `<h3>` follows \"Contact Information\" (h2); continue the hierarchy"). Small. (b374e12)
- [x] **V1-FIX-LANDMARK-MAIN** `src/rules/semantics/landmark-main.ts:78-91, 95-112` — when multiple `<main>` exist, inline each main's id/class for disambiguation ("Document has `<main id=\"app-main\">` (line 5) and `<main id=\"legacy-main\">` (line 45); demote one to `<section>`"). Small. (06035ee)
- [x] **V1-FIX-TABLE-HEADERS** `src/rules/semantics/table-headers.ts:154-157, 212-215` — inspect first-row `<tr>` children, detect header-shaped text (short, title-cased), suggest concrete td→th rewrite. Small. [cb70741 — `looksHeaderShaped` gates on ≤40 chars, starts-upper, non-numeric (currency/percent/signed rejected), and rejects ALL-CAPS multi-word shouts. `classifyHeaders` requires ≥2 cells and ≥60% header-shaped to populate each axis. Four-branch ladder in `buildSuggestionFromDetection`: (1) both axes → inline row + column candidates and mention scope="col" / scope="row" plus scope="colgroup" for complex tables; (2) row-only → inline detected column headers (e.g. `Name`, `Email`, `Role`, `Last Login`) and recommend scope="col"; (3) col-only → inline detected row labels and recommend scope="row"; (4) no signal → fallback naming both scopes and the role="presentation" layout escape. Five new unit tests cover row-only inline, col-only inline, numeric-only fallback, both-axes + colgroup mention, and JSX parity. Audit row was flipped during the link-no-href commit's index race (aeb5ddc).]

### v1.0.0 — criterion KB final sweep

- [x] **V1-KB-FINDER-GUIDANCE** 7 KB entries added (26158e7) + GUIDANCE_BY_ID removed (fca13f1): wcag22:1.2.1, 1.2.3, 1.4.1, 2.1.2, 2.3.1, 3.2.1, 3.2.2. wcag22:3.1.3 already had a KB entry. Map fully drained; checklist builder now uses criterion.description directly.

### v1.0.0 — external-codebase scan findings

- [x] **V1-DETECT-SILENT-EXT** `src/utils/path.ts:20-28` `PARSEABLE_EXTENSIONS` is `{.ts,.tsx,.js,.jsx,.html,.htm,.css}`. `hasParseableExtension` + `src/input/discover.ts` silently skip every other extension — no surfacing anywhere in the scan meta. On an Astro+SCSS tree, 226 source files (104 `.astro`, 122 `.scss`) were dropped at discovery; `filesScanned: 125` reads as "tool covered everything" when the tool actually ignored 64% of the repo. Landed: `discoverFilesWithDiagnostics` + `parseFilesWithDiagnostics` + warning code `extensions_skipped_no_parser` + `meta.analysisCoverage.skippedByExtension` wired through `runScanAndFormat`; `scan_project` threads the diagnostic. Follow-up: migrate remaining scan-surface tools (scan, scan_file, scan_diff) to `parseFilesWithDiagnostics` so they also emit the warning.
- [x] **V1-SHAPE-SNIPPET-EMPTY** `src/output/agent-response/build-finding.ts` + `src/output/agent-response/types.ts` — `buildSnippet` returned `{ before: [], highlighted: v.snippet ?? "", after: [] }`; `before` / `after` had zero writers anywhere in src/. Landed: `AgentFinding.snippet` is now `string | undefined`, omitted via conditional-spread when absent; `AgentSnippet` type removed; snapshot tests + CHANGELOG Breaking Changes entry updated.
- [x] **V1-SIZE-LABEL-ECHO-CAP** `truncateForEcho(text, max = 200)` helper added to `src/engine/ast-helpers.ts` (dda1414); single U+2026 ellipsis. Threaded through 13 rule files (227725b) — the three backlog-named offenders plus 10 additional sites discovered in the audit grep: `media/alt-text-missing` (4 sites), `semantics/button-name`, `document/iframe-title`, `tooltip/dismissable` (replaced bespoke truncator), `media/video-captions-missing`, `layout/reflow-hardcoded-width`, `parsing/duplicate-id`, `parsing/html-has-lang`, `document/lang-on-parts`, `document/meta-refresh`, `motion/pause-stop-hide`, `navigation/skip-link`. Read-only / controlled-vocabulary hits explicitly skipped (ARIA enums, table cell ≤40 cap, accesskey single chars). Helper unit tests + invariant assertions on label-in-name. `pointer/target-size` retains its own 40-char cap (intentional, tighter than default).
- [x] **V1-SIZE-RESPONSE-BUDGET** ADR 0021 (`docs/adr/0021-scan-response-size-budget.md`) accepted option (a): lower `DEFAULT_PAGE_LIMIT` 200 → 25 in `src/mcp/tool-scan-project.ts` (c1945fa). Calibration from a 16-fixture measurement corpus (real-world + synthetic): bytes-per-file is stable ~2.9–6.5 KB across profiles; 25 keeps typical responses under 100 KB and caps worst-case label-heavy at ~163 KB with honest `truncated` + `nextOffset` signals. 200-file responses were 1.24 MB — factor-of-8 cut, not marginal tuning. New unit test asserts default-cap triggers truncation at 25 on a 30-file fixture. Token-aware budget (option b) rejected to preserve existing `limit`/`offset` contract semantics consumers already understand.
- [x] **V1-SHAPE-FIXCLASS-HEADLINE** `src/mcp/scan-assembly.ts` plan-summary builder — returned `plan.summary: "129 violations (129 guidance fixes). 2 notes… 4 actionable manual review items + 17 untargeted criteria"`. The parenthetical sums four categorically different `fixClass` sub-buckets (`mechanical` 31, `guidance` 30, `runtime-only` 24, `verify-in-source` 46) under one "guidance fixes" label. Directly violates `@docs/kb/architecture/ai-first-consumer.md` §"Composite headline counts are dishonest" — agents budget against the summary string and get four distinct work kinds conflated. Fix: either stop summing in the prose ("129 violations (31 mechanical, 30 guidance, 24 runtime-only, 46 verify-in-source)") or drop the parenthetical and rely on the per-finding `fixClass` discriminator (already shipped in Q2-FIXKIND). Effort: small. Landed `33a1978` (Option A — per-lane breakdown, zero-count lanes omitted). Follow-ups: (1) rename top-level `mechanicalEditsAvailable`/`guidanceFixesAvailable` fields to avoid the fixClass enum-name collision; (2) `src/output/agent-response/build-plan.ts` CLI `--format agent` summary uses the same composite "mechanical edits / guidance fixes" pattern and warrants the same split.
- [x] **V1-SHAPE-RULECOV-COUNT** `findingsEmitted: number` added to `PerRuleCoverage` (`src/types/violation.ts:261-268`) and stamped on every entry in `buildPerRuleCoverage` via a new `countFindingsByRule` linear pass over `allViolations` (f035908). Always present, zero-is-meaningful per the AI-first consumer doctrine. Scanner now passes `allViolations` (pre-severity-filter) to keep telemetry honest about what the engine observed. Unit + integration tests assert `findingsEmitted === violation-stream-count` invariant; existing test fixtures updated to populate the new required field. Counter was NOT pre-computed (backlog hint inaccurate); the linear pass adds negligible cost.

### v1.0.0 — external-codebase scan findings (continued)

- [x] **V1-SHAPE-CLI-AGENT-HEADLINE** (331a805 — option (c): `buildFixClassBreakdown` extracted to neutral `src/output/agent-response/fix-class-breakdown.ts`; `src/mcp/plan-summary.ts` re-imports from the neutral location (MCP→output was already legitimate direction, inverse would have violated layering). `build-plan.ts:buildSummary` now tallies violations by `v.fixClass` and routes the parenthetical through the shared helper. `AgentPlan` field names (`mechanicalEditsAvailable`/`guidanceFixesAvailable`) unchanged — parent item's follow-up (1) stays separate. Three new snapshot tests: mixed-lane breakdown, zero-count lane omission, all-four-lanes fixture. CHANGELOG entry under `## [Unreleased]`.)

- [x] **V1-SIZE-RESPONSE-BUDGET-DENSITY** Option (b) landed (402e546 + 2a26c25 — `src/mcp/reference-guide.ts`: `hashFixDescription` / `hoistFixDescriptions` / `applyFixDescriptionHoist` / `hoistAndBuildReferenceGuide` extend the Q2-PROSE hoist; duplicates keyed by `(ruleId, 12-hex SHA-256 hash)` so two distinct descriptions under one rule both survive (verified on `semantics/label-in-name` which emits a 466-char and 632-char verdict independently). Threshold: hoist when `(ruleId, hash)` repeats ≥2 in the response; singletons stay inline. Wired through scan / scan_file / scan_project / scan_diff (matches Q2-PROSE coverage; suggest_fix / apply_fix / baseline skipped per small response bodies). `AgentFix.description` → optional + `AgentFinding.fixDescriptionRef: { hash }` replaces it on hoisted findings — never both. 11 unit tests in `tests/unit/mcp/reference-guide-hoist.test.ts` + 1 integration round-trip. Breaking CHANGELOG entry under `[Unreleased]`. Measurement on a design-system MCP response: 124,624 → 107,036 bytes (**14.1% / ≈ 17.6 KB saved**). Partial win flagged honestly: **100 KB MCP-ceiling target not met** — post-hoist response ≈ 107 KB (~26.7k tokens vs 25k ceiling). Remaining per-finding bloat: `message` ≈ 22 KB (per-finding context, not dedupable), `fix` ≈ 13 KB, `suppressWith` ≈ 7.6 KB, `criteriaTitles` ≈ 3.8 KB. Options (a) token-aware cap and (c) shift full prose to `suggest_fix` are now promoted to the follow-up item below.

- [x] **V1-SIZE-RESPONSE-BUDGET-DENSITY-2** Option (a) landed (66a9147 + 9c73266 + 538e392 — ADR 0021 amended with the token-density secondary budget; `src/mcp/token-budget.ts` exposes `applyTokenBudget` + `CHARS_PER_TOKEN_PROXY = 4` + `DEFAULT_TOKEN_BUDGET_CHARS = 88000` (~22000 tokens, ~3k headroom under the 25k ceiling); scan-tool wiring lives in `scan-project-budget.ts` + `scan-diff-budget.ts` to keep the scan-tool files under the 500-effective-line budget; wired through `scan` / `scan_project` / `scan_diff` — `scan_file` is a single-file shape with no trailing-file axis to trim, so it's not wired through, matching the V1-SIZE-RESPONSE-BUDGET-DENSITY (b) hoist's own skip); new `response_token_budget_truncated` warning code disambiguates density truncation from the existing file-count cap; `truncated: true` composes cleanly between the two caps, `totalFilesWithFindings` persists, `nextOffset` advances on `scan_project`, omitted on `scan`/`scan_diff` (no `limit`/`offset` contract there); progress guarantee keeps ≥1 file entry even when a single file exceeds the budget. Tests: `tests/unit/mcp/token-budget.test.ts` (helper invariants) + `tests/unit/mcp/token-budget-scan-project.test.ts` (50-file label-dense integration fixture). measurement (post-merge, in-process handler): body 107,036 B / ~26.7k tokens → 78,167 B / ~19.5k tokens; envelope 86,098 B / ~21.5k tokens; `truncated: true`, `nextOffset: 8`, `response_token_budget_truncated` fires; 25k MCP ceiling held with ~3.5k-token envelope headroom. ADR 0021 amendment updated with the post-merge measurement table. Option (c) remains on the table only if a future measurement shows the density cap can't hold typical codebases under 25k tokens — density cap holds typical scans under budget today.

### v1.0.0 — external-codebase scan findings (cont.)

- [x] **V1-FP-LABEL-IN-NAME-SELECT** `src/rules/semantics/label-in-name.ts:34-41` — `<select>` is in `HTML_INTERACTIVE_TAGS`, so the rule harvests its descendant text as "visible label." Evidence: `site/src/assets/examples/floating-labels/floating-label.html:77`, `<select class="form-select" id="floatingSelect" aria-label="Floating label select example">` with three `<option>` children fired 18× (13.7% of the whole scan): "visible text 'Open this select menu One Two Three' … not contained in aria-label". `<select>`'s accessible-name computation does **not** include `<option>` descendant text — only the currently-selected option's text is the widget's value, and its accessible name comes from `aria-label` / `<label for>` / none. Fix: exclude `<option>` text when computing the visible-label substring for a `<select>`; or more broadly, use only direct text nodes (not descendant `<option>`s) for `<select>`. Curate `tests/fixtures/real-world/floating-label-select/` with the three-option pattern asserting zero findings.
- [x] **V1-FP-TOOLTIP-DATA-BS-TOGGLE** `src/rules/tooltip/dismissable.ts` — rule has no awareness of JS-enhanced tooltip toggles. Evidence: `js/tests/integration/index.html:18`, `<button … data-bs-toggle="tooltip" title="Tooltip on top">` fired 18× across the scan. The tooltip JS **replaces** the native `title` with an ARIA-aware tooltip widget at runtime (`aria-describedby` + role=tooltip + keyboard dismiss); the `title` attribute is a source string, not a rendered native tooltip. Surface-don't-suppress per §1: keep the finding but enrich `reason` when sibling attrs `data-bs-toggle="tooltip"|"popover"` or `data-toggle="tooltip"|"popover"` (BS4 legacy) or `data-tippy-content` are present — "note: a JS tooltip library appears to enhance this element; the native `title` may be an input to a runtime widget. Verify the runtime behavior at the call site." Curate `tests/fixtures/real-world/data-bs-toggle-tooltip/`.
- [x] **V1-FP-HIDDEN-FOCUS-MODAL** `src/rules/aria/hidden-focus.ts` — rule cannot observe runtime `aria-hidden` toggles. Evidence: `js/tests/integration/index.html:41`, `<div class="modal fade" … aria-hidden="true">` containing focusable `<button class="btn-close">` fired 3× on this scan and is the canonical FP of the design-system/BEM modal family. The `limitations` block already names this class of gap, but the finding still lands at `severity: error` with no framing. Surface-don't-suppress: keep severity, but when `aria-hidden="true"` sits on an element with class containing `modal`, `dialog`, `drawer`, `offcanvas`, `popover`, `toast`, `overlay`, `backdrop`, enrich `reason` with "note: class name suggests a runtime-toggled overlay (`aria-hidden` likely flips when the widget opens); static analysis cannot observe the toggle — verify the open-state runtime behavior." Curate `tests/fixtures/real-world/modal-aria-hidden/`.
- [x] **V1-NOISE-RULE-PER-FILE-ROLLUP** `src/output/agent-response/build-plan.ts` + `src/mcp/scan-assembly.ts` — on one design-system scan, `forms/autocomplete-missing` fired 31× with 28 on `site/src/assets/examples/floating-labels/floating-label.html` alone; `semantics/label-in-name` fired 18× all on the same file (entangled with V1-FP-LABEL-IN-NAME-SELECT but the pattern generalizes). When one rule fires >N× on one file (threshold proposal: 10), keep every finding in `files[].findings` (no grouping, no dedup — surface-don't-suppress) but add an honest hint: e.g. `perRuleCoverage[ruleId].concentration?: { file, count }` pointing at the densest file when its share exceeds 50% of that rule's total. Signal for the agent that one file is likely where the idiom lives; zero information loss.

### v1.0.0 — external-codebase scan findings (cont.)

- [x] **V1-PARSER-SCSS** Landed (7c9987d parser + 96620ec wiring). `src/input/parsers/scss.ts` + `src/input/parsers/scss-internals.ts` implement a phased preprocess/flatten/substitute pipeline into the existing CSS AST — `$var: <literal>;` inlines to subsequent usages, non-literal RHS preserved un-substituted (no synthesized colors), nested selectors flatten one level, `@mixin`/`@include`/`@import`/`@use`/`@forward` stripped, `//` line comments normalized. `.scss` added to `PARSEABLE_EXTENSIONS` + `extensionMatches` alias to `.css`; routed through cli scan/conformance, mcp session + apply-fix + build-artifacts + agent-response + per-rule-coverage. 26 unit tests; recoverable parse errors emit ParseError rather than silent drop. Downstream contrast rules see SCSS files transparently — no rule code changes required.
- [x] **V1-PARSER-MDX** Landed (6a8977a parser + e6cc850 tests + e88956b wiring + 056d52f tsx-generic-classifier unblock). `src/input/parsers/mdx.ts` strips frontmatter, import/export statements, and code fences, extracts JSX + raw HTML blocks at line-start boundaries, delegates fragments to the existing TSX/HTML parsers. `.mdx` added to `PARSEABLE_EXTENSIONS`; routed through cli scan/conformance, mcp session + apply-fix, agent-response suppression pragmas, per-rule-coverage label. Non-literal expression blocks preserved un-resolved; unparseable fragments emit recoverable parseError. TSX generic-call classifier refuses to cross blank lines (previously swallowed JSX blocks whose preceding line ended in an identifier).
- [x] **V1-PARSER-ASTRO** Landed (39182db parser + a5c37a2 wiring). `src/input/parsers/astro.ts` strips the `---` frontmatter fence (line-preserving whitespace replacement), delegates the remaining template to the existing HTML parser. Opaque component tags (`<Icon>`, `<Header>`) flow through as HTML elements; `{expr}` literal text is honest (unresolved). `.astro` added to `PARSEABLE_EXTENSIONS` with `.html` extensionMatches alias; routed through cli scan/conformance, mcp session + apply-fix, agent-response suppression (HTML-comment form), per-rule-coverage "Astro" label. 21 unit tests cover line-preservation, unterminated fence, CRLF, garbage input, opaque-tag templates.
- [x] **V1-WARN-PARSE-ERRORS** (b6167be) `parse_errors_present` added to the `ScanWarningCode` union + emitted via `hasParseErrors(analysisCoverage)` inside the shared `computeScanWarnings` helper. Conditional-spread (never `[]`) — propagates through every consumer of `warningsField` / `warningsFromScanMeta` (scan_project, coverage, checklist, review_candidates, suggest_fix). Three new unit tests in `tests/unit/mcp/warnings.test.ts`; `docs/errors.md` unchanged (that file indexes structured-error codes + CLI exit codes, not MCP scan-warning codes).
- [x] **V1-WARN-EXT-ENUMERATE** Landed (430ee69 ADR 0023 + a549d5c impl). Option 2 chosen — bare `warnings: string[]` stays the stable triage surface; parallel `warningsDetails` sibling carries structured payloads keyed by code, conditional-spread so presence-only warnings add no noise. New `ScanWarningDetails` type + `computeScanWarningDetails` + `summarizeSkippedExtensions` helpers in `src/mcp/warnings.ts`; `warningsField` / `warningsFieldFromScanMeta` co-emit the sibling so every consumer (scan, scan_project, coverage, checklist, review_candidates, suggest_fix) picks it up. 13 new tests (6 payload builder + 5 composite shape + 2 integration on `tool-coverage`). CHANGELOG entry under `### Added` (additive, non-breaking).
- [x] **V1-RULE-ARIA-EXPANDED-DISCLOSURE** Landed (2d24801 rule + 9ca3f0d tests + b82dfd0 kb regen). `src/rules/aria/expanded-on-disclosure.ts` fires on `<button>` / `<a>` / `[role="button"]` matching any of three predicates (aria-controls target, data-toggle value in disclosure set, onclick classList.toggle/add/remove with visibility tokens) that lack `aria-expanded`. Native `<details>/<summary>` exempt. `data-bs-*` / `data-toggle` are additive evidence hints only; no framework branches. 21 unit tests, 6 fixtures, satisfies wcag22:4.1.2 + wcag21:4.1.2.
- [x] **V1-FINDER-MODAL-FOCUS-MGMT** (c65db1d + f6dbbfd) New `src/review/finders/modal-focus-management.ts` flags modal-shaped containers (`role="dialog"`/`role="alertdialog"` or word-boundary class literal containing `modal`/`drawer`/`offcanvas`) that show zero static focus-management hints in the same file. Hint signals (any one silences all modals in the file): container `tabindex="-1"`, descendant `autofocus` / `data-*autofocus*`, or source-text hit on `.focus(`, `trapFocus`, `focusTrap`, `focus-trap`, `FocusTrap`, `useFocusTrap`, `createFocusTrap`. Confidence `medium`; reason text makes the single-file-check framing explicit. Four criteria emitted per candidate: wcag22:2.4.3, wcag21:2.4.3, wcag22:2.4.11, wcag22:2.4.12. 6 fixtures (3 bad, 3 good), 14 unit tests. Coverage matrix regenerated — WCAG 2.2 2.4.12 promoted from attestation-only → finder.
- [x] **V1-FP-NESTED-INTERACTIVE-NONFOCUSABLE-OUTER** (c3227a4) When the outer element's explicit role is in the ARIA 1.2 composite-widget-delegating set (`tab`, `menuitem`, `option`, `treeitem`, `gridcell`, `row`), the reason text is enriched with a composite-widget-delegates-focus note citing the tablist-roving-tabindex expectation. Severity and finding set unchanged; candidate stays visible per surface-don't-suppress. 7 new unit tests cover the three delegating-role cases plus the non-delegating controls (tablist container, native-outer, `<a href>` inside `<button>`).
- [x] **V1-FP-HEADING-HIERARCHY-CITE-PREV** (21742b4) `reportSkippedLevels` now tracks the full previous `HeadingEntry`; message reads "previous was `<h1>` at line 14, this is `<h5>`. Skipped 3 level(s)." and the suggestion echoes the same line. Scope-note: `Violation` has no `detail` field and `src/types/` was out of scope, so the line is inlined in `message`/`suggestion` — strictly additive, no schema change. Zero detection change (same condition, same findings); three new test assertions cover line-citation, suggestion echo, and most-recent-prior-heading selection.
- [x] **V1-FP-AUTOCOMPLETE-CITE-TRIGGER** (4ae6c7a) `expectedToken` refactored to `matchPurpose` returning `{ expected, trigger }` tagged union (`{ kind: "type" | "name" | "id", ... }`); `describeTrigger()` renders the evidence into the message (`type="email" triggers this rule` / `name attribute contains "email"` / `id attribute contains "email"`). Type beats name/id when both match (most concrete signal); `name` vs `id` distinguished in citation. Fixture-dismissal scenario from backlog covered: `name="floatingInput"` stays silent; `name="emailInput"` fires with name-path citation (no false `type="email"`). Zero detection change.

### v1.0.0 — external-codebase scan findings (cont.)

Composite-headline counts (doctrine: "composite headline counts are dishonest — must count one kind of thing"):

- [x] **V1-COMP-PLAN-GUIDANCE-FIXES** (82f41e5) `plan.guidanceFixesAvailable` replaced with structured `plan.fixesByClass: { mechanical, guidance, runtimeOnly, verifyInSource }` — each key counts one kind of thing per the doctrine; `mechanicalEditsAvailable` retained as an orthogonal "actionable inline edit" counter. `next-step.ts` `fixable` now reads from `fixesByClass.mechanical + .guidance` to preserve prose-only-suggestion coverage that the old composite incidentally provided. New `tests/unit/output/agent-response/build-plan.test.ts` covers per-lane emission; `fixesByClass` conditional-spread when violations > 0.
- [x] **V1-COMP-CHECKLIST-MANUAL-REVIEW** (b60abc5) `checklist.summary.manualReviewRequired` dropped — the canonical dishonest-composite in `docs/kb/architecture/ai-first-consumer.md`. Consumers should read the existing `summary.actionable` + `summary.untargetedCriteria` split (also in `summary.headline`). No `ChecklistSummary`/`ChecklistResponse` type existed (ad-hoc shape at assembly site); integration tests adjusted to derive the total from the split when needed; no non-test consumer read the field. CHANGELOG Breaking entry added under `[Unreleased]`.
- [x] **V1-COMP-BOOTSTRAP-SCAN-TOTAL** Landed (46fd94b). `ScanSubset` now carries `violationsCount` + `notesCount` (both required) and present-when-meaningful `fixesByClass: { mechanical, guidance, runtimeOnly, verifyInSource }` + `mechanicalEditsAvailable` — all forwarded verbatim from upstream `scan_project` plan. `totalFindings` removed (bootstrap was the sole caller; split is strictly more information — CHANGELOG `### Breaking Changes` under `[Unreleased]`). `buildNextStep` / `buildNextStepStructured` read `violationsCount`; headline prose reads "N violation(s) + M note(s)". New focused test asserts per-lane numeric + lanes sum to `violationsCount` invariant.

Envelope honesty (doctrine: "verbose meta is scan-confidence signal; warnings are the silent-failure channel"):

- [x] **V1-ENV-LIST-RULES-BARE** (a83fe44) `list_rules` response now carries `meta: { rulesTotal, rulesMatched, standardsLoaded, standards: string[] }` + `nextStep` prose + `nextStepStructured: { tool: "scan_project", args }` (filter pre-fills `standard`). No metaMode — list_rules is pure registry enumeration, no scan-confidence delta across calls. Next-step builder extracted into `src/mcp/list-rules-next-step.ts` to keep `tools.ts` under line budget. Four new tests cover meta + nextStep branches (filter vs no-filter). Filter echo + matchedOf already landed in c868a39.
- [x] **V1-ENV-COVERAGE-NO-ANALYSIS-COVERAGE** (128c247) `coverage` tool response now carries `analysisCoverage` + `warnings` at the top level, unconditional (not gated behind metaMode). `parseFilesWithDiagnostics` swapped in so discovery diagnostics flow; `buildAnalysisCoverage` reused from scan-assembly; local `countFilesByExtension` helper mirrors the private one so the same `extensions_skipped_no_parser` + Tailwind-undercount codes fire on parity with `scan_project`. `src/reports/coverage.ts` untouched (kept data-pure). 4 new unit tests cover warning-fires / warning-absent / scanned-zero-files / cross-tool parity.
- [x] **V1-ENV-BOOTSTRAP-STRIPS-LIMITATIONS** (fa5220c) Optional `limitations?: readonly string[]` added to `ScanSubset`; `extractScanSubset` copies it verbatim from upstream `scan_project`'s `plan.limitations` (conditional-spread — omitted when upstream emits no prose). The bootstrap composer's response now surfaces the static-analysis caveat to CI readers pasting the `ciSnippet`. `buildCiSnippet()` deliberately unmodified — the optional CI-caveat expansion overlaps V1-ENV-CISNIPPET-DRY-RUN-DISHONEST which is scoped separately.
- [x] **V1-ENV-CISNIPPET-DRY-RUN-DISHONEST** Landed (65a9adf). `buildCiSnippet` takes `{ baselineExists, writeBaseline, baselineWrittenByThisCall }`; handler probes `${root}/${BASELINE_FILENAME}` via `existsSync`. Three content branches: (1) baseline on disk → plain `--baseline check` (steady state); (2) baseline absent + this call wrote it → `--baseline check` with a `# commit .ra11y-baseline.json before pushing` comment; (3) pure dry-run (no baseline, writeBaseline:false, or writeBaseline failure) → prepend `# create ... first:` + a `- run: npx @ra11y/core --baseline create` step before `--baseline check`. Top-level response shape unchanged (`ciSnippet: string`). Three new tests in `tool-bootstrap.test.ts`.
- [x] **V1-WARN-TOKEN-TRUNCATION-NO-LIMIT-ECHO** Landed (992cab2). `ScanWarningDetails` union extended with `response_token_budget_truncated?: { requestedLimit, effectiveLimit }`; new `tokenBudgetTruncatedDetailsField()` helper returns a spreadable fragment. `scan-project-budget.ts` + `scan-diff-budget.ts` thread pre-density page size (`requestedLimit`) and post-density page size (`effectiveLimit`) into `mergeBudgetedFields`. New `scan-budget.ts` module extracted the `scan` tool's merge to stay under line budget; `tools.ts` delegates to `mergeScanTokenBudget`. Tests cover truncation-fires + omission on default-budget passthrough. Shape follows ADR 0023 (option 2 — bare `warnings[]` stable, structured payload under `warningsDetails`).
- [x] **V1-META-RULES-EVALUATED-COVERAGE-DRIFT** (92ba8cc) `buildPerRuleCoverage` now iterates every active+filter-passing rule (not just tracker entries) and synthesizes a project-scoped branch for rules whose only lifecycle is `afterProject` (`focus/outline-visible`, `wrapper/drift`). `rulesEvaluated` derives from `perRuleCoverage.length` so the two cannot drift again. Zero-file scans honestly surface "no files were scanned; project-scoped rule had nothing to evaluate" instead of silent absence. Reused existing `"low"` confidence + structured `reason` (no enum widening needed). Four new invariant tests + one existing test updated. CHANGELOG `### Changed` entry added.

Filter/correctness bugs:

- [x] **V1-LIST-RULES-MATCHED-COUNT** (c868a39) `list_rules` handler now emits `matchedOf: { total, matched }` always and `filter: { standard }` via conditional-spread — agents can distinguish no-filter / no-op filter / narrowing filter from the response alone. Four new unit tests cover all three states plus the shape contract (`"filter" in data` absent when unset, `matched === total` on wcag22, `matched < total` on wcag21). Filter semantics under the current 53 rule set: `wcag22:` → 53 (universal), `wcag21:` → 50 (genuine subset), `section508`/`en301549` → 0 via direct-prefix match (their coverage flows through `equivalentTo`, not this handler's `startsWith` filter — the `matched: 0` is now a structured signal rather than an empty list; filter logic itself unchanged). Envelope additions (`meta`, `nextStep`) left for V1-ENV-LIST-RULES-BARE.
- [x] **V1-PROPOSE-BASELINE-DUP-FINDINGID** (acd6e61) `proposed[]` deduped by `findingId` — first-seen wins, order preserved. `tallyReasons` now runs over the deduped list so `counts.unclassified` and the `proposed.length == sum(counts)` invariant stay honest. Two new unit tests: three `<button title="…">` on one line collapse to one entry (shared findingId via identical line-context hash); invariant assertion that no two `proposed[]` entries share a `findingId` under any scan shape. No engine or findingId-generation changes.
- [x] **V1-SESSION-WRAPPERS-CWD-BLEED** (3b89f8c) Honesty patch (option b) shipped: `McpSession.configure()` records the anchor cwd (explicit `cwd` param preferred, falls back to `process.cwd()`) on first wrapper-landing call — first-write-wins. `session.sessionWrappersMismatchCwd(root)` + new `session_wrappers_configured_for_different_cwd` warning code wired into `scan_project` and `list_suppressions`. `sessionConfigure` schema gains optional `cwd` param; `docs/mcp/tool-reference.md` updated. Full correctness filter (option a — drop wrappers when roots mismatch) deferred as follow-up; 13 test-file cascade was past the turn budget.

Rule-metadata drift against spec (CLAUDE.md §3 invariant 4 — every rule cites every criterion it checks):

- [x] **V1-RULE-HIDDEN-FOCUS-ADD-2.1.1** (84a57b2 + 9f52268) `satisfies` widened to `[wcag22:4.1.2, wcag21:4.1.2, wcag22:2.1.1, wcag21:2.1.1]`; equivalence rollup now credits the rule under section508:2.1.1 + en301549:9.2.1.1. Header cites both SCs (Name-Role-Value + Keyboard); rationale paragraph added for the keyboard-silence failure mode. No behavior change — pure declarative coverage widening.
- [x] **V1-SC-2.4.11-AUTOMATABLE-MISMATCH** (3697e35 + 2079bb4) SC 2.4.11 retagged `automatable: "manual"` → `"partial"`; rationale comment cites `focus/not-obscured` as the auto-checking rule. No rule/satisfies changes (rule was already correct). wcag21/Section 508/EN 301 549 unchanged — 2.4.11 is wcag22-only. KB `docs/kb/wcag/2-4-11.md` + coverage matrix regenerated.
- [x] **V1-RULE-HEADING-H1-OVERCLAIM** (e2aad8f + 60efbba) Softened `docs.description`, `docs.rationale`, missing-h1 `suggestion`, and the file-header block comment: dropped "must have an `<h1>`" overclaim; new wording frames SC 1.3.1 as non-mandating but surfaces the missing-top-of-document-landmark concern and points at the disable pragma for fragment/layout cases. Detection logic / satisfies / severity / message strings unchanged.
- [x] **V1-RULE-AUTOCOMPLETE-MODAL** (575862b + 5d27ca8) `docs.description` and `docs.rationale` rewritten to cite SC 1.3.5 AA directly and reference the 53 input-purpose tokens; per-finding `message` + `suggestion` strings also updated to must-modality. Trigger-citation from the earlier 4ae6c7a commit preserved via merge-resolution ("appears to collect information about the user but has no autocomplete attribute — {describeTrigger}. WCAG 2.2 SC 1.3.5 (AA) requires …"). Detection unchanged; `satisfies` unchanged.

Review-finder reason-text enrichment:

- [x] **V1-FP-TIMING-CITE-DURATION** (d398092) `reason` now carries the call's duration argument (`this._config.interval`, `emulatedDuration`, etc.) and enclosing function/method name via text-level lookback. Honest omission when unknown — no guessed names. 11 new tests, zero detection change. Commit oversize driven by biome cognitive-complexity decomposition helpers.
- [x] **V1-FP-IMAGES-OF-TEXT-SVG-DATA-URI** (a2b8ee0) Additive note when `<img src>` is `data:image/svg+xml,...` and decoded payload contains neither `<text` nor `<tspan` — "text-baked-in concern is provably lower, but verify the SVG isn't rendering text glyphs directly in path data." Base64 SVGs skipped (not inspected). 6 new tests covering svg-with-text/svg-with-tspan/non-svg/base64/JSX parity.
- [x] **V1-FP-MULTIPLE-WAYS-CITE-COUNTS** (5afba2d) `reason` appends nav-signal counts (`<nav>`, `<a>`, `<input type="search">`, breadcrumb, sitemap link) derived by `summarizeHtmlSignals` / `summarizeJsxSignals`. Preserves existing SPA-shell annotation after em-dash. 4 new tests; zero detection change.

New review finders (detection gaps — zero candidates on strong static evidence):

- [x] **V1-FINDER-POINTER-GESTURES** (349e489 + b57c2f8) Two new detection paths on `src/review/finders/pointer-input.ts`: (a) co-occurrence — when `touchstart`+`touchmove` OR `pointerdown`+`pointermove` both appear as `addEventListener` in one file, emits one path-based candidate anchored at the first `touchmove`/`pointermove` offset and dedupes against the standalone candidate at the same location; (b) name-pattern — left-word-boundary regex `/\b(swipe|pan|pinch|rotate)/i` matched against file basename + class/function/const/let/var identifier declarations, skipping comment/JSDoc lines. `swipe.js`, `class PinchZoom`, `function rotate(`, `const panHandler =`, `panelSlide` fire; `span`, `planet`, `expanded`, `plan` stay silent. Confidence: `"high"` on the deterministic pair pattern, `"medium"` on name-pattern hits. 29 new tests + 6 fixtures (3 bad, 3 good). Surface-don't-suppress — no thresholds, no heuristic suppression.
- [x] **V1-FINDER-FOCUS-ORDER-NEGATIVE-TABINDEX** (802013d + ed2eca8 + 8197d84) New `src/review/finders/focus-order.ts` fires on `tabindex="-1"` (string or numeric expression) applied to natively-focusable controls (`<input>`, `<textarea>`, `<select>`, `<button>`, `<a href>`) OR elements with widget roles (`button`, `link`, `textbox`, `checkbox`, `radio`, `combobox`, `slider`, `spinbutton`, `switch`). Walks both HTML and TSX/JSX ASTs. `<a>` without `href` correctly skipped (not natively focusable); `<div>` without widget role skipped. Confidence `"high"` — deterministic predicate. Reason text frames the a11y-dialog-pattern vs. accidental-suppression question. Six fixtures (3 bad, 3 good across HTML + TSX), 17 unit tests, KB coverage matrix regenerated.

Conformance-claim honesty (the surfaces agents use to decide "should I claim this?"):

- [x] **V1-CONFORMANCE-LIMITATIONS-FIELD-MISSING** (d53eab0 + 72fd5e2 + 718ef76) `ConformanceBlocker.status` widens with `"undetermined"`; `ConformanceBlockerReason` gains `"runtime-evidence-required"`; top-level `limitations: string[]` emitted on the conformance statement (present-when-non-empty, omitted when zero undetermined criteria). New `src/reports/runtime-evidence-criteria.ts` — spec-derived enumeration of 22 WCAG SCs (1.4.3, 1.4.10, 1.4.11, 1.4.12, 1.4.13, 2.1.1, 2.4.3, 2.4.6, 2.4.7, 2.5.7, 2.5.8, 3.3.7, 3.3.8, 3.3.9 + WCAG 2.1 aliases) with inline comments citing the normative basis for each. Runtime-dependent SCs that reach the builder with zero fail-evidence + no attested/sampled source now emit `status: "undetermined"` / `reason: "runtime-evidence-required"` instead of the silent-miss `status: "pass", reason: "no-evidence"`. Attestations still clear the blocker; static failures still route to `"failing"`. Markdown renderer emits a `## Limitations` section when present, omits when absent. Ledger `EvidenceStatus` unchanged — undetermined lives only at claim level. CHANGELOG Breaking entry added.
- [x] **V1-VPAT-RUNTIME-ONLY-PARTIALLY-SUPPORTS** (e807012) Runtime-evidence-required criteria (22 SCs from the spec-derived `RUNTIME_EVIDENCE_REQUIRED_CRITERIA`) now route to `conformance: "Not Evaluated"` on clean scans with no fresh attestation, instead of collapsing into `"Partially Supports"` via `automatable: "partial"` metadata. Fresh `pass`/`fail`/`n/a` attestations release the criterion to its automatable default; pending attestations ignored; proven static failures still route to `"Does Not Support"` (conservative default — honest negative evidence preserved). Attestations threaded through a new optional `attestations` field on `VpatBuildOptions`; MCP `vpat` tool forwards its already-loaded durable store. **2.5.3 / 2.4.9 left on the static path** — both are `automatable: "partial"` with real rule coverage (`label-in-name`, `link-purpose`) and NOT in `RUNTIME_EVIDENCE_REQUIRED_CRITERIA`; inventing a parallel manual-review-only constant would create two sources of truth for the runtime-vs-manual distinction. 5 new unit tests + 1 MCP integration test; `docs/certification/vpat-mapping.md` updated.
- [x] **V1-ATTEST-EVIDENCE-SOURCE-UNSTRUCTURED** (fb789f0 + 539fbb2) `evidenceSource: "runtime_tool" | "manual_review" | "human_study" | "declaration"` now required on the TS type + on write (`requireValidRecord` rejects missing); absent on read coerces to `"declaration"` for backward compat, unrecognized values skip the record. Optional `toolName` / `runUrl` / `observedAt` (ISO-8601 via new `src/utils/iso-timestamp.ts` zero-dep validator) conditional-spread at every surface. Pragma-derived records stamp `"manual_review"` unconditionally. Attestation summary splits `totalCount` + `bySource` (no composite headline). Downstream: `conformance_statement` markdown + `list_attestations` + VPAT remarks cell all surface evidenceSource + toolName (present-when-meaningful). Two commits; 29 files touched.

Generator-script footgun (tooling, surfaced repeatedly by agents running /fix-drift inside worktrees):

- [x] **V1-TOOL-KB-GEN-PRESERVE-HANDWRITTEN** (b85940b) Option (c1) landed — the unlink decision extracted into a pure `chooseFilesToUnlink` helper in `scripts/generate-rule-kb.ts`. Preserves `index.md` / `README.md`, slugs listed in exported `HAND_AUTHORED_KB_SLUGS` (`fix-suggestion-audit`, `coverage`), and any filename already in the expected-write set. Script body wrapped in `if (import.meta.main)` per repo convention so tests import the helper without triggering regeneration. New `tests/unit/scripts/generate-rule-kb.test.ts` — 9 cases covering index/README preservation, stale-slug removal, overwrite path, both hand-authored filenames from the bug report, allowlist coverage, full-wipe case. Allowlist mirrors `check-kb-drift.ts` `knownRuleExtras` / `knownExtras` sets from the opposite direction; follow-up to hoist the shared constant noted but out of scope.

### v1.0.0 — four-repo field test findings

Parser / language coverage:

- [x] **V1-PARSER-LESS** No `.less` parser. `.less` files skip silently through `bower_components/` and legacy design-system trees — design tokens and theme overrides invisible to every rule. `scan_file` on a `.less` file returns `file-unsupported`. Fix: add a lightweight Less parser that shares the CSS AST (like the SCSS parser — variables inlined when literal, nested selectors flattened one level, `@import`/`@mixin`/`@include` stripped). Add `.less` to `PARSEABLE_EXTENSIONS` with `.css` alias via `extensionMatches`.
- [x] **V1-PARSER-SVG-TITLE** No SVG parser / no SVG-accessibility rules. a template catalog carries 222 `.svg` files; `scan_file` on `fontawesome-webfont.svg` returns `file-unsupported` and the remediation list does not include `.svg`. Inline SVGs inside `<button>` are handled by existing button-name SVG-title traversal (V1-DETECT-BUTTON-NAME-SVG) but standalone SVGs never get `semantics/svg-title-missing` / `aria/svg-role-img-name`. Fix: register `.svg` as a parseable extension, implement a minimal SVG parser (reuses HTML tokenizer, maps `<title>` / `<desc>` / `role="img"` / `aria-label`), and add a rule pair for missing accessible name when SVG is a standalone asset vs. a decorative `<use>` child. Pairs with V1-PARSER-LESS for parser-registry widening. (17fcf6db)
- [x] **V1-HTML-PARSER-MARKDOWN-URL-AUTOLINK** HTML parser treats bare Markdown URL `<https://…>` autolinks as an element open tag and emits `partialParseFiles[].reason: "Unclosed <https:> element"` on README files parsed as HTML residue (`README.md`). Tightens `Q-SHARED-MD-DISPATCH-UNIFY` — the routing is correct, the tokenizer's recovery is wrong. Fix: tokenizer must reject impossible element names (`https:`, `http:`, any scheme-shaped token with a `:` in a non-attribute-value position) as element openers and emit them as text. Add the pair: drop the false-positive `semantics/heading-hierarchy` "Document has no <h1>" that cascades from the truncated parse when the README's top-level `# <Project>` ATX heading is Markdown syntax that never reached the HTML residue pass.
- [x] **V1-SCSS-CONTRAST-VARIABLES-ZERO-OUTPUT** `.scss` files containing `$variable:` declarations with no same-rule color-pairs produce `findings: []` with `perRuleCoverage.contrast/minimum.coverageConfidence: "high"` — telemetry reads "clean on this file" when the reality is "no color pairs resolvable to literals." Reproduces on every Font Awesome SCSS file, every `_variables.scss`, every theme-token partial. Doctrine: zero-output success is ambiguous failure. Fix: when a `.scss` file contains `$variable:` declarations AND the SCSS preprocessor's variable-substitution pass produced zero literal-color usages in downstream selectors, emit `warnings: ["scss_unresolved_variables"]` + downgrade `coverageConfidence` to `"medium"` with `reason: "scss variables unresolved — scan the compiled CSS output for full contrast coverage"`. Pairs with V1-PARSER-SCSS (closed) — the parser works; this is the honest telemetry on the zero-output case.
- [x] **V1-CSS-CONTRAST-VAR-ROOT-RESOLUTION** `contrast/minimum` does not resolve `var(--token)` references when the token is declared at `:root` and consumed in a descendant rule. `:root { --fg:#fff; --bg:#fff } .card { color: var(--fg); background: var(--bg) }` silently passes; `.card { color:#fff; background:#fff }` fires 1.00:1. Every design-system CSS file uses this pattern; silent miss on real low-contrast designs. Fix: CSS parser or contrast rule resolves `var()` via a two-pass walk — collect `:root { --name: <literal> }` in pass 1, substitute at contrast-rule evaluation in pass 2. Out-of-`:root` custom properties stay unresolved (honest: the cascade can override). Doctrine: silent miss on common pattern.
- [x] **V1-CSS-CONTRAST-CASCADE-INHERITED** `contrast/minimum` requires both `color` and `background` on the same rule. Real-world CSS sets `body { color: #fff }` then declares `.btn { background: lightblue }` — the button text is `#fff` on `#ADD8E6` (1.53:1, error) but rule never fires. Fix: rule walks the ancestor-selector chain for a missing `color` (fall back to nearest `html` / `body` / matching-container color), OR adds a review-candidate tier "color not declared on this rule but background is — verify the cascaded foreground contrast." Pairs with V1-CSS-CONTRAST-VAR-ROOT-RESOLUTION — same class of cascade-awareness gap. Doctrine: silent miss on real-world cascade pattern.
- [x] **V1-PARSER-ERB** `.erb` extension returns `file-unsupported` (strict allow-list gate) even though `.html` files mixing ERB + Liquid parse cleanly (identical content, different filename). Evidence: `test/source/_layouts/default.html` mixes `<%= @page.author %>` ERB with Liquid `{{ ... }}` and scans fine; `test/fixtures/*.erb` errors out. Rails views + Middleman templates + SSG `lib/theme_template/*.md.erb` scaffolds invisible to every rule. Fix: add `.erb` to `PARSEABLE_EXTENSIONS`, route through the HTML parser's template-literal pass (ERB `<%= … %>` / `<% … %>` treated as literal text, same as Liquid per `template_files_parsed_as_literal`). The asymmetric allow-list is the bug, not parser capability.
- [x] **V1-TEMPLATE-CLASSIFIER-MARKDOWN-PROSE-FALSE-POSITIVE** `erb-or-ejs` / `jinja-or-liquid` template-directive detection fires on Markdown code-block examples — `docs/_docs/troubleshooting.md:261` (a fenced code block quoting `<%= Time.now %>` in prose) populates the `erb-or-ejs` bucket, yet zero `.erb` files reached the parser (all 14 `.erb` files in `skippedByExtension`). Same shape fires `jinja-or-liquid` on `History.markdown` (release-notes prose). The token scanner treats code-fence text as live directives. Fix: when detecting template-family directives in a `.md`/`.markdown` file, skip content inside triple-backtick fences AND inline-code spans before running the directive matcher. Pairs with Q4-TEMPLATE-DIRECTIVE-CLASSIFIER-DRIFT (closed) — same classifier, different false-evidence source.

Rule correctness / detection:

- [x] **V1-SCAN-PROJECT-LAYOUT-FILES-DROPPED** `scan_project` omits findings for `site/src/layouts/BaseLayout.astro` (4 findings per `scan_file`) and `DocsLayout.astro` (5 findings) across every paged offset on a typical scan. Files are parseable, not in `scannedBuildArtifacts`, not in `parseErrorFiles`, not in `partialParseFiles` — they simply do not appear in the 110-file list despite `filesByExtension` counting 104 `.astro` files scanned. `scan` with explicit `paths` including the same files returns the 4 findings; `scan_file` does too. Silent drop in `scan_project`'s aggregation path; no warning emitted. Doctrine: zero-output success at the per-file level. Fix: instrument `scan_project`'s per-file eligibility walk to identify which files produced findings per `scan_file` / `scan` that did not make it into the aggregated response, and surface the drop via a structured `warnings: ["scan_project_findings_aggregation_drop", {fileCount, sample}]` until root cause lands.
- [x] **V1-MOTION-PAUSE-STOP-FILES-EVALUATED-OFF-BY-ONE** `perRuleCoverage["motion/pause-stop-hide"].filesEvaluated` reports 105 on where every other HTML+JS+MD rule reports 104 (52 html + 51 js + 1 md = 104). One extra evaluation is counted somewhere in the rule's eligibility pass. Low severity (does not affect findings); silent drift in scan-confidence telemetry. Fix: audit the rule's applies/eligibility calculation against the shared file pool; add an invariant test that every rule with the same `appliesTo` set reports the same `filesEvaluated` on a given scan.
- [x] **V1-INFRA-RULE-ID-ALIAS-TABLE** (blocks V1-RULE-NAVIGATION-HREF-VOID-RENAME) Build a small rule-ID alias resolver at `src/engine/rule-aliases.ts` matching the ESLint / Biome / Stylelint pattern: `{from, to, deprecatedSince, removeIn}[]`; the resolver accepts the old ID in suppression pragmas, `ra11y.config.ts`, and CLI `--rules` flags; emits `warnings: ["deprecated_rule_id:<old>:<new>"]` on every resolution so agents can offer to rewrite. `list_rules` exposes deprecated entries with `deprecated: true` + `replacedBy` (ESLint `meta.deprecated.replacedBy` shape). No quiet aliasing — always emit the structured warning. Once this lands, CLAUDE.md §12 amends from "rename is major" to "rename behind the alias is minor" (the conditional clause it already contains). Research (ESLint `no-spaced-func` → `func-call-spacing`; Biome `noConsoleLog` → `noConsole`; Stylelint deprecated 76 stylistic rules in v15) confirms this is ecosystem-standard; no comparable lint tool uses "rename = major." Captured 2026-04-24 /continue post-run research — full rationale + sources in orchestrator's final summary.
- [x] **V1-RULE-NAVIGATION-HREF-VOID-RENAME** (depends on V1-INFRA-RULE-ID-ALIAS-TABLE) `navigation/href-javascript-void` rule name promises detection of `href="javascript:void(0)"` only but also fires on bare `href="#"` (an SSG `navigation.md` scan produces 50 findings, all `href="#"`, zero `javascript:void`). Rename to `navigation/href-placeholder` or `navigation/href-empty-or-void` (spans empty, `#`, `javascript:void(0)`, `javascript:;`). With the alias table in place this ships as MINOR (not major), per ecosystem convention — the alias makes old pragmas keep working + emits a deprecation warning; removal waits one minor release cycle. Pair with CHANGELOG `### Deprecated` entry (not `### Breaking Changes`).
- [x] **V1-HEADING-HIERARCHY-MARKDOWN-SELF-CONTRADICTION** `semantics/heading-hierarchy` fires "Document has no <h1>" on a design-system `README.md` parsed as HTML residue — even though `analysisCoverage.hints` prose says "Markdown files parsed as HTML residue: embedded HTML, image alt-text, and kramdown IAL are checked; link text, heading hierarchy, and prose are not." One of the two is wrong. The README has a Markdown ATX top-level heading the HTML parser never saw. Fix: skip `semantics/heading-hierarchy` (and any document-scope rule — `semantics/landmark-main`, `document/page-titled`) when the file extension is `.md`/`.markdown` AND the scan is using the HTML-residue pass. Update the hint to name exactly which rules are skipped. Pairs with V1-HTML-PARSER-MARKDOWN-URL-AUTOLINK (fixes the upstream parse-error vector); both should land together.
- [x] **V1-LIQUID-TEMPLATE-EXPRESSION-AS-SOLE-CHILD-REASON-ENRICHMENT** `<h1>{{ page.title }}</h1>`, `<title>{{ page.title }}</title>`, `<button>{{ t.submit }}</button>`, `<a aria-label="{{ … }}">` — rules that flag empty/non-descriptive content emit `severity: error` with generic fix text when the only child is a Liquid/Handlebars/ERB template expression. Q4-DOCUMENT-PAGE-TITLED-LIQUID-STRIP (closed) handled `document/page-titled`; the same enrichment is missing on `semantics/empty-heading`, `semantics/button-name`, `forms/non-empty-label`, and several other visible-text rules. Fix: add a shared helper `hasOnlyTemplateExpressionChild(node)` that detects the "sole child is a `{{…}}` or `{%…%}` or `<%…%>` or `<%=…%>` expression" pattern. Every visible-text rule runs it before emitting; when true, reason text appends "note: the only child is a template expression — rendered text determines conformance. Verify at a rendered output or suppress at source with `<!-- ra11y-disable wcag22:<sc> -->` if the interpolation is trusted." Surface-don't-suppress — finding still emits, severity unchanged, reason enriched.
- [x] **V1-FRONTMATTER-AS-TEMPLATE-DIRECTIVE-TRIGGER** `template_files_parsed_as_literal` warning does not fire on files carrying a YAML frontmatter fence (`^---\n…\n---\n`) when no Liquid/Handlebars/ERB tokens are detected. Evidence: `test/source/properties.html` (frontmatter + 1-line body, no Liquid) returns `warnings: ["no_config_found"]` only. An agent told "no template risk" for this file will miss that the `---` fence itself was literal in the parser's eyes and would (in a richer file) corrupt the HTML parse. Fix: frontmatter fence presence is an independent template-substrate signal — emit `template_files_parsed_as_literal` whenever the file starts with a `---\n…\n---\n` block OR a Liquid/Handlebars/ERB directive is detected. Pairs with V1-TEMPLATE-CLASSIFIER-MARKDOWN-PROSE-FALSE-POSITIVE — both tighten what counts as template evidence.
- [x] **V1-LINK-DESCRIPTIVE-TEXT-TITLE-DUPLICATE** `src/rules/navigation/link-descriptive-text.ts::hasAccessibleNameOverrideHtml` treats any non-empty `title=` as an accessible-name override, even when the title value duplicates the visible text. `<a href="#" title="read more">read more</a>` escapes the generic-phrase match because `title` is present — but the title adds nothing the text didn't. Fix: when `title`/`aria-label` equals (case-insensitive, trimmed) the element's visible text, don't treat it as an override; continue to the generic-phrase check. Apply the same fix on the JSX path (`hasAccessibleNameOverrideJsx`). Mechanical scope.
- [x] **V1-PAGINATION-SENSORY-LINE-DRIFT** 1.3.3 sensory-characteristic finder cites `docs/_docs/pagination.md:134` for the phrase "See below" whose actual position is line 136 — the finder points at the containing block-open rather than the text run. Same class of bug as Q4-LIQUID-LINE-DRIFT-SASS-SELECTOR (closed) but on Markdown body-text extraction. Fix: line-mapping must point at the text-node's own line, not the enclosing block. Add fixture `tests/fixtures/real-world/ssg-pagination-sensory-line-drift/` asserting the cited line exactly matches the text.
- [x] **V1-LIVE-REGION-RUNTIME-MUTATION-CANDIDATE** No review candidate fires for `wcag22:4.1.3 Status Messages` when JS mutates visible text via `innerHTML = …` / `.textContent = …` / `.innerText = …` on elements that are not declared as live regions. Canonical cases: `insect-catch-game` score + timer updated via `innerHTML` every second; `dad-jokes` joke swapped in via `innerHTML` after `fetch`; `event-keycodes` rewrites `<div class="key">` at every keydown. Pairs with Q3-RULE-LIVE-REGION-PRE-EXISTENCE (closed — detects declared live regions hidden on initial render); this is the inverse — elements mutated at runtime without a live-region declaration. Fix: new review finder `review/live-region-runtime-mutation-missing` — scan `.js`/`.tsx` for `elem.innerHTML = …`, `elem.textContent = …`, `elem.innerText = …`, `elem.insertAdjacentHTML(…)` where `elem` resolves to an HTML element that lacks `role="status"` / `role="alert"` / `aria-live="…"`. Confidence `medium` (cross-file identifier resolution is lossy; agent reads to verify).

Response shape / honesty:

- [x] **V1-META-COUNTS-BY-SURFACE-REGRESSION** `meta.countsBySurface` is `null` on every 2026-04-22 scan across all four target repos despite commit `8c954816 feat(mcp): surface cross-surface count drift via meta.countsBySurface`. The invariant the commit advertises ("cross-surface count drift is visible") is unverified in practice — consumers either assume "no drift" from `null` or add defensive null-checks. Fix: determine whether the field ships only when drift is detected (in which case: rename to `meta.countsBySurfaceDriftDetected?: {…}` + populate; document the semantic) OR ships always with `{scan, coverage, checklist}` numeric triples. Pair with a contract test that calls `scan_project` + `coverage` + `checklist` on the same cwd and asserts `countsBySurface` reflects the three responses' counters. Doctrine: ambiguous field shapes are dishonest.
- [x] **V1-FIX-SAFETY-CONSTANT-FIELD** Every finding in every scan surfaces `"fix": { "safety": "safe" }` including findings with `fixClass: "runtime-only"` (e.g. `tooltip/dismissable`, `motion/pause-stop-hide`). A field that never varies conveys no signal; worse, "safe" on a runtime-only fix is arguably wrong (static analysis cannot prove safety without runtime context). Fix: either drop `fix.safety` entirely, or make it vary with `fixClass` (`"safe"` for mechanical-with-unique-oldText, `"needs-review"` for verify-in-source, omit for runtime-only/guidance). Pairs with the present-when-meaningful doctrine rule.
- [x] **V1-FIX-DESCRIPTION-PRESENCE-INCONSISTENCY** Same rule, same scan: some findings carry `fix.description` inline while sibling findings of the same ruleId carry `fixDescriptionRef.hash` only. Observed: `motion/pause-stop-hide` on appears twice — once with a full description, once with safety-only. An agent cannot predict whether to join with `referenceGuide.fixDescriptions[ruleId][hash]` or read inline. Fix: make emission deterministic — ALL findings for a rule in a single response use the same shape (either all inline, or all hoisted + ref). The hoist threshold (V1-SIZE-RESPONSE-BUDGET-DENSITY, 402e546) operates per-rule; extend it to always hoist when the rule has ≥1 finding in the response, OR always inline when only one finding exists. Add `nextStep` on findings-with-ref pointing at the referenceGuide path so agents don't need to learn the convention.
- [x] **V1-CHECKLIST-PERCRITERION-CURSOR** `checklist` emits `perCriterionClipped: true` and `totalCandidates: 78` but the response carries 44 candidates with no `truncated`, no `nextOffset`, no per-criterion `clippedAt` cursor. An agent that counts the returned candidates and sees 44 < 78 has no way to fetch the other 34. Q2-CHECKLIST-LIMIT (closed) established pagination for the flat stream; the per-criterion clip is the orthogonal gap. Fix: when `perCriterionClipped: true`, emit either (a) `nextOffset` cursor that advances to the next criterion-batch OR (b) per-criterion `nextOffset` on each clipped criterion's entry OR (c) a single response-level `nextOffset` + `raiseMaxPerCriterionHint: N` so the caller can re-page with a higher `maxCandidatesPerCriterion`. Pick one; document the semantic; ship the cursor. Doctrine: ambiguous field shapes are dishonest.
- [x] **V1-SUPPRESS-DRY-RUN** `suppress` tool has no dry-run mode — the one call writes the pragma comment immediately. `apply_fix` defaults `dryRun: true`; `baseline` distinguishes `mode:"create"` vs `mode:"check"`. `suppress` is the only write tool without a preview path. Fix: add `dryRun?: boolean` input (default `false` to preserve current semantics, or default `true` to match the apply_fix pattern — pick one in the same spec). `dryRun:true` returns `{pragma, insertedLine, commentKind, meta}` without writing. Add `revertHint: "git checkout -- <file>"` (or content-hash pointer) to every write response. Doctrine: safe-by-default on write tools.
- [x] **V1-SCAN-FILE-CWD-CONTAINMENT** `scan_file({cwd: "/x/a", path: "../b/file.html"})` escapes the cwd sandbox and scans the sibling file. `suppress` correctly rejects the same pattern with `{code: "path-escapes-cwd"}`. Asymmetric guard — the escape boundary must be uniform across every tool taking `(cwd, file|path)`. Read-only so low-severity, but breaks the user's mental model. Fix: reuse the `suppress` escape guard in `scan_file`, `suggest_fix`, `apply_fix` (and any other `(cwd, path)` tool). Mechanical.
- [x] **V1-PROPOSE-BASELINE-RATIONALE-DEDUP** `propose_baseline` on a 156-file / 242-finding scan returns 76 KB (all on one line) — every entry inlines the same 128-char `rationale` string. Pattern already exists elsewhere (`review_candidates.prompts[criterionId].text` dedupes prose by criterion; `referenceGuide.fixDescriptions[ruleId][hash]` dedupes fix prose). Fix: hoist `rationale` to a top-level `rationales: { [reasonCode]: string }` map; per-entry carries `reason: "unclassified"` only. Add `limit` + `offset` pagination mirroring `scan_project`. Pairs with V1-PROPOSE-BASELINE-DUP-FINDINGID (closed) — same tool, different bloat vector. Doctrine: verbose meta is signal, but repeated identical prose is bloat.
- [x] **V1-CONFORMANCE-SCOPE-FILES-CAP** `conformance_statement.scope.files[]` inlines every scanned file path with no cap — on a 156-file repo the array alone is ~16 KB before any content. V1-CERT-STATEMENT-SHAPE (closed) added `scope.files` per WCAG §5.3.1; the size is unbounded. Fix: always emit `scope.filesCount: number`; gate the path list behind `verboseScope?: boolean` input (parallel to existing `verboseMeta` gating). Pairs with V1-CERT-STATEMENT-SHAPE.
- [x] **V1-SESSION-CONFIGURE-ALLOWWRITE-SILENT-DROP** `sessionConfigure({allowWrite: true})` with no `cwd` returns `{active: {allowWrite: false}}` — opt-in silently dropped with no warning. Follow-up call with `{allowWrite: true, cwd}` succeeds. The coupling between `cwd` and `allowWrite` is undocumented in `tools/list`. Canonical "zero-output success is ambiguous failure" at the session level. Fix: either accept `allowWrite:true` without `cwd` (session-scoped write gate), OR reject with `{error, code: "cwd-required-for-write-gate", remediation: …}`. Never return `allowWrite:false` as a silent success when the caller asked for `true`. Pairs with Q-SHARED-SCAN-FILE-ERROR-DISCRIMINATION doctrine.
- [x] **V1-FINDING-ID-STABILITY** `findingId` hash varies across `apply_fix` before/after snapshots even when the finding is unchanged (same ruleId, line, column, message). `password-generator` row 11 dry-run shows `373b28c71fbf` before → `c8bad95295ec` after at line 34 with identical evidence. `findingId` is load-bearing for suppress/dedup workflows; instability breaks agent dedup and cross-call correlation. Fix: audit the hash input — exclude ephemeral surrounding state (line-context that shifts when a preceding line changed), include only (ruleId, normalized-line-text, column, reasonKind). Add invariant test: `apply_fix` dry-run on finding A must not change `findingId` of unrelated finding B. Pairs with Q6-FINDINGID-COLLISION-SAMEFILE-SAMELINE (closed) — same primary-key discipline, different failure mode.
- [x] **V1-ZERO-SCAN-PASS-RATE-SENTINEL** `checklist`/`coverage` on a zero-file scan emit `summary.automatedCoverage.automatedCriteriaPassRate: 100` because division `passed/total` on 0 files yields "100%". Response also carries `warnings: ["scanned_zero_files"]` (honest at the warnings layer), but an agent summing pass-rate dashboards counts the call as clean conformance. Fix: when `warnings` contains `scanned_zero_files` OR `filesScanned === 0`, omit `automatedCriteriaPassRate` entirely (present-when-meaningful) OR emit it as `null` with a `reason: "no-files-evaluated"` sibling. Never emit 100 on zero-file scans. Pairs with Q-SHARED-PASS-RATE-COMPOSITE (closed).
- [x] **V1-HINTS-STRUCTURED-CODE** `meta.analysisCoverage.hints` ships as prose-only strings carrying actionable information ("15 PascalCase components are opaque...", "Markdown files parsed as HTML residue: …") but requires string-parsing to act on. Compare with structured `detect_native_wrappers.emptyReason` discriminator — same signal, structured. Fix: each hint becomes `{ code, text, detail? }` so agents branch deterministically. Hint-code taxonomy: `opaque_components_present`, `markdown_html_residue`, `ssg_build_output_hint`, `scss_variables_unresolved`, etc. Additive — keep `text` for backwards compat.
- [x] **V1-NEXTSTEP-DEDUP-META-VS-TOP-LEVEL** `scan_project` responses carry both `meta.nextStep` (nested) and `nextStep` at top level, plus `nextStepStructured` at top level and `meta.nextStepStructured` nested. Identical content in two places violates Q6 de-duplication doctrine. Fix: one pointer, one place — pick top-level for discoverability; drop the `meta` nesting. Minor.
- [x] **V1-BOOTSTRAP-BASELINE-NULL-SENTINEL** `bootstrap` emits `baseline: null` in dry-run mode (when `writeBaseline: false`). `null` here means "dry-run, not created" but is semantically distinguishable from "baseline-creation-failed" only by reading the tool docstring. Fix: conditional-spread — `...(writeBaseline ? { baseline } : {})` — plus a `warnings: ["baseline_dry_run"]` code when `writeBaseline:false`. Doctrine: ambiguous field shapes are dishonest. Pairs with V1-ENV-CISNIPPET-DRY-RUN-DISHONEST (closed).
- [x] **V1-SCAN-DIFF-SHALLOW-CLONE-HINT** `scan_diff` with `HEAD~N` on a shallow clone errors with `{code: "unknown-ref", remediation: "Fetch the remote first if you're comparing against an origin branch."}`. The remediation addresses branch-shaped refs but not shallow-clone depth — PR-review agents using `actions/checkout@v4` default `fetch-depth: 1` hit this every time. Fix: extend remediation with "If this is a shallow clone (`git rev-parse --is-shallow-repository` → true), run `git fetch --unshallow` or re-clone with `fetch-depth: 0`". Pair with a shallow-clone probe at error-construction time so the correct branch of advice is picked.
- [x] **V1-TRUNCATED-FIELD-PRESENCE-CONTRACT** Small scans omit `truncated` / `totalFilesWithFindings` from the response; large scans populate both. An agent reading `truncated` can't tell "not truncated" from "field never emitted on this scan shape." Fix: always emit `truncated: false` + `totalFilesWithFindings: N` on `scan_project` responses regardless of truncation state. Conditional-spread is WRONG here (the emptiness is meaningful — "not truncated" is a load-bearing negative answer). Doctrine: present-when-meaningful requires the value to be meaningful; here, the negative is meaningful.
- [x] **V1-LIST-SUPPRESSIONS-RULES-EVALUATED-DRIFT** `list_suppressions` emits `meta.rulesEvaluated: {loaded: 75}` but the tool is documented as "does NOT run rules; fast enumeration only." The field name is misleading — no rules were evaluated. Fix: rename to `rulesLoaded: {count: 75}` OR drop from this tool's response (rule count is irrelevant to suppression enumeration). Doctrine: cross-tool field semantics must be honest; `rulesEvaluated` on a tool that evaluates no rules is dishonest.
- [x] **V1-DETECT-NATIVE-WRAPPERS-CWD-NOT-FOUND-REGRESSION** Q-SHARED-DETECT-NATIVE-WRAPPERS-CWD-NOT-FOUND closed (`3b89f8c`-era) but both an SSG source tree and a vanilla-JS edge-case pass report the structured-error envelope working correctly. Regression-audit item: verify the guard is still present on `detect_native_wrappers` and add a regression-guard test asserting `{code: "cwd-not-found"}` on a non-existent path. If still holding, mark this item closed on first verify turn.
- [x] **V1-CONFIG-CONTEXT-TRIPLE-READOUT-REGRESSION** Q6-CONFIG-CONTEXT-TRIPLE-READOUT closed but all four 2026-04-22 repos reproduce the same triple-emission (`configSource: null` + `configSearchedFrom: <cwd>` + `configNote: "No ra11y.config found …"` all together, with `configSearchedFrom` echoing `cwd` exactly). Regression-audit item: verify the closure's conditional-spread on `configSearchedFrom` landed in the scan-assembly path and not only on a narrower surface. If the fix is narrower than the spec, widen to every MCP response that names the config-context triple.
- [x] **V1-PROPOSED-CONFIG-ALIAS-DEPRECATION-WARN** Q3-PROPOSE-CONFIG-FIELD-DRIFT closed as "transition alias for one release" — `bootstrap` still ships both `suggestedConfig` and `proposedConfig` with identical contents across all 4 repos on 2026-04-22. Regression-audit item: confirm the alias window is still live (CHANGELOG `### Deprecated`) and emit a `warnings: ["proposed_config_deprecated_use_suggested_config"]` code so agents drop the alias immediately instead of paying the double-payload cost on every `bootstrap` tool call. If the deprecation window is overdue, this is the removal item.
- [x] **V1-WARNINGS-DETAILS-CROSS-SURFACE-REGRESSION** Q4-WARNING-DETAILS-CROSS-SURFACE-UNIFY closed, but field tests reproduce the asymmetry: only `response_token_budget_truncated` carries a `warningsDetails` payload; `no_config_found`, `scanned_build_artifacts_present`, `extensions_skipped_no_parser`, `parse_errors_present`, `source_language_unsupported`, `template_files_parsed_as_literal` fire as bare codes. The payload-per-code contract is 1/7. Regression-audit item: verify the unify commit did add payloads for every code listed, OR narrow the doctrine to "only codes carrying quantitative payload get warningsDetails" and document that in the schema. Doctrine must match behavior.

Fix-suggestion regressions:

- [x] **V1-FIX-OLDTEXT-AMBIGUITY-LABEL-ADJACENT** `scan_project` per-finding `fix.oldText` for `forms/label-adjacent-unassociated` emits bare `"<label>"` for each of 5 sibling findings in `password-generator/index.html` — the same 4-char literal appears 5 times in the file, so an agent pasting through to `apply_fix` would clobber the first match every time regardless of intended line. Separately, `suggest_fix` correctly emits a multi-line unique-context `primary.edit.oldText` for the same rule. Two tools disagree on edit shape. Fix: unify — always ship the multi-line unique context. Update `buildFix` in `src/output/agent-response/build-finding.ts` to emit the same shape `suggest_fix` does when the rule supports an inline edit. Pairs with Q-SHARED-NEXTSTEP-SCHEMA-DISPATCH-TEST (closed) — same cross-tool contract discipline.
- [x] **V1-FIX-LANG-AUTOCOMPLETE-ALT-MECHANICAL-DOWNGRADE** `suggest_fix` returns `kind: "guidance"` for every finding tagged `fixClass: "mechanical"` on `document/lang-attribute`, `forms/autocomplete-missing` (×3), and `media/alt-text-missing` on canvas — 5 of 10 mechanical findings degrade to guidance. `document/lang-attribute`'s one-hop fix is literally `<html>` → `<html lang="en">`; `forms/autocomplete-missing` has deterministic token derivation from `type="password"` / `id="email"`. Pairs with Q6-SUGGEST-FIX-MECHANICAL-VS-GUIDANCE-DRIFT (open). Fix: every rule scan tags as `fixClass: "mechanical"` must return `kind: "edit"` from `suggest_fix`; when the edit depends on surrounding context the rule can't resolve (e.g. canvas needs `aria-label="<derived-from-parent-text>"`), emit `kind: "edit"` with the canonical default + a `needsReview: true` signal rather than degrading to guidance. OR re-tag as `verify-in-source` at scan time so `fixClass` and `suggest_fix.kind` stay consistent. Specific rules named: `document/lang-attribute`, `forms/autocomplete-missing`, `media/alt-text-missing`.
- [x] **V1-SUGGEST-FIX-VERIFYCOMMAND-ON-NONE** `suggest_fix` with `kind: "none"` (when no finding exists at the cited line) still ships a populated `verifyCommand` + `verifyCommandStructured` pair that tells the caller to "confirm the rule no longer fires at this location." A caller cannot distinguish "finding never existed" from "you already fixed it and verified." Fix: when `kind: "none"`, drop the verify fields (present-when-meaningful). Doctrine: ambiguous field shapes are dishonest.
- [x] **V1-SUGGEST-FIX-TAILWIND-HINT-SCOPED** `focus/outline-visible` guidance text appends a Tailwind escape-hatch suggestion ("If this element uses Tailwind's focus-visible:ring-*…") regardless of whether Tailwind was detected in the scan. On vanilla CSS repos  this reads as context-blind advice. Fix: gate the Tailwind hint on `meta.tailwindDetected` (already computed for the `tailwind_detected_css_undercounted` warning). When false, omit the hint; when true, keep. Mechanical scope in `suggest_fix`'s guidance-composition path.
- [x] **V1-SOURCECONTEXT-LINE-ENDING-NORMALIZE** `suggest_fix.sourceContext` preserves the source file's `\r\n` CRLF line endings (observed on `simple-timer/index.html` on vanilla HTML/JS projects). If `apply_fix` generates `newText` normalized to `\n`, a write would silently mix line endings. Fix: detect the file's native line-ending (probe first `\r\n` vs `\n` occurrence) and normalize `newText` to match before writing. Add fixture under `tests/fixtures/real-world/crlf-source-file/` + integration test: apply a fix to a CRLF file, assert file byte-content has only CRLF afterwards.

Minification / build-artifact classification:

- [x] **V1-BUILD-ARTIFACT-REGRESSION-AUDIT-MINIFIED** Q3-BUILD-ARTIFACT-SINGLE-LONG-LINE-SECOND-PROBE closed (second-probe shipped) but the "minified" mis-label reproduces across many project shapes: design-system trees surface ~50 authored files mis-labeled; SSG trees surface ~20 authored markdown + SCSS partials; vanilla-JS trees surface pretty-printed HTML with SRI-hash CDN links; bulk-template trees surface 1000+ hand-authored HTML/CSS entries. Either the closure shipped narrower than spec (did not add all secondary predicates), the classifier's predicate ordering is wrong (short-circuits on single-long-line before second-probe runs), OR the MCP subprocess is serving a pre-closure bundle. Regression-audit item: read `src/mcp/build-artifacts.ts`, confirm the second-tier predicate is a required conjunct (not a disjunct bypass), and curate fixtures for the three prevalent false-positive shapes: (a) HTML with inline CDN `<link integrity="sha512-...">` (one long line from the SRI hash), (b) HTML with inline Google Maps embed or inline SVG (one long line from the embed payload), (c) SCSS function body with long type signatures. Until it lands, the `scannedBuildArtifacts` warning mistakenly trains agents to skip real authored source.
- [x] **V1-BUILD-ARTIFACT-REASON-EXPLAIN** When a file is labeled `{reason: "minified"}` in `scannedBuildArtifacts`, the label carries no explanation of the heuristic signal ("longest line 519 chars > threshold 500", "minified-infix pattern match", "sibling sourcemap detected"). An agent wanting to verify a false positive has to re-open the file and guess the heuristic. Fix: enrich `reason` to `{kind: "minified", signal: "max-line-length-exceeds-threshold", value: 519, threshold: 500}` OR add a `reasonDetail: string` sibling field. Pairs with V1-BUILD-ARTIFACT-REGRESSION-AUDIT-MINIFIED — deterministic explanation helps both the classifier fix and consumer dismissal. Q4-SCANNED-BUILD-ARTIFACTS-REASON (closed) added the reason taxonomy; this item adds the per-entry signal.
- [x] **V1-MINIFIED-FILE-SCAN-KIND-SPLIT** Minified files are scanned with the full rule set and findings flow into `plan.violations` alongside authored-source findings. On one a template catalog sample: 187 total violations on a template where 41 live in `css/bootstrap.min.css` (vendor). An agent reading "187 violations" has no idea 41 are on files they cannot edit. Fix: add sibling counter `plan.violationsByScanKind: { source: 146, buildArtifact: 41 }` (or similar naming). Findings still emit in `files[]` per surface-don't-suppress; the headline counter splits by kind. Per-rule version is Q6-MOTION-PAUSE-STOP-PER-FILE-AGGREGATION (open). This is the cross-rule sibling. Doctrine: composite headline counts are dishonest.
- [x] **V1-SCANNED-MINIFIED-FILE-WARNING-CODE** No structured warning code for "this scan touched a file ra11y classifies as minified; findings on it may be unreliable." `scanned_build_artifacts_present` signals the scan touched some; it does not signal per-file confidence. Fix: add `scanned_minified_file` as a new structured warning code with `warningsDetails.scanned_minified_file: { files: [paths] }` payload. Pairs with V1-BUILD-ARTIFACT-REASON-EXPLAIN — together they let agents triage vendor noise without per-file reading.
- [x] **V1-JS-PARSE-ERROR-REASON-MISLEADING** `partialParseFiles` entries for minified `.js` routed to the TSX parser emit reasons like `"Unclosed JSX element <script>"` / `"<r.length>"` / `"<parseInt>"`. The reason string is parser-internal terminology about JSX — the honest diagnosis is "minified JavaScript parsed as TSX; re-parse as JS or skip." Current reason actively misleads: an agent reads "fix the JSX" when there is no JSX. Fix: when the TSX parser's error fires inside a file without `.tsx`/`.jsx`/`.mdx`/`.astro` extension AND the error site looks like a JS comparison operator (`identifier<other`), rewrite the reason to `"Minified or plain-JS <...> comparison operator parsed as JSX element; file likely needs a non-JSX parser."` Pair with Q-SHARED-TSX-PARSER-FALSE-JSX-CONTEXTS (reopened `[~]`) — this item is the reason-text enrichment so the reason stays honest until the underlying parser-choice fix lands.
- [x] **V1-PARSE-ERROR-LIVERELOAD-MIXED-SIGNAL** `livereload.js` routed to tsx parser errors with "Unclosed JSX element `<r.length>`" — yet `wcag22:2.2.1` review candidates still surface correctly from the same file at lines 1161 and 1178 with `setTimeout` context. `parseErrorFileCount` claims the file produced zero findings; reality is that some path in the candidate-finder bypasses the parser-error short-circuit. Either the bucket is mislabeled (should be `partialParseFiles`) or the finder bypass is silent. Fix: audit every review finder whose substrate is source text (not parsed AST) — if the source-text path produces candidates even when the AST parse failed, either (a) move the finding entry from `parseErrorFiles` to `partialParseFiles` when ANY candidate emerged, or (b) annotate the candidate with `fromSourceTextOnly: true` so the agent knows the AST didn't support the find. Per-file-coverage telemetry is load-bearing; `parseErrorFiles` claiming "invisible to rules" while candidates emerge is dishonest.
- [x] **V1-VENDOR-LIBRARY-BANNER-DETECTION** `bootstrap.min.css`, `animate.css`, `font-awesome.min.css` are identifiable from first-line banner comments (`/*! v3.3.1 */`, `/*! Font Awesome ... */`). No signal surfaces today — the agent sees contrast findings on `bootstrap.min.css` and cannot tell the file is a well-known vendor library. Fix: add `meta.vendorLibraries: [{path, library: "bootstrap", version: "3.3.1"}]` derived from first-line banner-comment matching against a curated list (bootstrap, font-awesome, animate.css, jquery, jquery-ui, fancybox, modernizr, normalize.css, reset.css). When populated, agents can triage "these 43 findings live inside bootstrap 3" instantly. Companion to V1-MINIFIED-FILE-SCAN-KIND-SPLIT. No suppression, no severity downgrade — only a label. Doctrine: labeled buckets are only honest when provable from the code; first-line banner matching is deterministic.
- [x] **V1-VENDOR-ANIMATION-LIB-GUARD-HINT** `animate.css` in a website-template (3263-line hand-authored library clone) emits 27 `motion/pause-stop-hide` findings — one per keyframe helper class. All 27 are spec-correct but the remediation is "wrap the whole `@import animate.css` in a `@media (prefers-reduced-motion: no-preference)`" — one wrap, not 27 pragmas. Fix: when a rule fires >N× (threshold: 20) on a single CSS file with the same selector-family pattern (`.animate__*` or similar keyframe-helper namespace) AND that file is tagged as a vendor library (depends on V1-VENDOR-LIBRARY-BANNER-DETECTION), emit `warnings: ["animation_library_without_reduced_motion_guard"]` + a response-level hint "wrap the vendor animation library in `@media (prefers-reduced-motion: no-preference)` instead of adding per-selector pragmas." Additive — findings still emit individually.

Checklist / review-candidate shape:

- [x] **V1-CHECKLIST-PRAGMA-IN-CANDIDATE** `checklist` response has no per-candidate `pragma` / `suppressWith` field — the MCP workflow instructions tell agents to "Suppress at source only when genuinely N/A via `<!-- ra11y-disable wcag22:… -->` / `{/* ra11y-disable wcag22:… */}`" but the prose pragma is absent from the checklist response. `scan_project` findings carry `suppressWith` per-finding; `checklist` candidates do not. An agent walking the checklist has no in-band pointer. Fix: every candidate emits `suppressWith: { html, jsx }` or `pragma: "<!-- ra11y-disable wcag22:<id> --> or {/* ra11y-disable wcag22:<id> */}"` — same shape the `scan_project` finding uses. Closes the loop the doctrine promises ("Dismiss by reading. Suppress at source only when genuinely N/A").
- [x] **V1-CHECKLIST-CRITERION-GROUP-DEDUP** Same candidate surfaces three times under `wcag22:1.2.1 / 1.2.3 / 1.2.5` (`ratio.mdx:14` — an `<iframe>` mention inside a `<Callout>` prose block). Three identical reason strings; three independent `attest` entries needed. Fix: when one candidate location (file:line + groupKey) satisfies multiple criteria, emit ONCE with `criteriaIds: string[]` array — mirror the `scan` finding shape where `criteria: [...]` is the existing pattern. OR group under a shared `criterionGroup` / cross-criterion reason block. Per-criterion `attest` can still happen, but the candidate record is one. Surface-don't-suppress: no suppression — same three criteria still covered.
- [x] **V1-UNTARGETED-CRITERIA-DEFAULT-EMIT** Checklist `summary.untargetedCriteriaCount: N` ships by default but the list is gated behind `showUntargeted: true`. Per the AI-first consumer doctrine (agents read in ms, not seconds; surface-don't-suppress), the default should invert: emit `untargetedCriteriaList` with bare criterion IDs by default; keep `showUntargeted: false` as a size-pressure escape. Low-cost change; high-value for agent consumers who need to see A-level blockers vs AAA prompts. Doctrine: surface-don't-suppress, verbose meta is signal.
- [x] **V1-REVIEW-CANDIDATE-REASON-MDX-CALLOUT-ENRICHMENT** MDX `<Callout>` prose that mentions an HTML tag name in code-spans (backticks) is parsed into a real candidate by iframe/media review finders — `the MDX docs-site `ratio.mdx`:14` quotes `<iframe>` in a `<Callout>` and surfaces three 1.2.x candidates with reason "iframe element -- verify embedded media". No real iframe exists at line 14. Surface-don't-suppress per doctrine, but reason should name the evidence honestly: "iframe token is inside an MDX `<Callout>` prose block (may be inside backticks/code); verify the element actually renders." Apply the same enrichment whenever the candidate's surrounding AST context is a documented prose container (`<Callout>`, `<Note>`, `<Warning>`, `<Example>` when its body is JSX/prose rather than raw HTML). Reason-enrichment only; candidates stay in the primary list. Pairs with Q3-MDX-EXAMPLE-TEMPLATE-LITERAL-SUBSTRATE (closed) — this item is the post-parse reason layer; that item was the substrate-extraction parser layer.
- [x] **V1-LIKELY-IRRELEVANT-INCONSISTENT** Same scan emits `likelyIrrelevant: 0` on a real design-system repo yet `likelyIrrelevant: 4` (1.2.1/1.2.3/1.2.4/1.2.5) on an empty scan. The design-system scan carries iframes that keep the media criteria out of the irrelevant bucket — arguably correct — but the inconsistency "0 in a real scan, 4 in an empty scan" reveals the bucket's eligibility predicate is not stable across scan shapes. Regression-audit: verify the predicate in `src/review/finders/media-*.ts` — `likelyIrrelevant` is triggered on emptiness (provable from code per doctrine) but the iframe-as-media heuristic leaks one finder that contradicts. Pairs with Q-SHARED-LIKELY-IRRELEVANT-IFRAME-NOT-MEDIA (open). Doctrine: labeled buckets must be provable from evidence.
- [x] **V1-PRAGMA-LIQUID-COMMENT-SYNTAX** SSG source files can use `<!-- ra11y-disable wcag22:… -->` but the HTML comment renders to the generated page. Authors prefer `{% comment %}ra11y-disable wcag22:… {% endcomment %}` which disappears at render. Same concern for every SSG: Hugo `{{/* ... */}}`, Eleventy `{% comment %}...{% endcomment %}`, Astro `{/* ... */}`. Fix: extend `src/config/inline-disables.ts` to recognize Liquid comment (`{% comment %}ra11y-disable …{% endcomment %}`), Hugo comment (`{{/* ra11y-disable … */}}`), and Astro JSX comment (`{/* ra11y-disable … */}`) as pragma carriers. Pick the syntax based on file extension / detected template family. Additive; no removal. Doctrine: the deterministic dismissal escape hatch must be usable without polluting rendered output.

`additionalPaths` / scope:

- [x] **V1-ADDITIONAL-PATHS-PRESENT-BUT-UNPARSEABLE** `scan_project({additionalPaths: ["rake/"]})` on a Ruby-only directory returns `filesAdded: 0` with NO `skipped[]` entry and no per-path reason. `nonexistent-subdir/` correctly reports `skipped: [{path, reason: "not-found"}]`. Caller cannot distinguish "dir absent" from "dir present but all files unparseable-by-extension" without re-checking with their own tools. Pairs with Q3-ADDITIONAL-PATHS-SKIP-REASON (closed, added `not-found` / `unsupported-extension` / `excluded-by-glob`). Fix: add a fourth `reason: "no-parseable-files"` with `extensions: {[ext]: count}` payload echoing the observed extensions. Add invariant test: every passed `additionalPaths` entry resolves to either `filesAdded > 0` OR a `skipped[]` entry — never silently nothing. Doctrine: present-when-meaningful requires the negative answer to be structurally distinguishable.
- [x] **V1-ADDITIONAL-PATHS-SCOPE-RESTRICT** `scan_project({additionalPaths: ["subdir/"]})` on a template catalog returned identical counts (4043 scanned, 40132 violations) to the root-scoped scan — `additionalPaths` only ADDS; it has no complement. A caller wanting to scope the project scan to a single subdirectory (without using `scan` which has different shape + no paging) has no affordance. Fix: add `restrictToPaths?: string[]` (or `scope?: string[]`) to `scan_project` that intersects the discovered file set with the passed paths. Alternatively document that `scan` (not `scan_project`) is the scoped variant and update `tools/list` descriptions accordingly. Pick one. Doctrine: one tool call should answer "what next?" — on a 1793-file-with-findings repo the agent has no scoping primitive other than giving up on `scan_project`.

Catalog-scale perf / scoping:

- [x] **V1-BULK-CATALOG-SCAN-PERF-12S** `scan_project` on a template catalog (4043 files / 172 templates / 16 MB) took `durationMs: 12188` vs the 3s budget for 1000 files/100k LOC. The corpus is ~16 MB of near-duplicate vendor libraries (bootstrap.css × 172 copies) — the scan is honest but the real-world performance class is "bulk vendor catalog." No hint or warning surfaces the perf class. Fix: when `durationMs > budget*K` (K=3) AND `scannedBuildArtifacts.length > N` (N=50), emit `warnings: ["bulk_catalog_detected"]` + `analysisCoverage.hints` suggesting `exclude: ['**/bootstrap*.css', '**/animate.css', ...]` OR a per-template scoped scan. Pair with V1-ADDITIONAL-PATHS-SCOPE-RESTRICT. Doctrine: agents need scan-confidence telemetry that includes perf class, not only correctness.
- [x] **V1-RESPONSE-SIZE-PRE-ESTIMATOR** `scan_project` runs the full scan THEN truncates to fit the response-token budget. On a template catalog, `limit: 100` returns `effectiveLimit: 1` — the agent pays 12 seconds of scan time and gets back one file. Fix: add `estimateOnly?: boolean` (or `dryRun?: boolean`) input that returns `{filesWithFindings, estimatedResponseBytes, suggestedLimit}` without per-file bodies. Lets agents plan pagination before committing to the full scan. Pairs with V1-BULK-CATALOG-SCAN-PERF-12S — both give the agent pre-scan signal about scale. (already landed via the oversize-envelope work; verified 2026-04-25 by /continue turn 9 audit — slim envelope keeps plan+meta+warnings+nextStep, drops files[])
- [ ] **V1-BENCH-4K-FILE-VENDOR-HEAVY-SCENARIO** Add a `scripts/bench.ts` scenario that synthesizes a 4000-file vendor-heavy corpus (≥50 near-duplicate vendor CSS copies, mixed HTML + JSX leaf templates) and asserts the new CLAUDE.md §11 row (`≤ 15 s`). Without this fixture, the budget row is aspirational. Pair with V1-BULK-CATALOG-SCAN-PERF-12S so both the warning emission AND the budget enforcement are tested. Decision rationale captured in 2026-04-24 J-6 research outcome (CLAUDE.md §11 row added).
- [ ] **V1-LIMITATIONS-EXTERNAL-HANDLER-RESOLUTION** Add the structured limitation code `external_handler_resolution_unavailable` to the `limitations[]` vocabulary on `scan_project` / `scan` / `scan_file` responses. The code surfaces when a finder needs cross-file evidence to confidently resolve a click/change handler binding (separate `.js` modules attaching listeners via `addEventListener`, JSX importing handler identifiers from sibling files). Test coverage: an integration test on a vanilla-JS fixture where a `<div class="btn">` has no inline handler but a sibling `.js` file calls `querySelector('.btn').addEventListener('click', …)` — the response surfaces the limitation code in `limitations[]` even though the rule fires confidently. Doctrine: don't duplicate capability the agent already has; the limitation surfacing is the structured pointer that lets the agent decide when to follow up with Read+Grep. Decision rationale captured in 2026-04-24 J-7 research outcome; gotcha doc lives at `docs/kb/gotchas/cross-file-handler-resolution.md`.
- [x] **V1-REASON-EXTERNAL-HANDLER-POINTER** Enrich `keyboard/handler-missing` (and any rule whose evidence could be answered by a handler defined in a sibling script) with reason-text that explicitly names the cross-file possibility — e.g. `"no inline handler found; if this element is wired from an external script, verify the binding in the file referenced by the nearest <script src="...">"`. Trigger heuristic: presence of `<script src="...">` in the same HTML document, OR JSX imports from a sibling `.js`/`.ts` module. Pairs with V1-LIMITATIONS-EXTERNAL-HANDLER-RESOLUTION — the limitation code is the response-level pointer; the reason text is the per-candidate pointer. Doctrine: surface-don't-suppress, but enrich reason with the dismissal/follow-up signal so the agent reads the pointer in-line.
- [x] **V1-CROSS-FILE-ROLLUP-PRIMITIVE** No cross-file rule-frequency rollup exists at response top-level; `meta.perRuleCoverage` has per-rule `findingsEmitted` but not cross-file rank. On a 1793-file-with-findings repo the agent has to page 1793 times (each `scan_project` call at `effectiveLimit: 1` costs ~12 s of server time or up to 300 KB of response tokens) to see the rule distribution. Fix: add `plan.topRules: [{ruleId, count, topFile}]` rank-ordered (top 10). Additive, zero information loss. The pieces already exist in `perRuleCoverage.findingsEmitted` + `concentration.file`; this item exposes them at the headline.
- [ ] **V1-GROUPKEY-COLLAPSED-RESPONSE-MODE** `groupKey` is emitted on every finding and is cross-file for location-agnostic groupings. No response-shape option compresses `(ruleId, groupKey)` into one entry with the file list. On a template catalog 40k findings likely collapse to <500 unique groups. Fix: add `collapseByGroupKey?: boolean` to `scan_project` (default false, preserves current shape). When true: response emits one entry per unique `(ruleId, groupKey)` with `occurrences: [{path, line, column}]` — same paging/truncation semantics. Additive — callers opt in. Doctrine: one tool call should answer "what next?" on scale. Pairs with Q6-PATTERN-FINGERPRINT-CROSS-TEMPLATE (open, cross-template pattern ID).
- [x] **V1-GROUPKEY-POLICY-DOC** `groupKey` semantics are undocumented. It's cross-file for `navigation/link-descriptive-text` + `navigation/href-javascript-void` but includes file-specific tokens for `media/alt-text-missing` (filename participates). Agents cannot tell without experimentation whether `groupKey` equality implies "same underlying snippet" or "same rule firing." Fix: add `referenceGuide.groupKeyPolicy: { fields: ["ruleId", "reasonKind", "hrefLiteral?", "srcBasename?", …] }` to the response root documenting which fields participate in the hash, per rule family. Alternative: one-sentence docstring in every rule's metadata surfaced via `explain_rule.groupKeyFields`. Doctrine: ambiguous field shapes are dishonest; a load-bearing key whose semantics vary by rule is the ambiguity.

### Additional items

Parser / language coverage:

- [x] **V1-PERRULE-COVERAGE-HONESTY-ON-PARSE-ERRORS** `scan_file` on a parse-errored file (e.g. `site/src/libs/remark.ts`) reports `meta.analysisCoverage.parseErrorFileCount: 1` while `perRuleCoverage` marks every rule as `filesEvaluated: 1, filesEligible: 1, findingsEmitted: 0, coverageConfidence: "high"` — a consumer reads "file fully evaluated, no violations" when the parser never ran the rules at all. Same shape for `partialParseFiles`. Fix: when a file is in `parseErrorFiles` or `partialParseFiles`, drop its per-rule `coverageConfidence` to `"low"` with `reason: "file-parse-error"` / `"partial-parse"` and do not count it in `filesEvaluated`. Doctrine: zero-output success is ambiguous failure (applied at per-rule granularity).

Rule correctness / detection:

- [x] **V1-FP-KEYBOARD-HANDLER-DATA-BS-CONTAINER** `keyboard/handler-missing` false-positives on container-level `data-bs-toggle="buttons"` (e.g. `<div class="btn-group" data-bs-toggle="buttons">`) and on the auto-init attribute `data-bs-ride="carousel"` — both are group-container / auto-init attributes, not interactive triggers. Worse, `suggest_fix` recommends wrapping the container in `<button type="button">`, which would nest interactive descendants (labels + inputs) inside a button and break the Bootstrap 5 layout. Fix: build an explicit disclosure-value allowlist for `data-bs-toggle` (`{modal, tooltip, popover, collapse, offcanvas, dropdown, tab}`) and exempt container values (`buttons`) plus every `data-bs-ride` auto-init attribute. Add fixtures `tests/fixtures/real-world/btn-group/` and `tests/fixtures/real-world/carousel-ride/`. Doctrine: surface-don't-suppress, but false-positive fix suggestions are worse than silent misses because they teach anti-patterns.
- [x] **V1-ROLE-FROM-CLASS-ADMONITION-HEADING-CHILD** `aria/role-from-class-only` fires on SSG admonition patterns `<div class="note info"><h5>Topic</h5><p>…</p></div>` — the `<h5>` IS the accessible-name / severity carrier (screen readers announce "heading level 5: Topic…"), but the rule flags the wrapper as role-bereft anyway. On one SSG scan, 53 of 323 findings (16%) are this shape. Same pattern recurs across every SSG admonition style (Hugo, Docusaurus, Astro, MDX). Fix: before flagging, check whether the admonition's first element child is an `h1..h6`; when true, downgrade to review-candidate or suppress (the heading carries structural severity). Doctrine: rule premise contradicts the structural severity signal AT already honors. Distinct from Q4-RULE-NOTE-ROLE-FROM-CLASS (which added the rule) — this is the tightening pass.
- [x] **V1-LINK-DESCRIPTIVE-TEXT-SAME-NAME-SAME-HREF** `navigation/link-descriptive-text` flags repeated `{same visible text, same href}` link pairs as "same accessible name / same href" violations. WCAG 2.4.4's rationale permits this — two links to the same destination are allowed to share a name; AT announces "visited" on re-encounter. Only same-name + DIFFERENT-href is the true violation. Current rule drops ≥8 FPs on an SSG docs tree (`docs/_docs/step-by-step/05-includes.md:52,79` twice to `http://localhost:4000`; `docs/_docs/structure.md:104,206` twice to `/docs/front-matter/`). Fix: dedupe the "duplicate-name" check by `(normalizedText, normalizedHref)` tuple — only flag when text matches but href differs. Distinct from V1-LINK-DESCRIPTIVE-TEXT-TITLE-DUPLICATE (title-duplicate axis).
- [x] **V1-NAVIGATION-HREF-VOID-RULE-SCOPE-DOC** `navigation/href-javascript-void`'s rule ID implies the JavaScript-void pattern only, but the rule also fires on bare `href="#"` with no fragment target. An agent registering a suppression via the rule ID may silence more than expected. This is the documentation half of V1-RULE-NAVIGATION-HREF-VOID-RENAME (which is the rename half): until the rename lands, `explain_rule` should enumerate every shape the rule catches (`javascript:void(0)`, `javascript:;`, bare `#`, empty `href=""`). Fix: `explain_rule[navigation/href-javascript-void].scope` enumerates all matched href shapes. Pairs with but distinct from the rename item.
- [x] **V1-ICON-FONT-HIDDEN-NONINTERACTIVE-ANCESTOR** `aria/icon-font-hidden` fires zero findings across 50+ FA-using pages — including `<h3>Double click on the image to <i class="fas fa-heart"></i> it</h3>` where the icon sits inside non-interactive visible text and still falls through silently. The rule appears to require an accessibly-named interactive ancestor; when the surrounding context is prose (h1..h6, p, li) the check never triggers. Fix: extend `aria/icon-font-hidden` to fire on `<i class="fa-*">` without `aria-hidden` sitting inside `<h1>..<h6>`, `<p>`, `<li>`, or any non-interactive visible-text context. Doctrine: surface-don't-suppress — the icon communicates meaning that AT cannot read without a hidden or label.

Fix-suggestion regressions:

- [x] **V1-SUGGEST-FIX-MECHANICAL-LANE-EMIT-EDIT** Every rule tagged `fixClass: mechanical` in the scan plan (`aria/expanded-on-disclosure`, `semantics/landmark-main`, `navigation/href-javascript-void`, `motion/pause-stop-hide`, `keyboard/handler-missing`, `semantics/heading-hierarchy`, etc.) still returns `kind: "guidance"` from `suggest_fix` — zero `kind: "edit"` observed across the a design-system sample. If `plan.fixesByClass.mechanical: 266` is load-bearing telemetry for "how many apply-now edits," it is currently inflated: most mechanical-tagged rules don't ship structured edits. Either the mechanical tag is wrong (re-tag at scan time so `fixClass` matches `suggest_fix.kind`) or `suggest_fix` is failing to construct the edit. Extends V1-FIX-LANG-AUTOCOMPLETE-ALT-MECHANICAL-DOWNGRADE to every `fixClass: mechanical` rule, not just the three named there. Doctrine: composite headline counts are dishonest — the lane counter must match what `suggest_fix` can actually produce.
- [x] **V1-BUTTON-NAME-ICON-FONT-MECHANICAL-EDIT** `semantics/button-name` on icon-font-only buttons emits `kind: "guidance"` even when the rule's own `referenceGuide.fixDescriptions` prose already names the deterministic aria-label (`Primary fix: aria-label="Close"`, `Primary fix: aria-label="Next"`). The derivation is mechanical — FA glyph → action — and ships for some files (faq-collapse, background-slider) but falls back to generic "add visible text" prose for others (simple-timer `fa-play`, password-generator `fa-clipboard`). Fix: unify the code path that picks the FA-aware fixDescription so it applies for every `<button>` whose sole child is `<i class="(far|fas|fab|fa) fa-<glyph>">`, and emit the structured `{oldText, newText}` edit matching the prose. Extends V1-FIX-LANG-AUTOCOMPLETE-ALT-MECHANICAL-DOWNGRADE.
- [x] **V1-FA-GLYPH-ARIA-LABEL-DERIVATION-UNIFY** FA-glyph → action-label derivation is inconsistent across the scan: the code path that maps `fa-times → Close` / `fa-arrow-left → Previous` fires for some files but not others (simple-timer, password-generator, event-keycodes fall through to generic fallback). Single shared derivation helper should be called by both the fixDescription composer and `suggest_fix` so the same mapping drives both surfaces. Pairs with V1-BUTTON-NAME-ICON-FONT-MECHANICAL-EDIT (the emit-as-edit fix) — this item is the derivation consistency axis. Doctrine: the tool doing it must be right every time or it trains agents to distrust the derivation.
- [x] **V1-SUGGEST-FIX-ALT-DIMENSIONS-ANTIPATTERN** `suggest_fix` for `media/alt-text-missing` literally echoes `alt="700x400"` as the suggested alt text when the image URL ends in `…/700x400` — teaching the WCAG 1.1.1 dimensions-as-alt anti-pattern that ra11y's own `media/alt-text-placeholder` rule would flag. Fix: strip URL basenames matching `/\d+x\d+$/` (and other placeholder shapes `/placehold(?:er)?\.\w+/`, `/\d{2,4}[.-]?\d{2,4}/`) from the suggested alt placeholder before emitting; fall back to generic "describe what this image communicates" prose. Doctrine: the tool must not teach patterns its own rules catch.
- [x] **V1-APPLY-FIX-LIQUID-FP-DIAGNOSIS** `apply_fix` on a finding whose root cause is a Liquid/template false-positive (e.g. `document/page-titled` firing on `<title>{{ page.title }}</title>`) returns `code: "edit-no-match"` with remediation "Re-run suggest_fix" — but `suggest_fix` on the same finding emits `kind: "guidance"` with no edit, so a naive agent loops. The error doesn't diagnose the upstream "finding is a template-directive FP" root cause. Fix: when `apply_fix` fails to locate `oldText` AND the target line contains a detected template-directive token (`{{…}}`, `{%…%}`, `<%=…%>`), emit a dedicated `code: "target-contains-template-directive"` + remediation "verify the rendered output — the cited finding may be a false positive on a template expression; suppress at source if the expression is trusted." Pairs with V1-LIQUID-TEMPLATE-EXPRESSION-AS-SOLE-CHILD-REASON-ENRICHMENT — this item is the apply-fix-side surface.

Response shape / honesty:

- [x] **V1-RULES-BY-EXTENSION-LABELING** `meta.analysisCoverage.rulesByExtension` and `perRuleCoverage` carry incompatible "what ran?" signals under look-alike labels: the first lists rules that FIRED at least once on the extension; the second lists rules that were EVALUATED. On `scan_file` of `accessibility.mdx` the first returns 2 rules, the second returns 53 — both plausibly "rules that ran." Fix: rename `rulesByExtension` to `rulesFiredByExtension`, OR merge into `perRuleCoverage` with an explicit `evaluated` / `fired` split. Doctrine: ambiguous field shapes are dishonest (at field-name level).
- [x] **V1-SESSION-RULECOUNT-VS-SCAN-RULESLOADED** `sessionConfigure.active.ruleCount` is off-by-one against `scan_project.meta.rulesEvaluated.loaded` (74 vs 75) on the same registry in the same session. Either align both counters, or label the difference (e.g. `ruleCount` excludes internal synthetic rules like `internal/rule-crash`; `rulesEvaluated.loaded` counts them). Doctrine: cross-surface counts that name the same concept must agree or disclose the difference.
- [ ] **V1-TEMPLATE-DIRECTIVES-FOUND-STRUCTURED** `analysisCoverage.templateDirectivesFound` ships as bare string tags (`["erb-or-ejs", "jinja-or-liquid"]`). An agent can't tell whether `erb-or-ejs` means "`<% %>` literals inside an HTML file the parser saw" (rules ran) or "an `.erb` file got fully skipped" (rules invisible). On an SSG source tree both sources contribute to the same bucket. Fix: emit `templateDirectivesFound: [{ kind, extension, fileCount, source: "literal-in-parseable-file" | "skipped-extension" }]` so agents branch deterministically. Doctrine: ambiguous field shapes are dishonest.
- [x] **V1-DETECT-NATIVE-WRAPPERS-PROJECTKIND-HINT** `detect_native_wrappers` on non-JSX projects (Ruby, Python, Go, static-site) returns `emptyReason: "no-pascalcase-onclick-components"` — technically accurate but the agent can't distinguish "no components detected in a JSX-using repo (coverage miss)" from "this isn't a JSX repo at all (tool doesn't apply)." Fix: add sibling `projectKind: "jsx" | "static-site" | "ruby" | "python" | "go" | "unknown"` derived from file-extension ratios (`.tsx`/`.jsx` > 0 → jsx; `.rb`/`.py`/`.go` dominant → named; otherwise `static-site` or `unknown`). Short-circuits speculative calls. Additive. Doctrine: one tool call should answer "what next?" — the current response forces a second call to the same tool on a different cwd to discriminate.
- [ ] **V1-SCAN-PROJECT-FINDINGS-BY-FILE-HISTOGRAM** `scan_project` exposes `totalFilesWithFindings: N` but no per-file or per-rule distribution at the response root — agents paginating to understand distribution walk every page blind. Fix: add `plan.findingsByFile: [{path, count}]` (top-N rank-ordered) OR `plan.findingsByRule: {ruleId: count}` (whole-scan histogram). Pairs with V1-CROSS-FILE-ROLLUP-PRIMITIVE (top rules) — this item is the per-file orthogonal. Additive; no information loss. Doctrine: one tool call should answer "what next?" on scale.
- [x] **V1-COVERAGE-SCANNED-POINTER-MISSING** `coverage({cwd})` returns `scanned: null` while `scan_project({cwd})` returns `scanned: {mode, root}` on the same cwd. An agent reading `coverage`'s response to verify what got scanned has to cross-reference a separate `scan_project` call. Fix: populate `coverage.scanned` with the same shape `scan_project` uses (`{mode, root, filesScanned, filesByExtension}`). Present-when-meaningful would drop `scanned: null`; when populated it must match `scan_project`'s pointer. Doctrine: cross-surface shape drift is dishonest.
- [ ] **V1-COVERAGE-MANUAL-CANDIDATES-TOTAL-CROSS-SURFACE** `checklist.summary.totalCandidates` (pre-paging per-candidate count) has no sibling on `coverage` — an agent asking "how many manual-review items" reads `coverage.manualWithCandidates: N` (criteria count) and `checklist.summary.actionable: M` (same criteria count) and `checklist.totalCandidates: K` (candidate count) — three numbers, one concept, disambiguated only by reading field names carefully. Fix: expose `coverage.manualCandidatesTotal: K` alongside the existing criteria-level counters, with the "candidates vs criteria" split explicit in both `coverage` and `checklist`. Doctrine: composite headline counts are dishonest; labels must make kind explicit.
- [x] **V1-CONFIG-NOTE-COLLAPSE-REDUNDANT-PROSE** `meta.configNote` ships a ~260-char prose string ("No ra11y.config found walking up from … — using built-in defaults …") on every scan when `configSource: null` — duplicating the `no_config_found` structured warning code. Fix: collapse to `configNote: true` when `warnings` contains `no_config_found`, rely on the structured warning for details; keep the prose field only when the detail differs from the code's documented meaning. Pairs with V1-CONFIG-CONTEXT-TRIPLE-READOUT-REGRESSION — same triple-readout concern, simpler axis. Low priority. Doctrine: verbose meta is signal, but redundant prose alongside a structured code is bloat.
- [ ] **V1-CHECKLIST-HEADLINE-COMPOSITE-SPLIT** `checklist.summary.headline` renders `"12 actionable · 12 untargeted · 0 likely irrelevant"` on one line — while `meta.plan.actionableManualItems` + `untargetedCriteria` live separately as structured counters. The adjacent rendering invites agents to sum "12+12 = 24 things to review." Fix: drop the one-line composite headline; keep the structured per-kind counters; rename the headline string to explicitly name the split (`"12 actionable items; 12 untargeted criteria; 0 likely irrelevant criteria"`) OR emit as `headline: { actionable: 12, untargeted: 12, likelyIrrelevant: 0 }` object. Minor but canonical composite-headline-lie shape. Doctrine: composite headline counts are dishonest.

Tool-orchestration / bootstrap / suppress / write-gate:

- [ ] **V1-BOOTSTRAP-SUGGESTED-CONFIG-EXCLUDE-LOUD** `bootstrap` tool emits a `suggestedConfig.exclude` mechanically derived from `scannedBuildArtifacts` entries — on one design-system scan the suggestion was `exclude: ["js/**", "scss/_functions.scss", "site/**"]`, which covers every source tree in the project (54 mis-classified artifact entries × a `.*/**` generalization). An agent pasting this into `ra11y.config.ts` would drop 2273 findings to near-zero. Fix: cap `suggestedConfig.exclude` at a conservative set (≤ 3 entries, require ≥N independent mis-classification signals per entry) OR route build-artifact paths through a commented-out `likely-build-paths` block the agent must opt into. Never emit blanket `js/**` / `site/**` excludes from artifact heuristics. Pairs with V1-BUILD-ARTIFACT-REGRESSION-AUDIT-MINIFIED downstream — but even with the classifier fixed, the generator should not amplify single heuristic signals into tree-sized excludes. Doctrine: surface-don't-suppress, applied at the generator step (an agent can't un-exclude a tree that was never parsed).
- [x] **V1-SUPPRESS-MARKDOWN-FILE-UNSUPPORTED** `suppress({file: "…/docs.md", ruleId: "aria/role-from-class-only", reason: …})` rejects with `{code: "file-unsupported", error: "suppress handles .tsx/.jsx, .ts/.js, .html/.htm, and .css only."}` — yet the scanner flags findings on `.md` / `.markdown` files (hundreds of findings across `aria/role-from-class-only`, `semantics/heading-hierarchy`, `semantics/table-*` on SSG projects), and the scan response echoes a `suppressWith: "// ra11y-disable-next-line aria/role-from-class-only"` shape that the suppress tool itself refuses to apply. The MCP workflow doctrine ("dismiss by reading, suppress at source only when genuinely N/A") is unimplementable for the majority of SSG findings. Fix: extend `suppress` to accept `.md` / `.markdown` and emit the correct pragma shape — `<!-- ra11y-disable-next-line ruleId: reason -->` for embedded-HTML findings, OR a dedicated kramdown IAL / `{::comment}` form. Update the echoed `suppressWith` field to match the comment-kind of the target file extension. Sub-item of the `ra11y-disable` pragma family (pairs with V1-PRAGMA-LIQUID-COMMENT-SYNTAX, which is the reader half; this is the writer half). Doctrine: the deterministic dismissal escape hatch must be usable on every file the scanner flags.
- [ ] **V1-PLAN-SAFE-EDITS-STALE-MCP-SERVER** Commit `fce189c8 refactor(mcp): drop plan.safeEditsAvailable composite counter` removed `plan.safeEditsAvailable` per the composite-headline-lies doctrine — but the live MCP server still emits it on both `scan_project` and `bootstrap` responses. Either (a) the MCP subprocess is serving a pre-drop bundle, (b) the MCP server's build artifact isn't being picked up by `npm link` / the smoke harness, or (c) the drop landed in one code path but not the other. Fix: audit the MCP server build / release pipeline — add a CI smoke test that scans a fixture and asserts `plan.safeEditsAvailable` is absent from the response; confirm the dropped-field refactor reaches every invocation path. Doctrine: doctrine changes must propagate to the shipped artifact, not just the source.
- [x] **V1-CHECKLIST-LIMIT-NEGATIVE-VALIDATION** `checklist({limit: -1})` silently clamps to 1 per the tool's documented clamp range [1, 2000]; the caller passed a clearly-invalid value and received paginated-to-one-entry results rather than a validation error. Fix: reject any `limit < 1` with `{code: "invalid-argument", field: "limit", remediation: "limit must be in [1, 2000]"}`. Same treatment for the `maxCandidatesPerCriterion` bound. Mechanical. Doctrine: silent coercion on invalid input masks caller bugs.

Checklist / review-candidate shape:

- [x] **V1-CHECKLIST-IFRAME-FINDER-MDX-BACKTICKS** The iframe-review finder's source-text substrate does not strip MDX inline-code spans (backticks) or fenced code blocks before scanning — on `site/src/content/docs/helpers/ratio.mdx:14,19` it flags prose `` "**Pro-Tip!** You don't need `frameborder="0"` on your `<iframe>`s" `` as three 1.2.x candidates with `confidence: "high"`, while the real `<iframe>` inside `<Example code={…}>` on line 22 goes un-flagged. Mirrors Q6-CHECKLIST-IFRAME-FINDER-JS-STRING-LITERALS (JS string literals) for MDX. Fix: the iframe finder's source-text path must strip inline-code spans (`` `…` ``) and fenced code blocks (```` ``` ```` ... ```` ``` ````) in `.md` / `.mdx` before the iframe-token match. Downgrade `confidence` to at most `"medium"` when the matching token sits inside what the source looks like a prose container. Pair fixtures: `tests/fixtures/real-world/mdx-iframe-in-backticks/`. Doctrine: confidence `"high"` on a backtick-escaped prose match is dishonest.
- [ ] **V1-REVIEW-CANDIDATES-SIBLING-REASON-DEDUP** `reviewCandidates[*].reason` duplicates prose across sibling findings for the same criterion — on `sound-board/index.html` six `<audio>` elements emit 6 identical candidates per criterion for `wcag22:1.2.1`, `1.2.3`, `1.2.5` (18 rows with 3 unique reason strings). The `reviewCandidates.prompts[criterionId].text` dedup already exists for top-level prompts but per-finding `reason` strings are not deduped by `(ruleId, criterionId, reason)` tuple. Fix: hoist the shared reason to `prompts[criterionId].genericReason` alongside existing `.text`, keep per-location `snippet`; OR cap sibling candidates of the same shape at `N=3` + emit `additionalSiblingCount: <rest>`. Doctrine-consistent precedent: the prompt-text dedup is the same move one level up.
- [x] **V1-HEADING-HIERARCHY-MARKDOWN-FIRES-DESPITE-HINT** `semantics/heading-hierarchy` fires "Document has no `<h1>`, first heading is `<h5>`" on every SSG Markdown file (29 findings, all admonition `<h5>`s being the only embedded-HTML headings) — even though `meta.analysisCoverage.hints` explicitly says "heading hierarchy [is] not checked" on Markdown-as-HTML-residue scans. One of the two surfaces is lying. Fix: actually skip the rule on Markdown-residue files (or any file where the parser saw embedded-HTML headings but the file's ATX `#`-headings were invisible), OR reason-enrich each finding with "partial-document-view: Markdown `#`-headings were not parsed; heading outline may be well-formed in the rendered output." Distinct from V1-HEADING-HIERARCHY-MARKDOWN-SELF-CONTRADICTION (the design-system README manifestation: ATX top-level heading); this item's manifestation is admonition-only headings in a file that has well-structured ATX headings upstream. Doctrine: self-reported coverage that contradicts what actually runs is dishonest.
- [x] **V1-CONFORMANCE-SCOPE-FILES-MINIFIED-LEAK** `conformance_statement.scope.files[]` lists files that `scan_project` labeled `reason: "minified"` in `scannedBuildArtifacts` — claiming coverage the scan did not produce. On the `scope.files` enumeration includes `rotating-nav-animation/index.html` and `sticky-navigation/index.html`, both in `meta.scannedBuildArtifacts` and both evaluated by zero rules. A procurement reviewer reads "156 files in scope" but two of them contributed nothing. Fix: either (a) omit build-artifact-labeled files from `scope.files[]` and surface them in `scope.skippedFiles: [{path, reason}]`, or (b) annotate each entry in `scope.files[]` with a per-file `evaluated: boolean` flag. Pairs with V1-CONFORMANCE-SCOPE-FILES-CAP. Doctrine: composite headline counts are dishonest — a file count that sums "actually evaluated" with "skipped as build artifact" under one "scope.filesCount" is the same bug at the procurement-surface layer.

Minification / build-artifact classification:

- [x] **V1-SCANNED-BUILD-ARTIFACTS-ENVELOPE-DRIFT** `meta.scannedBuildArtifacts` is documented (`src/mcp/build-artifacts.ts:593-666`) and typed as `BuildArtifactsGrouped { grouped, ungrouped, ungroupedTruncated? }` with `capMetaArray` applied to the ungrouped tail, but the on-wire response for `scan_project` on a template catalog ships **1168 flat `{path, reason}` entries** (~148 KB) — zero occurrences of `grouped`, `ungrouped`, `basename`, `suggestedGlob`, or `ungroupedTruncated` reach the wire. The grouper is called at `src/mcp/tool-scan-project.ts:1108-1113` but the serialization path between `groupBuildArtifactsByBasename` and the wire drops the envelope. This is the single largest source of response-bloat (responsible for `effectiveLimit: 1` truncations on every `scan_project` call at scale — body bodies are dropped so the flat SBA tail fits) and directly contradicts the Q-SHARED-META-ARRAY-BUDGET-CAP work. `metaMode: "delta"` cannot help either, because the delta baseline contains the same 148 KB payload. Fix: audit the serialization path between `groupBuildArtifactsByBasename` and the response root; post-fix grep the wire payload for `grouped` / `ungrouped` / `ungroupedTruncated` (must be present). Distinct from V1-MINIFIED-FILE-SCAN-KIND-SPLIT (per-kind counter) — this is the pre-existing grouped-envelope that isn't reaching the wire. Doctrine: the shape on the wire must match the type; advertising an envelope that flattens on emission is dishonest. (already landed via Q6-SCANNED-BUILD-ARTIFACTS-GROUP-BY-BASENAME; verified 2026-04-25 by /continue turn 8 audit — wire payload carries {grouped, ungrouped, ungroupedTruncated} envelope; no flat-array construction in code)
- [ ] **V1-BUILD-ARTIFACT-CLASSIFIER-BYPASS-HTML-FP** `classifyBuildArtifact` called directly on a template catalog' mis-classified authored HTML files (e.g. `above-educational-template/contact.html` — 213 lines, one line > 500 chars, median 27, ratio 0.5 %) returns `null` — so the classifier itself did NOT classify the file as `"minified"`. Yet the file reaches the wire labeled `reason: "minified"` (459 non-`.min.*` files on this corpus; top filenames `jquery.fancybox.pack.js ×64`, `jquery.js ×49`, `index.html ×40`, `html5shiv.js ×38`, `bootstrap.css ×33`, `contact.html ×18`, `custom-fonts.css ×18`). The label enters via a path that bypasses the per-file classifier. Fix: trace the path from `collectBuildArtifacts` / the `buildArtifactsFields` assembly down to every site that can stamp `reason: "minified"` on an entry, and confirm every stamp site routes through `classifyBuildArtifact`. Pairs with V1-BUILD-ARTIFACT-REGRESSION-AUDIT-MINIFIED, but distinct — that item investigates classifier predicate ordering; this item traces the non-classifier bypass. Doctrine: labels must be provable from the code; a label that says `minified` on a 213-line authored HTML file is dishonest.
- [x] **V1-OPAQUE-COMPONENT-NAMES-MINIFIED-TOKEN-LEAK** `opaqueCustomComponentNames` at scale contains `["AG.y", "B", "H.length", "J", "J.length", "Math.abs"]` — not React components but minified JS symbol fragments the tsx parser captured from minified bundles. An agent reading the list might add `Math.abs` to their `nativeWrappers` config. Fix: before emitting, filter the candidate list by `(a) contains "." (member-access path)` → drop; `(b) matches a well-known global prototype (Math, Object, Array, JSON, Reflect, Symbol, Proxy)` → drop; `(c) single-uppercase-letter with numeric suffix (A1, B2, etc.)` → drop. Same file set that triggers this is the `.js`-parsed-as-TSX shape covered by V1-JS-PARSE-ERROR-REASON-MISLEADING. Doctrine: labeled buckets are only honest when provable from the code; "PascalCase-ish token seen in a parsed file" is not provably a React component.

Catalog-scale perf / scoping:

- [ ] **V1-PATTERN-ID-DEAD-INFRASTRUCTURE** Commit `feba3df8` added `patternId` (cross-template fingerprint) to `Violation` with the engine stamping at `src/engine/rule-runner.ts:182` and `src/engine/stamp-project-emission.ts:47` — both gate on `emitted.snippet` being a non-empty string. Grep of `src/rules/` for `snippet:` returns **zero** hits — no in-tree rule populates the `snippet` field that the stamp predicate requires. Verified on a 40,132-finding at-scale scan: `patternId` occurrences in the response = 0. Particularly fatal at this scale: the three biggest-volume rules (`motion/pause-stop-hide`, `contrast/minimum`, `contrast/non-text` — ~43 % of findings combined) are CSS-declaration rules with no natural snippet source, so `patternId` can never reach them under the current design. Fix: decide — either (a) populate `emitted.snippet` on at least one snippet-emitting rule (HTML/JSX shape rules are the realistic candidates: `navigation/href-javascript-void`, `semantics/button-name`, `aria/expanded-on-disclosure`, `media/alt-text-missing`) so `patternId` is non-empty on at least one finding family; OR (b) remove the type field and delete the stamp sites — current state advertises an affordance that no finding carries. Pairs with Q6-PATTERN-FINGERPRINT-CROSS-TEMPLATE (open — proposes the cross-template fingerprint for CSS findings via a different hash path that does not depend on `snippet`). Doctrine: advertising an affordance that never reaches the wire is dishonest.
- [ ] **V1-CSS-CROSS-TEMPLATE-FINGERPRINT** CSS-declaration rules (`motion/pause-stop-hide` 8956, `contrast/minimum` 5458, `contrast/non-text` 2805) fire tens of thousands of times on a template catalog where 117 near-identical copies of `bootstrap.css` ship across 172 template dirs. `patternId` cannot fingerprint these (see V1-PATTERN-ID-DEAD-INFRASTRUCTURE — it gates on `snippet`) and `groupKey` operates per-file, so 117 identical `.img-thumbnail` transitions collapse to 117 distinct finding groups. Fix: add a CSS-rule-specific fingerprint — `cssPatternId = hash(ruleId + selectorFamily + propertyFamily + valueShape)` — that collapses "same selector + same declaration shape" across files. Likely reduces the 17k CSS-rule findings on this corpus to ~40 canonical patterns. Pairs with Q6-CONTRAST-VENDOR-CSS-CROSS-FILE-DEDUPE (open, same-basename) and Q6-PATTERN-FINGERPRINT-CROSS-TEMPLATE (open, HTML/JSX element-level). This item is the CSS-declaration sibling that neither covers. Doctrine: the agent has no honest way to say "this is one canonical a canonical CSS transition, 117 copies" without such an affordance.
- [ ] **V1-PROJECT-DIRECTORY-ROLLUP** is a mono-repo of 50 independent mini-projects under one root; `scan_project` treats it as one repo and merges findings. `meta.perRuleCoverage.findingsEmitted` tells per-rule but not per-project / per-directory. An agent triaging a mono-repo-of-demos or a website-template catalog benefits from `plan.topDirectories: [{path, violationCount, topRule}]` — rank-ordered to expose uneven distribution and let the agent pick the sub-tree that matters first. Additive extension of V1-CROSS-FILE-ROLLUP-PRIMITIVE (which proposes `plan.topRules`). Doctrine: one tool call should answer "what next?" — on a 50-project mono-repo, per-project scoping is the first "what next" question.
- [x] **V1-CHECKLIST-MAX-CANDIDATES-DEFAULT-LOWER** `checklist({limit: 5})` on a template catalog returns 84,879 chars — one criterion (`wcag22:1.3.2 Meaningful Sequence`) alone carries enough candidates to blow the MCP host token ceiling even with `perCriterionClipped: true`. The per-criterion clip fires but the default `maxCandidatesPerCriterion` is too high for bulk catalogs. Fix: lower the default `maxCandidatesPerCriterion` to ≤ 20 (from current default) AND emit a `maxCandidatesPerCriterionHint: N` that the caller can raise via the input param if they want a deeper cut. Pairs with V1-CHECKLIST-PERCRITERION-CURSOR — that item adds the cursor; this item tightens the default to match realistic host budgets. Doctrine: response-size-first defaults are load-bearing for usability on catalogs.

Rule correctness / detection (false positives + missing scope):

- [x] **V1-RULE-ICON-FONT-HIDDEN-TITLE-NOT-NAME** `aria/icon-font-hidden` accepts `title` as a confirmed accessible name (verified at `src/rules/aria/icon-font-hidden.ts:265-266` for HTML, `:291-292` for JSX). On a social-icon row `<a href="#" title="Facebook"><i class="fa fa-facebook"></i></a>` the rule emits "the parent `<a>` 'Facebook' already carries the accessible name" and `suggest_fix` recommends `aria-hidden="true"` on the `<i>` — applying that guidance regresses the link to no reliable accessible name (VoiceOver iOS does not announce `title`; WAI-ARIA APG warns against title-as-only-name). Pattern reproduces 5×/file across templates, hundreds of times at scale. Fix: drop `title` from `accessibleNameSource()`; promote the case to a sibling `link-name-relies-on-title` finding instead. Doctrine: surface-don't-suppress, but a fix-suggestion that teaches an anti-pattern is worse than silent miss because it propagates the regression.
- [x] **V1-RULE-HREF-EMPTY-NOT-FLAGGED** `navigation/href-javascript-void` short-circuits on `href=""` returning `null` (verified at `src/rules/navigation/href-javascript-void.ts:72`) with the comment "link-no-href's domain, not ours" — but `link-no-href` only fires on absent attribute, not on present-empty-string. Result: `<a href="">Forgot password?</a>` (atlanta signin.html:95 + 6 social-row anchors at :141-146) escapes both rules. WCAG 4.1.2: empty href reloads the page — same broken affordance as `href="#"`. Fix: extend the predicate to treat `href=""` like `href="#"`, OR widen `link-no-href` to include the empty-string case.
- [x] **V1-RULE-LINK-NO-HREF-MISSED-ON-ATTRIBUTE-ABSENT** `<a class="prev"><i class="fa fa-chevron-circle-left"></i></a>` on agile-agency `index.html:159` has NO `href` attribute (carousel control wired at runtime). `navigation/link-no-href` does not fire; `navigation/link-descriptive-text` also stays silent. The control is not keyboard-focusable AND has no accessible name. Fix: ensure `link-no-href` fires on absent-href anchors that have an interactive class signal (`btn`, `nav-link`, `prev`, `next`) OR a JS click handler in a sibling `<script>`. Pairs with V1-RULE-HREF-EMPTY-NOT-FLAGGED.
- [x] **V1-RULE-FORM-LABEL-ADJACENT-INPUT-WITHOUT-ID** `forms/label-adjacent-unassociated` fires on adjacent textarea+label pairs but skips `<label>Name</label><input type="text">` siblings where neither has `id`/`for`. On aerosky `contact.html:82-110` (5 form rows) it emits the textarea finding (line 106) and skips the 4 `<input>` rows above. Predicate likely requires the input to have an `id` so it can confirm the label *could* associate. Fix: also fire when both label has no `for` AND input has no `id` — the canonical "you forgot the for/id pair" template mistake. Highest-volume FN on this input set.
- [x] **V1-RULE-CONTRAST-MINIMUM-SCSS-ELIGIBILITY** `contrast/minimum` is gated on `.css`/`.html`/`.htm` only (verified at `src/rules/contrast/minimum.ts:91-93`). On an SSG docs-site scan, 18 `.scss` files exist with documented sub-AA color choices (`.shell .output { color: #888 }` on `#171717` ≈ 4.46:1; `.c { color: #818181 }` on `#212121` ≈ 4.5:1) — `contrast/minimum` perRuleCoverage shows `filesEvaluated:0`. Highest-leverage semantic miss across SSG ecosystems (Jekyll, Hugo, Middleman, Eleventy all ship SCSS source). Fix: add `.scss`, `.sass`, `.less` to `appliesTo.fileExtensions`. CSS variables and Sass interpolation can be deferred (`crossFileCapable: false` already does the honest-confidence signal). Same fix applies to `contrast/non-text`.
- [x] **V1-RULE-MOTION-PAUSE-STOP-HIDE-IGNORES-ITERATION-COUNT** `motion/pause-stop-hide` and the 2.3.1 cycles/s heuristic fire on every CSS `animation:` and `transition:` declaration regardless of `animation-iteration-count` or fill mode. On `animated-countdown/style.css:30` (`animation: hide 0.2s ease-out;` — default iteration-count 1, runs once) the rule fires AND the 2.3.1 candidate reads "~5.0 cycles/s" (mathematically wrong; one-shot animation cannot cycle). Same shape on `good-cheap-fast/style.css:54` (`forwards` fill mode). 55 of 222 corpus violations (25%) attributable. Fix: require `animation-iteration-count: infinite|N>3` OR `animation-duration > 5s` OR `transition-duration > 5s` before firing; encode iteration-count + duration as additive `reason` text.
- [x] **V1-MOTION-2.3.1-CYCLES-PER-SECOND-MATH** The flashing-content finder converts `animation-duration: 0.15s` to "~6.7 cycles/s" arithmetically without consulting `animation-iteration-count`. On `_docsearch.scss:133-137` (single 150ms entrance animation, no iteration-count) the candidate's reason text claims a 6.7Hz flash. Fix: when iteration-count is absent or `1`, drop the cycles/s number and switch reason to "single 150ms transition; flashing only if `iteration-count: infinite`." Distinct from V1-RULE-MOTION-PAUSE-STOP-HIDE-IGNORES-ITERATION-COUNT (firing-gate axis); this is the reason-text accuracy axis.
- [x] **V1-RULE-LANDMARK-MAIN-AND-HEADING-HIERARCHY-DEFER-ON-EMPTY-PAGE** Pages with NO headings AND no landmark elements silently pass `semantics/landmark-main` and `semantics/heading-hierarchy`. On `theme-clock/index.html` (no `<h1>`, no `<main>`) `scan_file` returns 0 findings; same for `kinetic-loader/`, `hoverboard/`, `random-image-generator/`. The empty-body case is a *stronger* 1.3.1 signal but lands as `findingsEmitted:0, coverageConfidence:high`. Fix: when a file has `<body>` but no headings AND no landmarks, fire both rules at the body root.
- [x] **V1-FINDER-3.3.4-SCOPE-NEEDS-FINANCIAL-LEGAL-GATE** The 3.3.4 review finder fires on `<form class="form subscribe-form">` newsletter signups across templates — but WCAG 3.3.4's normative scope is "legal commitments, financial transactions, modify/delete user data, test responses." Newsletter / contact / signin forms are explicitly outside scope. Fix: gate on specific class/id tokens (`payment`, `checkout`, `legal`, `terms`, `delete-account`, `transfer`) OR `action=` URL containing `/payment`/`/checkout`/`/legal`. Reason-text enrichment alone is insufficient — a finder that fires when the criterion's spec scope provably doesn't apply duplicates capability the agent has.
- [x] **V1-FINDER-1.3.2-ORDER-CSS-SELECTOR-DEFINITIONS** The 1.3.2 `order` CSS finder fires on selector *definitions* in vendor stylesheets (`bootstrap.css:734` `.order-first { order: -1; }` — vendor utility classes). Across templates corpus that's hundreds of identical fires per criterion. Fix: track whether the rule emitted on a vendor CSS file (path matches a vendor library banner; pairs with V1-VENDOR-LIBRARY-BANNER-DETECTION) — when so, downgrade or drop. The rule should fire on *applications* (HTML element with `class="order-N"`), not on selector definitions in vendor CSS.
- [x] **V1-FINDER-1.4.1-COLOR-EMPTY-BODY-FALSE-POSITIVE** The 1.4.1 status-color finder fires on `<p class="help-block text-danger"></p>` (empty body — validation-message placeholder). Color cannot be the sole signal of *nothing*. On templates corpus 10/10 sampled candidates were empty-body. Fix: gate the finder on the element having at least some non-whitespace text content OR non-presentational child element. Pairs with V1-FINDER-1.4.1-COLOR-READ-ELEMENT-BODY (next item).
- [x] **V1-FINDER-1.4.1-COLOR-READ-ELEMENT-BODY** The 1.4.1 status-color finder reads the `class` attribute but does not recurse into element body to check for visible text. Multiple cases where visible text IS inside the colored element (`<span class="badge bg-success">New</span>` cheatsheet:681; `<p class="text-warning">No grid classes…</p>` grid:85; `<small class="...text-success...">Added in v{version}</small>` AddedIn.astro:13) — reason claims "no visible text" while the visible text is the element's text node child. Selector logic must extend to element body before composing reason text. Distinct from V1-FINDER-1.4.1-COLOR-EMPTY-BODY-FALSE-POSITIVE.
- [x] **V1-FINDER-2.3.1-RAF-NEEDS-LUMINANCE-EVIDENCE** The 2.3.1 flashing finder fires on bare `requestAnimationFrame` calls in vendor scroll/animate libraries (`wow.js`, `wow.min.js`, `headroom.min.js`, `scrolltofixed.js`). 8/10 candidates on templates corpus were these. WCAG 2.3.1 is about color/luminance cycling >3 flashes/s; bare `requestAnimationFrame` is not evidence of that. Fix: require evidence of color/luminance cycling (animated `background-color`, `opacity`, `filter`) before firing.
- [x] **V1-FINDER-2.2.1-SETTIMEOUT-VENDOR-FILE-GATE** The 2.2.1 timing finder fires on every `setTimeout`/`setInterval` regardless of whether the call site is in a vendor file. On templates 10/10 sampled candidates cite `bootstrap.js`/`jquery-1.10.2.js` library internals. The finder also fires when the duration is a member-access (`self.options.interval`) — by definition not a static literal an agent can verify. Fix: when the finding sits in a file matching a vendor banner, downgrade to `info` and tag `inVendorFile: true`; when the duration is a member-access reference, encode that as additive reason text rather than firing high-confidence.
- [x] **V1-FINDER-1.4.5-IMAGES-OF-TEXT-MARKDOWN-ADJACENT-LINK-FP** The 1.4.5 "images-of-text" finder's "img short alt repeated in immediate sibling text node" predicate matches the *next markdown line* with a different sponsor link. On `README.markdown:58-67,78` 11 candidates fire on `[![Sponsor N]]` rows where the "sibling text" is the next sponsor's distinct markdown link, not duplicated alt. Fix: in markdown contexts, the "sibling text node" must be plain prose, not another link/image; or rewrite reason text to "next adjacent markdown link has similar alt." Pairs with V1-FINDER-1.4.5-PHOTO-WITH-LABEL.
- [x] **V1-FINDER-1.4.5-PHOTO-WITH-LABEL** The 1.4.5 finder fires when `<img alt="X">` is paired with adjacent `<p>X</p>` (`insect-catch-game/index.html:21,27,36,45` — 4 candidates each `<img alt="fly"><p>Fly</p>` inside `<button class="choose-insect-btn">`). 1.4.5 is about TEXT RENDERED AS PIXELS, not a photo with an HTML label. The duplicate-alt+label is at most a 1.1.1 decorative-vs-informative question. Fix: drop these candidates OR rephrase reason as "alt could be `''` if the image is decorative; otherwise verify the alt isn't pixel-text."
- [x] **V1-FINDER-1.3.3-SENSORY-FIXTURE-PROSE** The 1.3.3 sensory-characteristics finder substring-matches on common English words ("fields below", "right side", "button below", "see below") in test fixtures and demo prose. On `alert.html:13` flags `Holy guacamole! You should check in on some of those fields below.` — demo prose in an alert test fixture; no actual form fields below. On an SSG `pagination.md:134` flags "See below" in admonition prose. Fix: enrich reason with surrounding 1-2 sentences so the dismiss is mechanical; downgrade confidence in documented prose containers (`.md`/`.mdx` body). Also fix the modal.mdx:78 off-by-5-line citation.
- [x] **V1-FINDER-2.4.5-MULTIPLE-WAYS-REQUIRE-BODY** The 2.4.5 "Multiple Ways" finder treats every HTML root as a candidate site root. On `_includes/top.html:2` (a 19-line `<head>`-only partial) it fires "Likely root layout has no search/sitemap/breadcrumb/3-link nav" — irrelevant; the composed page has 2 navs + search + meta-links. On a vanilla-JS `3d-boxes-background/index.html` it fires on a single-page CSS-trick demo. On a design-system `js/tests/integration/index.html:2` it fires on a 67-line single-page test fixture. Fix: require evidence of `<body>` AND ≥1 anchor/nav before firing; for files in `_includes/`/`_partials/`/`_components/`, downgrade reason to "likely a fragment composed into a parent layout — verify multi-way nav lives in the parent."
- [x] **V1-RULE-BUTTON-NAME-FA-ICON-ONLY-STATIC** `semantics/button-name` does not fire on static `<button>` whose only children are `<i class="fa-…">` glyphs without `aria-label`/`aria-labelledby`. `faq-collapse/index.html:22-25` has 5 `<button class="faq-toggle"><i class="fas fa-chevron-down"></i><i class="fas fa-times"></i></button>` instances — `scan_file` returns 0 button-name findings. Fix: extend the selector to consider element's static children — when every child is presentational (`<i>`/`<span aria-hidden>` with no text), fire.
- [x] **V1-RULE-EXPANDED-ON-DISCLOSURE-FIRES-ABSENT-ATTRIBUTE** `aria/expanded-on-disclosure` requires the attribute to be PRESENT (often with a wrong value) before flagging — does not fire on disclosure-shaped widgets that LACK the attribute entirely. `theme-clock/index.html:12` (`<button class="toggle">Dark mode</button>`), `faq-collapse` (5 disclosure buttons), `mobile-tab-navigation` (active tab `<li>`s) all silently pass. Fix: when an element looks like a disclosure widget (toggles a `.collapse`/`.active`/`.show` class via JS click handler), fire even when `aria-expanded` is absent.
- [x] **V1-RULE-LANG-ZXX-WITH-PROSE** `parsing/html-has-lang` only checks attribute *presence*, not validity-against-content. On `js/tests/visual/floating-label.html:2` a file declares `<html lang="zxx">` (BCP 47 "no linguistic content") but the page has lots of English UI ("Email address", "Open this select menu", "Lorem ipsum"). Fix: warn when `lang` is `zxx`/`und`/`mul`/`mis` AND the document has visible text content.
- [x] **V1-RULE-INVALID-ID-NON-ASCII** `parsing/invalid-id-shape` rule scope appears limited to empty/whitespace IDs only — does not flag non-ASCII / accented characters. On `js/tests/visual/scrollspy.html:32, 86` there's `id="présentation"` and `href="#présentation"` — `e` vs `é` mismatches break URL fragment routing in older browsers and many AT shortcuts. Fix: extend to flag ids containing characters outside `[A-Za-z0-9_-]`.
- [x] **V1-RULE-LINK-DESCRIPTIVE-TEXT-MISSES-GENERIC-PRONOUNS** `navigation/link-descriptive-text` treats text nodes "This link" / "that link" as long enough to pass. WCAG 2.4.4's anti-pattern is the *semantic* generic-pronoun pattern, not literal length. On `js/tests/visual/modal.html:57` two `<a href="#" data-bs-toggle="tooltip">` inline links labelled "This link" and "that link" stay silent. Fix: extend the rule to flag text matching `/^(this|that|the|here|click here|read more|more|learn more|info|details)$/i`. Pairs with templates pass 2 FN-T6 / FN-T14 (button text "Go!"/"Submit"/"OK"/"Click here").
- [x] **V1-RULE-REDUNDANT-ROLE-ON-HOST-ELEMENT** No rule flags `role="form"` on `<form>`, `role="navigation"` on `<nav>`, `role="main"` on `<main>`, `role="button"` on `<button>`, `role="img"` on `<img>` — all canonical "redundant role" antipatterns. On templates corpus `<form role="form">` appears in 3+ files. Pattern is short, stable, ubiquitous. Add as `aria/redundant-role-on-host-element`. WCAG 4.1.2.
- [x] **V1-RULE-FORM-SUBMIT-AS-ANCHOR** No rule flags `<form>` containing an `<a class="btn">` as the apparent submit control with no `<button type="submit">`/`<input type="submit">`. Templates `sb-admin-2/login.html:59` literally has the HTML comment "Change this to a button or input when using this as a form" above a `<a href="index.html" class="btn btn-lg btn-success btn-block">Login</a>`. Add as `forms/submit-not-button-or-input`. WCAG 4.1.2 + 3.3.2.
- [x] **V1-RULE-REQUIRED-MARKER-WITHOUT-REQUIRED-ATTR** `<label>Username/Email <span class="text-danger">*</span></label>` paired with `<input type="text" class="form-control">` (no `required`, no `aria-required`). The color-only `*` IS flagged (`color/meaning-by-color-only`), but the input itself is not flagged. Even an SR user hearing "Username/Email star" cannot programmatically discover the field is required. Add as `forms/required-marker-without-required-attr`. WCAG 1.3.1 + 3.3.2.
- [x] **V1-FINDER-DECORATIVE-IMG-ADJACENT-MEANING-WIDEN** `decorative-img-with-adjacent-meaning` finder exists but does not catch the most common a template hero-slider pattern: `<img src="img/slides/N.jpg" alt="" />` immediately wrapped by visible caption blocks (`<strong>Online Education</strong><p>The best educational template</p>`). The image is the *content* of the slide; marking it decorative is wrong. Templates an educational template `index.html:56-78`, agile-agency `index.html:67-83,102-123,148,192-213`. Fix: lower the gate — fire when `<img alt="">` sits in a `<li>`/`<figure>`/`<div class*="slide">`/`<div class*="card">` whose siblings include `<h1..h6>` or `<p>` with non-trivial text.
- [x] **V1-RULE-ALT-TEXT-PLACEHOLDER-ROLE-AS-ALT** `media/alt-text-placeholder` (per V1-SUGGEST-FIX-ALT-DIMENSIONS-ANTIPATTERN) detects dimension-as-alt (`alt="700x400"`). Extend to detect role-as-alt antipattern: `alt="logo"|"image"|"picture"|"photo"|"icon"|"image of …"`. Templates above-educational `index.html:35` `<img src="img/logo.png" alt="logo"/>`; aerosky `contact.html:37` `<img alt="logo">`.
- [x] **V1-RULE-DUPLICATE-LANDMARK-UNLABELED-COMPOSITION-GUARD** `semantics/duplicate-landmark-unlabeled` fires at `error` on a SINGLE unlabeled `<form>` because "When composed into a layout, will likely sit alongside another `<form>`" — the composition is unobservable from the file the scanner has, so the predicate is a heuristic dressed up as a deterministic finding. Doctrine ("Heuristic emission is the symmetric twin of heuristic suppression," added 2026-04-25): leak-through severity is the silent-miss in the opposite direction. Fix: gate the emission on confirmed sibling evidence in-file (≥2 unlabeled forms, or ≥2 unlabeled `<nav>`s) — when only one unlabeled instance is observable, move the predicate to a review-candidate finder so `reason` text frames the question "is another form present at runtime?" instead of asserting it. WCAG 1.3.1 / 4.1.2.
- [x] **V1-RULE-COLOR-MEANING-BY-COLOR-ONLY-REASON-CONCEDES** `color/meaning-by-color-only` emits at `error` on `<button class="btn-danger">Danger</button>` while the rule's own `reason` text concedes the visible word "Danger" is present — i.e. the meaning is NOT color-only. Doctrine ("Reason text and severity must agree," added 2026-04-25): a reason that contradicts its own severity is a dishonest shape; the agent budgets against severity, reads the reason, discovers the budget was wasted, and learns to mistrust the rule on cases where it IS right. Closure paths (pick one): (a) suppress emission on the conceding branch (when accessible-name overlaps the color-conveyed token); (b) downgrade the conceding case to a review candidate; (c) rewrite the reason to surface what's *actually* still in question (e.g. "color may still be the *sole* cue for users who can't read the keyword"). WCAG 1.4.1.

Review-finder noise / dedup:

- [x] **V1-CHECKLIST-PRIORITY-AXIS-DEGENERATE** `summary.byPriority` ships `{high: N, medium: 0, low: 0}` on every corpus tested — every actionable criterion lands in `high` because `priorityFor()` (verified at `src/mcp/tool-checklist.ts:144-148`) returns `high` for every A/AA criterion that has candidates. Per-candidate `confidence` IS active (varies high/medium/low). Fix: derive criterion-level priority from the *distribution* of underlying candidate confidences (using `mode` or `median`, not max), OR drop the field. The signal-bearing axis is `confidence`; the criterion-level `priority` field is redundant. **Fourth-pass repeat**. Doctrine: labeled buckets that don't differentiate are buckets not earning their keep.
- [ ] **V1-CHECKLIST-CRITERION-CANDIDATE-DEDUPE-ACROSS-IDS** Same `(file, line, reason)` candidate fires under multiple criterion IDs with byte-identical reason text. On `ratio.mdx:14` cited 3× under `wcag22:1.2.1`/`1.2.3`/`1.2.5`; sound-board `<audio>` rows fire 6 elements × 3 criteria = 18 candidates with 3 unique reason strings; template-catalog `fancybox.pack.js:4` cited 74× × 3 criteria = 222 likelyIrrelevant rows. Fix: when a candidate's `(ruleId, snippet, reason)` tuple is identical across N criterion IDs, fold to one candidate with `criterionIds: [...]` array; `verdict_candidate` then applies one verdict to all listed criteria. Pairs with V1-REVIEW-CANDIDATES-SIBLING-REASON-DEDUP (per-criterion sibling axis); this is the cross-criterion same-candidate axis.
- [x] **V1-CHECKLIST-MAX-CANDIDATES-PER-CRITERION-CLAMP** `checklist({maxCandidatesPerCriterion: 20})` returned max 10 per criterion on templates corpus — caller-supplied value silently clamped to 10 with no narration. Either honor downstream and narrate the clamp, or document/remove. Pairs with V1-CHECKLIST-LIMIT-NEGATIVE-VALIDATION. Doctrine: silent coercion on input masks caller bugs.
- [x] **V1-CHECKLIST-VENDOR-FILE-FINGERPRINT-COLLAPSE** Three different copies of `jquery.flexslider.js` in three template directories all emit identical 2.5.1 candidates with identical reason text. Same library = same SHA = same finding × 3. Recent commit `feba3df8` added cross-template fingerprinting for CSS via `vendorOccurrences`; JS vendor dupes still ride the wire raw. Fix: extend the pattern to JS vendor files (`jquery.*`, `bootstrap.js`, `wow.*`, `headroom.*`, `fancybox*`, `flexslider*`) — collapse to one candidate with `occurrences: [{path, line}]`. Pairs with Q6-CONTRAST-VENDOR-CSS-CROSS-FILE-DEDUPE (closed for CSS) and V1-CSS-CROSS-TEMPLATE-FINGERPRINT — the JS-vendor sibling.

Response shape / cross-surface drift / honesty:

- [ ] **V1-PERRULE-COVERAGE-CONFIDENCE-PARSE-ERROR-CORPUS** At corpus scope on templates (4043 files, 501 parse-error files, 12.4% parse-error rate), `perRuleCoverage` reports `coverageConfidence: "high"` for **71/71 rules** — zero rules report `low` or `medium` despite 12% of files crashing the tsx parser. The single-file `scan_file` response *does* report `low` honestly (8/71 rules). The aggregation step turns "no .css scanned" into "0 .css files" but still flips `coverageConfidence` to `high` because the project-wide directory contained 8 .css files. Fix: a rule whose `filesEligible` includes ≥10% parse-errored or partial-parsed files should flip to `medium`; ≥25% to `low`. Distinct from V1-PERRULE-COVERAGE-HONESTY-ON-PARSE-ERRORS (per-file axis); this is the corpus-aggregation axis.
- [x] **V1-FIX-DESCRIPTION-INLINE-VS-REF-PER-FINDING-SHAPE-DRIFT** Within a single `scan_project` response, `fix` shape varies per-finding: rule A ships `fix: {safety, description, fixDescriptionRef}`; rule B ships `fix: {safety}` + sibling `fixDescriptionRef`; rule C ships `fix: {safety, description}` (inline only). On `news_item.html` line-3 vs line-17 of the same `navigation/link-descriptive-text` rule: the description is inlined when the rule fires once, ref-only when it fires multiple times (dedup optimization). Cross-tool: the same `findingId: "49f541ade48b"` (alert.html:9 semantics/landmark-main) ships `fix.description` inline on `scan_file` and ref-only on `scan_project` — agent toolchain pivoting between surfaces hits `undefined` reads. Fix: declare and enforce ONE shape — either always inline, OR always wrap as `fix.descriptionRef?: {hash}` *inside* `fix` (never as a sibling). Source-verified at `src/output/agent-response/types.ts:163` — types document both forms but the response-builder doesn't normalize.
- [x] **V1-CONFORMANCE-SCOPE-FILES-ARRAY-UNCAPPED** `conformance_statement.scope.files: readonly string[]` ships every scanned file path inline (verified at `src/reports/conformance.ts:175 + :834`). On templates corpus that's 4043 paths × ~90 chars = 360 KB; total response 421,628 chars (16.8× over MCP token ceiling). Most agent-impactful failure since this is the *publishable* artifact. Fix: ship `scope.fileCount: 4043, scope.root: "/tmp/..."` plus an opt-in `verboseMeta: true` knob that re-includes the file list. Conformance-statement is unusable on any project >400 files today. Distinct from V1-CONFORMANCE-SCOPE-FILES-MINIFIED-LEAK; this is the unbounded-array axis.
- [x] **V1-COVERAGE-PARSE-ERROR-FILES-UNCAPPED** `coverage` ships `analysisCoverage.parseErrorFiles` with all entries inline (501 entries × ~250 chars = ~125 KB) — single largest source of bulk in `coverage` responses. Fix: emit `parseErrorFileCount: 501` plus `topReasons: [{reason, count}]` rollup; behind `verboseMeta: true` re-include the full path list. Same shape applies to `partialParseFiles` (201 entries on templates). Pairs with V1-SCANNED-BUILD-ARTIFACTS-ENVELOPE-DRIFT (sibling array-uncapped axis on `scan_project`). Doctrine: any internal array > N entries must ship as `{topN, totalCount, truncated: true}`.
- [x] **V1-COVERAGE-META-BLOCK-MISSING** `coverage` returns no `meta` block at all — agents cannot verify it scanned the same file set as a prior `scan_project` call (no `filesScanned` echo, no `configSource`, no `rootSource`, no `warnings`, no `rulesEvaluated`). For a tool whose entire job is "did the scan really cover this?" this is structurally inverted. Fix: every tool that runs the scanner should ship the same `meta` shape. Pairs with V1-WARNINGS-DETAILS-CROSS-SURFACE-REGRESSION.
- [x] **V1-CROSS-SURFACE-WARNINGS-CODE-SET-DRIFT** Same scan state surfaces different warning codes per tool: `scan_project.warnings = [no_config_found, scanned_build_artifacts_present, extensions_skipped_no_parser, parse_errors_present, response_token_budget_truncated]` (5 codes); `coverage.warnings = [extensions_skipped_no_parser, parse_errors_present]` (2 codes); `scan_file.warnings = [no_config_found]` (1 code). `coverage` is missing `no_config_found` and `scanned_build_artifacts_present`; `scan_file` is missing `parse_errors_present` even when its own `analysisCoverage.parseErrorFileCount > 0`. Each tool emits a different subset; agents that wire warnings into routing logic miss the prompt unless they call `scan_project` first. Fix: shared meta-block populated by every tool that invokes the scanner.
- [ ] **V1-FINDINGS-SNIPPET-FIELD-OMITTED-ON-SCAN-SURFACES** `scan_project` and `scan_file` `findings[*]` lack `snippet` field; `checklist.items[*].candidates[*]` includes it. The asymmetry forces extra Read calls per finding and inflates `fix.description` prose. Source check: `src/output/agent-response/types.ts:127` declares `snippet?: string` (present-when-meaningful); the rule layer doesn't populate it for scan-side findings. Adding 1-3 lines per finding would (a) collapse the dishonest "is this fix mechanical?" question for cases like motion/pause-stop-hide, and (b) reduce per-finding `fix.description` prose. Pairs with V1-PATTERN-ID-DEAD-INFRASTRUCTURE which gates `patternId` on `emitted.snippet` being non-empty — populating snippets unblocks both.
- [ ] **V1-SUGGEST-FIX-SCHEMA-PRIMARY-ALTERNATIVES** The `suggest_fix` tool description promises "guidance with a ranked primary fix and alternatives" but `kind: "guidance"` ships a single `explanation` string with alternatives concatenated as prose ("`<label for>` ... or `aria-label` ... or `aria-labelledby`"). Schema-vs-reality drift. Fix: structure `kind: "guidance"` as `{primary: {description}, alternatives: [{description, rationale?}]}` so the agent can rank fixes without prose parsing. Also add missing fields: `nextStep`, `findingId` echo.
- [ ] **V1-SUGGEST-FIX-KIND-SUPPRESS-RECOMMENDED** `suggest_fix` on `semantics/heading-hierarchy` for a `_includes/` partial returns `kind: "guidance"` with explanation that primarily describes a *suppression* (pragma) rather than a fix. The guidance/edit/none discriminator is doing double duty as "structural recommendation" and "I give up — please add a pragma." Splitting `kind: "suppress-recommended"` from `kind: "guidance"` would make the agent's branching honest.
- [x] **V1-SESSION-CONFIGURE-ECHO-STATE** `sessionConfigure({rules: {"motion/pause-stop-hide": "warning"}})` returns `{active: {ruleCount: 74, allowWrite: true}}` — does not echo what was set. No `rules: {[ruleId]: severity}` map, no `nativeWrappers` array, no `cwd` echo. Agent typing a wrong rule ID gets silent no-op. `sessionConfigure.allowWrite: true` is the active default contradicting the schema doc ("Defaults to false") — needs investigation. Fix: echo the merged session config back; surface `allowWrite: true` as a warning when it flips on.
- [x] **V1-VPAT-NO-PASSING-CRITERIA-WARNING** `vpat` on a corpus with `summary.supports: 0` (every criterion fails or is "Partially Supports" or "Not Evaluated") ships as a publishable artifact with no warning. An empty `productName` triggers `product_metadata_placeholders_in_use`; an all-fail VPAT silently ships. Fix: add structured warning code `vpat_no_passing_criteria` when `summary.supports === 0`.
- [x] **V1-VPAT-PARTIALLY-SUPPORTS-VS-NOT-EVALUATED** `vpat` reports `Partially Supports` for SCs with **0 findings** AND no manual-review evidence (1.3.4 Orientation, 1.4.4 Resize Text, 1.4.6, 2.1.4, 2.3.3, 2.5.2, 4.1.3) — that's prose for "we don't know" but reads to a procurement officer as "we tested and some parts work." Fix: when `(automatedFindings === 0 && manualEvidence === 0 && attestations === 0)`, emit `Not Evaluated`, not `Partially Supports`. Pairs with V1-CONFORMANCE-LIMITATIONS-FIELD-MISSING.)
- [x] **V1-LIST-RULES-SATISFIES-INCONSISTENT-ACROSS-FAMILY** Adjacent same-family rules declare different `satisfies` arrays: `semantics/table-caption-missing` lists all 4 standards (`wcag22:1.3.1`, `wcag21:1.3.1`, `section508:1.3.1`, `en301549:9.1.3.1`); `semantics/table-headers` lists only 2 (wcag22 + wcag21); `semantics/table-th-scope-missing` lists all 4. Same family, three different shapes. Either equivalence resolution is broken in the registry, or `satisfies` is hand-curated and inconsistent. Fix: `list_rules` should display the *resolved* equivalentTo set. Audit every rule's `satisfies` against the equivalentTo index.
- [x] **V1-EXPLAIN-RULE-KNOWN-FALSE-POSITIVES-FIELD** `explain_rule` does not surface known FP/FN patterns. `motion/pause-stop-hide` describes itself as discerning between `infinite` and one-shot animations — but the implementation fires on every CSS `animation:` regardless. `aria/icon-font-hidden` describes itself as covering both labeled-button and unlabeled-button cases; the implementation defers when ancestor has no name. `keyboard/handler-missing` describes scope as "the data-bs-* idiom data-bs-toggle…" but fires on container `data-bs-toggle="buttons"`. Add `knownFalsePositives: string[]` / `knownLimitations: string[]` arrays so agents can triage faster. Doctrine: `explain_rule` lying about behavior mis-calibrates the agent's dismissal logic.
- [ ] **V1-DETECTED-FRAMEWORK-CONFIDENCE-AND-INHERITANCE** `meta.detectedFramework: {name: "jekyll", buildOutput: "_site/", buildCommand: "bundle exec jekyll build"}` fires on a single sentinel `_config.yml` at the corpus root with no `Gemfile`/`_layouts/`/`_includes/` corroboration. On templates (174 independent un-built static-template snapshots) the advice is wrong — `_site/` would be near-empty. Worse: scanning a sub-template directory inherits `detectedFramework: jekyll` even when the sub-tree has no `_config.yml` (the detector walked up to an ancestor). Fix: add `detectedFramework.confidence: "high" | "medium" | "low"` derived from corroborating evidence count; on sub-scope scans, only inherit the parent's framework label when the sub-tree has its own corroborating evidence.
- [ ] **V1-OPAQUE-CUSTOM-COMPONENT-NAMES-FILTER-MEMBER-ACCESS** `opaqueCustomComponentNames` ships `["AG.y", "B", "H.length", "J", "J.length", "Math.abs"]` on templates — minified-JS symbol fragments captured by tsx parser, not React components. `detect_native_wrappers.emptyReason: "no-jsx-onclick-candidates-found-but-opaque-components-present"` cites the same hallucinated names — `propose_config` does too. **Three downstream tools amplify the same upstream bug.** Fix: before emitting, filter the candidate list by (a) contains `.` (member-access path) → drop; (b) matches a well-known global prototype (`Math`, `Object`, `Array`, `JSON`, `Reflect`, `Symbol`, `Proxy`, `Promise`) → drop; (c) single-uppercase-letter with numeric suffix (`A1`, `B2`) → drop; (d) basename matches a vendor library banner → drop. Pairs with V1-OPAQUE-COMPONENT-NAMES-MINIFIED-TOKEN-LEAK — this names the deterministic filters.
- [ ] **V1-PROPOSE-CONFIG-EXCLUDES-DOWNSTREAM-AMPLIFICATION** `propose_config` writes blanket `exclude: ["docs/**"]` on (the only directory with content), `exclude: ["js/**", "site/**"]` on (the source trees), and `exclude` lists 167 template directories at scale (95% of repo). Pasting any of these silences ~99% of real signal. Root cause: the heuristic gates on directory-name patterns or on `scannedBuildArtifacts` mis-classifications and propagates them into config. Fix: `propose_config` should never exclude a directory that contains files with active findings — gate on "does this dir have grounded findings? if yes, do not exclude." Pairs with V1-BUILD-ARTIFACT-CLASSIFIER-BYPASS-HTML-FP and V1-BOOTSTRAP-SUGGESTED-CONFIG-EXCLUDE-LOUD — both upstream amplifiers. Doctrine: surface-don't-suppress, applied at the generator step.
- [ ] **V1-PROPOSE-CONFIG-EXCLUDES-RATIONALE** `propose_config` ships an exclude list with no per-entry rationale. Agent cannot tell which entries came from `scannedBuildArtifacts` mis-classification vs. `node_modules`-style definitional vs. ad-hoc heuristic. Add `excludesReason: [{glob, reason: "labelled minified by scan_project" | "definitional" | "heuristic"}]` per entry so the agent can audit before pasting. Pairs with V1-PROPOSE-CONFIG-EXCLUDES-DOWNSTREAM-AMPLIFICATION (the gate).
- [ ] **V1-DEFAULT-EXCLUDED-PATTERNS-TEST-STORY-DROP** `DEFAULT_EXCLUDED_PATTERNS` (verified at `src/input/discover.ts:60-70`) hard-codes `**/*.test.*`, `**/*.spec.*`, `**/*.stories.*`, `**/*.story.*`, `**/__tests__/**`, `**/stories/**`, `**/dev-tools/**`, `**/devtools/**` — the header comment literally says "scanning them produces noise (onChange on filter bars, render assertions, sample copy that happens to contain 'click below')." That's verbatim "human-attention suppression," which the AI-first doctrine rules out. A test file that uses `<button>` without an accessible name CAN be a real a11y bug — fixture code gets copy-pasted into production routinely. Drop: `**/*.test.*`, `**/*.spec.*`, `**/__tests__/**`, `**/*.stories.*`, `**/*.story.*`, `**/stories/**`, `**/dev-tools/**`, `**/devtools/**`. Keep: `**/__mocks__/**`. The `includeTests` opt-out escape hatch is the right shape; the default is wrong per doctrine.
- [ ] **V1-VERDICT-CANDIDATE-CANDIDATE-ID-PASSTHROUGH** `verdict_candidate` requires the caller to hand-build `{candidate: {criterionId, location, reason, snippet, confidence}, reviewPrompt}` from prior `checklist`/`review_candidates` responses (6 keys re-stitched). No `candidateId` link, no batch entry. At scale (5003 candidates on templates) one round-trip per candidate is unfeasible. Fix: add `candidateId` shortcut (`verdict_candidate({candidateId, reviewPrompt})`) that back-loads the snippet from the original call. Pairs with V1-CHECKLIST-ITEM-REVIEW-PROMPT-FIELD.
- [ ] **V1-CHECKLIST-ITEM-REVIEW-PROMPT-FIELD** `checklist.items[*]` does not carry `reviewPrompt` — but `verdict_candidate` requires it. The natural workflow is `checklist → verdict_candidate`, but the prompt-text lives only on `review_candidates.prompts[criterionId].text`. An agent has to call `review_candidates` separately or hand-author the prompt. Fix: include `reviewPrompt` per criterion in `checklist.items[]` so the workflow is one tool call shorter.
- [ ] **V1-FINDING-CATEGORY-VS-FIXCLASS-CONTRADICTION** Same finding ships `fixClass: "runtime-only"` AND `category: "auto-fix"` AND `effort: "trivial"`. On `theme-clock/style.css:13` motion/pause-stop-hide finding — `runtime-only` per `fixesByClass` means "the scanner can't ground a deterministic fix here" but `category: auto-fix` reads as "apply_fix can do this." Same finding has `fix.safety: "safe"` and a `fix.description` that DOES specify the exact CSS rewrite. Fix: one of the two labels must be retracted. Doctrine: ambiguous field shapes are dishonest.
- [x] **V1-PARTIAL-PARSE-FILES-WARNING-CODE** `analysisCoverage.partialParseFileCount` (13 on baseline, 201 on templates) does not trigger a warning code. `parse_errors_present` fires on total-failure files; partial parses get no warning. Fix: add `partial_parse_files_present` warning code (or fold into `parse_errors_present` with a `partialCount` payload in `warningsDetails`).
- [x] **V1-MISSING-WARNING-CWD-APPEARS-MISROOTED** When `scan_project` returns `filesScanned === 0` AND `warnings: ["scanned_zero_files"]`, no companion warning suggests "did you mean a parent dir?" or echoes a `nearestNonEmptyParent` path. Add `cwd_appears_misrooted` warning emitted when `filesScanned === 0` AND a sibling/parent dir would have produced findings (cheap walk-up check). Also detect when `subdir_scan && parent_has_config && walked_up: false`.
- [x] **V1-MISSING-WARNING-DIST-ONLY-SCAN** When `scannedBuildArtifacts.length === filesScanned` AND every file is dist-tagged (the entire scan is build-artifact-only), no headline signal escalates. A design-system dist-dir scan returned `1244 violations` against pure build artifacts with no warning. The existing `scanned_build_artifacts_present` is a boolean. Fix: add `dist_only_scan_detected` warning when `scannedBuildArtifacts.length === filesScanned && filesScanned > 0`. Doctrine analogue of `scanned_zero_files`.
- [ ] **V1-MISSING-WARNING-COVERAGE-CONFIDENCE-INCONSISTENT** When `parseErrorFileCount > 0` AND every rule reports `coverageConfidence: "high"`, the meta is internally inconsistent. Add structured warning `coverage_confidence_uniformly_high_with_parse_errors`. Tied to V1-PERRULE-COVERAGE-CONFIDENCE-PARSE-ERROR-CORPUS.

Tool-orchestration / new tool surfaces:

- [x] **V1-TOOL-VERBOSE-META-INVERTED-DEFAULT** Bulk-shipping tools (`coverage`, `conformance_statement`, `scan_project`, `scan_file` on dense HTML) all blow the MCP token ceiling at scale. Root cause shared: per-call telemetry blocks (`scope.files`, `parseErrorFiles`, `scannedBuildArtifacts`, `referenceGuide`, full `perRuleCoverage`) ship inline by default. `scan_diff` already has a `verboseMeta: true` knob. Fix: invert the default — `verboseMeta: false` strips per-rule coverage, file lists, reference-guide tables; adopt across all bulk-shippers. The lean default fits comfortably under 30 KB; the verbose mode is opt-in for triage. Pairs with V1-CONFORMANCE-SCOPE-FILES-ARRAY-UNCAPPED + V1-COVERAGE-PARSE-ERROR-FILES-UNCAPPED + V1-SCANNED-BUILD-ARTIFACTS-ENVELOPE-DRIFT — those items fix specific arrays; this is the structural default-inversion.
- [ ] **V1-TOOL-SCAN-PROJECT-SUMMARY-MODE** Add `scan_project({summaryOnly: true})` that returns `plan + meta + topRules: [{ruleId, count}] + topFiles: [{path, count}] + filesByExtension + plan.summary` and *omits* per-file `files` array entirely. Fits under 20 KB. First-call ergonomics on bulk catalogs (templates 4043 files / 40k findings). Pairs with V1-RESPONSE-SIZE-PRE-ESTIMATOR.
- [ ] **V1-TOOL-FINDING-BY-ID** No tool retrieves a finding by `findingId` after the scan. `suggest_fix` requires `ruleId + file + line` — but `scan_project` returns `findingId: "b0d1b34c01d1"` opaque IDs. Add `get_finding({findingId})`.
- [ ] **V1-TOOL-FINDINGS-BY-RULE** `scan_project` paginates by file; an agent triaging "all 169 `aria/expanded-on-disclosure` findings" must page 110 files. Add `findings_by_rule({ruleId, cwd})` that returns just the findings for one rule across the project. Replaces 22 paginated calls with one. Pairs with V1-CROSS-FILE-ROLLUP-PRIMITIVE.
- [ ] **V1-TOOL-SESSION-INSPECT** No read-only tool to retrieve current session state. After `sessionConfigure`, agents have no way to verify rule overrides, native wrappers, or excludes without making a no-op `scan` call. Add `sessionInspect()` returning the same shape `sessionConfigure.active` returns plus `rules`, `nativeWrappers`, `cwd`. Pairs with V1-SESSION-CONFIGURE-ECHO-STATE.
- [ ] **V1-TOOL-LIST-FINDERS** Manual-review finders (`review/multiple-ways`, `review/pointer-input`, etc. — visible in `review_candidates.prompts[*].finderId`) are NOT enumerated by any tool. Agents discover them only by accident through `review_candidates` results. Per the rules-silently-defer / finders-silently-fire pattern, having a `list_finders` tool would expose the manual-review surface area for audit.
- [ ] **V1-TOOL-AUDIT-RULE-COVERAGE** No tool surfaces "rule X did not fire on file Y, but heuristic predicates suggest it should have." The only way to discover an FN is to manually inspect `perRuleCoverage` and reason about it. Add `audit_rule_coverage({ruleId, file})` returning `{fired: false, eligibleByExtension: true, predicateMissed: true, hint: "trigger requires static-text child; file contains Liquid expression"}`.
- [x] **V1-MCP-DIST-STALE-CI-GATE** The deployed MCP `dist/cli.js` (verified Apr 21 19:03) is 2 days stale relative to current `main` HEAD (Apr 23 16:32). Multiple field-test "bugs" reproduce in the deployed binary but have already-merged fixes in source — concretely: pointer-input `panels`/`panel` substring FP (commit `10ab7391`, `src/review/finders/pointer-input.ts:97-122 + :312-313`), `configSearchedFrom` echo of cwd (Q6-CONFIG-CONTEXT-TRIPLE-READOUT closure at `src/mcp/tool-scan-project.ts:337-344`), `configNote` boilerplate (same closure), `plan.safeEditsAvailable` composite-headline drop (commit `fce189c8`). The 4-pass field-test repeatedly catches the same regressions because no test asserts `dist/cli.js` content hash matches HEAD before the MCP server starts. Fix: CI check that compares `dist/cli.js` content hash to a git-tracked manifest, OR a startup-time `console.warn` if `dist/` is older than the most recent `src/` commit, OR a `bun run mcp:check` that diffs `dist` against `bun build src/cli.ts` output. Pairs with V1-PLAN-SAFE-EDITS-STALE-MCP-SERVER (the symptom) — this is the systemic root cause.

Catalog-scale perf / scoping:

- [ ] **V1-CHECKLIST-VENDOR-IFRAME-OCCURRENCES-COLLAPSE** `checklist.likelyIrrelevant` ships 74 candidates per criterion × 4 criteria = 296 rows for templates corpus, all pointing at identical `<iframe id="fancybox-frame{rnd}"...` template literals in 74 copies of `fancybox.pack.js:4`. The bucket label is doctrine-correct (deterministic "no `<video>`/`<audio>` parsed") but the *quantity* is not — every fancybox install ships an identical line. Fix: collapse to one entry with `occurrences: 74`. Pairs with V1-CSS-CROSS-TEMPLATE-FINGERPRINT and V1-CHECKLIST-VENDOR-FILE-FINGERPRINT-COLLAPSE.
- [ ] **V1-CHECKLIST-IFRAME-EMBEDDED-MEDIA-NOT-IRRELEVANT** `checklist.likelyIrrelevant: 4` on groups SCs 1.2.1/1.2.3/1.2.5 under "no `<video>` or `<audio>` detected" — bucket label is provable from code (deterministic fact), but the candidates inside include `docs/_tutorials/video-walkthroughs.md:10` (`<iframe src="https://www.youtube.com/embed/T1itpPvFWHI">`) and `docs/pages/jekyllconf.md:16` (parameterized YouTube embed via Liquid). YouTube iframes ARE WCAG 1.2.x in scope. The bucket sweeps real candidates into "likely irrelevant" because the heuristic confuses "no native `<video>` tag" with "no media." Fix: either (a) detect `<iframe src*=youtube|vimeo|twitch|brightcove|wistia>` and demote those candidates from `likelyIrrelevant` back to `actionable`, or (b) keep them in main `items[]` with reason "iframe embed of likely media (`youtube.com/embed/...`) — no native `<video>` tag but media accessibility still applies." Doctrine: a bucket is honest only when label-AND-implication are both 100% from evidence; here the implication is heuristic.
- [ ] **V1-PLAN-FINDINGS-BY-FILE-AND-RULE-HISTOGRAM** `scan_project` exposes `totalFilesWithFindings` but no per-file or per-rule distribution at the response root. On an SSG docs tree (71 files-with-findings) the agent can't budget pagination cost without iterating all 71. On templates (1793 files) paging 1793× costs hours of round-trips. Fix: add `plan.findingsByFile: [{path, count}]` (top-N rank-ordered) AND `plan.findingsByRule: {ruleId: count}`. Pairs with V1-CROSS-FILE-ROLLUP-PRIMITIVE.
- [ ] **V1-AGENT-RESPONSE-REFERENCE-GUIDE-OPT-IN** `scan_file` on dense HTML (>100 LOC) blows the MCP token ceiling because `referenceGuide.suppressPlacement` + `referenceGuide.fixDescriptions` ship on every per-file response (~30-40 KB each call). Fix: ship `referenceGuide` only on the first call per session, OR behind `includeReferenceGuide: true` opt-in. Pairs with V1-TOOL-VERBOSE-META-INVERTED-DEFAULT.
- [ ] **V1-FILES-BY-EXTENSION-GROUND-TRUTH-UNDERCOUNT** `meta.filesByExtension` undercounts actual file counts on the a design-system docs corpus: reports `.css:31, .js:56, .scss:114, .md:13` while ground-truth `find -type f -name '*.css'` returns 47, `.js` 116, `.scss` 122, `.md` 17 — deltas of -16 / -60 / -8 / -4 with no obvious default-exclusion explaining them. Deltas are not consistent with `DEFAULT_EXCLUDED_PATTERNS` paths; silent drops somewhere between discovery and the bucket-by-ext counter. Fix: add an invariant test that `sum(filesByExtension.values()) + sum(skippedByExtension.values()) + excludedByPattern` equals the raw `find` count on a known fixture corpus; instrument the discovery pipeline to emit `meta.filesByExtension.excludedByPatternPerExt: {ext: count}` so the drop axis is visible. Doctrine: verbose meta is scan-confidence signal — a count that silently misses 60 JS files trains the agent to trust a dishonest denominator.
- [x] **V1-ALT-TEXT-PLACEHOLDER-SEQUENTIAL-SLIDE** `media/alt-text-placeholder`'s token list covers `image|screenshot|placeholder|TODO|photo|picture` but misses the canonical carousel-slide shape: `alt="First slide"`, `alt="Second slide"`, `alt="Third slide"` (design-system carousel docs, template kits, countless starter themes). Sequential positional labels are textbook placeholder — they carry the author's intent ("I'll describe this later") but ship as production alt text. Fix: extend token list to `/^(first|second|third|fourth|fifth|next|previous|prev|last)\s+slide$/i`, `/^slide\s+\d+$/i`, `/^(image|photo|picture)\s+\d+$/i`. Pairs with V1-RULE-ALT-TEXT-PLACEHOLDER-ROLE-AS-ALT (same rule, different antipattern axis).
- [x] **V1-TOOLTIP-DISMISSABLE-VISIBLE-TEXT-EQ-TITLE** `tooltip/dismissable` fix description offers three alternatives ("replace with visible text, aria-label, or custom tooltip library") but when visible text already equals `title`, the first two are redundant — the button at `js/tests/integration/index.html:18` (`<button title="Tooltip on top">Tooltip on top</button>`) already has the accessible name the alternatives recommend. Fix: when the flagged element's trimmed `textContent` equals the `title` attribute value (case-insensitive), emit only the tooltip-library upgrade hint and drop the redundant visible-text / aria-label alternatives. Reason-enrichment only — rule still fires (native title has no keyboard dismiss path). Doctrine: the tool must not suggest alternatives that re-state the existing state of the DOM.
- [x] **V1-MOTION-PAUSE-STOP-CAROUSEL-HAS-VISIBLE-CONTROLS-REASON** `motion/pause-stop-hide` fires on `data-bs-ride="carousel"` regardless of whether the carousel has user-operable `.carousel-control-prev` / `.carousel-control-next` controls in the same subtree. WCAG 2.2.2 is satisfied when a pause / stop / hide mechanism exists — visible prev/next buttons with keyboard focusability count as "stop" (user can halt auto-advance by interacting with controls). On `js/tests/visual/carousel.html:20` the rule fires despite controls at :37-44. Surface-don't-suppress per doctrine: keep the finding (controls being present does not prove they are keyboard-operable; AT pause flow is still weaker than an explicit pause button), but enrich reason with "note: descendant controls `.carousel-control-prev|next` detected at :37,:42 — verify keyboard focus + announcement carry pause semantics before dismissing." Reason-text only; no detection change.
- [x] **V1-ARIA-CONFLICTING-ROLE-DATA-BS-SLIDE-ENRICH** `aria/conflicting-role` on `<a role="button" data-bs-slide="prev">` (carousel anchor-as-button idiom) emits the generic "element has role that conflicts with its tag" reason without naming the data-bs-* context that pins down the fix. The canonical remediation is `<button type="button" data-bs-slide="prev">`, not "remove role." Fix: when `data-bs-slide` / `data-bs-dismiss` / `data-bs-toggle` / `data-bs-target` is present on the flagged element, enrich reason with "Bootstrap action trigger — use `<button type=\"button\">` (data-bs-* attributes work on buttons identically)." Reason-text enrichment; no detection change. Pairs with V1-FP-KEYBOARD-HANDLER-DATA-BS-CONTAINER (same attribute family, different rule).
- [x] **V1-TEMPLATE-CLASSIFIER-LIQUID-PIPE-FILTER-EVIDENCE** Classifier tags SSG includes / layouts as `handlebars-or-mustache` because the `{{ ... }}` tokens are ambiguous — but Liquid filter pipes (`{{ x | default: "en" }}`, `{{ y | escape }}`) are Liquid-exclusive syntax. Current per-file majority-vote doesn't credit the `|` pipe inside `{{ }}` as Liquid-only evidence; see `docs/_includes/top.html` tagged as handlebars. Fix: `{{ … | … }}` with a pipe separator inside the expression is high-confidence `jinja-or-liquid` — outweighs any `{{ plain }}` co-occurrence in the same file. Pairs with Q4-TEMPLATE-CLASSIFIER-LIQUID-AS-MUSTACHE-SINGLE-FILE (closed — covered the `{%- -%}` bracket axis; this item covers the filter-pipe axis). Doctrine: verbose meta integrity — wrong family tag mis-routes any downstream strip helper.
- [x] **V1-RULE-TABLE-TH-SCOPE-IMPLICIT-COL-SKIP** `semantics/table-th-scope-missing` fires on `<th>` that sits as a direct child of `<thead><tr>` — but HTML5 specifies implicit `scope="col"` for this shape, and AT (JAWS, NVDA, VoiceOver) all honor the implicit association. On `docs/_docs/configuration/options.md:16-17` a textbook `<thead><tr><th>Setting</th><th>Description</th></tr></thead>` is flagged as if scope were required. Fix: skip the rule when `<th>` is a direct child of `<thead>/<tr>` AND the table has a `<tbody>` sibling (the full standard pattern that browsers resolve implicitly); keep firing for `<th>` scattered in `<tbody>` rows (where scope IS required) and for tables without `<thead>`. Doctrine: the rule's spec is wrong — implicit scope is a determinate W3C/HTML5 rule, not a heuristic, so skipping here is spec-correctness not suppression.
- [ ] **V1-SCSS-RULES-COVERAGE-HOLE-JEKYLL** On an SSG source tree (18 `.scss` files, zero `.css`), `meta.analysisCoverage.rulesByExtension[".scss"]` lists only `focus/outline-visible` + `wrapper/drift` — the CSS-family rules (`contrast/minimum`, `contrast/non-text`, `contrast/enhanced`, `layout/*`, `focus/not-obscured`) never run despite SCSS containing plain CSS declarations the alias pipeline should route. Q4-SCSS-DISCOVERY-WIRE (closed) verified SCSS parses cleanly and fires `contrast/minimum` on the initial audit; the current SSG tree does NOT show those rules running. Either (a) the extensionMatches alias regressed, (b) the rule selectors themselves decline on SCSS substrate, or (c) the MCP subprocess is serving a stale bundle (V1-MCP-DIST-STALE-CI-GATE umbrella). Fix: add a cross-surface invariant test asserting every `.scss`-scanned repo fires at least one `contrast/*` rule when the source contains a low-contrast declaration; if the alias path is intact but selectors decline on `.scss`, widen the selector gate so CSS-family rules run on `.scss` after SCSS-only syntax is stripped. Pairs with Q6-SCSS-PARSING-DISCLOSURE (closed, telemetry axis) — this item is the detection axis.
- [ ] **V1-SCAN-FILE-META-CROSS-FILE-STATE-LEAK** `scan_file` on `docs/_includes/top.html` returns `meta.analysisCoverage.partialParseFiles` containing `error.html` (a different file in the same project). Per-file scans should report per-file diagnostics only; leaking project-scan state into a single-file response forces the agent to re-read and disambiguate whether `error.html` was actually involved in evaluating `top.html`. Fix: `scan_file` must filter `parseErrorFiles` / `partialParseFiles` / `scannedBuildArtifacts` / `opaqueCustomComponentNames` to only entries whose path equals the scanned file. Doctrine: cross-surface shape drift — `scan_file` and `scan_project` must not share the same unfiltered meta blob.
- [ ] **V1-CHECKLIST-AUTOMATED-PASS-RATE-SEMANTICS-UNCLEAR** `checklist.summary.automatedCoverage.automatedCriteriaPassRate: 58` on that ALSO carries 323 active violations and 71 files-with-findings reads to an agent as "most criteria pass" — but the denominator / numerator are undocumented in the response. Q-SHARED-PASS-RATE-COMPOSITE (closed) specified the split (`criteriaEvaluated / criteriaClean / criteriaWithFindings / criteriaUntestable`); the shipped field is still a bare scalar with no companion split on the checklist surface. Fix: either (a) ship the structured split alongside (or instead of) the scalar, (b) rename the field to `automatedCriteriaCleanRate` so the label names what it measures, or (c) drop the scalar entirely in favor of the per-bucket sibling counters. Distinct from V1-ZERO-SCAN-PASS-RATE-SENTINEL (zero-file edge case); this is the healthy-scan semantic-ambiguity axis. Doctrine: ambiguous field shapes are dishonest.
- [ ] **V1-FINDER-ACCESSIBLE-NAME-SR-ONLY-IMG-ALT-REDUNDANT** Canonical logo-link shape `<a href="/"><span class="sr-only">Brand</span><img src="logo.png" alt="Brand Logo"></a>` concatenates to accessible name "Brand Brand Logo" — duplicate token + redundant "Logo" suffix (AT announces the role automatically). On `docs/_includes/header.html:4-9` this shape ships unflagged; no rule or finder surfaces the sr-only + alt redundancy pattern. Fix: new review finder `review/accessible-name-redundant-composition` — compute accessible name for anchors / buttons whose children mix visually-hidden text (`.sr-only`, `.visually-hidden`, `[aria-hidden]`) with `<img alt="…">`; when the concatenated name contains case-insensitive duplicate tokens OR role-suffix redundancy (`logo|image|icon|button|link`), emit a 2.4.6 / 4.1.2 review candidate. Satisfies wcag22:2.4.6 + 4.1.2.
- [x] **V1-RULE-KEYBOARD-HANDLER-DRAGGABLE-NON-NATIVE** (already closed by 1510e5a6 — backlog stale) `keyboard/handler-missing` does not flag `<div draggable="true">` without `tabindex` + `onKeyDown`. On `drag-n-drop/index.html:11` the draggable div is only wired to mouse drag events in an external `.js`; keyboard users cannot initiate drag. `pointer/drag-alternative` fires (2.5.7 axis) but the 2.1.1 axis stays silent. Q5-DRAG-ALTERNATIVE-KEYBOARD-COMPOSE (closed) dealt with predicate overlap for already-detected draggable cases; on this shape the rule's own selector never matches because `<div draggable>` isn't in its native-interactive allowlist. Fix: extend `keyboard/handler-missing` selector to `[draggable="true"]` on non-native-interactive tags (`div`, `span`, `li`, `article`) — fire when neither `tabindex` nor `on[Kk]ey*` handler exists. Satisfies wcag22:2.1.1.
- [x] **V1-RULE-LABEL-FOR-ADJACENT-MISMATCH** Copy-paste bug where `<label for="email">Password:</label>` sits immediately above `<input id="password">` — label's `for` still matches a real id elsewhere in the document, so `forms/label-for-id-mismatch` declines; `forms/label-adjacent-unassociated` declines too (label DOES have a `for`). Net result: the password input escapes all label-family rules while AT double-announces the email label and leaves password unlabeled. `password-strength-background/index.html:31` is the canonical case. Fix: new sibling predicate in the label-family — fire when `<label for=X>` is immediately followed by an input whose `id !== X`, AND id `X` resolves to a different element in the same document. Distinct from V1-RULE-FORM-LABEL-ADJACENT-INPUT-WITHOUT-ID (no-id axis); this is the mismatched-ids-across-adjacency axis. Satisfies wcag22:1.3.1 + 3.3.2 + 4.1.2.
- [ ] **V1-FINDER-ALT-DUPLICATES-SIBLING-TEXT-INSIDE-BUTTON** Pattern `<button><p>Fly</p><img alt="fly"></button>` (`insect-catch-game/index.html:19-49`, 4 instances) — alt text duplicates a visible sibling text node inside the same interactive ancestor. 1.1.1 guidance says decorative/redundant images should use `alt=""`; the current duplication inflates accessible name ("Fly fly") and burdens AT. `media/alt-text-placeholder` misses this because the alt value is authored-looking ("fly" is not in the placeholder token list), yet in context it IS placeholder. Fix: extend `media/alt-text-placeholder` OR new review finder — when `<img alt="X">` is a descendant of `<button>` or `<a>` AND a sibling text node (direct or via `<p>`/`<span>`/`<strong>`) contains case-insensitive `X` (or X is a substring of the sibling), emit a 1.1.1 candidate recommending `alt=""`. Satisfies wcag22:1.1.1.
- [ ] **V1-RULE-TOGGLE-BUTTON-ARIA-PRESSED** `<button class="toggle">Dark mode</button>` on `theme-clock/index.html:12` flips `html.dark` — persistent on/off state, distinct from disclosure (which toggles visibility of a related region). Needs `aria-pressed="true|false"`, not `aria-expanded`. V1-RULE-EXPANDED-ON-DISCLOSURE-FIRES-ABSENT-ATTRIBUTE covers the disclosure-shape-without-attribute axis; this item is the toggle-button-without-pressed axis — different ARIA pattern. Fix: new review finder `aria/toggle-button-pressed-missing` — fire on `<button>` where class contains `/toggle|switch|mode/` OR visible text matches `/^(dark|light|night|day|on|off)\s*(mode)?$/i` AND element has no `aria-pressed`/`aria-checked`. Confidence `medium` (cross-file handler resolution lossy). Satisfies wcag22:4.1.2.
- [ ] **V1-FINDER-STATUS-MESSAGES-PLUS-MINUS-READOUT** 4.1.3 Status Messages finder does not fire on the plus/minus counter pattern: `<button>+</button><span id="size">10</span><button>-</button>` where the readout mutates on button click but has no `role="status"` / `aria-live`. `drawing-app/index.html:13` has the canonical shape. V1-LIVE-REGION-RUNTIME-MUTATION-CANDIDATE (open) covers the cross-file `elem.innerHTML = …` JS axis; this item is the static-DOM signal — provable from the HTML shape alone, higher-confidence than cross-file handler resolution. Fix: new review-candidate finder `review/status-message-plus-minus-readout` — fire on `<span>`/`<div>`/`<output>` sibling to two buttons whose trimmed text or class matches `/^[\+\-]$/` or `/\b(plus|minus|increase|decrease|up|down)\b/i`, where the target lacks `aria-live|role=status|role=alert|<output>`. Confidence `high` (fully static). Satisfies wcag22:4.1.3.
- [ ] **V1-TRUNCATED-RESPONSE-NEXTSTEP-PAGING-HINT** When `truncated: true` and `nextOffset` is present, `nextStep` prose still reads "call suggest_fix on the top finding" — ignoring that the agent has 80 files-with-findings and paged only 25. An agent following `nextStep` proceeds to single-finding triage and never pages the rest. Fix: when `truncated: true`, prepend `nextStep` with "Response truncated — {totalFilesWithFindings} files-with-findings, {filesReturned} returned. Call scan_project with offset:{nextOffset} to continue, OR triage the top returned finding first via: …" — AND ensure `nextStepStructured` offers a paging shape (`{tool: "scan_project", args: {offset: N}}`) alongside the finding-triage shape. Doctrine: one tool call should answer "what next?" — the current response points at the second-order question while hiding the first-order pagination cost. Pairs with Q-SHARED-LIMIT-REQUEST-VS-EFFECTIVE (closed, `effectiveLimit` surface axis).
- [x] **V1-2.4.5-SINGLE-PAGE-REASON-ENRICH** Per P1-I + doctrine, 2.4.5 Multiple Ways fires on every single-page demo (50/50 projects, hundreds of template kits) because static analysis cannot distinguish SPA shell from multi-page site. Keep surfacing (doctrine) but enrich reason-text with the dismissal signal: "document has 0 outbound `<a>` links, 0 internal-fragment anchors, and 0 breadcrumb/nav landmarks — likely single-page context; review before flagging." Pairs with V1-FP-MULTIPLE-WAYS-CITE-COUNTS (closed, cite-count addition) — that item added nav-signal counts; this item explicitly frames "0 of everything" as dismissal signal. Reason-text only — no suppression. Doctrine: enrich `reason` so agent dismisses faster without heuristic suppression.
- [ ] **V1-EMPTY-ROOT-DIV-SCRIPT-ONLY-WARNING** Canonical vanilla-JS demo shape: body contains `<div id="buttons"></div>` + `<script src="script.js"></script>` and nothing else; all interactive controls are created by the script at runtime. ra11y returns 0 findings for `sound-board`, `hoverboard`, `pokedex`, `toast-notification`; `plan.summary` reads "No automated findings." Zero-output success per doctrine — agent concludes "clean page" when the real answer is "static scan cannot evaluate runtime-generated DOM." Fix: emit structured warning `warnings: ["dynamic_content_container_detected"]` + sibling `warningsDetails.dynamic_content_container_detected: {bodyChildCount, emptyContainerIds, scriptSources}` when body has ≤3 non-script visible children AND contains an empty `<div id>` AND a sibling `<script src>`. Pairs with V1-LIVE-REGION-RUNTIME-MUTATION-CANDIDATE (runtime-mutation axis on populated DOM) — this is the never-populated-at-parse-time axis. Doctrine: zero-output success is ambiguous failure.
- [x] **V1-PARSING-MALFORMED-TAG-SURFACE-FINDING** `</h33>` (invalid heading tag — digit-typo on `</h3>`) in a template catalog `blazer-responsive-html5-coming-soon-template/index.html:47` surfaces only as `partialParseFiles[].reason`: no rule emits a finding at the location. Structural typos that a browser's error-recovery silently absorbs can shift semantic boundaries (heading level inference, landmark scope). Fix: emit a `parsing/malformed-tag` finding at every position where the partial-parse recovery resolved a structurally-invalid element token (impossible tag names like `h33`, `h7`, `h0`; numeric-suffixed tag names outside the HTML5 inventory). Pairs with V1-HTML-PARSER-MARKDOWN-URL-AUTOLINK (closed, scheme-shaped token recovery) — that fixed the parser; this item surfaces the recovery as a structured finding so the agent can action it. Satisfies wcag21:4.1.1 (parsing failures).
- [ ] **V1-REVIEW-CANDIDATE-CROSS-FILE-REPETITION-COLLAPSE** Website-templates' 528 HTML files trigger the same 2.4.5 candidate with byte-identical reason "Likely root layout has no search/sitemap/breadcrumb" on each `index.html`; `uniquePerCriterion` doesn't help because every candidate anchors to a different file path. V1-CHECKLIST-CRITERION-CANDIDATE-DEDUPE-ACROSS-IDS covers the same-candidate-across-criteria axis; V1-CHECKLIST-VENDOR-IFRAME-OCCURRENCES-COLLAPSE covers the vendor-file same-path axis. This item is the cross-file cross-template pattern-repetition axis. Fix: when the same `(ruleId, reasonHash)` fires on N files with N > threshold (e.g. 20), collapse to one top-level entry with `{pattern, occurrences: N, sampleFiles: [top-5-paths], totalFiles: N}` and elide the rest; the agent gets the signal once + sample coverage + the total without 528 near-identical rows. Doctrine: labeled buckets honest when the label is provable — "same-reason pattern fires on N files" is a deterministic fact.


### v1.0.0 — orchestrator follow-ups

Items surfaced during the /continue orchestrator run that landed the tenth-pass batch. Not user-surfaced drift — engineering follow-ups.

- [ ] **V1-SILENT-DROP-TYPEOF-BOOLEAN-AUDIT** — the V1-SESSION-CONFIGURE-ALLOWWRITE-SILENT-DROP fix (commits 7fd25c08, ded4616e) revealed a `typeof x === "boolean"` guard that silently discarded non-boolean inputs. Audit other MCP tool-input parsers for the same pattern — any `typeof` guard that quietly drops mismatched inputs instead of emitting a structured `invalid-param` error. Candidates: `sessionConfigure.autoDetectWrappers`, `scan_project.additionalPaths` shape, `suggest_fix.maxCandidates`, `checklist.maxCandidatesPerCriterion`, any other input shape assertions in src/mcp/*.ts. Fix with the discriminated `{ok, opts} | {ok:false, error}` pattern established in src/mcp/configure-opts.ts. Per AI-first doctrine, silent drops are dishonest — the agent thinks it configured a setting that never took effect.
- [ ] **V1-ANALYSIS-COVERAGE-FILE-LIMIT-APPROACHING** — `src/mcp/analysis-coverage.ts` hit 503 effective lines during the V1-FRONTMATTER-AS-TEMPLATE-DIRECTIVE-TRIGGER + V1-TEMPLATE-CLASSIFIER-MARKDOWN-PROSE-FALSE-POSITIVE integration; integrator self-refactored to drop back under 500. The file is absorbing markdown + frontmatter + code-span + template-engine classification all at once. Split into `markdown-classifier.ts` + `frontmatter-classifier.ts` + a thinner `analysis-coverage.ts` orchestrator before the next markdown-classifier item lands, or the next addition will bounce off the limits guard again.
- [ ] **V1-MDX-FENCED-CODE-BLOCK-INDENTED-PARITY** — `src/input/parsers/mdx.ts` has duplicated fenced/inline-code stripping logic from `markdown.ts` but is missing the indented-code-block strip that turn 7 of the 2026-04-25 /continue run added (commit 2af3e5f3). Port the indented-code-block prepass into `mdx.ts` so MDX gets the same false-positive coverage. Surfaced by parser-author specialist during Q7-MARKDOWN-FENCED-CODE-BLOCK-SCOPE — flagged as out-of-scope for that pick but worth following up.
- [ ] **V1-CORPUS-REFRESH-POST-DIST-FRESHNESS-GATE** — V1-MCP-DIST-STALE-CI-GATE (shipped 2026-04-25 in commit effb2645) caught a stale `dist/cli.js` on `main` itself. The 2026-04-25 multi-repo corpus probe that generated the Q8 + much of the V1 backlog ran against the pre-gate stale subprocess, so 4 of ~30 picks in the same /continue run returned `classification_mismatch: already_landed` (Q7-RULE-ANIMATION-ITERATION-COUNT-GATE, Q7-RULE-TABLE-TH-SCOPE-IMPLICIT-COL-NARROWING, V1-SCANNED-BUILD-ARTIFACTS-ENVELOPE-DRIFT, V1-RESPONSE-SIZE-PRE-ESTIMATOR). Re-run the multi-repo probe now that dist is gated and tick off any other already-landed items still sitting open in Q8 + V1. Per `feedback_regression_audit_stale_subprocess` memory: grep + verify rather than blind-re-implement.
- [ ] **V1-PLANNER-PRE-GREP-STALE-DETECTION** — The /continue planner subagent (`.claude/agents/planner.md`) currently classifies picks but does not pre-flight check whether the pick's predicate is already implemented. When 4 of 30 picks in a single run return `classification_mismatch: already_landed`, that's wasted dispatch budget the planner could have absorbed. Extend the planner to grep `git log --oneline --grep=<rule_id_or_predicate>` and `src/rules/<path>` for each pick before classifying; if there's strong evidence the predicate is implemented, mark the pick `deferred: stale_already_landed: <evidence>` so the orchestrator ticks it off without dispatching. Pairs with V1-CORPUS-REFRESH-POST-DIST-FRESHNESS-GATE (the corpus refresh removes the input; this item removes the wasted-dispatch failure mode for whichever stale items survive).
- [ ] **V1-COLLECT-MANUAL-CRITERION-IDS-PARTIAL-AUTOMATABLE** — `collectManualCriterionIds` in `src/engine/scanner.ts` only includes criteria with `automatable: "manual"`, so review finders citing `automatable: "partial"` criteria (e.g. the new `review/finders/reduced-motion-candidate.ts` on 2.3.3, plus `review/finders/carousel-pattern.ts` on 2.2.2 if applicable) may not activate via the production scanner. Surfaced by the `review/finders/reduced-motion-candidate` rule-implementer during /continue 2026-04-25 turn 10. Audit which finders cite partial-automatable criteria; either widen `collectManualCriterionIds` to include partial-automatable criteria when they have an associated finder, OR document the rule that finders must cite manual-only criteria. WCAG-side correctness: 2.3.3 IS partial-automatable (the `prefers-reduced-motion` media-query check is a deterministic predicate even though the SC overall is judgment-bearing). Bias toward widening — the failure mode is silent under-emission.
- [ ] **V1-WORKTREE-GUARD-EXPAND-UNTRACKED-DIRS** — Already shipped 2026-04-25 in commit 9f9bf8d4. The PostToolUse worktree-guard hook (`.claude/hooks/worktree-guards.ts`) was raising spurious "suspected escape" warnings on Write operations to fresh fixture directories because `git status --porcelain` collapsed new directories to a single `??` entry, hiding individual filenames from the basename match. Adding `--untracked-files=all` expanded the entries. This burned one rule-implementer dispatch in turn 8 (Q8-RULE-ICON-CHILD-MISSING-ARIA-HIDDEN — re-dispatched in turn 9 and landed cleanly). Logged here as durable evidence of the failure mode. Closed.

---



- **Implement the remaining manual-only criteria as finders.** The coverage matrix (V1-COVERAGE-MATRIX) will enumerate actual gaps — per finder review the finder list confirms most are already covered. If V1-COVERAGE-MATRIX surfaces genuine gaps, each becomes a Track R `R-<criterion>-<topic>` item, NOT a Track V item. Track V verifies; Track R implements detection.
- **Widen real-world fixtures beyond the five above.** SPA routing, internationalization, dark-mode contrast, reduced-motion — all worth fixtures, none are "find all violations" gates. Log as Track F `F-V1.x` items if the v1.0 bar rises.
- **OSSF Scorecard / npm audit workflows.** Nice-to-have for supply-chain posture; zero-dep invariant already covers most of the surface. Skip unless a real signal arrives.

---

## History — Phases 0–17 complete

Historical record; do not modify. The `/continue` skill does not walk this section.

- **Phase 0** — Autonomous Claude Code infrastructure (hooks, agents, skills, settings).
- **Phase 1** — Guard + generator scripts; `scripts/verify.ts` single entrypoint.
- **Phase 2** — Core types, registries, scanner skeleton, AST helpers, public API surface.
- **Phase 3** — WCAG 2.2 standard module; 86 active criteria + historical 4.1.1.
- **Phase 4** — In-house utilities (logger, assert, fs, path, glob, git, ansi, string-width, wrap, color, contrast, args, index).
- **Phase 5** — Parsers: tsx, html, css, tailwind + theme resolver + discover; unit + fuzz tests.
- **Phase 6** — Rule engine + first 5 rules (alt-text-missing, contrast/minimum, link-descriptive-text, focus/outline-visible, parsing/duplicate-id).
- **Phase 7** — Scanner + terminal/plain/json formatters + theme + snapshot tests.
- **Phase 8** — CLI: scan, list-rules, list-standards, explain, coverage, checklist, vpat, certification, init, doctor.
- **Phase 9** — WCAG 2.1 standard module (reuses rules via equivalentTo).
- **Phase 10** — Section 508 + EN 301 549 standard modules (thin equivalentTo wrappers).
- **Phase 11** — Remaining rules across contrast, focus, keyboard, aria, semantics, forms, pointer, navigation, layout, tooltip, document domains. 54 rules total.
- **Phase 12** — Alternative formatters: sarif, junit, markdown, html, agent.
- **Phase 13** — Reports: coverage, checklist, vpat, certification.
- **Phase 14** — Config, inline disables, baseline mode (create/check/update), monorepo `projects: []` support.
- **Phase 15** — Plugin API (`defineRule`, `defineStandard`, `defineFormatter`, `defineConfig`) + three example plugins + CI smoke job.
- **Phase 16** — Long-form docs: getting-started, CLI, configuration, architecture, KB (architecture + patterns + concepts + gotchas + glossary), certification guides, plugin authoring, MCP docs, ADRs 0001–0005.
- **Phase 17** — CI workflows (ci.yml, release.yml), issue + PR templates, dependabot.
- **Phase 18 partial** — README with real output snapshots + multi-standard-scan integration test + mcp-session integration test coverage audit. Remaining demo/tag/publish items live in Track D above.
- **Phase 19 partial** — In-house JSON-RPC 2.0 MCP server (~200 LoC), session state + AST cache by mtime, 10 live tools, session config for `nativeWrappers`, `.mcp.json` auto-attach, integration tests. Remaining hardening items live in Track M above.
- **Phase 22 partial** — Review finders shipped: use-of-color, error-identification, headings-and-labels, on-input-body (landed as reason-text tiers on on-input-change per CLAUDE.md §1), `tool-audit` meta-tool. Remaining finder + finder tests live in Track R.
