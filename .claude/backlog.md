# ra11y backlog

The `/continue` skill reads this file and dispatches work to specialist subagents. Each item should be small enough that one specialist can finish it in under 20 minutes. When an item would produce more work, split it in place before dispatching.

Legend: `[ ]` open · `[~]` in progress · `[!]` blocked (reason in comment).

Closing an item = deleting its `- [ ] **<ID>**` line in the same commit that lands the work, with a `Closes: <ID>` (or `Drops: <ID>`) trailer in the commit message. The commit body carries the rationale; look it up later via `git log --grep "Closes: <ID>"` or `bun scripts/show-closed.ts <ID>`. See CLAUDE.md §9.7. Only the three states in the legend above are valid; `scripts/check-commit.ts` rejects untrailered deletions and any other state.

---

## Track Q15 — Multi-corpus AI-first sweep (2026-05-02)

- [ ] **Q15-UNTARGETED-CRITERIA-PROJECT-VS-FILE-SAME-FIELD-NAME** `scan_project.plan.untargetedCriteria: 7` on a bulk corpus root; `scan_file` on a single HTML in the same corpus reports `plan.untargetedCriteria: 18`; `scan_file` on a `*.min.js` reports `plan.untargetedCriteria: 19`. Per-file untargeted-count cannot logically exceed the project total — the field name names one concept while two slices ship under it. Closure: rename per-file vs per-project so the slice difference is explicit (`untargetedCriteriaForFile` vs `untargetedCriteriaForProject`), or drop the per-file scalar in favor of `criteriaCoverage.byFile[file]`. Per AI-first doctrine "Composite headline counts are dishonest."
- [ ] **Q15-MIN-CSS-ACTIONABLE-MANUAL-ITEMS-INCLUDES-VENDOR-LANE** `scan_file` on `dist/*.min.css` ships `plan.actionableManualItems: 1` despite zero source-lane findings — all 137 emissions are `buildArtifact`-lane. The manual-items counter does not split by lane the way `fixesByClass` does. Closure: split `actionableManualItems` by lane (`actionableManualItemsBySource: { source: N, buildArtifact: M }`) or restrict the counter to `source`-lane findings; integration test pinning the lane filter. Per AI-first doctrine "Composite headline counts are dishonest."
- [ ] **Q15-TEMPLATE-INTERPOLATION-NO-PER-STYLE-CLASSIFIER** `meta.templateInterpolationFound` reports `{{x}}: 3396, {%x%}: 704, <%x%>: 9` — three template engines mixed (Liquid + ERB + Mustache-shape). No per-style classifier (e.g. `liquid_directives_unparsed` / `erb_directives_unparsed`) is emitted; the agent cannot scope around a specific engine. Closure: split `template_files_parsed_as_literal` into per-token-style codes (`liquid_directives_unparsed`, `erb_directives_unparsed`, `mustache_directives_unparsed`); each ships its own file list and count. Per AI-first doctrine "Routing skips that drop content are the symmetric twin of suppression" — split-by-predicate closure path.
- [ ] **Q15-NO-GROUPBY-FIRSTCHILDDIR-NEXTSTEP-ON-BULK-CATALOG** `scan_project` default `groupBy: none` on a bulk-catalog corpus (≥30 sibling subdirs each shaped `<dir>/index.html` + `<dir>/style.css` + `<dir>/script.js`) ships `nextStep` mentioning paging but never recommending `groupBy: "firstChildDir"`. The catalog shape is the canonical case for that grouping. Closure: when bulk-catalog shape is detected (per the small-N gate in V1-BULK-CATALOG-DETECTED-SMALL-N-THRESHOLD), `nextStepStructured` proposes `groupBy: "firstChildDir"` as an alternative narrowing path. Per AI-first doctrine "One tool call should answer 'what next?'"
- [ ] **Q15-BOOTSTRAP-OMITS-BULK-CATALOG-WORKFLOW-RECOMMENDATION** On a corpus shaped as ≥30 parallel sibling demo subdirs, `bootstrap.suggestedConfig` proposes only rule-severity tuning — no recommendation to set `groupBy: "firstChildDir"` or per-subdir `restrictToPaths`. Output is paste-safe but workflow-incomplete: the agent gets severity overrides and no scope guidance for the catalog shape that drives the noise floor. Closure: when bulk-catalog shape is detected, `suggestedConfig` includes `groupBy` and/or example `restrictToPaths` block alongside severity tuning; integration test on a bulk-catalog fixture asserting the workflow recommendation appears. Per AI-first doctrine "Bootstrap output must be paste-safe" — extension: paste-safety covers workflow recommendations on detected shapes.
- [ ] **Q15-VENDOR-FILE-SHA-FINGERPRINT-DEDUP-ACROSS-TEMPLATE-SUBDIRS** On a 100+ template-subdir bulk catalog, identical vendor files repeat verbatim — `bootstrap.min.css` × 116, `bootstrap.min.js` × 115, `font-awesome.min.css` × 89, `jquery.fancybox.css` × 64. Each instance is parsed and findings are emitted N times. Pairs with V1-CSS-CROSS-TEMPLATE-FINGERPRINT (same-CSS-pattern across files via selector hash) and V1-CHECKLIST-VENDOR-FILE-FINGERPRINT-COLLAPSE (vendor file collapse on candidates). This item is the deterministic-fingerprint variant: hash the first N bytes of the vendor file (or the full sha-1) and emit findings once with `occurrenceCount` + `occurrenceLocations` rather than N times. Avoids re-parsing AND collapses the response shape. Closure: file-fingerprint pre-pass on extension allowlist (`.css` / `.js` / `.svg` / `.woff2`) emits one canonical entry per distinct hash with the path list. Per AI-first doctrine "Composite headline counts are dishonest" — extension: composite emission counts.
- [ ] **Q15-REFERENCE-GUIDE-FIX-DESCRIPTIONS-PER-FINDING-DUPLICATION** `scan_file` on an HTML with `limit: 5` returns `referenceGuide.fixDescriptions["aria/tab-controls-missing"]` containing 22 keyed entries, every one a near-identical 320-char string differing only in the panel-id token. The reference-guide shape ships duplicates per-finding when one canonical description with a `${panelId}` placeholder would suffice. Closure: dedupe `referenceGuide.fixDescriptions` to one canonical description per `(ruleId, fix-shape)` pair, with template-placeholder substitution materialized at read time, OR only ship descriptions for findings actually returned in the page. Pairs with the existing token-reduction proposal "Hoist `referenceGuide.fixDescriptions` to per-ruleId once per response." Per AI-first doctrine "Composite headline counts are dishonest" — extension: per-finding fix-description duplication.
- [ ] **Q15-CHECKLIST-PROMPT-TEXT-CONCATENATED-ACROSS-CRITERIA** A single review candidate ships its `reason` as a `" | "`-joined concatenation across 4 distinct WCAG criteria (`1.2.1`, `1.2.8`, `1.2.9`, `1.4.7`) into one string. Per-criterion dismissal is ambiguous — the agent reading the joined string cannot dismiss one criterion without dismissing the union. Closure: split into N separate candidates (one per criterion), OR ship `reason` as `{ criterionId: text }` map so the agent can address one criterion at a time. Per AI-first doctrine "Composite headline counts are dishonest" — extension: composite reason text.

---

## Track Q16 — Multi-corpus AI-first sweep (2026-05-05)

Second multi-corpus AI-first sweep on four shape-distinct corpora (CSS-framework distribution with documentation site; static-site-generator with mixed Liquid/ERB templates; vanilla-stack catalog of ~50 sibling demo subdirectories; vendor-template bulk catalog of ~100 sibling subdirectories with extensive duplicated vendor JS/CSS). 54 raw findings → 22 after cross-corpus dedupe → 18 actionable after filtering against existing Track Q15 items.

- [ ] **Q16-WARNINGS-DETAILS-EMPTY-PAYLOAD-MULTI-CODE** Multiple warning codes ship empty `warningsDetails.<code>: {}` payloads — observed across all 4 corpora on `partial_parse_files_present`, `baseline_dry_run` (bootstrap), `dist_only_scan_detected` (scan_file on a vendor stylesheet), and `foreign_ecosystem_detected` (propose_config — additionally a colon-suffixed dynamic-value-in-warning-code key violation). Each ships `{}` while the warning name implies actionable specifics; sibling codes like `parse_errors_present` populate full payloads in the same response. The bug is structural, not per-code. Closure: structural assertion at response-assembly that every code in `warnings[]` either has a non-empty `warningsDetails` entry or is dropped. Rename `foreign_ecosystem_detected:<value>` to a static code with `warningsDetails.foreign_ecosystem_detected: { ecosystem, evidence[], hasPackageJson }`. Ship payloads for `baseline_dry_run` (`{didWrite, wouldHaveAdded}`) and `dist_only_scan_detected` (`{classifierReason, fileList[]}`). Pin via fixture `tests/fixtures/real-world/warningsdetails-empty-payload/`. Per AI-first doctrine "Empty `warningsDetails.<code>: {}` is dishonest."
- [ ] **Q16-PARTIAL-PARSE-FILES-PRESENT-EMPTY-PAYLOAD** `warningsDetails.partial_parse_files_present` ships as empty `{}` on every project-rooted surface (scan_project, checklist, coverage, bootstrap) across 3 of 4 corpora — while sibling code `parse_errors_present` populates `partialParseFileCount` / `partialParseByParser` / per-file detail in the same response. The data exists; it just isn't routed to the matching warning's payload. Specific instance of Q16-WARNINGS-DETAILS-EMPTY-PAYLOAD-MULTI-CODE; closes when that closes, but split out for fixture-first traceability. Closure: populate `partial_parse_files_present` payload from the same source `parse_errors_present` consumes, OR consolidate both into one `parse_errors_present` payload with a per-file `kind: "partial"|"error"` discriminator and drop the duplicate code from `warnings[]`. Per AI-first doctrine "Empty `warningsDetails.<code>: {}` is dishonest."
- [ ] **Q16-MANUAL-CANDIDATE-COUNTER-CROSS-SURFACE-DRIFT** Same `actionable.candidates` / `actionableManualItems` field name ships from scan_project, checklist, and coverage on identical cwd with denominators that differ by up to 187x — observed across all 4 corpora. Drift magnitudes: 11.2x (444 vs 4990), 13.8x (11 vs 152), 3x (102 vs 303), 187x (141 vs 26448). The three surfaces measure three different slices (criterion-count vs per-criterion grounded vs per-emission) under one field name; `actionable.criteria` agrees across surfaces but `actionable.candidates` does not. Closure: compute manual-candidate counts once in a shared scan-confidence helper; rename so each slice is explicit (`manualCandidatesGroundedByCriterion`, `manualCandidateEmissionsTotal`, `manualCandidatesReturnedAfterClip`); pin equality on the shared concept across surfaces with an integration test. Where surfaces legitimately measure different slices, the field name must reflect the asymmetry. Per AI-first doctrine "Cross-surface count invariant" + "Sibling fields naming the same concept must use one shape."
- [ ] **Q16-SCOPE-WARNING-SET-DRIFTS-ACROSS-PROJECT-ROOTED-SURFACES** Corpus-level scope warnings ship from scan_project but vanish on sibling project-rooted tools (checklist / coverage / propose_config) on identical cwd — observed across 3 of 4 corpora. Codes that drift: `truncated_files_dropped`, `response_token_budget_truncated`, `bulk_catalog_detected`, `js_innerhtml_template_literal_unparsed`, `dynamic_content_container_detected`, `linked_stylesheet_external_cdn_skipped`, `scanned_build_artifacts_present`, `source_language_unsupported`, plus dynamic warning payloads like `jsx_code_demo_prop_parsed_as_live_dom`. propose_config ships zero warnings/warningsDetails on a corpus where scan_project flagged 9 distinct concerns. An agent calling checklist or propose_config first has no scope-confidence telemetry that bulk-vendor or template-island conditions exist. Closure: single shared scan-confidence helper consumed by every project-rooted tool (scan_project, checklist, coverage, propose_config, bootstrap). On identical cwd the helper emits the same scope-level warning set across all four; surface-specific truncation codes are the only legitimate divergence. Add an integration test pinning `set(scan_project.warnings) - {surface-specific truncation codes} ⊆ checklist.warnings ∩ coverage.warnings ∩ propose_config.warnings`. Per AI-first doctrine "Per-tool lane and warning-set classification must agree" + "Cross-surface count invariant" — extension to scan-confidence telemetry.
- [ ] **Q16-TRUNCATION-REPORTER-CODE-DRIFT-AND-OVERLAP** Truncation events fire through different codes per surface and overlap within one response — observed across all 4 corpora. Two distinct shape problems: (a) per-surface code drift — scan_project uses `response_token_budget_truncated` + `truncated_files_dropped`, checklist uses `results_truncated_use_nextcursor`, coverage uses `response_dropped_files_oversize`. Identical conceptual event, three distinct names. (b) within-one-response overlap — `response_meta_truncated.fields` and `response_dropped_files_oversize.metaFieldsDropped` both enumerate dropped meta fields with non-aligned counts (e.g. 2 vs 12 entries, partial overlap on `analysisCoverage` / `scannedBuildArtifacts`). Plus per-warning inline `shownCount`/`totalCount` pairs duplicated by `response_dropped_files_oversize.slimTruncations`. Three reporters describing one truncation pass with no canonical channel. Subsumes per-surface name-retention regressions: empty `checklist.items: []` / `coverage.manualWithCandidates: []` under truncation indistinguishable from "no items needed"; `checklist.summary.actionable.criteria 17` while `items[].length: 16` with no sentinel for the dropped 17th. Closure: pick one canonical truncation reporter per scope (meta-only / files-array / per-warning slim). Other reporters reference via `seeAlso` rather than independently enumerate. On cross-surface code drift, every project-rooted tool emits the same canonical truncation-reporter set when underlying conditions hold. Add an integration test on a forced-truncation fixture: every truncation event reaches the agent through one named channel. Per AI-first doctrine "Truncation reporters must reconcile across warnings" + "Truncated containers must rename or sentinel."
- [ ] **Q16-FINDINGS-BY-RULE-PATH-NULL** `findings_by_rule({ruleId})` returns N findings with `path: null` on every entry — verified across multiple rule probes on a CSS-framework documentation corpus (focus/outline-visible 7, aria/dropdown-toggle-triple-aria-missing 142, semantics/duplicate-landmark-unlabeled 12, navigation/href-empty-fragment 1284). Line and snippet are populated; only `path` is stripped. The tool is unusable for routing — agent has line+snippet but no file to read, no address for `suggest_fix`, no anchor for source-level pragma. Closure: populate `path` on every finding emitted by `findings_by_rule`. Pin with fixture `tests/fixtures/real-world/findings-by-rule-null-path/` asserting non-null path resolving to a real file. Per AI-first doctrine "Per-finding identifiers must be addressable, not collision-prone."
- [ ] **Q16-SEVERITY-CONTRADICTS-FRAGMENT-OR-WRAPPER-HEDGE** Document-shaped rules ship `severity: warning|error` on partial-parse / fragment / wrapper-class files where the rule's own reason concedes the predicate may not hold — observed across multiple corpora. (a) `semantics/landmark-main` fires at `severity: warning, confidence: medium` on layout-wrapper / template-partial files (no `<body>`, frontmatter fence, slot/yield directives detected) with `couldBeWrongBecause: ["partial_or_layout_file_requires_composed_check"]` and reason 'Verify against the parent layout or partial chain'. 35 emissions on one corpus, 1 on another. (b) `navigation/href-empty-fragment` fires 44 times at `severity: error, confidence: high` with hedge text 'inside wrapper class=highlight — likely demonstration code, verify the anchor renders for real users before treating as broken' on a documentation corpus. (c) `document/page-titled` ships `severity: error, confidence: low` on a partial-parse file — severity and confidence already disagree on the same finding. (d) wcag22:2.2.2 carousel candidates ship `priority: high` while reasons concede 'pause control may live in sibling file' (extends the same shape to candidate-priority channel). (e) `color/meaning-by-color-only` ships severity:warning on `.carousel-item.active` despite `couldBeWrongBecause` hedge that state_class may have aria sibling. Closure: couple severity to confidence (when `confidence: low`, severity must be info or finding moves to review-candidate). When `couldBeWrongBecause` includes a fragment/composition/demonstration token, downgrade to `severity: warning, confidence: medium` and route to review-candidate, OR downgrade further to `severity: info, confidence: low`. Pin via fixture `tests/fixtures/real-world/landmark-main-on-fragment/`: scan_file on a fragment-classified fixture must not emit document-shaped rules at `severity: error|warning`. Per AI-first doctrine "Reason text and severity must agree" + "Reason / priority / fix-description must agree across all three channels."
- [ ] **Q16-CORPUS-WARNING-NOT-PROPAGATED-TO-PER-FINDING-CONFIDENCE** Per-finding `confidence` ignores corpus-level warnings naming the same files — observed across 2 corpora. (a) On a documentation corpus paired with code-demo MDX: scan_project ships `jsx_code_demo_prop_parsed_as_live_dom` with per-file `matchCount` (e.g. dropdowns.mdx: 28, navbar.mdx: 26, 72 files total). 142 emissions of `aria/dropdown-toggle-triple-aria-missing` and 1284 of `navigation/href-empty-fragment` ship at `severity: warning, confidence: medium, couldBeWrongBecause: null` against those same files. The corpus-level warning says the rule's evidence model is degraded on these files; per-finding emissions don't propagate. (b) Same pattern on `keyboard/handler-missing` where `cross_file_listener_resolution_not_attempted_by_rule` is in `couldBeWrongBecause` but `fix.description` carries 'Cross-file check: grep the selector in your HTML to confirm' at `severity: warning, confidence: medium` — fix-description hedge contradicts severity. (c) Mirror case: `semantics/empty-heading` and `navigation/link-descriptive-text` fire as severity:warning on stripped Liquid `{{ }}` token content (provably can't determine the predicate from the source). Closure: plumb corpus-level warning file lists into per-finding emission — when `finding.path` is in the warning's file list, downgrade `confidence` and append the warning code to `couldBeWrongBecause`. Pin: scan a fixture file that triggers a corpus-level warning; assert findings on that file carry the limitation token in `couldBeWrongBecause`. Per AI-first doctrine "Per-finding confidence must reflect per-rule coverage limitations."
- [ ] **Q16-NEXTSTEP-FIRST-BY-FILENAME-EVEN-AFTER-MITIGATION** `nextStep` on truncated/bulk responses still routes by lexical order, not impact — observed across 3 of 4 corpora. (a) On a documentation corpus, `effectiveLimit:1` reduces page from 25 to 1 file; nextStep recommends the alphabetically-first file (a doc/test fixture), not the highest-firing one. (b) On a vanilla-stack catalog with topRules[0]=keyboard/handler-missing topFile=drawing-app/script.js (43 fires), nextStep redirects from `_project_starter_/index.html` (alphabetical first) to `3d-boxes-background/script.js` (alphabetical second), not to the topFile. The first-by-filename mitigation moved one slot down the alphabetical axis instead of switching to impact ranking. (c) On a template-website bulk catalog, nextStep routes to a template-subdir file the bootstrap suggestedConfig simultaneously recommends excluding. Closure: sort `files[]` by descending finding count (or topRule density) before clipping when `effectiveLimit < requestedLimit`. nextStep must route to `topRules[0].topFile` outside `scannedBuildArtifacts` AND outside any path the bootstrap output recommends excluding. Pin: under truncation/bulk-catalog conditions, `nextStepStructured.path === topRules[0].topFile` and that file is not in `scannedBuildArtifacts` or the bootstrap exclude list. Per AI-first doctrine "NextStep prioritization on truncated/bulk responses must avoid first-by-filename routing."
- [ ] **Q16-PROPOSE-CONFIG-NEXTSTEP-DOES-NOT-NARROW** `checklist` and `coverage` both ship `nextStepStructured.tool: "propose_config", args: {}` on truncated bulk-vendor or source-volume corpora — observed across 3 of 4 corpora. propose_config's actual output: (a) on a vanilla-stack catalog ships `suggestedConfig` with three commented `rules:` lines and zero `exclude:` block; (b) on a static-site-generator corpus, `suggestedConfig.exclude` lists 2 already-classified `.min.js` files while truncation was source-lane; (c) on a template-website corpus, `args:{}` means 'rerun on the same cwd', no scope reduction. Following nextStep does not narrow the input set; the agent re-runs scan_project and re-trips truncation. Closure: when checklist/coverage truncate AND `bulk_catalog_detected` or source-lane volume drives truncation, route nextStepStructured to `scan_project({restrictToPaths:[<topDirectory not in suggestedExcludes>]})` directly — skip the propose_config detour. When propose_config IS the right step, pass explicit narrowing args, never `args:{}`. Pin: every cross-tool nextStepStructured.tool recommendation, traced one hop on the same cwd, must reduce input scope or rule set. Per AI-first doctrine "NextStep handoffs must terminate at a narrowing tool, never form a cycle between transport-failing siblings."
- [ ] **Q16-PROPOSE-CONFIG-EXCLUDE-MISSING-FROM-CLASSIFIER** `propose_config` does not consume `default_excluded_artifact_paths` or `bulk_catalog_detected.suggestedExcludes` from the shared classifier — observed across 2 corpora. (a) checklist+coverage nextStep prose says 'Call propose_config to emit an exclude block from the build-artifact classifier'; propose_config ships zero exclude entries despite `default_excluded_artifact_paths` populated and 115 binary asset files already classified. (b) On a template-website bulk catalog, `warningsDetails.bulk_catalog_detected.suggestedExcludes` is 5 globs that would collapse 600+ vendor copies; propose_config.suggestedConfig.exclude ships 954 individual file paths reachable via the same 5 globs. Two emitter paths disagree on dedup shape. Closure: propose_config consumes the same shared classifier output as the warning emitters. When `default_excluded_artifact_paths` or `bulk_catalog_detected.suggestedExcludes` are populated, the exclude block in suggestedConfig contains those globs by construction. Pin: when scan_project ships either signal, propose_config.suggestedConfig.exclude contains the same glob set (paths only when no glob collapses). Pairs with Q15-VENDOR-FILE-SHA-FINGERPRINT-DEDUP-ACROSS-TEMPLATE-SUBDIRS. Per AI-first doctrine "Bootstrap output must be paste-safe" + "Sibling fields naming the same concept must use one shape."
- [ ] **Q16-FINDINGID-COLLISION-WITHIN-CHECKLIST-RESPONSE** `checklist.items[*].candidates[*].findingId` ships duplicate ids across distinct emissions with different reasons — 7 ids appear twice within a single checklist response on a vanilla-stack catalog. Same `(rule, file, line, column)` but two distinct `reason` strings (e.g. one id appears for one html:line with two reasons: an auto-advance signal and a three-question verification). Calling `suggest_fix(findingId)` on a colliding id resolves ambiguously; suppressing on the id silences a sibling reason the agent never read. Closure: include the reason-discriminator (or per-emission counter) in the `findingId` hash so each emission gets a unique id, OR collapse the two reasons into one finding with combined reason text. Pin via fixture `tests/fixtures/real-world/findingid-collision-checklist/`: every `findingId` in a single response is unique; per-call surface addresses one emission per id. Per AI-first doctrine "Per-finding identifiers must be addressable, not collision-prone."
- [ ] **Q16-SCAN-FILE-NULL-SCALAR-FIELDS-AMBIGUOUS-EMPTY** `scan_file` ships `totalFindings: null`, `truncated: null`, `warnings: null`, `warningsDetails: null`, `findingsArrayDropped: null` on multiple files — null doubles as 'no findings' and 'not-applicable'; agent cannot disambiguate clean-scan from dropped-data. Observed: on a clean-shaped layout file, `totalFindings: null, truncated: null, findingsArrayDropped: null` while findings array contains 1 entry; on an SCSS partial, `totalFindings: null, warnings: null, warningsDetails: null` with empty findings array; on a heavy MDX, `totalFindings: 264, truncated: true` (fields populated). Closure: scan_file always populates `totalFindings: number` (0 on clean), `truncated: boolean` (false on clean), `findingsArrayDropped: boolean`, `warnings: array` (empty when none), `warningsDetails: object` (empty object only when warnings is empty). Use conditional spreads only for fields with no signal applicable. Pin: scan_file on a clean fixture returns deterministic non-null primitives. Per AI-first doctrine "Ambiguous field shapes are dishonest."
- [ ] **Q16-PROJECT-VS-FILE-FINDING-COUNT-DRIFT-SAME-PATH** `scan_project.plan.findingsByFile[entry].count` disagrees with `scan_file({path: same file}).totalFindings` on identical path — observed on an MDX-heavy documentation corpus: 252 vs 264 (~5% drift). Different rule-evaluation set: scan_file's `perRuleCoverage.withEligibleInputs` evaluates more rules in single-file mode, producing emissions invisible to the project-pass evaluation. The same conceptual counter ships from two surfaces with no rename. Closure: either align rule-evaluation set between scan_project per-file walking and scan_file (single-file with full rule pass), or rename the per-pass count to reflect the slice difference (`countInProjectPass` vs `countInFilePass`). Pin: pick a fixture file; assert scan_project.plan.findingsByFile[i].count === scan_file({path: that file}).totalFindings — or assert each of the renamed pair against its measurement. Per AI-first doctrine "Cross-surface count invariant."
- [ ] **Q16-SUGGEST-FIX-CONFIDENCE-DRIFTS-FROM-FINDING-CONFIDENCE** `suggest_fix` per-call shape ships `confidence` and `kind` that disagree with the source finding's labels — observed across 2 corpora. (a) On a documentation corpus: scan_project finding ships `severity: info, confidence: low`; suggest_fix({findingId}) on the same id ships `kind: guidance, primary.confidence: medium`. (b) On a template-website corpus: scan_project.plan.topRules[0].fixClass = 'verify-in-source' for `navigation/href-empty-fragment`; suggest_fix on a finding from that rule returns `kind: 'guidance'` — agent budgeting against `plan.fixesByClass.verifyInSource: 19212` expects per-call shape to match. Closure: suggest_fix carries forward the original finding's `confidence` rather than recomputing. Either re-tag rule's `fixClass` to match the per-call kind it actually emits, or have suggest_fix emit the kind that matches the rule's plan-tally lane. Pin: any findingId emitted by scan_project, when passed to suggest_fix, has `primary.confidence === scan_project finding.confidence` AND the per-call kind partitions into the same `plan.fixesByClass` lane the rule's `fixClass` advertises. Per AI-first doctrine "Per-call shape must agree with per-class plan tally" + "Per-tool review-candidate shape must agree across surfaces."
- [ ] **Q16-MARKDOWN-EXTENSION-ROUTING-INCOMPLETE** `meta.analysisCoverage.parseModeByExtension` ships `.mkdn: "html"` while `.md` and `.markdown` are `markdown-html-residue` — observed on a static-site-generator corpus. `.mkdn` is a markdown extension; treating it as raw HTML means frontmatter and ATX headings are not stripped, so `semantics/heading-hierarchy` will misfire on `.mkdn` while it correctly downgrades on `.md`/`.markdown`. Closure: add `.mkdn` to the markdown-html-residue routing branch alongside `.md` and `.markdown`; emit a `markdown_extension_route_drift` warning if any future markdown-style extension is unrouted. Pin via fixture `tests/fixtures/real-world/markdown-mkdn-extension/`: `.mkdn` parses to markdown-html-residue, ATX residue does not produce a heading-hierarchy emission. Per AI-first doctrine "Routing skips that drop content are the symmetric twin of suppression."
- [ ] **Q16-TEXT-SOURCE-SKIPPED-LUMPS-PARSER-ROUTABLE-WITH-DATA** `warningsDetails.text_source_skipped.perExtensionCounts` lumps parser-routable extensions with config/data files under one bucket — observed on a static-site-generator corpus where `.feature: 28` (Cucumber/Gherkin specs containing user-facing UI text), `.coffee`, `.rmd` (parser-routable in principle), source-language sources (`.rb: 160`), and config/data (`.yml`/`.toml`/`.csv`) all surface under one warning code. The agent has no per-bucket lever for re-routing parser-routable extensions vs accepting that data files are skipped. Same skipped-extension regression named in doctrine but recurring at the data-source level. Closure: split `text_source_skipped` into 3 codes by predicate: `source_language_unsupported` (already exists, deepens existing usage), `config_or_data_files_skipped` (.yml, .toml, .json, .csv), `parser_routable_extensions_skipped` (.coffee, .rmd, .feature) — the last subset is the agent's lever for re-routing. Per AI-first doctrine "Labeled buckets are suppression too" + "Skipped-extension warnings are split by predicate."
- [ ] **Q16-PLAN-NOTES-SCALAR-UNDOCUMENTED-AND-DISCONNECTED** `scan_project.plan.notes: 117` ships as a top-level scalar with no schema-side meaning — observed on a static-site-generator corpus. fixesByClass totals (mechanical:0, guidance:31, runtimeOnly:2, verifyInSource:166, suppressRecommended:49) sum to 248 (matches violationsByScanKind.source). 117 corresponds to no derivable subset (also not guidance+verifyInSource+runtimeOnly = 199). The agent budgeting against `notes` cannot reconcile it against per-class tallies. Closure: rename `notes` to a self-documenting key tied to one of the existing classes, or drop the field entirely — agents already get the breakdown in `fixesByClass`. Per AI-first doctrine "Composite headline counts are dishonest" + "Sibling fields naming the same concept must use one shape."

---

## Track V — v1.0.0 readiness

- [~] **V1-CHANGELOG-V1** Draft `## [1.0.0]` entry landed (1d784b7). Date placeholder `YYYY-MM-DD` stays until user says ship. Sections populated: Breaking Changes (exit-code freeze), Added (13 sub-groups), Changed (9 items), Deprecated (2 aliases), Fixed (19 items), Deferred (the four ADR 0018 items). New empty `## [Unreleased]` sits at top. Ready for `/release 1.0.0` when the user triggers.
